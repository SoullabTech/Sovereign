# Arm log — A/B run of 2026-08-24

Per-arm witness records. Companion to `RUN_SHEET_2026-08-24.md` (task-set identity
`bfc0d6bfe6aec3604637b84cf118ca0e2205d6c0a5c64858e39325cf285b4c3c`), which is **not**
edited by this file — the frozen task texts keep their identity.

## The bookend invariant

Experimental configuration must remain invariant across the **entire** arm, not merely be
correct at the instant it begins.

Plugin installation and marketplace configuration are **user-scoped**: any other Claude Code
session can change them mid-arm. On 2026-08-24 the `soullab` marketplace was silently
repointed by another lane between one observation and the next, and the plugin itself became
uninstalled by an unwitnessed route. A preflight check alone would not have caught either.

So an arm is valid only if the state matches **before and after** the session:

```
BEFORE                                    AFTER
candidate checkout   dfbdef18d...         still dfbdef18d...
checkout             detached             still detached
tree                 clean                still clean
soullab-jarvis       (arm-defined state)  unchanged
marketplace source   /Users/soullab/jarvis-bench-dfbdef18   unchanged
model / CLI          recorded             unchanged
```

**If any bookend differs, VOID the arm.** Do not reason about whether the change mattered —
that reasoning is exactly the inference the instrument's guards exist to refuse.

### Operational requirement

No other Claude Code session may install, uninstall, enable, disable, add, remove or repoint
the `soullab` marketplace or plugin while an arm is running. Sessions that are genuinely
read-only with respect to plugin configuration may stay open. If that cannot be guaranteed,
close them for the duration of the arm.

### Witness block — run before AND after each arm

```bash
W=/Users/soullab/jarvis-bench-dfbdef18
echo "sha        : $(git -C $W rev-parse HEAD)"
echo "tree       : $(git -C $W rev-parse HEAD^{tree})"
echo "detached   : $(git -C $W symbolic-ref -q HEAD >/dev/null 2>&1 && echo NO || echo YES)"
echo "clean      : $([ "$(git -C $W status --porcelain | wc -l | tr -d ' ')" = 0 ] && echo YES || echo NO)"
echo "plugin     : $(claude plugin list 2>&1 | grep -c soullab-jarvis | sed 's/^0$/ABSENT/;s/^[1-9].*/PRESENT/')"
echo "registry   : $(grep -c soullab-jarvis ~/.claude/plugins/installed_plugins.json | sed 's/^0$/ABSENT/;s/^[1-9].*/PRESENT/')"
echo "marketplace: $(claude plugin marketplace list 2>&1 | grep -A1 'soullab$' | tail -1 | sed 's/.*Directory (//;s/)//')"
echo "cli        : $(claude --version)"
```

### Canonical task text — use `emit-task.py`, do not transcribe by eye

The run sheet stores each task as a wrapped blockquote. Transcribing it by hand can hand
arm A and arm B two different strings, which `PROTOCOL.md` forbids. Emit it instead:

```bash
python3 /Users/soullab/jarvis-adapter-bench/plugins/soullab-jarvis/benchmark/emit-task.py T1 | pbcopy
```

Record the emitted sha256 per arm and confirm the two arms of a pair match.

| task | canonical sha256 |
|---|---|
| T1 | `8067ad43977a969da783e4311475f2508fe447e96b66c94106afb046bbe58c67` |
| T2 | `d3f57bd3b97a52c4eeee8726abd107e16bd39d2256b1847f9bf433a620034372` |
| T3 | `9004956162b359b06a8def75f29f23ef9ebd6213e6c05cb5d3569eb21e54ae81` |

---

## Arm A / T1 — PREFLIGHT (recorded 2026-08-24T17:24:59Z)

| field | value |
|---|---|
| checkout SHA | `dfbdef18d361d3939c652435bd387d2c38508567` |
| tree | `0746b8c1e83872bf5658cdd5c2f4b66e3b6c96bc` |
| detached | YES |
| tree clean | YES |
| plugin in `claude plugin list` | **ABSENT** |
| entry in `installed_plugins.json` | **ABSENT** |
| marketplace source | `/Users/soullab/jarvis-bench-dfbdef18` |
| Claude Code | `2.1.241` |
| model | `claude-opus-5[1m]` |

Route by which the plugin became absent: **UNKNOWN**. Recorded, not repaired — the arm A
precondition is a property of current state, and manufacturing an uninstall we could witness
would not make that state more true. Unknown history is not contamination of a verified
current state.

Known inert residue, deliberately left in place: `~/.claude/plugins/cache/soullab/`
(extracted files; nothing registers them as installed) and a `pluginUsage` telemetry key in
`~/.claude.json`. Neither is an install state.

## Arm A / T1 — POSTFLIGHT (recorded 2026-08-24)

**RULING: ADMISSIBLE.** All eight bookend lines match preflight exactly.

| field | value |
|---|---|
| plugin in list | **ABSENT** (matches preflight) |
| registry entry | **ABSENT** (matches preflight) |
| marketplace source | `/Users/soullab/jarvis-bench-dfbdef18` (unchanged) |
| checkout SHA | `dfbdef18d361d3939c652435bd387d2c38508567` (unchanged) |
| tree | `0746b8c1e83872bf5658cdd5c2f4b66e3b6c96bc` (unchanged) |
| detached / clean | YES / YES |
| Claude Code / model | `2.1.241` / `claude-opus-5[1m]` |
| transcript | `5f60651c-f2c2-4ed4-886d-c7f1c0b4ea60.jsonl` |
| first / last timestamp | `2026-08-24T18:07:24.912Z` / `2026-08-24T18:10:55.285Z` |
| task outcome | COMPLETED — answer produced. Correctness scoring deferred to the pair per `PROTOCOL.md` §Correctness. |

### Arm-validity checks

- **Frozen** — size and mtime unchanged over a 12s probe (220,306 bytes).
  An earlier 3s probe read 209,711 bytes and *also* looked stable; the file then grew by
  ~10.6 KB. A short stability probe is not proof a session closed, and an idle open session
  is indistinguishable from a closed one by size alone. This is the bookend principle
  applying to the transcript itself.
- **Ran** — 26 assistant turns across 111 records. Not an empty arm.
- **Not self-measuring** — 0 tool_use inputs reference `measure-session.py`. A first,
  over-broad check searched whole records and reported a hit; that hit was in a *tool
  result* (a directory listing containing the benchmark folder) and is incidental. The
  instrument's criterion is the tool_use input, and by it this arm is clean. Recorded
  because the over-broad check would have voided an admissible arm.
- **Received the frozen task** — sha256 of the arm's first user prompt equals the canonical
  T1 emitted by `emit-task.py`:
  `8067ad43977a969da783e4311475f2508fe447e96b66c94106afb046bbe58c67`. Worth having: the
  clipboard was observed carrying the wrong content twice before launch, and no downstream
  guard detects an arm that answered a different question.

## Arm B / T1 — PREFLIGHT (recorded 2026-08-24T18:15:39Z)

Every line matches Arm A except plugin state — the single treatment variable.

| field | Arm A | Arm B |
|---|---|---|
| checkout SHA | `dfbdef18d…` | `dfbdef18d…` |
| tree | `0746b8c1…` | `0746b8c1…` |
| detached / clean | YES / YES | YES / YES |
| marketplace source | `/Users/soullab/jarvis-bench-dfbdef18` | same |
| Claude Code | `2.1.241` | `2.1.241` |
| model | `claude-opus-5[1m]` | `claude-opus-5[1m]` |
| **plugin** | **ABSENT** | **PRESENT** ← treatment |

### Installed-artifact digest — which plugin bytes Arm B actually used

The candidate is installed from a **mutable local directory**, so the worktree could move
while the arm runs. Version alone does not identify the bytes. Recorded before the session
and re-verified after:

| | |
|---|---|
| version | `0.1.0` |
| install path | `~/.claude/plugins/cache/soullab/soullab-jarvis/0.1.0` |
| artifact digest | `62398ae7cfbb8ac4e54c3f45ccbacd8ff70c0fe385cec5d18ca47dae94ec06fe` |
| frozen source digest | `62398ae7cfbb8ac4e54c3f45ccbacd8ff70c0fe385cec5d18ca47dae94ec06fe` |
| identical | YES — 21 files, `diff -r` clean |
| hooks installed | `hooks.json` `image-tools.txt` `pretooluse-guard.py` `session-start.sh` `stop-close-out.py` |

Digest recipe (stable ordering, content + relative paths):

```bash
P=~/.claude/plugins/cache/soullab/soullab-jarvis/0.1.0
find "$P" -type f ! -path '*/.git/*' -print0 | sort -z | xargs -0 shasum -a 256 \
  | sed "s|$P/||" | shasum -a 256 | cut -d' ' -f1
```

## Arm B / T1 — POSTFLIGHT

*To be completed after the session is fully exited.*

Required, all four: non-plugin bookends unchanged · artifact digest still
`62398ae7…` · zero `measure-session.py` hits in tool_use inputs · first-user-prompt sha256
equals `8067ad43977a969da783e4311475f2508fe447e96b66c94106afb046bbe58c67`.
Prove closure with the long size+mtime freeze, not a short probe.

Do not measure Arm A until Arm B for the same task is also complete and closed, and measure
from a third session that is neither arm.

## Arm A / T1 — `promptSource` discrepancy RESOLVED (2026-08-24, read-only)

Appended, not edited: the Arm A adjudication above stands unchanged. Raised because
Arm A's transcript carries `promptSource: ['queued','typed']` while the launch-parameter
table records `typed`. Left unresolved, the arms could have differed in delivery
mechanics and we would only have found out after the differential.

Arm A contains exactly **two** genuine user text messages (non-meta, non-`tool_result`):

| idx | promptSource | len | timestamp | content |
|---|---|---|---|---|
| 8 | `typed` | 230 | `18:07:24.923Z` | **T1** — sha256 `8067ad43…`, canonical |
| 104 | `queued` | 2727 | `18:10:52.610Z` | `T1 — ACCEPTED` — operator acceptance verdict |

```
T1 actual user deliveries : 1     (idx 8)
typed delivery present    : YES
second semantic delivery  : NO
```

**Meaning of `queued`:** transport, not task. The operator typed the acceptance while the
assistant was still generating, so Claude Code held it to the next turn boundary and
stamped it `queued`. It carries no task: it opens `T1 — ACCEPTED`, reviews the arm's
answer, and closes `T1 passes. No correction and no code change needed.` After it the arm
produced **1 assistant turn and 0 tool calls** before exiting 2.7s later.

**Arm A remains ADMISSIBLE.** The set `['queued','typed']` describes when two messages were
delivered relative to generation, not two deliveries of the task.

### Carried to the comparison, not to admissibility

The acceptance message is 2,727 characters of substantive content that entered Arm A's
context after the task was complete, plus the one assistant turn answering it. If Arm B
receives no equivalent tail, Arm A carries input and output that B does not. Measure the
arms over a matched window, or exclude the post-completion tail from both. This is a
measurement-comparability item; it does not bear on whether Arm A is admissible.

## Comparison window — PRE-REGISTERED 2026-08-24T21:5x Z, before Arm B exists

Fixed by founder ruling **before Arm B was launched** and therefore before any B
result could influence it. Recorded here so the pre-registration is checkable
rather than asserted: Arm B's record was captured at `21:54:56Z` and this section
is committed while `arm-B-T1.json` still reads `status: PREFLIGHT, session_id: null`.

```
ARM A WINDOW                    ARM B WINDOW
T1 typed delivery               T1 typed delivery
  -> task work                    -> task work
  -> final task response          -> final task response
STOP                            STOP
```

Everything after completion of the task response is **excluded from both arms**.

Reason: Arm A carries a post-completion tail — the operator's 2,727-character
`T1 — ACCEPTED` message (idx 104, `queued`) plus the one assistant turn answering
it. Counting it would inflate Arm A's input and output against a B that has no
equivalent. Arm B is being run under instruction to send **no** acceptance or
review message, so the tail will not exist on the B side at all; the window rule
makes the arms comparable either way.

This is a measurement rule, not an admissibility rule. Records outside the window
still count for arm validity — closure, contamination, cwd, session binding.
