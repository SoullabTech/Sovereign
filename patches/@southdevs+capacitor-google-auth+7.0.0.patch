diff --git a/node_modules/@southdevs/capacitor-google-auth/SouthdevsCapacitorGoogleAuth.podspec b/node_modules/@southdevs/capacitor-google-auth/SouthdevsCapacitorGoogleAuth.podspec
index 28d5e6d..6e90593 100644
--- a/node_modules/@southdevs/capacitor-google-auth/SouthdevsCapacitorGoogleAuth.podspec
+++ b/node_modules/@southdevs/capacitor-google-auth/SouthdevsCapacitorGoogleAuth.podspec
@@ -8,8 +8,8 @@
     s.author = 'SouthDevs'
     s.source = { :git => 'https://github.com/devatsouth/CapacitorGoogleAuth.git', :tag => s.version.to_s }
     s.source_files = 'ios/Plugin/**/*.{swift,h,m,c,cc,mm,cpp}'
-    s.ios.deployment_target  = '12.0'
+    s.ios.deployment_target  = '15.0'
     s.dependency 'Capacitor'
-    s.dependency 'GoogleSignIn', '~> 6.2.4'
+    s.dependency 'GoogleSignIn', '>= 7.1.0'
     s.static_framework = true
   end
diff --git a/node_modules/@southdevs/capacitor-google-auth/ios/Plugin/Plugin.swift b/node_modules/@southdevs/capacitor-google-auth/ios/Plugin/Plugin.swift
index 26d5407..8859cc5 100644
--- a/node_modules/@southdevs/capacitor-google-auth/ios/Plugin/Plugin.swift
+++ b/node_modules/@southdevs/capacitor-google-auth/ios/Plugin/Plugin.swift
@@ -5,12 +5,13 @@ import GoogleSignIn
 /**
  * Please read the Capacitor iOS Plugin Development Guide
  * here: https://capacitor.ionicframework.com/docs/plugins/ios
+ *
+ * Updated for GoogleSignIn 7.x API (privacy manifest compliant)
  */
 @objc(GoogleAuth)
 public class GoogleAuth: CAPPlugin {
     var signInCall: CAPPluginCall!
     var googleSignIn: GIDSignIn!;
-    var googleSignInConfiguration: GIDConfiguration!;
     var forceAuthCode: Bool = false;
     var additionalScopes: [String]!;
 
@@ -22,7 +23,9 @@ public class GoogleAuth: CAPPlugin {
 
         let serverClientId = getServerClientIdValue();
 
-        googleSignInConfiguration = GIDConfiguration.init(clientID: customClientId, serverClientID: serverClientId)
+        // GoogleSignIn 7.x: configuration is set on the singleton
+        let config = GIDConfiguration(clientID: customClientId, serverClientID: serverClientId)
+        googleSignIn.configuration = config
 
         // these are scopes granted by default by the signIn method
         let defaultGrantedScopes = ["email", "profile", "openid"];
@@ -72,12 +75,17 @@ public class GoogleAuth: CAPPlugin {
         signInCall = call;
         DispatchQueue.main.async {
             if self.googleSignIn.hasPreviousSignIn() && !self.forceAuthCode {
-                self.googleSignIn.restorePreviousSignIn() { user, error in
-                if let error = error {
-                    self.signInCall?.reject(error.localizedDescription);
-                    return;
-                }
-                self.resolveSignInCallWith(user: user!)
+                // GoogleSignIn 7.x: restorePreviousSignIn callback signature changed
+                self.googleSignIn.restorePreviousSignIn { user, error in
+                    if let error = error {
+                        self.signInCall?.reject(error.localizedDescription);
+                        return;
+                    }
+                    if let user = user {
+                        self.resolveSignInCallWith(user: user)
+                    } else {
+                        self.signInCall?.reject("No user returned from restore");
+                    }
                 }
             } else {
                 let presentingVc = self.bridge!.viewController!;
@@ -93,13 +101,19 @@ public class GoogleAuth: CAPPlugin {
                     NSLog("no server client id found in config")
                     return;
                 }
-                let googleSignInConfiguration = GIDConfiguration.init(clientID: clientId, serverClientID: serverClientId)
-                self.googleSignIn.signIn(with: googleSignInConfiguration, presenting: presentingVc, hint: nil, additionalScopes: additionalScopes) { user, error in
+                // GoogleSignIn 7.x: configuration is set on singleton, signIn method changed
+                let config = GIDConfiguration(clientID: clientId, serverClientID: serverClientId)
+                GIDSignIn.sharedInstance.configuration = config
+                GIDSignIn.sharedInstance.signIn(withPresenting: presentingVc, hint: nil, additionalScopes: additionalScopes) { result, error in
                     if let error = error {
                         self.signInCall?.reject(error.localizedDescription, "\(error._code)");
                         return;
                     }
-                    self.resolveSignInCallWith(user: user!);
+                    guard let user = result?.user else {
+                        self.signInCall?.reject("No user returned from sign in");
+                        return;
+                    }
+                    self.resolveSignInCallWith(user: user);
                 };
             }
         }
@@ -112,15 +126,21 @@ public class GoogleAuth: CAPPlugin {
                 call.reject("User not logged in.");
                 return
             }
-            self.googleSignIn.currentUser!.authentication.do { (authentication, error) in
-                guard let authentication = authentication else {
-                    call.reject(error?.localizedDescription ?? "Something went wrong.");
-                    return;
+            // GoogleSignIn 7.x: tokens are now properties on GIDGoogleUser, use refreshTokensIfNeeded
+            let user = self.googleSignIn.currentUser!
+            user.refreshTokensIfNeeded { refreshedUser, error in
+                if let error = error {
+                    call.reject(error.localizedDescription);
+                    return
+                }
+                guard let user = refreshedUser else {
+                    call.reject("Failed to refresh tokens");
+                    return
                 }
                 let authenticationData: [String: Any] = [
-                    "accessToken": authentication.accessToken,
-                    "idToken": authentication.idToken ?? NSNull(),
-                    "refreshToken": authentication.refreshToken
+                    "accessToken": user.accessToken.tokenString,
+                    "idToken": user.idToken?.tokenString ?? NSNull(),
+                    "refreshToken": user.refreshToken.tokenString
                 ]
                 call.resolve(authenticationData);
             }
@@ -174,11 +194,12 @@ public class GoogleAuth: CAPPlugin {
     }
 
     func resolveSignInCallWith(user: GIDGoogleUser) {
+        // GoogleSignIn 7.x: tokens are now direct properties on GIDGoogleUser
         var userData: [String: Any] = [
             "authentication": [
-                "accessToken": user.authentication.accessToken,
-                "idToken": user.authentication.idToken,
-                "refreshToken": user.authentication.refreshToken
+                "accessToken": user.accessToken.tokenString,
+                "idToken": user.idToken?.tokenString ?? NSNull(),
+                "refreshToken": user.refreshToken.tokenString
             ],
             "serverAuthCode": user.serverAuthCode ?? NSNull(),
             "email": user.profile?.email ?? NSNull(),
