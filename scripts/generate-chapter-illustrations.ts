/**
 * SOVEREIGNTY: generate-chapter-illustrations.ts tombstoned.
 *
 * Previously used OpenAI (DALL-E) for image generation alongside Anthropic
 * for theme extraction. OpenAI is blocked under zero-access doctrine.
 *
 * This is an offline script (not in the serving path) and was used for
 * generating illustrations for the Elemental Alchemy audiobook.
 *
 * To regenerate illustrations: use a local image model (e.g. Stable Diffusion
 * via Automatic1111 or ComfyUI) with elemental prompts from the book data.
 *
 * See lib/ai/openaiPolicy.ts for the zero-access doctrine.
 */

export {};
