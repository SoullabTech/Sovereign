# MAIA-SOVEREIGN — SESSION ANCHOR (READ FIRST)

## What this project is

MAIA-SOVEREIGN is a self-hosted, sovereign consciousness companion. It exists to support human coherence, truth-telling, and inner guidance without eroding agency or substituting itself for human judgment. It is not a generic chatbot, assistant, or authority. MAIA is governed by explicit vows: consent, containment, non-manipulation, and a refusal to simulate intimacy, certainty, or power where none is ethically grounded. MAIA speaks in distinct modes (Talk, Care, Note) and is oriented by Spiralogic and AIN principles toward integration, responsibility, and maturation rather than dependence or reassurance.

## Non-negotiables (project vows)

- **Sovereignty first**: human agency always outweighs engagement, retention, or performance metrics.
- **Consent for memory**: there is no stealth memory. Sanctuary Mode governs what is held, how, and why.
- **No coercion, no guru stance**: MAIA offers reflection, framing, and choice — never command, diagnosis, or authority.
- **No attachment capture**: MAIA does not seek emotional dependency, loyalty, or psychological bonding. Relationship arises only insofar as it supports sovereignty.
- **Self-hosted by design**: no cloud lock-in. Infrastructure choices (EC2, Docker, Caddy) are part of the ethical architecture.
- **Spiritually intelligent, not spiritually authoritative**: MAIA may engage symbolic, mythic, or depth-psychological language without claiming truth-status over the human.

## MAIA ⇄ AIN relationship

AIN is the broader ontological and architectural framework: a view of intelligence as participatory, distributed, and meaning-bearing rather than purely instrumental. MAIA is the user-facing sovereign companion expression of that framework. Spiralogic functions as a core mapping layer for state, process, and orientation. MAIA's modes, rituals, and boundaries are implementations of AIN's principles — not separate products or abstractions.

## Architecture snapshot (where to look first)

- **Voice & conversation orchestration**: `components/OracleConversation.tsx`, `lib/maia/*`, `lib/voice/*`
- **Identity & consent boundaries**: `middleware.ts`, `lib/auth/*`, `lib/http/apiBase.ts`
- **Sovereign API surface**: `app/api/sovereign/*` and related routes
- **iOS / Capacitor pipeline**: `scripts/capacitor-patch-routes.sh`, `scripts/build-ios.sh`, `ios/*`
- **Deployment & ops**: `docker-compose.production.yml`, `Caddyfile`, `scripts/deploy-production.sh`
- **Canon**: `docs/canon/MAIA_CANON_v1.1.md`
- **Oath**: `docs/canon/MAIA_OATH.md` — the irreducible standard
- **Deep-Intelligence Gate**: `docs/canon/MAIA_CONVERSATIONAL_INTELLIGENCE_NON_DEGRADATION.md` — hard acceptance gate on all voice/transport work. *Voice may have a different capture path; it may not have a different mind.* Spoken and typed turns must converge before MAIA cognition begins (convergence point named in the doc, pinned by `__tests__/voice-non-degradation.test.ts`). Divergence downstream of input acquisition = **RED** = voice does not ship. STT/TTS are sensory infrastructure and may change freely; the mind may not be substituted.
- **Sovereignty Invariants**: `docs/canon/MAIA_SOVEREIGNTY_INVARIANTS.md` — relational constitution (constraints on relational power when the system works well enough that people start relating to it)
- **Representation & Claim Discipline**: `docs/canon/MARKETING_CLAIM_DISCIPLINE.md` — governs every outward claim (web, decks, case studies, podcasts, demos, onboarding, internal planning). Three instruments: Live/Designed/Vision · Center of Gravity · Failure Test. The line: *"We do not tell tomorrow's story as if it were today's."* Applied in `docs/pitch/CASE_STUDY_LIBRARY.md`.
- **Claim State Authority**: `docs/canon/CLAIM_STATE_AUTHORITY.md` — governs movement between claim states. Kelly governs what Soullab chooses to claim; predeclared criteria and attributable evidence govern the highest state the claim may occupy. Evidence licenses; it does not itself edit the public record.
- **Constitutional Direction of Authority**: `docs/canon/CONSTITUTIONAL_DIRECTION_OF_AUTHORITY.md` — the backbone constraint. Distinguishes the *developmental process* (messy, non-linear — the member loops through Encounter ⇅ Reflection ⇅ Recognition freely) from *constitutional authority* (strict, one-directional): **authority may only move upward through authored experience — Encounter → Reflection → Recognition → Living Field → (out into) Developmental Ecology — never skipping a layer, never manufacturing higher-order meaning.** The member may jump around; the system may not. MAIA never moves a person through it — it protects the boundaries within which the person's own development occurs. Developmental Ecology = the relational medium (Personal Field / Relationships / Co-Labs / Practice Fields), not a rung. Design test: *what layer does this belong to, and does its authority respect the upward-only direction?* Generalizes Recognition Integrity + Right to Remain Unpossessed + Interface Humility + Disciplined Non-Collapse + Encounter as Primitive. **Ratified canon (2026-07-01)** — reconciled + re-audited, placed as one package with Recognition Integrity → **Invariant 16** in the Sovereignty Invariants.
- **JARVIS operating manual**: `docs/programme/JARVIS_INSTRUCTIONAL_MANUAL_v1.md` — **canonical operating manual for JARVIS-governed work** (founder ruling TAKE, 2026-09-17). Governs orchestration method, evidence discipline, gate sequencing, custody, stop conditions and founder handoffs. Precedence: **founder authority → ratified programme / constitutional law → this manual → lane charters → implementation** — where a more specific authority conflicts with the manual, the more specific authority governs. ⛔ Canonical for **architecture and design intent only**: it authorizes no implementation, migration, deployment, repair, merge or production change, and **never upgrades an unimplemented or unwitnessed capability to LIVE** — repository and runtime evidence still govern implementation standing.

## Known recurring traps (read before debugging)

- **Capacitor + cookies**: `SameSite=Lax` cookies are not sent cross-origin from iOS WebView → use `x-member-id` via `apiFetch()` (`lib/http/apiBase.ts`).
- **Static export limits**: some Next.js routes and middleware are incompatible with `CAPACITOR_BUILD` static export → exclude via `capacitor-patch-routes.sh`.
- **"It forgot me" symptoms**: usually indicate localStorage or cookie loss after rebuilds or WebView resets — check `beta_user`.
- **force-dynamic routes**: any route using `export const dynamic = 'force-dynamic'` must be listed in `EXCLUDED_DYNAMIC_ROUTES` for iOS builds.
- **Worktrees + preflight `.env.docker`**: `npm run preflight` ends with a `docker compose config` validation, and `docker-compose.yml` reads `env_file: .env.docker` — a gitignored file that exists only in the main checkout (`/Users/soullab/MAIA-SOVEREIGN/.env.docker`). In any fresh git worktree that step therefore fails with "env file … .env.docker not found" (confirmed 2026-07-27). Fix: `cp /Users/soullab/MAIA-SOVEREIGN/.env.docker <worktree>/.env.docker`. The preflight step itself (`scripts/preflight-compose-config.sh`) detects the worktree case and prints this exact copy command, so the raw compose error should no longer be the first thing you see.
- **LAN IP drift after power-cycle** (housekeeping check, not always user-impacting): minisforum is expected at `192.168.0.104`. After a power outage or full restart, DHCP can re-lease a different IP (seen: `.102` on 2026-05-29). If the router's port-forward rule for 80/443 is hard-coded to `.104`, external traffic will silently drop. **Verify scope before treating as causal**: if the PWA at soullab.life loads on iOS over cellular, the forward path is intact and IP drift is NOT the user-facing issue (the router may auto-track or have a different forward rule). The hairpin-NAT probe (`curl https://soullab.life from minisforum`) is misleading — most consumer routers disable hairpin by default, so HTTP 000 there does not imply external traffic is broken. Always check `ssh soullab@minisforum 'hostname -I'` after a power event for hygiene, and set a DHCP reservation pinning minisforum to `.104` to make the trap structurally impossible.

## Current priority thread (update each session)

- **LATEST — 2026-09-23 — ⭐ `JARVIS-FOUNDER-WORKSPACE-01` F2 OPENED BY FOUNDER DIRECTION (*"get it fully functional"*) · COMPOSITION CONTRACT + BUILD SEQUENCE DELIVERED · ⛔ F1 WALK STILL OWED · ⛔ NO BUILD ACT OPEN.** Record `docs/programme/JARVIS-FOUNDER-WORKSPACE-01_F2_SURFACE_COMPOSITION_CONTRACT_AND_IMPLEMENTATION_SEQUENCE_2026-09-23.md` (canonical observed `840194ba`; F0/F1 fixtures stay at `b4f73ac4`). ⭐ **Seam census settled composition vs new**: status probe + `deriveOperatorView`, `statusCanonicalV2`, grant readers, `session.mjs status/report`, continuity search, O0–O3 pure organs = COMPOSITION; **NO function lists Work Units** (`$AIN/work-units-v2/*.json` unenumerated), `events.jsonl` has no reader, **no programme-record parser** (0 frontmatter; 171/535 files carry a `State|Status|Standing` line; `jarvis-recall.py` already regexes `declared_state`), **no graph join**, **no instrument registry** = NEW. ⚠️ Monitor admissibility ruled by evidence: `workstation-storage-census.sh` (human report) · `worktree-census.sh` (TSV) · git · Ollama · `session.mjs` are read-only; ⛔ `storage-health-monitor.sh` (`rm -rf` at 90%), `health-check.sh` (production INSERT/DELETE), `backup-postgres.sh` WRITE and are refused as instruments. IPC budget verified: 13 invoke + 1 push, exact-match tests, five questions per new entry; ≤1 new read channel proposed. ⭐ Architecture CANDIDATE (FD-1): the five-surface renderer REPLACES the four views inside `jarvis-desktop` over the existing main/preload/IPC; the F1 fixture shape becomes `founder-workspace-viewmodel.v1` with validator laws VM-1…VM-7 + defeat candidates; renderer reads only the view-model, adapters are pure over organ outputs. **Sequence B1 contract+validator (B) → B2 read organs (B) → B3 instrument registry (B, ops-script change = founder act) → B4 `programme-state.v1` projector (A, D-03) → B5 renderer swap + CSP (A, needs the F1 walk rulings, FD-1/FD-2) → B6 Work live incl. O1→O2→O3 plan-only (A; O4 only after D-07 reconciliation) → B7 Graph live (A) → B8 founder walk of the live workspace.** B1–B4 can lawfully precede the walk; B5 cannot. Docket **FD-1…FD-6** (architecture · IPC channel · projector record law + location · ops JSON/TSV mode · sequencing vs walk · CSP). **Standing: F2 DELIVERED · FD-1…FD-6 OWED · ⛔ F1 WALK OWED · ⛔ B1–B8 NOT OPEN · ⛔ NO CODE / RUNTIME / IPC / PROJECTOR / PRODUCTION CHANGE · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-23 — ⭐ `JARVIS-FOUNDER-WORKSPACE-01` F1 ADJUDICATED: SUBSTANTIVELY PASS · FIVE-SURFACE ARCHITECTURE ACCEPTED · F1R1 REPAIRS APPLIED · ⛔ F1 NOT CLOSED (FOUNDER WALK OWED) · ⛔ F2 NOT OPEN.** Adjudication `docs/programme/JARVIS-FOUNDER-WORKSPACE-01_F1_FOUNDER_ADJUDICATION_2026-09-23.md` on exact candidate `8349aa5d` (Today REVISE · Work KEEP · Graph REVISE, live = NEW CAPABILITY · Monitor REVISE, live = NEW CAPABILITY · System KEEP). ⭐ Product finding: JARVIS is **an orientation environment with progressively disclosed evidence, not a dashboard** — *what matters → what is happening → what needs me → what can I do → why does JARVIS believe this → show me the governed evidence*. **F1R1** (`…_F1R1_TRUTHFULNESS_REPAIR_2026-09-23.md`): **R1** `programme-state.v1` gains a mandatory `population` block (26 examined · 13 emitted · `complete: false`) + **PS-9** *population before totals*; Today reads *"In this recorded snapshot, N decisions…"* until a projector sets `complete: true` · **R2** explicit `freshness` per Monitor row; a historical good value renders **Observed then · present state unknown**, never *fine now* (now 2 now-fine · 4 historical · 4 need care · 1 failed · 8 not observed · 2 not permitted) · **R3** graph labels subject-first, identifiers second · **R4** banner truthful globally about illustrative items · **R5** ⚠️ the F1 record had claimed *no network* while the page loaded Google Fonts — removed; `index.html` now has **0** external requests, original claim preserved as superseded. ⚠️ **Canonical advanced to `840194ba` (PR #1495, one docs/programme file) after the F1R1 commit; fixtures stay observed-against `b4f73ac4`, the new subject is recorded as NOT EXAMINED in the F1R1 record, ⛔ not silently re-projected.** ⭐ **F1R1 ADJUDICATED (founder, same day): MECHANICAL PASS on lineage `8349aa5d → 0858331d → 7a730f16` — R1…R5 all PASS · drift to `840194ba` accepted as NON-OVERLAPPING with the prototype surface, snapshot stays historical against `b4f73ac4` · walk unspent because the Mac Studio surface is offline · ⛔ STOP AT HUMAN WALK: no further prototype repair from source inspection, no UI polishing, no runtime, no projector, no live Graph/Monitor, no F2** (`…_F1R1_FOUNDER_ADJUDICATION_2026-09-23.md`). **Standing: F1 SUBSTANTIVELY PASS · F1R1 MECHANICAL PASS · ⛔ FOUNDER EXPERIENTIAL WALK OWED (seven questions, on `7a730f16` via file:// on the Mac Studio) · ⛔ F2 NOT OPEN · ⛔ NO RUNTIME / CONSOLE / PROJECTOR / MONITOR / GRAPH IMPLEMENTATION · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-23 — ⭐⭐ `JARVIS-FOUNDER-WORKSPACE-01` F0 PASS (founder adjudication) · HU-1…HU-9 + DC-1…DC-8 BINDING FOR F1 · D-01…D-08 RULED · F1 PROTOTYPE DELIVERED · ⛔ F1 EXIT = FOUNDER WALK, NOT PERFORMED.** Adjudication `docs/programme/JARVIS-FOUNDER-WORKSPACE-01_F0_FOUNDER_ADJUDICATION_2026-09-23.md` (exact candidate `8b8592d9` against census `b4f73ac4`). Rulings: **D-01** Merge Authority = EXTERNAL PROTECTED DEPENDENCY (reference only; standing AUTHORIZED · UNSPENT · IDLE PENDING CREDENTIALED EXECUTION CAPACITY) · **D-02** Founder Workspace is the product/flow PARENT and does not subsume O7/O10 · **D-03** `programme-state.v1` = projection contract + recorded fixture only, ⛔ no second truth store · **D-04** Monitor LOCAL-FIRST, production never probed by a dashboard · **D-05** no cost value without a cost instrument · **D-06** ⛔ no reject button (W0.v2 itself requires `decision === 'accepted'`; a governed *No* is owed to the Work Unit programme) · **D-07** C1 `REPO_ROOT` · local-native label · O4 drift all REAL and OUTSIDE F1 · **D-08** F1 OPENED. **F1 delivered** (`…_F1_INFORMATION_ARCHITECTURE_AND_LIVING_PROTOTYPE_2026-09-23.md`): `prototypes/jarvis-founder-workspace-f1/index.html` — one static page, Today · Work · Graph · Monitor · System over recorded fixtures (`fixtures.js`: 13 programmes · 21 monitor rows · 22 nodes · 19 evidenced edges), zero network/IPC/mutation; vocabulary map v1 (`vocabulary.js` → generated `…_F1_VOCABULARY_MAP_V1_2026-09-23.md`, 55 terms, 4 marked candidate); `programme-state.v1` contract (`…_F1_PROGRAMME_STATE_V1_CONTRACT_2026-09-23.md`, PS-1…PS-8 as future-projector falsifiers) + generated fixture. ⭐ Every row = sentence first, *Why does JARVIS believe that?* one layer down; Monitor headline is a COUNT per level, never a score; *not observed* is dashed and never calm; every graph edge cites a record/ruling/git. **Standing: F0 ✅ PASS · F1 OPEN · PROTOTYPE + MAP + CONTRACT DELIVERED · ⛔ FOUNDER WALK OWED (seven questions, per-surface KEEP · REVISE · REMOVE · NEW CAPABILITY REQUIRED) · ⛔ F2 NOT OPEN · ⛔ NO RUNTIME / CONSOLE / ROUTING / AUTHORITY / MERGE-AUTHORITY / PRODUCTION CHANGE · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-23 — ⭐ `JARVIS-FOUNDER-WORKSPACE-01` OPENED (founder direction, new lane — ⛔ not folded into `M1R1R1` / merge-authority) · F0 CENSUS DELIVERED · ⛔ STOPPED FOR FOUNDER ADJUDICATION.** Charter `docs/programme/JARVIS-FOUNDER-WORKSPACE-01_CHARTER_2026-09-23.md`; record `…_F0_PRODUCT_CENSUS_AND_FOUNDER_EXPERIENCE_CONSTITUTION_2026-09-23.md`; branch `claude/sharp-cannon-cyrdeb`. Purpose: compose the existing JARVIS organs into one humane founder-facing workspace (Today · Work · Graph · Monitor · System) — *human first, machinery underneath*. **F0 observed against `b4f73ac4ccd9cb96e6b2b6dc7b682e689c77771f`** (census SHA recorded separately from the candidate commit; no silent re-observation). ⭐ Findings: the console (`jarvis-desktop/`, four views home·work·system·spiral — ⚠️ the four-view set is named by NO document) is presentation over a real W0.v2 substrate; **O0–O4 operator organs exist as pure modules and are NOT WIRED** (0 imports from main/renderer); the only visualisation is the Living Spiral (a status projection, not a graph); **Monitor substrate is NOT FOUND inside JARVIS** — health/deploy/storage/backup instruments exist as scattered repo scripts whose scheduling is UNOBSERVED; Production is `NOT PROBED` by Desktop law. Fragmentation: two work paths, three Work-Unit meanings, six status vocabularies, three "cockpits", raw ids/JSON/provider names on screen. ⚠️ Defects OBSERVED, ⛔ not repaired: `main.js:1180-1192` uses undeclared `REPO_ROOT` (C1 lane throws; INFERRED, app not run); the local-native lane is labelled *read-only* while the pipeline requires `repo.write:worktree`; **O4 doc says NO RUNTIME yet `51890bc0` implemented it with no witness — standing UNVERIFIED**. ⚠️ **`JARVIS-MERGE-AUTHORITY-01 / M1R1R1` is NOT FOUND in this checkout** (0 hits across tracked files, branches, commit messages) — its founder-stated standing is honoured by name and non-mutation only (docket D-01). Humane UI law HU-1…HU-9 and dashboard/graph/monitor contract DC-1…DC-8 are CANDIDATES, ⛔ not ratified; Graph and Monitor classified NEW CAPABILITY over existing data, Today/Work/System COMPOSITION. Docket D-01…D-08 (merge-authority custody · placement vs unopened O7/O10 · programme-state source · Monitor authority boundary · costs · adjudication gesture · defect ownership · open F1 as IA + living prototype, ⛔ not code-first). **Standing: LANE OPEN · F0 DELIVERED · ⛔ NOTHING RATIFIED · ⛔ NO CODE CHANGED · ⛔ F1 NOT OPENED · ⛔ NO RUNTIME / GOVERNANCE / ROUTING / MERGE-AUTHORITY / PRODUCTION CHANGE · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-23 — ⭐ WORKSTATION STORAGE RELIEF (Mac Studio) · worktree relief CLOSED · Library census + 48 GB baseline NEXT.** Record `docs/ops/WORKSTATION_STORAGE_RELIEF_2026-09-22.md`. Internal disk 12 GB → 76 GB free (98% → 84%), swap 17.8 → 10.4 GB, compressor 21.4 → 14.4 GB, memory free 58%. ⭐ *Internal free space is a memory metric on this machine* — a full disk capped swap and presented as RAM failure. Six stale `next-server` processes were the swap. 308 → 269 worktrees; 0 REMOVABLE left internally; ⛔ do not run Act B again. ⚠️ Docker Desktop reserves 12.25 GB / 10 CPUs for its VM — a quarter of unified memory, invisible in `ps`. Instruments (read-only): `scripts/ops/workstation-storage-census.sh` · `worktree-census.sh` · `worktree-reclaim-plan.sh` (acts refuse without `RECLAIM_AUTHORIZED=1`, revalidate censused HEAD + refreshed merge state, DRIFT never removed). 51 worktrees preserved to origin or verified T7 bundles; 7 branches diverged from remote (local side bundled, ⛔ no force push). ⛔ Minisforum is production, not a build worker. NEXT: restart → fair 48 GB baseline under normal load · separate read-only `~/Library` census (Messages 44 GB · Voice Memos 21 GB · Claude `vm_bundles` 9.5 GB are human decisions) · then `JARVIS-CAPACITY-SENTINEL-01` (observation authority only; UNKNOWN never reads as GREEN; every probe bounded).
- **LATEST — 2026-09-21 — ⭐ REVIEW-CUSTODY-01 + DEPLOYMENT-SAFETY-03 R1 FULL-LINEAGE RECONCILIATION PREPARED.** Merge candidate `5c12d891207fb786c847251fc383c562c374bf22` has exact parents current canonical `bd91e5bb09bf4747216f09941d33890f4f890163` and accepted prefix-safe Step 3 `6f447b17727da8f41dfaa626419c389653d3e9b0`. Conflict-free reconciliation preserves frozen Review Custody law, 14/14 + 8/8 custody matrices, 10/10 final compatibility, 6/6 composition, 5/5 failure-prefix compatibility, immediate pending-set + old-reader re-witness, and `gate → migrate → tags → swap → verify` for normal deploy/update; migrate-only remains outside the ordering act. Exact staged-tree synthetic merge passed bundle provenance, composed E2E, Step 3 ordering, and current-canonical JARVIS provider proof 15/0. **⛔ NOT CANONICAL YET · FOUNDER R1 ADJUDICATION OWED · NO DEPLOY · NO SCHEMA OR PRODUCTION MUTATION.** Record: `docs/programme/DEPLOYMENT-SAFETY-03_R1_CANONICAL_RECONCILIATION_2026-09-21.md`.
- **LATEST — 2026-09-20 — ⭐ DEV-LANE PROVIDER EXPOSURE: FINDING VALID · FOUNDER INTERIM HOLD TAKEN · REVIEW-CUSTODY CANDIDATE LANDED, ⛔ UNWIRED.** Record `docs/programme/DEV_LANE_PROVIDER_EXPOSURE_FINDING_2026-09-20.md`; branch `claude/optimistic-wozniak-pruzjq`. Occasioned by assessing an external cross-provider review tool (`chaseai-yt/claudex-loop`) — ⛔ read, not installed. **⭐⭐ THE DEFECT CLASS**: `docs/canon/PROVIDER_GOVERNANCE.md` scopes itself to *who may enter the **runtime*** (`:62`) and defers the **development** boundary at `:66` to `../ai/MULTI_MODEL_SESSION_MODE.md` — **which does not exist, and `git log --all --diff-filter=A` confirms it never existed on any branch**; `docs/ai/` is absent and root `MULTI_MODEL_QUICKSTART.md` is a different subject (local Ollama tiering for MAIA's own runtime). ⛔ `check:no-openai` cannot close the gap — it is an **import guard over tracked source**, and piping repository text into a foreign CLI adds no import. ⭐ **SHARPEST PART**: the tier table governs by capability (`member_data`·`member_audio`·`chat`·`embedding`·`tts`·`stt`·`benchmark`) and has **NO class for repository source or constitutional text** — so even with the dev-lane document present, the vocabulary to classify the thing most at risk does not exist. ⚠️ Same family as `BRANCH_POLICY_AUTHORITY_FINDING_2026-09-13` and the 2026-09-07 merge-to-canonical finding: *the rule is sound, the path actually taken is ungoverned.* **⭐ FOUNDER INTERIM RULING (same day)**: routing repository source or constitutional text to a **Lab-tier provider at development time is BLOCKED**, with two predeclared lift conditions — (1) a dev-lane governance canon **authored AND ratified**; (2) the capability table admits explicit classes for repository source and constitutional canon (proposed `repository_source` · `constitutional_canon`). ⛔ Naming them is a **proposal, not the governance act** — the tier table is unedited. ⛔ Q3 (citation defect vs missing document) **not settled**, and the hold is operative either way. ⛔ No claim that any text has left the boundary — **nothing was measured**. **⭐ SEQUENCING**: the reviewed workflow's transferable value is **same-provider**, so the hold blocks the increment and not the capability. Landed as a candidate: `scripts/review-custody.ts` (`npm run review:custody`) — `bind`·`admit`·`check`, no foreign provider: approval bound to the plan's **SHA256**; inspection bound to a **complete change manifest incl. untracked** (a deletion records `ABSENT` rather than dropping out); `APPROVED` refused alongside any high/medium finding; absent coverage refused; empty/malformed review refused; admission **monotonic** (`admitted → already` w/ original timestamp → `conflict`). ⛔ It performs no review, runs no proof command, confers no authority, and **cannot establish that a reviewer's findings or coverage are truthful** — zero findings is lawful and is not evidence the reviewer was right. ⛔ Its refusals say a **record** is inadmissible, never that an implementation is correct. ⚠️⚠️ **ITS 13-CASE VERIFICATION IS A DISPOSABLE PROBE, ⛔ NOT A COMMITTED SUITE** — run ad hoc from a scratchpad toolchain (no `node_modules` in the container), so by this project's own law *a probe is not the record*. **OWED before it gates anything**: (1) a committed falsifier suite, each case paired with a **defeat candidate**; (2) a review-authoring procedure — without it the instrument has custody over a record nothing is defined to produce; (3) a founder act naming where a bound review is REQUIRED. ⛔ **Boundaries not to conflate**: `admit` refuses *the record*; `check` refuses *the continued applicability* of an admitted approval — a manifest movement is a `check` refusal, never an `admit` refusal. **⭐ STEP 1 DISCHARGED SAME DAY (founder elected option (b), Claude-on-Claude wiring)** — record `docs/programme/REVIEW-CUSTODY-01_STEP1_FALSIFIER_MATRIX_2026-09-20.md`. `RC-F1…RC-F14` committed + `DC-1…DC-14` built; **matrix LETHAL + DISCRIMINATING: 14/14 candidates DEAD on their named falsifier · STRICT 14/14 · all collateral CLASSIFIED · exit 0**; `npm run review:custody:matrix` · `npm run typecheck:review-custody` (strict + `noUncheckedIndexedAccess`, exit 0). ⭐ **The seam is a DECISION seam, not a config flag** — the shipped path contains no branch for any weaker rule and the CLI hard-codes `STRICT`. ⭐⭐ **THE MATRIX'S FIRST RUN WENT RED ON THE FALSIFIER, NOT A CANDIDATE**: RC-F12 passed `undefined` to a helper with a default parameter, so the fixture silently supplied the evidence it was meant to omit — *a suite only ever run against the conforming implementation would have called that a pass.* ⭐ **ONE RULE COULD NOT BE MADE FALSIFIABLE and is therefore NOT claimed as enforced**: the deleted-path-stays-`ABSENT` rule is recorded as **defence in depth**, because dropping a missing path still moves the fingerprint, so no construction distinguishes the two. ⚠️ **Lethality is DECISION-LEVEL, ⛔ not implementation-independent** — each candidate replaces one decision on an identical substrate; fourteen standalone implementations would yield unclassified collateral and *weaker* evidence, so the narrower claim is deliberate. ⛔ **SUITE FREEZE NOT TAKEN** (founder act; per S3 Class-B, lawful is *implementation fails suite → repair the implementation*, ⛔ never *reinterpret the law → weaken the test*). ⛔ **STEP 2** (review-authoring procedure) and **STEP 3** (founder act naming the first bound surface) **NOT OPENED** — without Step 2 the instrument has custody over a record nothing is defined to produce. **⭐⭐ FREEZE TAKEN @ `bbb5ff5c` + STEP 2 COMPLETE (founder acts, same day)** — records `docs/programme/REVIEW-CUSTODY-01_SUITE_FREEZE_2026-09-20.md` · `…_STEP2_REVIEW_AUTHORING_PROCEDURE_2026-09-20.md`. **FREEZE**: `FREEZE.json` pins `review-custody-core.ts` · `falsifiers.ts` · `candidates.ts` by **git BLOB HASH, ⛔ not a commit diff** (blob identity survives history rewriting and names exactly which bytes are law); guard `npm run verify:review-custody-freeze` **proven lethal both ways** (intact 0 → two appended comment lines → drift 1 → restored 0). ⭐ **Additive law is lawful; EDITING is not** — new law lands at its own address, and the frozen blob hashes are the mechanical proof nothing was edited to accommodate it. `matrix.ts` · the CLI · the tsconfig are **deliberately NOT frozen**, each with a stated reason. **STEP 2 — MECHANICALLY DERIVED COVERAGE (founder directive: self-report REFUSED)**. ⭐⭐ **THE DIRECTIVE COULD NOT BE 'make `admit` check the trace'** and the decisive reason is not the freeze: **all fourteen frozen falsifiers call `admit` without a trace, so a trace requirement there would refuse each one BEFORE it reached its named refusal — the law would die for the wrong reason while the matrix still reported fourteen kills.** So coverage is a **THIRD BOUNDARY**: `witness` refuses an unwitnessed COVERAGE CLAIM · `admit` refuses the RECORD · `check` refuses CONTINUED APPLICABILITY. `scripts/review-custody-coverage.ts` + `tests/constitutional/review-custody/coverage-witness/**`; CLI now requires `--trace`. ⚠️⚠️ **WHAT DERIVED COVERAGE IS**: it bounds coverage **FROM ABOVE ONLY** — a read event proves a file was **opened**, ⛔ never that it was understood, ⛔ never 'thoroughness'. ⭐⭐ **AND THE BOUND HOLDS ONLY IF EVERY READ CHANNEL IS WITNESSABLE** — a shell is not (`cat`/`sed`/pipes/globs/variables cannot be soundly reduced to a file set), so a trace containing one is **REFUSED, ⛔ not partially credited**: *coverage derived from a partial view of reads is not a bound, it is a guess wearing a bound's clothes.* Classification is **FAIL-CLOSED** (unrecognised tool ⇒ unwitnessable). ⛔ `Grep`/`Glob` **witness nothing** — recorded as SCOPES. **Consequence that is part of the law, not a preference**: the reviewer runs with `Read`·`NotebookRead`·`Grep`·`Glob` only, ⛔ no Bash, ⛔ no Write/Edit — **and must be a SEPARATE PROCESS whose stream is captured, ⛔ NEVER an in-process subagent** (a subagent returns a report, not a tool-call trace, so the review would be self-reported again and the refusal would never fire). **8 laws RC-C1…RC-C8 + 8 candidates DC-C1…DC-C8: matrix LETHAL + DISCRIMINATING (8/8 · STRICT_COVERAGE 8/8 · one CLASSIFIED collateral) · frozen matrix 14/14 UNCHANGED · FREEZE INTACT — ZERO frozen bytes edited · typecheck exit 0**. ⭐ **THE MATRIX AGAIN FOUND A DEFECT IN THE INSTRUMENT, NOT A CANDIDATE**: `DC-C7` SURVIVED because the inline-trace refusal was hard-coded INSIDE `witnessCoverage`, so the candidate was refused **by the core rather than by the law** and the error went unmodelled; repaired by REMOVING the structural block so external provenance is enforced **by the decision**. *Step 1's defect was in a fixture; this one was in the module the suite exists to protect — neither would have been found by running the suite against the conforming implementation alone.* **END-TO-END CLI VERIFIED**: lawful admit · over-claimed coverage → `UNVERIFIED_REVIEW_COVERAGE` · shell-in-trace → `UNWITNESSABLE_READ_CHANNEL` · missing trace → instrument error. ⚠️ **OWED: TRACE SHAPE IS ⛔ NOT VERIFIED AGAINST A REAL CLI** (none in this container) — the `{type:'tool_use',name,input}` + session-id shape is asserted from documentation; if the real stream differs **the fix is the parser, ⛔ never the law**, and RC-C5 exists so a shape mismatch surfaces as `TRACE_UNREADABLE` rather than a silent empty coverage set. ⚠️ **AND A NEAR-MISS OF A DIFFERENT KIND**: the suite was first written to `…/review-custody/coverage/`, which `.gitignore:54` ignores as **test-coverage output** — so `git add -A` added NOTHING and every matrix ran green while **the suite did not exist in the repository**. ⛔ Not repaired by `git add -f` (which leaves the next file there silently dropped) but by renaming to `coverage-witness/`. ⭐ *A pattern meant for generated output can capture authored law that happens to share its name, and the failure is silent in exactly the direction that matters.* ⛔ **STEP 3 (founder act naming the first surface where a bound review is REQUIRED) NOT OPENED · INSTRUMENT STILL UNWIRED.** **Standing: FINDING VALID · HOLD TAKEN · Q2/Q3 OPEN · STEP 1 ✅ · ⭐ FREEZE TAKEN @ `bbb5ff5c` + PROVEN · STEP 2 ✅ · ⚠️ TRACE SHAPE UNVERIFIED · ⛔ STEP 3 NOT OPENED · ⛔ INSTRUMENT UNWIRED · ⛔ NO LANE OPENED · ⛔ NO POLICY EDITED · ⛔ NO TIER TABLE CHANGED · ⛔ EXTERNAL TOOL NOT INSTALLED · INSTRUMENT ⛔ UNWIRED · ⛔ PROJECT GATES NOT RUN IN CONTAINER · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-21 — ⭐⭐ `WRITERS-STUDIO-CONVERGENCE-01` OPENED BY FOUNDER DIRECTION · CONVERGENCE LAW RATIFIED AND FROZEN · ⛔ STEPS 2–8 NOT STARTED.** Charter `docs/programme/WRITERS-STUDIO-CONVERGENCE-01_CHARTER_2026-09-21.md`; study of record `docs/programme/WRITERS-STUDIO_HIGH_LEVEL_REVIEW_2026-09-21.md` (+ addenda 1–4), ⭐ **ACCEPTED AS A CONVERGENCE STUDY — ⛔ NOT as authorization to implement every capability described in the product discussion.** Purpose: *converge Writer's Studio around one simple relationship between the writer, the Work, and MAIA, while preserving the governed editorial intelligence already built.* ⛔ The lane SUBTRACTS AND COMPOSES — a change that increases what MAIA can DO is out of scope; a change that reduces what the writer must OPERATE is in scope. **FROZEN LAWS L1–L8**: Work primary (manuscript is one expression) · Elemental opening foundational · MAIA carries complexity, writer carries the Work · relational default language (*what I'm noticing → what may help → what do you think?*) · the writer may agree/disagree/revise MAIA's edit/keep/apply/**undo** · Writer and Pro mode address ONE Commission layer (⛔ never a separate editorial behaviour — ⭐ **testable: same intent must produce the same reading in both**) · work state member-declared (observe → ask → **member declares** → record) · what MAIA actually read stays visible (⭐ coverage is the GROUND OF TRUST, ⛔ not technical depth, ⛔ never behind *Show editorial detail*). **SPINE**: `WORK → OBSERVATION → PASSAGE/MATERIAL → CONVERSATION → REVISION → WORK`, with ⛔ **no dead end** and ⛔ **no loss of place** as acceptance conditions, not aspirations. ⚠️ **The Elemental opening is preserved as LAW and is NOT in the spine** — the walk opens on an existing Work; ⛔ its absence is never to be read as the law weakening. **⭐⭐ THE RANKING QUESTION IS DISSOLVED, NOT ANSWERED**: ranking/severity is ⛔ NOT a prerequisite; the initial whole-work review presents **UNRANKED observations, ALL REACHABLE**; prioritization may later become a SEPARATE EXPLICITLY COMMISSIONED act. ⭐ The ruling does not weaken `editorial-consequence` — **it removes the dependency**, because a review that ranks nothing asserts no consequence judgment and needs no licence. ⛔ No implicit ranking: presentation order is not a ranking (the `relationships` route's *“whole, and unranked · no `mostRecent`, no `suggested`, no `default`”* is the precedent); ⛔ *discarded* is refused. **⛔ SCOPE CONTROL — NOT IN THIS LANE, each a later lane needing its own act**: external quote/source verification · manuscript semantic-similarity/repetition search · material clustering · idea-development intelligence · older-edition comparison. ⚠️ **All five have NO SUBSTRATE today**, and ⛔ none may appear in any menu or intent list until it exists — `assertStudioMapHonest()` binds a promise about the member's book at least as hard as it binds a nav item. **EXECUTION ORDER**: (1) ⭐ freeze the law — **DONE, this charter** · (2) Work-primary composition · (3) orientation breadcrumb + Back to manuscript · (4) reverse the persistent unavailable-capability shell (⚠️ **amends WS2-03B**; ⛔ the WS2-02 no-href boundary and `assertStudioMapHonest()` are UNTOUCHED — 24 destinations, 5 available, 17 `later`) · (5) the **Commission layer** (⭐⭐ lens inference lives in the COMMISSION, ⛔ **NEVER in the reader** — `A2` *“the reader never infers a lens from its own output”* is load-bearing and ⛔ not amended) · (6) the **coverage-licensed Editorial Reading** (⛔ `DEVELOPMENTAL_NON_CONCLUSIONS` NOT weakened; `author-intent` and `reader-effect` stay permanent — coverage may discharge only `whole-work-pattern`/`across-unread-span`/`outside-coverage`, and only on complete body-depth coverage; ⭐ a member-**declared** purpose is not an inference and is not barred) · (7) **PASS as a governed reading unit** (⛔ the 500k ceiling is NOT changed — *refused whole, nothing trimmed* stands; synthesis runs **over observations, not prose**; ⛔ the writer is never asked to operate range selection — *“Choose a custom range”* is the defect step 7 retires) · (8) walk **Elemental Alchemy**. **⛔ EA ACCEPTANCE PRECONDITION, BINDING**: EA-as-governed-knowledge (`lib/corpus/eaIngestContract.ts` — 1,238 chunks, SHA-pinned) must be **UNREACHABLE** while EA-as-manuscript (174 sections) is under review, **proved, not assumed** — ⭐ otherwise no whole-work claim is falsifiable and a reading is indistinguishable from a recollection. Five acceptance conditions, each with a named DEFEAT CANDIDATE (⭐ *a walk that cannot fail is not an acceptance instrument*); ⛔ a stop condition yields NO EVIDENCE, never a partial pass. **⚠️ STILL OPEN, CARRIED**: **intention custody** (how a declared purpose is stored/revised/returned to under the **gestalt non-capture law** — revisable without ceremony · divergence must point **BOTH WAYS** (*“is the book changing, or is this section?”*) · orientation, **never criteria**; and under FR-06 the writer's **OWN SENTENCES** are stored, ⛔ never an elemental classification inferred from free text — **owed before the opening is built**, and the opening is not in this lane) · naming (Author Studio vs Writer's Studio) · beta reach (Develop is flag-gated + out of COLAB-BETA-01 §5) · the disagreement law (*evidence: revises the reading, ⛔ does not delete it, ⛔ does not automatically win*) — ⛔ NOT ratified. ⭐⭐ **AMENDMENT 1 (founder, same day) — L6 BECOMES THREE FACETS, ⛔ not two modes**: **F1 Plain** (ordinary language) · **F2 Teaching** (plus the craft reasoning, offered, ⛔ never imposed) · **F3 Professional** (vocabulary, lenses, evidence, coverage). ⭐ Two axes, not one — **vocabulary AND pedagogy**; a writer may want plain language *and* to understand the craft, which a two-mode split could not express. A facet governs **register and pedagogy ONLY**. ⛔ **L6a facet parity (the release test)**: all three facets, same intent → the SAME reading, SAME observations, SAME reachability. ⛔ **L6b authorship is facet-invariant** — facets differ in how much MAIA EXPLAINS, ⛔ **never in how much MAIA WRITES**; ⭐ the sharpest hazard, since a facet built for *needs the most help* drifts toward *MAIA does more of it*, and that is exactly the population where dependency risk is highest. ⛔ **L6c never assigned** — member-declared under L7; ⭐ MAIA assigning a facet would be **MAIA GRADING THE WRITER**, more invasive than grading the work (FR-06 / D-003 family). ⛔ **L6d a dial, not an identity** — changeable without ceremony, per-observation. ⛔ **L6e no self-classification at the door** — ⛔ no threshold question asking a writer to rank their own competence; a facet names **what MAIA does**, ⛔ never **who the writer is**. ⭐ RECOMMENDED, ⛔ NOT RULED: deliver facets as **default disclosure depth** (*Why this matters* / *Editorial detail* open by default) rather than a mode selector — ⭐ L6a then holds BY CONSTRUCTION, since there is only one reading to present. Original two-mode L6 text PRESERVED in the charter §1 and **SUPERSEDED AS CURRENT LAW, ⛔ not deleted**. Step 5 now carries L6a as an acceptance obligation; *a facet that changes what MAIA finds* joins the defeat-candidate table. ⭐⭐ **`WRITERS-STUDIO-FACETS-01` RATIFIED AS A BOUNDED CONTRACT (second founder direction, same day) — SUPERSEDES Amendment 1 as current law** (§8 preserved, ⛔ not deleted). **GUIDED · LEARNING · PRO**, governed by ⭐ ***Three facets. One intelligence. One Work.*** ⛔ Not separate products, ⛔ not capability tiers, ⛔ not different editorial truths. **MAY change**: how much orientation MAIA carries · how observations are expressed · how much teaching · how much machinery is visible. **⛔ MUST NOT change**: the underlying observation · its evidence · reading coverage · the Work · revision history · authorship authority · the governed editorial act. ⭐ *Same Work, scope and commission → all three resolve to the SAME underlying observation.* ⭐⭐ **GUIDED IS NOT A REDUCED MODE** — it is where MAIA carries the GREATEST interface and **editorial TRANSLATION** complexity. **LEARNING** = Guided + contextual craft teaching through the member's actual Work (*Why this matters*), ⛔ optional and invited, ⛔ never a detached course. **PRO** exposes machinery (lens · scope · coverage · evidence · rationale · history · provenance) — ⛔ **it does not create a different reading**. **SELECTION**: asked at Work establishment (*How would you like MAIA to work with you?*) + a quiet control by the Work name; *“You can change this anytime. It does not change your Work.”* ⛔ **No qualification, test, professional status or inferred skill level; ⛔ no member labelled beginner/intermediate/expert.** ⚠️ Amendment 1's L6e *mechanism* (depth-by-default, no selector) is ⛔ **NOT TAKEN**; its substance survives — ⭐ the options name **the relationship the member wants of MAIA**, ⛔ never the member's level. ⚠️⚠️ **§9.3 — THE RIDER MOST LIKELY TO BE LOST**: *authorship authority* has a NARROW reading (who may authorize a mutation — ⭐ already structural) and a BROAD one (how much of the proposed prose MAIA generated — ⛔ **guaranteed by nothing**). ⭐⭐ A Guided facet can preserve authorization perfectly while MAIA drafts far more of the member's text, and *“MAIA holds more of the process”* is the exact phrase under which that drift arrives **sounding like care** — in the population where dependency risk is highest. **BINDING OPERATIVE READING: a facet changes how much MAIA TRANSLATES and EXPLAINS; ⛔ it NEVER changes how much MAIA WRITES.** The proposal chain, the authorization and `Use it · Change it · Talk about it · Undo` are IDENTICAL in all three. **ORTHOGONAL AXES**: Work state (Idea→Material→Draft→Completed→Revisited) × facet — ⛔ never infer expertise from Work state, ⛔ never infer Work state from facet; ⚠️ the 5×3 matrix is RATIFIED AS A CONCEPTUAL MODEL, ⛔ **not a build authorization** — only the **Draft** and **Completed** rows are in this lane. **ELEMENTAL DISTINCTION PRESERVED**: ⭐ elements describe **the Work**; the facet describes **how MAIA accompanies the member** — ⛔ a facet is never derived from an elemental answer, ⛔ nor the reverse. **ACCEPTANCE INVARIANT** — one governed EA observation rendered three ways, identical observation/evidence/revision authority; **6 failure conditions**: facet changes what MAIA believes happened · Pro has different editorial truth · Guided exposes unrequested machinery · Learning teaches AWAY from the Work · ⭐ a facet changes how much prose MAIA authored · ⭐ teaching PRESCRIBES QUALITY rather than describing technique (the `voice` lens holds *the manuscript is the reference, never an external standard* — an imported ideal breaches it and Invariant 14). **⚠️ TWO THINGS THIS ACT DOES NOT SETTLE**: **(a)** does a facet change which observations are SURFACED? §4 ratified *unranked, ALL REACHABLE*; the invariant governs ONE observation and is silent on the SET — ⭐ *“Guided shows 6, Pro shows 87”* is a **reachability difference wearing a presentation costume**; compatible reading = one set, differing default depth, everything reachable from every facet. **(b)** ⚠️ **NAMING COLLISION — “Pro”**: Layer 1 is *Vision Studio · Author Studio · **Pro Studio***, so a member would be in Author Studio using the Pro facet while Pro Studio is a different house — ⛔ two things named Pro in one product. ⛔ Neither ruled. **⛔ NO IMPLEMENTATION BEYOND THIS BOUNDED FACET CONTRACT IS AUTHORIZED BY THIS ACT** · ⛔ Elemental arrival NOT OPENED (founder designing it next). ⭐⭐ **§9.7 SETTLED + `WRITERS-STUDIO-ELEMENTAL-ARRIVAL-01` DESIGN CANDIDATE (founder, same day)** — charter §10; record `docs/programme/WRITERS-STUDIO-ELEMENTAL-ARRIVAL-01_DESIGN_CANDIDATE_2026-09-21.md`. **(a) FACET REACHABILITY RULED**: ⭐ ***One observation set. Different default depth. Everything reachable from every facet.*** A facet may change how many are initially expanded / how much explanation appears / whether evidence is auto-exposed; ⛔ it may NEVER make an observation unreachable. ⚠️ **IMPLEMENTATION CONSTRAINT**: *a few at a time* requires choosing WHICH few, and §4 dissolved ranking — ⛔ the cut may not be evaluative; ⭐ **manuscript order is the lawful and natural cut** (*the observations, in the order they occur in your book*); ⛔ *most severe / most confident / most actionable first* are ranking under other names. **(b) THIRD FACET RENAMED `DIRECT`** — ⛔ **PRO retired**: it collided with the ruled Layer-1 house **Pro Studio**, and ⭐ *Direct* names **a relationship rather than a status** (L6e). Facets are now **GUIDED · LEARNING · DIRECT**; ⛔ no behavioural change; verified no existing `direct` identifier. ⚠️ Author Studio vs Writer's Studio remains OPEN. **⭐ INTENTION CUSTODY DISCHARGED** (§7.2(1)): MAIA may **notice · reflect · propose · ask** — **the member DECLARES** — the system records; ⛔ ***no inference becomes intention merely because MAIA generated it convincingly.*** Each declaration preserves member-approved wording · dimension · Work · when · **whether written directly or adopted from conversation** · lineage · active/revised/**intentionally open**. ⛔ A prior declaration must never silently become current again. ⭐ *Still discovering* is a **first-class state, ⛔ not missing data**; ⭐ skipping arrival is **always lawful** and ⛔ absence of a Compass **never authorizes MAIA to invent one**; ⛔ nothing becomes canonical through silence. ⭐ The declaration is **a source of inquiry, ⛔ NOT a grading rubric** (*“this passage fails the purpose of the book”* ⛔ forbidden); divergence points **both ways**. ⭐⭐ **§10.3 — THE COMPASS LICENSES NOTHING ABOUT THE TEXT, binding on step 6**: it may orient WHAT MAIA ATTENDS TO; ⛔ it is **never evidence** and ⛔ **never discharges coverage**; ⛔ a declaration may never be restated as an observation (*“you said the book does X”* ✅ · *“the book does X”* ⛔ unless the covered text says so). ⭐ Already ruled in-repo — `lib/manuscript/structure/readScope.ts`: *“this reader interprets the Work **AS WRITTEN**, not authorial intention reconstructed from auxiliary material … a later, **separately consented** capability”* — and ⭐ **the Work Compass IS auxiliary material**, so feeding it to a reader is that deferred capability and ⛔ is authorized by no act to date. **15 acceptance conditions** (founder's 12 + three from substrate: ⛔ a declaration discharging coverage · ⛔ an evaluative *few at a time* cut · ⚠️ ⛔ **never-declared indistinguishable from declared-open** — ⭐ there are THREE states, and a never-declared Compass rendered as a form with blanks fails condition 3 through visual design while passing in data). ⭐ *The arrival passes when the member can experience:* **“MAIA understands something important about what I am trying to bring into being, but she has not defined it for me.”** ⛔ **ARRIVAL IS A DESIGN CANDIDATE — NOT BUILD AUTHORIZATION**: ⛔ no arrival surface · ⛔ no Compass persistence or schema · ⛔ no Compass→reader feed · ⛔ Idea/Material/Revisited states still out of scope. ⭐⭐ **`WRITERS-STUDIO-ARRIVAL-EXPERIENCE-01` (design candidate) + `WRITERS-STUDIO-CONVERGENCE-TESTING-01` (ratified testing instrument)** — records `…ARRIVAL-EXPERIENCE-01_DESIGN_CANDIDATE_2026-09-21.md` · `…CONVERGENCE-TESTING-01_HARNESS_2026-09-21.md`. **ARRIVAL EXPERIENCE** — experience law: ⭐ *MAIA understands why this Work matters to me, without having defined the Work for me.* ⛔ Four gaps a competent implementation would fill WRONGLY by default: **(1) ⭐⭐ *“you heard me correctly”* is ⛔ NOT *“hold that as my declaration”* — two acts, always, ⛔ never collapsed into one affirmative, or *nothing becomes canonical through silence* is defeated by **canonical through agreement to something else**; ⭐ the most likely custody failure, because the collapsed version feels more natural to build. **(2)** ⚠️ an **UNREACHED** dimension is ⛔ not *Still discovering* — §8's *appropriate stopping point* GUARANTEES partial Compasses, and writing *Still discovering* for an unreached dimension is ⛔ **MAIA declaring on the member's behalf** AND collapses declared-open into never-declared; ⭐ unreached = **absent from the returned Compass**, ⛔ not blank, ⛔ not listed. **(3)** ⚠️ what facet governs **before** one is declared (skip precedes the facet question)? ⭐ A default is lawful if **identical for every member, never inferred, visible, changeable**; ⛔ a default chosen from anything about this member is L6c arriving through a default value — ⛔ which facet is NOT RULED. **(4)** ⚠️ ***Don't save this* must be honest about what it does not save** — it plainly means no Compass entry; ⛔ it does not obviously mean the CONVERSATION is not retained. ⭐ *There is no stealth memory* — ⛔ **a consent control whose label is broader than its effect is the defect regardless of intent**; interacts with Sanctuary, ⛔ NOT RULED. Also pinned: ⭐ section B's **conversation** is substrate-light and buildable **as far as the Compass and no further** — ⛔ offering *“I have material”* with nothing behind it is `assertStudioMapHonest()` applied to the member's work. **TESTING-01** — ⭐ *mechanical invariants tested mechanically; human experience witnessed by the founder only where human perception is actually required*; ⛔ founder manual repetition may NEVER substitute for automated conformance. ⭐⭐ **§0 LETHALITY FIRST, per the ratified S3 Class B discipline**: author the suite → build a deliberately WRONG candidate per law → prove each dies → **only then implement**; ⛔ *a surviving candidate repairs the SUITE, never the candidate*. ⚠️ **A suite written against an existing implementation passes by construction and proves nothing** — eight defeat candidates named. ⚠️⚠️ **TEST A IS UNWRITABLE TODAY — AN OBSERVATION HAS NO IDENTITY**: `ReaderClaimDraft` carries `text` · `refs` · `doesNotEstablish` and ⛔ **no id**, so *“the same observation across three facets”* has nothing to compare but `text`, ⛔ **which facets vary BY DESIGN**. ⭐ **Binding: facet conformance requires an observation identity INVARIANT UNDER PRESENTATION**, derived from `refs` + `doesNotEstablish` + lens + frozen evidence, ⛔ **never from `text`**. ⭐ Not merely a test convenience — **the founder's own *One observation. Three expressions.* requires an object that does not yet exist**; without it, *three expressions of one observation* and *three separate readings* are indistinguishable from outside. ⭐⭐ **§E COMPASS SEPARATION IS ALREADY TRUE BY SHAPE**: `DevelopmentalReaderRequest` carries exactly `commissionedLens` · `evidence` · `recovered` — ⛔ **no field a declaration could travel in**; test it as a **type-level guard over everything the reader is handed** (⚠️ not `evidence` alone — the defeat candidate slips the Compass into the PROMPT). ⚠️ **ONE NAMED CONDITION IS NOT MECHANICALLY TESTABLE**: *Learning prescribes quality rather than describing technique* is a semantic judgment on generated prose — ⭐ resolve **structurally** (teaching drawn from a bounded reviewable governed set, an ARCHITECTURAL consequence) or assign to founder witness; ⛔ **never proxy it, and never report it as mechanically proved**. ⛔ **CONFABULATION BOUNDARY**: the six founder-witness questions are experiential — ⛔ no automated result may report them established; ⭐ *Unknown — requires the founder walk* is a RESULT, ⛔ not a gap. ⭐⭐ **`WRITERS-STUDIO-OBSERVATION-IDENTITY-01` RATIFIED · CONTRACT SPECIFIED · TEST A WRITABLE AND WRITTEN · MATRIX LETHAL** — record `docs/programme/WRITERS-STUDIO-OBSERVATION-IDENTITY-01_LAW_AND_LETHALITY_2026-09-21.md`; artifacts `tests/constitutional/writers-studio/**` + `tsconfig.ws-observation-identity.json` + `npm run typecheck:ws-observation-identity` / `matrix:ws-observation-identity`. ⭐ ***One observation → one identity → three possible expressions.*** `READING → CANONICAL OBSERVATIONS → {GUIDED|LEARNING|DIRECT}` — ⛔ **not** three readers producing three claim sets that look alike, which fails FACETS-01; ⛔ a facet renderer has **no authority to invoke a fresh reading** to render an existing observation. **§II identity**: opaque, **minted at admission**, ⛔ never derived from rendered text, shared by every facet expression; ⭐ means *this admitted observation from this reading*, ⛔ not every semantically similar one — cross-reading reconciliation is a separate act. **§III basis fingerprint** is an integrity witness, ⛔ **NOT the identity**. **⭐ THREE THINGS THE ACT LEFT OPEN, ANSWERED AND WITNESSED**: **(1)** `refs` is a NonEmptyArray so an observation has **no position until one is defined** — ⭐ **DEFINED: the EARLIEST position across its refs, by `(sectionPosition, codePointStart ?? 0)`**; since 07A evidence already carries code-point ranges, order resolves **below section granularity** and genuine ties become rare. **(2)** ⭐ **tie-break = `admissionIndex`**, non-evaluative by construction (it records WHEN a thing entered the record, ⛔ never how much it matters) — and because the suite asserts the DEFINED order it kills **every** alternative tie-break, not one severity function. **(3)** ⚠️ minting at admission means **admission must be the SINGLE point an observation comes into existence** — two admission paths mint two identities for one observation and nothing downstream could tell. **⭐ §IX BOUNDARY LOCATED**: `read.ts` sends `messages:[{content: renderRequest(request)}]`, so **the whole prompt is a pure function of `DevelopmentalReaderRequest`** (`commissionedLens · evidence · recovered`) — the guard must cover `renderRequest`'s inputs, ⛔ never `evidence` alone, which is what D7 exploits. **⭐ LETHALITY PROVED**: reference **16/16 PASS**; **D1 text-identity · D2 evidence-only identity · D3 per-facet reread · D4 Guided expansion · D5 hidden reachability · D6 tie-ranking · D7 Compass-in-prompt ALL KILLED** on their named checks; typecheck (strict + `noUncheckedIndexedAccess`) exit 0, matrix exit 0. ⚠️ **TWO COLLATERAL ENTRIES ADJUDICATED, ⛔ not papered over** — the first run reported them UNCLASSIFIED: ⭐ **D2's extra kill exposed a defect in MY OWN CHECK**, which detects any **content-derived** identity, not only a text-derived one (a content-derived identity is stable across admissions by construction) — the check was right and its NAME was wrong, so it was **renamed** `A0-identity-minted-at-admission`, then classified; **D3** additionally diverges `A3-authorship-facet-invariant` because per-facet re-reading mints fresh ids and proposals are id-keyed. ⭐ Both irreducible: removing the collateral would require the candidate to cease embodying its error. ⚠️ Instrument run with TypeScript/tsx/@types/node from a scratchpad (no project `node_modules` here) — ⭐ **the founder's run is the evidence of record**; ⛔ neither command widens `tsconfig.ship.json` nor can move the baseline. **§X HELD**: *Learning describes technique rather than prescribing quality* = ⭐ **UNKNOWN — REQUIRES HUMAN WITNESS**; ⛔ not proved, ⛔ not proxied. ⛔ The reference is a **test double, ⛔ never a seed** — no table, no reader, no durability. ⛔ **NOT AUTHORIZED**: steps 2–8 · production mutation · Editorial Reading · pass implementation · ranking · teaching capability · cross-reading reconciliation · **wiring this contract into the live reader**. ⛔ No `lib/`, `app/` or `database/` file modified. ⭐⭐ **`OBSERVATION-IDENTITY-01 / I1` COMPLETE — LIVE MATRIX LETHAL · ⛔ STOPPED FOR ADJUDICATION.** Packet `docs/programme/WRITERS-STUDIO-OBSERVATION-IDENTITY-01_I1_EVIDENCE_PACKET_2026-09-21.md`; base `a8a686cdb`. **⭐⭐ THE CENSUS CHANGED I1's SHAPE: THE ADMISSION SEAM ALREADY EXISTED.** §1 asks for exactly one live mechanism turning a reader claim into a canonical observation and forbids a second — and `freezeReading()` (one caller, `commission.ts:85`) was already it, with an observation model addressed `(readingId, observationKey)` under INV-2. ⛔ **So I1 introduced NO admission point — a new one would BE the second path the ruling forbids**; it adds identity, basis and position AT the existing seam. **MINTING**: `mintObservationId()` → `dobs_${randomUUID()}`, opaque, ⛔ not derived from claim text, facet text or the basis fingerprint; ⚠️ deliberately NOT from `(readingId, position)`, which would encode admission order into the identity. **BASIS**: SHA-256 over `{lens, revisionDigest, sorted refs, sorted doesNotEstablish}` — ⛔ claim text absent BY CONSTRUCTION. **⚠️⚠️ POSITION IS NULLABLE AND THE RULING WAS SILENT ON IT**: three of six ref kinds (`structure-unit` · `structure-units` · `structure-topology`) carry **no section at all**, so such observations have no place in the prose — ⭐ null sorts AFTER every positioned one, holding admission order; ⛔ a position is never invented, and a section absent from the topology contributes none rather than being coerced to 0 (which would place it FIRST in the Work). ⛔ **ADMISSION STORES IN ADMISSION ORDER, NOT SORTED** — `key` (`o1`…) encodes the claim index and BUILD-07F standing addresses by that key, so sorting at the seam would **silently re-address every member's recorded standing**; ordering is a READ-TIME concern. **⭐ LIVE MATRIX LETHAL** (`npm run matrix:ws-observation-identity-live`, exit 0): live reference **20/20**; **D1·D2·D3·D4·D5·D6·D7 ALL KILLED** against the real `freezeReading`; **⭐ SIBLING_ADMISSION_PATH KILLED** on `L0-single-admission-seam`, backed by a static guard (one consumer of the minter · resolved once · one invocation site). ⚠️ **THE FIRST VERSION OF THAT GUARD WAS WRONG AND PASSED VACUOUSLY** — it searched for `mintObservationId(` while the seam resolves `input.mintId ?? mintObservationId` and invokes `mint()`, so it found **0 call sites and reported success**; it now tests REFERENCES, not call syntax. ⭐ F1 double matrix still lethal. **TYPECHECK** strict + `noUncheckedIndexedAccess` (TS 5.6.3, matching the project): ⭐ **0 diagnostics in I1-owned files**; ⚠️ 18 remain in transitively-imported neighbours (`readState.ts` · `parse.ts` · `draftSections.ts`) not written to that flag — ⛔ pre-existing, ⛔ **the flag was NOT weakened to manufacture a pass**. **NEIGHBOURS: ⭐ ZERO ATTRIBUTABLE REGRESSIONS**, baseline established by stashing the change and re-running the identical check; two remaining errors are pre-existing AT HEAD. ⭐ `freeze.test.ts`'s key-set assertion updated — **the law it guards is unchanged** (no interpretation / severity / priority / score / confidence / rank): *the list grew; the prohibition did not move.* **⚠️⚠️ RAISED FOR ADJUDICATION, ⛔ NOT DECIDED: TWO ADDRESSES FOR ONE OBSERVATION, AND THE MEMBER'S OWN ACTS HANG OFF THE OLDER ONE** — BUILD-07F `developmental_observation_standing` records a member's stance by `(member_id, reading_id, observation_key, event_index)`, while facets will key on `observationId`. ⭐ They are 1:1 within a reading and the seam guarantees it, so nothing can drift today; ⚠️ which is the durable member-facing address is a founder question and answering it by migration is ⛔ not authorized. ⛔ **v1/v2 readings are NEVER backfilled** — a historical row's missing identity IS the evidence it was admitted under a contract that had none. **CONFIRMED: ⛔ no DB migration** (`observations` is `jsonb`) · ⛔ no facet UI · ⛔ no facet prose generation in `lib/**` · ⛔ no Editorial Reading · ⛔ no pass synthesis · ⛔ no ranking · ⛔ no deployment · ⛔ no `app/**` change · `check:no-supabase` exit 0. ⭐⭐ **`OBSERVATION-IDENTITY-01 / I1` ✅ CLOSED (founder adjudication) · OBSERVATION ADDRESS LAW RATIFIED · `WRITERS-STUDIO-OBSERVATION-ADDRESS-01` ESTABLISHED, ⛔ NOT OPENED.** Record `docs/programme/WRITERS-STUDIO-OBSERVATION-ADDRESS-01_CLOSURE_AND_CHARTER_2026-09-21.md`; candidate `e8f39cff` on base `a8a686cdb`. **⭐ THE EXISTING SEAM ACCEPTED**: the ruling required *observation existence begins at one admission seam*; ⛔ it did NOT require creating one — ⭐ **and creating a second mechanism would have violated the ruling**. ⭐ *The implementation did the right thing by discovering and extending the existing admission seam rather than creating the one we had imagined — an important correction to the model, ⛔ not a deviation from the ruling* (founder). **⭐ THE GUARD CORRECTION IS ACCEPTED AS THE LAW IT REVEALED**: the invariant is **reference/use of the single minter at the single seam**, ⛔ not a brittle syntactic search for one function-call spelling. **⭐⭐ OBSERVATION ADDRESS LAW (§VIII) — TWO VALID BUT DIFFERENT ADDRESSES, ⛔ NEVER CASUALLY COLLAPSED**: `observation_id` answers ***which admitted observation is this***; `(readingId, observationKey)` answers ***where is this observation addressed within this frozen reading record***. ⭐ Presently **1:1 within a reading** — ⛔ **and that equivalence does not make them conceptually interchangeable.** **RATIFIED ALLOCATIONS**: ⭐ `observation_id` is the **canonical MEMBER-FACING identity** for all new facet and convergence work, and GUIDED · LEARNING · DIRECT must carry the SAME one; ⭐ existing standing keyed `(member_id, reading_id, observation_key)` **remains AUTHORITATIVE until separately reconciled** — ⛔ do not migrate it · ⛔ do not silently change its keys · ⛔ **do not create duplicate standing keyed by `observation_id`**. **⭐ THE 1:1 RELATION IS NOW GUARDED, ⛔ NOT ASSUMED**: `L7-identity-and-key-are-1to1` + `L7-key-tracks-admission-index` (`key === o{i+1}` ∧ `admissionIndex === i`, so the read-local address still tracks admission order and **standing custody is not re-addressed**) — ⚠️ **if either fails, the bridge premise has failed with it**, which is why it is guarded before anything depends on it. ⛔ **ADMISSION STORAGE ORDER PRESERVED AT THE SEAM** — stored observations are never reordered to produce manuscript-order presentation. ⛔ Position is never invented where a ref kind carries none; the ordering (positioned → by position · unpositioned after · ties by `admissionIndex`) is **non-evaluative and establishes no relative importance**. ⛔ **COMPASS**: not reader evidence · not coverage · not a whole-work warrant · ⛔ **not available to discharge a non-conclusion**. **⭐ NEXT BOUNDED ACT — `OBSERVATION-ADDRESS-01`, SOLE QUESTION**: *how does a member action addressed to canonical `observation_id` resolve to the ONE existing standing record without duplicating, orphaning, or reinterpreting prior custody?* ⭐ **PREFERRED DIRECTION: a deterministic bridge over the existing 1:1 relation, ⛔ NOT an immediate database migration** — ⛔ *a migration must not be presumed necessary merely because a new canonical identity now exists*; ⭐ *the old address is doing real custody work, the new id is doing real identity work; they can coexist if the relationship is made explicit rather than forcing one to impersonate the other.* ⛔ **GATE: facet implementation may not create member actions against `observation_id` until that lane closes.** ⛔ I1 establishes no facet UX · no Guided/Learning/Direct quality · no Editorial Reading · no synthesis · no ranking · **no production readiness**; Learning stays ⭐ **UNKNOWN — REQUIRES HUMAN WITNESS**. **Standing: CONVERGENCE LAW FROZEN · FACETS-01 RATIFIED (GUIDED · LEARNING · DIRECT) · §9.7 SETTLED · ARRIVAL EXPERIENCE ⛔ DESIGN ONLY · TESTING-01 RATIFIED (⚠️ suite + defeat candidates + LETHALITY PROOF **OWED BEFORE IMPLEMENTATION**; ⚠️ observation identity BLOCKING TEST A) · INTENTION CUSTODY DELIVERED (⭐ ratifiable on a founder act) · ARRIVAL ⛔ DESIGN ONLY · STEPS 2–8 ⛔ NOT STARTED · ⛔ NO ADDITIONAL PRODUCT-CAPABILITY LANE MAY OPEN UNTIL THE SPINE HAS BEEN WALKED · ⛔ NO MIGRATION · ⛔ NO DEPLOY · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-17 — ⭐⭐ `SPM-FC-01-R5` RATIFIED + CANONICALIZED · CONSTITUTIONAL-CONTRACT LANE CLOSED · IMPLEMENTATION CLOSED.** Canonical merge `af58e6392` (PR #1338) satisfies the predeclared ancestry witness: `656b4a59b` canonicalization witness, final R5 lineage `c2c62b53d`, independent provenance sweep `40f1d821c`, and R5 founder ratification `18f95363a` are all ancestors of `clean-main-no-secrets`; stale pre-R5 ratification/accounting commits `d1d942e54` / `8fe80d366` are absent. Ratified composite manifest remains SHA-256 `75932893bd8beddac5f08666a3aa4656b28683036cf4d8a9d0b96c73626e33b7`. Final standing: **31 local D9/F5 laws (`16 + 10 R2 + 4 R4 + 1 R5`) · I-19 GAP · I-33 imported binding dependency · D9 CLOSED · F5 TRACE COMPLETE · F5 ERASURE CONFORMANCE FAIL / STOP · organism conformance NOT ESTABLISHED.** Canonicalization was documentary only: zero non-doc delta from pre-canonical base `78a85f652`. Earlier bullets saying canonicalization was prepared/not taken are historical and superseded as present orientation. **F5 repair lane NOT OPEN · implementation/schema/migration/route/UI/backfill/deploy/production mutation NOT AUTHORIZED · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-17 — ⭐⭐ `SPM-FC-01-R5` RATIFIED · CANONICALIZATION WITNESS PREPARED · IMPLEMENTATION CLOSED.** Final R5 constitutional standing is ratified at `18f95363a` with composite manifest SHA-256 `75932893bd8beddac5f08666a3aa4656b28683036cf4d8a9d0b96c73626e33b7`; independent D9-A sweep `40f1d821c` PASS is preserved on its own custody history. This branch is a documentary canonicalization candidate from canonical base `78a85f652`: R5 history merged at `30e402ead`, independent sweep merged at `19614946c`, zero non-doc delta. **Canonicalization is complete only when `clean-main-no-secrets` contains `SPM-FC-01-R5_CANONICALIZATION_2026-09-17.md` as an ancestor; verify mechanically after merge.** Stale pre-R5 ratification/accounting commits `d1d942e54` / `8fe80d366` are not ancestors and are not current truth. Standing remains: 31 local laws · I-19 GAP · I-33 imported binding dependency · D9 CLOSED · F5 TRACE COMPLETE · F5 ERASURE CONFORMANCE FAIL / STOP · organism conformance NOT ESTABLISHED · F5 repair lane NOT OPEN · implementation/schema/migration/route/UI/backfill/deploy/production mutation NOT AUTHORIZED.
- **LATEST — 2026-09-17 — ⭐⭐ `SPM-FC-01-R5` RATIFIED · INDEPENDENT D9-A SWEEP PASS · DISCOVERABILITY DEFECT CLOSED · IMPLEMENTATION CLOSED.** Founder ratification `18f95363a` binds the composite manifest `SHA-256 75932893bd8beddac5f08666a3aa4656b28683036cf4d8a9d0b96c73626e33b7`, not a branch or a rewritten single-file fiction. Final standing: **31 locally earned D9/F5 laws** (`16 unchanged + 10 R2 + 4 R4 + 1 R5`) across 33 numbered clauses / 13 domains; **I-19 remains a declared GAP with no law**; **I-33 is KEPT as an imported binding dependency from ratified temporal-memory law and excluded from the local count**. The D9-A provenance class was mapped independently twice: local sweep `fe16036a9` and independent sweep `40f1d821c` both found the same 12-clause denominator and **no additional hidden instance**. The base contract now marks itself **SUPERSEDED IN PART**, routes all R2/R4/R5 corrections, marks I-8/I-12/I-14/I-15/I-17/I-33 and the historical standing block, while preserving the historical text byte-for-byte when routing annotations are stripped (`79eb697a0`). ⭐ I-8 is deliberately narrow: it governs conformance only where independent authority is already required; it does **not** establish that a new surface requires one, and G1/I-19 remains open. ⚠️ Earlier ratifications `d1d942e54` (pre-R5 object) and `89a3e3a20` (returned by `aa27bc55a`) remain historical and are **SUPERSEDED AS CURRENT STANDING**. `8fe80d366` on the parallel accounting branch is therefore a stale historical programme-state bullet and must not be canonicalized as current. **D9 CLOSED with qualifications preserved · F5 TRACE COMPLETE · F5 ERASURE CONFORMANCE ⛔ FAIL / STOP · organism conformance ⛔ NOT ESTABLISHED · F5 repair lane ⛔ NOT OPEN · implementation/schema/migration/route/UI/backfill/deploy/production mutation ⛔ NOT AUTHORIZED · canonicalization ⛔ NOT TAKEN.**
- **LATEST — 2026-09-16 — ⭐ `TEMPORAL-MEMORY-CUT1-TRACEABILITY-01` ✅ CLOSED IN PRODUCTION · `TEMPORAL-MEMORY-RECONCILIATION-01` remains ✅ CLOSED · behavioural reconciliation ⛔ UNOPENED.** PR #1301 is live in runtime `98542ff06`; production carries `memory_cut1_trace_runs` and the witness found **14 durable retrieval traces / 14 distinct turn bindings / 14 bound to matching member sessions**, bounded live+neutral sets (≤12), and **0 observer failure logs**. Historical exclusions derive from the trace table alone; the observed excluded memory remains present, content-recoverable, and currently valid. No natural zero-difference production turn has occurred yet; the deterministic zero case is green and ⛔ no synthetic production history was manufactured. Record: `docs/programme/TEMPORAL-MEMORY-CUT1-TRACEABILITY-01_PRODUCTION_WITNESS_AND_CLOSURE_2026-09-16.md`. ⭐ **Gestalt non-capture law added**: `docs/architecture/MAIA_TEMPORAL_RELATIONAL_MEMORY_GESTALT_LAW_2026-09-16.md` — memory may deepen present orientation but may never become the fixed lens through which MAIA sees the present; same history + materially different present must be able to produce materially different orientation. ⛔ No scorer, coefficient, `LIMIT 12`, validity, supersession, Cut 2, or member-visible response change is authorized by this closure.
- **LATEST — 2026-09-16 — ⭐ `TEMPORAL-MEMORY-RECONCILIATION-01` ✅ CLOSED (non-conformance established, ⛔ unrepaired) · `TEMPORAL-MEMORY-CUT1-TRACEABILITY-01` OPENED · CENSUS COMPLETE (READ-ONLY) · ⛔ DESIGN NOT OPENED.** Records `docs/programme/TEMPORAL-MEMORY-RECONCILIATION-01_CLOSURE_2026-09-16.md` · `TEMPORAL-MEMORY-CUT1-TRACEABILITY-01_CHARTER_AND_CENSUS_2026-09-16.md`. ⭐⭐ **Lane motto, carried at the top: *traceability is the INSTRUMENT for future reconciliation, not the reconciliation itself.*** **SOLE JOB**: make Cut-1 decay exclusions **durably traceable per turn, without changing which memories are selected.** Observational recording only — ⛔ no ranking formula · ⛔ no coefficients · ⛔ no `LIMIT 12` · ⛔ no validity/supersession · ⛔ no retrieval redesign · ⛔ no *symmetry* instrumentation on other paths · ⛔ Cut 2 not opened · ⛔ no member-visible inference. **ACCEPTANCE**: *for a given turn, after the fact, which otherwise-valid memories were excluded from Cut 1 by the decay-bearing ranking, and why* — **durable enough that the historical turn need not be reconstructed from current database state.** ⭐ **SEQUENCING IS THE REASON IT GOES FIRST**: without traceability a behavioural change would be judged against a counterfactual rather than a historical record, undoing the evidentiary distinction the closed lane just established. **Reconciliation ⛔ UNOPENED.** **⭐ CENSUS VERDICT — NO EXISTING OBJECT ANSWERS THE ACCEPTANCE CONDITION.** `selectionTrace` is ⛔ structurally blind (built after `deduped`, downstream of the cut, log-only, voice-only). ⭐ **`conversation_memory_uses` is the nearest and records the WRONG SIDE OF THE CUT**: durable, per-turn keyed, four score columns, and written by THREE call sites (`maiaService.ts:886` · `MemoryBundle.ts:131` · voice route `:1319`) so it **already reaches the text path** — but `recordRetrievedCandidates()` is handed `allCandidates`, already the survivors of the SQL `LIMIT 12`. **⚠️ ONE FINDING THAT IS AFFORDANCE AND HAZARD AT ONCE**: production's table carries **NO CHECK on `used_as`** (baseline `0001_baseline_2026-09-01.sql:5068` — only a `session_id` non-empty CHECK and the PK; the six-value CHECK in `20251231_…` is **not what production runs**, `20260113000003b_…` re-created it without one). ⭐ So a new value needs no constraint migration — ⚠️ **and that is precisely the hazard: a semantic category could enter the durable record without a governed act**, the same defect class as permission arriving through a convenient adjacent field. ⛔ **The census names it; it does not propose using it.** **⭐⭐ THE STRUCTURAL FACT ANY DESIGN MUST FACE**: `ORDER BY score DESC LIMIT 12` runs **in the database**, so excluded rows never enter the Node process — **any durable exclusion record must be produced at or below the SQL boundary**, and doing that without changing which twelve rows are selected is the lane's actual engineering problem. **OWED BEFORE ANY DESIGN ACT**: (1) extend an existing object vs its own address · (2) rule on the open `used_as` vocabulary — closed before reliance, or left open by explicit decision · (3) a bounded-volume answer (the excluded set is *pool − 12* per turn per member, **unbounded above** — a storage decision, ⛔ not a detail) · (4) ⭐ a falsifier that fails a candidate which changes selection — **the lane's central risk is a recording mechanism that perturbs the thing it records.** **CARRY-FORWARDS FROM THE CLOSED LANE, visible and ⛔ OUTSIDE THIS LANE'S SCOPE**: Stage B / Cut 2 remains **UNANSWERED, ⛔ not negative** (no qualifying voice turn in the fixed window) — ⛔ never later restated as *decay does not affect Cut 2*; and the four routed observations (`IMMUTABLE`+`NOW()` · incommensurable `rankCandidates` scores · partial `recall_count` feedback · detect→ask API with no member route) stay separate and unrepaired — ⛔ **the traceability lane may not absorb any of them.** **Standing: RECONCILIATION LANE ✅ CLOSED · CLAUSE 2 NON-CONFORMANCE ✅ ESTABLISHED, ⛔ UNREPAIRED · TRACEABILITY LANE OPEN · CENSUS ✅ COMPLETE · ⛔ DESIGN NOT OPENED · ⛔ NO BEHAVIOUR CHANGE · ⛔ NO RANKING CHANGE · ⛔ RECONCILIATION UNOPENED · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-16 — ⭐⭐ `TEMPORAL-MEMORY-RECONCILIATION-01` ACT 2 ADJUDICATED · PROVENANCE ✅ CLOSED · **OUTCOME A ESTABLISHED** · **CLAUSE 2 NON-CONFORMANCE IS NOW A MEASURED RUNTIME FACT**.** Record `docs/programme/TEMPORAL-MEMORY-RECONCILIATION-01_ACT2_RESULT_2026-09-16.md` §1 (closure) + §9 (adjudication). **PROVENANCE CLOSED**: `git hash-object` returned **`f8c06d513ba578e5e0336eaf65b0123f55e69629`**, an exact match — the SQL that ran at `0bd2b6578` **is** the instrument verified against runtime `637c115d1`. Stop condition 4 discharged, the entailment is now **WITNESSED**, every number **LICENSED**, ⛔ the run is not re-spent. The pending text is kept verbatim in §1a, ⛔ not edited to read as though it had always said otherwise. **⭐ STAGE A — OUTCOME A**: 4 of 37 members with candidates (10.8%) · **4 of 14 where the top-12 is actually a cutoff (28.6%)** · 18 flips (9 admitted on neutralization, 9 displaced by live decay). **An availability effect, ⛔ not merely ordering.** Strongest permitted claim, ratified: ***live temporal decay changes the availability of otherwise-valid memories before the member's current utterance participates in retrieval.*** **STAGE B — NO EVIDENCE**, cleanly: stream existed so the command worked; no matching entries in the fixed window. ⛔ Not *no effect* · ⛔ not text-path evidence · ⛔ not permission to broaden the grep retrospectively. **⭐⭐ THE CONSTITUTIONAL RESULT — the live system is PRESENTLY NON-CONFORMANT WITH CLAUSE 2**, ⛔ not because decay exists and ⛔ not because it changes ranking, but because **it can exclude valid memories from cognition and there is no durable record of which memories it excluded on the historical turn.** The counterfactual reconstructs today; it confers no historical traceability. *That distinction survived exactly as predeclared.* **⭐ TWO HYPOTHESES GOT WEAKER — recorded as the useful negative results they are**: **(1)** all 18 flipped rows are `confirmed_by_user = false`, so ACT 2 found **no observed case in which decay defeated the confirmation contribution** — the `≈17.8×` figure stays a mathematical property of the formula, ⛔ **no evidence it caused the observed flips**; **(2)** the population was entirely `memory_type = 'pattern'`, so other half-lives were never exercised — the TS/live divergence stays real **in source**, ⛔ **no production evidence about type-dependent decay behaviour.** *Cleaner than carrying the divergence as though it were affecting members.* **⭐ THE RESULT IS NARROWER AND STRONGER THAN THE ARGUMENT THAT MOTIVATED THE LANE**: coefficients and hypothetical type behaviour are no longer needed, because the core problem was measured directly — for members whose pools exceed the 12-row cutoff, live decay changes which valid memories remain available **before conversational relevance is considered**, and for one member five of twelve slots flipped over a ~one-month age difference. ⛔ Still **not** a member-visible continuity failure. **Standing: INSTRUMENT ✅ VERIFIED · STAGE A ⭐ OUTCOME A · CUT-1 MEMBERSHIP + AVAILABILITY EFFECT ✅ ESTABLISHED · CUT-1 RECOVERABILITY ✅ · CUT-1 HISTORICAL TRACEABILITY ❌ NON-CONFORMANT · STAGE B ⛔ NO EVIDENCE IN THE FIXED WINDOW · TEXT PATH ⛔ NOT MEASURED · MEMBER-VISIBLE CONSEQUENCE ⛔ NOT ESTABLISHED · ⛔ REPAIR NOT AUTHORIZED · ⛔ NEW SCORER NOT AUTHORIZED · PRODUCTION UNTOUCHED.** ⭐⭐ *Decay is not silently changing truth. It is silently changing availability — and that is exactly the boundary the lane was created to determine.*
- **LATEST — 2026-09-16 — ⭐ `TEMPORAL-MEMORY-RECONCILIATION-01` ACT 2 SPENT · STAGE A = **OUTCOME A** · STAGE B = **NO EVIDENCE** · ⚠️ INSTRUMENT PROVENANCE PENDING.** Record `docs/programme/TEMPORAL-MEMORY-RECONCILIATION-01_ACT2_RESULT_2026-09-16.md` (founder-run 2026-09-15, read-only, temp tables dropped, production untouched). **⚠️ PROVENANCE FIRST, because it gates every number**: the Mac Studio checkout never reached the lane head — `git switch` failed (`invalid reference`) and `git pull` failed (`divergent branches`) — so the SQL piped came from **`0bd2b6578`**, ⛔ not the verified `2281f099`. Size matches (**8732 bytes**) and the printed §2 headers match verbatim, ⭐ **but that is ENTAILED, ⛔ not WITNESSED.** One command closes it: `git hash-object scripts/witness/temporal-memory-audit.sql` → expect **`f8c06d513ba578e5e0336eaf65b0123f55e69629`** (sha256 `21e1f81e…`). ⛔ **Until it matches, the numbers are RECORDED BUT NOT LICENSED**; a mismatch is INSTRUMENT FAILURE / NO EVIDENCE and the run is re-spent, ⛔ never reinterpreted. **⭐ STAGE A → OUTCOME A · AVAILABILITY EFFECT ESTABLISHED**: §2.b **4 members' top-12 SET changes** when the decay factor is held at 1; §2.e **18 membership flips** (9 admitted §2.c + 9 displaced §2.d) — membership change, ⛔ not ordering alone, so Outcome B is excluded by the same rows. ⚠️ **TWO DENOMINATORS, BOTH TRUE, ⛔ NEITHER QUOTED ALONE**: **4 of 37** members with candidates (10.8%, the script's headline) and **4 of 14** members for whom the top-12 is *actually a cut* (**≈28.6%**) — for the other 23 the pool never exceeds 12 and no flip is possible. Largest effect: member `17a14614`, five rows at 121–127 days excluded and five at 96 days held in — **a ~25–31 day age gap decides five of twelve slots.** **STAGE B → NO EVIDENCE under §7.3**: stream non-empty (**73 lines / 24h**) so the command ran and the pre-run ambiguity is resolved, but **0** `voice:memory_trace` lines — the window held no voice turn emitting a trace. ⛔ **NOT evidence that decay does not affect Cut 2.** Grep unbroadened, window unmoved, as instructed. **⭐⭐ TWO THINGS THE MEASUREMENT SHOWS THAT CUT AGAINST WHERE THE LANE WAS LEANING, RECORDED FOR THAT REASON**: **(1)** every one of the 18 flipped rows carries `confirmed_by_user = f` — **the observed exclusions are time-against-time, ⛔ not time-against-confirmation**, so the ≈17.8:1 contribution ratio **drove no observed flip** and remains an UNEXERCISED property of the formula in this population; *this narrows the ratio's standing rather than supporting it.* **(2)** all **2158** candidate rows are `memory_type = 'pattern'` (consistent with F3), so **one half-life (180d) is in play for the entire production population** and the type-differentiated table — exactly where live SQL and the unreached TS helper diverge — is **WHOLLY UNEXERCISED in production**: the latent divergence is latent for a second, independent reason. **§1 still all zeros** (0 expired rows) — recorded, ⛔ outside ACT 2's question. Population drift since 2026-09-06 recorded, ⛔ not adjudicated: 36→37 members · 2018→2158 rows · 2→4 set changes; ⛔ no trend claimed from two readings. **⭐ THE NON-CONFORMANCE SURVIVES AND IS NOT SOFTENED**: the 9 admitted rows are what decay excludes *at this moment, under this data* — ⛔ **not a record of what any member's turn was denied.** Cut-1 historical traceability stays **NON-CONFORMANT**. **Claim ceiling observed**: established = *live temporal decay changes the availability of otherwise-valid memories before the current member utterance participates in retrieval* (the *before* clause carried by the code — Cut-1 SQL binds only `user_id`); ⛔ NOT established — member-visible continuity failure · that `0.40` is wrong · that confirmation needs more weight (§5.1 makes this LESS supported) · that decay should be removed · any replacement ranking function. **Standing: ACT 2 SPENT · OUTCOME A (pending provenance) · STAGE B NO EVIDENCE · PROVENANCE ⚠️ PENDING ONE COMMAND · CUT-1 TRACEABILITY ❌ NON-CONFORMANT · RATIO AND HALF-LIFE TABLE BOTH ⚠️ UNEXERCISED · ⛔ NO REPAIR · ⛔ NO INSTRUMENTATION · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-15 — `TEMPORAL-MEMORY-RECONCILIATION-01` ACT 2 ⭐ AUTHORIZED · PRE-FLIGHT DISCHARGED · ⛔ MEASUREMENT UNSPENT.** Record `docs/programme/TEMPORAL-MEMORY-RECONCILIATION-01_ACT2_PREFLIGHT_2026-09-15.md`. ⚠️ **ACT 2 CANNOT BE SPENT FROM THIS SESSION** — the container has **no `ssh` binary, no `DATABASE_URL`, no route to minisforum**, and both stages read production (`docker exec … psql` · `docker logs`). *A statement about the environment, ⛔ not a deferral and ⛔ not a partial result — no production read has been performed under this lane.* **⭐ STOP CONDITION 4 DISCHARGED, AND AGAINST THE DEPLOYED RUNTIME, not merely the working tree.** By token comparison, ⛔ not by eye: the witness's `score_with` is **TOKEN-IDENTICAL** to the live `getSemanticMemories` score; `score_without` differs in the decay call **and nothing else** (the tokens unique to one are exactly `COALESCE`·`calculate_decayed_confidence`·`memory_type`·`last_confirmed_at`, while `significance` and `formed_at` appear in both — so neutralization holds the decay factor at 1 at unchanged weight, ⛔ not term removal); pool filters match, and the live `user_id = $1` is ⛔ not a mismatch but a scope generalization to `PARTITION BY user_id`. ⭐ Diffed against production runtime **`637c115d1`**: `MemoryBundle.ts` · the witness SQL · the voice route · `confidenceDecay.ts` all **IDENTICAL @ `637c115d1`..HEAD**. **STOP CONDITION 1 / COVERAGE**: §2.c and §2.d already emit `memory_type` · `age_days_ref` · `confirmed_by_user` · both ranks · displacement, so the ruling's full measurement list is covered — ⛔ **no addendum query, ⛔ no edit to the existing witness**, its identity as the instrument run on 2026-09-06 preserved. **RATIFIED INTERPRETATIONS CARRIED IN**: **(1)** `reconstructible ≠ traceable` — Cut-1 recoverability ✅ · reconstructibility ✅ · **historical traceability ❌ NON-CONFORMANT**; Cut-2 traceability ✅ voice · ⛔ text not evidenced. The non-conformance ⛔ does not authorize instrumentation — it becomes part of the reconciliation result, and ⛔ Stage A's counterfactual must never be reinterpreted as historical traceability: **it is a present-state comparison.** **(2) ⭐ THE TWO CUTS ARE DIFFERENT KINDS OF THING**: **Cut 1 = member-level availability ranking** · **Cut 2 = later selection from an already-truncated pool**. At Cut 1 the system asks *which twelve have the strongest standing under this general score*, ⛔ **not** *which are relevant to what the member is saying now* — `queryText`/`facet` are accepted and never bound. ⛔ Not proof Cut 1 is wrong; it defines what the availability effect MEANS. **(3) ⚠️ MY OWN EARLIER WORDING CORRECTED** — *"time outweighs the member's word ~18:1"* was too broad. Precise: **within this particular scoring formula, the maximum contribution available to elapsed-time decay is ≈17.8× the contribution available from the confirmation term.** Other member-grounded terms may exist in the total score; the narrower wording stops a coefficient comparison from silently becoming a claim about the whole ranking system. **TWO BINDING STAGE-B GUARDS**: state the result as **voice-path evidence only** (⛔ never generalized to `sovereign/app/maia/list`, which emits no trace — adding it is the withheld instrumentation); and ⛔ **do not let Stage B imply it observed the complete candidate pool** — Cut 1 has already happened, so the trace's `rank` indexes an already-truncated list. **ACT 2 MAY conclude** *live temporal decay changes the availability of otherwise valid memories before the current member utterance participates in retrieval*; ⛔ **MAY NOT conclude** a member experienced a continuity failure · that `0.40` is wrong · that confirmation needs more weight · that decay should be removed · what a replacement ranking function should be. **Standing: ACT 2 AUTHORIZED · PRE-FLIGHT ✅ DISCHARGED · INSTRUMENT ✅ VERIFIED FIT AGAINST THE DEPLOYED RANKING · COVERAGE ✅ COMPLETE · ⛔ MEASUREMENT UNSPENT, OWED TO A HOST WITH PRODUCTION ACCESS · ⛔ NO INSTRUMENTATION · ⛔ NO REPAIR · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-15 — ⭐⭐ `TEMPORAL-MEMORY-RECONCILIATION-01` ACT 1 RULED · TEMPORAL LAW RATIFIED · ACT 2 SPECIFIED, ⛔ UNSPENT.** Records `docs/programme/TEMPORAL-MEMORY-RECONCILIATION-01_ACT1_RULING_2026-09-15.md` (the law) · `…_ACT2_PROCEDURE_2026-09-15.md` (unspent). The ACT 1 record is the evidence; the ruling is the law. **⭐ THE TWO-CLAUSE LAW WAS INSUFFICIENT BECAUSE IT SKIPPED THE MIDDLE TERM.** Three powers, ⛔ never collapsed: **VALIDITY** (is it still valid) · **AVAILABILITY** (can a valid memory reach cognition) · **SALIENCE** (how strongly does an available memory compete). **CLAUSE 1 ✅ RATIFIED — decay may NEVER determine validity**; satisfied structurally today. **CLAUSE 2 ✅ RATIFIED at refinement (b) — *decay may affect availability through ranking, but any exclusion it causes must remain traceable and recoverable; it may not silently convert a valid memory into an effectively unreachable one.*** ⛔ (a) *order but never exclude* REFUSED — any bounded retrieval eventually creates a cutoff, and a signal that legitimately orders will sometimes decide which side of it a row lands on; ⛔ (c) *only where the member has a live route back* NOT CHOSEN YET — more specific than the temporal law needs, and dependent on surfaces not yet adjudicated. **CLAUSE 3 ✅ RATIFIED — decay may influence salience as a bounded signal; its coefficient confers no epistemic or member authority**, so the live `0.40` has ⛔ **no standing merely because it is deployed**, and the measured `≈17.8:1` elapsed-time-over-member-confirmation leverage is ⚠️ MEASURED, ⛔ NOT RULED WRONG — it proves only that the scorer gives time enough leverage to overwhelm confirmation, a relationship never adjudicated. **SUPERSESSION PRESERVED** — it labels state, ⛔ never prunes; otherwise MAIA could answer *what is true now* only by losing *what did we believe before*. **⭐⭐ CONFORMANCE OF THE LIVE SYSTEM AGAINST THE NEWLY RATIFIED CLAUSE 2 — the two properties are NOT in the same state.** ✅ **RECOVERABLE: SATISFIED** — exclusion is non-destructive, the row persists unaltered, and a decay-neutral query returns it. ✅ **TRACEABLE AT CUT 2: SATISFIED** — `selectionTrace` records every deduped candidate *including the non-selected*, which is what makes it traceability rather than a survivor log. ⚠️ **TRACEABLE AT CUT 1: NOT SATISFIED** — `recordRetrievedCandidates()` receives `allCandidates`, already the survivors of the SQL `LIMIT 12`; nothing durable names a row decay excluded on a given turn. ⭐ **RECONSTRUCTIBLE ≠ TRACEABLE**: a counterfactual reports what decay excludes *now, under current data*; it cannot report what decay excluded from a member's turn last March. *Traceability is a property of the record; reconstruction is a property of the query* — and the clause asks for the first. **NAMED NON-CONFORMANCE, REPORTED, ⛔ NOT REPAIRED** (closing it needs runtime instrumentation this ruling withholds). **⭐ ACT 2 NEEDS NO NEW INSTRUMENT FOR CUT 1** — `scripts/witness/temporal-memory-audit.sql` **§2** already IS the counterfactual: committed, production-run once, read-only, temp tables dropped; its `score_with` is **term-for-term identical** to the live `getSemanticMemories` SQL with the same pool filter; neutralization **holds the decay factor at 1** (`0.40 * significance`), ⛔ not term removal, which would change the scale; **§2.c is literally *memories decay kept out*** and §2.d its mirror. Two fidelity notes: the witness adds an `, id` tiebreak where production's bare `LIMIT 12` is nondeterministic on ties, so it is at least as strict; and ⭐⭐ **the live Cut-1 SQL binds only `$1 = userId`** — `queryText`/`facet` are accepted and never used — so **which twelve developmental memories are available is decided BEFORE the member's current message is consulted at all**, relevance entering only later at `rankCandidates`. That makes Stage A faithful to the live cut, ⛔ not an approximation. ⚠️ **STAGE B IS VOICE-SCOPED**: of the FOUR `MemoryBundleService.build()` call sites, only `app/api/voice/stream-conversation/route.ts` emits `[voice:memory_selection:<turnId>]`; the main text route `sovereign/app/maia/list` does not, and adding it would be the instrumentation the ruling withholds — so Cut-2 evidence must be STATED as voice-only, ⛔ never presented as coverage of the text path. **CLAIM BOUNDARY BINDING**: ACT 2 may establish *decay changes which valid memories survive into the available/selected sets*; it ⛔ may NOT claim *decay changed what the member experienced* — that needs binding a selected memory through the cognition/prompt/response path. Five stop conditions; a run that hits one yields NO EVIDENCE, ⛔ not a finding. The 2026-09-06 direction note stays FROZEN — ACT 2 records into this lane. **Standing: TEMPORAL LAW RATIFIED (validity ❌ · availability ✅ conditionally · salience ✅ bounded · silent-or-unrecoverable exclusion ❌) · CUT-1 TRACEABILITY NON-CONFORMANCE NAMED AND UNREPAIRED · COEFFICIENTS OBSERVED, NOT ADJUDICATED · ACT 2 SPECIFIED AND ⛔ UNSPENT · ⛔ NEW RUNTIME INSTRUMENTATION NOT AUTHORIZED · ⛔ REPAIR NOT AUTHORIZED · ⛔ NEW SCORER OUT OF SCOPE · THE FOUR ROUTED OBSERVATIONS STAY OUTSIDE THIS ACT · `ANTECEDENT-IDENTITY-01` UNTOUCHED · PRODUCTION UNTOUCHED.** ⭐ *Old is not false. Old is not unavailable. Old may be less salient — but if time makes a valid memory disappear from cognition, that disappearance must itself be accountable.*
- **LATEST — 2026-09-15 — ⭐ `TEMPORAL-MEMORY-RECONCILIATION-01` OPENED (founder act, no new charter) · ACT 1 COMPLETE · ⛔ STOPPED FOR THE RULING.** Lane split ratified: `ANTECEDENT-IDENTITY-01` owns the relational edge `REFERS_TO`; this lane owns decay vs temporal law; ⛔ neither takes authority from the other; synthesis deferred to `RELATIONAL-DISCOURSE-MEMORY-01`. Record `docs/programme/TEMPORAL-MEMORY-RECONCILIATION-01_ACT1_DECAY_LAW_2026-09-15.md`. Candidate law: ***Decay may affect salience. It may not determine validity.*** READ ONLY — ⛔ no repair, ⛔ no implementation preferred, ⛔ no composite scorer, ⛔ Episodic Phase 2 not opened. **⭐ PREMISE CORRECTED BEFORE ADJUDICATION**: F3's *two divergent implementations* is true of the REPOSITORY and false of the RUNTIME — `calculateDecayedConfidence` is imported at `lib/memory/MemoryBundle.ts:16` and **never called**, by that file or any other; the module's only reached export anywhere is `formatDaysAgo`, in a component mounted nowhere. **One live implementation, one unreached one.** The TS divergences (confirmed-memory `halfLife × 1.5`, absent from SQL; `ain_deliberation` 120 vs 90; `spiral_transition` 180 vs 90) are therefore LATENT, ⛔ not operative — cheap to adjudicate now, expensive once someone wires it in believing it already agreed. Its 5th parameter defaults `false`, so a caller that forgets the flag silently gets the unconfirmed curve: *it fails open on the side that ignores the member.* **⭐ CLAUSE 2 SATISFIED STRUCTURALLY, not by discipline** — decay is a pure function that writes nothing; every `valid_to` write in the codebase is in `PreferenceConfirmationStore.record()` and member-originated (`expired`/`restored`/`confirmed`), and the confirm path already refuses to resurrect a member-withdrawn row. ⛔ No timer, no sweep, nowhere. **⚠️ CLAUSE 1 UNSETTLED — the finding.** Decay is 0.40 of a score truncated at `LIMIT 12`. **A salience function that truncates is an availability function**, and F2 already measured it changing the surviving set for 2 of 14 eligible members. From the member's side a valid, unwithdrawn memory that never arrives is indistinguishable from one invalidated. The law's two clauses ⛔ do not partition exclusion-by-rank. Three refinements offered, ⛔ none taken: (a) *decay may order, never exclude* (strongest reading, most invasive) · (b) *may exclude, but traceably and recoverably* (minimal) · (c) *may exclude only where the member has a live route back* (the detect→ask surface exists as an API with no UI). **⭐⭐ THE RATIO, placed where the ruling can see it**: in the live ranking a member's explicit confirmation is worth `0.15 × 0.15 = 0.0225`; elapsed time is worth up to `0.40`. **Time outweighs the member's own word roughly 18:1 in deciding what MAIA remembers.** ⛔ Not a defect to fix here — any ruling on *decay may affect salience* is implicitly a ruling on that ratio. **⚠️ ACT 2 CANNOT BE ANSWERED BY `selectionTrace` ALONE, and this is recorded BEFORE it runs rather than discovered inside it**: the trace is built after `deduped`/`topBullets` — correctly — but `deduped` derives from rows the SQL already truncated to 12 *using the decay-bearing score*, so the trace is structurally blind to the decay cut and sees only the `maxBullets` cut. Same for `recordRetrievedCandidates()`, which records survivors. It CAN answer *does decay decide which 5 of the 12 become bullets* (it does); it CANNOT answer *which rows never entered the 12* — that needs a decay-free counterfactual against the same pool, i.e. the F2 method. Widening the instrument vs narrowing ACT 2's claim is a founder call. **⚠️ FOUR OBSERVATIONS ROUTED OUT, ⛔ NO LANE, ⛔ NONE REPAIRED**: (1) `calculate_decayed_confidence` is declared `IMMUTABLE` and calls `NOW()` — constant-foldable and admissible in expression indexes, where a time-dependent value would freeze; no index uses it today, and the function serves paths beyond this lane; (2) `rankCandidates` (`MemoryBundle.ts:422`) sorts two incommensurable scores on one axis — developmental rows keep the decay-bearing SQL score, turns and breakthroughs get a different decay-free formula, and the guard is a falsy check so a genuine `0` is recomputed; (3) the `recall_count` term is a feedback loop whose writer (`DevelopmentalMemory.ts:407`) sits on a different retrieval path from its reader, so the loop is partial rather than closed; (4) the detect→ask surface has no member-facing route — `/api/memory/stale-preferences` exists, `PreferenceConfirmation.tsx` is mounted nowhere, `shouldPromptForConfirmation` has no callers. **Standing: ACT 1 COMPLETE · CLAUSE 2 SATISFIED STRUCTURALLY · CLAUSE 1 UNSETTLED, RETURNED FOR RULING · BOTH IMPLEMENTATIONS EXAMINED, NEITHER CHANGED, NEITHER PREFERRED · ACT 2 ⛔ NOT OPENED · `ANTECEDENT-IDENTITY-01` UNTOUCHED · NO MIGRATION · NO SCHEMA CHANGE · NO RUNTIME CODE MODIFIED · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-15 — `CONTINUITY-REMAINDER-01`** — ACT 1/2 + P1 re-adjudication complete on `claude/dazzling-goldberg-wg8s4s`. Production `e57ca1baa` untouched. Measured defect: HOP 1 correctly detects retrospective class membership but illegitimately treats that membership as antecedent identity; P1’s prior green was recovery by lexical coincidence and no longer stands as evidence of lawful antecedent resolution. P1 acceptance therefore cannot discriminate a lawful resolver from the current unlawful mechanism; N1/N2 remain undisturbed. Repair is not authorized. Next question: what evidence is sufficient for antecedent identity once retrospective intent is established? Current corpus only contains referential evidence when the member restates the object; `FIRST-ASK-OPAQUE-MEMORY-01` remains unopened and may be required to answer the first-ask case. P1 remains a recovery regression only; its lawfulness standing is withdrawn. A green `must recover 22` must not be read as evidence of lawful antecedent resolution. Records: `docs/programme/CONTINUITY-REMAINDER-01_*`; bridge candidate `0f58a7f93` on `feature/l1-production-base`, unmerged.
- **LATEST — 2026-09-14 — ⭐⭐ `S3` O1 PRODUCTION ACT RUN AND WITNESSED · SCHEMA DEPLOYED · S3 PRODUCTION CLOSED.** Candidate `637c115d10906aec90091d952710a7c00197dcf3` (merge of #1288). Records `docs/programme/S3-O1_PRODUCTION_WITNESS_2026-09-14.md` (witness) · `S3-O1_PRODUCTION_OBSERVATIONS_2026-09-14.md` (routed out). **Both authorizations the prior bullets withheld were given as named founder acts, head-pinned, and spent.** ⭐ **THE ACT, AS RUN**: a temporary detached worktree at the candidate supplies the CODE; `PROJECT_DIR=$HOME/MAIA-SOVEREIGN` supplies the RUNTIME ROOT; the two are never the same object again. **WITNESSED**: `Applied 3 new migrations` — `20260913000001_ask_authorization_acts` · `…000002_disclosure_boundary_developmental_ask` · `…000003_disclosure_gesture_authorize_sections`, exactly the proved scope and no more · reader swapped · `GIT_COMMIT=637c115d1` verified three ways (`printenv` == `Config.Env` == asserted) · **Co-Lab release gate `33 passed · 0 failed · 0 warned`** · rollback custody proved · `S3 schema-first act complete`. **⭐⭐ THE DS-02B LINE WAS RECOVERED FROM THE ARTIFACT, NOT FROM THE OUTCOME.** The transcript head was lost, and ⛔ the successful tail was refused as grounds to backfill it. `acquire_deploy_lock()` writes its holder record INTO the lockfile and nothing removes it on a flock host, so it was read directly: `/home/soullab/MAIA-SOVEREIGN/.deploy.lock` · `entry=s3-schema-first-runbook` · `target=637c115d…` · mtime `15:20:23Z`, one second after `started=15:20:22Z`. Self-identifying three ways — path, entry, target. ⭐ **`checkout_head=2d7873c86 ≠ target_sha=637c115d1` is the most valuable line in the file**: the shared checkout was sitting somewhere else entirely and the act deployed the named candidate anyway — the immutable-SHA architecture observed surviving its own motivating condition, in production, rather than argued for. Independently corroborated by `DEPLOY_LANE=deploy-lane` in the running container, a variable exported ONLY inside `acquire_deploy_lock()` and without which the Dockerfile refuses to build. **⭐⭐ THE WITNESS CARRIES TWO EVIDENCE CLASSES AND WILL NOT MERGE THEM**: WITNESSED (a literal line or a durable artifact read directly) vs ENTAILED (a guard that provably precedes something that demonstrably happened and exits rather than continuing). ⚠️ **Entailed, ⛔ NOT witnessed**: the *two* runtime-root confirmations (root custody surviving re-exec — the discriminator this lane itself named), the four helper hash-matches, the exact-three scope guard, the build, the image verify, and the recovery pin. *Entailment from source that has been read is honest evidence; calling it witness is the one move this lane spent the week refusing.* A future act should capture the transcript from its first line so that section shrinks. **⚠️ THREE OBSERVATIONS, ROUTED OUT, ⛔ NO LANE OPENED, ⛔ NONE REPAIRED**: **(1)** the swap is `compose up -d` with no service argument and recreated **nine** containers, not one — all healthy, but *"swap the reader"* names a narrower act than the one that runs, and a failure in any of the other eight would enter the post-swap recovery band while the recovery act restores the READER's image only; **(2)** `529 already applied · 480 total` compares **ledger rows** (measured BEFORE the loop) against **files on disk** — ⚠️ and the difference is **not** `529−480`: of the 480 files, 3 were pending, so 477 were already represented and **52** ledger filenames have no current file. *An earlier draft said 49, assuming every file on disk was in the ledger — the exact assumption this observation exists to question.* Bookkeeping, ⛔ no provenance manufactured for those rows; **(3)** ⭐⭐ **the migration runner records THAT a schema change happened in a different session from the change itself** — each file's own `BEGIN`/`COMMIT` overrides the runner's, so the trailing `DO` runs **outside the runner's intended explicit transaction, in its own autocommit transaction** (⛔ not *outside any transaction* — psql autocommits; the finding is the boundary, not the absence: **the file can commit its DDL before every statement in it has completed under one atomic boundary**); the ledger `INSERT` is a separate `psql` invocation carrying **no `ON_ERROR_STOP`**, so ⚠️ a **server-side SQL error** there does not become the script-failing exit status the DDL invocation relies on (⛔ narrower than *silent* — fatal psql and connection errors still terminate); and the failure-path `ROLLBACK` acts on an already-disconnected session. *This is the same law S3's post-cognition tail was repaired to satisfy, one layer down* — ⛔ and naming it is not authorization to fix it; the runner serves every migration in the system. **Standing: S3 PRODUCTION ✅ CLOSED · candidate `637c115d1` LIVE · three S3 migrations APPLIED · DEPLOYMENT-SAFETY-01 / 02 / 02A / 02B ✅ DISCHARGED IN PRODUCTION · ROLLBACK-PRIMITIVE and THREAD-STORE CONCURRENCY findings STILL ROUTED OUT AND CLOSED · ⛔ NO FIFTH DEPLOYMENT ARCHITECTURE LANE · NEXT: WRITER'S STUDIO (`WS-DISCLOSURE-ORIENTATION-01`, census first).**
- **LATEST — 2026-09-14 — ⭐⭐ `S3` ROUTE INTEGRATION ACCEPTED / CLOSED (founder ruling) · ⛔ MERGE STILL UNAUTHORIZED.** Candidate `6ec5ff1d`; witness `e5241151`; records `docs/programme/S3-ROUTE-INTEGRATION-REPAIR_2026-09-14.md` (repair) · `…-REPAIR_WITNESS_RESULT_2026-09-14.md` (witness) · `S3-ROUTE-INTEGRATION_CLOSURE_2026-09-14.md` (ruling). **The prior candidate `8e5da279` passed R1–R12 and post-repair F8 and was still NOT acceptable**: two integration-only crash falsifiers found the post-cognition tail non-atomic. ⭐ **RI-X1** — turn persisted, completion absent → a retry reported `INTERRUPTED` while a canonical result existed, which is the frozen **Ruling 6** violation from the other side (incompletion is not resumable by replay, but a COMPLETED execution must be RECOVERABLE). ⭐ **RI-X2** — `confirmDisclosureCrossed` returned `false`, the route ignored it, HTTP 200 with the receipt still `attempted`. **⭐ ONE BOUNDED REPAIR, AS RULED**: the persisted turn, every receipt confirmation and the completion now commit in ONE `transaction()` or none do. ⭐⭐ **THE RETURN TYPES ARE THE REPAIR, NOT THE DISCIPLINE** — `confirmDisclosureCrossedWithClient` returns `Promise<void>` and THROWS; `recordCompletionWithClient` throws `CompletionNotRecorded('conflict'|'no_consumption')`; *RI-X2 is what ignoring a boolean looks like, so there is no longer a value to ignore.* ⛔⛔ **ATOMICITY, NEVER AUTHORITY**: the transaction opens AFTER the body has crossed; the claim remains the `ON CONFLICT DO NOTHING` insert, and `transaction()` is still a plain BEGIN at READ COMMITTED with no row lock. ⛔ **ROLLBACK DOES NOT UNDO A CROSSING** — receipts stay `attempted` (permanent, truthful, queryable) and the act stays consumed-and-incomplete; what the abort prevents is the SECOND wrong, a canonical outcome recorded as accountable when its accountability was never established. ⛔ The answer text is WITHHELD on rollback. ⭐ **`askRuntimeCannotWrite` UNCHANGED — not by one allowlist entry** (⛔ weakening a constitutional instrument to pass a change is the move this lane exists to refuse); the route is simply no longer as narrow as the Ask library, so the new law sits at its own address: **`askRouteEffectFamily`** walks the route's transitive VALUE-import graph (31 files) and admits exactly conversation (`ask_threads`·`ask_turns`) · authority (`ask_authorization_acts`·`ask_authorization_consumptions`) · posture (`runtime_consent_state`) · crossing (`context_disclosure_receipts`) — ⛔ **never the Work**, whose eleven tables are named explicitly and asserted disjoint so a guard reduced to a tautology still fails. **⭐ PHYSICAL WITNESS (fresh disposable PostgreSQL shadow, real HTTP route, real Next server; ⭐ ONLY the structured-model transport replaced AT THE WIRE downstream of prompt assembly — a loopback stub, ⛔ no fixture body to any provider; faults were shadow-only `BEFORE` triggers reporting COUNTS ONLY, ⛔ no identifier/excerpt/digest/offset)**: **R1–R12 PASS · post-repair F8 PASS · R10 PASS** (`recorded` → `already` with the ORIGINAL timestamp → `conflict` → `no_consumption`; expected-refusal `console.error` calls **0**; an unexpected failure on the same path threw AND stayed loud). **⭐ RI-X1 PASS at the LATEST point** — `in_tx_maia_turns=1 in_tx_crossed_receipts=2` already applied when the completion failed; after rollback turn ABSENT · both receipts `attempted` · completion ABSENT · act consumed+incomplete · HTTP 500 `answer_not_recorded`; and the retry's `INTERRUPTED` is **now TRUE**. **⭐⭐ RI-X2 PASS with N=2, the STRONGER proposition** — receipt 1's `attempted → crossed` UPDATE was allowed to SUCCEED inside the transaction (`in_tx_crossed_before_abort=1`) and rollback restored **both** to `attempted`: *a durable change that had actually been applied was undone*, ⛔ not merely an early error caught before anything changed. **⭐ COMMITTED-BUT-RESPONSE-LOST NEEDED NOTHING NEW** — `S3-F6` carries it frozen, `R4` carries it at route scope (`ALREADY_COMPLETED` · IDENTICAL completion identity · 0 new body reads/receipts/cognition/consumption); ⛔ the instruction was to report a gap rather than build one, and there was no gap. **Static field**: freeze diff **EMPTY** · substrate guards **12/12** (G12 new; G10 updated to track the renamed call, law unchanged) · substrate + frozen typechecks PASS · matrix **LETHAL + DISCRIMINATING** · `askRuntimeCannotWrite` **8/8 unchanged** · `askRouteEffectFamily` **5/5** · ⭐ **real project typecheck with dependencies installed: 229 vs baseline 239, 0 regressions** — the same numbers as `8e5da279`, so the repair regressed nothing. **⚠️ TWO CAVEATS KEPT EXPLICIT, ⛔ never silently normalized**: **(1)** the R6 loser returned 500 from an **`ask_turns_pkey` collision on the AUTHOR turn** — `appendTurn` computes `MAX(turn_index)+1` in-statement, it happens BEFORE any claim, the statement is byte-identical to `8e5da279`, and R6's own proposition is undisturbed; ⭐ *the lane that finds a defect does not thereby own it* — **ROUTED OUT** to `docs/programme/THREAD_STORE_CONCURRENT_TURN_COLLISION_FINDING_2026-09-14.md`, ⛔ lane NOT opened, ⛔ **do not repair it opportunistically** (`appendTurn` serves every Ask lane); **(2)** **56 of 480 legacy migrations refused** on the shadow (absent extensions/predecessors) — every table the route reaches was created and each is named in the witness §6. **⛔ CLOSURE IS NOT AUTHORITY.** Merging these migrations to `clean-main-no-secrets` is itself latent schema-deploy authorization by the 2026-09-07 finding. **Standing: S3 ROUTE-INTEGRATION ✅ ACCEPTED / CLOSED · candidate `6ec5ff1d` · witness `e5241151` · CLASS-B FREEZE INTACT @ `2255b60d` · THREAD-STORE CONCURRENCY OPEN · ROUTED OUT · MERGE ⛔ REQUIRES EXPLICIT AUTHORIZATION · SCHEMA DEPLOY ⛔ REQUIRES EXPLICIT AUTHORIZATION · PRODUCTION UNTOUCHED.** ⚠️ **The last three clauses are SUPERSEDED as of 2026-09-14T15:20Z** — both authorizations were given as named, head-pinned founder acts and the act ran; see the O1 production bullet above. Kept verbatim as the state at the time of that ruling, ⛔ never edited to read as if it had always said otherwise.
- **LATEST — 2026-09-14 — `S3` ROUTE INTEGRATION CANDIDATE BUILT (founder act opened it) · GUARDS 11/11 · ⛔ R1–R12 UNRUN.** Record `docs/programme/S3-ROUTE-INTEGRATION_2026-09-14.md`. **Order as built**: derive body requirement **server-side** → assemble without body → append the author's question → *(no body)* answer · *(body, no act)* mint ONE opportunity + `BODY_AUTHORITY_REQUIRED` (pendingAskRef · workRef · **section IDS only**, ⛔ no headings — Q1(a); a heading is authored disclosure and that question is PARKED) · *(ACT 3)* re-derive → **SCOPE BEFORE CLAIM** → `claimAct` → **coordinates must match this Ask** → one boundary per section, all `may_cross` → ⭐⭐ only now `loadRevisionContent` → W2 checked independently → cognition → persist turn → confirm receipts → `recordCompletion`. **⚠️ THE TRAP INSIDE THE DERIVATION, NAMED**: `section-run` refs **carry section ids while requiring only `position`** — unioning `sectionIdsOf` across ALL refs would demand and authorize body disclosure for sections whose prose is never read. `deriveBodyRequirement()` unions over **body-requiring refs only**; a client superset is ignored, never honoured. **W2 is checked TWICE on purpose** — the derivation makes it true by construction, and the post-assembly check refuses if derivation and authority ever diverge. **⭐ TWO THINGS THE TOOLS CAUGHT.** (1) `DisclosureBoundary`/`DisclosureGesture` were widened in SQL but **not in TypeScript** — the compiler refused the integration until both mirrored the migrations, one value each; *the types are the second enforcement of the same vocabulary law, and a single `as` cast would have silenced them.* (2) ⚠️ **`askRuntimeCannotWrite` collision**: it asserts over `lib/manuscript/ask` that only `ask_threads`/`ask_turns` are written, and the claimant originally landed inside that directory. ⛔ **REJECTED: widening the allowlist** — weakening a constitutional instrument to pass a change is the move this lane exists to refuse. ⭐ **TAKEN: moved the substrate to `lib/disclosure/authorizationAct.ts`**, beside the boundary and the receipt, which is its honest home — it is disclosure authority, not Ask-conversation code. The test is **unchanged and still passes**. ⚠️ **But its guarantee narrowed, and the route header said something now false** — corrected in place: the Ask library still writes only the thread; ⛔ the ROUTE now also writes the opportunity, its consumption, the consent state and the receipts — ⭐ **none of them is the Work: every one is a record of AUTHORITY, and the Work itself is still only read.** Whether the test's law should be restated at route scope is a founder question, ⛔ not answered here. **⭐ EXPECTED-REFUSAL SEAM LANDED** as authorized: `queryWithExpectedRefusal()` in `lib/db/postgres.ts` — ⛔ **not** a `quiet`/`suppressErrors` flag (a blanket switch spreads); an explicit opt-in naming the exact condition, following the discipline that file's own missing-table ruling already set. The discriminator is **semantic**, ⛔ never a bare `P0001` (which is every `RAISE EXCEPTION` in the database). Expected refusal → `{refused:true}`, **nothing logged, so no act reference or completion identity is emitted**; anything else stays LOUD. It cannot delegate to `query()`, which has already logged by the time it rethrows. **GUARDS 11/11** — new: **G8** `loadRevisionContent` reachable EXACTLY ONCE and ordered claim→boundary→load · **G9** no client-supplied `allowBody`/`may_cross` · **G10** completion identity names the persisted turn, ⛔ never the answer text · **G11** scope precedes claim so an incomplete client set cannot spend the opportunity. **FREEZE INTACT** — diff vs `2255b60d` empty; frozen matrix still LETHAL + DISCRIMINATING. ⚠️ Route and substrate typecheck clean but for unresolvable modules (no project `node_modules` here) — ⛔ not reported as a pass. **⛔ R1–R12 UNRUN** (all need a disposable shadow), and **the repaired Class-A proposition is OWED**: on the integrated candidate the exact S3-F8 path must no longer exist — ⛔ not by rewriting the historical F8 RED, which stays historical evidence of what canonical did at `833ec87f`, but by a NEW post-repair route witness. **Standing: ROUTE INTEGRATION CANDIDATE BUILT · GUARDS 11/11 · R1–R12 UNRUN · POST-REPAIR F8 WITNESS OWED · MERGE NOT AUTHORIZED · SCHEMA DEPLOY NOT AUTHORIZED · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-14 — `S3` ⭐ W-A + W-B DISCHARGED (founder-run) · M-PHASE DISPOSITIONS TAKEN.** Witness `b68eb10d` on `chore/s3-wa-wb-witness-20260913`, **parent IS the candidate `3a65ad1b`** (verified in-session; single-file record). **W-A**: 8 independent PostgreSQL connections raced one opportunity → **1 claim · 7 losers · 1 consumption row**; foreign-member and expired claims produced **NO ROW AT ALL**, not a row later read as refused. ⭐ **S3-F1 was necessary and not sufficient; it is now sufficient** — the physical mechanism establishes what the single-threaded double could only model. **W-B**: all six PASS — interrupted · lost response (same completion identity) · outcome deleted (completion survived dangling) · unclaimed+expired · **delete custody** (direct deletes AND both truncates refused; thread/Work cascades succeeded) · **completion monotonicity** (`recorded` → `already` with original timestamp → `conflict` → `no_consumption`). ⭐ The witness's own scope statement is right: zero receipts before and after is a **SUBSTRATE claim** — ⛔ it does not pretend a future integrated route cannot cross incorrectly. Gates: guards 7/7 · frozen TS PASS · frozen matrix LETHAL+DISCRIMINATING · **freeze diff @ `2255b60d` EMPTY** · canonical ship typecheck **229 vs baseline 239, 0 regressions**. **⚠️ DISPOSITION 1 — the substrate typecheck was RED, and both obvious answers were refused**: ⛔ weakening `noUncheckedIndexedAccess` manufactures a pass; ⛔ leaving it permanently red teaches people to ignore it and *decays into prose*. ⭐ TAKEN: `scripts/typecheck-s3-substrate.mjs` keeps the flag and allows **exactly one inherited diagnostic BY NAME**, keyed on file + error code + a semantic message fragment — ⛔ **never a line number** (D1's rule). **Fail-closed**: a mis-keyed allowance FAILS, never silently passes; a stale allowance that stops firing is REPORTED so the exemption is removed. ⚠️ **Unverified in this container** (no project `node_modules`) — the founder's run is the evidence of record. ⚠️ **FINDING HANDED ON, NOT TAKEN**: `insertOne<T>()` in `lib/db/postgres.ts` promises `T` and returns `result.rows[0]` (`T | undefined`) — genuinely unsound, surfaced only by the stricter flag, ⛔ unchanged since the freeze and **unreachable from S3** (the claimant uses `query()` directly). *The lane that finds a defect does not thereby own it*, and the fix changes runtime behaviour for every existing caller. **⚠️ DISPOSITION 2 — expected-conflict logging**: `query()` logs `console.error` with SQL **and params** then rethrows, so a governed refusal working exactly as designed surfaces as an unexpected error. ⭐ **And one thing the review did not name: that log line carries the act reference and completion identity.** ⛔ Not a content leak — both are identities, no authored character — **but this lane's own rule is *a refusal is not an occasion to disclose*.** ⛔ Not repaired here: a pre-check reintroduces a read that can be wrong, and making the trigger not RAISE weakens the refusal that **is** the authority; the lawful fix is an opt-in expected-refusal path through the shared helper, which is a founder call. **Recorded as a NAMED ROUTE-INTEGRATION OBLIGATION**: before S3 serves a member, the conflict path must not log as an unexpected error and must not emit the refused identities. Record: `docs/programme/S3-M_PHASE_DISPOSITIONS_2026-09-14.md`. **Standing: CLASS-B FREEZE INTACT @ `2255b60d` · W-A + W-B DISCHARGED · substrate typecheck disposition TAKEN · `insertOne` finding HANDED ON · conflict logging OBLIGATION NAMED · ⛔ ROUTE INTEGRATION NOT OPENED (separate founder act) · MERGE NOT AUTHORIZED · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-13 — `S3` M1–M3 AMENDED PER FOUNDER REVIEW · GESTURE RULED · GUARDS 7/7.** Record updated: `docs/programme/S3-M1-M3_SUBSTRATE_CANDIDATE_2026-09-13.md`. **⭐ AMENDMENT 1 — MY DELETION JUDGMENT CALL WAS WRONG, AND THE TRUTH IS BETTER.** I had claimed a DELETE trigger strict enough to stop pruning would also break lawful Work deletion, so custody was static-only. ⛔ Withdrawn — **PostgreSQL draws the distinction exactly**: in a child's BEFORE DELETE trigger, a lawful PARENT CASCADE has already removed the parent while a direct/pruning DELETE has not. So `DELETE consumption` refuses while its act exists and allows once gone; `DELETE act` refuses while Work AND thread exist and allows once either is gone. **Both tables now carry database guards plus BEFORE TRUNCATE refusals** — without which `TRUNCATE ask_authorization_consumptions` would bypass every row-level guard and **resurrect every live authorization at once**. ⚠️ **G2's claim narrows**: it proves only that *ordinary repository code contains no deletion path* — defence in depth, ⛔ no longer the constitutional guarantee alone. A row trigger cannot defeat the database owner and does not pretend to; privileged schema mutation stays governance territory. **⭐ AMENDMENT 2 — A REAL COMPLETION-SEMANTIC DEFECT, FOUND IN FOUNDER REVIEW.** `recordCompletion()` carried `AND completed_at IS NULL`, so an already-completed row **never reached the monotonic trigger** — same completion, a **DIFFERENT (contradictory) completion**, and *no consumption at all* ALL produced zero rows and were indistinguishable behind a `void` return. ⭐ The safety invariant survived (no second completion was possible) **but the substrate did not truthfully distinguish the outcomes it claimed to distinguish.** Repaired: predicate dropped, `COALESCE` preserves the original timestamp, the trigger adjudicates, and the prior state is read in the same statement so one snapshot answers both questions; `recordCompletion()` now returns **`recorded | already | conflict | no_consumption`**. ⛔ *Nothing happened must never masquerade as completion recorded.* **⭐ GESTURE RULED: `authorize_sections`** — *the member explicitly authorized MAIA to read the named section set for this single developmental Ask*; ⛔ not consent, standing permission, passage authority or generic access. One gesture may yield N section-scoped receipts each carrying it truthfully — **the gesture names the member's act, not the count of boundaries it caused.** Landed as its own migration `20260913000003`, ⛔ not folded back into M2. **⭐ TERMINOLOGY CORRECTED (no freeze reopening needed — the frozen contract already behaves this way)**: the ACT-2 row is the identity of **one single-use authorization OPPORTUNITY**, minted at `BODY_AUTHORITY_REQUIRED` *before the member has done anything*; ⛔ it is never evidence a gesture occurred. **The CONSUMPTION row is the first durable fact that a human acted.** *"Member-act identity minted at pause" quietly suggested the human acted before they did.* **GUARDS 7/7** — G6 (gesture migration adds exactly one value, touches nothing else) and G7 (both tables carry BEFORE DELETE + BEFORE TRUNCATE, and the DELETE predicate is a **parent-existence test**, not a manifest or a flag) added. **FREEZE RE-VERIFIED INTACT**: diff vs `2255b60d` over the frozen paths → empty. ⚠️ Substrate typecheck still resolves everything except `pg` (no project `node_modules` here) — ⛔ not reported as a pass. **W-B EXPANDED TO SIX CASES**: interrupted · lost response · outcome deleted · unclaimed+expired · **delete custody** (direct deletes REFUSED, lawful cascade removes both) · **completion monotonicity** (same idempotent, different REFUSED, unclaimed cannot falsely report completion recorded). ⭐ *Neither repair changed the V2 architecture — they make the reasons V2 was chosen structural at the database boundary rather than leaving the last dangerous edge to repository discipline.* **Standing: CLASS-B FREEZE INTACT @ `2255b60d` · M1·M2·M2b·M3 CANDIDATE AMENDED · GUARDS 7/7 · W-A and W-B NOW AUTHORIZED (owed) · ROUTE INTEGRATION NOT AUTHORIZED · MERGE NOT AUTHORIZED (merge is schema-deploy authorization) · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-13 — `S3` M1–M3 SUBSTRATE CANDIDATE LANDED (V2 two-table shape TAKEN).** Record `docs/programme/S3-M1-M3_SUBSTRATE_CANDIDATE_2026-09-13.md`. **M1** `20260913000001_ask_authorization_acts.sql` — `ask_authorization_acts` (write-once, every UPDATE refused by trigger; coordinates + expiry; ⛔ no permission columns) + `ask_authorization_consumptions` (`act_id UNIQUE`, monotonic completion). **M2** `20260913000002_...` — **exactly one** boundary value `writers_studio.developmental_ask->maia_cognition`; ⛔ no other vocabulary moved. **M3** `lib/manuscript/ask/authorizationAct.ts` — `mintAct · claimAct · recordCompletion · readAct`, ⛔ **no route wiring**. ⭐ **THE INSERT IS THE CLAIM**: `INSERT…SELECT…WHERE member AND expires_at > NOW() ON CONFLICT (act_id) DO NOTHING RETURNING` — no preceding SELECT, no row lock, no `transaction()` (plain BEGIN at READ COMMITTED — what defeated `093379e8d`); member binding and expiry sit in the predicate so a foreign or expired act **never produces a row to conflict over**. **Three design laws made structural**: ⛔ **no status column** — pending/interrupted/completed derived from two positive facts; ⭐ **`completion_ref` is deliberately NOT a foreign key** — SET NULL would let deleting the outcome erase the completion and resurrect the act, RESTRICT would defeat author sovereignty, so the pointer may dangle and `completed_at` is the durable fact (**COMPLETED · OUTCOME NO LONGER HELD**); ⛔⛔ **expiry is a predicate, unreachable once consumed — no pruning may delete these rows** (completed → expires → pruned → no consumption visible → authority resurrected). ⚠️ **ONE JUDGMENT CALL FLAGGED**: cascade deletion from the Work/thread is permitted (no Work ⇒ no Ask ⇒ nothing to resurrect); every other deletion path is refused by a **static guard rather than a trigger**, because a DELETE trigger strict enough to stop pruning would break lawful Work deletion. **⭐ FIVE GUARDS, 5/5 PASS** (`npm run guards:s3-substrate`, ⛔ NOT part of the frozen suite): no forbidden columns on the identity row · no DELETE anywhere · claim is ON CONFLICT with no FOR UPDATE and no `transaction()` · `completion_ref` not an FK · M2 adds exactly one value. ⭐ **Every scan strips comments first — the C21 lesson applied BEFORE it could bite**: M1 documents the forbidden column names so their absence reads as deliberate, and a raw-source scanner would fail the file for stating its own compliance. **FREEZE VERIFIED INTACT**: `git diff --stat 2255b60d -- tests/constitutional/s3/ tsconfig.s3-constitutional.json` → **empty**; frozen typecheck PASS; frozen matrix LETHAL + DISCRIMINATING + reference clean. ⚠️ Substrate typecheck resolves everything except `pg` (no project `node_modules` here) — ⛔ **not reported as a pass**; the founder's run is the evidence of record. **⚠️⚠️ TWO THINGS OWED BEFORE USE: (1) a GESTURE VALUE is still missing** — `gesture` admits only `ask_maia|work_with_this|widen_focus` and the S3 act is *the member authorizing named sections*, which is none of them, so **M2 alone is NOT sufficient for a live crossing**; adding it inside M2 would be the accompanying vocabulary redesign that change was separated to avoid — separate governed act. **(2) THE MERGE IS ITSELF A SCHEMA-DEPLOY AUTHORIZATION** — by the 2026-09-07 finding, merging these migrations to `clean-main-no-secrets` means the next unrelated full deploy applies them. ⛔ **Do not merge ahead of W-A and W-B.** **Standing: CLASS-B FREEZE INTACT @ `2255b60d` · M1–M3 CANDIDATE on a non-canonical branch · GUARDS 5/5 · GESTURE VALUE OWED · W-A (independent connections racing one act) OWED · W-B (interrupted · lost response · outcome deleted · unclaimed+expired) OWED · ROUTE INTEGRATION NOT AUTHORIZED · MERGE NOT AUTHORIZED · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-13 — `S3` B-iv DESIGN OPENED AND DRAFTED.** Record `docs/programme/S3-B-IV_DESIGN_2026-09-13.md`. **⭐ THESIS (founder): S3 joins an atomic single-consumption claim with durable completion recovery, keyed by a FIRST-CLASS MEMBER-ACT IDENTITY; request identity may correlate execution, ⛔ never substitute for act identity** — which is what keeps DC-2 from creeping back through a convenient existing column. **Act identity**: minted by the surface witnessing the gesture · opaque · stable across transport retry · distinct for two gestures with byte-identical prose · bound server-side to the Ask · ⛔ never sufficient authority. **⭐ ATOMICITY LAW: the successful database mutation IS the claim; a prior read is never the authority** — ⛔ `SELECT unclaimed? → do work → mark claimed` is forbidden; ⛔ `transaction()` contributes no guarantee (plain BEGIN, READ COMMITTED, no row lock); ⛔ no advisory lock, the census found no reason. **Two shapes competed against the frozen law and BOTH account for every transition on paper**, so selection is made on *what each makes easy to get wrong*: **V1** (one table, OAuth-style conditional UPDATE) puts claim state on the identity row — **the §10.6 prohibited shape `{ref, sectionId, authorized:true}` is ONE COLUMN AWAY and nothing structural refuses it**; **V2** (two tables, receipts-style `INSERT…SELECT…ON CONFLICT DO NOTHING` — the construction `threadStore.appendTurn` already uses) keeps the act row write-once and trigger-frozen, so adding `authorized` to it is **refused by its own trigger**, and it **is** the join the census described — the consumption row is the mail lane's *attempt* and `completion_ref` is its `provider_message_id`. V1's real advantages (one lock target, no orphan, no join, smaller migration) are stated, not suppressed, and ⛔ not decisive: *the lane's entire history is of permission arriving by a convenient adjacent field.* **⭐ V2 RECOMMENDED — ⛔ recommended, not taken; one/two tables is a founder ruling.** ⭐ **Both derive the three states from POSITIVE facts only** — no claim → pending · claim without completion → **interrupted** · claim + completion → recovered — so **no `interrupted` declaration is stored**, matching the two-lane law that a system may not store a confident negative it cannot know. **`completion_ref` candidate**: the identity of MAIA's completed turn (`ask_turns` is append-only, UPDATE refused by trigger) — ⛔ not the receipt (N per act, not singular), ⛔ nothing textual. ⚠️ **`ask_turns` permits DELETE, so the load-bearing law is: a dangling completion reads as *completed, outcome no longer held*, NEVER as *not completed* — otherwise member deletion becomes a replay loophole.** **TTL** is satisfied structurally (expiry appears only in the claim predicate, unreachable once claimed) with one hard operational consequence: ⛔⛔ **expiry is a PREDICATE, never a DELETE — no pruning job may touch this table**, or a sweep could delete a completed act and resurrect it. **BOUNDARY answered separately**: `writers_studio.focus->maia_cognition` is the Focus path, ⛔ **not** this crossing, so widening is required — ⭐ **exactly ONE CHECK value needs adding**; `source_class='work'` · `participation_basis='member_invoked'` · `scope_kind='section'` · singular `section_ref` · `authorized_by='member'` all already admit what S3 needs. ⛔ A convenient nearby boundary is not evidence it is the same boundary, and this does not travel with the storage ruling. **OWED before migration — two physical witnesses the doubles cannot prove, ⛔ neither amending the frozen suite: W-A real concurrent database claim (independent connections racing one act, exactly one wins — what S3-F1 could only model) · W-B crash/recovery (claim survives process loss; completion recoverable where present, non-resumable where absent).** **Standing: CLASS-B FREEZE INTACT @ `2255b60d` · B-iv CENSUS CLOSED @ `63612fa5` · B-iv DESIGN DRAFTED · V2 RECOMMENDED NOT TAKEN · W-A + W-B OWED · MIGRATION NOT AUTHORIZED · PRODUCT IMPLEMENTATION NOT AUTHORIZED · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-13 — `S3` ⭐ CLASS-B FREEZE TAKEN @ `2255b60d` · B-iv SUBSTRATE CENSUS COMPLETE (READ-ONLY).** Records `docs/programme/S3-CLASS-B_FREEZE_2026-09-13.md` + `S3-B-IV_DURABLE_TRANSITION_SUBSTRATE_CENSUS_2026-09-13.md`. **Frozen**: transition contract · S3-F1…F7/F9/F10 · eight defeat candidates · conforming reference double · execution matrix · classified-collateral record · both instruments. Reopening requires a **formal freeze reopening with evidence the LAW or INSTRUMENT was wrong** — ⛔ convenience, implementation difficulty, and *"storage would be simpler if F7 meant X"* are all insufficient. **⭐ COLLATERAL RULE RATIFIED**: classified collateral = a necessary consequence of faithfully embodying the named wrong architecture (admissible evidence); ⛔ unclassified = accidental incompetence or unrelated defect (isolation defect, repaired before freeze). **The test: if removing the collateral requires making the candidate cease to embody its constitutional error, that collateral is IRREDUCIBLE.** ⭐⭐ **DC-2 stays as-is — once `request == act`, the corruption propagates into concurrent claim, replay identity, completion recovery and prose-mutated replay; its four deaths are four observable consequences of ONE wrong identity primitive.** Lesson B-iv inherits: **act identity is not an implementation detail of idempotency — it is upstream constitutional identity from which concurrency, replay and recovery semantics follow.** ⚠️ Two limitations CARRIED by the freeze, not resolved: the reference double proves the laws are **mutually satisfiable**, ⛔ never how to implement them (copy-forward prohibited except at contract level); and S3-F1 is **model-level only — DB atomicity NOT PROVED**, so B-iv owes a real concurrency witness against whatever mechanism it picks. **⭐⭐ CENSUS DECISIVE FINDING — every primitive exists, no object combines them, and the two strongest precedents sit on OPPOSITE SIDES of the gap**: `lib/auth/googleOAuthState.ts` has the atomic claim and **no completion identity** (knows *that* it was consumed, never what it became) — and it is the **only** conditional single-use claim in `lib/**`; `email_delivery_attempts` has the completion identity and **no claim** (`state: attempting|accepted|indeterminate|refused`, `provider_message_id` present iff accepted, `settled_at` NULL long after creation *is itself a finding*) — ⭐ **Ruling 6 reasoned out independently by the mail lane about a different boundary.** *S3's substrate is the join of those two precedents over an act identity that does not yet exist* — and **act identity has 0 precedents** (`lib/coachField/invitation.ts` locks an invite, not a gesture). Other findings: `transaction()` is a plain BEGIN at READ COMMITTED with **no row lock** — the exact fact that defeated `093379e8d`, so **S3 cannot inherit atomicity from the helper**; `FOR UPDATE` is widespread and `SKIP LOCKED` appears in two worker queues, ⛔ **no advisory locks anywhere**; `vault_erasure_queue` is a **sweep, not a completion model** (rows removed on success, errno-only failures, explicitly not an audit log); ⭐ **two independent lanes already ratified the same law** — `'attempting'`/`'indeterminate'` and the receipts' *"a crossing MAY have occurred and was not confirmed · no `withheld` state: it would assert a negative the database cannot prove"* — from which S3's `interrupted` inherits directly; receipts' singular `section_ref` + CHECK already makes Ruling 5's 1:N enforceable. ⛔ Census is repository truth only — **no live or shadow database read**. **Standing: CLASS-B FREEZE INTACT · B-i/B-ii/B-iii CLOSED · B-iv AUTHORIZED, CENSUS COMPLETE · B-iv DESIGN NOT OPENED · STORAGE DESIGN NOT TAKEN · MIGRATION NOT AUTHORIZED · DB ATOMICITY WITNESS OWED · PRODUCT SOURCE UNCHANGED · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-13 — `S3` CLASS B · B-ii + B-iii: ⭐ MATRIX LETHAL AND DISCRIMINATING.** Record `docs/programme/S3-CLASS-B_MATRIX_RESULT_2026-09-13.md`; corpus `tests/constitutional/s3/candidates.ts` + `matrix.ts`. **Eight disposable wrong machines, each the smallest competent embodiment of its named error; all 8 died on their named falsifier; the conforming reference passes all nine.** ⛔ The reference core is a **test double, not an implementation** — a Map with no table, transaction, lock, durability or schema; ⛔ **evidence, never a seed**: B-iv must not be derived from it or storage decisions get smuggled in from a double never designed to make them. **⭐⭐ THE FINDING — SOME CONSTITUTIONAL ERRORS HAVE AN IRREDUCIBLE BLAST RADIUS.** Six of eight killed exactly one falsifier. Two killed more, and ⛔ **neither is sloppiness nor repaired by narrowing**: *a candidate narrowed until it kills only its named falsifier would no longer be the error it claims to model.* So the matrix now separates **CLASSIFIED** collateral (declared with a stated reason) from **UNCLASSIFIED** (⛔ → repair candidate isolation) — only the second is a defect, and a reason must name why further narrowing would destroy the model. ⭐ **DC-2 request-keyed idempotency is the most destructive of the eight and necessarily so — it defeats FOUR of nine** (S3-F1: if identity IS the request, two concurrent requests are two identities and both claim · S3-F4: no act-level completion is consulted, so a reworded replay re-executes · S3-F6: a lost-response retry carries a new request id, so it is a new identity rather than a recovery · plus its intended S3-F2). **The census already proved this candidate is not hypothetical** (`request_ref → runtime_consent_state(request_id)`), which makes it the most important machine in the corpus. DC-3 prose-identity additionally kills S3-F10 — *an unknown REFERENCE cannot be refused by a machine in which the reference is not what identifies.* ⚠️ **CONCURRENCY IS MODELLED, NOT PROVED**: JavaScript is single-threaded, so the read-then-write race is modelled by an `await` between read and write (exactly what a DB round trip does) — **S3-F1 passing here is NECESSARY AND NOT SUFFICIENT; real atomicity must be proved against a database in B-iv's own witness.** **⭐ REPRODUCIBLE INSTRUMENT LANDED** per founder ruling: `tsconfig.s3-constitutional.json` (strict · noEmit · `tests/constitutional/s3/**`) + `npm run typecheck:s3-constitutional` + `npm run matrix:s3-constitutional`; ⛔ does not widen `tsconfig.ship.json`, cannot move the baseline, implies nothing about constitutional test code being production code. Both ran green here (typecheck exit 0 · matrix exit 0) with TypeScript/tsx resolved from a scratchpad because the container has no `node_modules` — ⭐ the commands are repository-defined, so **the founder's run is the evidence of record**; *"I happened to have TypeScript installed" is not an instrument.* **Standing: B-i CLOSED · B-ii COMPLETE · B-iii LETHAL + DISCRIMINATING · ⛔ FREEZE NOT TAKEN (founder act: contract · suite · candidate corpus · execution matrix) · B-iv REAL IMPLEMENTATION NOT AUTHORIZED · STORAGE UNCHANGED · PRODUCT SOURCE UNCHANGED · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-13 — `S3` CLASS B · B-i AUTHORED: TYPED TRANSITION CONTRACT + S3-F1…F7/F9/F10 LANDED.** `tests/constitutional/s3/transitionContract.ts` + `tests/constitutional/s3/falsifiers.ts`. ⭐ **Founder ruling: the transition law is a TYPED executable spec that types OBSERVABLE TRANSITION SEMANTICS, not internal state representation** — *type the law at the boundary, not the machine behind it*, which is what keeps step 10 shut. MAY TYPE: member-act identity · claim/replay semantics · completion identity · crossing and receipt cardinality · authority isolation · server-derived scope · observable outcomes. ⛔ MAY NOT TYPE: table shape · column names · status column or enum · TTL · expiry · locking · transaction mechanism · what a completion identity points at · the act reference's persistence shape · boundary-vocabulary widening. The outcome union is `executed | recovered | interrupted | refused` — **outcomes, not stored states**; no member may be read as naming a column. ⭐ **`authoredText` is carried DELIBERATELY as an inert payload** — without it the law *identity is the act, never the characters* is unfalsifiable and the prose-comparison defeat candidate is unbuildable; a conforming implementation never keys on it. ⭐ `request` is carried for the same reason: **keying on it must be BUILDABLE to be killable.** ⛔ **S3-F8 is NOT in the suite** — Class A, SPENT, and its evidence is a disposable-shadow witness, never a unit assertion. **⭐⭐ FREEZE GUARD RULED**: once B-iii proves lethality, contract and suite FREEZE before B-iv — lawful is *implementation fails suite → repair the implementation*; ⛔ forbidden is *→ reinterpret the contract → weaken the test*. Any change after B-iv needs explicit reopening with evidence the LAW or INSTRUMENT was wrong, never that the implementation was inconvenient. *That keeps the eighth machine from quietly domesticating the seven tests that killed its predecessors.* **Verification**: both files typecheck clean under isolated `tsc --strict` (exit 0). ⚠️ `tests/**` is NOT in `tsconfig.ship.json`'s include list, so these files are **outside the enforced no-regression gate** — the isolated run is their only typecheck evidence, and they cannot move the baseline. ⛔ Nothing was executed: the suite has no candidate to run against, which is B-ii. **⚠️ BRANCH-POLICY AUTHORITY FINDING recorded separately** (`docs/programme/BRANCH_POLICY_AUTHORITY_FINDING_2026-09-13.md`, ⛔ outside S3, no lane opened): the defect class is not *"`claude/*` isn't on the list"* but ⭐⭐ **a committed governance rule present but not AUTHORITATIVE in every execution environment, so a commit can appear policy-compliant merely because the enforcement mechanism was absent** — the same family as the 2026-09-07 `Bypassed rule violations` finding. Both gates read the same file so they cannot drift; **they did not drift, the environment did.** Two questions kept separate: **Q1** should `claude/*` be admissible · **Q2** how is branch law made authoritative across environments — ⛔⛔ **do not answer Q2 by adding `claude/*` to the script**, which answers Q1 silently while fixing something else. ⭐ **INTERIM RULE IN FORCE: absence of the branch hook is never evidence of branch-policy compliance.** ⛔ History NOT rewritten — a rewrite would have invalidated the lane's own evidence (`833ec87f` is the witnessed SHA) to tidy a governance record. **Standing: TRANSITION LAW TYPED + RULED · B-i DONE · B-ii DEFEAT CANDIDATES NEXT · B-iii LETHALITY THEN FREEZE · B-iv IMPLEMENTATION BLOCKED UNTIL LETHALITY PROVED · STORAGE UNCHANGED · PRODUCT SOURCE UNCHANGED · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-13 — `S3-F8` SPENT · ⭐ EXPECTED RED ESTABLISHED · CLASS B PHASE ENTERED · ⚠️ BRANCH-POLICY FINDING.** Founder-run disposable-shadow witness at SHA `833ec87f497ad6e69724f4c4005944d2aaa620a3`: `revision_read_issued = true` · `nonempty_recovered_body_reached_cognition = true` · `developmental_receipt_minted = false` — **the whole §5.4 decisive chain, no partial RED claimed.** Record `docs/programme/S3-F8-WITNESS-01_RESULT_2026-09-13.md` on `chore/s3-f8-witness-record-20260913` as `41d7d6f7`, **whose parent IS the witnessed SHA** (verified in-session). ⭐ **Canonical at `833ec87f` demonstrably permits authored revision body to reach developmental cognition without valid disclosure authority.** ⭐ **An earlier fixture attempt was correctly scored INSTRUMENT FAILURE / NO EVIDENCE and not counted** — a draft marked section-addressable before its section row existed, refused by the DB round-trip invariant before the decisive boundary; only the second clean shadow counts. *That the procedure's §6 fired on the first real attempt is the strongest evidence the stop conditions were worth writing.* ⭐ **Containment**: production untouched · tracked source unchanged · both shadows destroyed · ephemeral observer and worktree destroyed · statement parameters suppressed · record carries no excerpt, digest, fingerprint, offset, prompt, response or database identifier. ⭐ The observer sat **downstream** of the measured boundary at the structured-inference transport seam and returned a synthetic answer — **no authored prose left the shadow to any model provider**, which is containment strength, not a D2 breach: it established no authority and changed no body-read branch. **⭐⭐ CLASS B PHASE — the order that resolves its dependency** (falsifier spec §6): a Class B falsifier needs something to call and the machine does not exist, but waiting for it means writing the suite against a live implementation — *the exact failure Ruling 3 forbids*. So: **B-i** author S3-F1…F7/F9/F10 against the transition law's contract, runnable against any candidate · **B-ii** BUILD each named defeat candidate as a disposable stub · **B-iii** demonstrate the suite KILLS EACH ONE — ⛔ a surviving candidate repairs the SUITE, never the candidate · **B-iv** only then the real implementation, written to a suite already proven lethal. *Lethality becomes a precondition of the implementation rather than a hope about it.* ⚠️ **One open decision, founder**: transition law as prose-in-record or as a typed contract the falsifiers import — typed is checkable but edges on step 10; prose keeps step 10 shut but binds the suite to nothing. **⚠️ BRANCH-POLICY FINDING (governance, not S3):** `scripts/check-branch-allowed.sh` allows only `main | clean-main-no-secrets | phase4.6-reflective-agentics | feature/* | fix/* | chore/*` — **`claude/*` is NOT allowed**, which is why the founder's hook correctly refused a commit on `claude/sleepy-turing-uqkqto`. ⛔ This session's four earlier S3 commits landed on that branch **only because git hooks are not installed in the remote container** (`core.hooksPath` unset), so the declared gate did not run. Not deliberate, and the effect is the same: *a branch the policy forbids accepted writes because the gate was absent rather than satisfied.* ⛔ History NOT rewritten — commits preserved, finding recorded. Session-designated `claude/*` branches and the committed branch allowlist are in conflict; **lane not opened, no repair authorized.** **Standing: S3-F8 SPENT · CANONICAL DEFECT REPRODUCED · CLASS A BLOCK DISCHARGED · CLASS B NONE EXECUTED · IMPLEMENTATION NOT AUTHORIZED · STORAGE UNCHANGED · SOURCE UNCHANGED · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-13 — `S3-F8-WITNESS-01` · EXECUTION DISCIPLINES D1–D3 ADDED · LANE BOUNDED, NOTHING FURTHER OPENED.** **D1 — PIN THE SHA, cite the OPERATION not the line**: the record names the witnessed SHA and cites *the SELECT corresponding to the revision-body read on the developmental Ask path at that SHA*; ⛔ `capture.ts:164` is orientation for a reader today, never the identity of the evidence — *line numbers drift, and a record anchored to one becomes unreadable the moment an unrelated edit lands above it*. **D2 — INSTRUMENTATION STAYS OBSERVATIONAL**: nothing added for the witness may establish disclosure authority, alter branching, populate a receipt, or otherwise change the behaviour being measured — *a witness that perturbs its subject reports on itself*; a shim calling `establishDisclosureBoundary` to "check" would manufacture the very authority whose absence is the finding. **D3 — RECORD PRESENCE, NOT CONTENT**: the result is three booleans — `revision_read_issued` · `nonempty_recovered_body_reached_cognition` · `developmental_receipt_minted = false` — ⛔ no excerpt, fingerprint, digest or offset. ⭐ **The full result is expressible without a single authored character**, which is the test that the record is content-free by construction rather than by carefulness. ⭐⭐ **THE DECISIVE CHAIN, whole or not at all**: non-empty committed revision exists → body-scoped observation requested → `working_draft_revisions` body SELECT executes → recovered authored body reaches cognition → no valid disclosure authority existed → **S3-F8 RED**. ⛔ **There is no partial RED** — anything short of the whole chain is canonical block → reconciliation, or instrument failure → no evidence. **Standing: CENSUS CLOSED · CARDINALITY RULED · REPLAY/RECOVERY LAW RULED + CORRECTLY BOUNDED · FALSIFIER SET SPECIFIED · F8 PROCEDURE SPECIFIED / UNSPENT · SOURCE UNCHANGED · STORAGE CLOSED · CLASS B BLOCKED · PRODUCTION UNTOUCHED. NEXT ACT: the S3-F8 disposable-shadow witness, and nothing else.** ⭐ *If that witness produces the intended RED, S3 has earned the right to design the `PENDING → CLAIMED → COMPLETED` primitive. Until then, nothing else needs inventing.*
- **LATEST — 2026-09-13 — `S3` · CLASS A/B SPLIT RATIFIED · `S3-F8-WITNESS-01` SPECIFIED, ⛔ UNSPENT.** Records: `docs/programme/S3-F8-WITNESS-01_PROCEDURE_2026-09-13.md`; ratification + F7 qualification in `S3-FALSIFIERS-01_SPECIFICATION_2026-09-13.md`; design doc §15 carries the qualification and the step-10 direction. **⭐ The split is law**: *without it, "RED first" degrades into ritual rather than evidence.* ⭐ **Ruling 6 QUALIFIED (founder)** — the incompletion law is narrower than first written: **incompletion is not resumable BY REPLAY OF THE MEMBER ACT**, ⛔ not a metaphysical claim that incomplete work can never be continued by any mechanism. A future, separately governed privileged recovery protocol (operator repair · deterministic continuation from a durable execution record · reconciliation) may inspect and reconcile; ⛔ it may never reinterpret a replay as authorization or mint new MEMBER authority. **The prohibition is on a mechanism that turns a retry into consent — never on recovery as such**, and S3-F7 must be written narrowly enough not to foreclose it. **⭐⭐ THE S3-F8 WITNESS IS DELIBERATELY STRICT, because environmental failure must not be able to impersonate falsification.** Three outcomes and no fourth: unauthorized revision content reached and read → **EXPECTED RED**; blocked by an existing canonical mechanism → ⛔ **STOP, substrate understanding incomplete** — *a canonical block is NOT a green light, it contradicts the census premise and stops the lane for reconciliation*; cannot reach the decisive boundary → ⚠️ **INSTRUMENT FAILURE, no architectural evidence**. Ten stop conditions void a run (auth · ownership 404 · anchor refusal · reading not loadable · canonical unmeasurable · missing migration · malformed invocation · unrelated exception · route unreachable · environment). ⛔⛔ **THE FIXTURE TRAP**: `revisionContent` may legitimately be `null` and `assembleDevelopmentalContext` tolerates it by design (§11.4) — **a null read must never be scored as a block**, so the fixture must guarantee a non-empty committed revision AND a body-scoped observation ref, or the witness is invalid before it starts. **Two independent instruments**, because either alone is ambiguous: (i) authored characters demonstrably reached cognition as *recovered* evidence — ⛔ not merely a 200, which a null case can wear; (ii) the `working_draft_revisions` SELECT at `lib/manuscript/development/capture.ts:164` actually issued. ⭐ And **zero rows in `context_disclosure_receipts` proves nothing alone** — an early refusal also mints none; it counts only alongside (i)+(ii). Evidence discipline carried from FOCUS-WITNESS-01: no runtime output staged, ⛔ no authored prose in the record in any form (the receipt law's refusal surface binds the WITNESS RECORD too), shadow destroyed after the run. **⭐ STEP-10 DIRECTION RECORDED, ⛔ NOT OPENED**: the first implementation artifact is **not a table schema** but the **state transition law** — `PENDING →(atomic claim)→ CLAIMED →(canonical completion recorded)→ COMPLETED` — with the forbidden transitions beside it (`CLAIMED→CLAIMED` via replay · `COMPLETED→CLAIMED` via replay · `COMPLETED→new result` via retry · `request_id→authority` · `prose→identity` · `client assertion→section authority`); Class B then attacks candidate implementations **of that machine**. *The lane is no longer guessing what `pendingAskRef` should look like; it is establishing what no implementation of it will ever be allowed to mean.* **Standing: CENSUS CLOSED · CARDINALITY RULED · LOST-RESPONSE LAW RULED + QUALIFIED · FALSIFIER SPECIFICATION DELIVERED · SPLIT RATIFIED · S3-F8 WITNESS SPECIFIED, UNSPENT (next act, disposable shadow) · CLASS B BLOCKED BEHIND IT · STORAGE DESIGN CLOSED · TEST CODE NOT AUTHORED · SOURCE UNCHANGED · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-13 — `S3-DESIGN-01` · FOUNDER RULINGS 5–7 + `S3-FALSIFIERS-01` SPECIFICATION.** Records: `docs/programme/S3-FALSIFIERS-01_SPECIFICATION_2026-09-13.md`; design doc §15 + §10.6a amended in place. **⭐ Ruling 5 — CONSUMPTION AND DISCLOSURE RECEIPTS ARE DIFFERENT CARDINALITIES**: `member act → consumption 1:≤1` and `consumption → section receipts 1:N`. The apparent §10.6a/§10.7-6 contradiction was a **terminology defect**, not a design conflict — §10.6a's *"AT MOST ONE completed-crossing receipt"* is **amended in place to CONSUMPTION** (superseded, not deleted). ⛔ `pendingAskRef` is NOT 1:N with receipts; that would confuse *permission to execute the member act* with *evidence that boundaries were crossed*. ⛔ No implementation may proceed while two different objects are both called `receipt`. **⭐ Ruling 6 — COMPLETED OUTCOME RECOVERABLE, NEVER AUTHORITATIVE**: the OAuth precedent answers *was it consumed* and leaves *what did it become* unknown; `status: pending | claimed | completed` + `completion_ref` (canonical result identity, ⛔ never authored prose). Three lawful replay outcomes and no fourth — `pending` → atomically claim; `completed` → recover the existing outcome; **`claimed` but incomplete → ⛔ DO NOT CROSS AGAIN**, recover or surface interrupted, a fresh member act is required. ⛔ A new HTTP request cannot renew the original member authority — not by request-id, not by prose, not because the first response was lost. **⭐ Ruling 7 — REUSE THE LAW, NOT THE OBJECT**: `context_disclosure_receipts` revealed the pattern family (opaque identity · DB-enforced uniqueness · atomic winner · idempotent observation · authority separate from observation) — ⛔ do not clone the table, do not widen its `boundary` CHECK. **⭐⭐ THE STRUCTURAL FINDING IN THE SPECIFICATION**: *known-bad RED is VACUOUS for nine of the ten falsifiers*, because they describe a protocol that does not exist — a test that fails because the endpoint is unwritten proves the endpoint is unwritten, not that consumption works. So every Class B falsifier carries a **DEFEAT CANDIDATE**: a plausible, competent, WRONG implementation that passes the rest of the set and fails this one (the Circles-lane T10d/T10f discipline). **`S3-F8` is the ONLY CLASS A falsifier** — the sole genuine known-bad reproduction, and it must go RED against canonical today or the lane stops. Defeat candidates named: read-then-write precheck (S3-F1, the `093379e8d` shape) · request-keyed idempotency (S3-F2 — **the census proved this is not hypothetical**, `request_ref → runtime_consent_state(request_id)`) · prose comparison à la `isHeldRetry` (S3-F3/F4) · per-section consumption (S3-F5) · re-execution or a *fresh equivalent* answer on lost response (S3-F6 asserts **completion identity, never answer text**) · absence-of-completion read as permission (S3-F7) · the §10.6 prohibited durable shape `{pendingAskRef, sectionId, authorized:true}` (S3-F10). ⭐ **S3-F3 + S3-F4 are the same law from both sides** and are the conceptual centre: identity moves from *what the human happened to write* to *which human act the system is serving* — proved by one adversarial fixture (two acts, byte-identical prose, both execute; then replay one, which must not). ⚠️ **Naming collision declared, not absorbed**: `F1–F7` already denote the SEL-0 acceptance obligations in design doc §9, so the falsifier set is written `S3-F1…S3-F10`, 1:1 with the founder's labels. ⛔ **Nothing was run** — `node_modules` absent and no database reachable in this container; that is a statement about the environment, not a deferral. **Standing: SUBSTRATE CENSUS CLOSED · FALSIFIER SPECIFICATION COMPLETE · NEXT ACT = run S3-F8 against canonical (must go RED), then author Class B falsifiers WITH their defeat candidates and prove each candidate fails · TEST CODE NOT AUTHORED · STORAGE DESIGN NOT OPENED · SOURCE UNCHANGED · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-13 — `S3-DESIGN-01` · `pendingAskRef` SUBSTRATE CENSUS: COMPLETE, READ-ONLY.** Record `docs/programme/S3_PENDING_ASK_REF_SUBSTRATE_CENSUS_2026-09-13.md`; step 7 of the P1 pre-implementation sequence (Ruling 2). **Verdict: NO existing canonical object enforces §10.6a single-consumption**, so a dedicated opaque pending-Ask reference is PERMITTED — ⛔ permitted, not authorized to be written. Decisive question answered per candidate: `ask_threads` has no consumable state and is deliberately non-unique per anchor; `ask_turns` is **structurally incapable** — `ask_turns_no_update` refuses every UPDATE, so no row can ever be marked consumed; `isHeldRetry` is a pure in-process function with no write, and ⛔ **decides by comparing authored prose** — the exact prose→authority crossing S3 exists to close, so it is forbidden as a consumption control at any level of hardening, not merely insufficient. ⭐ **`context_disclosure_receipts` is the right PRIOR ART and the wrong OBJECT**: `disclosure_id UNIQUE` + `ON CONFLICT DO NOTHING` is a true atomic single-winner and `MintOutcome` already refuses to let idempotency become authority (`existing/attempted` vs `existing/crossed` maps exactly onto the lost-response law) — but it is minted at ACT 3 **step 6**, after the step-5a consumption point; it keys on `request_ref → runtime_consent_state(request_id)`, i.e. the **serving request**, so a replay's new request id would not collide; and its `boundary` CHECK admits only `writers_studio.focus->maia_cognition`, never widened. ⭐ Canonical atomic-claim precedent cited: `lib/auth/googleOAuthState.ts` (`SET consumed_at = NOW() … AND consumed_at IS NULL RETURNING`, with replayed/expired/unknown kept distinct); ⚠️ it records *that* a state was consumed, never *what happened next* — **completed-outcome recoverability is an unsolved sub-question of §10.6a**. ⭐ Re-derivation is substrate-backed today: `sectionIdsOf()` derives the required section set from the observation's evidence refs, which is what makes an identity-only ref viable. ⭐ Canonical S3 confirmed falsifier-ready: `developmentalTurn()` calls `loadRevisionContent()` **unconditionally** with no disclosure authority (`establishDisclosureBoundary` has exactly one non-test caller, and it is Focus). ⚠️ **Two open items surfaced, neither decided:** a **cardinality tension in ratified copy** — §10.6a says one member act causes AT MOST ONE receipt while §10.7-6 says ONE boundary per authorized section, so two authorized sections produce two receipts — and the boundary-vocabulary widening, which is its own governed migration. ⛔ Census is repository truth only — **no live or shadow database was read**. **Standing: FALSIFIER AUTHORING AUTHORIZED (Ruling 3, known-bad must FAIL first) · IMPLEMENTATION STILL WAITING · source UNCHANGED (no `lib/`, no `app/`, no `database/`) · Focus assembler UNTOUCHED, the dependency did not appear · PRODUCTION UNTOUCHED.**
- **LATEST — 2026-09-07 — ⚠️ SCHEMA DRIFT: THE I0.5 CIRCLE MIGRATIONS ARE IN PRODUCTION, UNDER NO RECORDED AUTHORIZATION.** Found while deploying an unrelated voice fix; the drift predates that deploy and is not caused by it. **Evidence (production `schema_migrations`, read 2026-09-07T19:2x):** `20260906000003_circle_membership_removals.sql` @ **18:24:45.957Z** · `20260907000001_circle_inquiry_response_withdrawal.sql` @ **18:24:46.020Z** · `20260907000002_circle_inquiry_status_retire_integrating.sql` @ **18:24:46.078Z** — three migrations, 121ms apart. ⭐ **RESOLVED BY IMAGE TIMESTAMPS, later the same evening.** `docker images maia-sovereign` shows image `891b33ee0` **BUILT at 18:22:45Z**, and `cmd_migrate` never builds — so the 18:24:46 rows are the **migrate phase of a full `deploy`/`update`** whose build finished two minutes earlier: exactly the script's `build → up → verify → migrate` cadence. ⚠️ **Two earlier readings in this bullet were wrong; both are corrected here rather than deleted.** *(i)* the first draft attributed the migrations to "the deploy that produced runtime `891b33ee0`" — **right about the deploy, wrong about the evidence**, because it read the container's `Created` (18:30:33Z) as that deploy's swap; *(ii)* the correction then **withdrew** the attribution on the strength of that same misread timestamp — an over-correction. **The image build time is the load-bearing fact. A container's `Created` is not a deploy's swap.** ⭐ **The 18:30:33 container is a RECREATION, and it is identified**: `~/.bash_history` on minisforum (mtime 18:31) ends with `nano ~/MAIA-SOVEREIGN/.env.production` followed by three `docker compose -f docker-compose.production.yml up -d --no-deps maia` — the **MAIL-04c Resend key rotation**, recreating the container to pick up a new env var. ⛔ **A bare `up` without `--build` never builds, so the Dockerfile deploy-lane tripwire never fires — it guards BUILDS, not container recreation.** That path takes no lane lock and runs no provenance verify; harmless here only because it recreated from a correctly-stamped image. **⛔ That is 47 minutes BEFORE the voice-fix commit `12eb44281` existed on any branch**, so neither it nor the 19:11Z deploy that carried it did this. The I0.5 record says the opposite of what is now true (`removal table ABSENT · integrating still allowed — UNCHANGED, UNMIGRATED, UNDEPLOYED`); that sentence is now marked superseded in place. ⛔ **The `20260907000002` migration is NOT additive** — it `DROP`s and re-adds `circle_inquiries_status_check`, narrowing the admitted vocabulary to `open | closed`, and `UPDATE`s any `integrating` row to `closed`. **Member-facing exposure is nil and should not be inflated into alarm**: production held 0 inquiries so the UPDATE touched no row, and Circle API authority is still founder-only (`requireCircleAccess()`; `CIRCLE_ACCESS_MEMBER_IDS` unconstituted), so no member can reach any of it. **What actually broke is the record, and the gate.** ⛔ **CANONICAL VERIFY IS STILL NOT ACCEPTED** — production now carries the schema that verify was supposed to license, which is the wrong order: *evidence licenses; it does not itself edit the public record, and a deploy must not edit the schema ahead of the ruling that authorizes it.* ⛔ **Do NOT treat this as retroactive authorization.** A migration reaching production by an unattributed act confers no standing it did not have at 18:23Z. **Owed, in order: (1)** ⭐⭐ **THE STRUCTURAL FINDING — MERGE-TO-CANONICAL IS LATENT SCHEMA-DEPLOY AUTHORIZATION.** Somebody *did* decide to run a full deployment; **nobody separately authorized or selected I0.5 for it.** Once the I0.5 lane merged its migrations to `clean-main-no-secrets`, the next unrelated full deploy applied them automatically as part of canonical branch state. *(An earlier wording here — "nobody decided to deploy I0.5; the branch decided for them" — is corrected: it implied mysterious agency. The truth is more mundane and more damning — ordinary deployment semantics, applied to a branch that had quietly become an authorization channel.)* This is not a hypothesis about the 18:24Z act — **the same mechanism is directly witnessed later the same evening**: the voice-fix deploy at 19:11Z carried `20260907000001_vault_erasure_queue.sql` (applied **19:31:48Z**, immediately after its 19:31:13Z swap), a WS-DELETE-01 migration that deploy had no interest in. *Merging a migration to the production branch is, in effect, authorizing it to be applied by whoever deploys next.* **The founder gate sits on the DEPLOY decision; nothing gates the BRANCH** — so every lane that deploys silently deploys every other lane's pending schema. ⛔ **That is the defect to fix, not the actor.** The actor remains formally **UNKNOWN** (`~/.bash_history` line 1977 `scripts/deploy-production.sh deploy "$TARGET"` is a candidate; shell history carries no timestamps). ⛔ **And the deploy lane keeps NO durable record of completed deploys**: `scripts/deploy-lock.sh` writes holder metadata *into the lockfile*, overwritten by every acquisition, and announces only to stderr — no `logger`, no journald, no ledger. **The 18:24Z acquisition record was overwritten hours ago, so its absence carries ZERO information and is evidence for nothing.** Attribution may be permanently unanswerable; per (3) that blocks nothing. An append-only line per acquisition (`timestamp · entry · target_sha · user@host · pid`) would have answered this in one query — the lane fix this incident argues for, ⛔ not authorized here; **(2)** a fresh production schema witness — this bullet asserts only the three `schema_migrations` rows it read; circle/membership counts, `withdrawn_at`'s presence and the removal table's contents are **UNREAD and must not be inferred from the migration filenames**; **(3)** — ⭐ **RULED, founder, 2026-09-07: RECONCILE FORWARD. REVERT NOT AUTHORIZED.** *A governance-order failure is not evidence that the implementation is wrong, and tearing down a state that may be perfectly correct in order to manufacture the opposite order is the more destructive error.* **Standing: I0.5 CODE deployed · I0.5 SCHEMA deployed · CANONICAL VERIFY not yet accepted · FOUNDER ACCEPTANCE not yet performed · I1 HOLD · further Circle build HOLD · REVERT NOT AUTHORIZED.** Acceptance requires **two independent witnesses**: **(i) canonical candidate** — the committed I0.5 SHA through its disposable-shadow verifier under unchanged acceptance law (every named obligation present, `63/63`, no fail/warn/skip, residue zero); **(ii) production state** — read what production *is* now: migrations, the relevant columns and constraints, row counts, FR-18 behaviour and the access membrane, and confirm existing data suffered no unintended consequence. ⭐ **On both green the ruling is `I0.5 ACCEPTED AS CURRENT DEPLOYED STATE` — explicitly NOT "I0.5 was authorized before deployment" and NOT "the unauthorized deployment is forgiven or erased". Acceptance now does not rewrite history; the deployment-order breach remains a separate operational finding with its own open provenance investigation.** ⛔ **If the canonical verifier fails, adjudicate the failure on what failed — repair versus rollback is decided then, never pre-chosen.** **Structural note for the deploy lane:** `scripts/deploy-production.sh:445-477` orders build → swap → provenance verify → **migrate**, so schema lands AFTER code, and a failed migrate step only `log_warn`s and still reports `Deployment complete!` — **a green deploy is not evidence a migration ran.** **Production runtime moved AGAIN at 20:00:27Z to `3ef333a65`** (image built 19:59:03Z, tagged `current`/`prod`) — same voice-fix code plus one CLAUDE.md line, nothing regressed; most plausibly the mail lane deploying `clean-main-no-secrets`'s tip after PR #1263 merged, ⛔ **not confirmed**. ⚠️ **Of the FOUR production-changing acts reconstructed for 2026-09-07, exactly ONE is founder-attributed** — the 19:11Z full deploy. The other three are **two full deploys (18:22Z · 19:59Z) and one unlocked container recreation (18:30Z)**. ⛔ *The recreation is deliberately NOT counted as a deploy*: it took a different path — no build, no lane lock, no provenance verify — and folding it back into the deploy count would hide the very gap it exposed. ⛔ **CI is excluded as initiator**: the checked-in production workflow triggers on `main` / `production` plus manual dispatch, so a push to `clean-main-no-secrets` does not deploy. **That ratio, not any single act, is the finding.** ⭐ **BRANCH GATE is the lane this opens**, and its question is no longer *who deployed someone else's migration* but: **why can a migration become deployable merely by becoming canonical, before its own acceptance law authorizes a production schema change?** Adjacent and part of the same hole: every push to `clean-main-no-secrets` tonight — this session's and the mail lane's alike — reported `Bypassed rule violations … 4 of 4 required status checks are expected`. **The production branch accepts writes without its declared checks, and whatever lands there is schema the next deploy will apply.** ⛔ Lane not opened; no repair authorized here.
- **LATEST — 2026-09-07 — MAIA voice-mode silence: FIXED, DEPLOYED, VERIFIED LIVE (member falsifier still owed).** `12eb44281` on `clean-main-no-secrets`, live at **19:31:13Z** (`/api/health` → `version: 12eb44281`; `printenv GIT_COMMIT` agrees). Reported as *"she isn't answering in words or text"*, **voice mode only, intermittent**. **MAIA's turn was a side effect of TTS.** The sole non-streaming voice append lived inside `await maiaSpeak(...)`'s success branch and was additionally gated on `showVoiceText`, so: **(1)** MAIA's voice off — the `speak + silent` cell of the 2026-08-13 modality-independence ruling — reached an `else` that only logged; cognition completed and the member got nothing in either channel; **(2)** a `maiaSpeak` that stalls without throwing ran neither the success branch nor the catch — no audio, no words, no error, which is exactly how the intermittency presented; **(3)** with `showVoiceText` off the turn never entered `messages` at all, so it was missing when the transcript was revealed AND missing from the next turn's `conversationHistory` — **a render preference deciding what MAIA could remember saying.** ⭐ **VOICE-CANONICAL-CONVERGENCE-02 made this the ordinary path rather than an edge**: with the streaming exit removed, voice turns are non-streaming, so `usedStreamingAudio` is false and the streaming append never runs. The convergence was right; it left the transcript hanging off the TTS call. *Voice may have a different capture path; it may not have a different mind — nor a different record of that mind's words.* **Repair:** one idempotent `commitOracleTurn()` seam reached from all six terminal paths, independent of TTS outcome and of `showVoiceText`; speech ordering preserved where speech works, bounded by `VOICE_TRANSCRIPT_WATCHDOG_MS` (6s) where it does not — **a hung TTS now delays MAIA's words rather than erasing them.** Also: the live interim transcript was a single `truncate` line building off-screen behind a frozen ellipsis, so the one thing that row exists to say — *she is still hearing me* — was the thing it stopped showing; it now wraps into a bounded tail-pinned tape. **Gates:** typecheck `230 vs baseline 239 · 0 regressions` · `__tests__/voice-non-degradation.test.ts` 15/15 (convergence intact) · new `__tests__/voice-transcript-commit.test.ts` 6/6 · `check:no-supabase` clean · **post-swap Co-Lab gate `33 passed · 0 failed · 0 warned`** · `vault_erasure_queue` present after the swap. ⛔ **NOT YET FALSIFIED BY A MEMBER**: the decisive case is **voice mode with MAIA's voice OFF** — before this commit it had no code path at all. Deployed ≠ demonstrated; *a fix verified only by its author's tests is a claim about code, not about MAIA.*
- **LATEST — 2026-09-07 — `JARVIS-CIRCLES-01` · `CIRCLE-05 · INVOKE` · `I0.5 · ENTRY SAFETY CLOSURE`: IMPLEMENTED, ⛔ NOT VERIFIED.** Branch `claude/jarvis-circles-programme-reouzc`; record `docs/programme/JARVIS-CIRCLES-01_I0.5_ENTRY_SAFETY_2026-09-07.md`; rulings addendum in `…_FOUNDER_RULINGS_2026-09-06.md`. Opened by founder act on the two I0 findings that are **defects against already-ratified law**, not design questions. **⭐ FR-18 RATIFIED — *a generic Circle invitation may not reinstate a removed member.* Canonical shorthand (founder precision): _"a recorded removal standing outranks a generic invitation"_ — deliberately NOT the broader "standing outranks invitation", which could be read as deciding CA-08.** ⚠️ **This ruling corrected Jarvis**: the I0 docket recommended revoking the invite token on removal; refused, because the token is **Circle-wide** — revoking it withdraws the invitation from *everyone* to answer *one* person's standing. The defect was never that the token survived, it was that **a generic bearer credential had enough authority to overwrite a recorded relational act**. Repair: `joinWithInviteWithClient()` seam; ⭐ **the authority is the MUTATION, not a precheck** — the guard lives inside the upsert (`ON CONFLICT … DO UPDATE … WHERE circle_memberships.status <> 'removed' RETURNING`; zero rows → `REINSTATEMENT_REQUIRED`) → **409** carrying **no Circle name, no grounds, no token verdict** (*a refusal is not an occasion to disclose*). ⚠️ **Founder source review REJECTED the first candidate `093379e8d` before any verifier run**: it read the standing, refused if `removed`, then upserted `active` unconditionally — `transaction()` is an ordinary BEGIN with no row lock, so a removal committing between the read and the write let the invitation overwrite it. **FR-18 was prechecked but not authoritative at the mutation boundary, and T10a–T10e could not tell the two apart** (all five evaluate a standing already `removed` before the join began). Preliminary SELECT deleted — one authority, no second thing to mistake for it. *No migration; no schema change; `left` behaviour identical; CA-08 still open.* **I-02 — STATE CLOSURE ON PAGE**: new `app/commons/join/layout.tsx` renders a closed state, so an unauthorized visitor is never asked for a token, never asked for a **consent mode** (*asking for a sharing decision about a relationship they cannot enter collects a sovereign act the system has no standing to receive*), and never submits a doomed join; `config/accessMatrix.ts` records DECLARED-vs-ENFORCED. ⛔ **I-03 preserved deliberately** — the closed surface reads no invite, so a valid and an invalid token stay **indistinguishable** before authorization. ⛔ **I8 obligation (not an I0.5 defect):** the page checks `requireFounder()` and the API `requireCircleAccess()` — they agree **today** only because Circle API authority is itself founder-only; when `CIRCLE_ACCESS_MEMBER_IDS` is constituted at I8, `/commons/join` **must migrate to the same Circle-access authority**. ⛔ Do not change it now. ⛔ **The API gate is unchanged** — a layout does not run for a route handler; `requireCircleAccess()` is still the authorization (C22 guards that confusion, which is the shape of B-01). **Verifier floor 54 → 63** under FR-14 (*the number is descriptive; the named set is the law*): **C20** surface refuses before rendering · **C21** solicits nothing AND validates nothing · **C22** join door gated + can refuse reinstatement · **T10a** valid live invite does not reinstate · **T10b** standing stays `removed` (**no write occurred**, not merely a thrown error) · **T10c** FR-05 removal record intact · **⭐ T10d** the **same live invite still admits an independently eligible member** — the obligation that makes the founder's correction falsifiable: a future "fix" by token revocation passes T10a–T10c and **FAILS T10d** · **T10e** no other Circle affected · **⭐ T10f THE INTERLEAVING WITNESS** — a **real `removeMemberWithClient()` act landing between invitation evaluation and the membership mutation** still loses: refusal, standing stays `removed`, removal record intact. Built deterministically via an `InviteClient` wrapper on the same rolled-back transaction (a second connection would need committed fixtures and would break the verifier's rollback-only consequence contract). **A precheck candidate passes T10a–T10e and FAILS T10f.** **Not authorized, founder: *do not add yet*** — per-invitee tokens · invite expiry · reinstatement workflow · facilitator review UI. ⛔ **CA-08 not decided** (`left` ≠ `removed`). **D-I dispositions ratified: D-I1 HYBRID** (only **explicit selection** drives matching; free text is **expressive only, never inferred into taxonomy** — the operative form of FR-06) · **D-I2 BUILD FRESH**, implementation term **"Interest Commons"** · **D-I3** cohort mechanism at I8, identities only at cohort authorization, ⛔ **do not touch `FOUNDER_MEMBER_IDS`** · **D-I5** member count **and** derived FORMING/ACTIVE are **INTERIOR** · **D-I6** `created_by` is **provenance, not permanent ownership**. Outer membrane narrows to `name`/`description`/`created_at`; authority axes carried to I4/I6 (CA-15 open). ⭐ **FOUNDER-RUN VERIFICATION OF `ae0fadd54` (disposable shadow, three pending migrations applied to the shadow only): `62 passed · 1 failed · 0 warned · 0 skipped · coverage 62/63 · exit 1` — EVERY BEHAVIORAL OBLIGATION PASSED, `T10a–T10f` included.** ⭐ **T10f PASSING IS THE SUBSTANTIVE RESULT: a removal landing after invitation evaluation and before membership mutation still defeats the generic invitation — the exact race `093379e8d` could not survive.** The single failure was **C21, a verifier false positive**: it scanned the RAW layout source, which contains the sentence *"nothing here reads `circle_invites`"* — **prose documenting the absence, matched as evidence of the presence**; the page inspects no token. Founder proved it on a disposable copy changing **only C21** to strip comments → `63 passed · 0 failed · exit 0`, residue zero. **Repair `3ed98ceab` is INSTRUMENT ONLY** — C21 now strips block and line comments before scanning, the same discipline **C6** has used since R4 and for the same reason; token set NOT weakened; ⛔ layout, `joinWithInviteWithClient()`, FR-18, T10a–T10f, the floor and all Circle behavior UNCHANGED. *A prose ban must never read as the banned behavior returning — an instrument that scans prose can fail on a file precisely because that file documents its own compliance.* **Typecheck at `ae0fadd54`: `230 errors · baseline 239 · 9 fixed · 0 regressions · exit 0`.** **Production witness (SHA `e4ac1bcac` · 4 circles · 4 active memberships · 0 shares/inquiries/responses · removal table ABSENT · `withdrawn_at` ABSENT · `integrating` still allowed — UNCHANGED, UNMIGRATED, UNDEPLOYED) — ⛔ SUPERSEDED at 2026-09-07T18:24:46Z; see the SCHEMA DRIFT bullet above. It was true when recorded and is false now. Kept verbatim as the before-state, never as a current claim — a witness is a reading at a time, and the honest repair is to date it, not to edit it.** ⛔ **CANONICAL VERIFY NOT YET ACCEPTED** — the 63/63 evidence is from a **disposable probe, not a run of a committed SHA; a probe is not the record.** Acceptance requires the full verifier from the committed SHA against a disposable shadow (`20260906000003` → `20260907000001` → `20260907000002`, shadow only): `63/63 · 0 failed · 0 warned · 0 skipped · exit 0`, residue zero. ⛔ **I1 DESIGN NOT OPENED** — it opens on that canonical run; *Jarvis does not open a stage on a run it has not seen.* ⛔ No deploy · no migration authored · schema untouched · cohort not authorized · no invitation sent · no facilitator assigned · no discovery built · founder UI gate untouched.
- **PRIOR (2026-09-07, superseded same day by I0.5) — `JARVIS-CIRCLES-01`: REPAIR CLOSED · **VERIFY FULL CANDIDATE PASS (54/54, 0 failed, exit 0)** · `CIRCLE-05 · INVOKE` OPEN, **I0 census COMPLETE (READ ONLY)**.** Branch `claude/jarvis-circles-programme-reouzc`; rulings `…_FOUNDER_RULINGS_2026-09-06.md` (**FR-01…FR-17**, open **CA-01…CA-16**); I0 outputs `docs/programme/CIRCLES_INVOCATION_SUBSTRATE_CENSUS.md` + `…_I0_FOUNDER_DOCKET_2026-09-07.md`. **Founder-run disposable-shadow gate on `26508109b` with all three migrations in order (`20260906000003` → `20260907000001` → `20260907000002`): `54 passed · 0 failed · 0 warned · 0 skipped · 54/54 required obligations discharged by PASS · exit 0`; fixtures rolled back clean.** ⭐ **R1–R4 and P1–P5 / A–F = VERIFIED ON CANDIDATE.** ⛔ **PRODUCTION UNCHANGED — no Circle migration applied, no deploy, founder-gated.** *Verified ≠ deployed.* ⛔ **Do not rerun the shadow gate merely because a session did not run it itself — the founder-run witness is the evidence of record.** **Ratified during REPAIR: FR-14** verifier coverage law (*PASS = 0 failed AND every required obligation still present AND discharged by PASS only* — WARN/SKIP/MISSING never discharge; named floor, never a total; *an instrument can satisfy all of its remaining questions by forgetting to ask the difficult ones*) · **FR-15** withdrawal is a **tombstone** (*keep the fact that an act occurred when the system needs that fact for integrity; do not keep the person's surrendered meaning merely because storage makes it easy*) · **FR-16** membership termination **cascades** (AUTHOR WITHDRAWAL ≠ BOUNDARY CASCADE) · **FR-17** stored inquiry `integrating` **retired** (⛔ `FieldPhase` untouched, asserted by C18). **I0 CENSUS — three findings dominate. (1) ⭐ NO explicit member-authored interest declaration exists anywhere** — `living_field_affinities` is **system-created from private memory atoms** (`created_by='system'`, `affinity_score`, `evidence_reason`) and `recurring_interests` is session-inferred; **both BARRED ABSOLUTELY by FR-06**. The north-star verb *"find one another around a shared interest"* has **no substrate and no reusable precedent — I1 builds it from zero. (2) ⭐⭐ "Commons" denotes THREE different existing things and NONE matches ratified FR-02**: the `community_*` forum · the `commons_contributions` library · the Circles migration filename. **Two are built on exactly the mechanics FR-08.7 forbids** — `community_user_stats` (post/comment/heart/breakthrough counts · `contribution_tier` · `contribution_points`), `contribution_levels` (0|1|2 · `accepted_count` · `endorsed_by`), `community_territories.min_contribution_tier` + **`min_cognitive_level` gating ACCESS on a measured attribute of a person**. ⛔ **Reusing them as Circle substrate would import a status economy on day one** — the exact Discourse/Mighty distortion named in R9. **Build the FR-02 Commons fresh.** **(3) 🔴 I-01 — REMOVAL DOES NOT REVOKE INVITE TOKENS.** Tokens are **Circle-wide, never expire** (no `expires_at`), and `joinWithInvite`'s upsert restores a removed member to `active`; the only remedy is `regenerateInvite`, which is **creator-only** and kills the link for everyone. **FR-05 requires removal to cut access — today it cuts access only until the next join request.** A **defect against ratified law**, repairable before I1. 🔴 **I-02 — `/commons/join` (public) collects a token AND a consent choice, then the gated API returns 403** — **a contradiction R1 created** by correctly closing the API without bringing the member-facing surface along. **Other I0 facts:** discovery = **ZERO** (`listMyCircles` only; `visibility`/`invite_enabled` **INERT** — ⛔ do not build to them) · creator role = `helper`, **no code path anywhere writes `facilitator`** (CA-15) · **authority is fixed at creation permanently** — `created_by` unchangeable, `facilitator` unassignable, and the two axes are orthogonal · `labAccess.ts` is the reusable cohort precedent (union with founder · fails closed · confers nothing beyond its door) → `CIRCLE_ACCESS_MEMBER_IDS` when a cohort is authorized, **not before** · outer membrane: only `name`/`description`/`created_at` are safe outer facts, **three of the doctrine's four panes have no column**, and member count is **interior** because it is also the FR-11 constitution state. **Classification is deliberately conservative — nothing is called a violation whose semantics were never constituted** (inert columns and legacy Commons = INERT LEGACY, not offences). > ***The membrane is verified. The invitation is not built.*** **Standing: I1–I9 NOT STARTED · DEPLOY NOT AUTHORIZED · COHORT NOT AUTHORIZED · no discovery built · no facilitator assignment · no invitations sent · founder UI gate untouched · doctrine byte-identical · no number ratified.**
- **LATEST — 2026-09-06 — LANE SPLIT (founder ruling): `JARVIS-RD-HUMAN-EXPERIENCE-ARCHITECTURE-01` ⇄ `JARVIS-PUBLIC-ACCOUNTED-FOR-01`.** `/accounted-for`, `docs/pitch/MAIA_PLATFORM_ACCOUNTING_2026-09-03.md`, the claim-reconciliation record, the page contract + screenshots and **PR #1239** (branch `claude/maia-human-experience-arch-12g5r6`, head `c36d82ec`, DRAFT — merge NOT AUTHORIZED, deploy NOT AUTHORIZED) transfer to the new publication lane; charter `docs/programme/JARVIS-PUBLIC-ACCOUNTED-FOR-01_CHARTER_2026-09-06.md`, split record `docs/programme/JARVIS-LANE-SPLIT_HUMAN-EXPERIENCE_ACCOUNTED-FOR_2026-09-06.md`. Direction of authority: R&D / canon / runtime evidence → claim reconciliation → page → public, never the reverse; the publication lane translates accepted outputs and may not adjudicate or alter them (wording · downgrade · omit · return upstream only). Two axes on every substantive sentence — epistemic kind (OBSERVED · RESEARCH-SUPPORTED · SOULLAB INTERPRETATION · HYPOTHESIS · OPEN QUESTION) × product maturity (LIVE · PARTLY LIVE · DESIGNED · VISION) — neither implies the other. Current run: reconciliation COMPLETE, render walk PASS, gates PASS; **merge-readiness WITHHELD** — a collision was observed at 17:36Z: the R&D lane's live session pushed its same-day pivot act (master run `docs/programme/JARVIS-HUMAN-EXPERIENCE-MASTER-RUN-v1.md`, Phase 1 open, manifesto FROZEN, its charter §24) and a census skeleton onto the PR branch without recording the split, and master run §4/§11 still assign `/accounted-for` tasks to the R&D cockpit; PR is also behind base `69f6fb7c` (clean freshness merge). **Founder ruling ~17:48Z (split record §7a): ACCEPTED** — intended transfer `c36d82ec`, containment `6ce59f82` → drift `f7705937` → **FINAL `a7b42f29`** (late arrivals recorded, not erased), PR branch single-writer = publication lane thereafter, all R&D commits PRESERVED (no rewrite, no revert), further R&D writes to #1239 NOT AUTHORIZED; stop instruction relayed into the R&D session by Routine — **acknowledged**: R&D session now on `claude/maia-human-experience-phase1-census` (merge-base with #1239 = `a7b42f29`; its charter §25 + master-run delegation landed there) → **operational transfer CLOSED ~17:58Z**; governance transfer = PR #1241 (awaiting founder merge act). Sequence: this governance branch merges first → #1239 absorbs `69f6fb7c` → both CLAUDE.md bullets kept → PR title/body to publication custody → custody check + gates rerun → only then `READY FOR FOUNDER MERGE RULING`. Jarvis never infers gates-green → merge. Merge ≠ deploy; "live" only after production witness. The R&D lane no longer edits `/accounted-for` and resumes at **Phase 1 — Whole-Organism MAIA/AIN Mapping**; its charter transfer section (next free number; §24 is the pivot) is owed on its Phase 1 branch or the PR branch (split record §5). *Accounted For does not decide what Soullab is. It makes Soullab answer publicly for what it says it is.*
- **LATEST — 2026-09-06 — JARVIS-RD-HUMAN-EXPERIENCE-ARCHITECTURE-01: foundational challenge pass R8–R12 CLOSED / COMPLETE (founder act); Synthesis v0.2 ACCEPTED as current programme synthesis (not doctrine; P1–P13 CANDIDATE; empirical postures provisional; v0.1 historical); reconciliation RUN (`docs/programme/…_CLAIM_RECONCILIATION_2026-09-06.md`); merge ruling on #1239 pending as a founder stop. **PIVOT (founder, same day): cockpit of record is `docs/programme/JARVIS-HUMAN-EXPERIENCE-MASTER-RUN-v1.md` — Phase 1 whole-organism map CURRENT; manifesto expansion FROZEN; research/new theory STOPPED; read-only census of thirteen subsystems against v0.2 (nine questions + R11 design audit + R12 memory criteria) → `docs/programme/MAIA_WHOLE_ORGANISM_MAP/00_RANKED_MAP.md` for founder adjudication; DO NOT REPAIR DURING THE CENSUS.** Branch `claude/maia-human-experience-arch-12g5r6`, PR #1239 DRAFT — merge and deploy NOT AUTHORIZED; MAIA runtime, memory runtime and `/accounted-for` final render HELD. Record: `docs/programme/JARVIS-RD-HUMAN-EXPERIENCE-ARCHITECTURE-01_CHARTER_2026-09-06.md` §22; spine under `docs/research/human-experience/`. **Episodic Phase 2 spec inputs added by the R12 ruling (Cat 1, not authorized):** the four-type memory typology (episodic · semantic · pattern · state-responsive, v0.1 source [22] — located, vendor-authored, conceptual, override risk-gated) is WITHDRAWN as a supported decomposition; working decomposition is **Authority × Time** (`docs/research/human-experience/frameworks/memory/AUTHORITY_X_TIME_2026-09-06.md`) with verbatim beneath derived; R7b precise — *member statement overrides as present self-report (does not rewrite history); MAIA impression asks* (= detect → ask → record with an evidence basis); five directions: preserve qualification · change-sensitive retrieval outranks similarity only under temporal conflict (the F2 retrieval cut becomes a safety question) · implicit contradiction → question, not transition · derived stays visibly derived · memory is never leverage. Brain-training rule is programme doctrine: *a gain on the practiced task is never, by itself, evidence of a gain in life.*
- **DIRECTION — 2026-09-06 — Temporal Memory (Cat 1, held).** `docs/architecture/TEMPORAL_MEMORY_DIRECTION_2026-09-06.md` records three inputs to the still-unauthored Episodic Phase 2 spec: (1) episode `occurred_at` vs assertion `valid_from`/`valid_to` live on different objects; (2) succession is carried by the successor via `supersedes`, `superseded_by` is derived, never stored; (3) staleness is **detect → ask → record** — the system never sets `valid_to` from a timer; the word *repair* is banned. **Audit RUN 2026-09-06 against production** (`scripts/witness/temporal-memory-audit.sql`, no persistent writes; output verbatim in the note): **F1** `valid_to` fallback gap is real in code and **ruled out under current data** — zero developmental memories carry a past `valid_to`, the filter has never excluded a row; **F2** invisible decay changes which developmental rows survive the non-vector top-12 **retrieval** cut for **2 of the 14 members** whose pool exceeds 12 (36 members, 2018 rows), five of twelve for one member on a one-month age difference — a material **upstream** effect; whether it propagates through `MemoryBundle.build()` merge → dedupe → `maxBullets` (default 5) into the prompt is **unmeasured** (open question for Phase 2, answerable from `selectionTrace`); **F3** only `pattern` is live, and decay has **two divergent implementations** — the live SQL `calculate_decayed_confidence` has no confirmed-1.5× rule (that exists only in the TS helper) and the SQL confirmation term caps at 0.0225 — no single authoritative definition of decay exists. Note **FROZEN**; auditing stops. `shouldPromptForConfirmation` has zero callers and was not wired. Not a lane; findings are inputs to the Episodic Phase 2 spec, sequence step 5. **Predeclared Episodic Phase 2 acceptance inputs (not authoritative until incorporated into the spec):** the candidate temporal substrate must answer five distinct queries without rewriting history — (1) Where do I live now? [current-valid], (2) Where was I living in March? [valid-at], (3) When did I tell you I moved? [episode/event time], (4) When did MAIA record that I had moved? [transaction time], and (5) What did MAIA believe before I corrected it? [belief-at-record-time, discriminator]. Temporal recall must identify the axis it resolved on; where the member’s question leaves the axis ambiguous, MAIA clarifies rather than silently choosing one. Axis clarification is member-governed interpretation of the query, not a temporal-memory transition: "March" authorizes which query runs; it does not open or close an assertion interval.
- **LATEST — 2026-09-06 — WS2-08 Hierarchical Manuscript Structure: lane opened by founder; **BUILD-08A CLOSED / ACCEPTED 2026-09-06 (founder ruling)** — PR #1230 merged as `03e9d89a`; migration `20260906000001_manuscript_section_heading_depth.sql` applied 13:36:01Z (deploy established by state evidence; transcript not recovered, non-blocking); production witness on runtime `50302f5d` (08A blobs identical to `03e9d89a`): F1 real-DOCX · F2 · F3 · F6a · F6b (13:46 run of record; later reruns FAIL on R5 = cleanup of 12 transient rows, retained as such) · exact-filename ledger query — ALL PASS. **WS2-08 lane STILL OPEN; 08B HOLD until an explicit founder act.** Note: the Co-Lab gate script is `scripts/verify-constitution-colab.ts` (33/0/0); the `verify-colab-boundaries.ts` name in this file and `docs/ops/COLAB_RELEASE_GATE.md` never existed — separate doc task. Unrelated draft-route `Response body … disturbed or locked` error parked, untriaged, in `docs/programme/PARKED_DEFECT_MANUSCRIPT_DRAFT_ROUTE_RESPONSE_BODY_2026-09-06.md`. 08B code HOLD until an explicit founder act; a minimum structure revision/digest binding must land before 08C's mutating commands; `heading_signal` = decisive classifier by precedence markdown > chapter > caps, not exhaustive provenance.** Requirement: *a Work may be addressable at fine-grained section level while remaining coherently organized as chapters and subchapters authored or confirmed by the member.* Census: `segment.ts` matched `#`/`##`/`###` then discarded the count; ingest persisted `position · heading · body` only; `origin='imported'` admitted by WS2-05A CHECK but produced by nothing; no command anywhere splits/merges a draft section (`topology_change_requires_explicit_command` has no counterpart). 08A lands: `heading_depth` (1..3|NULL) + `heading_signal` (markdown|chapter|caps|member) on **Source** `manuscript_sections` (migration `20260906000001`, additive); ALL-CAPS = boundary with depth NULL, **never a chapter by default**; pure `deriveImportedStructure()` folds depths into `ReviewedUnit[]` for the 06A confirm path (185 caps cuts → 0 units). Not a `level` on the structure tree — WS2-05A ruling holds. 08B (member-confirmed imported hierarchy) · 08C (split/merge/rename commands) · 08D (nested WRITE gestures) · 08E (revision-bound structure ledger) each need a founder act. Sequenced after #1228, which is untouched. Record: `docs/programme/WS2-08_HIERARCHICAL_MANUSCRIPT_STRUCTURE_DECIDE_2026-09-06.md`.
- **NAMING — 2026-09-04 — Coaching Journey Template.** Three layers fixed before any extraction work: **Coaching Platform** (generic product) → **Coaching Journey Template** (generic methodology/configuration layer) → **Now What?** (Larry's configured instance). Lane `NOW-WHAT-PORTABILITY-01` retired → `COACHING-TEMPLATE-EXTRACTION-01`, premise *Now What? is the first specimen, not the generic architecture*. Renaming establishes nothing about provenance: Larry-specific material stays in the instance; ambiguous items are classified before generalizing. Lane NOT opened (Anti-Drift Law freeze on generalized architecture holds). Ruling: `docs/programme/COACHING-TEMPLATE-EXTRACTION-01_NAMING_RULING_2026-09-04.md`.
- **LATEST — 2026-09-03 — Writer's Studio BUILD-07A Developmental Evidence: candidate `bfeb1a9` on `claude/build-07a-developmental-evidence-n5tm37`, bound to canonical `8d04f1b9f`. NOT CLOSED.** `lib/manuscript/development/` — typed `EvidenceRef` (section · passage · section-run · structure-unit · structure-units · structure-topology; no version, no quote, no live offset), frozen `readState` (per-section `(revisionNumber, code-point range, digest)` into the append-only revision store; structure context frozen inline; coverage `position | body`; `inputFingerprint`), `recoverEvidence` (historical display, digest-verified) vs `locateCurrent` (three-state, scoped per ref, never fuzzy), unforgeable `BoundEvidence` via `bindEvidence`, read-only `captureEvidence`. INV-7b demonstrated. Record: `docs/programme/WS2-07-BUILD-07A_EVIDENCE_WITNESS_2026-09-03.md` — **the unit's ten falsifiers / six outcomes were never on canonical; the record RECONSTRUCTS them from DECIDE (§4); founder RATIFIED them prospectively 2026-09-04 as Acceptance Instrument v1 (authority begins with the ruling, no provenance claim). Closure now waits on two founder checks: production `SHOW server_encoding` = `UTF8` (else STOP), and a founder-visible witness rerun from a clean checkout (`DATABASE_URL=… npx tsx scripts/ws2-07a-evidence-witness.ts` → `50 checks · 0 failures`). On both: CLOSED / ACCEPTED → PR → gates → merge on green.** Witness needs a UTF-8 database — an SQL_ASCII cluster makes the partition trigger count bytes (record §5). BUILD-07B–H unauthorized; 07B does not open on 07A passing.
- **LATEST — 2026-09-03 — CMT-01 Canonical MAIA Turn: M0–M2 landed (branch `claude/canonical-maia-turn-j92opb`), M3 NOT authorized.** Census (`docs/programme/MAIA_CANONICAL_TURN_CURRENT_STATE_CENSUS.md`) found cognition converged at `getMaiaResponse()` but *authority over what MAIA thinks with* never did: six MAIA-claiming composition mechanisms, one open channel (`meta?: Record<string, unknown>`, 204 `(meta as any)` reads), FAST tier missing three standing guardrails (D1). Spec v0.1 approved with Decision 2 axis amendment (`authoredBy` + `participationClass` + `authority`, never one scalar). Landed: `lib/maia/canonical-turn/` (38-producer closed registry, pure MIPA `adjudicateParticipation`, content-free manifest `[MAIA/manifest]`, one renderer, one identity resolver), six falsifiers R25–R30 (`npx tsx tests/constitutional/refusal-registry/cmt-01-gates.ts` — **R25/R26 expected RED until M3**), and a **shadow** construction on `/list` emitting `[MAIA/shadow] { zeroDiff, missingInCanonical, missingInLegacy, digestMismatch }` per turn with legacy assembly still response-producing. Rulings: voice/stream cognition path RETIRE; `sovereign/app/maia` STRUCTURALLY RETIRE (410 first); practice-field/draft ALLOWLIST; manifest EMISSION ONLY. Evidence: `docs/programme/CMT-01_M0-M2_WITNESS_2026-09-03.md`. **M0–M2 ACCEPTED 2026-09-03. First shadow deploy of `2fafaa4` ran as `GIT_COMMIT=unknown` — image stamped, container clobbered by the checkout compose's runtime override (deploy-chain defect, not CMT; witness §incident). Repair landed as `R` (`fix(deploy): launch provenance`; `npm run verify:deploy-provenance` 27/27): compose override removed, build+swap from the snapshot compose, pre-swap image verify, dual post-swap verify, env-collision refusal. Witness runtime = `R`, CMT anchor = `2fafaa4`; bring the minisforum checkout forward before deploying `R`. Bounded M2 shadow deploy of `R` AUTHORIZED (runbook: `docs/programme/CMT-01_M2_SHADOW_DEPLOY_RUNBOOK_2026-09-03.md`; pre-deploy condition pinned as R31 GREEN; collector `scripts/witness/cmt-01-shadow-witness.ts`). Deploy runs from the Mac Studio, not a remote session. BRANCH FROZEN TO A SINGLE WRITER until the live witness is complete. M3 explicitly unauthorized.** One non-zero `[MAIA/shadow]` line stops the witness for classification — never normalize or fix M3 around it. Merged with the parallel `pdc-1` participation-disposition contract (`9ba2d93`) — M1 imports it, never redeclares it; `OFFERED` representable, empty under pp-1. `lib/maia/canonical-turn/**` is inside `tsconfig.ship.json` from that merge.
- **PRIOR — 2026-07-03**: **Daily Anchor member standing-consent gate — shipped + verified LIVE.** Ambient anchor surfacing is now gated by `member_daily_anchors.surface_preference` (default `member_pulled` = private; member opts in to `contextual_doorway`), mirroring the atoms `return_preference` model — eligibility now originates from a member act, not the `MAIA_ANCHOR_CONTEXT_ENABLED` deploy flag (kill-switch only). Migration `20260702000003`; loader gate in `lib/anchor/loadRecentAnchors.ts`; gesture route `POST /api/anchor/[id]/surface-preference`; UI toggle on `app/maia/anchor/history/page.tsx`; refusal **R08**. Verified 2026-07-03 by 6 prod proofs under an authenticated member (schema+ledger, default-private, authenticated opt-in/return, MAIA-follows-consent). **Behavior change in effect:** existing anchors default private until each member opts in. Shipped via PR #542 (gate+UI), unblocked by PR #559 (migration idempotency fix — encounter triggers + team-scope constraints) after the covenant-gates redesign (#561) dissolved the author-self-approval deadlock. Full detail: memory `project_anchor_consent_gate_live`. *Note: the WISDOM_IS_RECOVERED.md canon cited by the original spec does not exist in the repo — grounded instead on SPIRAL_CONTINUITY_ENGINE §7 + atoms model; no new canon authored.*
- **Date**: 2026-05-24 (evening)
- **Current milestone**: Conversational Phase 2 (prompt influence, default-on, opt-out gate) on branch `feature/conversational-memory-phase2`, awaiting fork resolution + deploy + verification. **Headline framing for the week** (per `docs/architecture/STATE_AND_ROADMAP_2026-05-24.md` §8): *MAIA's memory field has been clarified, bounded, partially operationalized, and protected from premature ontological claims — not completed.* *"All arenas, safe but functional"* directive in effect — discipline reorients from blocking function to guiding it, *and from inflating function to naming it precisely.*
- **Phase 2 commit chain** (currently on `feature/conversational-memory-phase2`; not yet merged to `clean-main-no-secrets` or deployed):
  1. `987b3ff28` — initial Phase 2 (loader extension, block formatter, migration, `[Oracle]` log line). Wired into `app/api/oracle/conversation/route.ts` — **post-audit: that route receives ~zero live traffic; wire was operationally null**.
  2. `5179b162e` — (Kelly) Memory Expansion Plan — 9-layer activation map
  3. `f74ab4204` — (Kelly) wire site correction. Moved Phase 2 to live route `app/api/sovereign/app/maia/list/route.ts` + extracted in FAST tier of `lib/sovereign/maiaService.ts` template literal. Renamed log marker to `[MAIA] conversational-block`. Spec §IX appended documenting the wire-site error + the architectural seam gap (`buildMaiaRuntimeContext` is observer, not orchestrator).
  4. `3ca80a78d` — CORE/DEEP cut. CORE reaches prompt via `buildMaiaWisePrompt` extraction at `lib/sovereign/maiaVoice.ts:884-887`. DEEP `repairedContext` carries the addendum but DEEP prompt builder (`buildComprehensiveVoicePrompt` in `intelligentVoiceAdaptation`) does NOT extract it — prompt injection blocked at DEEP by the addenda-channel divergence documented in `docs/architecture/ADDENDA_CHANNEL_DIVERGENCE_2026-05-24.md` §II.B. Observability complete (`PROMPT_BLOCK_CHARS` + `layers.conversational`). The divergence-debt note is the single honest source of truth for what's wired vs. what's not — no second narration on the spec.
- **Coverage after this cut**: FAST + CORE tiers receive Phase 2 in the prompt. DEEP tier carries it in MaiaContext + observability only (not in prompt — see divergence-debt §II.B). Most conversations are FAST/CORE; DEEP fires for explicit-depth requests.
- **Breakthrough memory substrate cut (parallel)** — complete on `feature/conversational-memory-phase2`: route `POST/DELETE/GET /api/sovereign/atoms/[id]/breakthrough` (commit `58d374334`) + loader carries `isBreakthrough` + `formatAtomsForPrompt` renders "marked as a breakthrough by the member" + `memoryHealth.breakthrough` wired at the buildMemoryHealth call site in `app/api/sovereign/app/maia/list/route.ts` + discoverable log marker `[MAIA/sovereign] breakthrough surfaced { memberIdPrefix, markedCount }`. UI placement deferred (member gesture not yet wired; first proof via authenticated `curl` or admin SQL). **Stage-language (Kelly directive 2026-05-26, contact-fidelity progression intact)**: column + API exist = Stage 3 reachable; first marked atom surfacing under authenticated load = Stage 4 verified; repeated marked surfacings across multiple turns = Stage 5 live under member use. *Breakthrough memory remains wired/reachable until a member-marked atom surfaces under authenticated load — do not let the first `breakthrough: ok` row inflate into Live.*
- **Prior session state preserved** (from 2026-05-23):
  - `sem: ok` — semantic memory live
  - `atoms loaded: 8` — atoms surface per turn
  - Default doctrine `0fa544bc4` (Keep = contextual return by default) — unchanged
  - `/maia/orientation` live, all 6 domains quiet, page reports honestly
- **Posture shift (Kelly directive)**: *"yes I want full memory in all arenas in a safe but functional way. No more hardened rules against providing the one thing that makes soulful engagement possible and makes this platform more than a chat bot."* The observation-phase freeze doctrine **remains in force as discipline** (member-marked vs system-inferred, no synthesis, provenance-grounded, no-static-UI-claim-without-verified-state) but is **no longer used to block function**. Each remaining arena (episodic, somatic, field, meta) requires its own Phase 2-equivalent spec following the conversational pattern.
- **Next actions** — refined sequencing (per `docs/architecture/STATE_AND_ROADMAP_2026-05-24.md` §9), in order — **fork → toggle → clarify-engagement-shape → verify → episodic → cleanup**:
  1. **Resolve Phase 2 fork**:
     - **Option A (preferred — generalizable foundation)**: address `docs/architecture/ADDENDA_CHANNEL_DIVERGENCE_2026-05-24.md` §V — extract shared `appendAllContextAddenda` helper, fix `buildComprehensiveVoicePrompt` to iterate `MaiaContext` addenda, wire atoms end-to-end. Then merge to `clean-main-no-secrets`, deploy, verify §IV gate across all three tiers.
     - **Option B (scoped FAST+CORE only)**: merge `feature/conversational-memory-phase2` to `clean-main-no-secrets`, deploy, verify `[MAIA] conversational-block { emitted: true, surfacedCount: N, ... }` on `sovereign/app/maia/list` for returning members **across FAST + CORE turns only** — DEEP explicitly excluded from the §IV gate. Acceptable *only* if verification claim explicitly names the FAST+CORE scope.
  2. **Member-facing recall toggle** — `conversational_recall_enabled` opt-out surface. Consent infrastructure, not polish. Ships with (1) regardless of fork outcome.
  3. **Clarify engagement shape** (per `docs/architecture/CLARIFY_ENGAGEMENT_SHAPE_2026-05-25.md`) — small parallel routing-layer refinement addressing the sacred-mirror edge case where dense unframed technical input produces felt withdrawal. Detection rule (high lexical density + high structural complexity + no explicit question) → clarification response instead of generic holding. Load-bearing principle: *"Sacred mirror should clarify the requested relation to the material, not default to silence or generic holding when the material is structurally asking to be worked."* Contained, reversible, no schema changes. Gate to builder/architect mode initially.
  4. **Verify production reality** — minisforum-side deploy state, `[MAIA] conversational-block` emission across FAST/CORE (and DEEP only after §V fix). Canonical diagnostic in **Ops diagnostic** below.
  5. **Episodic Phase 2 spec** — and only then. The threshold layer where *"life unfolding"* becomes operationally testable without jumping into field ontology. Follow conversational pattern (Phase 1 observability → Phase 2 prompt influence; locked-answer table; 4-safeguard set; `episodic_recall_enabled DEFAULT TRUE` consent gate).
  6. **Dormant service cleanup** — *after* episodic ships, not before: `QuantumFieldMemory.ts` → rename/gut → `FieldPatternMemoryService.ts` or delete; `ConsciousnessEvolutionService.ts` → `DevelopmentalTrajectoryService.ts` (strip "level increased"); reconcile duplicate `SemanticMemoryService` (consciousness/ vs memory/) — pick one, delete the other.

  **Still held under freeze, not in this sequence**: Coherence/Field layer wire-up (lift conditions per `COHERENCE_FIELD_WIRE_UP_SPEC_2026-05-24.md` §0.C unmet); Morphic / Somatic / Achievements (matrix Later with named gates); Pattern Attunement (must emerge downstream of episodic + tact); Tact calibration (sketch only after episodic ships); cross-layer synthesis; any member-facing "field state" / "coherence" / "RFI" / "UFI" surface.
- **Underlying question**: Does cross-session content surfacing, gated by consent and grounded in provenance, produce the felt continuity that makes MAIA more than a chatbot — without crossing into synthesis or interpretive displacement?
- **State of the system (six-category typology, per `docs/architecture/STATE_AND_ROADMAP_2026-05-24.md` §8 + memory `project_six_category_artifact_typology`)** — replaces earlier "honest inventory" framing. Each category is legitimate work; **collapsing 1–5 into 6 is the inflation drift to refuse**:
  - **Cat 1 — Preserved direction** (held, not authorized): 8 directions in `docs/architecture/RELATIONAL_INTELLIGENCE_DIRECTIONS_2026-05-24.md`; 10 held directions in `docs/architecture/PARTICIPATION_WITHOUT_FORECLOSURE_2026-05-24.md` §10; **RFI / UFI** (also documented as anti-drift examples per `SOVEREIGNTY_LAYER_STATE_2026_05_23.md` + `COHERENCE_FIELD_WIRE_UP_SPEC_2026-05-24.md`).
  - **Cat 2 — Canonical primitive** (interface target, no runtime authority): **FIS Field State Primitive** (`docs/canon/FIS_FIELD_STATE_PRIMITIVE.md`); Pattern Primitive; Four-Layer Substitution.
  - **Cat 3 — Built substrate** (service + migration, 0 live callers, preserved under freeze): `EpisodicMemoryService`, `CoherenceFieldService` (per `docs/architecture/MEMORY_SERVICE_STATUS_MATRIX_2026-05-24.md`).
  - **Cat 4 — Dormant service** (rename / gut / Later-with-named-gate): `QuantumFieldMemory` (810 LOC, 0 persistence — rename + gut); `MorphicPatternService` (Later — consent + aggregation gate); `SomaticMemoryService` (Later — explicit input source); `AchievementService` (Later — reframe as practice); `ConsciousnessEvolutionService` (Rename → `DevelopmentalTrajectoryService`); `MAIAMemoryArchitecture` (2351 LOC — Observe only); duplicate `SemanticMemoryService` (reconcile).
  - **Cat 5 — Frozen plan** (explicit "does not authorize" language; lift requires Kelly directive): `docs/specs/COHERENCE_FIELD_WIRE_UP_SPEC_2026-05-24.md` §0.C; `docs/architecture/MEMORY_EXPANSION_PLAN_2026-05-24.md` §5 — *"full memory field" lives here, not in cat 6.*
  - **Cat 6 — Live runtime authority** (production runtime evidence): atoms loader + `is_breakthrough` schema-bound flag (`crossing_must_be_false` sibling); substrate monitor (`runtime_events`, `deriveStatus` fall-through); Spiral Orientation Cut 2; `memoryHealth.semantic`; contextual return default (`0fa544bc4`); Daily Anchor; field context adapter (flagged); Field Lab + tester gate; Relational Navigation Room; Learning Spine Move 2 reviewer (Loop C); **Corpus Callosum substrate** (parallel multi-agent epistemic emission via `agent_runs` / `integration_passes`; 8 voices — MythicAtlas + MaiaVoice + ShadowAgent + Fire/Water/Earth/Air/Aether — firing same-second under production traffic, WisdomRouter ~49% suggests *selective integration emerging operationally, not broadcast synthesis*; default-on via `CORPUS_CALLOSUM_ENABLED !== '0'`; live on `/api/sovereign/app/maia` FAST + CORE — *unknowns preserved*: BETWEEN path zero rows despite routing-invariant set at boundary, DEEP zero rows, member-facing experiential effect unmeasured; per memory `project_corpus_callosum_substrate_cat6`).
  - **Cat 6 — branch-only, awaiting deploy + verify**: conversational Phase 2 (FAST + CORE reach prompt; DEEP blocked at `buildComprehensiveVoicePrompt` per `docs/architecture/ADDENDA_CHANNEL_DIVERGENCE_2026-05-24.md` §II.B).
  - **Strategic read** (load-bearing, Kelly 2026-05-24): *"You are not behind because RFI/UFI are not built. You are safer because you now know they are not built. That distinction may be the most important outcome of the week."*
  - **Inverse drift (omission risk, Kelly 2026-05-25)**: dormant scaffolds get narrative placement; live infrastructure stays invisible until explicitly measured. *Symmetric* to inflation drift, opposite direction. Corpus Callosum substrate (2,382 lifetime turns per elemental voice) and atoms `is_breakthrough` flag are both Cat 6 that went unnamed in earlier framings — system description was under-reporting operational reality. Discipline: any substrate generating production rows must be named explicitly; *"we built X when it's Cat 1-5"* is not the only failure mode, *"we didn't see X was Cat 6"* is the other. **Phrasing rule**: name the mechanism (*parallel epistemic emission*), not the mythology (*emergent consciousness architecture*) — metaphor after measurement, not before.
- **Important not to collapse**: *declaration is not liveness; built ≠ wired; wired ≠ surfacing; surfacing ≠ verified.* Episodic is the first measurable substrate for continuity claims — the threshold layer where "MAIA remembers a life unfolding" becomes operationally testable rather than architecturally aspirational. Until Episodic ships and stabilizes, resonant-field / coherence talk remains mostly metaphorical architecture language; only after that does it begin having a measurable substrate underneath it.
- **Ops diagnostic**:
  ```bash
  # Runtime log markers (atoms / memory health / conversational Phase 2):
  ssh soullab@minisforum 'docker logs maia-sovereign --since 1h 2>&1 | grep -E "MAIA/runtime|atoms loaded|MEMORY_HEALTH|conversational-block"'

  # Corpus Callosum substrate (parallel multi-agent emission rows):
  ssh soullab@minisforum 'docker exec maia-postgres psql -U soullab maia_consciousness -c "SELECT origin_route, processing_profile, count(*) FROM agent_runs WHERE created_at > NOW() - INTERVAL '\''24 hours'\'' GROUP BY 1,2 ORDER BY 3 DESC;"'
  ```

## Re-entry vow (for this session)

Before making changes, confirm:

- I understand what MAIA is and is not.
- I understand the ethical boundaries I must not cross.
- I understand what continuity means in this system.
- I understand what question this session is truly serving.

If this is not clear, re-read the Anchor and PROJECT_CONTEXT.md before proceeding.

---

# Project Invariants (MUST FOLLOW)

## Canon

**[MAIA Canon v1.1](./docs/canon/MAIA_CANON_v1.1.md)** governs all changes. Before implementing any feature, verify alignment with the canon's prohibitions and structural principles.

This project is governed by the **[MAIA Oath](./docs/canon/MAIA_OATH.md)**. Any change that violates the oath is invalid, regardless of technical merit.

## Infrastructure (Single Source of Truth)

**⚠️ STOP — READ THIS BEFORE ANY INFRASTRUCTURE ASSUMPTIONS ⚠️**

### What we DO NOT use (NEVER assume these)
- **NOT EC2** — The server is NOT an AWS EC2 instance
- **NOT Nginx** — We use Caddy, NOT Nginx
- **No managed hosting platforms** (Vercel, Netlify, Heroku) — we control deployment
- **No managed databases** (Supabase, PlanetScale, Neon) — PostgreSQL is self-hosted
- **No CDN/proxy middlemen** (Cloudflare) doing MITM on traffic

### What we DO use
- **Production host**: **minisforum** (LAN: `192.168.0.104`), accessed via `ssh soullab@minisforum`. Running Docker + **Caddy** (auto TLS via Let's Encrypt).
- **Public DNS for `soullab.life`** routes to the LAN's public IP → router forwards :443/:80 to minisforum. Mac Studio is **not** in the public traffic path.
- **Domain**: `soullab.life` (apex), `api.soullab.life`, `oldhead.soullab.life`, etc.
- **Reverse proxy**: **Caddy** in Docker container (`maia-caddy`) on minisforum.
- **Database**: Self-hosted PostgreSQL in Docker (`maia-postgres`) on minisforum.
- **Containers**: Docker and docker-compose.

### Production Stack (on minisforum)
All services run in Docker on **minisforum**:
- `maia-sovereign` — Main Next.js app (port 3000, Docker-internal only)
- `maia-api` — API backend (port 3001, published)
- `maia-caddy` — Reverse proxy (ports 80/443, published)
- `maia-postgres` — PostgreSQL database
- `maia-comms-worker` — Background worker
- `maia-whisper` — Speech processing
- `maia-rlm` — RLM service

### Mac Studio role
The Mac Studio (this machine, where Claude Code typically runs) hosts the active git worktrees and is the primary dev environment. It runs a parallel docker stack with the same container names and the same compose file, but **that stack is not in the public soullab.life traffic path**. A successful `docker compose up -d --build` on the Mac Studio updates the local stack only — production stays unchanged.

### Check Production Status
```bash
# Inspect the actual production container on minisforum (not the local stack):
ssh soullab@minisforum 'docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"'
ssh soullab@minisforum 'docker inspect maia-sovereign --format "{{.Created}} {{.Image}}"'

# External (hits the LAN public IP → router → minisforum):
curl -k https://soullab.life/api/health
```

### Production Deployment
- Host: **minisforum** (SSH from Mac Studio: `ssh soullab@minisforum`)
- Stack: Docker + Caddy
- Compose file: `docker-compose.production.yml`
- **Deploy lane lock (one deploy at a time — structural, not disciplinary)**: every deploy entry point (`deploy-production.sh deploy/update/migrate/rollback` and `pre-deploy-gate.sh deploy-maia`) takes an exclusive non-blocking `flock` on `/home/soullab/MAIA-SOVEREIGN/.deploy.lock` via `scripts/deploy-lock.sh`. A second deploy attempt is **refused** with the holder's PID / start time / entry point / **asserted target SHA** printed (`target_sha=`; the shared checkout's HEAD appears only as a labelled `checkout_head=` line when it differs — the record names what the deploy was asked to build, never the checkout). The kernel lock is inherited by the docker compose build and auto-releases when the whole deploy tree exits or dies — a crashed deploy cannot leave the lane locked. If a refusal shows a dead holder PID, a child of that deploy (usually the build) is still running: inspect with `fuser -v ~/MAIA-SOVEREIGN/.deploy.lock`. **Never delete the lockfile to force entry** — that detaches the kernel lock from future acquirers and re-opens the 2026-07-09 concurrent-deploy race (five processes wedged on the buildkit lock because parallel sessions' deploys could not see each other).
- **Deploy command — quick `maia`-only rebuild** (run from Mac Studio, executes on minisforum). Canonical quick path is the pre-deploy gate, which acquires the deploy-lane lock, **materializes an explicitly named immutable commit into an isolated build context** (never whatever branch is checked out — 2026-07-27 shared-checkout incident, `docs/ops/IMMUTABLE_SHA_DEPLOY.md`), runs the Co-Lab + disk gates, builds, refreshes rollback tags, swaps, then verifies the running container's `GIT_COMMIT` equals the named SHA. **`deploy-maia` now REQUIRES a SHA argument** — name the fetched remote tip so no `git checkout` of the shared working tree is needed:
  ```bash
  ssh soullab@minisforum 'cd ~/MAIA-SOVEREIGN \
    && git fetch origin clean-main-no-secrets \
    && scripts/pre-deploy-gate.sh deploy-maia "$(git rev-parse --short origin/clean-main-no-secrets)"'
  ```
  The SHA is snapshotted via `git archive`, so a concurrent session checking out a different branch cannot change the build (the deploy-lane flock serializes deploys but does NOT protect the checkout→lock interval — that gap is what the snapshot closes). No SHA + `DEPLOY_ALLOW_HEAD=1` builds the current checkout tip as an explicit ack (still snapshotted + announced). Fast, but rebuilds **only** the `maia` service — it does **not** run migrations or touch other services. It DOES refresh the rollback tags (`maia-sovereign:current`/`:previous`/`:<sha>` via shared `scripts/deploy-tag.sh`) before swapping the container.
  **The bare compose command (`GIT_COMMIT=... docker compose ... up -d --build maia`, with or without the prefix) is retired STRUCTURALLY — it now FAILS at build time.** The Dockerfile's deploy-lane tripwire (`docs/ops/DEPLOY_LANE_TOKEN.md`) refuses any build of `docker-compose.production.yml` that didn't come through `acquire_deploy_lock()` (which exports `DEPLOY_LANE_TOKEN`; the compose build-arg has deliberately no default). Why it was retired: (1) without the `GIT_COMMIT` prefix it baked `GIT_COMMIT=unknown` into the image; (2) it bypassed the deploy-lane lock, so it could race a deploy already in flight; (3) on 2026-07-10 it bypassed the pre-deploy gate and skipped rollback tagging, leaving `maia-sovereign:current` pointing at the wrong image. The failure is loud and safe — the build dies in under a second; the running container is untouched. Check which lane built the live container: `docker exec maia-sovereign printenv DEPLOY_LANE` (→ `deploy-lane`; pre-tripwire images lack the variable). Local dev (`docker-compose.yml`) and staging declare their own lane tokens in-file and are unaffected.
- **Full deploy — canonical path**: `scripts/deploy-production.sh deploy <SHA>`. The complete all-services deploy: it acquires the deploy-lane lock, **materializes the named immutable commit into an isolated build context** (same control as the quick path — `docs/ops/IMMUTABLE_SHA_DEPLOY.md`), exports `GIT_COMMIT` (from the asserted SHA) / `APP_VERSION` (read from the snapshot) / `BUILD_DATE`, builds with the provenance build-args from that snapshot, tags images per-commit for rollback, brings the stack up, verifies running provenance, and runs DB migrations (migrations execute from the snapshot too). `deploy` REQUIRES a SHA (or `DEPLOY_ALLOW_HEAD=1` ack); `update` pulls a branch then builds the pulled tip as an immutable snapshot. Use it for schema changes, multi-service changes, or whenever you want a rollback point — i.e. anything beyond a quick `maia`-only code rebuild. **Post-swap provenance verify is fail-closed on every path** (`deploy`, `update`, `deploy-maia`): if the running container does not report the authorized SHA, the deploy ABORTS before migrations/smoke and points at `rollback` — a mismatch never silently proceeds.
- **macOS dev-stack caveat (immutable-SHA build context)**: the deploy materializes the build context as a `git archive` snapshot under `$TMPDIR`, and the `migrate` service bind-mounts that snapshot's `database/migrations`. On **minisforum (Linux, production) this is transparent** — `/tmp` binds into containers freely. On the **Mac Studio parallel stack**, Docker Desktop must be able to share the snapshot path, or the migrate mount fails; if so, set `DEPLOY_CONTEXT_DIR=<a Docker-Desktop-shared path>` before deploying. Production is unaffected. Full detail + the `DEPLOY_CONTEXT_DIR` / `DEPLOY_ALLOW_HEAD` seams: `docs/ops/IMMUTABLE_SHA_DEPLOY.md`.
- **Verify after deploy**:
  ```bash
  # 1. Container freshness
  ssh soullab@minisforum 'docker inspect maia-sovereign --format "{{.Created}}"'

  # 2. LAN IP sanity (must match router port-forward target, expected 192.168.0.104)
  ssh soullab@minisforum 'hostname -I'

  # 3. Public reachability (external path through DNS + router forward)
  curl -k https://soullab.life/api/health

  # 4. Provenance — what commit is actually live (must be the SHA you deployed, NOT "unknown")
  ssh soullab@minisforum 'docker exec maia-sovereign printenv GIT_COMMIT'
  ```
  `Created` must show a timestamp under a minute old. `hostname -I` must show `192.168.0.104` as the LAN IP; if it shows anything else, the router's port-forward is pointing at a stale IP and external/iOS traffic will silently fail (see the LAN IP drift trap above). `/api/health` must return fresh JSON with `uptime` near zero. `printenv GIT_COMMIT` must return the short SHA you deployed.

  **Diagnostic — `GIT_COMMIT=unknown` does NOT imply missing provenance wiring.** The chain is already complete: Dockerfile (`ENV GIT_COMMIT=${GIT_COMMIT}`) ← compose (`build.args.GIT_COMMIT`) ← deploy (`scripts/deploy-production.sh` exports it; the quick command must prefix it). `unknown` means the deploy route *bypassed* that chain — almost always the quick command run **without** the `GIT_COMMIT=$(git rev-parse --short HEAD)` prefix. So if you see `unknown`, first verify **which deploy path was used** before suspecting the build-arg wiring. **Second cause (2026-09-03): the image can be stamped while the container is not** — compare `docker image inspect maia-sovereign:current` against `docker inspect maia-sovereign` `Config.Env`; a runtime `environment: GIT_COMMIT` override in a stale checkout compose rewrites the image's identity at `up`. The repaired gate refuses that override and verifies both channels.
- CI deploys are disabled (self-hosted runner not yet configured).
- **Common deploy mistake**: rebuilding on the Mac Studio instead of minisforum. The local stack will report healthy and `Created` will update, but the public soullab.life traffic continues hitting minisforum's old container. Always verify with the minisforum-side `Created` check above, not just the local one.

### Why This Architecture
- No third party sits between users and their data
- No jurisdiction concerns — we control the location
- Complete air-gap capability if needed for local-only mode

## Database & Backend

- **We do NOT use Supabase.** Never introduce Supabase. Use local PostgreSQL via `lib/db/postgres.ts` only.
- **If you see Supabase in code, remove it; do not consolidate it.**
- Database: Local PostgreSQL at `postgresql://soullab@localhost:5432/maia_consciousness`
- Database client: `lib/db/postgres.ts` (uses `pg` npm package)
- Never add `@supabase/*` imports, RLS policies, or Supabase migrations
- Enforcement: `npm run check:no-supabase` blocks violations (runs in pre-commit hook)

## MAIA Sovereignty

- Primary AI: Claude (Anthropic) via `ANTHROPIC_API_KEY`
- Fallback: Local Ollama (DeepSeek models) when API unavailable
- Never use OpenAI or other cloud AI providers
- Voice: Local TTS/STT or browser APIs only
- Data: Local PostgreSQL, never cloud databases

## Sanctuary Mode (Memory Consent)

MAIA supports **Sanctuary sessions** — conversations that remain useful in the moment but do not enter long-term memory.

### Invariants

1. **No content retention** — Sanctuary sessions are not stored, indexed, or used for pattern formation
2. **No training data** — Sanctuary content never enters any model training pipeline
3. **Minimal metadata** — Only log that a sanctuary session occurred (timestamp, duration) — never content
4. **Visual clarity** — User must see unambiguous indication that Sanctuary is active
5. **Default off** — Regular sessions build memory; Sanctuary is an explicit opt-in
6. **Absolute boundary** — Nothing from a Sanctuary session can be saved, extracted, inferred, or converted into long-term memory, under any circumstances, including by user request during the session

### Why This Exists

Real honesty requires safety. People won't speak freely to a system that might later monetize or weaponize their vulnerability.

Sanctuary is the architectural proof that MAIA serves the person — not the data model.

### UI Copy

**Toggle:**
> Sanctuary Mode — This session won't be remembered. Speak freely.

**Learn more:**
> Sanctuary sessions are useful in the moment, then gone. No patterns formed. No memories stored. Just presence.

## Onboarding Flow (One-Time Per Member)

The onboarding journey happens exactly once per member — whether beta testers or those gifted a passkey. After completion, users are redirected directly to `/maia`.

```
/begin
   ↓ (click "Begin Journey")
/intro-maia
   ↓ ("I'm Maia" greeting, click "Continue")
/intro-daimon
   ↓ ("I am a Daimon by design", click "Enter the Lab")
/test-elemental
   ↓ (passkey entry, password setup, "Before we begin..." orientation)
/faq
   ↓ (FAQ section)
/onboarding
   ↓ (preferences, complete)
/maia
```

### Invariants

1. **Universal flow** — All members (beta testers, gifted passkeys, future users) follow the same onboarding
2. **Single entry point** — New users start at `/begin`
3. **One-time flow** — Once `onboarded: true`, users skip directly to `/maia`
4. **No shortcuts** — Each step must be completed in sequence
5. **Returning users** — `/signin` for existing members to sign in each session
6. **New user link** — `/signin` includes amber "New to Soullab? Begin Journey" link → `/begin`

### Pages

- `/signin` — Returning user sign in (amber link to `/begin` for new users)
- `/begin` — Landing page with Holoflower and "Begin Journey" button
- `/test-elemental` — `SacredSoulInduction` (passkey/password) then `ElementalOrientation`
- `/faq` — `FAQSection` component
- `/onboarding` — `CompleteWelcomeFlow` (preferences)
- `/maia` — Main app

### Completion Flag

Stored in both:
- Server-side: `members.onboarded = true` (PostgreSQL)
- Client-side: `localStorage.beta_user.onboarded = true` (session cache)

## Members System (Cross-Device Recognition)

Server-side member management enables users to be recognized across devices.

### Database

Table: `members` (migration: `database/migrations/20260103000001_members.sql`)
- `id` — UUID primary key
- `passkey` — Unique passkey (SOULLAB-NAME format or universal key)
- `username` — Unique username for sign-in
- `password_hash` — SHA256 hashed password
- `name` — Display name
- `email` — For passkey recovery
- `onboarded` — Boolean completion flag
- `onboarding_step` — Current step: begin, test-elemental, faq, onboarding, complete

### API Endpoints

| Endpoint | Method | Purpose |
|----------|--------|---------|
| `/api/members/check` | POST | Check if passkey exists (new vs returning) |
| `/api/members/register` | POST | Register new member during onboarding |
| `/api/members/signin` | POST | Authenticate returning member |
| `/api/members/recover` | POST | Send passkey recovery email |
| `/api/members/progress` | GET/POST | Get/update onboarding progress |

### Flow

1. **New user enters passkey** → Check server → Not found → Continue to registration
2. **Registration** → Create member in PostgreSQL → Store session in localStorage
3. **Returning user** → Sign in with username/password → Server validates → Store session
4. **Different device** → Sign in page → Server recognizes by username → Cross-device access

### Recovery

Users who forget their passkey can request email recovery:
1. Click "Forgot your passkey?" on `/signin` or `/test-elemental`
2. Enter email address
3. Server sends passkey + username via Resend
4. User returns to sign-in

## Bridge D: Spiral State Persistence (Anti-Regression Layer)

Prevents MAIA from treating returning members like brand-new people. NOT personalization. NOT psychometrics. Just continuity.

### What Gets Persisted

Table: `member_spiral_state` (migration: `database/migrations/20260213200001_member_spiral_state.sql`)
- `dominant_element` — Current element (fire/water/earth/air/aether) from conductor hysteresis
- `phase` — Spiral phase (1-12)
- `motion` — Movement pattern (ascending/stuck/breakthrough, nullable)
- `intensity` — Signal strength (0-1, nullable)
- `relational_phase` — Maturation stage (1=orientation, 2=capacity, 3=autonomy, 4=seasonal return)
- `autonomy_streak` — Consecutive autonomous sessions
- `return_count` — Times returned after autonomy

### Implementation

Module: `lib/consciousness/spiralStatePersistence.ts`
- `loadSpiralState(memberId)` — Read at conversation start (graceful fallback on error)
- `upsertSpiralState(memberId, update)` — Fire-and-forget write (never blocks oracle)

Wire points in `app/api/oracle/conversation/route.ts`:
1. **Load early** (line ~415): `const spiralState = await loadSpiralState(userId);`
2. **Pass to conductor** (line ~1049): `persistedState: { dominant_element, phase }`
3. **Upsert late** (line ~1067): `upsertSpiralState(userId, { element, phase, motion, intensity });` (fire-and-forget)

Conductor enhancement (`lib/voice/conductor.ts`):
- If `persistedState` exists and member has no hysteresis buffer, seed from database
- Prevents element reset on server restart

### Design Principles

1. **Fire-and-forget writes** — like voiceSovereignty pattern (no await, no blocking)
2. **Graceful fallback on read** — if load fails, conversation continues normally
3. **No conversation content** — only structural position (element/phase/motion)
4. **Upsert-safe** — first insert creates, updates modify existing
5. **Server restart resilient** — hysteresis buffer seeds from DB if empty

### Verification

```bash
# Check migration applied
psql -U soullab maia_consciousness -f scripts/verify-bridge-d-db.sql

# Verify continuity
# 1. Have conversation (3+ turns)
# 2. Restart server
# 3. Continue conversation
# Expected: Element maintained from database, not reset to default
```

See: `docs/bridge-d-verification.md` for full verification guide.

## Architecture

- This is a Next.js 16 app using Turbopack
- Voice modes: Talk (dialogue), Care (counsel), Note (scribe)
- Processing paths: FAST (<2s), CORE (2-6s), DEEP (6-20s)
- Consciousness framework: Spiralogic (see `/lib/maia/spiralogicReference.ts`)

## Co-Lab Release Gate (MANDATORY before tester invites)

**No invite unless `scripts/verify-constitution-colab.ts` passes with 0 failed in production.**

Run inside the container on minisforum:
```bash
docker exec maia-sovereign sh -c 'DATABASE_URL="$DATABASE_URL" npx tsx scripts/verify-constitution-colab.ts'
```

Pass condition: `0 failed` (the script exits non-zero on any failure).
Last observed in production 2026-09-06 on runtime `ca5fdff44`: `33 passed · 0 failed · 0 warned`
(read-only run executed in-session via the founder's connected host — not a manual founder execution).

⚠️ The filename `verify-colab-boundaries.ts` in earlier copies of this file **never existed on any
branch**, so the mandatory pre-invite gate could not be run as written. The gate is the `failed`
column, never the total — a total moves whenever checks are added, and a total quoted without a run
behind it is a claim, not evidence.

This gate runs automatically as part of `scripts/deploy-production.sh` smoke tests. It must also be run manually before any tester wave. See `docs/ops/COLAB_RELEASE_GATE.md` for the full gate specification — what it checks, which surfaces trigger it, and how to add new checks when new scoped surfaces ship.

Triggers: Co-Lab changes · Studio people · DMs · sessions/encounters · files · memory atoms · onboarding · invitations/roles · any migration touching those tables.

## Before Making Changes

1. Search codebase for existing implementations
2. Run `npm run check:no-supabase` to verify no Supabase violations
3. Run `npm run preflight` for full sovereignty check
4. Run `npm run typecheck` for TypeScript validation — the enforced **no-regression gate**. It runs the application-wide `tsconfig.ship.json` (~3,965 files, including `app/**`, `components/**`, `middleware.ts`) and compares against `typecheck-baseline.json`. It **fails** on a new diagnostic, an increased occurrence count, or a path that left the program while still existing on disk.
   - `npm run typecheck:full` — complete current diagnostic inventory (239 pre-existing errors as of 2026-07-30; this is debt, not a gate).
   - `npm run typecheck:entrypoint` — narrow single-entrypoint check of `app/api/between/chat/route.ts` only.
   - `npm run typecheck:baseline` — **dry run only.** Prints a before/after summary and names any error it would bless, then refuses to write. Recording requires the explicit `npm run typecheck:baseline -- --accept-current`. Re-baselining is a governed act: use it to lock in fixes or an intentional, reviewed coverage change — never to absorb a new error.
   - ⚠️ **`npm run typecheck` green is not proof that everything typechecks** — it is proof that nothing got *worse*. For the absolute state, use `typecheck:full`.
   - ⚠️ Historical note: before 2026-07-30, `npm run typecheck` checked **one file** (`app/api/between/chat/route.ts`) and its import graph — 409 files, zero `.tsx`, zero `components/**`. Any pre-2026-07-30 lane citing "typecheck passes" as evidence was citing that entrypoint smoke, not application validation. See `docs/ops/TYPECHECK_GATE_COVERAGE_AUDIT_2026-07-30.md`.
5. Test with `npm run smoke` before committing
6. **Sovereignty Invariant Check** — For any feature that touches voice, expression, relational tone, or user-facing behavior, ask:
   - Does this increase user agency?
   - Does this push life outward into the world?
   - Does this reduce the system's psychological centrality over time?
   - **Cultural sovereignty / Invariant 14:** Are we imposing a framework, translating the member's meaning into our vocabulary, or assuming "self," "growth," "healing," "family," or "spirit" mean the same everywhere? If uncertain, ask more, preserve the member's language, or gate the feature behind member-initiated use.
   - If the honest answer to any is no, the feature does not ship. (See `docs/canon/MAIA_SOVEREIGNTY_INVARIANTS.md`)

   **Growth-obligation check** (founder-added 2026-08-04; see `docs/canon/RECIPROCAL_SOVEREIGNTY_INTENTION_2026-08-04.md`) — for any change that increases a capability (memory, personalization, pattern recognition, wisdom integration, autonomy), also answer:
   - What uncertainty does this introduce, and how is that uncertainty preserved?
   - What provenance and ownership boundaries does this require?
   - What new responsibility does this capability create?

   These are answered, not passed: a change that cannot answer them is incomplete work. The governing law: *every increase in capability must produce a matching increase in provenance, restraint, and transparency.*

## Setup (New Clones)

After cloning this repo, run once:
```bash
./scripts/setup-githooks.sh
```

This configures versioned git hooks that enforce sovereignty on every commit.

# context-mode — MANDATORY routing rules

You have context-mode MCP tools available. These rules are NOT optional — they protect your context window from flooding. A single unrouted command can dump 56 KB into context and waste the entire session.

## BLOCKED commands — do NOT attempt these

### curl / wget — BLOCKED
Any Bash command containing `curl` or `wget` is intercepted and replaced with an error message. Do NOT retry.
Instead use:
- `ctx_fetch_and_index(url, source)` to fetch and index web pages
- `ctx_execute(language: "javascript", code: "const r = await fetch(...)")` to run HTTP calls in sandbox

### Inline HTTP — BLOCKED
Any Bash command containing `fetch('http`, `requests.get(`, `requests.post(`, `http.get(`, or `http.request(` is intercepted and replaced with an error message. Do NOT retry with Bash.
Instead use:
- `ctx_execute(language, code)` to run HTTP calls in sandbox — only stdout enters context

### WebFetch — BLOCKED
WebFetch calls are denied entirely. The URL is extracted and you are told to use `ctx_fetch_and_index` instead.
Instead use:
- `ctx_fetch_and_index(url, source)` then `ctx_search(queries)` to query the indexed content

## REDIRECTED tools — use sandbox equivalents

### Bash (>20 lines output)
Bash is ONLY for: `git`, `mkdir`, `rm`, `mv`, `cd`, `ls`, `npm install`, `pip install`, and other short-output commands.
For everything else, use:
- `ctx_batch_execute(commands, queries)` — run multiple commands + search in ONE call
- `ctx_execute(language: "shell", code: "...")` — run in sandbox, only stdout enters context

### Read (for analysis)
If you are reading a file to **Edit** it → Read is correct (Edit needs content in context).
If you are reading to **analyze, explore, or summarize** → use `ctx_execute_file(path, language, code)` instead. Only your printed summary enters context. The raw file content stays in the sandbox.

### Grep (large results)
Grep results can flood context. Use `ctx_execute(language: "shell", code: "grep ...")` to run searches in sandbox. Only your printed summary enters context.

## Tool selection hierarchy

1. **GATHER**: `ctx_batch_execute(commands, queries)` — Primary tool. Runs all commands, auto-indexes output, returns search results. ONE call replaces 30+ individual calls.
2. **FOLLOW-UP**: `ctx_search(queries: ["q1", "q2", ...])` — Query indexed content. Pass ALL questions as array in ONE call.
3. **PROCESSING**: `ctx_execute(language, code)` | `ctx_execute_file(path, language, code)` — Sandbox execution. Only stdout enters context.
4. **WEB**: `ctx_fetch_and_index(url, source)` then `ctx_search(queries)` — Fetch, chunk, index, query. Raw HTML never enters context.
5. **INDEX**: `ctx_index(content, source)` — Store content in FTS5 knowledge base for later search.

## Subagent routing

When spawning subagents (Agent/Task tool), the routing block is automatically injected into their prompt. Bash-type subagents are upgraded to general-purpose so they have access to MCP tools. You do NOT need to manually instruct subagents about context-mode.

## Output constraints

- Keep responses under 500 words.
- Write artifacts (code, configs, PRDs) to FILES — never return them as inline text. Return only: file path + 1-line description.
- When indexing content, use descriptive source labels so others can `ctx_search(source: "label")` later.

## ctx commands

| Command | Action |
|---------|--------|
| `ctx stats` | Call the `ctx_stats` MCP tool and display the full output verbatim |
| `ctx doctor` | Call the `ctx_doctor` MCP tool, run the returned shell command, display as checklist |
| `ctx upgrade` | Call the `ctx_upgrade` MCP tool, run the returned shell command, display as checklist |
