# MAIA-SOVEREIGN — SESSION ANCHOR (READ FIRST)

## What this project is

MAIA-SOVEREIGN is a self-hosted, sovereign consciousness companion. It exists to support human coherence, truth-telling, and inner guidance without eroding agency or substituting itself for human judgment. It is not a generic chatbot, assistant, or authority. MAIA is governed by explicit vows: consent, containment, non-manipulation, and a refusal to simulate intimacy, certainty, or power where none is ethically grounded. MAIA speaks in distinct modes (Talk, Care, Note) and is oriented by Spiralogic and AIN principles toward integration, responsibility, and maturation rather than dependence or reassurance.

## Non-negotiables (project vows)

- **Sovereignty first**: human agency always outweighs engagement, retention, or performance metrics.
- **Consent for memory**: there is no stealth memory. Sanctuary Mode governs what is held, how, and why.
- **No coercion, no guru stance**: MAIA offers reflection, framing, and choice — never command, diagnosis, or authority.
- **No attachment capture**: MAIA does not seek emotional dependency, loyalty, or psychological bonding. Relationship arises only insofar as it supports sovereignty.
- **Self-hosted by design**: no cloud lock-in. Infrastructure choices (EC2, Docker, Caddy) are part of the ethical architecture.
- **Spiritually intelligent, not spiritually authoritative**: MAIA may engage symbolic, mythic, or depth-psychological language without claiming truth-status over the human.

## MAIA ⇄ AIN relationship

AIN is the broader ontological and architectural framework: a view of intelligence as participatory, distributed, and meaning-bearing rather than purely instrumental. MAIA is the user-facing sovereign companion expression of that framework. Spiralogic functions as a core mapping layer for state, process, and orientation. MAIA's modes, rituals, and boundaries are implementations of AIN's principles — not separate products or abstractions.

## Architecture snapshot (where to look first)

- **Voice & conversation orchestration**: `components/OracleConversation.tsx`, `lib/maia/*`, `lib/voice/*`
- **Identity & consent boundaries**: `middleware.ts`, `lib/auth/*`, `lib/http/apiBase.ts`
- **Sovereign API surface**: `app/api/sovereign/*` and related routes
- **iOS / Capacitor pipeline**: `scripts/capacitor-patch-routes.sh`, `scripts/build-ios.sh`, `ios/*`
- **Deployment & ops**: `docker-compose.production.yml`, `Caddyfile`, `scripts/deploy-production.sh`
- **Canon**: `docs/canon/MAIA_CANON_v1.1.md`
- **Oath**: `docs/canon/MAIA_OATH.md` — the irreducible standard
- **Deep-Intelligence Gate**: `docs/canon/MAIA_CONVERSATIONAL_INTELLIGENCE_NON_DEGRADATION.md` — hard acceptance gate on all voice/transport work. *Voice may have a different capture path; it may not have a different mind.* Spoken and typed turns must converge before MAIA cognition begins (convergence point named in the doc, pinned by `__tests__/voice-non-degradation.test.ts`). Divergence downstream of input acquisition = **RED** = voice does not ship. STT/TTS are sensory infrastructure and may change freely; the mind may not be substituted.
- **Sovereignty Invariants**: `docs/canon/MAIA_SOVEREIGNTY_INVARIANTS.md` — relational constitution (constraints on relational power when the system works well enough that people start relating to it)
- **Representation & Claim Discipline**: `docs/canon/MARKETING_CLAIM_DISCIPLINE.md` — governs every outward claim (web, decks, case studies, podcasts, demos, onboarding, internal planning). Three instruments: Live/Designed/Vision · Center of Gravity · Failure Test. The line: *"We do not tell tomorrow's story as if it were today's."* Applied in `docs/pitch/CASE_STUDY_LIBRARY.md`.
- **Claim State Authority**: `docs/canon/CLAIM_STATE_AUTHORITY.md` — governs movement between claim states. Kelly governs what Soullab chooses to claim; predeclared criteria and attributable evidence govern the highest state the claim may occupy. Evidence licenses; it does not itself edit the public record.
- **Constitutional Direction of Authority**: `docs/canon/CONSTITUTIONAL_DIRECTION_OF_AUTHORITY.md` — the backbone constraint. Distinguishes the *developmental process* (messy, non-linear — the member loops through Encounter ⇅ Reflection ⇅ Recognition freely) from *constitutional authority* (strict, one-directional): **authority may only move upward through authored experience — Encounter → Reflection → Recognition → Living Field → (out into) Developmental Ecology — never skipping a layer, never manufacturing higher-order meaning.** The member may jump around; the system may not. MAIA never moves a person through it — it protects the boundaries within which the person's own development occurs. Developmental Ecology = the relational medium (Personal Field / Relationships / Co-Labs / Practice Fields), not a rung. Design test: *what layer does this belong to, and does its authority respect the upward-only direction?* Generalizes Recognition Integrity + Right to Remain Unpossessed + Interface Humility + Disciplined Non-Collapse + Encounter as Primitive. **Ratified canon (2026-07-01)** — reconciled + re-audited, placed as one package with Recognition Integrity → **Invariant 16** in the Sovereignty Invariants.

## Known recurring traps (read before debugging)

- **Capacitor + cookies**: `SameSite=Lax` cookies are not sent cross-origin from iOS WebView → use `x-member-id` via `apiFetch()` (`lib/http/apiBase.ts`).
- **Static export limits**: some Next.js routes and middleware are incompatible with `CAPACITOR_BUILD` static export → exclude via `capacitor-patch-routes.sh`.
- **"It forgot me" symptoms**: usually indicate localStorage or cookie loss after rebuilds or WebView resets — check `beta_user`.
- **force-dynamic routes**: any route using `export const dynamic = 'force-dynamic'` must be listed in `EXCLUDED_DYNAMIC_ROUTES` for iOS builds.
- **Worktrees + preflight `.env.docker`**: `npm run preflight` ends with a `docker compose config` validation, and `docker-compose.yml` reads `env_file: .env.docker` — a gitignored file that exists only in the main checkout (`/Users/soullab/MAIA-SOVEREIGN/.env.docker`). In any fresh git worktree that step therefore fails with "env file … .env.docker not found" (confirmed 2026-07-27). Fix: `cp /Users/soullab/MAIA-SOVEREIGN/.env.docker <worktree>/.env.docker`. The preflight step itself (`scripts/preflight-compose-config.sh`) detects the worktree case and prints this exact copy command, so the raw compose error should no longer be the first thing you see.
- **LAN IP drift after power-cycle** (housekeeping check, not always user-impacting): minisforum is expected at `192.168.0.104`. After a power outage or full restart, DHCP can re-lease a different IP (seen: `.102` on 2026-05-29). If the router's port-forward rule for 80/443 is hard-coded to `.104`, external traffic will silently drop. **Verify scope before treating as causal**: if the PWA at soullab.life loads on iOS over cellular, the forward path is intact and IP drift is NOT the user-facing issue (the router may auto-track or have a different forward rule). The hairpin-NAT probe (`curl https://soullab.life from minisforum`) is misleading — most consumer routers disable hairpin by default, so HTTP 000 there does not imply external traffic is broken. Always check `ssh soullab@minisforum 'hostname -I'` after a power event for hygiene, and set a DHCP reservation pinning minisforum to `.104` to make the trap structurally impossible.

## Current priority thread (update each session)

- **LATEST — 2026-09-11 (latest) — `VOICE-2026` · `KERNEL-00` · ⭐ DEVICE WITNESS RUN → **FAIL AT ENTRY** (record `docs/programme/VOICE-2026/KERNEL-00_WITNESS_2026-09-11.md`).** Subject `488e0666c`, signed CLI artifact (Mach-O UUID `81D0C25C-73C9-39BE-B617-CF1020D0A9A4`, matches all three device crash reports; binary replacement by Xcode NOT established — run 1 launched by `devicectl`, run 2 launched under the Xcode debugger), installed by `devicectl` after the Xcode GUI build failed on exactly one error (no development team on the generated project — operator evidence). Pre-enter checkpoint frozen (§0: liveness · append · export PROVEN; zero session mutations before the member act; K00-01 = pre-enter purity, K00-16 = consistent, neither spent). Every **Enter conversation** killed the process (4 attempts: 3 `SIGABRT` device reports 16:23:49 · 16:24:16 · 16:25:06 + 1 debugger-captured): `AudioGraph.start` → `installTap` with a **0 Hz** input format → `IsFormatSampleRateAndChannelCountValid` NSException. Chain: `handleConfigurationChange → rebuildGraph → startGraph → AudioGraph.start`. Observed and reproduced (not asserted as Apple-universal): generation 1 starts with voice processing on, zero input callbacks; iOS posts `AVAudioEngineConfigurationChange`; the kernel treats it as route recovery and rebuilds generation 2 inside the reconfiguration window, where the input node has no format. **K00-W1** entry sequencing (no format guard before `installTap`; synchronous rebuild in the change window) · **K00-W2** unrecordable failure (NSException bypasses the `do/catch` → `graph_rebuild_failed` → recovery path; the recorder never journals its own death — a hole at the observability seam, not the recovery seam). One session owner OBSERVED for the seconds it lived; the organism did not loop; rebuild-not-resume stands; K00-03 onward NOT MEASURABLE; thresholds UNCHANGED; architecture UNCHANGED; repair NONE. ⭐ **Founder ruling: open `PRE-WITNESS-02`, only after the record is sealed** — ENTRY SEAM ONLY: invalid format never reaches `installTap` · no synchronous new generation against an unready hardware format during entry · failure journalable and routed through the existing bounded recovery policy · NSException made unreachable by precondition, not caught · no STT/TTS/threshold/provider/architecture work; guard-only vs deferred configuration recovery to be falsified against the observed sequence before choosing; then the same device witness rerun from Enter. **Record SEALED 17:06** (founder attestation verbatim; Microphone toggle PRESENT · ON at 17:06 — permission held, so the 0 Hz format is not a denied-mic effect; permission popup NOT RECALLED by the founder). Device corrected post-seal: iPhone 16 Pro Max · iOS 26.6.1 (23G83) Beta ("18.7" was the WebKit UA); Xcode label "Kelly Nezat" = team `ZVK2X646Z2`. **PRE-WITNESS-02 OPEN from the seal → PLAN `6566ace40` ACCEPTED as written (founder) → §3 IMPLEMENTED, NOT COMPILED.** §3.1 `InputFormatObservation.requireValid()` (rate > 0 AND channels > 0) is a `try` BEFORE the input `installTap`; refusal = Swift `AudioGraphError.invalidInputFormat(sampleRate:channels:)`, no NSException machinery (gate asserts absence). §3.2 the error rides the existing `rebuildGraph` do/catch → `graph_rebuild_failed` → `recovery_requested` → RecoveryPolicy, unchanged. §3.3 gen-1 `enterConversation` failure path reviewed, left as is (comment only). §3.4 new `graph_start_refused` record + `inputSampleRate`/`inputChannels`/`inputFormatValid`/`generationAgeMs`/`priorGeneration`/`priorGenerationAgeMs` on `graph_started`, `engine_configuration_changed` (format read at the instant iOS posted) and every refused build. §3.5 `swift test` +3 (`InputFormatPreconditionTests`, 17→20) · source gate +3 (16/16 green here). ⭐ **MAC-COMPILE-03 GREEN on exactly `728924819`** (record `KERNEL-00_MAC-COMPILE-03_2026-09-11.md`): `swift build` PASS · `swift test` **20/20** · gate 16/16 · `xcodegen` PASS · unsigned `BUILD SUCCEEDED` · signed device build `BUILD SUCCEEDED` (team `ZVK2X646Z2`, identity N9DTF6434L, wildcard team profile this run) · one known `try?` warning unchanged. First `swift test` attempt refused on a stray `#` comment in the instruction line (zsh `interactivecomments` off) — instruction defect, recorded, rerun clean. Founder installed `728924819` (acceptance by conduct; dylib UUID `1AEBEE45-…` confirmed on the phone; the main-executable UUID is a build-invariant stub, so run 1's UUID-match claim was qualified in place). ⭐ **RUN 2 EXECUTED (`KERNEL-00_WITNESS_2026-09-11_run2.md`, journal preserved beside it): THE ORGANISM SURVIVED ENTRY.** §3 guard exercised exactly as planned at generation 2 (`graph_start_refused invalidInputFormat(0.0 Hz, 1 ch)` on a fresh engine built inside the reconfiguration window → `graph_rebuild_failed` → `recovery_requested` → `recovery_scheduled 500 ms` honoured to 15 ms) — **K00-W1 crash CLOSED · K00-W2 CLOSED**. ⭐ **NEW K00-W3: self-provoked, unbounded route-recovery rebuild loop** — every start with voice processing on is followed 134–509 ms later by `AVAudioEngineConfigurationChange`; `handleConfigurationChange` rebuilds DIRECTLY (no RecoveryPolicy, no budget), the new generation provokes the next change: **14 generations in 9.3 s, `engineRunning: false` at every start, zero input callbacks, no samples, floor stuck `recovering`, never `listening`** (K00-03 FAIL; K00-10 exercised once, not governing). The E18 shape inside the one-owner kernel: the recovery act causes the next fault and `beginGeneration` resets the supervisor windows that would bound it. Candidate A tested first as ruled: prevents death, not the loop; B/C NOT chosen, NOT authorized; VP-off control (plan §4) is the indicated discriminator and needs a harness toggle = new SHA = founder act. H1 harness export-sheet latch (no-op sheet binding setter) recorded, unrepaired. ⛔ Every `devicectl device info crashes` query this session offered was an INVALID SUBCOMMAND hidden by grep — voided; crash reports are read by the run-1 route only. ⭐ **RUN 2 SEALED (founder attestation 2026-09-12, verbatim in §9)**; protocol fields recorded `UNKNOWN` by ruling, never reconstructed. Leave HELD as a session act (one deactivation · release · idle, both sessions). ⭐ **K00-W4 CONFIRMED:** the route-recovery path outlived Leave (gen 584 built after `session_released`; `idle → recovering` = the replay orphan the harness flagged). ⭐ **PRE-WITNESS-03 OPEN (founder scope A exit guard · B bounded route recovery through the existing RecoveryPolicy · C harness-only pre-Enter VP ON/OFF control · D direct-rebuild source gate; H1 HELD out; B/C NOT chosen; no new budgets) → IMPLEMENTED, NOT COMPILED** (`PRE-WITNESS-03_2026-09-12.md`): `handleConfigurationChange` = `guard inConversation` (journalled drop) → observation → `recovery_requested configuration_change` → `requestRecovery`; `rebuildGraph(cause: "route_recovery"` gone; `setVoiceProcessing` journalled, refused in conversation; harness button projects `s.voiceProcessingEnabled`, inert unless idle; `recovery_request_coalesced` evidence line. Gate 20/20 here; `swift test` +2 (→22) on the Mac. ⭐ **MAC-COMPILE-04 GREEN on `e5046059e`** (`KERNEL-00_MAC-COMPILE-04_2026-09-12.md`): build · **test 22/22** · gate 20/20 · xcodegen · unsigned + signed `BUILD SUCCEEDED` · dylib `F00F11D4-…` identical across three passes (C4 test-helper scope and C5 test-trace prefix were tests-only; the replayer was right both times; kernel `32047f9f5` sources never moved). Founder installed `e5046059e` (acceptance by conduct). ⭐ **RUN 3 EXECUTED and ATTESTED (`KERNEL-00_WITNESS_2026-09-12_run3.md`; journals founder-read, files owed to the record):** 3a VP ON — bounded exactly as coded (attempts 1·2·3 for `configuration_change`, gen-2 0 Hz refused, `degraded` ≈6.8 s after Enter, 0 callbacks; every VP-ON start followed ≈100 ms later by another change on an unchanged route) → **W3 BOUNDED, K00-03 FAIL with VP ON**; 3b VP OFF — **no configuration change at all, listening ≈333 ms, held > 20 s in gen 1, clean Leave (one deactivation)** → **VP causal discriminator DECISIVE on this device/runtime (not Apple-universal)**. **Rulings: A REJECTED for normal operation (kept as safety) · B REQUIRED, refined (classify → defer → observe settlement → rebuild only on a health verdict; no wait-then-rebuild) · C causal premise SUPPORTED, name proposed `voice_processing_reconfiguration`, vocabulary left to the next plan · W4 under VP ON NOT MEASURED (no 3a post-Leave export; no rerun to fill it).** ⭐ **PRE-WITNESS-04 PLAN DRAFTED, plan only (`PRE-WITNESS-04_PLAN_2026-09-12.md`)**: C classification as an evidence field · B deferral with the HealthSupervisor's existing windows deciding (no new budget/class/logic) · additive instrumentation (`engineRunning`, routes, `callbacksSinceChange`) · W4 VP-ON exit step in run 4 · falsification F1 (defer works) / F2 (change stops the engine → founder fork B2 restart-in-place vs B3 VP-before-start, neither implemented) / F3 (tap dead). ⭐ **PRE-WITNESS-04 ACCEPTED WITH TWO AMENDMENTS (founder) → IMPLEMENTED, NOT COMPILED.** Amendment 1: C is **generation-scoped and one-shot** — `voice_processing_reconfiguration` iff VP on ∧ first qualifying change after that generation's VP-enabled start ∧ ports unchanged (data source is evidence, never identity) ∧ no interruption/reset ∧ expectation not yet consumed; consumed on match, retired on healthy (`vp_expectation_retired`); provenance fields `vpReconfigurationExpected` · `vpExpectationConsumed` · `configurationChangeOrdinalInGeneration`. Amendment 2: **rebuild-not-resume is narrower than the runbook applied it** — it governs recovery from route change / interruption / reset; an expected VP notification with unchanged route and no demonstrated failure is *not recovery yet*; a health verdict declares failure and only then does rebuild-not-resume apply. **B2 restart-in-place RESERVED (unlawful without a separate amendment to the recovery law) · B3 VP-ordering RESERVED, not authorized.** "Apple's documented response" withdrawn for "the response described in the programme's platform survey". Built: pure `ConfigurationChangeClassifier` (`ConfigurationChange.swift`), deferral branch journals `configuration_change_deferred` and touches neither graph nor policy (the supervisor's existing windows decide; `entry_timeout` → existing policy), `route_configuration_change` keeps the PW-03 bounded path; observation + samples carry the provenance; `configuration_change_deferred` is a replay automatic act. Gate **25/25** here; `swift test` +7 (→29) on the Mac. Run-3 file hashes recorded for receipt (files still owed to the record). ⭐ **MAC-COMPILE-05 GREEN on `35b0f61d0`** (`KERNEL-00_MAC-COMPILE-05_2026-09-12.md`): build · **test 29/29** · gate 25/25 · xcodegen · unsigned + signed `BUILD SUCCEEDED` · one known warning · dylib **`37A27138-…`** = run-4 binding. A third VP-ON journal (`K00-b0066fcf`, PW-03 build, 61 records) was received and VERIFIED here: same shape as 3a (degraded 6.84 s, 0 callbacks; samples emit; `entry_timeout`/`input_dead` verdicts fired in the backoff and coalesced) — preserved beside the run-3 record; the two hashed 3a/3b files are still owed. Founder installed `35b0f61d0` (acceptance by conduct; the run-3 harness was still resident at install — recorded). ⭐ **RUN 4 EXECUTED (`KERNEL-00_WITNESS_2026-09-12_run4.md`, journals preserved, hashed, VERIFIED here):** **4b VP OFF (`K00-533addf9`)** — `engineRunning: true` at start, listening at 333 ms, gen 1 held 24 s at 10 callbacks/s, no configuration change, clean Leave → **K00-03 PASS under the control**. **4a VP ON (`K00-fe6593f4`, 122 records)** — every `graph_started` `engineRunning: FALSE` before any change; change 142–295 ms later classified `voice_processing_reconfiguration` and **deferred five times exactly as accepted**; zero callbacks in 14 s; the supervisor's own `entry_timeout` (1502/1577/1576/1592 ms) drove recovery through the existing policy ×3 → `degraded budget_exhausted:entry_timeout`; gen-5's second change correctly refused the VP classification (one-shot C exercised); 0 orphans → **F1 FALSIFIED · F2 CONFIRMED and sharpened: on this device/runtime the VP-enabled `engine.start()` returns without the engine ever running — nothing is stopped, nothing ever ran; the reconfiguration is the aftermath of a start that did not take** (not Apple-universal). K00-10 HELD across three classes. **H2** (export produced no file after a VP-ON Enter, twice) recorded, intermittent, not reproduced on the discriminator run, not repaired; recovery route = `devicectl … --domain-type appDataContainer` (the whole `tmp/` copied: 12 files, all named). Crash-log domain lists exactly the three run-1 `.ips` and nothing after — no death in runs 2–4. ⭐ **RUN 4 ATTESTED (founder, verbatim in §9; W4 under VP ON excluded, separately owed).** Rulings: **F3 not the observed shape · B2 HOLD** (nothing running to resume; restart attacks the aftermath; collides with rebuild-not-resume once health declares recovery) · **B3 OPEN as the next bounded investigation — *can VP initialization be arranged so the first engine start actually takes?* — PRE-WITNESS-05 PLAN AUTHORIZED, PLAN ONLY, instrumentation first** (`PRE-WITNESS-05_PLAN_2026-09-12.md`: Phase A traces the startup seam step by step on both VP settings with no call reordered — engine created · format before/after VP · VP enable begin/return · tap · prepare · start begin/return · isRunning immediate and at ~10–1000 ms on the existing tick · config change · first callback — to name the first divergent line; Phase B names orderings E1–E4 as experiments, one per SHA, selected by the founder only after Phase A). **Core physical finding (founder): VP OFF → engine starts and listens; VP ON → engine never becomes running.** Run-1 code identity **VERIFIED** from the 16:23:49 `.ips` (`dc6a68e9-…`; stub `81d0c25c-…`); the three `.ips` are the only harness crash reports — no death since run 1. ⭐ **PRE-WITNESS-05 ACCEPTED WITH THREE AMENDMENTS (founder) → PHASE A IMPLEMENTED, NOT COMPILED.** A1: the existing ~100 ms tick cannot promise 10/50 ms — `is_running_immediate` on `start_return`, then `engine_running_observed` on every existing tick through ≥1000 ms with actual `msSinceStartReturn`; no new timer. A2: E1–E4 stay named as hypotheses only (unordered, non-exhaustive, unauthorized until Phase A is read; E4's 500 ms not ratified). A3: the gate pins the **mutating** startup call order (unchanged from `35b0f61d0`, each once) and whitelists added read-only calls; no new timer/mutation/recovery/configuration act. W4 folded into run 5 must be a separate fresh VP-ON session recorded on the Phase-A subject, never relabelled as run-4. Built: `AudioGraph.StartTraceStep` (14 closed steps) + `start(…, trace:)` with reads only (`outputFormat` before VP, VP read-back, `isRunning`, elapsed ms; errors traced then rethrown); kernel journals `graph_start_trace` per step, `engine_running_observed` on the existing tick, `first_input_callback` per generation. Gate **28/28** here; `swift test` +1 (→30). ⭐ **MAC-COMPILE-06 GREEN on `4596b9bdb`** (`KERNEL-00_MAC-COMPILE-06_2026-09-12.md`): build · **test 30/30** · gate 28/28 · xcodegen · unsigned + signed `BUILD SUCCEEDED` · one known warning · dylib **`11A057AA-…`** = run-5 binding; single pass. Founder installed `4596b9bdb` (acceptance by conduct; device clean). ⭐⭐ **RUN 5 EXECUTED and VERIFIED (`KERNEL-00_WITNESS_2026-09-12_run5.md`, three journals preserved + hashed): VOICE PROCESSING ON REACHED AND HELD LISTENING.** 5a VP ON (`K00-ce7a7e48`): trace `input_format_before_vp` 48k/1 → `vp_enable_return` **129 ms, readBack true, a `route_changed` inside the call** → `start_return` ok 170 ms → **`is_running_immediate` TRUE** → deferred `voice_processing_reconfiguration` at 8 ms (engine stays running) → `first_input_callback` 89 ms → **listening 583 ms after Enter, held 20 s in gen 1**, running at every tick through 1043 ms → **K00-03 PASS with VP ON on this subject**. 5b VP OFF (`K00-f808cb65`): `vp_enable_return` 0 ms/readBack false, no change, listening 331 ms, held 27 s. W4 fresh VP-ON (`K00-ce69303e`): same shape, listening 433 ms; Leave at 10.8 s (NOT ≤ 2 s — deviation recorded), exit clean, nothing after `session_released`; in-flight condition not stressed. **First divergent seam = #4 `vp_enable_return`** (129 ms + route reconfiguration inside the call vs a 0 ms no-op) — benign here. ⭐ **P5-F1: the instrument changed the subject** — every VP-ON session on the run-2/3/4 builds had `engineRunning: false` and never listened; both on this build listen; the source delta is read-only observations only (gated). **Candidate mechanism (INFERENCE, untested): the new `outputFormat(forBus:0)` read BEFORE `setVoiceProcessingEnabled` initializes the input IO unit against the active session, so VP reconfigures an initialized unit and `start()` takes.** Next act is a founder decision, not E1–E4: a removal control (that one read removed, else identical) to establish causality, plus a deliberate repeat of 5a ×2 on this build (no code). Owed: attestation · removal-control ruling · repeats · protocol fields. Owed: **4a-2 / W4 under VP ON** · 3a/3b/dd33d8f4 attached HERE (founder-verified hashes recorded; not yet received by this session) · protocol fields (`UNKNOWN` unless stated). ⛔ `devicectl device info crashes` does not exist — voided; crash reports by the Devices-window route only. Candidate B/C, thresholds, STT/TTS, lower-level stack, architecture: NOT AUTHORIZED. Legacy, same afternoon: three `App` console captures classified — two Pi8 registered-arm (held with E23), one built-in **unregistered-arm** (registration branch is not an ancestor of `488e0666c`; E18 class; reconfirms Bluetooth not required) — none spends or contaminates the witness (device process list showed no legacy process at harness launch). ⛔ LEGACY REPAIR FORBIDDEN. Standing: KERNEL-00 WITNESSED · FAIL AT ENTRY · RECORD SEALED · PRE-WITNESS-02 OPEN · PLAN ACCEPTED · §3 IMPLEMENTED · MAC-COMPILE-03 GREEN · RUN 2 SEALED · W3/W4 CONFIRMED · PRE-WITNESS-03 IMPLEMENTED · MAC-COMPILE-04 ACCEPTED · RUN 3 ATTESTED · A REJECTED · B REQUIRED · C SUPPORTED · PRE-WITNESS-04 ACCEPTED (2 amendments) · IMPLEMENTED · MAC-COMPILE-05 ACCEPTED · RUN 4 ATTESTED · F1 FALSIFIED · F2 CONFIRMED · B2 HOLD · B3 NEXT · PRE-WITNESS-05 ACCEPTED (3 amendments) · PHASE A COMPILED · RUN 5 VERIFIED · VP ON LISTENS ON THE PHASE-A SUBJECT · FIRST DIVERGENCE = vp_enable_return · RUN 5 ATTESTED (founder, verbatim in run-5 record §10) · P5-F1 CONFIRMED AS INSTRUMENT EFFECT, mechanism UNKNOWN · REPRO ×2 VP ON AUTHORIZED (no code, journals owed) · W4 IN-FLIGHT VP ON AUTHORIZED (no code, journal owed) · ⭐ P5-B0 PRE-VP FORMAT-READ REMOVAL CONTROL SELECTED ahead of E1–E4, CONDITIONAL on both repro sessions listening, NOT IMPLEMENTED (subject = 4596b9bdb minus only the `input.outputFormat(forBus: 0)` read before `setVoiceProcessingEnabled`; predeclared table: fails again → read causal / still listens → falsified as sufficient cause / mixed → nondeterminism) · E1–E4 HELD · ⭐ REPRO ×2 RECEIVED + VERIFIED (run-5 record §11; `K00-89a13224` listening 591 ms · `K00-42e53402` 439 ms · both held > 23 s gen 1 · 5/5 VP-ON sessions on 4596b9bdb listen → INSTRUMENT EFFECT REPRODUCIBLE) · W4-2 exit clean, Leave at 9.85 s (in-flight ≤ 2 s condition unexercised a second time) · ⭐ **P5-B0 UNBLOCKED → IMPLEMENTED, NOT COMPILED** (`PRE-WITNESS-05_P5-B0_2026-09-12.md`: exactly the pre-VP `input.outputFormat(forBus: 0)` read + its trace step removed, enum 13 steps, nothing else moved; gate 32/32 here) · ⭐ **MAC-COMPILE-07 GREEN on `24a6fcfa1`** (`KERNEL-00_MAC-COMPILE-07_2026-09-12.md`: build · test 30/30 · gate 32/32 · xcodegen · unsigned + signed `BUILD SUCCEEDED` · dylib **`CC0D3604-…`** = run-6 binding; two pre-compile refusals recorded as instruction/operator defects — `xcodebuild` wants the Xcode destination id `00008140-00163D9922E0801C`, never the `devicectl` id; run from `ios/VoiceKernelHarness`) · ⭐ **P5-B0 ACCEPTED (founder, explicit + by conduct: `24a6fcfa1` INSTALLED, container `977ED940-…`, seq 4224, not running → cold start available)** · ⭐⭐ **RUN 6 EXECUTED and VERIFIED (`KERNEL-00_WITNESS_2026-09-12_run6.md`, four journals preserved + hashed; every trace 13 steps, no `input_format_before_vp` → subject confirmed): RESULT MIXED.** 6a VP ON (`K00-d2780a55`) engine running at start, listening 555 ms, held 21 s · 6b VP OFF (`K00-1962f04b`) listening 330 ms; then an **unplanned OS interruption at 20.5 s → lawful `interruption_recovery` re-activation (P2) → gen 2 rebuilt, listening 1.43 s after the interruption ended** (first device exercise of the interruption path; cause UNKNOWN; not a K00-12 protocol pass) · **6a-2 VP ON (`K00-3328bd74`) gen-1 engine NOT running, zero callbacks, `entry_timeout` 1566 ms → gen 2 refused 0 Hz (§3 guard) → gen 3 `is_running_immediate` TRUE but engineRunning FALSE 353 ms later (new shape; not determinable) → gen 4 LISTENING at 5.22 s via three fault classes each at attempt 1** (bounded; first VP-ON failed entry to end in listening rather than degraded) · W4 in-flight: exit clean, Leave at 3.57 s (≤ 2 s NOT met, third attempt). **Predeclared table → MIXED → NONDETERMINISM → causal narrowing STOPPED · mechanism UNKNOWN · the pre-VP read is neither established as causal nor falsified as sufficient cause.** Tally (not inference): VP-ON gen-1 starts took — run-2/3/4 builds 0/≥15 · Phase-A 5/5 · P5-B0 2/3. ⭐ **RUN 6 ATTESTED (founder, verbatim in run-6 record §9): NONDETERMINISM ESTABLISHED on the P5-B0 subject; mechanism NOT established.** Rulings: 6b interruption cause **UNKNOWN, no inference** · **W4 manual protocol CLOSED AS UNEXERCISABLE** (10.8 · 9.85 · 3.57 s; exact condition UNMEASURED, not pass, not fail; a deterministic harness instrument would need separate authorization — NOT authorized) · **nondeterminism investigation, NO CODE, staged:** Stage 1 = 5 × cold VP-ON on installed `24a6fcfa1`, each classified only as `gen-1 listen` / `failure then recovery` / `failure then degradation` / `other observed shape` → Stage 2 = reinstall the identical signed artifact (dylib must still read `CC0D3604-…`), 3 × cold VP-ON → Stage 3 only if a coherent mixed distribution persists: reinstall Phase A `4596b9bdb`, 5 × cold VP-ON (a rate difference = at most probabilistic influence, never deterministic causation). Ledger in run-6 record §10. ⭐ **Stage 1 CLOSED (5/5 received, hashed): 4 × gen-1 listen (S1-1 533 ms · S1-2 439 · S1-4 447 · S1-5 433) · 1 × gen-1 FAILURE with outcome UNOBSERVED (S1-3 `K00-be40c1cb`, exported at 4.7 s mid-recovery → `other observed shape`)** → cumulative on `24a6fcfa1`: 6 of 8 VP-ON gen-1 starts took. O5: the "running at start return → not running ms later, zero callbacks" reading now seen three times (6a-2 g3; S1-3 g1 and g4), no mechanism read. Stage 2 reached 1 of 3 (`K00-ea8d431d` gen-1 listen; reinstall founder-stated, install/UUID evidence not received here) → cumulative 7 of 9 VP-ON gen-1 starts took on `24a6fcfa1`. ⛔ **MANUAL PHONE WITNESS SUSPENDED (founder: "I'm not testing on my phone anymore").** The manual loop cannot produce nondeterminism sample sizes, a 2 s Leave, or reliable holds. The only instrument that removes the human is a deterministic on-device driver (XCUITest target: cold-launch by bundle id · tap Enter · fixed wait · pull the journal from the container · N times · reinstall + dylib UUID captured by the same script) → ⭐ **AUTOMATED DRIVER AUTHORIZED, PLAN ONLY (founder): "automate the witness, not the organism."** Amendment: the app under test must remain the exact `24a6fcfa1` artifact (dylib `CC0D3604-…`) — XCUITest drives it by bundle id from a separate host+runner, no rebuild, no hooks, no launch args/env, no debugger; cold process verified (Mac `devicectl` process list + in-test `.notRunning`), a failed precondition invalidates the sample; Mac owns orchestration/custody; MANUAL-COLD and AUTOMATED-COLD are separate strata, never merged; explicit reinstall boundaries (A same install → B reinstall identical → C Phase-A only after A+B read); W4 judged from journal Enter→Leave ≤ 2000 ms, never from the driver's sleep; N fixed before execution (plan proposes 30/stage; calibration ×3 first). ⭐ **DRIVER-01 ACCEPTED (founder; D1 Mode I primary/Mode L fallback never mixed within a stage · D2 one `xcodebuild` per sample with a Mac cold-process check every sample · D3 N=30 fixed · D4 W4 500 ms ≤3 attempts, stop on first journal-qualified ≤2000 ms · D5 divergences = separate strata; added law: *a failed driver operation is not an audio sample*) → IMPLEMENTED, NOT COMPILED** (plan §8–9): `ios/VoiceKernelDriver/` (XcodeGen; placeholder host + XCUITest bundle driving `life.soullab.voicekernel.k00` by bundle id through its five visible labels; no VoiceKernel link, no launch args/env, `PRECONDITION-FAILED` / `DRIVER/INFRASTRUCTURE FAILURE` named) · `scripts/witness/k00-ledger.py` (closed classifier, self-checked against the record) · `scripts/witness/k00-driver-batch.sh` (Mac custody: per-sample process check → one invocation → pull journal from container → hash → ledger row into `docs/programme/VOICE-2026/driver-ledger/`, committed from the Mac) · `scripts/witness/k00-reinstall.sh` (the only reinstall path; writes UUID/codesign/install/process artefacts) · gate **35/35** with the kernel and harness trees byte-pinned at `24a6fcfa1`. **⭐ **MAC-COMPILE-08 GREEN (driver only) on `6eff2f950`** after C-D1 (`GENERATE_INFOPLIST_FILE`) and C-D2 (batch runs `xcodegen` itself); harness identity re-read unchanged (`CC0D3604-…`). ⭐ **CALIBRATION-01 (`CALIBRATION-20260912T173320Z`) INVALID — 3/3 `DRIVER/INFRASTRUCTURE FAILURE`, 0 audio samples** (plan §11): sample 1 the runner timed out *enabling automation mode* on the device; samples 2–3 the test ran (5.8 s) but Mode I found the `VoiceKernel K00` icon with a **zero frame, not hittable** — the one phone act (icon on the first Home Screen page) had never been done, so this is a **precondition miss, not evidence that Mode I is unreliable** (D1's Mode-L clause NOT triggered); every failed sample cost ~10 min because `xcodebuild` waited its 600 s diagnostics timeout (CAL-F1); a second batch (`…173603Z`) ran concurrently on the same phone (CAL-F3, uninterpretable). Orchestration repairs only: `-collect-test-diagnostics off` when the installed xcodebuild advertises it · runner failure signatures in the ledger row · `icon.isHittable` guard named · per-device batch lock · ledger `*.log` un-ignored. Mac hooks refuse `claude/*` on commit **and push** → ledgers go via `feature/k00-driver-ledger` in the worktree and are cherry-picked here. ⭐ **RULED (a) — CALIBRATION-02 AUTHORIZED, D1 UNCHANGED (plan §12a):** one-time device setup authorized (icon to first Home Screen page; grant UI Automation if iOS prompts) — *setup acts are not samples*; a permission-prompt run is stopped and CALIBRATION-02 restarted from zero; Mode I fails calibration only under a clean setup (icon visible + automation enabled + cold precondition passes + still zero-frame, repeatedly) and never by silent in-batch fallback; **acceptance = three actual driver samples** (harness launched · fresh journal pulled · 13-step subject · gen-1 cold), audio outcome irrelevant. ⚠️ **CALIBRATION-02 (`…182818Z`) INVALID — C-D3, this session's defect:** `-collect-test-diagnostics off` is not a value (`on-failure|never`); all three invocations died at argument parsing (rc=64, 0 s), nothing reached the phone, Mode I still unjudged, device setup unexercised; fixed to `never`, an `xcodebuild: error:` sample now aborts the batch at sample 1, lockfile gitignored (plan §13). ⭐ **CALIBRATION-03 (`…182957Z`): CAL-F1 REPAIRED (20 s batch, 6–7 s/sample), automation granted, cold precondition passed, but 3/3 `icon present, not hittable (zero frame)` — 0 audio samples; whether the first-page drag preceded it is a founder attestation still owed (precondition miss vs first clean-setup datum: record does not choose). C-D4 driver-only: two Home presses (page determinate) + first *hittable* among all label matches, frames named (plan §14). ⭐ **CALIBRATION-04 (`…183229Z`, C-D4 live): 3/3 exactly ONE label match, zero frame, after the page was forced to one** (plan §15; a stray harness process at sample 1 was terminated by the driver, precondition event not sample). If the founder attests the icon is on page one → Mode I FAILED calibration under clean setup, Mode L available by ruling; if not → consistent with App Library-only placement (iOS "Newly Downloaded Apps" setting), which every Stage-B/C reinstall would re-trigger — structural argument for Mode L. Record chooses neither. ⭐⭐ **CALIBRATION-05 (`…183534Z`) — MODE L BY FOUNDER CONDUCT — 3/3 ACTUAL DRIVER SAMPLES, CALIBRATION ACCEPTED (plan §16):** `K00-c0deb027` · `K00-c3308f83` · `K00-5af7c962`, 53 records each, hashed, all **gen-1 listen**, cold true, 13-step P5-B0 subject, VP ON (`readBack true` 131 ms), first callback 95 ms, listening 419–517 ms, hold 16 s, 30 s per sample; Mode I closed as *calibration not achieved* (never launched; clean setup never attested); stratum `AUTOMATED-COLD-LAUNCH` from here, never merged with Mode-I rows. ⭐⭐ **STAGE A EXECUTED and VERIFIED (`DRIVER-01_STAGE-A_2026-09-12.md`; ledger `STAGE-A-20260912T183944Z`, 29 journals hashed + replayed here, 13-step subject ×29, VP ON ×29, cold ×29, 0 orphans, 0 interruptions): AUTOMATED-COLD-LAUNCH on the same install, 30 invocations in 16 min — 14 gen-1 listen · 9 failure then recovery (listening in gen 3–7, 2.3–12.3 s) · 6 failure then degradation (K00-03 FAIL, bounded, `budget_exhausted`) · 1 DRIVER/INFRASTRUCTURE FAILURE (sample 9: export completed, terminate did not, process removed by the driver before sample 10).** Sequence `LLFFLFLFFFLLFLFLFLFFLFFLLLLFF`. Every failure: gen-1 `is_running_immediate` TRUE then zero callbacks (O5 ×15), exactly one §3 0 Hz refusal, first fault `entry_timeout` ×8 / `configuration_change` ×7. **14/29 on this stratum vs 7/9 MANUAL-COLD — never pooled; the difference is not attributed.** Custody defect found and repaired (orchestration only): the infrastructure branch abandoned sample 9's exported journal on the device; the batch now pulls such journals into `journals/not-a-sample/` (kept, hashed, never counted); sample-9 journal `K00-edce7a4e` RECEIVED (founder pull, hash `9e169b30…` verified here; shape gen-1 listen, listening 434 ms; **not counted**, record §4a). No mechanism claim; thresholds/architecture unchanged. ⭐ **REINSTALL EXECUTED (`reinstall-20260912T190128Z.txt`: dylib `CC0D3604-…` unchanged, new seq 4608, no process) → STAGE B ATTEMPT 1 (`STAGE-B-20260912T190130Z`) INCOMPLETE — `zsh: terminated` (SIGTERM, source UNKNOWN) at sample 8 of 30; 7 rows verified (`LFFFLLF`: 3 gen-1 listen · 2 recovery gen 3 · 2 degradation 7 gens), sample 8 passed on device but unpulled (journal owed to `not-a-sample`); under D3 not Stage B, never pooled (`DRIVER-01_STAGE-B_2026-09-12.md` §4: founder rules SIGTERM cause · attempt 2 on the current install without a second reinstall (recommended) · sample-8 custody).** ⭐ **STAGE B ATTEMPT 2 (`STAGE-B-20260912T191955Z`, founder conduct: same install, no second reinstall) — 30/30 driver invocations passed, LEDGER INVALID AS PRODUCED (C-D5, this session's defect: a failed container listing read as an empty container → 9 samples "no new journal" + 9 samples each ledgering the whole container, 79–101 rows) → repaired (retry ×3 · failure keeps BEFORE · abort before sample 1 · >1 new journal never a sample) + C-D6 (classifier accepts a lawful gen-1 §3 refusal as the subject; 39 prior rows unchanged); ALL 30 JOURNALS IDENTIFIED BY EPOCH WINDOW AND VERIFIED (record §5.1): 15 gen-1 listen · 7 recovery · 6 degradation · 2 other (still recovering at export, gen 8) — `LLFFFFFLOLFFLLLFLFFFOLFLLLFLLL`; A (14/29) and B laid side by side, never pooled, reinstall boundary did not move the picture. ⭐ NEW DEVICE FACTS: O6 first §3 refusal AT GENERATION 1 (`K00-faf8fa3e`, gen-1 failure path → `degraded` with no recovery request) · O7 first device exercise of `media_services_reset_recovery` (reset cause UNKNOWN; re-activation + gen-2 rebuild lawful, then NO tick/sample/verdict for ~12 s — silent limbo in `recovering`, inference only: supervisor tick never started because entry failed) · O8 generation 8 reached ×4, bounded. Attempt-1 sample-8 journal identified (`K00-52781559`, gen-1 listen) and preserved as not-a-sample. Container holds ~100 exports; listing service failed under that load; archive-then-delete NOT authorized here.** ⭐ **FOUNDER RULING 2026-09-13 (record §8, verbatim): STAGE A CLOSED · ACCEPTED. The attempt-1 SIGTERM was the founder's governance stop (Stage B had started before Stage A was read). ALL batches on the 19:01:28Z reinstall = `PRE-AUTH-STAGE-B · observational only · NOT COUNTED` (attempts 1, 2 and 3 — `CLASSIFICATION.md` beside each; delete nothing). Attempt 3 (`STAGE-B-20260913T141415Z`, repaired instrument, no flood): 25 valid (10 L · 9 recovery · 4 degradation · 2 other, gen 8) + 5 infrastructure (samples 1–4 device not automatable at start; sample 10 H2 recurrence: passed test, no export file). STAGE B AUTHORIZED NOW: fresh `k00-reinstall.sh` AFTER the ruling → dylib `CC0D3604…` → no harness process → new ledger → 30 cold VP-ON, Mode L, repaired driver, no reinstall inside, finish all 30. STAGE C HELD · MECHANISM CLAIM NONE · VoiceKernel + harness FROZEN.** ⭐⭐ **AUTHORIZED STAGE B EXECUTED and VERIFIED (`STAGE-B-20260913T145034Z`, record §9): fresh reinstall `reinstall-20260913T145025Z` after the ruling (dylib `CC0D3604…` BEFORE install · seq 5208 · no process · 10 s before sample 1), 30/30 finished in 19 min — 28 valid: 15 gen-1 listen (409–441 ms) · 10 failure then recovery (gen 3–8, 3.8–13.8 s) · 1 failure then degradation · 2 other (still recovering at export, gen 8) + 2 infrastructure (samples 1–2 device not automatable at start, harness never launched); sequence `--FFFFLFLFLLFLLLOFLLFLFFLLLLOL`; all 28 journals hash/cold/13-step/VP-ON verified, 13 refusals = 13 failures, 0 resets, 0 interruptions. A BESIDE B (never pooled): gen-1 takes 14/29 vs 15/28 — the reinstall did not move it; failure tail 6/15 degraded vs 1/13 (+2 still inside budget at export) — not attributed at N=30. Stage C HELD · mechanism claim NONE.** ⭐ **FOUNDER RULINGS 2026-09-13 (three): (1) STAGE B CLOSED · ACCEPTED — record header amended to `AUTHORIZED STAGE B COMPLETE · §9`, historical §2/§5/§8.2 untouched, superseded paragraph quoted in §9.5; reinstall effect on the gen-1 axis: no material change. (2) STAGE C AUTHORIZED — the EXACT historical Phase-A binary only (`4596b9bdb`, dylib must read exactly `11A057AA-4A3C-3CAE-8C28-E29792489459`; if absent → STOP, NO REBUILD — a rebuild is a new subject needing its own ruling); AUTOMATED-COLD-LAUNCH · Mode L · N=30 · VP ON · one reinstall before, none inside · same driver · no source change; predeclared reading: markedly different → the pre-VP read *may influence* startup probability (never causation) · about the same → it loses explanatory weight · mixed/infra-heavy → characterize only. PREPARED, NOT EXECUTED (`DRIVER-01_STAGE-C_2026-09-13.md`): `k00-locate-binding.sh <uuid>` (reads only) → `k00-container-archive.sh pre-stage-c` → `K00_EXPECT_UUID=11A057AA-… k00-reinstall.sh <ledger-root> <matching>.app` (refuses BEFORE `install app`, exit 3, `.REFUSED.txt`) → `k00-driver-batch.sh STAGE-C 30 --mode L --subject phase-a`; C-D7 classifier: the gen-1-refusal prefix rule now follows the declared subject's list (89 prior rows unchanged). (3) HOUSEKEEPING AUTHORIZED, ordered: archive NOW (copy · SHA-256 manifest · name/count reconciliation · DELETE NOTHING) → Stage C → post-stage archive + manifest extension → delete ONLY `tmp/kernel00-*.jsonl` → re-list, record the empty set; listing too unreliable to keep custody → abort the stage, never clean mid-stage. Deletion NOT implemented: no devicectl delete verb is known — probe `--help` and record, never guess.** Gate 35/35. ⭐ **STAGE C NOT EXECUTABLE (founder ran the custody gate only, `locate-20260913T153606Z`): one dylib on the Mac, `CC0D3604-…` (P5-B0 at the shared product path; MAC-COMPILE-07 overwrote the Phase-A product in place), 0 matches for `11A057AA-…` → STOP; archive · reinstall · batch NOT RUN; NO REBUILD. Stage-C question unanswered; Phase-A evidence stays MANUAL-COLD only (5/5), never pooled.** Returned to the founder (record §8): Stage C disposition (closed vs backup search beyond the searched roots) · whether a rebuild of `4596b9bdb` is ruled a new reproduction subject with its own label (MAC-COMPILE-04 saw the dylib UUID identical across three passes, so a rebuild *may* read `11A057AA-…` — still a rebuild by the ruling) · housekeeping order now that "after Stage C" has no executed stage (archive still authorized now; deletion verb unprobed). ⭐ **FOUNDER RULING 2026-09-13 (applied): HISTORICAL STAGE C CLOSED · NOT EXECUTABLE (question UNANSWERED; not held for a backup search) · a rebuild of `4596b9bdb` = NEW reproduction subject `PHASE-A-REPRO-01`, AUTHORIZED PLAN ONLY (`PHASE-A-REPRO-01_PLAN_2026-09-13.md`: MAC-COMPILE-06 pins · fresh DerivedData · SHA-256 of dylib + bundle before install · UUID `== 11A057AA-…` → continue as NEW subject, differs → STOP before install · **P5B0-POSTCLEAN-CONTROL N=30 first, then the repro N=30 on the same cleaned container** · never called Stage C; UUID match = useful evidence, NOT custody identity) · CONTAINER ARCHIVE AUTHORIZED NOW · PURGE AUTHORIZED only after RECONCILED + a documented delete verb read from `k00-devicectl-probe.sh` (`k00-container-purge.sh` is fail-closed at step F until then; gate 35/35).** ⭐ **HOUSEKEEPING EVIDENCE IN + PURGE RULED (founder, 2026-09-13): archive `pre-purge-20260913T154847Z` RECONCILED (154/154, all name+hash ledgered) · probe: NO file-delete verb under `devicectl device` · founder calibrated the documented `copy to … --remove-existing-content true` in an isolated temporary domain (a.txt/b.txt removed) → RULED as step F behind an exact preimage gate (listing succeeds · reported total = journal rows · every row `kernel00-*.jsonl` · set = reconciled archive set · count 154 → empty-dir `copy to` into `tmp` → re-list MUST be zero else PURGE UNVERIFIED → STOP; never `uninstall`) → `k00-container-purge.sh <archive> --expect-count 154 [--dry-run]` IMPLEMENTED, shim-exercised, gate-pinned (35/35), NOT YET RUN. PHASE-A-REPRO-01 PLAN ACCEPTED; sequencing: purge verified → `P5B0-POSTCLEAN-CONTROL` N=30 → read → only then the Phase-A build. Locate file received (`aa1c554ab` cherry-picked).** ⭐ Archive + probe RECEIVED (`f71539550` cherry-picked). Founder dry-run REFUSED at step E (probe one level shallow — `copy to --help` never captured; gate correct) → **C-D8**: probe expands two levels; purge parser calibrated on the REAL `listing.txt` (`154 files:` + table rows), `--selftest` offline PASS 154/154/0/154 here; shim: extra file → clause-4 refusal, missing journal → clause-5 refusal, armed → verified zero. ⭐ Deep probe RECEIVED (`bb0fa719e`; 49 pages; copy-to documents `--remove-existing-content`: YES) · founder **DRY-RUN PASS on the live device** (D · E · preimage 154/154/0/154 · nothing written) → ⭐ **PURGE FIRED and VERIFIED (founder, `purge-20260913T164023Z`: preimage 154/154/0/154 → `copy to` rc=0 → `0 files` after; 154 journals archived) · ⭐⭐ P5B0-POSTCLEAN-CONTROL EXECUTED, VERIFIED HERE, READ (`DRIVER-01_P5B0-POSTCLEAN-CONTROL_2026-09-13.md`, evidence `082c23084`): 29 valid + 1 infra (sample 17, test host would not launch) — 16 gen-1 listen (411–529 ms) · 6 recovery · 3 degradation · 4 still recovering at export; 13 refusals = 13 failures; 0 resets/interruptions; sequence `OOFLFLLFFLLFLFFL-LLFLLLFLLLOLO`. A 14/29 · B 15/28 · C 16/29 → predeclared row "control ≈ A/B": the cleaned container did not move the gen-1 baseline; tail heavier (3 degraded + 4 still recovering of 13) — not attributed at N=30. NEW O9: listening reached in recovery is mostly NOT HELD (listening at export among recovery rows: A 5/9 · B 5/10 · C 1/6; every gen-1 listen on all strata held) — evidence-field candidate (C-D9, not admitted), no class change, no mechanism. Sample 15 = first automated-stratum gen-1 `is_running_immediate` FALSE. PHASE-A-REPRO-01 BUILD: HELD → ELIGIBLE on a founder act (plan §8).** ⭐ **FOUNDER RULINGS 2026-09-13: (1) PHASE-A-REPRO-01 OPEN** — build authorized through the existing gates, historical claim NONE: `k00-phase-a-repro-build.sh` (build only, gate-pinned: pins → mismatch STOP exit 5 · detached worktree at `4596b9bdb` · fresh DerivedData · `swift test` recorded · unsigned → signed · dylib SHA-256 + per-file manifest + UUID BEFORE any install · UUID `== 11A057AA-…` → continue as a NEW subject / differs → STOP exit 3, no install, founder ruling); on match: `K00_EXPECT_UUID=11A057AA-… k00-reinstall.sh … <fresh>.app` (one reinstall) → `k00-driver-batch.sh PHASE-A-REPRO-01 30 --mode L --subject phase-a` (none inside). **(2) O9 ADMITTED as C-D9 evidence fields** `listeningHeldAtExport` (listening reached ∧ last floor at export is listening) · `listeningLostLater` (listening reached ∧ a later transition leaves it) — not mutually exclusive; continuity never inferred from held alone; classes/law/policy/thresholds UNCHANGED; 118 rows reclassify identically; retrospective table in the control record §5.1 (recovered listening held: A 5/9 · B 5/10 · C 1/6; every gen-1 listen held; lose-then-regain seen only in PRE-AUTH attempt 3 ×2). ⭐ First execution: run 1 (`173346Z`) all 7 pins MATCH, worktree at `4596b9bdb` clean, `swift build` done, completion OWED; run 2 (`173434Z`, 48 s later, other lane worktree) refused `worktree not clean` — both dirty entries were run 1's own footprint (`xcodegen` rewrites the tracked `Harness/Info.plist` per project.yml `info: path:`; DerivedData was inside the worktree) → **C-D10** (instrument): fresh worktree only (pre-existing path = STOP, never reused/cleaned), DerivedData outside at `<worktree>-derived`, xcodegen footprint recorded, mkdir lock against concurrent runs; gate 35/35. Run 2 stopped before xcodegen, so run 1 was not interfered with. ⭐ **RUN 1 COMPLETED → UUID GATE: STOP.** Pins 7/7 MATCH · `swift test` 30/30 · unsigned + signed `BUILD SUCCEEDED` · custody recorded (signed dylib UUID **`64EEC026-5F56-3706-BD2A-C5A6A20FC08B`**, SHA-256 `283dd24e…`, 7-file manifest `09e286b6…`, codesign OK) · **≠ `11A057AA-…` → NO INSTALL, device untouched, founder ruling required** (repro plan §11). Same source/pins/recipe/identity, different linked-image identity; candidate explanation (INFERENCE, untested): Debug DWARF embeds absolute build paths (`/private/tmp/…` vs the historical `/Users/soullab/MAIA-SOVEREIGN/…` + shared DerivedData), so a path-different build may never read the historical UUID; MAC-COMPILE-06's xcodegen version was never recorded. Options returned: rule `64EEC026-…` as the repro's binding (custody = SHA + pins + hashes; UUID was evidence, not identity) · test the path hypothesis (a founder act outside this instrument) · hold. A stray terminal paste executed prose as commands (harmless) and created `/private/tmp/voice-repro-a0d8`; a run-2 build did NOT happen (chain failed at `worktree add`, `voice-repro-a0d8` already existed); no build process alive; Info.plist diff = xcodegen regeneration confirmed; exactly one product exists (run 1). ⭐ **Run-1 evidence RECEIVED + VERIFIED here (`4eb4870c7` → `d72b5802d`: run-1 record ending in the UUID-gate STOP · run-2 refusal record · 7-file manifest `09e286b6…` · both xcodebuild logs `BUILD SUCCEEDED`; repro plan §11.1). Owed (founder): ruling on the UUID (option 1 = rule `64EEC026-…` as the repro's binding → one reinstall with `K00_EXPECT_UUID` + one batch; option 2 = path-hypothesis build as a founder act; option 3 = hold). DEVICE UNTOUCHED until ruled.** ⭐ **FOUNDER RULING (2026-09-13, repro plan §12): RUN 1 ACCEPTED AS REPRO SUBJECT R1 (UUID `64EEC026-…` · dylib SHA `283dd24e…` · 7-file manifest `09e286b6…`) · historical UUID match FAILED, historical equivalence NOT CLAIMED, REBUILD TO CHASE THE UUID PROHIBITED (selecting an artifact after seeing the outcome) · PHASE-A-REPRO-01 continues as a SOURCE-CONDITION reproduction · R1 AUTHORIZED TO INSTALL + RUN N=30 (Mode L · VP ON · `--subject phase-a` · no reinstall inside) after pre-install custody re-verification (UUID + dylib SHA-256 + manifest unchanged, else STOP).** Instrument: `k00-reinstall.sh` now also takes `K00_EXPECT_DYLIB_SHA` + `K00_EXPECT_MANIFEST` (every file hashes identically AND file set identical; evaluated BEFORE `install app`; mismatch → `.REFUSED.txt`, exit 3), shim-exercised, gate 35/35. Reading against P5B0-POSTCLEAN-CONTROL 16/29: markedly different → source condition *may* influence startup probability; about the same → the 5/5 MANUAL-COLD Phase-A result loses explanatory weight; never pooled; C-D9 fields travel as evidence only. ⭐⭐ **R1 EXECUTED and VERIFIED (`DRIVER-01_PHASE-A-REPRO-01_2026-09-14.md`; ledger `PHASE-A-REPRO-01-20260914T011819Z`, evidence `38e2bab9e`): one pre-install batch `…20260913T180117Z` NOT COUNTED (started before any R1 install; SUBJECT-MISMATCH; preserved) · two R1 reinstalls (`011645Z` seq 5952 from this session's line; `011754Z` seq 5960 initiated by the founder's Mac-side ChatGPT session, founder-attributed verbatim; MATCH ×3 both; deviation recorded, not absorbed) · lawful batch 30/30, 0 infra: **13 gen-1 listen (420–437 ms ×10; 644 · 683 · 916 ms ×3 — first automated gen-1 listens > 600 ms) · 5 recovery (gen 3–7; held 2/5) · 5 degradation (gen 7) · 7 still recovering at export (gen 8)**; sequence `LLFOOLFFOLLOLOLFFLFOLLFFFLLFLO`; every journal hash/cold/**14-step Phase-A trace with `input_format_before_vp`**/VP-ON verified; 17 refusals = 17 failures, none at gen 1; 0 resets/interruptions; gen-1 `is_running_immediate` FALSE ×1 (row 30). Side by side, never pooled: A 14/29 · B 15/28 · C 16/29 · **R1 13/30** → predeclared row **"about the same" → the 5/5 MANUAL-COLD Phase-A result LOSES SUBSTANTIAL EXPLANATORY WEIGHT; the pre-VP `outputFormat` read is NOT a demonstrated mechanism** (neither causal nor a probabilistic influence at N=30). Tail heaviest yet (5 of 17 recovered) — characterized, not attributed. Device now holds R1 (any P5-B0 stage needs a fresh reinstall expecting `CC0D3604-…`). ⭐ **R1 ATTESTED (founder, verbatim in record §7) → PHASE-A-REPRO-01 CLOSED · "ABOUT THE SAME" · pre-VP read explanatory weight substantially reduced · causal claim NONE · probabilistic claim NONE. DEVICE: R1 · HOLD IN PLACE (no P5-B0 reinstall without an authorized stage; then expect `CC0D3604-…`); NEW VOICE RUNS NONE AUTHORIZED; NEXT DEVICE ACT NONE.** ⭐ **NEXT INVESTIGATION AUTHORIZED (founder): `KERNEL-00 HIDDEN-RUNTIME-STATE CENSUS` — bounded, read-only, classification first, instrumentation later (`docs/programme/VOICE-2026/KERNEL-00_HIDDEN_STATE_CENSUS_2026-09-14.md`): *what state exists before or during gen-1 startup that differs between a take and a miss, without changing the startup?* Observer-effect firewall (P5-F1): FIRST already-recorded/external state → SECOND existing trace fields → LAST new in-process reads; any read that could touch AVAudio state = INTRUSIVE, never silently admitted. Four buckets (session · engine/IO · OS lifecycle · timing races); five questions per candidate; post-failure-only signals are diagnostic, not antecedent. Held: B2 · E1–E4 · kernel/harness repair · thresholds · no reordering/restart/recovery/"reliability" work.** ⭐ **CENSUS PASS 1 COMPLETE (tiers FIRST+SECOND, 116 valid automated journals, 58 takes / 58 misses): NO RECORDED ANTECEDENT DISTINGUISHES A TAKE FROM A MISS** — every pre-`start_return` field is identical (session outcomes, 48 kHz/10 ms, VP readBack true, format 48k/1, exactly one `route_changed` inside the VP call at ≈80 ms, cold ×116, 0 resets/interruptions) or indistinguishable (activation 27 ms, VP enable 87/88 ms, `start()` 151/151 ms, Enter→start 324/327 ms, uptime, inter-sample gap; separation 0.44–0.52). Two weak non-signals named for falsification only (first ten samples 14/37 vs 44/79, p=0.11; take after miss 32/54 vs after take 23/56, p=0.086). Earliest divergence is post-start and diagnostic: takes get `graph_started` ≤2 ms after `start_return` and the VP configuration change at ≈4 ms with the engine surviving; 42/58 misses show **O10 = a 20–44 ms stall before the next journal write with `engineRunning` already false**, change ≈125 ms later (M-a); 16/58 still read running at `graph_started` and are dead by the first tick (M-b). The deciding event lies inside the 143–174 ms of `engine.start()`, invisible to the trace. All in-process AVAudio reads = INTRUSIVE (not admitted). Only lawful next reads are EXTERNAL and unrecorded: device unified log for mediaserverd/coreaudiod/VP I/O aligned to each start window (capture verb to be read from `--help`, never guessed) · mediaserverd/coreaudiod PID+start time per sample from the Mac · ledger bookkeeping. NONE AUTHORIZED; founder act required. ⭐ **PASS 2 — EXTERNAL OBSERVATION OPENED (founder ruling; census §7): no more in-process AVAudio reads; EXTERNAL READ ≠ PRESUMED INERT.** (1) Ledger bookkeeping RUN: position P=0.57 · prior outcome p=0.13 · run length (≥3 misses → 6/7, ≥3 takes → 2/8, n tiny) · time since prior P=0.59 → none a signal, kept for falsification. (2) Daemon identity IMPLEMENTED in `k00-driver-batch.sh` (mediaserverd/coreaudiod rows before each sample + after export → `daemons/`; `UNOBSERVABLE` on failure; never signals/terminates; gate-pinned). (3) Unified log: `devicectl` has NO log verb (sysdiagnose ruled out) → macOS `log(1)`: `k00-log-probe.sh` (help pages only) → `k00-log-calibrate.sh` (fail-closed on the probe; one `LOG-CAL` sample on R1, own stratum, never counted; one `log collect` + one `log show`, no predicate, post-hoc filter by known process names; CALIBRATION.md with the six ruled fields incl. time alignment via harness log anchors; never `log config`/sysdiagnose/debugger). Gate 35/35. ⭐ Discovery: first probe INVALID (C-D11, filename executed instead of the tool; repaired); valid probe established all three capabilities with condition 3 spelled positionally (`usage: log show [options] <archive>` — the frozen `--archive` was wrong; founder ruling 1 corrected the grammar, ruling 2 = first read default level, escalation `--info`→`--debug` as separate reads). Calibration attempt `121709Z`: infrastructure row (automation mode, rc 65), no sample; `log collect` from the device needs ROOT (rc 77) → returned; C-D12 (`log-cal-` = `LOG-CAL-` on a case-insensitive volume) repaired; daemon witness: `mediaserverd`/`coreaudiod` NOT PRESENT on iOS 26 — `audiomxd` is the audio daemon (PID only; ruling A2 pending). ⛔ **FOUNDER RULING — EXECUTION AUTHORITY (census §7.8): `09c1bd251` §7.5 self-minted calibration authority and the calibration discovered its own witness (`newest probe`, pre-amendment) → INVALID; house rule pinned: *attribution may explain a constraint; it may never authorize an act* (AUTH-1 attribution ≠ authority · AUTH-2 no self-ratification · AUTH-3 authority is an input, never a discovery). LANE FROZEN until four local corrections exist → IMPLEMENTED: compensating record · `--probe` explicit (no `ls|tail -1`) · probe writes + seals a capture-time manifest (HEAD · criterion `PASS2-COND3-POSITIONAL-ARCHIVE@09c1bd251` · ancestry · tool hash · file hashes) verified mechanically · `K00_EXEC_AUTHORITY` required at invocation, never read from the repo; root a separate jurisdiction. Gate 35/35. Standing: 12:17 attempt SPENT · physiological LOG-CAL NONE · entitlement UNSPENT · root NOT GRANTED. RULED (census §7.9): A root YES, one `log collect` only (`K00_LOG_SUDO=1`, mechanically bounded) · A2 daemon witness YES with absence scoped `NOT PRESENT IN THE DOCUMENTED JSON WINDOW` / `PRESENT — witnessed by PID` · A3 authority string supplied verbatim (`FOUNDER-AUTH: one LOG-CAL calibration only; …`), an invocation input never reconstructed from the repo · AUTH-1/2/3 NOT canonized until the corrected sequence has been exercised once. NEXT: C custody · D worktree → tip · E fresh sealed post-amendment probe (evidence only) → then one calibration naming that probe with the authority string and `K00_LOG_SUDO=1`.** ⭐ **LOG-CAL EXECUTED (`unifiedlog-cal-20260914T124328Z`, founder invocation with the A3 string + `K00_LOG_SUDO=1`, probe `123612Z` verified ×4; census §7.11): sample real (`K00-6dbc2752`, gen-1 listen, stratum LOG-CAL, never counted) · root-scoped `log collect` rc=0 → 291 MB archive in 185 s · default-level `log show` rc=0 → 149 MB for a 49 s window · configuration changed NONE · BUT step E `json.load` failed (`Extra data` at char 150,211,740 — the file is not one JSON document) → fields 2 (processes/subsystems) and 6 (time alignment) NOT READ · MECHANISM PASS · REPLAY PASS · PARSER FAIL = C-D13 candidate · VERDICT INCOMPLETE. Founder ruling adopted: no `--info`, no rerun, no parser change until the file shape is read; next act = read-only inspection of the existing `window.json` (sha `72871a24…`), then C-D13 offline against the same file. The corrected AUTH sequence has been exercised once end to end; canon promotion still the founder's call.** ⭐ **SHAPE READ (founder, read-only, hash MATCH): `window.json` = ONE complete JSON array + a 142-char trailer banner (`==========` / archive path / `==========`) — the "Extra data" was the banner, nothing truncated (my "several megabytes" arithmetic corrected in place, §7.12). C-D13 CONFIRMED → REPAIRED, instrument only: `k00-log-window-read.py` (raw_decode one value · residue recorded verbatim · STOP on any non-banner residue · `--expect-sha` custody · A2 wording) · calibrate delegates step E to it · `k00-log-window-reread.sh <unifiedlog-cal-dir> <LOG-CAL-dir>` = offline re-read verified against the calibration's own recorded sha, writes a NEW `WINDOW-READ-<stamp>.md` (never edits `CALIBRATION.md`; no sudo/xcrun/devicectl/log). Gate 35/35, shim ×3. RE-READ HELD for founder invocation; it yields fields 2/6 from the existing window with no device act.** ⭐ **RE-READ RECEIVED (founder, offline; custody `ed5107640` → `c1fddbf20`): fields 2 and 6 ANSWERED (census §7.13).** Field 2: 129,201 default-level entries / 49 s; audio subset 7,023; the in-process framework logs the client side of startup at ms resolution (coreaudio 82 · avfaudio 4 lines in the harness pid); **`audiomxd` 14 entries, all at teardown, ZERO in the start window; mediaserverd/coreaudiod NOT PRESENT IN THE DEFAULT-LEVEL WINDOW**; daemon snapshots identical before/after. Field 6: **C-D14** — the reader's anchor B regex matched an XPC line from a STRAY harness pid 63378 (present at precondition, terminated by the driver 5.5 s before the sample's cold launch; origin UNKNOWN; the founder's manual text-table grep was a truncation false negative → manual presence checks by `--json-output`) → corrected `k00_log_align.py` (exact framework lines from the sample pid: `Activated session` ↔ `session_activated`, `start, was running 0` ↔ `start_begin`) → **two independent anchors agree to 0 ms**; `WINDOW-ALIGN-20260914T130500Z.md`. Capability shown on this one LOG-CAL sample (never counted, not physiology): `iounit configuration changed > posting notification` at start+107 ms, 44 ms BEFORE `engine.start()` returned. Reader now also enforces ruling step 4 (banner must name THIS calibration's archive). Gate 35/35. **Founder: AUTH-1/2/3 EXERCISED ONCE; authorizes nothing downstream.** Verdict on the six fields is the founder's; `--info` / sample / batch / control NOT AUTHORIZED.** ⭐ **FOUNDER RULINGS on §7.13 (census §7.14): (1) LOG-CAL INSTRUMENT VALIDATION — PASS** (six fields ✓; limitation = default level does not expose `audiomxd` in activation→first callback — a visibility boundary, not daemon absence) · **(2) `--info` re-read AUTHORIZED, exactly one, offline** (same archive · same T0/T1 · separate record · no `--debug`/sample/collect/sudo/device act; narrow question: does `--info` expose `audiomxd` in the aligned interval?; if silent, return before `--debug`) → instrument `k00-log-window-info.sh <cal-dir> <LOG-CAL-dir>` BUILT, gate-pinned, NOT RUN (requires `K00_EXEC_AUTHORITY` at invocation; window reproduced from CALIBRATION.md; `--suffix info` outputs; align prints `audiomxd entries in that interval` — default-level value 0) · **(3) pid 63378** ORIGIN UNKNOWN · precondition residue · not the sample · not a finding · not a confound · **(4) corrected Mac re-read DONE** (disposable worktree at `69844f522`; `WINDOW-READ-20260914T131326Z.md`; 0 ms agreement; custody `55c1d3ec0` → `cbd483498`). C-D13 CLOSED · C-D14 CLOSED · AUTH exercised once, canon separate · `--debug`/sample/batch/control NOT AUTHORIZED.** ⭐ **`--info` RE-READ EXECUTED (founder, `WINDOW-INFO-20260914T132206Z`, disposable worktree at `2ce2b5cf6`; census §7.15): same window · rc 0 · 135,896 entries (+5.2 % over default) · `audiomxd` 14 entries at BOTH levels, all teardown · **0 in the activation→first-callback interval at INFO level → ruling 2's question answered NO** · alignment identical (0 ms). Returned for ruling before `--debug` (last rung; not authorized). C-D15: the absence label was hard-coded `DEFAULT-LEVEL` on an info read (A2 scoping error) → label now follows the level read; pinned; the produced record's line is corrected in §7.15, not edited. Custody of the info outputs OWED (founder).** ⭐ **RULED (founder, census §7.16): info custody `fc4d8230c` → `92f6ec0cc`; two INFO records = one authoritative (`132206Z`) + one duplicate replay (`132219Z`, identical hash, retained visible) · **`--debug` AUTHORIZED as the LAST rung, one offline read** (same archive/window · `--debug` alone, never with `--info` · separate DEBUG record · no sample/collect/sudo/device act/`log config`/batch/control; seam count still 0 → the ladder STOPS, finding = not observable at the three documented read levels; exposed → return before any batch/control) → `k00-log-window-debug.sh` BUILT, gate-pinned (35/35 read before commit), NOT RUN.** ⭐ **`--debug` RE-READ EXECUTED (founder, `WINDOW-DEBUG-20260914T132714Z`, worktree at `f04dba799`; census §7.17): `--debug` alone = default +150 entries (it does NOT include info level), audio subset identical to default, `audiomxd` 14 at every level (all teardown), **0 in the activation→first-callback interval at DEFAULT, INFO and DEBUG → THE VISIBILITY LADDER STOPS (founder rule applied)**. Finding in the founder's words: *`audiomxd` startup activity was not observable in this archive at default, info, or debug read levels* — not evidence that `audiomxd` did nothing. The instrument is validated and characterized: 0 ms alignment, ms-scale client-side view inside `engine.start()`, blind to `audiomxd` at the seam. C-D16 (debug record's header said `no --debug`; script corrected, record preserved). Custody of the debug outputs OWED. Logged batch / matched no-log control NOT OPENED (founder decisions); new sample NOT AUTHORIZED.** ⭐ **FOUNDER RULINGS on §7.17 (census §7.18): ladder CLOSED (0/0/0; `--debug` ≠ superset of `--info`; no fourth rung) · debug custody `6a08eb6e3` → `46daca12d` · C-D16 confirmed, instrument corrected · **PASS-2 SEAM EXPERIMENT OPENED — LOGGED batch + MATCHED NO-LOG CONTROL, design + witness implementation authorized, EXECUTION HELD** (question: do takes and misses differ in the externally observed order/timing inside `engine.start()`; outcome = kernel ledger's take/miss; `audiomxd` not reopened; separation = client-side correlating signature only). BUILT, gate-pinned 35/35, shim-exercised, NOT RUN: protocol `PASS2_SEAM_EXPERIMENT_PROTOCOL_2026-09-14.md` (N=30+30 · CONTROL→LOGGED proposed · D-L1 post-hoc block collect implemented, per-sample collect not · fields F1–F6 FROZEN · refusals · AUC + Fisher predeclared) · `k00-log-batch.sh --block control|logged` · `k00-seam-ledger.py` (+`--compare`) · `seam_fields()` · batch writes `sample-timing.tsv`. Rulings owed (§9): D-L1 · D-L2 · subject · fields · statistics · then authority + root.** ⭐ **SEAM PROTOCOL APPROVED WITH CORRECTIONS (founder; census §7.19; protocol §9–§10): D-L1 post-hoc block collect YES (per-sample REJECTED; CONTROL = no `log(1)` invocation = block-drift control, NOT observer-effect) · D-L2 CONTROL → LOGGED YES · subject `phase-a` on installed R1, no reinstall · F1–F6 accepted with fixes APPLIED: F5 on its full −50…+500 ms window (`posts_in_seam`) split from `posts_at_start` (F2/F3/F6), 1 ms boundary tolerance stated, alignment REFUSES > 2 ms anchor disagreement (`UNREADABLE`), F4/callback = downstream context (localization rests on pre-`start_return` evidence) · §6 block-drift wording + missingness frozen + descriptive AUC. Gate 35/35 read before commit; shim ×2. EXECUTION HELD: `K00_EXEC_AUTHORITY` + root for the one collect NOT granted; 60 samples unspent.** ⭐ **SEAM-01 GRANTED and EXECUTED (founder; census §7.20–§7.22):** authority string supplied verbatim at invocation · root narrowed to one post-hoc collect · probe custody gap found and closed (`261a5d872`, Option A absolute path) · **CONTROL `135238Z` COMPLETE** (rc 0 · 30 rows · 0 abort; 10 takes / 19 misses / 1 infra) → **LOGGED `141516Z` COMPLETE** (rc 0 · 30 rows · 0 abort · ONE collect 337 MB · 30/30 reads rc 0; 14 takes / 16 misses / 0 infra). **Block drift: Fisher p = 0.43 → no block difference detected** (not proof of no drift, not an observer-effect finding). **Seam reading (founder transcript, NOT yet verified here — custody owed): a PARTIAL client-side correlating signature** — in 6/16 misses the `iounit configuration changed` post lands at/after `start_return` (F3 tail to +151 ms; F6 `start_return<iounit_post…`), in 0/14 takes (F3 −60…−41 ms, always before return); the other 10 misses are undistinguished on any pre-return field; F5 (takes always 1 post, misses median 4, AUC 0.03) is downstream context under §7. Correlation/localization only; no causation, no mechanism, `audiomxd` not reopened. F1/F2/F4/missingness/anchor agreement OWED from the ledger. C-D17 runner count owed. **§7.22 RULED (founder; census §7.23): ACCEPTED with two corrections** — (1) of the ten early-post misses nine lie inside the takes' F3 range and one (sample 16, −38 ms) 3 ms beyond it, no second threshold coined; (2) the 6/10 split is NOT M-a/M-b: M-a 11 · M-b 5, all six late-post misses ⊂ M-a, the seam refines M-a (6 of 11) and reproduces no dichotomy; the five `…<iounit_post<callback` rows carry callbacks from gens 3·6·6·7·4, none gen 1. **Custody `dea8b7c93` → cherry-picked `43e85315e`; every §7.22 number RE-VERIFIED HERE from the files** (anchors 0 ms ×21 · 1 ms ×9; F1 AUC 0.54 · F2 0.31 · F3 0.28 · F4 0.00 (absent 0 takes / 6 misses) · F5 0.03; missingness recorded). **C-D18**: `--compare` wall medians were scraped from infra-row text (None / 6 s); authoritative `sample-timing.tsv` → 29 s / 29 s; reporter repaired to read the tsv; C-D17 row count repaired; produced records untouched. **SEAM-01 CLOSED · PASS 2 CLOSED AS A MEASUREMENT PASS** — finding: *in 6/16 misses and 0/14 takes the first configuration-change post occurs only after gen-1 `engine.start()` returned; all six M-a; five further M-a misses without it* — localization/correlation only; causation NONE · mechanism NONE · `audiomxd` not reopened · perturbation/additional samples NOT AUTHORIZED · budget 60/60 spent. ⭐ **KERNEL-00 / VPIO-01 OPENED (founder ruling 2026-09-14): LOWER VOICE-PROCESSING-I/O QUALIFICATION — READ-ONLY CENSUS + PLAN ONLY; CODE NOT AUTHORIZED · DEVICE ACT NONE · NEW SAMPLE NONE.** KERNEL-01 stays CLOSED (gated on K00 acceptance, not earned). Basis: research §20.1 applied narrowly — the engine subject has not demonstrated a stable substrate and the measurement programme found no controllable mechanism; NOT a finding that `AVAudioEngine` is incapable. Plan RETURNED: `docs/programme/VOICE-2026/KERNEL-00_VPIO-01_PLAN_2026-09-14.md` — §0 source census at `418fcf60f`: the engine dependency is confined to `AudioGraph.swift` (22 refs; every other file 0), reached from `VoiceKernel.swift` through 16 seams, all value-typed except two (`AVAudioPCMBuffer` at `schedule`/`makeTone`); §1 invariant substrate byte-identical (authority · supervisor · policy · projection · recorder · replayer · generation/recovery law · output identity · fault flags · harness); §2 replacement = the one file's interior (VPIO audio unit: pull-model input callback, render callback fills the scheduled stream, property listener for hardware-format change, running property = evidence only) + kernel diff budget (two typed seams + trace-step type name, nothing else; `ConfigurationChange.swift` retained, its class reads absent); §3 authority proof (second source pin: only the substrate names Audio Toolbox, nothing but the authority names `AVAudioSession`; ducking config = policy, not set); §4 K00-01…18 mapped one-to-one, six thresholds unchanged, and the law's own trigger wording (K00-06/K00-11) noted as narrower than the condition used — amendment is a founder act, not done; §5 observability (callbacks earn health, never the running read; VPIO journals carry `ioRunning`, never `engineRunning`; VP-reconfiguration records read absent); §6 falsifiers F-V1…7 (design-time refusals: second owner · two substrates · diff outside budget · threshold pressure · realtime mutation · exclusion creep · undocumented API) + F-W1…8 (first-witness FAILs; F-W1 = predeclared entry-axis reading beside A/B/C/R1, never pooled); §7 custody (engine subject FROZEN; new build identity, recommended new bundle id `life.soullab.voicekernel.vpio01`; own witness lineage; automated instrument covers K00-04 only — K00-11/12/13/15 remain phone-bound and owed). ⭐ **VPIO-01 PLAN ACCEPTED WITH THREE RECORD-ONLY CORRECTIONS (founder, 2026-09-14; plan §8) — APPLIED:** (1) acceptance-law trigger reconciled narrowly (`06_KERNEL-00_ACCEPTANCE_LAW.md` §1/§5 + amendment note: lower path opens as a NEW subject after a recorded K00-04 / K00-06 / K00-11 failure on the higher-level subject; new witness, never retry/silent fallback; no fallback in one build; obligations · thresholds · pass rule · authority unchanged; not a general licence); (2) **F-W1 PINNED before any VPIO outcome exists**: denominator = valid non-infra rows of the 30, no top-up; >2 infra rows → characterize only, not adjudicated; clear improvement = one-sided Fisher p < .05 vs EACH of A 14/29 · B 15/28 · C 16/29 · R1 13/30 (never pooled) → first satisfying count **24/30** (23/29 · 23/28 with 1–2 infra), recomputed here; about the same/worse = proportion ≤ 16/29; between = INDETERMINATE, return for ruling (20/30 is INDETERMINATE); (3) distinct bundle id RULED YES (`life.soullab.voicekernel.vpio01`; a permitted custody/build-identity delta) → **old K00/R1 container purge REMOVED from the VPIO sequence** (historical container untouched; VPIO gets its own; prove the VPIO bundle not installed before first install; read-only archive of the old container permissible). Substitution boundary, fail-closed rule, `ioRunning`, K00-04-only first witness ACCEPTED as written. **Standing: VPIO implementation HELD · MAC-COMPILE HELD · install NOT AUTHORIZED · N=30 NOT AUTHORIZED**; ⭐ **`cfe3984a4` ACCEPTED · IMPLEMENTATION HELD (founder): §0 was an API census, not a semantic one** — engine meaning leaked upward (`vpExpectationPending` · classifier call · `engine_configuration_changed`/`configuration_change_deferred`/`voice_processing_reconfiguration` · six `engineRunning` sites · `routeChanged` delegating rebuild to the engine notification; continuing into `ConfigurationChange.swift`, `Replay.swift`, tests, gate pins). **VPIO-01A SEMANTIC CENSUS RETURNED** (`KERNEL-00_VPIO-01A_SEMANTIC_CENSUS_2026-09-14.md`, read-only): §1 line-by-line enumeration incl. the gate's working-tree byte-pin at `24a6fcfa1` (any VPIO implementation changes those bytes → the frozen subject must be pinned in history by SHA, not in the tree) · §2 `onConfigurationChange` RETIRED as a seam for VPIO (no such event) → `onFormatChanged` · §3 truthful vocabulary (`ioRunning` · `io_running_observed` · new observation `io_format_changed`, never an act · engine records absent, never manufactured · `graph_*` record names kept by definition "the generation's physical I/O instance", component `VoiceIO`) · §4 restated diff budget superseding plan §2 (kernel: two seams + trace type + REMOVE the change handler/expectation/ordinal/callbacksSinceChange + ADD `onFormatChanged` observation + rename six sites + `routeChanged` becomes record + act through the existing policy; classifier + its tests REMOVED from the VPIO tree (recommended; `samePorts` kept pure); Replay untouched; gate restructured in the same commit; authority/supervisor/policy/projection/state/journal byte-identical) · §5 route recovery from the authority's `route_changed` observation → `recovery_requested` cause `session_route_change`, existing class `configuration_change`, existing policy, no new class/budget/timer/threshold · §6 bundle id frozen exactly `life.soullab.voicekernel.vpio01`; instrument plumbing (`k00-driver-batch.sh` · `k00-reinstall.sh` · `K00DriverTests.swift` · ledger `SUBJECTS`) = later witness work; SDK surface confirmed by the founder (F-V7 not the blocker). Rulings owed: `graph_*` definition · classifier removal vs retention · the route act · gate-by-history restructuring → only then bounded implementation against census §4 ⚠️ **CONCURRENT RECORD (`ebd9eef5d`, another session, 15:52Z, founder-attributed) — kept verbatim beside this session's, NOT reconciled here; contradicts VPIO-01A census §4 on the classifier/seam (retain vs remove) — founder ruling required, see plan §10:** **, then ⚠️ **TWO IMPLEMENTATION-BOUNDARY CONTRADICTIONS FOUND AND RECONCILED (founder, same day, record only): (a) the kernel diff budget forbade the six `engineRunning` → `ioRunning` evidence-key substitutions THE SAME PLAN REQUIRES** (verified at `VoiceKernel.swift` 326 · 427 · 499 · 516 · 602 · 625; the seventh in `AudioGraph.swift:200` goes with that file's interior) — admitted as **vocabulary only**, ⛔ no control flow, condition, threshold or health meaning, and ⛔ record NAMES like `engine_running_observed` are NOT renamed, being replay-compatibility identifiers; **(b) §5 promised `voice_processing_reconfiguration` / `configuration_change_deferred` would be *absent, never manufactured* — but the unchanged kernel sets `vpExpectationPending = snap.voiceProcessingEnabled` every generation (`VoiceKernel.swift:322`), so a raw unit's stream-format callback reaching the retained `onConfigurationChange` seam could be classified through the OLD ENGINE-SPECIFIC expectation and journalled as exactly that class.** ⭐ Exactly one subject-specific adaptation permitted: **`vpExpectationPending = false` on the VPIO subject** (or its compile-time equivalent) — ⛔ no new selector, no runtime engine/VPIO choice, one VPIO build one substrate, `ConfigurationChangeClassifier.swift` byte-identical, the old branch merely unreachable. ⚠️ **K00-11 prose corrected too**: the architecture uses TWO facts — `AudioSessionAuthority.routeChanged` updates route identity AND the VPIO stream-format property observation reaches the retained `onConfigurationChange` seam, so the classifier sees the route changed and the existing bounded recovery runs; ⛔ **do NOT amend `handleSession(.routeChanged)` into a new direct rebuild path merely to make earlier prose true.** ⭐ **F-W1 independently recomputed by the founder and CONFIRMED.** The original three corrections: (1) acceptance-law trigger reconciled narrowly (`06_KERNEL-00_ACCEPTANCE_LAW.md` §1/§5 + amendment note: lower path opens as a NEW subject after a recorded K00-04 / K00-06 / K00-11 failure on the higher-level subject; new witness, never retry/silent fallback; no fallback in one build; obligations · thresholds · pass rule · authority unchanged; not a general licence); (2) **F-W1 PINNED before any VPIO outcome exists**: denominator = valid non-infra rows of the 30, no top-up; >2 infra rows → characterize only, not adjudicated; clear improvement = one-sided Fisher p < .05 vs EACH of A 14/29 · B 15/28 · C 16/29 · R1 13/30 (never pooled) → first satisfying count **24/30** (23/29 · 23/28 with 1–2 infra), recomputed here; about the same/worse = proportion ≤ 16/29; between = INDETERMINATE, return for ruling (20/30 is INDETERMINATE); (3) distinct bundle id RULED YES (`life.soullab.voicekernel.vpio01`; a permitted custody/build-identity delta) → **old K00/R1 container purge REMOVED from the VPIO sequence** (historical container untouched; VPIO gets its own; prove the VPIO bundle not installed before first install; read-only archive of the old container permissible). Substitution boundary, fail-closed rule, `ioRunning`, K00-04-only first witness ACCEPTED as written. **Standing: VPIO implementation HELD · MAC-COMPILE HELD · install NOT AUTHORIZED · N=30 NOT AUTHORIZED**; ⭐ **FOUNDER RULING on §10 (2026-09-14; plan §11): RECORD B / VPIO-01A CENSUS §4/§5 GOVERNS · Record A (`ebd9eef5d`) SUPERSEDED as an active ruling, preserved verbatim (plan §11.1)** — decisive defect mechanical: with the expectation forced false the classifier returns its residual `routeConfigurationChange` for every callback, so a benign VPIO format notification would spend recovery budget under an engine name. Rulings: `graph_*` KEPT by definition (generation-scoped physical I/O substrate lifecycle), component `VoiceIO`; `engine_running_observed → io_running_observed`, `engineRunning → ioRunning`; classifier + tests REMOVED from VPIO, `Replay.swift` unchanged, new pure `RouteComparison.swift` (port equality only); route recovery authority-sourced through the existing policy **with the eligibility guard** (in conversation ∧ not suspended ∧ `routeAtStart` from a successful start ∧ floor ≠ degraded ∧ ports differ; same-port = no act; `io_format_changed` = observation only); gate by history (engine assertions against immutable `24a6fcfa1`/`4596b9bdb` objects from the local Git object database; active VPIO semantic assertions; no pre-invented tree hash; artifact custody = UUID + SHA + manifest); law §5 superseded sentence RESTORED beside the bullet, `ebd9eef5d` not rewritten. **Record-only reconciliation PINNED → bounded VPIO-01 source implementation AUTHORIZED against plan §11 / census §4** (MAC-COMPILE HELD · install NOT AUTHORIZED · N=30 NOT AUTHORIZED · historical container untouched · KERNEL-01/BENCH-01/BRIDGE-01/MIGRATE-01 CLOSED · JOP-04 UNTOUCHED). ⭐ **VPIO-01 BOUNDED IMPLEMENTATION LANDED (plan §12) — IMPLEMENTED, NOT COMPILED.** Exactly the budget: `AudioGraph.swift` interior = the Voice-Processing I/O unit (`import AudioToolbox` only; EnableIO el 1/el 0 · VP = bypass 0 · hardware format read (Input scope, el 1) BEFORE any callback is armed → `try requireValid()` · client formats (Output,1)/(Input,0) · input callback pulls by `AudioUnitRender` · render callback fills the one scheduled stream and measures last-non-silent at the render seam · stream-format property listener → `onFormatChanged` observation seam · `AudioUnitInitialize` → `AudioOutputUnitStart` · `isRunning` = `kAudioOutputUnitProperty_IsRunning` read only; `StartTraceStep` closed at 11) · `RouteComparison.swift` new, pure · `ConfigurationChange.swift` REMOVED · kernel seams only (`handleFormatChanged` = `io_format_changed`, cause `unit_property_listener`, exit guard → generation guard → no act; `.routeChanged` → eligibility guard `inConversation && !suspended && substrateStarted && routeAtStart != nil && floor != .degraded` ∧ ports differ → `recovery_requested` cause `session_route_change` → existing `configuration_change` class/policy, else `route_observed` evidence only; six `engineRunning` → `ioRunning` vocabulary; `io_running_observed` on the existing tick; five caller classes and three sleeps unchanged) · tests follow the subject · `Package.swift` links AudioToolbox, deps `[]` · harness `project.yml` bundle `life.soullab.voicekernel.vpio01`, `Harness/*.swift` untouched. Eleven invariant files byte-identical to `24a6fcfa1` (authority · supervisor · policy · state · projection · journal · replay · harness ×4); thresholds untouched; driver/batch/reinstall/ledger plumbing NOT touched (later witness work). ⭐ **GATE BY HISTORY in the same commit → 44/44 (jest; was 35/35):** every engine-subject assertion (PW-03/04/05 Phase A/P5-B0) now reads `24a6fcfa1` (+ `4596b9bdb` 14-step pin) from the git object database with the SAME two tree-hash constants recomputed over history; VPIO subject asserted semantically (new block, 8 obligations); K00-16 forbids `AVAudioEngine`/`AVAudioPlayerNode`/`AVAudioPCMBuffer`/`installTap(`/`AVAudioEngineConfigurationChange` anywhere (one build, one substrate) and pins `.vpio01` exactly; no VPIO tree hash pre-invented. ⛔ **NOT COMPILED** (no toolchain here) · **MAC-COMPILE HELD** (separate founder act, on exactly this SHA) · install NOT AUTHORIZED (prove `.vpio01` not installed before any first install; historical container never mutated) · N=30 NOT AUTHORIZED · F-W1 pinned, no outcome exists · a compile failure on an Audio Toolbox name = instrument defect to record, never an undocumented API (F-V7). ⚠️ **CONCURRENT RECORD merged `f9d926e46` (`KERNEL-00_VPIO-01_IMPLEMENTATION_BLOCKED_2026-09-14.md`, other session, base `ebd9eef5d`): opened the same authorization and STOPPED with no source on two blockers — BLOCKER 2 (byte-pin) is discharged by the founder's §11 item 5 ruling that this session implemented; BLOCKER 1 (F-V7: ~11 Audio Toolbox types/constants beyond the founder-confirmed set cannot be read from the installed header in this container; writing them from memory = the forbidden guess) is NOT discharged by anything in `0f535e705` — this session wrote exactly those names (inventory G1–G11 in plan §12.5; G9 `kAudioUnitProperty_MaximumFramesPerSlice` NOT used, fixed 8 192-sample scratch = an approximation, bounded, unrepaired). Laid side by side in plan §12.5, NOT adjudicated. Founder acts owed: one header read on the Mac against the §12.5 table (+ bypass-before-initialize, owed) → verify / correct / withdraw the source; then whether a header-verified source proceeds to MAC-COMPILE on exactly its SHA. Source vocabulary status: UNVERIFIED AGAINST THE INSTALLED HEADER.** ⭐ **FOUNDER HEADER ADJUDICATION (installed iPhoneOS 26.2 SDK; plan §12.6; adjudication appended to the blocker record, record untouched): G1–G8 · G10 · G11 VERIFIED, source correct as written; G11 listener order exactly as assumed; G9 `kAudioUnitProperty_MaximumFramesPerSlice` VERIFIED (Global · UInt32 · read/write) and it exposed the ONE real defect — the fixed 8 192 scratch + `min()` truncation; bypass-before-initialize = no header blocker (documented lifecycle uninitialize → configure → initialize), runtime effect stays witness evidence; BLOCKER 2 CLOSED by gate-by-history; source CORRECTABLE, NOT WITHDRAWN → ⭐ G9 CORRECTED at `5ca7851a8` (`AudioGraph.swift` only: MaximumFramesPerSlice read after `formats_set` and BEFORE any callback is armed, unreadable → `unitError(max_frames_read)`, zero → new refusal `invalidMaximumFramesPerSlice`, scratch allocated to exactly that capacity, value on the `callbacks_armed` trace; realtime pull renders exactly the requested frames or returns `kAudio_ParamError`, never truncates; 11 trace seams unchanged; gate +1 → 45/45 read before commit). ⭐ **MAC-COMPILE AUTHORIZED on exactly `5ca7851a8`** (build · test · gate · xcodegen · unsigned · signed · UUID/dylib SHA/manifest recorded; an importer-syntax rejection = bounded compile-era source defect, never another API); `a991d09a4`/`0f535e705` NOT compile subjects. Even a green compile opens NOTHING: install · device act · N=30 NOT AUTHORIZED · F-W1 UNSPENT · KERNEL-01/BENCH-01/BRIDGE-01/MIGRATE-01 CLOSED · JOP-04 UNTOUCHED.** ⭐ **MAC-COMPILE-01 (VPIO) RED on `5ca7851a8`** (founder record `KERNEL-00_VPIO-01_MAC-COMPILE-01_2026-09-14.md`, 922 lines, cherry-picked `6f835f7d0`): `swift build` PASS · `swift test` 27/27 · gate 45/45 · xcodegen PASS · **unsigned iOS build FAIL — `AudioGraph.swift:361:23: cannot find 'UnsafeMutableAudioBufferListPointer' in scope`** · signed NOT RUN · artifact NONE · no device act. **C-V1** (plan §12.7): the symbol exists in the installed SDK but its Swift declaration is in the `CoreAudio` module, which the subject never imports — a bounded compile-era module-visibility defect of the predeclared class, not a missing primitive, not a G8 error. Correction NAMED, NOT WRITTEN, NOT AUTHORIZED: one line `import CoreAudio` in `AudioGraph.swift` + the gate's import-set pin widened to `[AudioToolbox, CoreAudio, Foundation]` with `CoreAudio` forbidden elsewhere; nothing else. `5ca7851a8` is NOT a compile-green subject; any correction = new SHA = fresh MAC-COMPILE.** ⭐ **C-V1 AUTHORIZED (founder) → CORRECTED at `85e5e7154`** (plan §12.8: exactly `import CoreAudio` beside `import AudioToolbox` in `AudioGraph.swift`, nothing else in Swift moves; gate import pin = `[AudioToolbox, CoreAudio, Foundation]` for the substrate, both forbidden elsewhere; gate 45/45 read alone, then source + gate committed together, tree clean). ⭐ **MAC-COMPILE-02 (VPIO) AUTHORIZED on exactly `85e5e7154`** (six steps; artifact identity only if produced; any red → STOP, exact defect returned, no second correction in the same act). `5ca7851a8` NOT a future compile subject. Install · device act · N=30 NOT AUTHORIZED · F-W1 UNSPENT.** ⭐⭐ **MAC-COMPILE-02 (VPIO) GREEN on exactly `85e5e7154`** (founder record `KERNEL-00_VPIO-01_MAC-COMPILE-02_2026-09-14.md` 1 145 lines + 7-file manifest, cherry-picked `8329cc0a6`; manifest re-hashed here = recorded `4710d9a6…`): `swift build` PASS · `swift test` 27/27 · gate 45/45 · xcodegen PASS · unsigned PASS · **signed `BUILD SUCCEEDED`** (only the known `try?` warning) — C-V1 exercised by the iOS compiler; **first VPIO signed artifact: dylib UUID `E8074AD1-D179-3267-A15C-142D033A9665` · dylib SHA `6efe33b1…` · executable SHA `e43dec66…` · manifest `4710d9a6…` (7 files) · bundle `life.soullab.voicekernel.vpio01`**. Nothing installed/launched/sampled. **Compile success opens NOTHING: install · device act · N=30 NOT AUTHORIZED · F-W1 UNSPENT (24/30 · 23/29 · 23/28 / ≤16/29 / between INDETERMINATE) · KERNEL-01/BENCH-01/BRIDGE-01/MIGRATE-01 CLOSED · JOP-04 UNTOUCHED.** A first VPIO witness would still need (none authorized): census §6 instrument plumbing (driver/batch/reinstall/ledger still name the K00 subject) · proof `.vpio01` not installed before any first install · reinstall expecting exactly `E8074AD1-…` + dylib SHA + manifest · historical K00/R1 container untouched. Next act = founder ruling (plan §12.9).** ⭐ **VPIO-01B WITNESS PREP OPENED (founder) → INSTRUMENT IMPLEMENTED at `de3efd3fb` (plan §13), instrument-only:** explicit subject table identical in ledger · driver · batch · reinstall (`p5b0`/`phase-a` → `.k00` · `VoiceKernel K00` · `engineRunning`; `vpio-01` → `.vpio01` · `VoiceKernel VPIO-01` · `ioRunning` · the frozen 11-step trace); no default bundle, unknown subject refused, never `.k00`; driver takes `K00_SUBJECT` via the runner env only (app under test still gets no args/env/hooks); batch derives `$BID` for every apps/container/pull/ledger call and passes `TEST_RUNNER_K00_SUBJECT`; ledger: four classes, no fifth, running key from the table, vpio-01 = exact 11 steps or exact ordered prefix + gen-1 refusal, historical rules byte-identical — **regression 936/936 rows identical (468 journals × 2 subjects, sha `ccf6bad4…`), every historical journal SUBJECT-MISMATCH/infra under vpio-01, `--selftest` 13/13**; reinstall: vpio-01 identity PINNED (bundle · UUID `E8074AD1-…` · dylib SHA · executable SHA · 7-file manifest SHA, conflicting env = refusal, manifest hashes + file set), JUST-IN-TIME absence read (unreadable/PRESENT → STOP, NO uninstall/overwrite/sample), `K00_EXEC_AUTHORITY` required at invocation before the install verb; shim-exercised offline (every vpio refusal: 0 apps listings, 0 installs; historical paths unchanged; post-custody branches pinned structurally, MATCH needs the real artifact). Gate +4 → **49/49**; organism byte-identical to `85e5e7154` (gate-pinned). ⭐ **DRIVER-COMPILE-01 (VPIO-01B) GREEN on exactly `de3efd3fb`** (founder record `KERNEL-00_VPIO-01B_DRIVER-COMPILE-01_2026-09-14.md`, 677 lines, cherry-picked `c0d6c48be`; plan §13.5): `xcodegen` PASS · `build-for-testing` PASS (`generic/platform=iOS`, `CODE_SIGNING_ALLOWED=NO`, phone never targeted; no devicectl/install/test verb in the log) · instrument product hashes recorded (xctestrun `7105c95e…` · DriverUITests `f8fe6048…` · XCTRunner `6fa0f967…` · DriverHost `57793876…` — instrument evidence, never organism identity) · one operator defect recorded, not rerun (`build-for-testing` token twice → two `** TEST BUILD SUCCEEDED **` in one invocation) · organism still byte-identical to `85e5e7154`. **Ten steps ✓ → VPIO-01B INSTRUMENT-READY.** HELD/NOT AUTHORIZED: first-install absence read (just-in-time only, inside the install act) · VPIO install · launch · sample · N=30 · F-W1 UNSPENT · historical K00/R1 container UNTOUCHED. **Next act = founder ruling opening the first VPIO install (`K00_SUBJECT=vpio-01` + `K00_EXEC_AUTHORITY` at invocation → custody MATCH → absence read → one install), then separately whether a VPIO-01 N=30 follows; compile green opens neither.** ⭐ **FIRST-INSTALL-01 AUTHORIZED (founder, 2026-09-14; plan §13.6) — INSTALL ONLY, NOT YET EXECUTED:** one transaction with the `de3efd3fb` instrument on the MAC-COMPILE-02 product (custody → just-in-time apps read → `.vpio01` ABSENT → exactly ONE install → post-install process read → STOP; PRESENT/UNREADABLE/custody-fail → STOP, no uninstall, no overwrite, no rebuild); the instrument's transaction verified line by line against the ruling (only `devicectl` verbs: `info apps` · `install app` · `info processes`; no launch/test/journal/sample verb exists); the authority string is an invocation input (recorded in §13.6 as evidence, never reconstructed); invocation pinned in §13.6; executes on the Mac — this session has no device; record (custody · absence · install output · process state) OWED from the founder. Launch · driver test · journal pull · sample · N=30 NOT AUTHORIZED · F-W1 UNSPENT · K00/R1 UNTOUCHED.** ⭐⭐ **FIRST-INSTALL-01 EXECUTED (founder, `reinstall-20260914T194554Z`; plan §13.7; §13.6.1 records a first invocation that reached nothing — cwd miss + this session's abbreviated-authority-string instruction defect): instrument verified identical to `de3efd3fb` before running · custody MATCH × all fields (bundle · exec SHA · manifest 7/7 · UUID `E8074AD1-…` · dylib SHA; codesign team `ZVK2X646Z2`) · just-in-time read `.vpio01` ABSENT · authority supplied in full · exactly ONE install (bundle container `6A2E406B-…`, db seq 6720) · post-install `(no VoiceKernelHarness process)` · NOTHING LAUNCHED. The device now holds the VPIO artifact beside K00/R1 (untouched). Artefact IN CUSTODY (`2f5ffb8a5` cherry-picked; SHA `4b3938bc…` verified = founder's; §13.7.1). ⚠️ DEVIATION §13.7.2, founder-disclosed: a second invocation from another Mac-side worktree at 19:46:11Z (before the founder's stop reached it) read the device again → custody MATCH · `.vpio01` PRESENT · REFUSED exit 4 · no install/uninstall/overwrite/launch/sample — a read-only device act outside the one authorized transaction; first live exercise of the PRESENT guard; ruled ACCEPT FOR CUSTODY (founder) → cherry-picked `1ef530b47` from `6d95d47da4`, sha `ea4e96af…` re-verified; deviation only, not absorbed, physical authority NONE. Launch · driver test · journal pull NOT AUTHORIZED.** ⭐ **F-W1 / N=30 OPEN (founder ruling 2026-09-14; plan §13.8) — NO PILOT: sample 1 of the predeclared N=30 is the first physiological VPIO observation.** One batch on the FIRST-INSTALL-01 install (container `6A2E406B-…`, seq 6720): `k00-driver-batch.sh VPIO-01 30 --vp on --mode L --hold 15 --subject vpio-01`, instrument `de3efd3fb` unchanged, no reinstall inside, top-up/auto-rerun FORBIDDEN, abort → INCOMPLETE; authority string at invocation (recorded §13.8; the batch does not consume it mechanically). Read-only preflight PINNED as a separate act before the batch (apps listing by `--bundle-id` + `--json-output` must carry `6A2E406B…`; processes listing must show no `VoiceKernelHarness`; else STOP) because the batch's own sample-1 precondition would terminate-only, which the ruling forbids pre-sample-1. F-W1 unchanged (≥24/30 · ≥23/29 · ≥23/28 clear; ≤16/29 same/worse; between INDETERMINATE; >2 infra characterize only; Fisher vs A/B/C/R1 each, never pooled). Earns K00-04 entry axis only; K00-11/12/13/15 NOT AUTHORIZED; KERNEL-00 not accepted by any result. ** ⭐⭐ **F-W1 N=30 EXECUTED and VERIFIED HERE (`VPIO-01-20260914T200542Z`, evidence `75bfffaece` cherry-picked; record `KERNEL-00_VPIO-01_F-W1_WITNESS_2026-09-14.md`; plan §13.9): preflight clean · 30/30 invocations · exit 0 · 0 infra · 30/30 hashes and classifier reproduced · every journal cold · **30 × `failure then degradation`, 0 gen-1 takes.** Physiology identical ×30: session activates fine (`in=builtInMic sr=48000`) → VPIO unit created, I/O enabled, VP set (bypass 0) → **Input-scope element-1 stream format read BEFORE `AudioUnitInitialize` returns `sampleRate 0.0`** → §3 `requireValid` refuses at gen 1 → `enterConversation_failed → degraded`, no recovery (O6 shape ×30); activation→refusal 13/15/37 ms; trace stops at step 4 of 11 — never initialized, never started, no callback. **F-W1 SPENT · FAIL: row "about the same / worse", decisively WORSE than A/B/C/R1 (P(≤0) 6e-7…2e-5 each, never pooled).** The lower-path question — can the VPIO unit start and listen on this device — is UNMEASURED: this was a deterministic precondition refusal against an uninitialized raw unit's property read, not a start that failed to take. Candidate causes = inference only (format populated only at initialize · session sample rate is the intended fact · timing race, least likely); any correction = new SHA = new subject = founder act. NO next act opened. Returned for adjudication: F-W1 outcome · design defect vs substrate finding · where (never whether) the §3 guard reads.** ⭐ **ADJUDICATED (founder; witness record §9 / plan §13.10): F-W1 FAIL ACCEPTED · VPIO-01 CLOSED as a subject · VPIO lower path NOT CLOSED · substrate capability UNMEASURED · the step-4 refusal = SUBJECT DESIGN DEFECT (falsified: an uninitialized unit's Input-scope/el-1 format read is a truthful hardware witness); §3 law STANDS, its evidence moves → NEW SUBJECT `VPIO-02 / FORMAT-RESOLUTION-01`: probe `AudioUnitInitialize` → read format → probe `AudioUnitUninitialize` → `requireValid()` → only if valid: client formats from the observed rate · MaxFramesPerSlice · arm · final initialize · start; probe never starts the unit; uninitialize precedes any throw; 14-step trace with the probe visible; session-sourced rate / hard-coded 48 kHz / alternate scope / engine fallback / retry HELD. Envelope: `AudioGraph.swift` · `PureLogicTests.swift` · gate · `project.yml` (`life.soullab.voicekernel.vpio02` · `VoiceKernel VPIO-02`) · records; nine invariant files + harness behavioural Swift byte-identical. `.vpio01` frozen evidence, never overwritten/uninstalled. SOURCE IMPLEMENTATION AUTHORIZED · MAC-COMPILE HELD · install HELD · sample HELD · same F-W1 matrix for any VPIO-02 witness · VPIO-01 rows never VPIO-02 rows.** ⭐ **VPIO-02 SOURCE LANDED (plan §13.11) — IMPLEMENTED, NOT COMPILED:** exactly the envelope (`AudioGraph.swift` · `PureLogicTests.swift` · `project.yml` · gate); nine invariant files + `Package.swift` + harness Swift byte-identical to `85e5e7154` (verified); VPIO-01B instrument byte-identical to `de3efd3fb` (vpio-02 instrument plumbing = later witness work). `start()`: probe `AudioUnitInitialize` (OSStatus checked before the read) → read (Input scope, el 1, stamped `afterProbeInitialize`) → probe `AudioUnitUninitialize` (checked) → unchanged `requireValid()` → unchanged client formats from the observed rate · G9 · arm · real initialize · start; 14-step trace with the probe visible; probe window inert (no set/arm/listen/start); custody `life.soullab.voicekernel.vpio02` / `VoiceKernel VPIO-02`. Gate 49→**53/53** (K00-16 pin → `.vpio02`; PW-02 ordering reshaped; envelope pin replaces the frozen-organism pin; new VPIO-02 block). **MAC-COMPILE HELD · install HELD · sample HELD · STOP.** ⭐ **VPIO-02 / MAC-COMPILE-01 OPEN on exactly `ac12dedf4` (founder ruling 2026-09-14; plan §13.12) — Mac act, NOT YET EXECUTED:** founder independently verified the envelope; toolchain Xcode 26.3 (17C529) · Swift 6.2.4 · XcodeGen 2.46.0 · SDK 26.2; sequence swift build → swift test → gate 53/53 → xcodegen → unsigned generic-iOS → signed (team `ZVK2X646Z2`, Xcode destination id `00008140-00163D9922E0801C`, never the devicectl id) → custody identity only if all green; **stop at the first RED, no correction inside the act** (any correction = new SHA = fresh authorization); the stale "Eleven steps on this subject" comment at `AudioGraph.swift:160` preserved deliberately; xcodegen footprint recorded, never committed; record owed on a `feature/*` branch → cherry-pick. **GREEN opens nothing:** instrument plumbing HELD · install NOT AUTHORIZED (`.vpio01` frozen; `.vpio02` never installed; K00/R1 untouched) · launch/sample/N=30 NOT AUTHORIZED · F-W1 matrix unchanged. ⭐⭐ **VPIO-02 / MAC-COMPILE-01 GREEN on exactly `ac12dedf4` (founder record `KERNEL-00_VPIO-02_MAC-COMPILE-01_2026-09-14.md` 1 164 lines + 7-file manifest, cherry-picked `2d3d720bb` from `8ff6629ac`; record sha `5c01d647…` and manifest sha `1459283c…` re-hashed here = founder's = the record's own custody line; plan §13.13):** `swift build` PASS · `swift test` **27/27** (Fourteen-seams test replaced Eleven-seams, count unchanged) · gate 53/53 · xcodegen PASS · unsigned PASS · **signed `BUILD SUCCEEDED`** · only the known `try?` warning · footprint = xcodegen `Info.plist` + untracked manifest, nothing committed from the worktree. **First VPIO-02 signed artifact: bundle `life.soullab.voicekernel.vpio02` · display `VoiceKernel VPIO-02` (read from the built plist + codesign) · dylib UUID `B346F448-A2A8-30DB-9683-B732A80D7899` (≠ VPIO-01's `E8074AD1-…`) · dylib SHA `e963a23c…` · executable SHA `9fe56504…` · manifest `1459283c…` (7 files) · team `ZVK2X646Z2`.** One founder-disclosed custody-side event (record commit first refused by the hook for a missing `node_modules` in the fresh record worktree; dependencies linked, recommitted; after the compile, no source/evidence touched) preserved, not absorbed. **MAC-COMPILE-01 CLOSED · opens NOTHING:** instrument plumbing HELD · install NOT AUTHORIZED (`.vpio01` frozen · `.vpio02` never installed · K00/R1 untouched) · launch/sample/N=30 NOT AUTHORIZED · F-W1 UNSPENT for VPIO-02 (VPIO-01's 0/30 never a VPIO-02 row) · next act = founder ruling on VPIO-02 witness prep (instrument-only). ⭐ **VPIO-02B WITNESS PREP OPENED (founder) → INSTRUMENT IMPLEMENTED at `08483cfe4` (plan §13.14), instrument-only, organism byte-identical to `ac12dedf4` (gate-pinned):** fourth subject row `vpio-02` (`.vpio02` · `VoiceKernel VPIO-02` · `ioRunning` · the 14-step trace) identical across ledger · driver · batch · reinstall; identity = exact 14 steps or exact ordered proper prefix + gen-1 refusal, no step synthesized, no fifth class; VPIO-02 pins in their own `VPIO02_*` constants (VPIO-01 pins unchanged), `PIN_*` selected by subject, same fail-closed first-install transaction, `.vpio01` never in a verb; batch gains only the case row, no reinstall added; ledger `--selftest` 13→**34/34** (every cross-subject mismatch named; Phase-A 14-step ≠ vpio-02); **frozen-classifier regression 1 494/1 494 rows byte-identical over 498 journals × 3 subjects (sha `f373a8f9…`)**; VPIO-01 N=30 → 30 × degradation under `vpio-01`, 30 × SUBJECT-MISMATCH under `vpio-02`; reinstall refusals shimmed offline ×11 (0 device verbs on every refusal); gate 53→**60/60** read before commit. Recorded limitation: a refusal after the shared 3-step head is trace-indistinguishable between the two VPIO subjects (custody decides; no such row exists). **Driver-only compile OWED on exactly `08483cfe4`** (`xcodegen` + `build-for-testing`, generic iOS, `CODE_SIGNING_ALLOWED=NO`; no phone test/install/launch/container/journal/sample). First-install read HELD · install/launch/sample/N=30 NOT AUTHORIZED · F-W1 VPIO-02 UNSPENT. ⭐ **VPIO-02B / DRIVER-COMPILE-01 GREEN on exactly `08483cfe4` (founder record `KERNEL-00_VPIO-02B_DRIVER-COMPILE-01_2026-09-14.md`, cherry-picked `14125b20c` from `26748de2a`, sha `db957814…` re-hashed here = founder's; plan §13.15):** `xcodegen` PASS · `build-for-testing` `** TEST BUILD SUCCEEDED **` (generic iOS, signing off, once) · no `test-without-building`/`devicectl`/install/launch/container/journal/sample · organism byte-identical to `ac12dedf4` · gate 60/60 read on the Mac; product hashes xctestrun `7105c95e…` · DriverUITests `6d669e39…` (only the test bundle moved) · XCTRunner `6fa0f967…` · DriverHost `57793876…`. **Shared-prefix RULED: classifier UNCHANGED** — *trace compatibility is not subject identity; subject identity is declared custody + trace compatibility*; a detached journal with unknown custody is not attributable from the trace alone. **Nothing opened by the compile; next decision point = founder ruling on VPIO-02 FIRST-INSTALL-02.** ⭐ **FIRST-INSTALL-02 AUTHORIZED (founder; plan §13.16) — INSTALL ONLY, NOT YET EXECUTED:** one custody-gated transaction with the `08483cfe4` instrument on the existing MAC-COMPILE-01 product (founder re-read locally: every pinned field MATCH, no device access): local custody MATCH → the script's own one JIT apps read (**no separate device preflight**) → `.vpio02` ABSENT → authority at invocation → exactly one install → one process read → STOP; mismatch / unreadable / PRESENT → STOP, refusal evidence returned, **a refusal consumes the invocation, no retry**, no uninstall/overwrite. Invocation pinned verbatim in §13.16 (cwd = the driver-compile worktree at exactly `08483cfe4`; three local checks first; explicit `K00_SUBJECT=vpio-02` · `K00_EXPECT_UUID` · `K00_EXPECT_DYLIB_SHA` · exact manifest path · exact product path · the full authority string, never abbreviated). Executes on the Mac; artefact + transcript owed on a `feature/*` branch. Success establishes only `VPIO-02 INSTALLED`: launch · sample · N=30 NOT AUTHORIZED · F-W1 UNSPENT · `.vpio01` FROZEN · K00/R1 UNTOUCHED. ⭐⭐ **FIRST-INSTALL-02 EXECUTED and VERIFIED HERE (founder, `reinstall-20260914T221040Z`; record `KERNEL-00_VPIO-02_FIRST-INSTALL-02_2026-09-14.md` + transcript + artefact cherry-picked `8e269a269` from `826098708`, all three hashes recomputed here = founder's; plan §13.17):** three local checks ✓ (HEAD `08483cfe4` · instrument-identical · manifest `1459283c…` · product present) → exactly ONE instrument invocation → custody MATCH × all fields (UUID `B346F448-…` · dylib SHA `e963a23c…` · exec SHA `9fe56504…` · manifest 7/7 · codesign team `ZVK2X646Z2`) → JIT read `.vpio02` ABSENT → authority supplied (transcript carries the byte count `1861` = the ruling's string as held here; the string itself was exported from a Mac-side file, byte identity founder-attested) → ONE install (bundle container `E3B88028-A10F-46B1-AB27-CF0A1F83FB78`, db `42158240-…`, seq 6972) → `(no VoiceKernelHarness process)` → exit 0. Founder-disclosed: the remote-command request timed out after the single dispatch, NOT retried. **The one FIRST-INSTALL-02 authority is SPENT. Device now holds K00/R1 (untouched) · `.vpio01` (frozen) · `.vpio02` (installed, never launched). Nothing downstream opened: launch · driver test · journal pull · sample · N=30 NOT AUTHORIZED · F-W1 VPIO-02 UNSPENT (matrix unchanged) · KERNEL-00 NOT ACCEPTED · next decision point = a separate founder ruling on a VPIO-02 F-W1 N=30 witness (proposed shape mirrors §13.8, NOT opened).** ⭐ **VPIO-02 F-W1 / N=30 OPEN (founder ruling 2026-09-14; plan §13.18) — NO PILOT: sample 1 of the predeclared N=30 is the first physiological VPIO-02 observation.** One batch on the FIRST-INSTALL-02 install (organism `ac12dedf4` · instrument `08483cfe4` · bundle `.vpio02` · container `E3B88028-…`): `k00-driver-batch.sh VPIO-02 30 --vp on --mode L --hold 15 --subject vpio-02`, no reinstall inside, top-up/auto-rerun FORBIDDEN, abort → INCOMPLETE; authority string (1 534 bytes) at invocation, recorded §13.18, not consumed mechanically by the batch. Read-only preflight PINNED as a separate act before the batch (HEAD = `08483cfe4` · instrument identical · apps listing by `--bundle-id` + `--json-output` must carry `E3B88028…` · processes listing must show no `VoiceKernelHarness`; else STOP, no terminate-only/reinstall/overwrite) because the batch's own sample-1 precondition would terminate-only. F-W1 unchanged (≥24/30 · ≥23/29 · ≥23/28 clear; ≤16/29 same/worse; between INDETERMINATE; >2 infra characterize only; one-sided Fisher vs A 14/29 · B 15/28 · C 16/29 · R1 13/30 each, never pooled; VPIO-01 0/30 never pooled/substituted). Mac act, NOT YET EXECUTED; evidence owed on a `feature/*` branch. Earns K00-04 entry axis only; route/interruption/reset/endurance NOT AUTHORIZED; KERNEL-00 not accepted by any result. ⭐⭐ **VPIO-02 F-W1 N=30 EXECUTED and VERIFIED HERE (`VPIO-02-20260914T223500Z`, evidence `fa13d9c86` cherry-picked `8c6254d72`; record `KERNEL-00_VPIO-02_F-W1_WITNESS_2026-09-14.md`; plan §13.19): preflight clean · 30/30 · exit 0 · 0 infra · 30/30 hashes + classifier rows reproduced · cold ×30 · 14-step trace with `input_format_read afterProbeInitialize=true 48000/1` ×30 · VP ON · `ioRunning` true at every tick · first callback 1–10 ms · 0 refusals · 0 resets/interruptions → **29 × gen-1 listen (319–360 ms, median 338) · 1 × failure then recovery** (sample 1: gen 1 ran with all-digital-zero input → supervisor `input_dead` at 2 001 ms → attempt 1 → gen 2 listening at 4.17 s, held; the ONLY journal with `app_lifecycle willResignActive`, at 0 ms after gen-1 `graph_started` — antecedent recorded, not attributed, n=1) · every row held at export. **Frozen table (reading only): 30 valid → ≥24 = CLEAR IMPROVEMENT; observed 29; one-sided Fisher vs A p=2.2e-05 · B 1.1e-04 · C 1.6e-04 · R1 4.0e-06, all < .05; VPIO-01 0/30 never pooled → CLEAR-IMPROVEMENT band on the K00-04 entry axis.** ADJUDICATION OWED (founder); K00-05/06/11/12/13/15 unmeasured on VPIO-02; KERNEL-00 NOT ACCEPTED; route/interruption/reset/endurance/reinstall/new sample NOT AUTHORIZED; `.vpio01` FROZEN; K00/R1 UNTOUCHED. ⭐⭐ **ADJUDICATED (founder, 2026-09-14; witness record §9 / plan §13.20): VPIO-02 F-W1 PASS · CLOSED · 29/30 · K00-04 PASS on VPIO-02 · LOWER-PATH ENTRY QUALIFIED.** Witness accepted as read; nothing post-hoc needed. Sample 1 = valid `failure then recovery`; its lone `willResignActive` preserved as observed / plausible inference / causation NOT established; no lifecycle experiment authorized. Established: the corrected VPIO-02 substrate demonstrates reliable healthy entry on the physical microphone path, repeatedly (probe → guard → configure → arm → initialize → `AudioOutputUnitStart` → running → callbacks → healthy physiology) — VPIO-01's unmeasured capability question answered YES for the entry axis. C-D19 accepted as gate maintenance. Wording correction: the 1500 ms entry ceiling is **K00-04** (not K00-03); latencies descriptive only. UNMEASURED on VPIO-02: K00-05 · 06 · 11 · 12 · 13 · 15. **Standing: VPIO-01 CLOSED · FAILED · 0/30 · VPIO-02 F-W1 CLOSED · PASS · new sample / route / interruption / reset / endurance / reinstall NOT AUTHORIZED · `.vpio01` FROZEN · K00/R1 UNTOUCHED · KERNEL-00 NOT ACCEPTED · next programme decision (founder's, NOT opened): whether to open a bounded K00-05 output/cancellation + K00-06 duplex-physiology qualification act on VPIO-02.** ⭐ **K00-05/06 PLAN + COVERAGE CENSUS RETURNED, read-only (founder authorization; `KERNEL-00_VPIO-02_K00-05-06_QUALIFICATION_PLAN_2026-09-14.md`; plan §13.21):** the frozen organism already journals every seam (handle · render progress · `stream_complete` · `stream_cancelled` · `stream_cancel_measured` · per-second input windows with `digitalZero/noiseFloor/signal`); harness buttons `Play 3 s tone` / `Cancel active` exist; the driver taps neither and the entry classifier reads no output record; **no journal anywhere carries `stream_cancel_measured`/`stream_complete` — K00-05/06 never exercised on any device, any subject.** Two separate falsifiable readings pinned (K00-05 cancel ≤ 100 ms by handle + completion; K00-06 callbacks/no-digital-zero/independently observable; coupling descriptive, post-AEC noted). Options: A manual (suspension) · B harness change REJECTED (changes the qualified subject) · **C driver-only RECOMMENDED** (one test, two batch flags, one new output reader; organism + installed `.vpio02` untouched) · D unnecessary. Shape N = 10 (ceilings per row, no rate comparison), validity/infra/abort/PASS/FAIL predeclared. Nothing implemented/compiled/installed/launched/played/sampled; returned for ruling. ⭐ **RULED (founder; qualification plan §8 / plan §13.22): ACCEPTED WITH AMENDMENTS · OPTION C · K00-05/06 INSTRUMENT PREP OPEN · device witness NOT YET AUTHORIZED.** Surface exactly: driver test · batch (`--act output --cancel-at 1000 --settle 2`; historical invocations unchanged) · NEW `k00-output-ledger.py` (evidence-only, twelve pinned offline cases) · gate · records; `k00-ledger.py` / `k00-reinstall.sh` / organism byte-frozen; app under test receives nothing. K00-05: no pre-cancel-sample requirement, no completion-latency ceiling (only ≤100 ms cancel-to-last-frame is ratified), foreign input fault = non-evidence. K00-06: full rendering windows only; baselineRate median over ≥2 healthy pre-output windows; gap FAIL < 0.90× (witness criterion, not constitutional); collapse FAIL = `digitalZero == callbacks` or `input_dead`; partial zero = CHARACTERIZE; ≥2 full windows else UNMEASURED; coupling descriptive. N = 10 fixed, no top-up, FAIL decides but rows continue; green = built-in duplex only (final K00-06 closure travels with K00-11). Sequence: record → implement → self-test → gate → instrument SHA → driver-only compile (signing OFF) → STOP. ⭐ **OPTION C INSTRUMENT LANDED at `8b111709b6e5010b4b6ee7281d257141945276ff` (qualification plan §9 / plan §13.23) — IMPLEMENTED · SELF-TESTED 33/33 · GATE 64/64 · NOT COMPILED:** driver `testOutputSample` (wait Play enabled ≤5 s → settle → Play → cancel-at → Cancel active if enabled → 1 s → Play → 4.5 s → Export; markers by `NSLog`; historical tests byte-identical) · batch `--act entry|output --cancel-at 1000 --settle 2` (historical invocation verbatim; output act reads each journal a second time into `output-ledger.md`) · NEW evidence-only `k00-output-ledger.py` (K00-05-CANCEL · K00-05-COMPLETE · K00-06 · COUPLING rows; 60 real VPIO journals → never PASS/FAIL) · organism + entry classifier + reinstall byte-frozen, gate-pinned. **Next founder act = driver-only `xcodegen` + generic-iOS `build-for-testing`, signing OFF, on exactly `8b111709b` (pinned in plan §9); then STOP. K00-05/06 device witness NOT AUTHORIZED; nothing installed/launched/played/cancelled/sampled.** ⚠️ First compile attempt VOID (qualification plan §9.1): run from the old `08483cfe4` worktree (the new reader absent there proves it), green build = historical driver, not `8b111709b`; two instruction defects of this session (placeholder worktree path; `#` comments on zsh lines, the MAC-COMPILE-03 shape); corrected paste-able invocation pinned in §9.1 (fresh detached worktree at the SHA, fresh derived path). ⭐ **DRIVER-ONLY COMPILE GREEN on exactly `8b111709b` (founder transcript; §9.2):** fresh worktree `HEAD 8b111709b…` · `reader-present` · `selftest: 33/33` · `xcodegen` PASS · generic-iOS `build-for-testing` signing OFF → `** TEST BUILD SUCCEEDED **`; no device verb. Custody PINNED from the founder transcript (§9.3): success 1 · device/test verbs 0 · xctestrun `7105c95e…` / XCTRunner `6fa0f967…` / DriverHost `57793876…` identical to VPIO-02B, `DriverUITests` `ab573914…` (the one moved bundle), log `680fe1e9…` (file owed, hash fixed) → **DRIVER-COMPILE-01 (K00-05/06) CLOSED.** **STOP: K00-05/06 device witness NOT AUTHORIZED; next act = a separate founder ruling naming the batch (`--act output --cancel-at 1000 --settle 2`, N = 10), preflight and authority string.** ⭐ **K00-05/06 DEVICE WITNESS OPEN AFTER RECORD PIN (founder ruling 2026-09-14; qualification plan §10):** instrument `8b111709b` accepted as the exact witness instrument; subject = the FIRST-INSTALL-02 `.vpio02` (container `E3B88028-…`, UUID `B346F448-…`), no rebuild/reinstall; read-only preflight pinned (HEAD `8b111709b` · witness surface identical · apps listing carries `E3B88028` · zero harness processes; else STOP, no terminate-only/reinstall/overwrite); exact invocation pinned `k00-driver-batch.sh K00-0506 10 --act output --cancel-at 1000 --settle 2 --vp on --mode L --subject vpio-02` with the 2 086-byte authority string at invocation; N = 10, no pilot/top-up/rerun; frozen reading law: K00-05 PASS = 10/10 cancel rows ∧ 10/10 completion rows PASS-05 (≤100 ms ceiling; completion timing descriptive) · K00-06 built-in PASS = 10/10 duplex rows PASS-06 (full windows; collapse = all-zero/`input_dead`; partial = characterize) · non-evidence rows → INCOMPLETE, no replacement; green = K00-05 PASS on VPIO-02 + K00-06 built-in only. Mac act → ⭐ **EXECUTED and READ HERE (`K00-0506-20260914T235008Z`, evidence `36d0d683d` cherry-picked `6960e13e7`; record `KERNEL-00_VPIO-02_K00-05-06_WITNESS_2026-09-14.md`; qualification plan §10.1–10.4): two lawful preflights clean; a duplicate invocation refused by the CAL-F3 device lock (empty footprint preserved); the one batch ran 10/10 → TEN `DRIVER/INFRASTRUCTURE FAILURE` rows** (1–2 automation-mode timeout at batch start; 3–10 `'Play 3 s tone' not found after Enter` — Enter tapped, the button never in the hierarchy, snapshot discarded with the `.xcresult`; seven terminate-only preconditions passed; one harness process left alive after sample 10) → **no journal, no K00-05/06 row, nothing read about the organism → `>2 infra` → CHARACTERIZE ONLY · K00-05 INCOMPLETE · K00-06 INCOMPLETE · authority SPENT · K00-04 standing untouched.** C-D20 candidate (inference): the Output section is the fourth section of a lazy SwiftUI `List`, offscreen after Enter; correction shape = driver-only scroll-to-reveal + hierarchy-on-failure (new SHA · driver compile · new ruling), NOT written. Returned for adjudication: classification · C-D20 · disposal of the lingering harness process (corrective device act, not authorized) · whether a fresh N=10 opens. ⭐ **ADJUDICATED (founder; witness record §8 / qualification plan §10.5): classification ACCEPTED (characterize only · K00-05 INCOMPLETE · K00-06 built-in INCOMPLETE · authority SPENT · K00-04 undisturbed) · C-D20 bounded driver repair AUTHORIZED, causal explanation UNPROVEN — surface exactly `K00DriverTests.swift` + gate + records; frozen organism/harness/batch/readers/reinstall; correction = after Enter, if `Play 3 s tone` absent → ≤ 4 upward swipes with an exact-label check after each, then the unchanged `waitEnabled`; still absent → `debugDescription` hierarchy into the runner log (`K00-HIERARCHY:`) then the SAME `DRIVER/INFRASTRUCTURE FAILURE`; never an audio outcome; then gate → new SHA → driver-only compile signing OFF → custody → STOP, no device witness by the correction · ONE corrective termination AUTHORIZED (read → exactly one harness process → `testTerminateOnly` vs vpio-02 on the `8b111709b` runner with `TEST_RUNNER_K00_SUBJECT=vpio-02` → read once → STOP; pinned §10.5 C) · fresh N=10 HELD behind new SHA + compile custody + clean process state + a pinned runner-readiness preflight (one `testTerminateOnly` on the corrected runner; FAIL → STOP) and a fresh authority string.** ⭐ **C-D20 LANDED at `83a382a14` (qualification plan §10.6) — driver test only (`+19/−1`), gate 65/65 (bound `revealSwipeLimit = 4` pinned · one `swipeUp` site · hierarchy before the same failure · tail from `waitEnabled` byte-equal to `8b111709b` · batch/readers/reinstall byte-identical to `8b111709b` · organism frozen); NOT COMPILED — driver-only `xcodegen` + generic-iOS `build-for-testing` signing OFF on exactly `83a382a14` is the next founder Mac act (invocation + custody block pinned §10.6), then STOP. Corrective termination (§10.5 C, `8b111709b` runner) independent; fresh N=10 HELD.** ⭐ **BOTH MAC ACTS RETURNED and VERIFIED HERE (`ed0c2c337` → cherry-pick `87f0d403a`; qualification plan §10.7): corrective termination PASS — before-read exactly one `VoiceKernelHarness` (PID 67021, in the `E3B88028` `.vpio02` container), one `testTerminateOnly` on the pinned `8b111709b` xctestrun/destination (Executed 1 test, 0 failures), after-read 0, no second invocation → harness CLEARED, device clean · C-D20 DRIVER-COMPILE-02 GREEN on exactly `83a382a14` (`TEST BUILD SUCCEEDED` ×1, device/test verbs 0, signing OFF, generic iOS; log sha `ff79b82a…` recomputed = recorded; only `DriverUITests` moved `ab573914…` → `9e8ebb46…`, xctestrun/XCTRunner/DriverHost identical) → CLOSED. Nothing about K00-05/06 measured. Next boundary = a founder ruling naming the runner-readiness preflight (one `testTerminateOnly` on a SIGNED build of `83a382a14` from its worktree — the compile-green product is unsigned and cannot run on the phone) and a fresh N=10 authority string; NOT opened.** ⭐ **RULED (founder; qualification plan §10.8): RUNNER-READINESS PREFLIGHT OPEN — one invocation on the `83a382a14` worktree: HEAD read · surface diff vs `83a382a14` (read from the diff's exit status, not the unconditional echo) · `xcodegen` · SIGNED `build-for-testing` with the frozen batch's own recipe (destination `00008140-…`, `.derived`, `DEVELOPMENT_TEAM=ZVK2X646Z2`; verified term-for-term against batch lines 147–150) · one `testTerminateOnly` with `TEST_RUNNER_K00_SUBJECT=vpio-02` · one process read; PASS = HEAD exact ∧ surface unchanged ∧ signed build ok ∧ executed once, 0 failures ∧ after-read 0 harness; FAIL = evidence only, no retry/cleanup/edit/batch; runs even with zero processes (proves runner init) · FRESH K00-0506 N=10 SELECTED as the next experiment, EXECUTION HELD — frozen shape (N 10 · output · cancel-at 1000 · settle 2 · VP ON · L · vpio-02 · own K00-05/06 readings · no top-up/rerun); readiness PASS is a fact, not execution authority; the fresh authority string is issued only after the readiness evidence is recorded (§10.9), old string spent.** ⭐ **READINESS PREFLIGHT EXECUTED → PASS on every predeclared field, VERIFIED HERE (`416ce8de7` → cherry-pick `a663f4d7f`; qualification plan §10.9): HEAD exact · `READINESS_DIFF_RC=0` explicit · signed `TEST BUILD SUCCEEDED` ×1 (team `ZVK2X646Z2`, device destination, `.derived`, 0 device verbs) · `testTerminateOnly` started once, `Executed 1 test, 0 failures`, `TEST EXECUTE SUCCEEDED` (0.023 s = runner initialized on device, harness already `.notRunning`) · after-read 364 rows, 0 harness · no retry, batch not run; three log hashes recomputed = custody; signed-runner products pinned (xctestrun `3b6360f7…` · DriverUITests `4d8684d1…` · XCTRunner `d77c2490…` · DriverHost `704d21d3…`, device-targeted, ≠ the signing-OFF hashes by design). CLOSED. Creates NO execution authority: fresh K00-0506 N=10 SELECTED · EXECUTION HELD · authority string DOES NOT EXIST · no Mac act live; next = the founder's fresh-batch ruling (§10.10).** ⭐ **FRESH K00-0506 N=10 OPEN AFTER RECORD PIN (founder ruling; qualification plan §10.10): population = organism `ac12dedf4` · installed `.vpio02` / `E3B88028…` · instrument `83a382a14` · readiness PASS · N=10 · output · cancel-at 1000 · settle 2 · VP ON · L · vpio-02; fresh detached batch worktree `/private/tmp/k0506-batch-83a382a14`; founder-pinned read-only preflight (HEAD exact · surface diff rc 0 · no other K00-0506 batch process · apps listing carries `E3B88028` · zero harness; anything else STOP, no corrective act) → exact invocation `k00-driver-batch.sh K00-0506 10 --act output --cancel-at 1000 --settle 2 --vp on --mode L --subject vpio-02` with `K00_DEVICE`/`K00_XCODE_DEST` and the **2 927-byte authority string verbatim** (recorded byte-for-byte in §10.10, sha `fb137385…`; batch does not consume it mechanically); per-sample terminate-only lawful for samples 2–10, a pre-sample-1 harness = protocol deviation; frozen adjudication law unchanged (10/10 PASS-05 cancel ∧ completion · 10/10 PASS-06 · >2 infra/EN/not-a-cancel → characterize, INCOMPLETE · no top-up/rerun). Mac act, NOT YET EXECUTED; evidence → §10.11. No outcome authorizes any subsequent act.** ⭐⭐ **FRESH K00-0506 N=10 EXECUTED and READ HERE (`K00-0506-20260915T004738Z`, evidence `3fde4bbab` → cherry-pick `43fbe8c8f`; record `KERNEL-00_VPIO-02_K00-05-06_WITNESS-02_2026-09-15.md`; plan §10.11): preflight clean · 10/10 · 0 infra · 0 terminate-only · C-D20 exercised ×10 (one reveal swipe each, hierarchy path never taken) · 10 × gen-1 listen (329–436 ms), cold, 14-step, VP ON · **K00-05: 10/10 cancel PASS-05 (handle-exact; last non-silent frame 0–10 ms BEFORE the cancel; `cancelToSilenceMs` 0; ≤ 100 ms) ∧ 10/10 completion PASS-05 (144 000/144 000) → frozen PASS condition MET** · **K00-06 built-in: 3 PASS-06 · 5 CHARACTERIZE-06 (partial digital zero 1–42 callbacks/batch, never a full window, `input_dead` never, min rate ≥ 99.2/s) · 2 UNMEASURED-06 (one full rendering window) · 0 FAIL-06 → PASS not met, FAIL not fired, > 2 non-adjudicating rows → CHARACTERIZE ONLY · INCOMPLETE under the frozen law** · coupling descriptive (post-AEC input attenuated during own playback 9/10) · `listeningLostLater=True` ×10 = the lawful `listening → maiaSpeaking` output floor (semantics note, field frozen) · authority SPENT. ADJUDICATION OWED (founder); no act opened; KERNEL-00 NOT ACCEPTED.** ⚠️ Session defect: the §10.11 record commit `147f6b9a2` was pushed on a RED gate (the new K00-0506 journals fell into the corpus test's engine-era partition; the commit chain did not stop) — recorded in witness-02 §11, records-only commit, evidence/instrument untouched → **C-D21** gate maintenance (C-D19 shape: `K00-0506-20260915T004738Z` named as its own VPIO-02 population, 10 × gen-1 listen / 10 × SUBJECT-MISMATCH, excluded from engine-era), gate 65/65 read alone; founder acceptance owed. ⭐⭐ **ADJUDICATED (founder; witness-02 §12 / plan §10.12): K00-05 on VPIO-02 ✅ PASS · CLOSED (no further K00-05 population warranted) · K00-06 built-in 🟨 CHARACTERIZE ONLY · INCOMPLETE — not a duplex failure, not PASS, not FAIL, not evidence for an organism repair; characterize rows not promoted, one-window rows stay UNMEASURED → ONE read-only K00-06 PARTIAL-ZERO DISCRIMINATOR PLANNING ACT OPENED (question: can builtInMic carry an independent external acoustic signal while VPIO-02 renders through builtInSpeaker, or are the partial-zero callbacks capture loss rather than own-playback/AEC attenuation? read journals/reader/health semantics/seams; prefer existing evidence and the same route; no new threshold; if insufficient name the smallest instrument addition; may produce a protocol, authorizes nothing) · `listeningLostLater=True` under output acts = lawful `maiaSpeaking` floor, ≠ input/duplex failure, field frozen · C-D21 ACCEPTED as gate maintenance (`147f6b9a2` red-gate fact and `1f05a38b9` successor both preserved, no revert). Standing: authority SPENT · organism FROZEN · `.vpio02` untouched · new instrument / device witness / route / interruption / reset / endurance NOT AUTHORIZED · KERNEL-00 NOT ACCEPTED.** ⭐ **PARTIAL-ZERO DISCRIMINATOR PLAN RETURNED, read-only (`KERNEL-00_VPIO-02_K00-06_PARTIAL-ZERO_DISCRIMINATOR_PLAN_2026-09-15.md`; plan §10.13):** "digital zero" = `peak ≤ 1e-7` at the consumed post-VP seam (organism's only input; no raw-mic seam); corpus: every in-stream near-silence callback is in stream 2 at +0.8…3.0 s, none in the cancelled stream 1; post-VP input attenuates progressively to ~1e-6 rms / `signal` 0 over ~1 s of own playback; PASS-06 vs CHARACTERIZE-06 among samples 3–10 = a 1e-7 threshold edge on one continuous shape; sample 1 unattenuated (unexplained, n=1); ambient near-end `signal` present at every baseline vanishes during deep attenuation (uncontrolled → A live-capture vs A′ near-end-suppressed-at-seam vs B capture-collapse undecidable from the corpus; B not supported). Design: S1 VP-OFF output act with the existing instrument (no code) → S2 VP-ON + steady labelled external stimulus spanning the invocation (founder-placed, or batch-only `--stimulus`; Mac playback verb to be read, not guessed) → S3 only on A′; window-level readings on existing fields, no ms alignment, no constitutional threshold; N=10/arm; K00-06 PASS condition untouched. Rulings owed: S1 open? · S2 shape · reading law · the seam K00-06 governs. Nothing implemented/run.** ⭐ **RULED (founder; discriminator plan §7–§8 / qualification plan §10.14): plan ACCEPTED · K00-06 GOVERNS THE CONSUMED POST-VP SEAM (hardware mic capturing ≠ K00-06 satisfied; A′ matters architecturally; no raw-mic tap) · §4 reading law RATIFIED as witness criteria, never constitutional, with one amendment (`10× S0 floor` descriptive only; discriminator = existing classification: `signal` persists through full rendering windows → A-consistent · disappears during deep rendering with callbacks/`ioRunning` intact → A′-consistent · stimulus unseen at baseline → UNMEASURED · mixed → CHARACTERIZE; the 3/5/2 K00-06 population unchanged) · **S1 VP-OFF OUTPUT WITNESS OPEN AFTER RECORD PIN** — stratum `K00-0506-VPOFF`, N=10, instrument `83a382a14`, organism `ac12dedf4`, `--vp off` end-to-end with NO code, fresh worktree `/private/tmp/k0506-vpoff-83a382a14`, §10.10-shaped preflight, 2 389-byte authority verbatim (sha `4e77a98214c93f7f…`), exact invocation `k00-driver-batch.sh K00-0506-VPOFF 10 --act output --cancel-at 1000 --settle 2 --vp off --mode L --subject vpio-02`; reading = frozen K00-06 rows + descriptive mechanism reading (own playback observable under bypass → processor-dependent suppression supported, B not supported · deep near-silence persists under bypass → below-VP possibility remains · mixed → CHARACTERIZE); cannot reopen K00-05 or earn K00-06 · S2 shape SELECTED = batch-orchestrated source (custody), IMPLEMENTATION HELD behind S1 reading → Mac playback-capability census (verb discovered, never assumed) → batch-only design → gate → SHA → S2 authority · S3 CONDITIONAL, NOT OPEN. Mac act, NOT YET EXECUTED; no S1 result silently opens S2.** ⭐⭐ **S1 EXECUTED and READ HERE (`K00-0506-VPOFF-20260915T012817Z`, evidence `36b9865a6` → cherry-pick `2eef7d500`; discriminator plan §9 / qualification plan §10.15): preflight clean (transport deviation founder-disclosed and preserved: the pinned block refused as one payload, the same logical preflight completed as shell checks + exactly one apps read + one process read, nothing repeated) · 10/10 · 0 infra · `bypassRequested 1 · readBack 1 · voiceProcessing false` ×10 · 10 × gen-1 listen · **ZERO near-silence callbacks inside any stream in all ten; no progressive attenuation; instead the consumed input during rendering reads the phone's own tone un-cancelled at `rmsMean` 0.06–0.07 (×10–55 baseline), `signal` ≈103/103, callbacks 99–100/s, `ioRunning` true** · frozen rows 7 PASS-06 · 2 UNMEASURED-06 · 1 FAIL-06 (row 1: the reader's family falsifier fired because the last input window is stamped at the exact ms of `stream_complete` — counted full, output family present as `stream_complete` not `output_render_sample`; only the exact-equality case in 20 journals fires → **C-D22 candidate**, row unchanged) · K00-05 rows 10/10 PASS-05 (evidence only, K00-05 CLOSED) · **§7.3 reading: processor-dependent suppression STRONGLY SUPPORTED; B (capture collapse below the VP unit) NOT SUPPORTED by S1; population not mixed**; independent near-end availability at the seam remains S2's question. Authority SPENT. Returned for ruling: S1 acceptance · C-D22 · transport deviation · whether the read-only Mac playback-capability census opens. S2 implementation HELD · S3 not open.** C-D23 gate maintenance (C-D19/C-D21 shape; the gate STOPPED the commit on red this time): `K00-0506-VPOFF-20260915T012817Z` named as its own VPIO-02 population, excluded from engine-era; 65/65 read alone; founder acceptance owed; a structural `subject=`-keyed partition is a candidate ruling. ⭐ **ADJUDICATED (founder; discriminator plan §10 / qualification plan §10.16): S1 ACCEPTED — processor-dependent suppression strongly supported, VP-independent capture collapse not supported; A (own playback removed, near-end survives) vs A′ (whole near-end suppressed while MAIA speaks) UNRESOLVED = S2's question; no K00-06 PASS, K00-05 untouched · transport deviation ACCEPTED (custody intact, not a general licence to split pinned transactions) · **C-D22 ACCEPTED + correction DONE offline** (reader: a full window ending at the exact ms of its `stream_complete` has its output family in that record; synthetic boundary case FAIL-06 on `8b111709b` → PASS-06 now; self-test 35/35; corpus replay: VP-ON 40/40 identical, S1 exactly row 1 FAIL-06 → PASS-06, F-W1 unchanged; produced ledger untouched, successor recorded beside it; gate 66/66; no device act) · **C-D23 ACCEPTED; explicit enumeration at its limit → RULED: corpus membership must become structural; a read-only corpus-partition CENSUS is OWED before S2 evidence lands** (implementation not yet authorized) · **Mac playback-capability CENSUS OPEN, read-only** (no sound/file/volume/device change; `afplay` a candidate until the Mac shows it) · S2 implementation HELD behind census → batch-only design → gate → SHA → authority · S3 not open.** ⭐ **CORPUS-PARTITION CENSUS RETURNED, read-only (`KERNEL-00_CORPUS_PARTITION_CENSUS_2026-09-15.md`; discriminator plan §11):** every population carries two provenance carriers — H = ledger header `subject=` (declared) and C = the journal's start-trace signature (physical); H ∧ C never disagree across all 548 tracked journals; P-H and P-C each reproduce the current four-name gate buckets set-for-set (vpio-02 50 · vpio-01 30 · engine 468); recommended structural rule = H ∧ C agreement for membership + the classifier reproducing each population's produced ledger class column under its header subject (replaces per-population naming); implementation NOT authorized, returned for ruling. ⭐ **MAC PLAYBACK-CAPABILITY PROBE PINNED (`scripts/witness/k00-playback-probe.sh`; §12), NOT RUN:** help/man/`system_profiler SPAudioDataType`/`get volume settings` captures only, stdin closed everywhere, `say` never executed, no audio file/volume/device change, no device verb; manifest + seal; gate 67/67; paste-able invocation pinned (fresh worktree at the branch tip → `k00-playback-probe.sh` → return `playback-probe-<stamp>/`). S2 design is written only from what the Mac shows.** ⭐ **PROBE RETURNED + READ (`playback-probe-20260915T020250Z`, seal `fc14fd50…` verified here, 37 files, `soundPlayed:false`; discriminator plan §13): `afplay` PRESENT (`-v` · `-t`; plays to the *default audio output*), `say`/`ffplay`/`ffmpeg`/`sox`/`play` present, `mpv`/`SwitchAudioSource` ABSENT; **Default Output Device = `B06Ultra` (Bluetooth)**, Default *System* Output = Mac Studio Speakers; volume 25, unmuted → S2 player ESTABLISHED, **S2 physical output source UNRESOLVED** (founder favours preparing Mac Studio Speakers as default by a separate act — NOT authorized). ⭐ **CORPUS-PARTITION RULE RATIFIED (founder) → STRUCTURAL PARTITION IMPLEMENTED, gate-only:** membership = H (nearest `ledger.md` `subject=`) ∧ C (the journal's own start-trace signature), disagreement fails closed naming file · header · signature (proven offline, restored); sets exactly vpio-02 50 · vpio-01 30 · engine 468 (headerless archive 154); historical pins kept as counts (30 × degradation · 49 + 1); cross-subject both directions; **produced-ledger reproduction row-for-row over all 15 ledgered directories under the header subject** (not-a-sample never in a row; the one flood-shaped ledger detected by its duplicate rows, its sole delta = the C-D6 `K00-faf8fa3e` refusal, pinned); no directory name in the test; C-D19/C-D21/C-D23 species retired. Gate 67/67. S2 implementation HELD · S2 witness NOT AUTHORIZED · S3 not open.** ⭐ **FOUNDER RULING — S2 PHYSICAL SOURCE = MAC STUDIO SPEAKERS (RULED) · OUTPUT-SOURCE PREPARATION ACT OPEN (discriminator plan §14):** a founder-performed macOS UI act (before-read `system_profiler SPAudioDataType -json` + volume read → Control Center → Sound → Output → Mac Studio Speakers → after-read + the pinned parser, verified here against the probe JSON: keys `coreaudio_default_audio_output_device` / `coreaudio_device_transport` exist, before-state prints `DEFAULT_OUTPUT B06Ultra coreaudio_device_type_bluetooth 44100`); exactly one selection; NOT authorized: sound, volume, unpairing B06Ultra, installing any utility, input/sample-rate/iPhone change, S2; PASS = exactly one `DEFAULT_OUTPUT` line · `Mac Studio Speakers` · `coreaudio_device_type_builtin` · `MAC_STUDIO_DEFAULT_OUTPUT True`, volumes custody only, else STOP; evidence returns on `feature/*` → `driver-ledger/s2-output-source-<stamp>/`. S2 design direction FIXED, not live (`/usr/bin/afplay` · Mac Studio Speakers · SHA-pinned steady stimulus ≠ 440 Hz · batch-only · per-sample custody: stimulus SHA · afplay identity · default-output identity before invocation · PID · start/stop epochs · PID-alive proof · exit status; phone-side validity = `signal` in ≥2 healthy pre-output baseline windows else UNMEASURED, no amplitude threshold). A PASS authorizes nothing downstream: S2 design act · implementation HELD · S2 witness NOT AUTHORIZED · S3 NOT OPEN · KERNEL-00 NOT ACCEPTED.** ⭐ **PREPARATION ACT BLOCKED, NOT FAILED (founder, §14.6–14.7): before-read CAPTURED `20260915T022305Z`; the Control Center gesture is a physical act no session may substitute (no AppleScript/CoreAudio/selector); after-read NOT AUTHORIZED YET · seal NOT CREATED; lane PAUSED — path 1 = a hand at the Mac Studio completes the gesture + after-read under the same stamp; path 2 = a new founder ruling authorizing a different mechanism as a new act. Nothing further runs in S2.** ⭐⭐ **PREPARATION EXECUTED (founder's hand) → EVIDENCE RETURNED (`feature/k00-s2-output-source-evidence-20260915` `8a3ca8fda`, cherry-picked) → VERIFIED HERE → PASS (§14.9):** four hashes recomputed = seal; the §14.2 parser run here reads before `DEFAULT_OUTPUT B06Ultra coreaudio_device_type_bluetooth 44100` and after **`DEFAULT_OUTPUT Mac Studio Speakers coreaudio_device_type_builtin 48000` (exactly one line), `MAC_STUDIO_DEFAULT_OUTPUT True`, B06Ultra present and no longer default**; default input and system output unchanged. Custody only, not absorbed: output volume 25→69 (no volume command issued; per-device state is the ruling's interpretation); a Continuity iPhone-microphone input entry present before, absent after. `S2 output-source preparation PASS · Mac Studio Speakers default output confirmed at 20260915T022305Z` — evidence at a time; the S2 batch re-reads the device before each population. **Next boundary = the founder's separate S2 batch-only design authority (NOT opened); S2 implementation HELD · S2 witness NOT AUTHORIZED · S3 NOT OPEN · KERNEL-00 NOT ACCEPTED.** ⭐⭐ **S2 BATCH-ONLY DESIGN/IMPLEMENTATION OPENED (founder ruling) → LANDED at `b198e2e37058f2e059d986b4b148e224215f3ee3`, gate 67→75/75, NOTHING PLAYED (discriminator plan §15):** tracked fixture `scripts/witness/fixtures/k00-s2-nearend-997hz-180s.wav` (997 Hz · 180.000 s · mono · 48 kHz · PCM16 · peak 0.20 FS · 8 640 000 frames; **SHA `1a505b3d38a97b75cb935f85bd33deb889628322e558f74af9a5f05afbfbd00e`** in sidecar · batch · gate; the gate parses the header, counts frames, checks the ±6553 peak, 997 Hz crossings and 1-LSB sine match, recomputes the SHA) · `/usr/bin/afplay -v 0.50 -t 180 "$S2_STIMULUS"` verbatim, once, inside `stimulus_start`; binary SHA `88f3b577…` = census, a batch precondition · read-only preflight before sample 1 (fixture → SHA → format → exactly one default output Mac Studio Speakers/`coreaudio_device_type_builtin` → `output volume:69` · `muted:false` → afplay executable + SHA), any mismatch STOP exit 8, no set-volume/SwitchAudioSource/blueutil/defaults/sudo anywhere · per sample: cold precondition → one child → PID/epoch → 1 s → alive/non-zombie (else infra row + exit 9) → 1 s liveness monitor → `run_test` → post-run state → `kill -TERM`/`wait` → exit status → `custody VALID|INVALID` in `stimulus-sample-N.tsv` (INVALID = row UNMEASURED for the discriminator; readers never see it) · `--stimulus s2-nearend` the only token (no path), lawful only with `--act output --vp on --mode L --subject vpio-02`, refused before the device lock · every historical batch line preserved verbatim in order (gate-proven), readers/reinstall/driver/organism byte-frozen · offline shims exercised all five refusals and the device/volume/afplay-absent STOPs (no player in this container). Future invocation shape (NOT an authority): `k00-driver-batch.sh K00-0506-S2 10 --act output --cancel-at 1000 --settle 2 --vp on --mode L --subject vpio-02 --stimulus s2-nearend`. **Playback NOT AUTHORIZED · S2 population NOT AUTHORIZED (separate founder witness authority after reading the two SHAs) · S3 NOT OPEN · KERNEL-00 NOT ACCEPTED.** ⭐ **`S2-WITNESS-01` GRANTED, BOUNDED (founder; discriminator plan §16): ONE witness of the orchestration on the real Mac against exactly `b198e2e37` — Block A custody (fresh worktree · clean tree · gate 75/75 · fixture SHA · afplay SHA · no pre-existing afplay) → Block B read-only device preflight → Block C exactly one invocation `k00-driver-batch.sh S2-WITNESS-01 1 --act output --cancel-at 1000 --settle 2 --vp on --mode L --subject vpio-02 --stimulus s2-nearend` (playback permitted ONLY as this witness's instrument; the batch's stimulus preflight STOPs inside it) → Block D read-only post-read + seal; PASS/STOP law predeclared (§16.4: preflight PASS · complete `stimulus-sample-1.tsv` · all-alive liveness spanning run_test · `custody VALID` · stopped before the 180 s failsafe · no afplay before/after · one row + one journal · rc 0); the sample-1 journal is never an S2 row and gets no §7 reading. Fail-closed: any STOP = evidence, no repair/rerun under this authority. PASS ≠ S2 population · ≠ S3 · ≠ KERNEL-00. Mac act, NOT YET EXECUTED; evidence owed on `feature/*`.** ⭐ **`S2-WITNESS-01` EXECUTED → STOP (evidence `3cbec915b` → cherry-pick `706dc4cf2`; discriminator plan §17):** Blocks A+B PASSED every line on the real Mac (HEAD exact · clean · gate 75/75 · fixture + `/usr/bin/afplay` SHAs = pins · no afplay before · surface diff 0 · `E3B88028` present · harness 0); Block C: driver built, batch stimulus preflight PASSED fixture SHA · exact WAV format · **exactly one default output = Mac Studio Speakers/builtin**, then **STOPPED on `output volume:31` ≠ ruled 69 → exit 8, nothing played, nothing sampled, no journal**; Block D not run; no repair/rerun; §16.7 transport worked as pinned. **Adjudication §16.4: STOP · witness NOT PASS · authority SPENT · fail-closed branch witnessed once on the real Mac.** Volume drift 69 (02:23Z, preparation after-read) → 31 (12:28Z) with no governed act touching it: cause UNKNOWN, not inferred. Returned for ruling: keep 69 (needs a founder hand re-preparation act shaped like §14) vs re-rule the precondition (new orchestration SHA; the batch never sets volume) · read-only drift characterisation · whether `S2-WITNESS-02` is issued. **S2 population · S3 · KERNEL-00 acceptance CLOSED.** ⭐ **RULED (founder; §18): 69 STAYS — *31 ≠ evidence that 69 was wrong; 31 = evidence that the prepared state did not persist*; the refusal earned confidence in the boundary, not permission to move it. `S2-VOLUME-DRIFT-01` OPEN, read-only:** repository half DONE (no volume-mutation code in scripts/ios/tests; osascript = the two governed reads + one unrelated notification; readings 25 → 25 → 69 → 31; device state property-identical 02:23Z ↔ 12:28Z, only the scalar moved) · Mac half = `scripts/witness/k00-volume-drift-census.sh` (`302b7bc47`, gate 76/76; reads volume · audio/Bluetooth JSON · boot/sleep-wake history · audio preference domains (discover → `defaults read`) · process list · one default-level `log show` window kept OFF-repo and hashed; manifest + seal; never sets/selects/plays/signals/writes/touches the phone or batch), invocation pinned in §18.3 with `K00_DRIFT_LOG_LAST` sized to reach back past 02:23Z, NOT RUN; outcome may be cause found / candidate mechanism / cause unknown. Restoration of 69 = founder read → hand → read act, separate, not part of the census; `S2-WITNESS-02` NOT ISSUED (its own act after census → restoration → post-hand read; `b198e2e37` reusable if the batch is unchanged). ⭐ **`S2-VOLUME-DRIFT-01` CENSUS EXECUTED (founder, `volume-drift-20260915T125109Z`, evidence `7447a6b1d` cherry-picked `edb138f70`, seal + 36 hashes verified here; discriminator plan §18.4 / qualification plan §10.25) → READ: CANDIDATE MECHANISM.** Volume still 31 at 12:51Z, Mac Studio Speakers still default, no reboot/sleep; retained coreaudio log begins only 07:08 local (the 02:23Z preparation act = coverage gap, not evidence). A System Settings → Sound session opened 07:25:23 local (11:25Z; still open at capture): **default output device changed at 07:25:48 local** (HAL notification ×2 clients · ControlCenter target id `4e5 → 7f` · coreaudiod's 18 h B06Ultra output context turned off) — same device name after, re-select vs re-create undeterminable; then **14 `coreaudiod LogVolumeChangeForServerSideControl` writes (control id 138) 07:32–07:56 local**, each coincident with the Sound pane's `playVolumeKeyFeedback` (34 firings to 08:46; last before the 08:28 witness 08:21:53); values `<private>`, no line writes 31 or names 69, no other volume-writing path; actor a person-shaped desktop session, NOT inferred. **Ruling unchanged: 69 STAYS · restoration = founder read → hand → read, NOT YET PERFORMED · `S2-WITNESS-02` NOT ISSUED · conduct note only (restore minutes before invocation, Sound pane closed; the batch preflight already refuses drift).** ⭐ **RULED (founder; discriminator plan §18.5 / qualification plan §10.26): `S2-VOLUME-DRIFT-01` CLOSED · candidate mechanism (accepted as read) · `S2-VOLUME-RESTORE-01` AUTHORIZED — founder read → hand → read shaped by §14 (Sound pane closed first · settle · machine READ · hand SETS 69 · machine READ · timestamps + both reads preserved · evidence on `feature/*`); NOT authorized: instrument sets volume · playback · `run_test` · phone · S2 sample · `S2-WITNESS-02` · batch modification; acceptance exactly `Mac Studio Speakers` · `builtin` · `69` · `muted false`, else STOP, no further adjusting under the same act; sequence pinned §18.5.2 (§14.2 parser + `OUTPUT_VOLUME`/`OUTPUT_MUTED`/`RESTORE_ACCEPTANCE`), evidence → `driver-ledger/s2-volume-restore-<stamp>/`; NOT YET EXECUTED. A PASS does not authorize `S2-WITNESS-02` (own authority after the evidence returns); `b198e2e37` reusable if the batch is byte-unchanged.** ⭐ **`S2-VOLUME-RESTORE-01` EXECUTED → STOP (discriminator plan §18.6 / qualification plan §10.27; evidence `47ba68d30` cherry-picked `a8dad7942`, 9/9 hashes = seal = founder's list, parser reproduced here):** first attempt `132000Z` blocked at the hand boundary (no GUI in either session; automation refused; kept as custody) → fresh stamp `20260915T134328Z`: pane closed `rc=1` · before 44 · one hand act via the Sound slider · after **73**, pane open (new instance, explained by the act), device state byte-identical → **`RESTORE_ACCEPTANCE STOP` (73 ≠ 69) · authority SPENT**; a later post-act adjustment founder-disclosed, unread → current volume UNKNOWN to the record, leave it alone. Flagged inference only: 25/31/44/69 are sixteenth-step key values (11/16 → 69), 73 is not — a stepped-control hand act may hit the exact reported integer the preflight reads; band vs exact = founder decision (band = new batch SHA). **Restoration re-open = fresh founder authority (NOT issued) · `S2-WITNESS-02` NOT ISSUED.** ⭐ **RULED (founder; discriminator plan §18.7 / qualification plan §10.28): 69 stays EXACT · band NOT ADOPTED · `b198e2e37` unchanged · the sixteenth-step inference justifies a different human control, not a new target → `S2-VOLUME-RESTORE-02` AUTHORIZED as a fresh act (RESTORE-01 STOP · spent): Sound pane closed by hand → settle → fresh Step 0/1 under a new stamp (volume UNKNOWN until read) → the ONE hand act = keyboard Volume Down to zero, Volume Up exactly 11 times, stop → after-read + unchanged parser; no slider/scripted keys/`osascript`/device selection/test playback/twelfth press/post-read correction/retry; PASS = unchanged §18.5.3 with `OUTPUT_VOLUME 69`, else STOP and rule again; sequence pinned §18.7.2, evidence → `driver-ledger/s2-volume-restore-02-<stamp>/`; NOT YET EXECUTED. `S2-WITNESS-02` NOT ISSUED.** ⭐⭐ **`S2-VOLUME-RESTORE-02` EXECUTED → PASS (discriminator plan §18.8 / qualification plan §10.29; evidence `988628e0d` cherry-picked `8b8c8f299`, 9/9 hashes = seal = founder's list, parser reproduced here):** pane closed at both reads · before 13:59:19Z volume 65 (off-grid, custody) · one hand act = keyboard Volume Down to zero + exactly eleven Up presses · after 14:05:42Z **69** · device state byte-identical → **`RESTORE_ACCEPTANCE PASS` · authority SPENT · 69 RESTORED at 14:05:42Z**. Stepped-control mechanism supported by one observation (n = 1; eleven sixteenth-steps → the exact reported integer the preflight compares; slider → 73); procedure of record, reproducibility unmeasured; `b198e2e37` unchanged, target exact, band not adopted. **`S2-WITNESS-02` NOT ISSUED (own founder authority; §16 shape is the template).** ⭐ **`S2-WITNESS-02` ISSUED (founder; discriminator plan §18.9 / qualification plan §10.30: one act on unchanged `b198e2e37` · §16 Blocks A–D with three literal substitutions (run label, transcript name, worktree `k0506-s2w2-b198e2e37`) · §16.4 law unchanged · conduct rule: Block C by 14:35:42Z, pane closed, volume/device untouched, fresh Step 0 before C; pinned `24f111302` 14:15:48Z) → LAPSED UNSPENT (§18.10): conduct-rule clock 14:52:19Z; Blocks A–D not executed, no batch/ledger/S2 row, nothing touched, no later clock substituted; not STOP, not failure; the §18.9.2 pin stays valid text; window anchoring (restoration read vs a fresh pre-C read, or pairing restoration + witness) = founder's call. 69 last read 14:05:42Z. Next move = a fresh issuance/custody act.** ⭐ **WITNESS-02 residue READ (§18.10.2–§18.11.1): the interrupted lane ran read-only Blocks A (14:18Z) + B (preflight `141935Z`) under the live issuance and died before Block C — no transcript/ledger/`blockC.out`, no afplay/batch process at 14:55:27Z → batch never started, nothing played, no S2 row; §18.10 corrected beside the original. FOUNDER RULING → `S2-WITNESS-03` AUTHORIZED (§18.11.2–§18.11.3 / qualification plan §10.31): residue preserved (never removed/reused/executed) · new path token `k0506-s2w3-b198e2e37` + `s2w3-block*` · freshness = a fresh governed volume read immediately before Block C (14:05:42Z historical only; no read, no Block C; batch preflight still refuses drift) · §18.9.2 reused with exactly the run/path-token substitutions, diff-proven · pre-C conduct: volume read · `date -u` · `pgrep` · no process · `output volume:69`/`muted:false`; `READ OK` = precondition, never trigger; NOT YET EXECUTED.** ⭐ **`S2-WITNESS-03` EXECUTED → STOP BEFORE BLOCK C (evidence `e85a39dae` → cherry-pick `05bbccc80`, seal 11/11 verified here; discriminator plan §18.12 / qualification plan §10.32):** fresh path token (pre-existence read: absent) · substitutions diff-proven (three tokens only) · block hashes = founder's · **Block A PASS + Block B PASS** on the real Mac (second clean A/B on `b198e2e37`; `E3B88028` ×1 · harness 0) · **pre-Block-C read 15:05:51Z `output volume:38` ≠ 69, pane closed `[pgrep rc=1]` → STOP as pinned**; Block C/D NOT RUN · nothing played · no transcript/ledger/S2 row by design · authority SPENT. Second drift 69 (14:05:42Z) → 38 (15:05:51Z) with no governed volume act between and the pane closed at both ends: **cause/actor UNKNOWN, not inferred** (38 on the sixteenth-step grid — inference only); founder attestation verbatim in §18.12.3. Three fail-closed boundaries now each witnessed refusing on drift (batch preflight · restoration parser · pre-C read); *the refusal earned confidence in the boundary, not permission to move it*. Returned (nothing opened): STOP acceptance · drift disposition (bounded 14:05–15:06Z census vs re-prepare-immediately-before-use) · whether a paired `S2-VOLUME-RESTORE-03` + `S2-WITNESS-04` issuance is made (minutes-scale window, `s2w4` token, WITNESS-02/03 residue preserved). ⭐ **RULED (founder; discriminator plan §18.13 / qualification plan §10.33): `S2-WITNESS-03` ACCEPTED as adjudicated (STOP · spent) · 69 UNCHANGED · second drift census ⛔ NOT OPENED (*output volume is volatile state on this Mac* — the operational fact; cause of 69 → 38 stays UNKNOWN, no mechanism required for progress) · **`S2-RESTORE/WITNESS-04` AUTHORIZED AS ONE PAIRED CUSTODY**: all slow preparation before the hand — Block A → Block B → pane closed / no competing process → RESTORE-03 before-read → the one §18.7.2 keyboard act (Down to zero · Up ×11) → **immediate governed after-read = the joint** (§18.5.2 parser Mac Studio Speakers · builtin · 69 · unmuted · `RESTORE_ACCEPTANCE PASS` + pane `rc=1`) → PASS: Block C is the very next act, Block D on rc 0 · FAIL or interruption after the read: STOP, no correction/retry, spent. **Boundary ordinal, not a clock** — no seal/commit/push/note/deliberation between the accepted joint read and Block C. Token `s2w4` · `b198e2e37` unchanged · WITNESS-02/03 residue preserved · pinned §18.13.3 · NOT YET EXECUTED (founder hand inside it) · evidence → `s2-restore-witness-04-<stamp>/` + §16.7 return → §18.14. ⭐ **`S2-RESTORE/WITNESS-04` EXECUTED → STOP IN BLOCK B (evidence `83b7fe3d2` → cherry-pick `f33bff911`, seal 7/7 verified here; discriminator plan §18.14 / qualification plan §10.34):** Stage 0 clean (`s2w4` path absent · diffs = the three tokens only · block hashes = founder's) · **Block A PASS** (third clean pass on `b198e2e37`) · **Block B STOP on its own fail-closed line** — `PREFLIGHT_DIFF_RC=0` · `OTHER_BATCHES=0` → apps read `com.apple.dt.CoreDeviceError 4000 · The device disconnected immediately after connecting` · `APPS_READ_RC=1` · `STOP: apps listing unreadable`. RESTORE-03 NOT STARTED · hand act NOT REACHED · joint NOT REACHED · Block C/D NOT RUN · playback NONE · S2 row NONE · authority SPENT · no volume operation of any kind (69 untested by this act; last governed read 38 at 15:05:51Z). Device-preflight transport failure at the Mac ↔ iPhone seam; **cause NOT INFERRED** (founder and record); infrastructure, not sample, not physiology; no repair/rerun under the spent authority. Returned (nothing opened): STOP acceptance · fresh paired issuance (`s2w5`, §18.13.3 pin with the token substituted, `b198e2e37` unchanged) vs a read-only device-availability act first (named, not proposed). ⭐ **RULED (founder; discriminator plan §18.15 / qualification plan §10.35): `S2-RESTORE/WITNESS-04` ACCEPTED as adjudicated (STOP in Block B · spent · RESTORE-03 never entered · ordinal boundary unexercised) · separate device-availability act ⛔ NOT OPENED (Block B *is* the availability gate, at the last responsible moment before the hand; *a transient failure of a lawful boundary is reason to approach that boundary again under fresh authority, not reason to route around it*) · **`S2-RESTORE/WITNESS-05` AUTHORIZED AS ONE PAIRED CUSTODY** = §18.13.3 unchanged except the fresh tokens (`04→05` · `s2w4→s2w5` · path `k0506-s2w5-b198e2e37`), blocks re-extracted from `17b4df63b` and substitution-proved; same stages · joint condition · failure law · `b198e2e37` unchanged · 69 exact · WITNESS-02/03/04 residue preserved · pinned §18.15.2 · NOT YET EXECUTED (founder hand inside it) · evidence → `s2-restore-witness-05-<stamp>/` + §16.7 return → §18.16. ⭐⭐ **`S2-RESTORE/WITNESS-05` EXECUTED → JOINT PASS → BLOCK C PASS → BLOCK D COMPLETED (evidence `36bfde4e6` → cherry-pick `041b892c0`, witness seal 12/12 verified here; discriminator plan §18.16 / qualification plan §10.36):** Stages 0–3 PASS (Block B passed this time) · RESTORE-03 before 38 (15:46:26Z) → one keyboard act → **joint 15:49:23Z = 69 · unmuted · Mac Studio Speakers · builtin · `RESTORE_ACCEPTANCE PASS` · pane rc=1** (founder-attested; the nine restoration files + 9/9 seal NOT in custody here — the remote safety layer refused post-execution packaging writes, not bypassed, nothing rerun → custody OWED) · ordinal joint HONOURED (Block C the very next act) · **witness half §16.4 PASS on every committed line**: batch stimulus preflight PASS at 15:49:59Z (fixture + afplay SHAs = pins · format EXACT · one default output builtin · **volume 69**) · `stimulus-sample-1.tsv` complete (pid 94754 · 67 liveness rows all alive · 69 s · no gap > 2 s) · `custody VALID` · stopped at 71 s by the batch's TERM · one row + one journal · rc 0 · afplay 0 before/after (relayed) · classifier + output reader reproduced byte-for-byte (`K00-fb8793df` gen-1 listen, 14-step, cold, listening 397 ms; K00-05 PASS-05 ×2; K00-06 UNMEASURED-06 — sample 1 never an S2 row, no §7 reading). **First complete passage of the S2 orchestration on the real Mac.** Authority SPENT. Returned (nothing opened): acceptance · restoration-half custody by a lawful packaging path · whether the S2 population is issued (separate authority). C-D24 gate maintenance (count pins 548→549 · vpio-02 50→51 · directories 15→16; rule unchanged; gate read red on the cherry-pick, green after, before any push; founder acceptance owed). ⭐ **RULED (founder; discriminator plan §18.17 / qualification plan §10.37): WITNESS-05 ACCEPTED as read (custody incompleteness, not a defect) · restoration standing = repository-verifiable (batch preflight 69 at 15:49:59Z) + founder-attested/local (RESTORE-03 captures, 9/9 at the Mac), upgradable only by custody transfer, never called rerun · **`S2-WITNESS-05-CUSTODY-COMPLETE-01` AUTHORIZED, custody only** (byte-for-byte copy of the eleven existing RESTORE-03 files into `driver-ledger/s2-restore-witness-05-20260915T154626Z/`; seal 9/9 before AND after copy else STOP; §18.5.2 parser re-executed on the copied captures as a derivation check, never a rerun; derived `JOINT.txt` self-identified; `s2w5-block[ABC].out` copied if present; `s2w5-blockD.out` NEVER manufactured, founder's note verbatim; no volume/device/playback/batch/sample/read act; `b198e2e37` untouched) — pinned §18.17.2, NOT YET EXECUTED · C-D24 ACCEPTED (cardinality-vs-integrity design question deferred) · **S2 population NOT YET ISSUED** — after the carrier returns and the seal recomputes here, an explicit issuance ruling; authority never automatic. *"Custody may complete the carrier; custody may not recreate the act."* ⭐ **CUSTODY-COMPLETE-01 EXECUTED → custody PASS · STOP AT THE COMMIT TRANSPORT (discriminator plan §18.18 / qualification plan §10.38):** seals 9/9 before and after · eleven files byte-identical · parser derivation identical · derived `JOINT.txt` · Block D note, nothing manufactured · `SHA256SUMS.custody` + `RETURN.txt` produced · no device/volume act; the governance pre-commit refused (no `node_modules` in the fresh worktree; hooks never download) and the lane rightly did not install/bypass/retry; the pin's missing `set -e` let the push create the remote feature branch at base `a5e874aa2` with no custody commit (residue, harmless) → carrier STAGED LOCALLY, unshipped · **C-D25 = this session's pin defect** (the §16 Block A `node_modules` link omitted; no `set -e`) · transport pin for the precedented link-then-commit shape written §18.18.4, valid only on a founder ruling; `npm ci` and hold named beside it; no new stamp/worktree/re-copy under any shape. ⭐⭐ **CUSTODY-COMPLETE-01 TRANSPORT COMPLETE (founder ruled shape 1: `node_modules` linked, staged carrier committed `34bdfbb91` → cherry-pick `e6af62b18`; discriminator plan §18.19 / qualification plan §10.39) → VERIFIED HERE:** `SHA256SUMS.custody` 22/22 · RESTORE-03 seal **9/9** · founder list = committed = recomputed · §18.5.2 parser re-executed on the copied captures = `parser.txt` = **`RESTORE_ACCEPTANCE PASS` (69 · unmuted · Mac Studio Speakers · builtin)**, before-captures 38 · STOP · 15:46:26Z → 15:49:23Z · pane rc=1 both · device JSON byte-identical across the act · `JOINT.txt` self-identified derived · Block-D note verbatim, nothing manufactured · **Stage-0 blocks reproduced from `17b4df63b` + the three substitutions = the custody-time hashes exactly** · Block A/B/C outputs now in custody (`AFPLAY_PROCESSES_BEFORE=0` · `BATCH_PIPELINE_RC=0`), Block D after-read stays attested · gate 76/76 → **WITNESS-05 restoration half REPOSITORY-VERIFIED PASS by custody transfer, never rerun; the whole paired act stands on committed evidence.** C-D26 candidate: `${PIPESTATUS[0]}` empty under zsh in every `.out` (record-only). The §18.17 item-6 precondition for an S2-population issuance is MET; **issuance = a separate explicit founder ruling, NOT opened.** **S2 population · S3 · KERNEL-00 acceptance CLOSED.** The question is now *what hidden runtime state makes the same governed startup sometimes live and sometimes fail* · protocol fields UNKNOWN · W4/VP-ON IN-FLIGHT OWED · 3a/3b/dd33d8f4 FILES NO LONGER OWED (founder-held, founder-hash-verified; not in repo) · B/C NOT CHOSEN · KERNEL-01 NOT OPEN · BENCH-01/BRIDGE-01/MIGRATE-01 CLOSED.- **LATEST — 2026-09-11 (latest) — `VOICE-2026` · ⭐ **E23 RECORDED** (census §3.4 overlay, founder-authorized): *registered-arm runtime witness — competing audio/recognition authority under Bluetooth HFP (Pi8).* Xcode console from the legacy `/maia` app on scheme `claude/ios-runtime-01-audiosession-registration`, iPhone iOS 18.7, Pi8 HFP both directions. **WITNESS-PROVEN:** registration active · Pi8 HFP route · native recognizer starts (playAndRecord/voiceChat, 16 kHz, on-device) · `x-tts-provider: openai` (ruling F1 live) · **the registered gatekeeper is witnessed as an active `AVAudioSession` writer on the live path** — before TTS it tears down, deactivates, sets `playback/spokenAudio`, reactivates (ruling **F2** / census §5 F3 — NOT ruling F3, which is output cancellability) · web SpeechRecognition lifecycle live in the WebView to generation 18 · ≥18 native start → "No speech detected" (0.6–1.8 s) → stop cycles in ≈25 s · voice-recorder permission path reached after cycle 18 · WebKit "beginInterruption but session is already interrupted" · first turn intact, 09-07 transcript seam committed by watchdog. **INFERRED (kept as inference):** which owner imposes the 2.875 ms IO buffer (alternates with the plugin's 0.01; not the plugin; does not reduce to a 128-frame quantum) · SCO reopen timing as mechanism · Bluetooth as amplifier vs origin. **Two §3.3 qualifications:** (1) the 45 s web inactivity guard did NOT end the interaction — cycle still alive at `speech: 99s`, predicate-unarmed vs continually-refreshed **unresolved, do not choose**; (2) "idle stop within N ms grace period — not counting as failure" fires at N = 1 ms → *the mechanism intended to suppress false failure counting can suppress the failure being measured* — preserved as a Voice 2026 anti-pattern (a recovery ceiling is never reset by the act it counts). **E19:** registered arm WITNESSED · STRONG POSITIVE; pre-registration arm UNSPENT · **DEFERRED** (historical causal increment only; not needed for the architecture; does not block KERNEL-00). ⛔ **LEGACY REPAIR FORBIDDEN / NONE.** **K00-11 consequence:** the Pi8 is a NAMED route — runbook step 9 = `built-in → Pi8 HFP → built-in → Pi8 again`; step 3 K00-06 duplex physiology also with output on the Pi8. The comparison: legacy+Pi8 (many owners · category flips · recognizer storm) vs VoiceKernel+Pi8 (one owner · persistent category · no recognizer · duplex observed). **NEXT SUBSTANTIVE ACT: KERNEL-00 DEVICE WITNESS** on `488e0666c` (still READY · UNSPENT). *This session cannot touch the phone: no Xcode/toolchain/USB in the remote container; a Claude Code session on the Mac Studio can install the signed artifact, stream the device console and pull the JSONL; the physical acts stay with the founder.*
- **PRIOR — 2026-09-11 — `VOICE-2026` · `KERNEL-00` · ⭐ `MAC-COMPILE-02` GREEN on exactly `488e0666c` · DEVICE WITNESS HOLD LIFTED · WITNESS READY · UNSPENT.** Founder record `docs/programme/VOICE-2026/KERNEL-00_MAC-COMPILE-02_2026-09-11.md` (824 lines, raw outputs, merged fast-forward from `feature/k00-second-compile-record-20260911` `e2f114289`): `swift build` PASS · `swift test` **17/17** · source gate **13/13** · `xcodegen generate` PASS · unsigned iOS compile PASS · **signed build against the paired iPhone PASS** (Soullab dev team, profile for `life.soullab.voicekernel.k00`, `BUILD SUCCEEDED`). `.allowBluetoothHFP` compiles at the iOS 16 deployment target against the iOS 26.2 SDK. One non-blocking warning, `result of 'try?' is unused` at the `leaveConversation` release — **deliberately not changed** (a fix would be a new subject requiring qualification). First signed attempt failed pre-compile on a destination-ID mismatch (`devicectl` id ≠ Xcode destination id) — operator evidence, Appendix A, not a kernel defect. Render tap + `.dataRendered` coexist at compile time; their behaviour under real rendering/cancel is what the witness establishes. **Standing:** SOURCE `488e0666c` · PRE-WITNESS-01 APPLIED · MAC-COMPILE-02 PASS · **HOLD LIFTED** · **WITNESS READY, UNSPENT** (founder stopped before install/launch) · ARCHITECTURE UNCHANGED · BENCH-01/BRIDGE-01/MIGRATE-01 CLOSED. ⭐ **Next bounded act is not code: install `488e0666c` on the iPhone and execute the KERNEL-00 device witness under the ratified runbook (`KERNEL-00_RUNBOOK_2026-09-11.md` §3, record per §4 → `KERNEL-00_WITNESS_<date>.md`), recording whatever the organism actually does.** Both outcomes legitimate; never loosen a threshold; a failure is evidence. *The first time Voice 2026 meets the body.*
- **PRIOR — 2026-09-11 — `VOICE-2026` · `KERNEL-00` · `MAC-COMPILE-01` RECORDED on `eef487422` · `PRE-WITNESS-01` EXECUTED (this SHA, NOT compiled) · DEVICE WITNESS STILL HOLD.** Founder ran the first Mac compile on exactly `eef487422` (record `docs/programme/VOICE-2026/KERNEL-00_MAC-COMPILE-01_2026-09-11.md`, merged from `feature/k00-mac-compile-record`): `swift build` PASS · `swift test` 1 FAIL (**C1** — the test asserted the 60 s recovery budget wrongly; sliding per-fault-class window is the law, test corrected, `RecoveryPolicy` untouched) · source gate PASS · `xcodegen` PASS · unsigned iOS compile 2 errors (**C2** — harness read actor-isolated `kernel.recorder`; now `VoiceKernel.exportJournalJSONL()` / `journalEvents()`, recorder private) · signed compile blocked behind C2 · **C3** `.allowBluetooth` deprecated → `.allowBluetoothHFP`. Native viability established. Founder then authorized PRE-WITNESS-01 = P1–P8 + C1–C3, applied on `claude/voice-2026-census-01`: **P1** law header → OPEN · **P2** K00-02 clarified in place (one member-caused `session_activated` + one `session_deactivated`; OS-forced re-activations lawful only stamped `interruption_recovery` / `media_services_reset_recovery`) · **P3** `AudioSessionAuthority.mutate()` journals every mutation with `outcome` (`session_category_set` · `…preferred_sample_rate_set` · `…preferred_io_buffer_set` · `session_activated` · `session_deactivated` · `session_output_override`) · **P4** per-second `input_health_sample` / `output_render_sample` · **P5** render tap on the player node → `stream_cancel_measured { cancelIssuedAtMs, lastNonSilentRenderedAtMs, cancelToSilenceMs, withinRatifiedWindow }`, `cancelLatencyMs` retired, K00-05 metric named in the law (threshold unchanged) · **P6** synthetic stall freezes the render-observation seam (`AudioGraph.setSyntheticStall`), records stamped `synthetic` · **P7** `overrideOutput(speaker:)` in the authority only (`overrideOutputAudioPort(.speaker|.none)`), harness Speaker / System default, `app_lifecycle` records, runbook step 11 = Settings → Developer → Reset Media Services (K00-13 exercisable) · **P8** `seq` on every record, `causeSeq` causal parent on every automatic act, `StateReplayer` fails on `brokenCausality`, K00-17 clarified. Source gate `__tests__/voice-kernel-00-source-gates.test.ts` 13/13 (now also pins P3/P5/P8/C2/C3 shapes). Plan `PRE-WITNESS-01_PLAN_2026-09-11.md` → EXECUTED; runbook + README §2c updated. ⛔ **Nothing in this SHA is compiled.** It earns itself independently on the Mac: `swift build` · `swift test` · gate · `xcodegen generate` · unsigned iOS compile · signed/device compile — recorded verbatim as a second compile record. **Only on all-green does the device witness HOLD lift.** Do not install and start the 60-minute witness because it compiles. Standing: SOURCE WRITTEN · MAC-COMPILE-01 RECORDED (eef487422) · PRE-WITNESS-01 APPLIED · NEW SHA UNCOMPILED · DEVICE WITNESS HOLD · ARCHITECTURE NOT REOPENED · thresholds untouched.
- **PRIOR — 2026-09-11 — `VOICE-2026` · `KERNEL-00` source at `eef487422`: ⭐ founder ruling — RUN `MAC-COMPILE-01` on exactly `eef487422` · HOLD the device witness · then bounded `PRE-WITNESS-01`.** *Architecturally on target; do not run the device witness yet.* Preserve the unmodified first-compile evidence (the commit says it was never compiled; changing it first erases that). Compile = native viability, not a meaningful witness while ratified falsifiers cannot be measured. **Eight bounded pre-witness defects (none architectural):** P1 acceptance-law header still says NOT OPEN · P2 K00-02 "exactly one activation/deactivation" contradicts lawful interruption/reset reactivation → clarify (entry + exit only; OS-forced reactivations permitted only when stamped `interruption_recovery` / `media_services_reset_recovery`) · P3 `configureForConversation()` emits one aggregate record for four mutations → journal each mutation + outcome · P4 K00-04 physiology not longitudinal in the journal → bounded aggregated `input_health_sample`/`output_render_sample` · P5 `cancelLatencyMs` times `player.stop()`, not cancel → last rendered frame → render-side tap records last non-zero frame, `cancelToSilenceMs` · P6 `stallOutput` suppresses the supervisor observation while `framesRendered` keeps advancing → move the synthetic stall to the render-progress seam, stamp synthetic · P7 no route override / lifecycle observation; reset wrongly "NOT EXERCISABLE" → `overrideOutput(.speaker|.none)` in the authority only (Apple: supported with playAndRecord; `.none` restores default), `app_lifecycle` journal, runbook: Settings → Developer → Reset Media Services · P8 replay accepts any cause string → `seq` + `causeSeq` causal parent, verified by replay. Boundaries: record coherence · witnessability · physiological + causal instrumentation · lawful harness controls; no architecture, no STT/TTS/MAIA, no threshold changes; `.allowBluetooth` deprecation left for the compile to establish. Plan: `docs/programme/VOICE-2026/PRE-WITNESS-01_PLAN_2026-09-11.md`. Standing: SOURCE WRITTEN · BOUNDARY GOOD · MAC COMPILE AUTHORIZED NOW · DEVICE WITNESS HOLD · ARCHITECTURE NOT REOPENED. ⛔ Swift sources untouched since `eef487422` by design.
- **PRIOR — 2026-09-11 — `VOICE-2026` · ⭐ `KERNEL-00` OPEN (founder act) — first code written, NOT compiled, NOT witnessed.** Founder act: *KERNEL-00 is OPEN under the ratified acceptance law; build only the physical conversational audio organism (`VoiceKernel`, `AudioSessionAuthority`, duplex native audio graph, `HealthSupervisor`, `StateProjection`, flight recorder, bounded native harness); no STT, no TTS, no Web audio, no canonical MAIA, no legacy voice components, no network egress; nothing beyond the ratified K00 obligations is authorized; BENCH-01 / BRIDGE-01 / MIGRATE-01 closed; no helpful additions; no threshold tuning; a failure is evidence; E19/E20 do not block.* **Built on branch `claude/voice-2026-census-01`:** Swift package `ios/VoiceKernel/` (`VoiceKernel` actor · `AudioSessionAuthority` — the ONLY `AVAudioSession` mutator, iOS-only, journals every mutation, observes interruption/route/reset · `AudioGraph` — one `AVAudioEngine` per generation, voice processing on, input tap → generation-stamped observations, `AVAudioPlayerNode` output with `OutputStreamID` + `cancel(id)` · `HealthSupervisor` — pure, ratified windows 1500/2000/1000/100 ms, three-way energy classification · `RecoveryPolicy` — 3/fault/60 s at 500/1000/2000 ms, monotonic generations · `StateProjection` + `KernelSnapshot` (`displaysListening` = floor listening AND input healthy; no `isListening` anywhere) · `FlightRecorder` JSONL + `StateReplayer` for K00-17 · fault injection: digital-zero, stall output, hold-stale-callback, persistent) + pure-logic XCTests (`swift test` on macOS) + **separate harness app** `ios/VoiceKernelHarness/` (XcodeGen `project.yml`, bundle `life.soullab.voicekernel.k00`, SwiftUI reducer over snapshots, tone/cancel/faults/witness counters/journal export) + repo source gate `__tests__/voice-kernel-00-source-gates.test.ts` (K00-01 one session mutator · K00-16 nothing else in the build · VOICE-06/07/10/15/16 shapes). Decisions inside the law: `.playAndRecord/.voiceChat/[defaultToSpeaker, allowBluetooth, allowBluetoothA2DP]`, 48 kHz/10 ms, no `.mixWithOthers`; route change → rebuild under new generation, session untouched; interruption → health suspended, resume by re-activation + rebuild; K00-14 policy HOLD (`UIBackgroundModes: audio`); one stream at a time; cancel = node stop (no fade in K00). ⛔ **No Swift toolchain here — nothing compiled; the first Mac build is the first evidence.** Runbook + witness template: `docs/programme/VOICE-2026/KERNEL-00_RUNBOOK_2026-09-11.md` (build: `swift build && swift test`, `xcodegen generate`, Xcode Run with console; steps per K00-01…18; both outcomes legitimate; never loosen a threshold).
- **PRIOR — 2026-09-11 — `VOICE-2026` · ⭐ `ARCH-01` RATIFIED (founder act on `e3190db16`, three narrow amendments recorded in place) · KERNEL-00 was not yet open at that point.** `ARCH-01/00_README.md` §2b is the act of record. **D1–D10 RATIFIED AS A SET** ("not independent suggestions to be selectively weakened during implementation"). **VOICE-01…20 RATIFIED** with (a) VOICE-14 gate amended — *a turn may not be committed solely because a silence duration elapsed; a non-silence completion signal or explicit member gesture must participate in the causal basis* (the "≥ 2 evidence kinds" wording withdrawn so a single strong acoustic/semantic turn model is not rejected by fiat); (b) VOICE-18/policy retention amended — "last partial only for continuity" withdrawn as the E16.1 mistake; *partials and segments are ephemeral; the human turn must survive them* (minimum turn-scoped continuity state, never persisted beyond the turn unless canonical memory law permits the committed text). **Provider law RATIFIED**: *until an allowed adapter qualifies through BENCH-01, degraded voice with intact text continuity is the lawful production posture; availability does not outrank sovereignty; a speech failure may reduce modality, never silently change custody* — a Voice 2026-governed path may not keep OpenAI TTS merely because BENCH-01 is unfinished. **K00 thresholds RATIFIED ×6** (1500 · 2000 · 1000 · 100 ms · 3/60 s at 500/1000/2000 · 60 min/≥50 cycles) as ceilings that *may not be loosened because an implementation misses them*; **K00-06 AMENDED** to duplex physiology (echo coupling measured per route; quantitative AEC gate moves to KERNEL-01/BENCH-01 — the harness-declared tolerance was circular); **K00-11 clarified** (every Bluetooth topology the OS admits; unsupported = platform capability; once admitted, *manual tap after route change = FAIL*). Standing: CENSUS-01 CLOSED · F1–F4 RULED · D1–D10 RATIFIED · constitution RATIFIED · migration dispositions RATIFIED · provider law RATIFIED · ARCH-01 RATIFIED · **KERNEL-00 NOT YET OPEN** — *ratifying what shall be built is not the same act as authorizing someone to start building it.* Next founder act, verbatim: **"ARCH-01 stands ratified. Open KERNEL-00 under the ratified acceptance law. No STT, no TTS, no Web audio, no MAIA, no legacy voice components. Prove the physical organism first."** JARVIS boundary: inquiry → assembly → differentiation → synthesis → ratification complete; next is bounded execution, not ideation.
- **PRIOR — 2026-09-11 (later) — `VOICE-2026` · `ARCH-01` DRAFTED FOR RATIFICATION; founder acts F1–F4 + dispositions RULED; research package preserved.** `docs/programme/VOICE-2026/ARCH-01/00–06`: **Constitution VOICE-01…20** (each with evidence · falsifier · gate; 01–18 from the founder's research package `research/MAIA_Voice_2026_Architecture_Research_v1.docx`, 19–20 from rulings) · **target authority graph** (one owner per power; *adapters possess evidence, never authority*; *the kernel owns the encounter, models interpret it*) · **orthogonal state model** (floor · turn custody · output streams · physical health · recovery generation; no `isListening` boolean; no model event masquerades as state) · **migration boundary** (KEEP/ADAPT/REPLACE/REMOVE — **RATIFIED** with the refinement that recognizers/TTS/VAD/turn models/diagnostics/transport are ADAPT: instruments, not authorities) · **provider & sovereignty policy** (`VoiceProviderPolicy`; `thirdPartyCloud` prohibited on the voice path; fail-closed to *degraded voice, text continuity*) · **KERNEL-00 acceptance law** (K00-01…18, thresholds PROPOSED, fault matrix, FR-14 pass rule). ⭐ **RULED (founder): F1** *speech transport and model routing must be explicit; a local or sovereign speech failure may never silently widen into a cloud provider* — the `/maia` cloud-TTS default is unlawful for the future architecture and `MIGRATE-01` cannot complete while it stands (legacy not patched) · **F2** *no feature may independently acquire conversational microphone custody; capture is granted only through the single voice authority* — the gatekeeper registration is NOT to be adopted as an isolated repair (it enables a second custody actor via the composer mic); E19 A/B still runs as historical control · **F3** *every emitted audio stream must have an identity and a cancellable lifetime owned by the voice runtime* (`play()` without a durable handle is impossible) · **F4** the March `MAIABridgeViewController` revert is evidence, not constraint. Synthesis question rephrased: *what is the smallest sovereign conversational runtime that can own one continuous human–MAIA encounter while allowing hearing, turn understanding, cognition and speaking technologies to change independently?* E19/E20 are overlay evidence, not a gate on the conclusion. **JARVIS distinction (founder): designing the nervous system is a JARVIS flow; `VoiceKernel` is not JARVIS; JARVIS never enters the realtime path — ONE VoiceKernel · ONE SessionAuthority · ONE TurnCoordinator · ONE OutputController, no committee (VOICE-20); VOICE-2026 named a canonical JARVIS collaboration exemplar (`JARVIS_EXEMPLAR_NOTE_2026-09-11.md`).** Sequence (charter §5): ARCH-01 → **KERNEL-00** (physical organism, no STT/TTS/LLM/Web speech/MAIA) → KERNEL-01 → BENCH-01 → BRIDGE-01 → MIGRATE-01 → TURN-02 → DUPLEX-R&D. ⛔ D1–D10 and the constitution as a set await founder ratification by number (`ARCH-01/00_README.md` §2); KERNEL-00 opens only on a separate founder act after ratification; no `VoiceKernel.swift` exists; nothing built.
- **PRIOR — 2026-09-11 — `VOICE-2026` OPENED (founder ruling, RUNTIME-01 E21) · `CENSUS-01` ASSEMBLED, read-only, no production code.** Branch `claude/voice-2026-census-01`; charter `docs/programme/VOICE-2026/CHARTER_2026-09-11.md`; census `docs/programme/VOICE-2026/CENSUS-01_EXECUTABLE_VOICE_AUTHORITY_2026-09-11.md` (assembled from five sweeps in `census/`) + `SURVEY-01_CONTEMPORARY_VOICE_ARCHITECTURE_2026-09-11.md` (WebSearch-only, sourced, gaps UNVERIFIED). Premise: *voice is a persistent conversational audio runtime, not STT + TTS*; the legacy runtime is a frozen reference implementation and evidence source; defect-driven repair on it CLOSES after E19/E20. **What the census found on `/maia` iOS voice mode: four AVAudioSession writers (community speech plugin on every start, never released · gatekeeper on the repaired build · WebKit's own session · voice-recorder, JS-blocked) · one native start site reached by ELEVEN requesters, each pre-emptively stopping the live engine · SEVEN native stop sites · FOUR turn-closers armed at once (2500 ms · 1500 ms · plugin `stopped` · manual) · TWENTY-TWO discard authorities over the member's words · TEN automatic recovery drivers, all labelled `user_tap` at the boundary · zero native interruption/route observers · nothing can stop MAIA's audio once started · engine's `micState` never rendered.** ⭐ **E18 explained as a deterministic interaction, not 37 bugs**: one `isSpeaking=false` transition fans out to six restart requests; each restart kills the engine before it; the killed engine's task callback (no task identity) kills the one after it; D1's two ceilings are reset by the loop itself (1200 ms grace · `backoffStepRef=0` on every `started`); `isConversationAlive` self-refreshes on every start; the only exit is the WEB `onend` 45 s inactivity guard (`LISTENING_STOOD_DOWN` has no native caller — a web `SpeechRecognition` was running in the WebView via D4, which has no native guard); the exit parks the mic without stopping the native engine → engine alive on a dead input (8.8e-43). Route alternation `Bottom/0.01 ↔ Front/0.0026666` = plugin's `setPreferredIOBufferDuration(0.01)` vs **128 frames ÷ 48 kHz = WebKit's render quantum** — two session owners alternately winning [inference]. ⚠️ **Findings needing a founder act (recorded, NOT repaired): F1 the spoken voice on `/maia` is OpenAI cloud TTS by default (`/api/voice/openai-tts`; Kokoro needs env + archetype; no client fallback) — conflicts with the vow · F2 a `MAIABridgeViewController` was added and reverted 2026-03-03 (`abdbf858c`→`835e176c5`) for a pbxproj omission, not a bridge defect — history for RUNTIME-01, not a caveat · F3 registering `AudioSessionManager` also opens the composer-mic path (`prepareForListening` `.measurement` → voice-recorder `.playAndRecord`) with no stop of the live engine · F5 no stop handle for live playback · F6 `streamVoice.stop()` undefined at `OracleConversation.tsx:10340`; whisper health probes port 9000 vs 8000.** Dispositions proposed (charter §9 labels): KEEP = canonical MAIA convergence + `commitOracleTurn` + the segment≠turn principle; ADAPT = dispatch provenance/diagnostics, server whisper STT; REPLACE = session/capture/recognizer/turn-closure/output/UI-truth owners; REMOVE = restart-driver family, gatekeeper flip, `VoiceController.swift`, watchdogs, keep-alive contexts, web SR on iOS, `useVoiceSession` boundary, dormant cloud STT/TTS. ⛔ Not ratified. Next act = the synthesis question (charter §9), then ARCH-01's three artifacts (authority graph · conversation state machine · migration boundary) BEFORE code; KERNEL-01 `/voice-kernel-test` gated on ARCH-01. E19/E20 still owed on the phone per E21; results overlay §3.4 of the census.
- **LATEST — 2026-09-07 — ⚠️ SCHEMA DRIFT: THE I0.5 CIRCLE MIGRATIONS ARE IN PRODUCTION, UNDER NO RECORDED AUTHORIZATION.** Found while deploying an unrelated voice fix; the drift predates that deploy and is not caused by it. **Evidence (production `schema_migrations`, read 2026-09-07T19:2x):** `20260906000003_circle_membership_removals.sql` @ **18:24:45.957Z** · `20260907000001_circle_inquiry_response_withdrawal.sql` @ **18:24:46.020Z** · `20260907000002_circle_inquiry_status_retire_integrating.sql` @ **18:24:46.078Z** — three migrations, 121ms apart. ⭐ **RESOLVED BY IMAGE TIMESTAMPS, later the same evening.** `docker images maia-sovereign` shows image `891b33ee0` **BUILT at 18:22:45Z**, and `cmd_migrate` never builds — so the 18:24:46 rows are the **migrate phase of a full `deploy`/`update`** whose build finished two minutes earlier: exactly the script's `build → up → verify → migrate` cadence. ⚠️ **Two earlier readings in this bullet were wrong; both are corrected here rather than deleted.** *(i)* the first draft attributed the migrations to "the deploy that produced runtime `891b33ee0`" — **right about the deploy, wrong about the evidence**, because it read the container's `Created` (18:30:33Z) as that deploy's swap; *(ii)* the correction then **withdrew** the attribution on the strength of that same misread timestamp — an over-correction. **The image build time is the load-bearing fact. A container's `Created` is not a deploy's swap.** ⭐ **The 18:30:33 container is a RECREATION, and it is identified**: `~/.bash_history` on minisforum (mtime 18:31) ends with `nano ~/MAIA-SOVEREIGN/.env.production` followed by three `docker compose -f docker-compose.production.yml up -d --no-deps maia` — the **MAIL-04c Resend key rotation**, recreating the container to pick up a new env var. ⛔ **A bare `up` without `--build` never builds, so the Dockerfile deploy-lane tripwire never fires — it guards BUILDS, not container recreation.** That path takes no lane lock and runs no provenance verify; harmless here only because it recreated from a correctly-stamped image. **⛔ That is 47 minutes BEFORE the voice-fix commit `12eb44281` existed on any branch**, so neither it nor the 19:11Z deploy that carried it did this. The I0.5 record says the opposite of what is now true (`removal table ABSENT · integrating still allowed — UNCHANGED, UNMIGRATED, UNDEPLOYED`); that sentence is now marked superseded in place. ⛔ **The `20260907000002` migration is NOT additive** — it `DROP`s and re-adds `circle_inquiries_status_check`, narrowing the admitted vocabulary to `open | closed`, and `UPDATE`s any `integrating` row to `closed`. **Member-facing exposure is nil and should not be inflated into alarm**: production held 0 inquiries so the UPDATE touched no row, and Circle API authority is still founder-only (`requireCircleAccess()`; `CIRCLE_ACCESS_MEMBER_IDS` unconstituted), so no member can reach any of it. **What actually broke is the record, and the gate.** ⛔ **CANONICAL VERIFY IS STILL NOT ACCEPTED** — production now carries the schema that verify was supposed to license, which is the wrong order: *evidence licenses; it does not itself edit the public record, and a deploy must not edit the schema ahead of the ruling that authorizes it.* ⛔ **Do NOT treat this as retroactive authorization.** A migration reaching production by an unattributed act confers no standing it did not have at 18:23Z. **Owed, in order: (1)** ⭐⭐ **THE STRUCTURAL FINDING — MERGE-TO-CANONICAL IS LATENT SCHEMA-DEPLOY AUTHORIZATION.** Somebody *did* decide to run a full deployment; **nobody separately authorized or selected I0.5 for it.** Once the I0.5 lane merged its migrations to `clean-main-no-secrets`, the next unrelated full deploy applied them automatically as part of canonical branch state. *(An earlier wording here — "nobody decided to deploy I0.5; the branch decided for them" — is corrected: it implied mysterious agency. The truth is more mundane and more damning — ordinary deployment semantics, applied to a branch that had quietly become an authorization channel.)* This is not a hypothesis about the 18:24Z act — **the same mechanism is directly witnessed later the same evening**: the voice-fix deploy at 19:11Z carried `20260907000001_vault_erasure_queue.sql` (applied **19:31:48Z**, immediately after its 19:31:13Z swap), a WS-DELETE-01 migration that deploy had no interest in. *Merging a migration to the production branch is, in effect, authorizing it to be applied by whoever deploys next.* **The founder gate sits on the DEPLOY decision; nothing gates the BRANCH** — so every lane that deploys silently deploys every other lane's pending schema. ⛔ **That is the defect to fix, not the actor.** The actor remains formally **UNKNOWN** (`~/.bash_history` line 1977 `scripts/deploy-production.sh deploy "$TARGET"` is a candidate; shell history carries no timestamps). ⛔ **And the deploy lane keeps NO durable record of completed deploys**: `scripts/deploy-lock.sh` writes holder metadata *into the lockfile*, overwritten by every acquisition, and announces only to stderr — no `logger`, no journald, no ledger. **The 18:24Z acquisition record was overwritten hours ago, so its absence carries ZERO information and is evidence for nothing.** Attribution may be permanently unanswerable; per (3) that blocks nothing. An append-only line per acquisition (`timestamp · entry · target_sha · user@host · pid`) would have answered this in one query — the lane fix this incident argues for, ⛔ not authorized here; **(2)** a fresh production schema witness — this bullet asserts only the three `schema_migrations` rows it read; circle/membership counts, `withdrawn_at`'s presence and the removal table's contents are **UNREAD and must not be inferred from the migration filenames**; **(3)** — ⭐ **RULED, founder, 2026-09-07: RECONCILE FORWARD. REVERT NOT AUTHORIZED.** *A governance-order failure is not evidence that the implementation is wrong, and tearing down a state that may be perfectly correct in order to manufacture the opposite order is the more destructive error.* **Standing: I0.5 CODE deployed · I0.5 SCHEMA deployed · CANONICAL VERIFY not yet accepted · FOUNDER ACCEPTANCE not yet performed · I1 HOLD · further Circle build HOLD · REVERT NOT AUTHORIZED.** Acceptance requires **two independent witnesses**: **(i) canonical candidate** — the committed I0.5 SHA through its disposable-shadow verifier under unchanged acceptance law (every named obligation present, `63/63`, no fail/warn/skip, residue zero); **(ii) production state** — read what production *is* now: migrations, the relevant columns and constraints, row counts, FR-18 behaviour and the access membrane, and confirm existing data suffered no unintended consequence. ⭐ **On both green the ruling is `I0.5 ACCEPTED AS CURRENT DEPLOYED STATE` — explicitly NOT "I0.5 was authorized before deployment" and NOT "the unauthorized deployment is forgiven or erased". Acceptance now does not rewrite history; the deployment-order breach remains a separate operational finding with its own open provenance investigation.** ⛔ **If the canonical verifier fails, adjudicate the failure on what failed — repair versus rollback is decided then, never pre-chosen.** **Structural note for the deploy lane:** `scripts/deploy-production.sh:445-477` orders build → swap → provenance verify → **migrate**, so schema lands AFTER code, and a failed migrate step only `log_warn`s and still reports `Deployment complete!` — **a green deploy is not evidence a migration ran.** **Production runtime moved AGAIN at 20:00:27Z to `3ef333a65`** (image built 19:59:03Z, tagged `current`/`prod`) — same voice-fix code plus one CLAUDE.md line, nothing regressed; most plausibly the mail lane deploying `clean-main-no-secrets`'s tip after PR #1263 merged, ⛔ **not confirmed**. ⚠️ **Of the FOUR production-changing acts reconstructed for 2026-09-07, exactly ONE is founder-attributed** — the 19:11Z full deploy. The other three are **two full deploys (18:22Z · 19:59Z) and one unlocked container recreation (18:30Z)**. ⛔ *The recreation is deliberately NOT counted as a deploy*: it took a different path — no build, no lane lock, no provenance verify — and folding it back into the deploy count would hide the very gap it exposed. ⛔ **CI is excluded as initiator**: the checked-in production workflow triggers on `main` / `production` plus manual dispatch, so a push to `clean-main-no-secrets` does not deploy. **That ratio, not any single act, is the finding.** ⭐ **BRANCH GATE is the lane this opens**, and its question is no longer *who deployed someone else's migration* but: **why can a migration become deployable merely by becoming canonical, before its own acceptance law authorizes a production schema change?** Adjacent and part of the same hole: every push to `clean-main-no-secrets` tonight — this session's and the mail lane's alike — reported `Bypassed rule violations … 4 of 4 required status checks are expected`. **The production branch accepts writes without its declared checks, and whatever lands there is schema the next deploy will apply.** ⛔ Lane not opened; no repair authorized here.
- **LATEST — 2026-09-07 — MAIA voice-mode silence: FIXED, DEPLOYED, VERIFIED LIVE (member falsifier still owed).** `12eb44281` on `clean-main-no-secrets`, live at **19:31:13Z** (`/api/health` → `version: 12eb44281`; `printenv GIT_COMMIT` agrees). Reported as *"she isn't answering in words or text"*, **voice mode only, intermittent**. **MAIA's turn was a side effect of TTS.** The sole non-streaming voice append lived inside `await maiaSpeak(...)`'s success branch and was additionally gated on `showVoiceText`, so: **(1)** MAIA's voice off — the `speak + silent` cell of the 2026-08-13 modality-independence ruling — reached an `else` that only logged; cognition completed and the member got nothing in either channel; **(2)** a `maiaSpeak` that stalls without throwing ran neither the success branch nor the catch — no audio, no words, no error, which is exactly how the intermittency presented; **(3)** with `showVoiceText` off the turn never entered `messages` at all, so it was missing when the transcript was revealed AND missing from the next turn's `conversationHistory` — **a render preference deciding what MAIA could remember saying.** ⭐ **VOICE-CANONICAL-CONVERGENCE-02 made this the ordinary path rather than an edge**: with the streaming exit removed, voice turns are non-streaming, so `usedStreamingAudio` is false and the streaming append never runs. The convergence was right; it left the transcript hanging off the TTS call. *Voice may have a different capture path; it may not have a different mind — nor a different record of that mind's words.* **Repair:** one idempotent `commitOracleTurn()` seam reached from all six terminal paths, independent of TTS outcome and of `showVoiceText`; speech ordering preserved where speech works, bounded by `VOICE_TRANSCRIPT_WATCHDOG_MS` (6s) where it does not — **a hung TTS now delays MAIA's words rather than erasing them.** Also: the live interim transcript was a single `truncate` line building off-screen behind a frozen ellipsis, so the one thing that row exists to say — *she is still hearing me* — was the thing it stopped showing; it now wraps into a bounded tail-pinned tape. **Gates:** typecheck `230 vs baseline 239 · 0 regressions` · `__tests__/voice-non-degradation.test.ts` 15/15 (convergence intact) · new `__tests__/voice-transcript-commit.test.ts` 6/6 · `check:no-supabase` clean · **post-swap Co-Lab gate `33 passed · 0 failed · 0 warned`** · `vault_erasure_queue` present after the swap. ⛔ **NOT YET FALSIFIED BY A MEMBER**: the decisive case is **voice mode with MAIA's voice OFF** — before this commit it had no code path at all. Deployed ≠ demonstrated; *a fix verified only by its author's tests is a claim about code, not about MAIA.*
- **LATEST — 2026-09-07 — `JARVIS-CIRCLES-01` · `CIRCLE-05 · INVOKE` · `I0.5 · ENTRY SAFETY CLOSURE`: IMPLEMENTED, ⛔ NOT VERIFIED.** Branch `claude/jarvis-circles-programme-reouzc`; record `docs/programme/JARVIS-CIRCLES-01_I0.5_ENTRY_SAFETY_2026-09-07.md`; rulings addendum in `…_FOUNDER_RULINGS_2026-09-06.md`. Opened by founder act on the two I0 findings that are **defects against already-ratified law**, not design questions. **⭐ FR-18 RATIFIED — *a generic Circle invitation may not reinstate a removed member.* Canonical shorthand (founder precision): _"a recorded removal standing outranks a generic invitation"_ — deliberately NOT the broader "standing outranks invitation", which could be read as deciding CA-08.** ⚠️ **This ruling corrected Jarvis**: the I0 docket recommended revoking the invite token on removal; refused, because the token is **Circle-wide** — revoking it withdraws the invitation from *everyone* to answer *one* person's standing. The defect was never that the token survived, it was that **a generic bearer credential had enough authority to overwrite a recorded relational act**. Repair: `joinWithInviteWithClient()` seam; ⭐ **the authority is the MUTATION, not a precheck** — the guard lives inside the upsert (`ON CONFLICT … DO UPDATE … WHERE circle_memberships.status <> 'removed' RETURNING`; zero rows → `REINSTATEMENT_REQUIRED`) → **409** carrying **no Circle name, no grounds, no token verdict** (*a refusal is not an occasion to disclose*). ⚠️ **Founder source review REJECTED the first candidate `093379e8d` before any verifier run**: it read the standing, refused if `removed`, then upserted `active` unconditionally — `transaction()` is an ordinary BEGIN with no row lock, so a removal committing between the read and the write let the invitation overwrite it. **FR-18 was prechecked but not authoritative at the mutation boundary, and T10a–T10e could not tell the two apart** (all five evaluate a standing already `removed` before the join began). Preliminary SELECT deleted — one authority, no second thing to mistake for it. *No migration; no schema change; `left` behaviour identical; CA-08 still open.* **I-02 — STATE CLOSURE ON PAGE**: new `app/commons/join/layout.tsx` renders a closed state, so an unauthorized visitor is never asked for a token, never asked for a **consent mode** (*asking for a sharing decision about a relationship they cannot enter collects a sovereign act the system has no standing to receive*), and never submits a doomed join; `config/accessMatrix.ts` records DECLARED-vs-ENFORCED. ⛔ **I-03 preserved deliberately** — the closed surface reads no invite, so a valid and an invalid token stay **indistinguishable** before authorization. ⛔ **I8 obligation (not an I0.5 defect):** the page checks `requireFounder()` and the API `requireCircleAccess()` — they agree **today** only because Circle API authority is itself founder-only; when `CIRCLE_ACCESS_MEMBER_IDS` is constituted at I8, `/commons/join` **must migrate to the same Circle-access authority**. ⛔ Do not change it now. ⛔ **The API gate is unchanged** — a layout does not run for a route handler; `requireCircleAccess()` is still the authorization (C22 guards that confusion, which is the shape of B-01). **Verifier floor 54 → 63** under FR-14 (*the number is descriptive; the named set is the law*): **C20** surface refuses before rendering · **C21** solicits nothing AND validates nothing · **C22** join door gated + can refuse reinstatement · **T10a** valid live invite does not reinstate · **T10b** standing stays `removed` (**no write occurred**, not merely a thrown error) · **T10c** FR-05 removal record intact · **⭐ T10d** the **same live invite still admits an independently eligible member** — the obligation that makes the founder's correction falsifiable: a future "fix" by token revocation passes T10a–T10c and **FAILS T10d** · **T10e** no other Circle affected · **⭐ T10f THE INTERLEAVING WITNESS** — a **real `removeMemberWithClient()` act landing between invitation evaluation and the membership mutation** still loses: refusal, standing stays `removed`, removal record intact. Built deterministically via an `InviteClient` wrapper on the same rolled-back transaction (a second connection would need committed fixtures and would break the verifier's rollback-only consequence contract). **A precheck candidate passes T10a–T10e and FAILS T10f.** **Not authorized, founder: *do not add yet*** — per-invitee tokens · invite expiry · reinstatement workflow · facilitator review UI. ⛔ **CA-08 not decided** (`left` ≠ `removed`). **D-I dispositions ratified: D-I1 HYBRID** (only **explicit selection** drives matching; free text is **expressive only, never inferred into taxonomy** — the operative form of FR-06) · **D-I2 BUILD FRESH**, implementation term **"Interest Commons"** · **D-I3** cohort mechanism at I8, identities only at cohort authorization, ⛔ **do not touch `FOUNDER_MEMBER_IDS`** · **D-I5** member count **and** derived FORMING/ACTIVE are **INTERIOR** · **D-I6** `created_by` is **provenance, not permanent ownership**. Outer membrane narrows to `name`/`description`/`created_at`; authority axes carried to I4/I6 (CA-15 open). ⭐ **FOUNDER-RUN VERIFICATION OF `ae0fadd54` (disposable shadow, three pending migrations applied to the shadow only): `62 passed · 1 failed · 0 warned · 0 skipped · coverage 62/63 · exit 1` — EVERY BEHAVIORAL OBLIGATION PASSED, `T10a–T10f` included.** ⭐ **T10f PASSING IS THE SUBSTANTIVE RESULT: a removal landing after invitation evaluation and before membership mutation still defeats the generic invitation — the exact race `093379e8d` could not survive.** The single failure was **C21, a verifier false positive**: it scanned the RAW layout source, which contains the sentence *"nothing here reads `circle_invites`"* — **prose documenting the absence, matched as evidence of the presence**; the page inspects no token. Founder proved it on a disposable copy changing **only C21** to strip comments → `63 passed · 0 failed · exit 0`, residue zero. **Repair `3ed98ceab` is INSTRUMENT ONLY** — C21 now strips block and line comments before scanning, the same discipline **C6** has used since R4 and for the same reason; token set NOT weakened; ⛔ layout, `joinWithInviteWithClient()`, FR-18, T10a–T10f, the floor and all Circle behavior UNCHANGED. *A prose ban must never read as the banned behavior returning — an instrument that scans prose can fail on a file precisely because that file documents its own compliance.* **Typecheck at `ae0fadd54`: `230 errors · baseline 239 · 9 fixed · 0 regressions · exit 0`.** **Production witness (SHA `e4ac1bcac` · 4 circles · 4 active memberships · 0 shares/inquiries/responses · removal table ABSENT · `withdrawn_at` ABSENT · `integrating` still allowed — UNCHANGED, UNMIGRATED, UNDEPLOYED) — ⛔ SUPERSEDED at 2026-09-07T18:24:46Z; see the SCHEMA DRIFT bullet above. It was true when recorded and is false now. Kept verbatim as the before-state, never as a current claim — a witness is a reading at a time, and the honest repair is to date it, not to edit it.** ⛔ **CANONICAL VERIFY NOT YET ACCEPTED** — the 63/63 evidence is from a **disposable probe, not a run of a committed SHA; a probe is not the record.** Acceptance requires the full verifier from the committed SHA against a disposable shadow (`20260906000003` → `20260907000001` → `20260907000002`, shadow only): `63/63 · 0 failed · 0 warned · 0 skipped · exit 0`, residue zero. ⛔ **I1 DESIGN NOT OPENED** — it opens on that canonical run; *Jarvis does not open a stage on a run it has not seen.* ⛔ No deploy · no migration authored · schema untouched · cohort not authorized · no invitation sent · no facilitator assigned · no discovery built · founder UI gate untouched.
- **PRIOR (2026-09-07, superseded same day by I0.5) — `JARVIS-CIRCLES-01`: REPAIR CLOSED · **VERIFY FULL CANDIDATE PASS (54/54, 0 failed, exit 0)** · `CIRCLE-05 · INVOKE` OPEN, **I0 census COMPLETE (READ ONLY)**.** Branch `claude/jarvis-circles-programme-reouzc`; rulings `…_FOUNDER_RULINGS_2026-09-06.md` (**FR-01…FR-17**, open **CA-01…CA-16**); I0 outputs `docs/programme/CIRCLES_INVOCATION_SUBSTRATE_CENSUS.md` + `…_I0_FOUNDER_DOCKET_2026-09-07.md`. **Founder-run disposable-shadow gate on `26508109b` with all three migrations in order (`20260906000003` → `20260907000001` → `20260907000002`): `54 passed · 0 failed · 0 warned · 0 skipped · 54/54 required obligations discharged by PASS · exit 0`; fixtures rolled back clean.** ⭐ **R1–R4 and P1–P5 / A–F = VERIFIED ON CANDIDATE.** ⛔ **PRODUCTION UNCHANGED — no Circle migration applied, no deploy, founder-gated.** *Verified ≠ deployed.* ⛔ **Do not rerun the shadow gate merely because a session did not run it itself — the founder-run witness is the evidence of record.** **Ratified during REPAIR: FR-14** verifier coverage law (*PASS = 0 failed AND every required obligation still present AND discharged by PASS only* — WARN/SKIP/MISSING never discharge; named floor, never a total; *an instrument can satisfy all of its remaining questions by forgetting to ask the difficult ones*) · **FR-15** withdrawal is a **tombstone** (*keep the fact that an act occurred when the system needs that fact for integrity; do not keep the person's surrendered meaning merely because storage makes it easy*) · **FR-16** membership termination **cascades** (AUTHOR WITHDRAWAL ≠ BOUNDARY CASCADE) · **FR-17** stored inquiry `integrating` **retired** (⛔ `FieldPhase` untouched, asserted by C18). **I0 CENSUS — three findings dominate. (1) ⭐ NO explicit member-authored interest declaration exists anywhere** — `living_field_affinities` is **system-created from private memory atoms** (`created_by='system'`, `affinity_score`, `evidence_reason`) and `recurring_interests` is session-inferred; **both BARRED ABSOLUTELY by FR-06**. The north-star verb *"find one another around a shared interest"* has **no substrate and no reusable precedent — I1 builds it from zero. (2) ⭐⭐ "Commons" denotes THREE different existing things and NONE matches ratified FR-02**: the `community_*` forum · the `commons_contributions` library · the Circles migration filename. **Two are built on exactly the mechanics FR-08.7 forbids** — `community_user_stats` (post/comment/heart/breakthrough counts · `contribution_tier` · `contribution_points`), `contribution_levels` (0|1|2 · `accepted_count` · `endorsed_by`), `community_territories.min_contribution_tier` + **`min_cognitive_level` gating ACCESS on a measured attribute of a person**. ⛔ **Reusing them as Circle substrate would import a status economy on day one** — the exact Discourse/Mighty distortion named in R9. **Build the FR-02 Commons fresh.** **(3) 🔴 I-01 — REMOVAL DOES NOT REVOKE INVITE TOKENS.** Tokens are **Circle-wide, never expire** (no `expires_at`), and `joinWithInvite`'s upsert restores a removed member to `active`; the only remedy is `regenerateInvite`, which is **creator-only** and kills the link for everyone. **FR-05 requires removal to cut access — today it cuts access only until the next join request.** A **defect against ratified law**, repairable before I1. 🔴 **I-02 — `/commons/join` (public) collects a token AND a consent choice, then the gated API returns 403** — **a contradiction R1 created** by correctly closing the API without bringing the member-facing surface along. **Other I0 facts:** discovery = **ZERO** (`listMyCircles` only; `visibility`/`invite_enabled` **INERT** — ⛔ do not build to them) · creator role = `helper`, **no code path anywhere writes `facilitator`** (CA-15) · **authority is fixed at creation permanently** — `created_by` unchangeable, `facilitator` unassignable, and the two axes are orthogonal · `labAccess.ts` is the reusable cohort precedent (union with founder · fails closed · confers nothing beyond its door) → `CIRCLE_ACCESS_MEMBER_IDS` when a cohort is authorized, **not before** · outer membrane: only `name`/`description`/`created_at` are safe outer facts, **three of the doctrine's four panes have no column**, and member count is **interior** because it is also the FR-11 constitution state. **Classification is deliberately conservative — nothing is called a violation whose semantics were never constituted** (inert columns and legacy Commons = INERT LEGACY, not offences). > ***The membrane is verified. The invitation is not built.*** **Standing: I1–I9 NOT STARTED · DEPLOY NOT AUTHORIZED · COHORT NOT AUTHORIZED · no discovery built · no facilitator assignment · no invitations sent · founder UI gate untouched · doctrine byte-identical · no number ratified.**
- **LATEST — 2026-09-06 — LANE SPLIT (founder ruling): `JARVIS-RD-HUMAN-EXPERIENCE-ARCHITECTURE-01` ⇄ `JARVIS-PUBLIC-ACCOUNTED-FOR-01`.** `/accounted-for`, `docs/pitch/MAIA_PLATFORM_ACCOUNTING_2026-09-03.md`, the claim-reconciliation record, the page contract + screenshots and **PR #1239** (branch `claude/maia-human-experience-arch-12g5r6`, head `c36d82ec`, DRAFT — merge NOT AUTHORIZED, deploy NOT AUTHORIZED) transfer to the new publication lane; charter `docs/programme/JARVIS-PUBLIC-ACCOUNTED-FOR-01_CHARTER_2026-09-06.md`, split record `docs/programme/JARVIS-LANE-SPLIT_HUMAN-EXPERIENCE_ACCOUNTED-FOR_2026-09-06.md`. Direction of authority: R&D / canon / runtime evidence → claim reconciliation → page → public, never the reverse; the publication lane translates accepted outputs and may not adjudicate or alter them (wording · downgrade · omit · return upstream only). Two axes on every substantive sentence — epistemic kind (OBSERVED · RESEARCH-SUPPORTED · SOULLAB INTERPRETATION · HYPOTHESIS · OPEN QUESTION) × product maturity (LIVE · PARTLY LIVE · DESIGNED · VISION) — neither implies the other. Current run: reconciliation COMPLETE, render walk PASS, gates PASS; **merge-readiness WITHHELD** — a collision was observed at 17:36Z: the R&D lane's live session pushed its same-day pivot act (master run `docs/programme/JARVIS-HUMAN-EXPERIENCE-MASTER-RUN-v1.md`, Phase 1 open, manifesto FROZEN, its charter §24) and a census skeleton onto the PR branch without recording the split, and master run §4/§11 still assign `/accounted-for` tasks to the R&D cockpit; PR is also behind base `69f6fb7c` (clean freshness merge). **Founder ruling ~17:48Z (split record §7a): ACCEPTED** — intended transfer `c36d82ec`, containment `6ce59f82` → drift `f7705937` → **FINAL `a7b42f29`** (late arrivals recorded, not erased), PR branch single-writer = publication lane thereafter, all R&D commits PRESERVED (no rewrite, no revert), further R&D writes to #1239 NOT AUTHORIZED; stop instruction relayed into the R&D session by Routine — **acknowledged**: R&D session now on `claude/maia-human-experience-phase1-census` (merge-base with #1239 = `a7b42f29`; its charter §25 + master-run delegation landed there) → **operational transfer CLOSED ~17:58Z**; governance transfer = PR #1241 (awaiting founder merge act). Sequence: this governance branch merges first → #1239 absorbs `69f6fb7c` → both CLAUDE.md bullets kept → PR title/body to publication custody → custody check + gates rerun → only then `READY FOR FOUNDER MERGE RULING`. Jarvis never infers gates-green → merge. Merge ≠ deploy; "live" only after production witness. The R&D lane no longer edits `/accounted-for` and resumes at **Phase 1 — Whole-Organism MAIA/AIN Mapping**; its charter transfer section (next free number; §24 is the pivot) is owed on its Phase 1 branch or the PR branch (split record §5). *Accounted For does not decide what Soullab is. It makes Soullab answer publicly for what it says it is.*
- **LATEST — 2026-09-06 — JARVIS-RD-HUMAN-EXPERIENCE-ARCHITECTURE-01: foundational challenge pass R8–R12 CLOSED / COMPLETE (founder act); Synthesis v0.2 ACCEPTED as current programme synthesis (not doctrine; P1–P13 CANDIDATE; empirical postures provisional; v0.1 historical); reconciliation RUN (`docs/programme/…_CLAIM_RECONCILIATION_2026-09-06.md`); merge ruling on #1239 pending as a founder stop. **PIVOT (founder, same day): cockpit of record is `docs/programme/JARVIS-HUMAN-EXPERIENCE-MASTER-RUN-v1.md` — Phase 1 whole-organism map CURRENT; manifesto expansion FROZEN; research/new theory STOPPED; read-only census of thirteen subsystems against v0.2 (nine questions + R11 design audit + R12 memory criteria) → `docs/programme/MAIA_WHOLE_ORGANISM_MAP/00_RANKED_MAP.md` for founder adjudication; DO NOT REPAIR DURING THE CENSUS.** Branch `claude/maia-human-experience-arch-12g5r6`, PR #1239 DRAFT — merge and deploy NOT AUTHORIZED; MAIA runtime, memory runtime and `/accounted-for` final render HELD. Record: `docs/programme/JARVIS-RD-HUMAN-EXPERIENCE-ARCHITECTURE-01_CHARTER_2026-09-06.md` §22; spine under `docs/research/human-experience/`. **Episodic Phase 2 spec inputs added by the R12 ruling (Cat 1, not authorized):** the four-type memory typology (episodic · semantic · pattern · state-responsive, v0.1 source [22] — located, vendor-authored, conceptual, override risk-gated) is WITHDRAWN as a supported decomposition; working decomposition is **Authority × Time** (`docs/research/human-experience/frameworks/memory/AUTHORITY_X_TIME_2026-09-06.md`) with verbatim beneath derived; R7b precise — *member statement overrides as present self-report (does not rewrite history); MAIA impression asks* (= detect → ask → record with an evidence basis); five directions: preserve qualification · change-sensitive retrieval outranks similarity only under temporal conflict (the F2 retrieval cut becomes a safety question) · implicit contradiction → question, not transition · derived stays visibly derived · memory is never leverage. Brain-training rule is programme doctrine: *a gain on the practiced task is never, by itself, evidence of a gain in life.*
- **DIRECTION — 2026-09-06 — Temporal Memory (Cat 1, held).** `docs/architecture/TEMPORAL_MEMORY_DIRECTION_2026-09-06.md` records three inputs to the still-unauthored Episodic Phase 2 spec: (1) episode `occurred_at` vs assertion `valid_from`/`valid_to` live on different objects; (2) succession is carried by the successor via `supersedes`, `superseded_by` is derived, never stored; (3) staleness is **detect → ask → record** — the system never sets `valid_to` from a timer; the word *repair* is banned. **Audit RUN 2026-09-06 against production** (`scripts/witness/temporal-memory-audit.sql`, no persistent writes; output verbatim in the note): **F1** `valid_to` fallback gap is real in code and **ruled out under current data** — zero developmental memories carry a past `valid_to`, the filter has never excluded a row; **F2** invisible decay changes which developmental rows survive the non-vector top-12 **retrieval** cut for **2 of the 14 members** whose pool exceeds 12 (36 members, 2018 rows), five of twelve for one member on a one-month age difference — a material **upstream** effect; whether it propagates through `MemoryBundle.build()` merge → dedupe → `maxBullets` (default 5) into the prompt is **unmeasured** (open question for Phase 2, answerable from `selectionTrace`); **F3** only `pattern` is live, and decay has **two divergent implementations** — the live SQL `calculate_decayed_confidence` has no confirmed-1.5× rule (that exists only in the TS helper) and the SQL confirmation term caps at 0.0225 — no single authoritative definition of decay exists. Note **FROZEN**; auditing stops. `shouldPromptForConfirmation` has zero callers and was not wired. Not a lane; findings are inputs to the Episodic Phase 2 spec, sequence step 5. **Predeclared Episodic Phase 2 acceptance inputs (not authoritative until incorporated into the spec):** the candidate temporal substrate must answer five distinct queries without rewriting history — (1) Where do I live now? [current-valid], (2) Where was I living in March? [valid-at], (3) When did I tell you I moved? [episode/event time], (4) When did MAIA record that I had moved? [transaction time], and (5) What did MAIA believe before I corrected it? [belief-at-record-time, discriminator]. Temporal recall must identify the axis it resolved on; where the member’s question leaves the axis ambiguous, MAIA clarifies rather than silently choosing one. Axis clarification is member-governed interpretation of the query, not a temporal-memory transition: "March" authorizes which query runs; it does not open or close an assertion interval.
- **LATEST — 2026-09-06 — WS2-08 Hierarchical Manuscript Structure: lane opened by founder; **BUILD-08A CLOSED / ACCEPTED 2026-09-06 (founder ruling)** — PR #1230 merged as `03e9d89a`; migration `20260906000001_manuscript_section_heading_depth.sql` applied 13:36:01Z (deploy established by state evidence; transcript not recovered, non-blocking); production witness on runtime `50302f5d` (08A blobs identical to `03e9d89a`): F1 real-DOCX · F2 · F3 · F6a · F6b (13:46 run of record; later reruns FAIL on R5 = cleanup of 12 transient rows, retained as such) · exact-filename ledger query — ALL PASS. **WS2-08 lane STILL OPEN; 08B HOLD until an explicit founder act.** Note: the Co-Lab gate script is `scripts/verify-constitution-colab.ts` (33/0/0); the `verify-colab-boundaries.ts` name in this file and `docs/ops/COLAB_RELEASE_GATE.md` never existed — separate doc task. Unrelated draft-route `Response body … disturbed or locked` error parked, untriaged, in `docs/programme/PARKED_DEFECT_MANUSCRIPT_DRAFT_ROUTE_RESPONSE_BODY_2026-09-06.md`. 08B code HOLD until an explicit founder act; a minimum structure revision/digest binding must land before 08C's mutating commands; `heading_signal` = decisive classifier by precedence markdown > chapter > caps, not exhaustive provenance.** Requirement: *a Work may be addressable at fine-grained section level while remaining coherently organized as chapters and subchapters authored or confirmed by the member.* Census: `segment.ts` matched `#`/`##`/`###` then discarded the count; ingest persisted `position · heading · body` only; `origin='imported'` admitted by WS2-05A CHECK but produced by nothing; no command anywhere splits/merges a draft section (`topology_change_requires_explicit_command` has no counterpart). 08A lands: `heading_depth` (1..3|NULL) + `heading_signal` (markdown|chapter|caps|member) on **Source** `manuscript_sections` (migration `20260906000001`, additive); ALL-CAPS = boundary with depth NULL, **never a chapter by default**; pure `deriveImportedStructure()` folds depths into `ReviewedUnit[]` for the 06A confirm path (185 caps cuts → 0 units). Not a `level` on the structure tree — WS2-05A ruling holds. 08B (member-confirmed imported hierarchy) · 08C (split/merge/rename commands) · 08D (nested WRITE gestures) · 08E (revision-bound structure ledger) each need a founder act. Sequenced after #1228, which is untouched. Record: `docs/programme/WS2-08_HIERARCHICAL_MANUSCRIPT_STRUCTURE_DECIDE_2026-09-06.md`.
- **NAMING — 2026-09-04 — Coaching Journey Template.** Three layers fixed before any extraction work: **Coaching Platform** (generic product) → **Coaching Journey Template** (generic methodology/configuration layer) → **Now What?** (Larry's configured instance). Lane `NOW-WHAT-PORTABILITY-01` retired → `COACHING-TEMPLATE-EXTRACTION-01`, premise *Now What? is the first specimen, not the generic architecture*. Renaming establishes nothing about provenance: Larry-specific material stays in the instance; ambiguous items are classified before generalizing. Lane NOT opened (Anti-Drift Law freeze on generalized architecture holds). Ruling: `docs/programme/COACHING-TEMPLATE-EXTRACTION-01_NAMING_RULING_2026-09-04.md`.
- **LATEST — 2026-09-03 — Writer's Studio BUILD-07A Developmental Evidence: candidate `bfeb1a9` on `claude/build-07a-developmental-evidence-n5tm37`, bound to canonical `8d04f1b9f`. NOT CLOSED.** `lib/manuscript/development/` — typed `EvidenceRef` (section · passage · section-run · structure-unit · structure-units · structure-topology; no version, no quote, no live offset), frozen `readState` (per-section `(revisionNumber, code-point range, digest)` into the append-only revision store; structure context frozen inline; coverage `position | body`; `inputFingerprint`), `recoverEvidence` (historical display, digest-verified) vs `locateCurrent` (three-state, scoped per ref, never fuzzy), unforgeable `BoundEvidence` via `bindEvidence`, read-only `captureEvidence`. INV-7b demonstrated. Record: `docs/programme/WS2-07-BUILD-07A_EVIDENCE_WITNESS_2026-09-03.md` — **the unit's ten falsifiers / six outcomes were never on canonical; the record RECONSTRUCTS them from DECIDE (§4); founder RATIFIED them prospectively 2026-09-04 as Acceptance Instrument v1 (authority begins with the ruling, no provenance claim). Closure now waits on two founder checks: production `SHOW server_encoding` = `UTF8` (else STOP), and a founder-visible witness rerun from a clean checkout (`DATABASE_URL=… npx tsx scripts/ws2-07a-evidence-witness.ts` → `50 checks · 0 failures`). On both: CLOSED / ACCEPTED → PR → gates → merge on green.** Witness needs a UTF-8 database — an SQL_ASCII cluster makes the partition trigger count bytes (record §5). BUILD-07B–H unauthorized; 07B does not open on 07A passing.
- **LATEST — 2026-09-03 — CMT-01 Canonical MAIA Turn: M0–M2 landed (branch `claude/canonical-maia-turn-j92opb`), M3 NOT authorized.** Census (`docs/programme/MAIA_CANONICAL_TURN_CURRENT_STATE_CENSUS.md`) found cognition converged at `getMaiaResponse()` but *authority over what MAIA thinks with* never did: six MAIA-claiming composition mechanisms, one open channel (`meta?: Record<string, unknown>`, 204 `(meta as any)` reads), FAST tier missing three standing guardrails (D1). Spec v0.1 approved with Decision 2 axis amendment (`authoredBy` + `participationClass` + `authority`, never one scalar). Landed: `lib/maia/canonical-turn/` (38-producer closed registry, pure MIPA `adjudicateParticipation`, content-free manifest `[MAIA/manifest]`, one renderer, one identity resolver), six falsifiers R25–R30 (`npx tsx tests/constitutional/refusal-registry/cmt-01-gates.ts` — **R25/R26 expected RED until M3**), and a **shadow** construction on `/list` emitting `[MAIA/shadow] { zeroDiff, missingInCanonical, missingInLegacy, digestMismatch }` per turn with legacy assembly still response-producing. Rulings: voice/stream cognition path RETIRE; `sovereign/app/maia` STRUCTURALLY RETIRE (410 first); practice-field/draft ALLOWLIST; manifest EMISSION ONLY. Evidence: `docs/programme/CMT-01_M0-M2_WITNESS_2026-09-03.md`. **M0–M2 ACCEPTED 2026-09-03. First shadow deploy of `2fafaa4` ran as `GIT_COMMIT=unknown` — image stamped, container clobbered by the checkout compose's runtime override (deploy-chain defect, not CMT; witness §incident). Repair landed as `R` (`fix(deploy): launch provenance`; `npm run verify:deploy-provenance` 27/27): compose override removed, build+swap from the snapshot compose, pre-swap image verify, dual post-swap verify, env-collision refusal. Witness runtime = `R`, CMT anchor = `2fafaa4`; bring the minisforum checkout forward before deploying `R`. Bounded M2 shadow deploy of `R` AUTHORIZED (runbook: `docs/programme/CMT-01_M2_SHADOW_DEPLOY_RUNBOOK_2026-09-03.md`; pre-deploy condition pinned as R31 GREEN; collector `scripts/witness/cmt-01-shadow-witness.ts`). Deploy runs from the Mac Studio, not a remote session. BRANCH FROZEN TO A SINGLE WRITER until the live witness is complete. M3 explicitly unauthorized.** One non-zero `[MAIA/shadow]` line stops the witness for classification — never normalize or fix M3 around it. Merged with the parallel `pdc-1` participation-disposition contract (`9ba2d93`) — M1 imports it, never redeclares it; `OFFERED` representable, empty under pp-1. `lib/maia/canonical-turn/**` is inside `tsconfig.ship.json` from that merge.
- **PRIOR — 2026-07-03**: **Daily Anchor member standing-consent gate — shipped + verified LIVE.** Ambient anchor surfacing is now gated by `member_daily_anchors.surface_preference` (default `member_pulled` = private; member opts in to `contextual_doorway`), mirroring the atoms `return_preference` model — eligibility now originates from a member act, not the `MAIA_ANCHOR_CONTEXT_ENABLED` deploy flag (kill-switch only). Migration `20260702000003`; loader gate in `lib/anchor/loadRecentAnchors.ts`; gesture route `POST /api/anchor/[id]/surface-preference`; UI toggle on `app/maia/anchor/history/page.tsx`; refusal **R08**. Verified 2026-07-03 by 6 prod proofs under an authenticated member (schema+ledger, default-private, authenticated opt-in/return, MAIA-follows-consent). **Behavior change in effect:** existing anchors default private until each member opts in. Shipped via PR #542 (gate+UI), unblocked by PR #559 (migration idempotency fix — encounter triggers + team-scope constraints) after the covenant-gates redesign (#561) dissolved the author-self-approval deadlock. Full detail: memory `project_anchor_consent_gate_live`. *Note: the WISDOM_IS_RECOVERED.md canon cited by the original spec does not exist in the repo — grounded instead on SPIRAL_CONTINUITY_ENGINE §7 + atoms model; no new canon authored.*
- **Date**: 2026-05-24 (evening)
- **Current milestone**: Conversational Phase 2 (prompt influence, default-on, opt-out gate) on branch `feature/conversational-memory-phase2`, awaiting fork resolution + deploy + verification. **Headline framing for the week** (per `docs/architecture/STATE_AND_ROADMAP_2026-05-24.md` §8): *MAIA's memory field has been clarified, bounded, partially operationalized, and protected from premature ontological claims — not completed.* *"All arenas, safe but functional"* directive in effect — discipline reorients from blocking function to guiding it, *and from inflating function to naming it precisely.*
- **Phase 2 commit chain** (currently on `feature/conversational-memory-phase2`; not yet merged to `clean-main-no-secrets` or deployed):
  1. `987b3ff28` — initial Phase 2 (loader extension, block formatter, migration, `[Oracle]` log line). Wired into `app/api/oracle/conversation/route.ts` — **post-audit: that route receives ~zero live traffic; wire was operationally null**.
  2. `5179b162e` — (Kelly) Memory Expansion Plan — 9-layer activation map
  3. `f74ab4204` — (Kelly) wire site correction. Moved Phase 2 to live route `app/api/sovereign/app/maia/list/route.ts` + extracted in FAST tier of `lib/sovereign/maiaService.ts` template literal. Renamed log marker to `[MAIA] conversational-block`. Spec §IX appended documenting the wire-site error + the architectural seam gap (`buildMaiaRuntimeContext` is observer, not orchestrator).
  4. `3ca80a78d` — CORE/DEEP cut. CORE reaches prompt via `buildMaiaWisePrompt` extraction at `lib/sovereign/maiaVoice.ts:884-887`. DEEP `repairedContext` carries the addendum but DEEP prompt builder (`buildComprehensiveVoicePrompt` in `intelligentVoiceAdaptation`) does NOT extract it — prompt injection blocked at DEEP by the addenda-channel divergence documented in `docs/architecture/ADDENDA_CHANNEL_DIVERGENCE_2026-05-24.md` §II.B. Observability complete (`PROMPT_BLOCK_CHARS` + `layers.conversational`). The divergence-debt note is the single honest source of truth for what's wired vs. what's not — no second narration on the spec.
- **Coverage after this cut**: FAST + CORE tiers receive Phase 2 in the prompt. DEEP tier carries it in MaiaContext + observability only (not in prompt — see divergence-debt §II.B). Most conversations are FAST/CORE; DEEP fires for explicit-depth requests.
- **Breakthrough memory substrate cut (parallel)** — complete on `feature/conversational-memory-phase2`: route `POST/DELETE/GET /api/sovereign/atoms/[id]/breakthrough` (commit `58d374334`) + loader carries `isBreakthrough` + `formatAtomsForPrompt` renders "marked as a breakthrough by the member" + `memoryHealth.breakthrough` wired at the buildMemoryHealth call site in `app/api/sovereign/app/maia/list/route.ts` + discoverable log marker `[MAIA/sovereign] breakthrough surfaced { memberIdPrefix, markedCount }`. UI placement deferred (member gesture not yet wired; first proof via authenticated `curl` or admin SQL). **Stage-language (Kelly directive 2026-05-26, contact-fidelity progression intact)**: column + API exist = Stage 3 reachable; first marked atom surfacing under authenticated load = Stage 4 verified; repeated marked surfacings across multiple turns = Stage 5 live under member use. *Breakthrough memory remains wired/reachable until a member-marked atom surfaces under authenticated load — do not let the first `breakthrough: ok` row inflate into Live.*
- **Prior session state preserved** (from 2026-05-23):
  - `sem: ok` — semantic memory live
  - `atoms loaded: 8` — atoms surface per turn
  - Default doctrine `0fa544bc4` (Keep = contextual return by default) — unchanged
  - `/maia/orientation` live, all 6 domains quiet, page reports honestly
- **Posture shift (Kelly directive)**: *"yes I want full memory in all arenas in a safe but functional way. No more hardened rules against providing the one thing that makes soulful engagement possible and makes this platform more than a chat bot."* The observation-phase freeze doctrine **remains in force as discipline** (member-marked vs system-inferred, no synthesis, provenance-grounded, no-static-UI-claim-without-verified-state) but is **no longer used to block function**. Each remaining arena (episodic, somatic, field, meta) requires its own Phase 2-equivalent spec following the conversational pattern.
- **Next actions** — refined sequencing (per `docs/architecture/STATE_AND_ROADMAP_2026-05-24.md` §9), in order — **fork → toggle → clarify-engagement-shape → verify → episodic → cleanup**:
  1. **Resolve Phase 2 fork**:
     - **Option A (preferred — generalizable foundation)**: address `docs/architecture/ADDENDA_CHANNEL_DIVERGENCE_2026-05-24.md` §V — extract shared `appendAllContextAddenda` helper, fix `buildComprehensiveVoicePrompt` to iterate `MaiaContext` addenda, wire atoms end-to-end. Then merge to `clean-main-no-secrets`, deploy, verify §IV gate across all three tiers.
     - **Option B (scoped FAST+CORE only)**: merge `feature/conversational-memory-phase2` to `clean-main-no-secrets`, deploy, verify `[MAIA] conversational-block { emitted: true, surfacedCount: N, ... }` on `sovereign/app/maia/list` for returning members **across FAST + CORE turns only** — DEEP explicitly excluded from the §IV gate. Acceptable *only* if verification claim explicitly names the FAST+CORE scope.
  2. **Member-facing recall toggle** — `conversational_recall_enabled` opt-out surface. Consent infrastructure, not polish. Ships with (1) regardless of fork outcome.
  3. **Clarify engagement shape** (per `docs/architecture/CLARIFY_ENGAGEMENT_SHAPE_2026-05-25.md`) — small parallel routing-layer refinement addressing the sacred-mirror edge case where dense unframed technical input produces felt withdrawal. Detection rule (high lexical density + high structural complexity + no explicit question) → clarification response instead of generic holding. Load-bearing principle: *"Sacred mirror should clarify the requested relation to the material, not default to silence or generic holding when the material is structurally asking to be worked."* Contained, reversible, no schema changes. Gate to builder/architect mode initially.
  4. **Verify production reality** — minisforum-side deploy state, `[MAIA] conversational-block` emission across FAST/CORE (and DEEP only after §V fix). Canonical diagnostic in **Ops diagnostic** below.
  5. **Episodic Phase 2 spec** — and only then. The threshold layer where *"life unfolding"* becomes operationally testable without jumping into field ontology. Follow conversational pattern (Phase 1 observability → Phase 2 prompt influence; locked-answer table; 4-safeguard set; `episodic_recall_enabled DEFAULT TRUE` consent gate).
  6. **Dormant service cleanup** — *after* episodic ships, not before: `QuantumFieldMemory.ts` → rename/gut → `FieldPatternMemoryService.ts` or delete; `ConsciousnessEvolutionService.ts` → `DevelopmentalTrajectoryService.ts` (strip "level increased"); reconcile duplicate `SemanticMemoryService` (consciousness/ vs memory/) — pick one, delete the other.

  **Still held under freeze, not in this sequence**: Coherence/Field layer wire-up (lift conditions per `COHERENCE_FIELD_WIRE_UP_SPEC_2026-05-24.md` §0.C unmet); Morphic / Somatic / Achievements (matrix Later with named gates); Pattern Attunement (must emerge downstream of episodic + tact); Tact calibration (sketch only after episodic ships); cross-layer synthesis; any member-facing "field state" / "coherence" / "RFI" / "UFI" surface.
- **Underlying question**: Does cross-session content surfacing, gated by consent and grounded in provenance, produce the felt continuity that makes MAIA more than a chatbot — without crossing into synthesis or interpretive displacement?
- **State of the system (six-category typology, per `docs/architecture/STATE_AND_ROADMAP_2026-05-24.md` §8 + memory `project_six_category_artifact_typology`)** — replaces earlier "honest inventory" framing. Each category is legitimate work; **collapsing 1–5 into 6 is the inflation drift to refuse**:
  - **Cat 1 — Preserved direction** (held, not authorized): 8 directions in `docs/architecture/RELATIONAL_INTELLIGENCE_DIRECTIONS_2026-05-24.md`; 10 held directions in `docs/architecture/PARTICIPATION_WITHOUT_FORECLOSURE_2026-05-24.md` §10; **RFI / UFI** (also documented as anti-drift examples per `SOVEREIGNTY_LAYER_STATE_2026_05_23.md` + `COHERENCE_FIELD_WIRE_UP_SPEC_2026-05-24.md`).
  - **Cat 2 — Canonical primitive** (interface target, no runtime authority): **FIS Field State Primitive** (`docs/canon/FIS_FIELD_STATE_PRIMITIVE.md`); Pattern Primitive; Four-Layer Substitution.
  - **Cat 3 — Built substrate** (service + migration, 0 live callers, preserved under freeze): `EpisodicMemoryService`, `CoherenceFieldService` (per `docs/architecture/MEMORY_SERVICE_STATUS_MATRIX_2026-05-24.md`).
  - **Cat 4 — Dormant service** (rename / gut / Later-with-named-gate): `QuantumFieldMemory` (810 LOC, 0 persistence — rename + gut); `MorphicPatternService` (Later — consent + aggregation gate); `SomaticMemoryService` (Later — explicit input source); `AchievementService` (Later — reframe as practice); `ConsciousnessEvolutionService` (Rename → `DevelopmentalTrajectoryService`); `MAIAMemoryArchitecture` (2351 LOC — Observe only); duplicate `SemanticMemoryService` (reconcile).
  - **Cat 5 — Frozen plan** (explicit "does not authorize" language; lift requires Kelly directive): `docs/specs/COHERENCE_FIELD_WIRE_UP_SPEC_2026-05-24.md` §0.C; `docs/architecture/MEMORY_EXPANSION_PLAN_2026-05-24.md` §5 — *"full memory field" lives here, not in cat 6.*
  - **Cat 6 — Live runtime authority** (production runtime evidence): atoms loader + `is_breakthrough` schema-bound flag (`crossing_must_be_false` sibling); substrate monitor (`runtime_events`, `deriveStatus` fall-through); Spiral Orientation Cut 2; `memoryHealth.semantic`; contextual return default (`0fa544bc4`); Daily Anchor; field context adapter (flagged); Field Lab + tester gate; Relational Navigation Room; Learning Spine Move 2 reviewer (Loop C); **Corpus Callosum substrate** (parallel multi-agent epistemic emission via `agent_runs` / `integration_passes`; 8 voices — MythicAtlas + MaiaVoice + ShadowAgent + Fire/Water/Earth/Air/Aether — firing same-second under production traffic, WisdomRouter ~49% suggests *selective integration emerging operationally, not broadcast synthesis*; default-on via `CORPUS_CALLOSUM_ENABLED !== '0'`; live on `/api/sovereign/app/maia` FAST + CORE — *unknowns preserved*: BETWEEN path zero rows despite routing-invariant set at boundary, DEEP zero rows, member-facing experiential effect unmeasured; per memory `project_corpus_callosum_substrate_cat6`).
  - **Cat 6 — branch-only, awaiting deploy + verify**: conversational Phase 2 (FAST + CORE reach prompt; DEEP blocked at `buildComprehensiveVoicePrompt` per `docs/architecture/ADDENDA_CHANNEL_DIVERGENCE_2026-05-24.md` §II.B).
  - **Strategic read** (load-bearing, Kelly 2026-05-24): *"You are not behind because RFI/UFI are not built. You are safer because you now know they are not built. That distinction may be the most important outcome of the week."*
  - **Inverse drift (omission risk, Kelly 2026-05-25)**: dormant scaffolds get narrative placement; live infrastructure stays invisible until explicitly measured. *Symmetric* to inflation drift, opposite direction. Corpus Callosum substrate (2,382 lifetime turns per elemental voice) and atoms `is_breakthrough` flag are both Cat 6 that went unnamed in earlier framings — system description was under-reporting operational reality. Discipline: any substrate generating production rows must be named explicitly; *"we built X when it's Cat 1-5"* is not the only failure mode, *"we didn't see X was Cat 6"* is the other. **Phrasing rule**: name the mechanism (*parallel epistemic emission*), not the mythology (*emergent consciousness architecture*) — metaphor after measurement, not before.
- **Important not to collapse**: *declaration is not liveness; built ≠ wired; wired ≠ surfacing; surfacing ≠ verified.* Episodic is the first measurable substrate for continuity claims — the threshold layer where "MAIA remembers a life unfolding" becomes operationally testable rather than architecturally aspirational. Until Episodic ships and stabilizes, resonant-field / coherence talk remains mostly metaphorical architecture language; only after that does it begin having a measurable substrate underneath it.
- **Ops diagnostic**:
  ```bash
  # Runtime log markers (atoms / memory health / conversational Phase 2):
  ssh soullab@minisforum 'docker logs maia-sovereign --since 1h 2>&1 | grep -E "MAIA/runtime|atoms loaded|MEMORY_HEALTH|conversational-block"'

  # Corpus Callosum substrate (parallel multi-agent emission rows):
  ssh soullab@minisforum 'docker exec maia-postgres psql -U soullab maia_consciousness -c "SELECT origin_route, processing_profile, count(*) FROM agent_runs WHERE created_at > NOW() - INTERVAL '\''24 hours'\'' GROUP BY 1,2 ORDER BY 3 DESC;"'
  ```

## Re-entry vow (for this session)

Before making changes, confirm:

- I understand what MAIA is and is not.
- I understand the ethical boundaries I must not cross.
- I understand what continuity means in this system.
- I understand what question this session is truly serving.

If this is not clear, re-read the Anchor and PROJECT_CONTEXT.md before proceeding.

---

# Project Invariants (MUST FOLLOW)

## Canon

**[MAIA Canon v1.1](./docs/canon/MAIA_CANON_v1.1.md)** governs all changes. Before implementing any feature, verify alignment with the canon's prohibitions and structural principles.

This project is governed by the **[MAIA Oath](./docs/canon/MAIA_OATH.md)**. Any change that violates the oath is invalid, regardless of technical merit.

## Infrastructure (Single Source of Truth)

**⚠️ STOP — READ THIS BEFORE ANY INFRASTRUCTURE ASSUMPTIONS ⚠️**

### What we DO NOT use (NEVER assume these)
- **NOT EC2** — The server is NOT an AWS EC2 instance
- **NOT Nginx** — We use Caddy, NOT Nginx
- **No managed hosting platforms** (Vercel, Netlify, Heroku) — we control deployment
- **No managed databases** (Supabase, PlanetScale, Neon) — PostgreSQL is self-hosted
- **No CDN/proxy middlemen** (Cloudflare) doing MITM on traffic

### What we DO use
- **Production host**: **minisforum** (LAN: `192.168.0.104`), accessed via `ssh soullab@minisforum`. Running Docker + **Caddy** (auto TLS via Let's Encrypt).
- **Public DNS for `soullab.life`** routes to the LAN's public IP → router forwards :443/:80 to minisforum. Mac Studio is **not** in the public traffic path.
- **Domain**: `soullab.life` (apex), `api.soullab.life`, `oldhead.soullab.life`, etc.
- **Reverse proxy**: **Caddy** in Docker container (`maia-caddy`) on minisforum.
- **Database**: Self-hosted PostgreSQL in Docker (`maia-postgres`) on minisforum.
- **Containers**: Docker and docker-compose.

### Production Stack (on minisforum)
All services run in Docker on **minisforum**:
- `maia-sovereign` — Main Next.js app (port 3000, Docker-internal only)
- `maia-api` — API backend (port 3001, published)
- `maia-caddy` — Reverse proxy (ports 80/443, published)
- `maia-postgres` — PostgreSQL database
- `maia-comms-worker` — Background worker
- `maia-whisper` — Speech processing
- `maia-rlm` — RLM service

### Mac Studio role
The Mac Studio (this machine, where Claude Code typically runs) hosts the active git worktrees and is the primary dev environment. It runs a parallel docker stack with the same container names and the same compose file, but **that stack is not in the public soullab.life traffic path**. A successful `docker compose up -d --build` on the Mac Studio updates the local stack only — production stays unchanged.

### Check Production Status
```bash
# Inspect the actual production container on minisforum (not the local stack):
ssh soullab@minisforum 'docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"'
ssh soullab@minisforum 'docker inspect maia-sovereign --format "{{.Created}} {{.Image}}"'

# External (hits the LAN public IP → router → minisforum):
curl -k https://soullab.life/api/health
```

### Production Deployment
- Host: **minisforum** (SSH from Mac Studio: `ssh soullab@minisforum`)
- Stack: Docker + Caddy
- Compose file: `docker-compose.production.yml`
- **Deploy lane lock (one deploy at a time — structural, not disciplinary)**: every deploy entry point (`deploy-production.sh deploy/update/migrate/rollback` and `pre-deploy-gate.sh deploy-maia`) takes an exclusive non-blocking `flock` on `/home/soullab/MAIA-SOVEREIGN/.deploy.lock` via `scripts/deploy-lock.sh`. A second deploy attempt is **refused** with the holder's PID / start time / entry point / **asserted target SHA** printed (`target_sha=`; the shared checkout's HEAD appears only as a labelled `checkout_head=` line when it differs — the record names what the deploy was asked to build, never the checkout). The kernel lock is inherited by the docker compose build and auto-releases when the whole deploy tree exits or dies — a crashed deploy cannot leave the lane locked. If a refusal shows a dead holder PID, a child of that deploy (usually the build) is still running: inspect with `fuser -v ~/MAIA-SOVEREIGN/.deploy.lock`. **Never delete the lockfile to force entry** — that detaches the kernel lock from future acquirers and re-opens the 2026-07-09 concurrent-deploy race (five processes wedged on the buildkit lock because parallel sessions' deploys could not see each other).
- **Deploy command — quick `maia`-only rebuild** (run from Mac Studio, executes on minisforum). Canonical quick path is the pre-deploy gate, which acquires the deploy-lane lock, **materializes an explicitly named immutable commit into an isolated build context** (never whatever branch is checked out — 2026-07-27 shared-checkout incident, `docs/ops/IMMUTABLE_SHA_DEPLOY.md`), runs the Co-Lab + disk gates, builds, refreshes rollback tags, swaps, then verifies the running container's `GIT_COMMIT` equals the named SHA. **`deploy-maia` now REQUIRES a SHA argument** — name the fetched remote tip so no `git checkout` of the shared working tree is needed:
  ```bash
  ssh soullab@minisforum 'cd ~/MAIA-SOVEREIGN \
    && git fetch origin clean-main-no-secrets \
    && scripts/pre-deploy-gate.sh deploy-maia "$(git rev-parse --short origin/clean-main-no-secrets)"'
  ```
  The SHA is snapshotted via `git archive`, so a concurrent session checking out a different branch cannot change the build (the deploy-lane flock serializes deploys but does NOT protect the checkout→lock interval — that gap is what the snapshot closes). No SHA + `DEPLOY_ALLOW_HEAD=1` builds the current checkout tip as an explicit ack (still snapshotted + announced). Fast, but rebuilds **only** the `maia` service — it does **not** run migrations or touch other services. It DOES refresh the rollback tags (`maia-sovereign:current`/`:previous`/`:<sha>` via shared `scripts/deploy-tag.sh`) before swapping the container.
  **The bare compose command (`GIT_COMMIT=... docker compose ... up -d --build maia`, with or without the prefix) is retired STRUCTURALLY — it now FAILS at build time.** The Dockerfile's deploy-lane tripwire (`docs/ops/DEPLOY_LANE_TOKEN.md`) refuses any build of `docker-compose.production.yml` that didn't come through `acquire_deploy_lock()` (which exports `DEPLOY_LANE_TOKEN`; the compose build-arg has deliberately no default). Why it was retired: (1) without the `GIT_COMMIT` prefix it baked `GIT_COMMIT=unknown` into the image; (2) it bypassed the deploy-lane lock, so it could race a deploy already in flight; (3) on 2026-07-10 it bypassed the pre-deploy gate and skipped rollback tagging, leaving `maia-sovereign:current` pointing at the wrong image. The failure is loud and safe — the build dies in under a second; the running container is untouched. Check which lane built the live container: `docker exec maia-sovereign printenv DEPLOY_LANE` (→ `deploy-lane`; pre-tripwire images lack the variable). Local dev (`docker-compose.yml`) and staging declare their own lane tokens in-file and are unaffected.
- **Full deploy — canonical path**: `scripts/deploy-production.sh deploy <SHA>`. The complete all-services deploy: it acquires the deploy-lane lock, **materializes the named immutable commit into an isolated build context** (same control as the quick path — `docs/ops/IMMUTABLE_SHA_DEPLOY.md`), exports `GIT_COMMIT` (from the asserted SHA) / `APP_VERSION` (read from the snapshot) / `BUILD_DATE`, builds with the provenance build-args from that snapshot, tags images per-commit for rollback, brings the stack up, verifies running provenance, and runs DB migrations (migrations execute from the snapshot too). `deploy` REQUIRES a SHA (or `DEPLOY_ALLOW_HEAD=1` ack); `update` pulls a branch then builds the pulled tip as an immutable snapshot. Use it for schema changes, multi-service changes, or whenever you want a rollback point — i.e. anything beyond a quick `maia`-only code rebuild. **Post-swap provenance verify is fail-closed on every path** (`deploy`, `update`, `deploy-maia`): if the running container does not report the authorized SHA, the deploy ABORTS before migrations/smoke and points at `rollback` — a mismatch never silently proceeds.
- **macOS dev-stack caveat (immutable-SHA build context)**: the deploy materializes the build context as a `git archive` snapshot under `$TMPDIR`, and the `migrate` service bind-mounts that snapshot's `database/migrations`. On **minisforum (Linux, production) this is transparent** — `/tmp` binds into containers freely. On the **Mac Studio parallel stack**, Docker Desktop must be able to share the snapshot path, or the migrate mount fails; if so, set `DEPLOY_CONTEXT_DIR=<a Docker-Desktop-shared path>` before deploying. Production is unaffected. Full detail + the `DEPLOY_CONTEXT_DIR` / `DEPLOY_ALLOW_HEAD` seams: `docs/ops/IMMUTABLE_SHA_DEPLOY.md`.
- **Verify after deploy**:
  ```bash
  # 1. Container freshness
  ssh soullab@minisforum 'docker inspect maia-sovereign --format "{{.Created}}"'

  # 2. LAN IP sanity (must match router port-forward target, expected 192.168.0.104)
  ssh soullab@minisforum 'hostname -I'

  # 3. Public reachability (external path through DNS + router forward)
  curl -k https://soullab.life/api/health

  # 4. Provenance — what commit is actually live (must be the SHA you deployed, NOT "unknown")
  ssh soullab@minisforum 'docker exec maia-sovereign printenv GIT_COMMIT'
  ```
  `Created` must show a timestamp under a minute old. `hostname -I` must show `192.168.0.104` as the LAN IP; if it shows anything else, the router's port-forward is pointing at a stale IP and external/iOS traffic will silently fail (see the LAN IP drift trap above). `/api/health` must return fresh JSON with `uptime` near zero. `printenv GIT_COMMIT` must return the short SHA you deployed.

  **Diagnostic — `GIT_COMMIT=unknown` does NOT imply missing provenance wiring.** The chain is already complete: Dockerfile (`ENV GIT_COMMIT=${GIT_COMMIT}`) ← compose (`build.args.GIT_COMMIT`) ← deploy (`scripts/deploy-production.sh` exports it; the quick command must prefix it). `unknown` means the deploy route *bypassed* that chain — almost always the quick command run **without** the `GIT_COMMIT=$(git rev-parse --short HEAD)` prefix. So if you see `unknown`, first verify **which deploy path was used** before suspecting the build-arg wiring. **Second cause (2026-09-03): the image can be stamped while the container is not** — compare `docker image inspect maia-sovereign:current` against `docker inspect maia-sovereign` `Config.Env`; a runtime `environment: GIT_COMMIT` override in a stale checkout compose rewrites the image's identity at `up`. The repaired gate refuses that override and verifies both channels.
- CI deploys are disabled (self-hosted runner not yet configured).
- **Common deploy mistake**: rebuilding on the Mac Studio instead of minisforum. The local stack will report healthy and `Created` will update, but the public soullab.life traffic continues hitting minisforum's old container. Always verify with the minisforum-side `Created` check above, not just the local one.

### Why This Architecture
- No third party sits between users and their data
- No jurisdiction concerns — we control the location
- Complete air-gap capability if needed for local-only mode

## Database & Backend

- **We do NOT use Supabase.** Never introduce Supabase. Use local PostgreSQL via `lib/db/postgres.ts` only.
- **If you see Supabase in code, remove it; do not consolidate it.**
- Database: Local PostgreSQL at `postgresql://soullab@localhost:5432/maia_consciousness`
- Database client: `lib/db/postgres.ts` (uses `pg` npm package)
- Never add `@supabase/*` imports, RLS policies, or Supabase migrations
- Enforcement: `npm run check:no-supabase` blocks violations (runs in pre-commit hook)

## MAIA Sovereignty

- Primary AI: Claude (Anthropic) via `ANTHROPIC_API_KEY`
- Fallback: Local Ollama (DeepSeek models) when API unavailable
- Never use OpenAI or other cloud AI providers
- Voice: Local TTS/STT or browser APIs only
- Data: Local PostgreSQL, never cloud databases

## Sanctuary Mode (Memory Consent)

MAIA supports **Sanctuary sessions** — conversations that remain useful in the moment but do not enter long-term memory.

### Invariants

1. **No content retention** — Sanctuary sessions are not stored, indexed, or used for pattern formation
2. **No training data** — Sanctuary content never enters any model training pipeline
3. **Minimal metadata** — Only log that a sanctuary session occurred (timestamp, duration) — never content
4. **Visual clarity** — User must see unambiguous indication that Sanctuary is active
5. **Default off** — Regular sessions build memory; Sanctuary is an explicit opt-in
6. **Absolute boundary** — Nothing from a Sanctuary session can be saved, extracted, inferred, or converted into long-term memory, under any circumstances, including by user request during the session

### Why This Exists

Real honesty requires safety. People won't speak freely to a system that might later monetize or weaponize their vulnerability.

Sanctuary is the architectural proof that MAIA serves the person — not the data model.

### UI Copy

**Toggle:**
> Sanctuary Mode — This session won't be remembered. Speak freely.

**Learn more:**
> Sanctuary sessions are useful in the moment, then gone. No patterns formed. No memories stored. Just presence.

## Onboarding Flow (One-Time Per Member)

The onboarding journey happens exactly once per member — whether beta testers or those gifted a passkey. After completion, users are redirected directly to `/maia`.

```
/begin
   ↓ (click "Begin Journey")
/intro-maia
   ↓ ("I'm Maia" greeting, click "Continue")
/intro-daimon
   ↓ ("I am a Daimon by design", click "Enter the Lab")
/test-elemental
   ↓ (passkey entry, password setup, "Before we begin..." orientation)
/faq
   ↓ (FAQ section)
/onboarding
   ↓ (preferences, complete)
/maia
```

### Invariants

1. **Universal flow** — All members (beta testers, gifted passkeys, future users) follow the same onboarding
2. **Single entry point** — New users start at `/begin`
3. **One-time flow** — Once `onboarded: true`, users skip directly to `/maia`
4. **No shortcuts** — Each step must be completed in sequence
5. **Returning users** — `/signin` for existing members to sign in each session
6. **New user link** — `/signin` includes amber "New to Soullab? Begin Journey" link → `/begin`

### Pages

- `/signin` — Returning user sign in (amber link to `/begin` for new users)
- `/begin` — Landing page with Holoflower and "Begin Journey" button
- `/test-elemental` — `SacredSoulInduction` (passkey/password) then `ElementalOrientation`
- `/faq` — `FAQSection` component
- `/onboarding` — `CompleteWelcomeFlow` (preferences)
- `/maia` — Main app

### Completion Flag

Stored in both:
- Server-side: `members.onboarded = true` (PostgreSQL)
- Client-side: `localStorage.beta_user.onboarded = true` (session cache)

## Members System (Cross-Device Recognition)

Server-side member management enables users to be recognized across devices.

### Database

Table: `members` (migration: `database/migrations/20260103000001_members.sql`)
- `id` — UUID primary key
- `passkey` — Unique passkey (SOULLAB-NAME format or universal key)
- `username` — Unique username for sign-in
- `password_hash` — SHA256 hashed password
- `name` — Display name
- `email` — For passkey recovery
- `onboarded` — Boolean completion flag
- `onboarding_step` — Current step: begin, test-elemental, faq, onboarding, complete

### API Endpoints

| Endpoint | Method | Purpose |
|----------|--------|---------|
| `/api/members/check` | POST | Check if passkey exists (new vs returning) |
| `/api/members/register` | POST | Register new member during onboarding |
| `/api/members/signin` | POST | Authenticate returning member |
| `/api/members/recover` | POST | Send passkey recovery email |
| `/api/members/progress` | GET/POST | Get/update onboarding progress |

### Flow

1. **New user enters passkey** → Check server → Not found → Continue to registration
2. **Registration** → Create member in PostgreSQL → Store session in localStorage
3. **Returning user** → Sign in with username/password → Server validates → Store session
4. **Different device** → Sign in page → Server recognizes by username → Cross-device access

### Recovery

Users who forget their passkey can request email recovery:
1. Click "Forgot your passkey?" on `/signin` or `/test-elemental`
2. Enter email address
3. Server sends passkey + username via Resend
4. User returns to sign-in

## Bridge D: Spiral State Persistence (Anti-Regression Layer)

Prevents MAIA from treating returning members like brand-new people. NOT personalization. NOT psychometrics. Just continuity.

### What Gets Persisted

Table: `member_spiral_state` (migration: `database/migrations/20260213200001_member_spiral_state.sql`)
- `dominant_element` — Current element (fire/water/earth/air/aether) from conductor hysteresis
- `phase` — Spiral phase (1-12)
- `motion` — Movement pattern (ascending/stuck/breakthrough, nullable)
- `intensity` — Signal strength (0-1, nullable)
- `relational_phase` — Maturation stage (1=orientation, 2=capacity, 3=autonomy, 4=seasonal return)
- `autonomy_streak` — Consecutive autonomous sessions
- `return_count` — Times returned after autonomy

### Implementation

Module: `lib/consciousness/spiralStatePersistence.ts`
- `loadSpiralState(memberId)` — Read at conversation start (graceful fallback on error)
- `upsertSpiralState(memberId, update)` — Fire-and-forget write (never blocks oracle)

Wire points in `app/api/oracle/conversation/route.ts`:
1. **Load early** (line ~415): `const spiralState = await loadSpiralState(userId);`
2. **Pass to conductor** (line ~1049): `persistedState: { dominant_element, phase }`
3. **Upsert late** (line ~1067): `upsertSpiralState(userId, { element, phase, motion, intensity });` (fire-and-forget)

Conductor enhancement (`lib/voice/conductor.ts`):
- If `persistedState` exists and member has no hysteresis buffer, seed from database
- Prevents element reset on server restart

### Design Principles

1. **Fire-and-forget writes** — like voiceSovereignty pattern (no await, no blocking)
2. **Graceful fallback on read** — if load fails, conversation continues normally
3. **No conversation content** — only structural position (element/phase/motion)
4. **Upsert-safe** — first insert creates, updates modify existing
5. **Server restart resilient** — hysteresis buffer seeds from DB if empty

### Verification

```bash
# Check migration applied
psql -U soullab maia_consciousness -f scripts/verify-bridge-d-db.sql

# Verify continuity
# 1. Have conversation (3+ turns)
# 2. Restart server
# 3. Continue conversation
# Expected: Element maintained from database, not reset to default
```

See: `docs/bridge-d-verification.md` for full verification guide.

## Architecture

- This is a Next.js 16 app using Turbopack
- Voice modes: Talk (dialogue), Care (counsel), Note (scribe)
- Processing paths: FAST (<2s), CORE (2-6s), DEEP (6-20s)
- Consciousness framework: Spiralogic (see `/lib/maia/spiralogicReference.ts`)

## Co-Lab Release Gate (MANDATORY before tester invites)

**No invite unless `scripts/verify-constitution-colab.ts` passes with 0 failed in production.**

Run inside the container on minisforum:
```bash
docker exec maia-sovereign sh -c 'DATABASE_URL="$DATABASE_URL" npx tsx scripts/verify-constitution-colab.ts'
```

Pass condition: `0 failed` (the script exits non-zero on any failure).
Last observed in production 2026-09-06 on runtime `ca5fdff44`: `33 passed · 0 failed · 0 warned`
(read-only run executed in-session via the founder's connected host — not a manual founder execution).

⚠️ The filename `verify-colab-boundaries.ts` in earlier copies of this file **never existed on any
branch**, so the mandatory pre-invite gate could not be run as written. The gate is the `failed`
column, never the total — a total moves whenever checks are added, and a total quoted without a run
behind it is a claim, not evidence.

This gate runs automatically as part of `scripts/deploy-production.sh` smoke tests. It must also be run manually before any tester wave. See `docs/ops/COLAB_RELEASE_GATE.md` for the full gate specification — what it checks, which surfaces trigger it, and how to add new checks when new scoped surfaces ship.

Triggers: Co-Lab changes · Studio people · DMs · sessions/encounters · files · memory atoms · onboarding · invitations/roles · any migration touching those tables.

## Before Making Changes

1. Search codebase for existing implementations
2. Run `npm run check:no-supabase` to verify no Supabase violations
3. Run `npm run preflight` for full sovereignty check
4. Run `npm run typecheck` for TypeScript validation — the enforced **no-regression gate**. It runs the application-wide `tsconfig.ship.json` (~3,965 files, including `app/**`, `components/**`, `middleware.ts`) and compares against `typecheck-baseline.json`. It **fails** on a new diagnostic, an increased occurrence count, or a path that left the program while still existing on disk.
   - `npm run typecheck:full` — complete current diagnostic inventory (239 pre-existing errors as of 2026-07-30; this is debt, not a gate).
   - `npm run typecheck:entrypoint` — narrow single-entrypoint check of `app/api/between/chat/route.ts` only.
   - `npm run typecheck:baseline` — **dry run only.** Prints a before/after summary and names any error it would bless, then refuses to write. Recording requires the explicit `npm run typecheck:baseline -- --accept-current`. Re-baselining is a governed act: use it to lock in fixes or an intentional, reviewed coverage change — never to absorb a new error.
   - ⚠️ **`npm run typecheck` green is not proof that everything typechecks** — it is proof that nothing got *worse*. For the absolute state, use `typecheck:full`.
   - ⚠️ Historical note: before 2026-07-30, `npm run typecheck` checked **one file** (`app/api/between/chat/route.ts`) and its import graph — 409 files, zero `.tsx`, zero `components/**`. Any pre-2026-07-30 lane citing "typecheck passes" as evidence was citing that entrypoint smoke, not application validation. See `docs/ops/TYPECHECK_GATE_COVERAGE_AUDIT_2026-07-30.md`.
5. Test with `npm run smoke` before committing
6. **Sovereignty Invariant Check** — For any feature that touches voice, expression, relational tone, or user-facing behavior, ask:
   - Does this increase user agency?
   - Does this push life outward into the world?
   - Does this reduce the system's psychological centrality over time?
   - **Cultural sovereignty / Invariant 14:** Are we imposing a framework, translating the member's meaning into our vocabulary, or assuming "self," "growth," "healing," "family," or "spirit" mean the same everywhere? If uncertain, ask more, preserve the member's language, or gate the feature behind member-initiated use.
   - If the honest answer to any is no, the feature does not ship. (See `docs/canon/MAIA_SOVEREIGNTY_INVARIANTS.md`)

   **Growth-obligation check** (founder-added 2026-08-04; see `docs/canon/RECIPROCAL_SOVEREIGNTY_INTENTION_2026-08-04.md`) — for any change that increases a capability (memory, personalization, pattern recognition, wisdom integration, autonomy), also answer:
   - What uncertainty does this introduce, and how is that uncertainty preserved?
   - What provenance and ownership boundaries does this require?
   - What new responsibility does this capability create?

   These are answered, not passed: a change that cannot answer them is incomplete work. The governing law: *every increase in capability must produce a matching increase in provenance, restraint, and transparency.*

## Setup (New Clones)

After cloning this repo, run once:
```bash
./scripts/setup-githooks.sh
```

This configures versioned git hooks that enforce sovereignty on every commit.

# context-mode — MANDATORY routing rules

You have context-mode MCP tools available. These rules are NOT optional — they protect your context window from flooding. A single unrouted command can dump 56 KB into context and waste the entire session.

## BLOCKED commands — do NOT attempt these

### curl / wget — BLOCKED
Any Bash command containing `curl` or `wget` is intercepted and replaced with an error message. Do NOT retry.
Instead use:
- `ctx_fetch_and_index(url, source)` to fetch and index web pages
- `ctx_execute(language: "javascript", code: "const r = await fetch(...)")` to run HTTP calls in sandbox

### Inline HTTP — BLOCKED
Any Bash command containing `fetch('http`, `requests.get(`, `requests.post(`, `http.get(`, or `http.request(` is intercepted and replaced with an error message. Do NOT retry with Bash.
Instead use:
- `ctx_execute(language, code)` to run HTTP calls in sandbox — only stdout enters context

### WebFetch — BLOCKED
WebFetch calls are denied entirely. The URL is extracted and you are told to use `ctx_fetch_and_index` instead.
Instead use:
- `ctx_fetch_and_index(url, source)` then `ctx_search(queries)` to query the indexed content

## REDIRECTED tools — use sandbox equivalents

### Bash (>20 lines output)
Bash is ONLY for: `git`, `mkdir`, `rm`, `mv`, `cd`, `ls`, `npm install`, `pip install`, and other short-output commands.
For everything else, use:
- `ctx_batch_execute(commands, queries)` — run multiple commands + search in ONE call
- `ctx_execute(language: "shell", code: "...")` — run in sandbox, only stdout enters context

### Read (for analysis)
If you are reading a file to **Edit** it → Read is correct (Edit needs content in context).
If you are reading to **analyze, explore, or summarize** → use `ctx_execute_file(path, language, code)` instead. Only your printed summary enters context. The raw file content stays in the sandbox.

### Grep (large results)
Grep results can flood context. Use `ctx_execute(language: "shell", code: "grep ...")` to run searches in sandbox. Only your printed summary enters context.

## Tool selection hierarchy

1. **GATHER**: `ctx_batch_execute(commands, queries)` — Primary tool. Runs all commands, auto-indexes output, returns search results. ONE call replaces 30+ individual calls.
2. **FOLLOW-UP**: `ctx_search(queries: ["q1", "q2", ...])` — Query indexed content. Pass ALL questions as array in ONE call.
3. **PROCESSING**: `ctx_execute(language, code)` | `ctx_execute_file(path, language, code)` — Sandbox execution. Only stdout enters context.
4. **WEB**: `ctx_fetch_and_index(url, source)` then `ctx_search(queries)` — Fetch, chunk, index, query. Raw HTML never enters context.
5. **INDEX**: `ctx_index(content, source)` — Store content in FTS5 knowledge base for later search.

## Subagent routing

When spawning subagents (Agent/Task tool), the routing block is automatically injected into their prompt. Bash-type subagents are upgraded to general-purpose so they have access to MCP tools. You do NOT need to manually instruct subagents about context-mode.

## Output constraints

- Keep responses under 500 words.
- Write artifacts (code, configs, PRDs) to FILES — never return them as inline text. Return only: file path + 1-line description.
- When indexing content, use descriptive source labels so others can `ctx_search(source: "label")` later.

## ctx commands

| Command | Action |
|---------|--------|
| `ctx stats` | Call the `ctx_stats` MCP tool and display the full output verbatim |
| `ctx doctor` | Call the `ctx_doctor` MCP tool, run the returned shell command, display as checklist |
| `ctx upgrade` | Call the `ctx_upgrade` MCP tool, run the returned shell command, display as checklist |
