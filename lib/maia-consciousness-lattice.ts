/**
 * SOVEREIGNTY: maia-consciousness-lattice tombstoned.
 *
 * Previously used or passed OpenAI API key/fetch calls.
 * MAIA operates under a zero OpenAI runtime access doctrine.
 * Either @ts-nocheck prototype or all callers are prototypes — not in serving path.
 * Retained as tombstone to preserve git history.
 *
 * See lib/ai/openaiPolicy.ts.
 */

export {};
