// backend
// Minimal stub to unblock compilation.
// Safe default: returns null (no primer injected).

export function getWisdomPrimerForUser(
  _arg: string | { userId?: string | null; mode?: string | null }
): string | null {
  return null;
}
