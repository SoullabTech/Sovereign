/**
 * SOVEREIGNTY: AnthonyElementalVoice tombstoned.
 *
 * Previously used PersonalizedVoiceService (OpenAI TTS) for elemental masculine voice.
 * PersonalizedVoiceService is now tombstoned under zero-OpenAI doctrine.
 * No live callers — dead prototype retained for git history.
 *
 * See lib/ai/openaiPolicy.ts.
 */

export {};
