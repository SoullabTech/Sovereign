# Android Release Signing Configuration
# Copy this file to .env.android and fill in your actual values
# DO NOT commit .env.android to git!

# Path to your Android keystore file (relative to project root)
ANDROID_KEYSTORE_PATH=./android-release.keystore

# Keystore password (used when creating the keystore)
ANDROID_KEYSTORE_PASSWORD=your_keystore_password_here

# Key alias (name of the key in the keystore)
ANDROID_KEY_ALIAS=maia-release

# Key password (can be same as keystore password)
ANDROID_KEY_PASSWORD=your_key_password_here

# To generate a keystore, run:
# keytool -genkey -v -keystore android-release.keystore \
#   -alias maia-release \
#   -keyalg RSA -keysize 2048 -validity 10000

# To build a signed release APK:
# source .env.android
# npm run android:release
