# Observation Declaration — now-what — 2026-07-30

**Immutable for this sitting.** Every observation in this study belongs to the build below.
No mixed evidence. No switching halfway through. Verify with:

```bash
bash scripts/field-study-scaffold.sh now-what --verify
```

> ⚠️ **This declaration was corrected by hand after scaffolding.** The scaffold's
> auto-stamped values are preserved verbatim beside the corrections. They are **not**
> cleaned up, because the discrepancy is itself calibration evidence for Q1. See
> `13-instrument-ledger.md`.

## Implementation under study

The scaffold provides one `Commit` field. This sitting required **three** distinct
identities, which that single field silently collapsed.

| Object | Value | Establishes |
|---|---|---|
| **Deployed instance** (authoritative) | `db245336d` | What members can actually encounter |
| **Clean checkout** (Read basis) | `db245336d`, detached worktree, 0 dirty paths | What code produced that instance |
| **Content fingerprint** | see instrument section | Whether the examined artifact is equivalent |

- **Field:** now-what
- **Environment:** **production soullab.life**, deployed instance `db245336d`
  (verified: `docker exec maia-sovereign printenv GIT_COMMIT`)
- **Read basis:** detached worktree at `db245336d`, 0 uncommitted paths
- **Date:** 2026-07-30

⛔ **The checkout may support Read evidence. It may never substitute for observation of the
deployed instance.**

### Scaffold auto-stamped values — PRESERVED, SUPERSEDED

```text
Commit:                    6f0e6d6c8
Branch:                    chore/preserve-larry-experience-audit
Working-tree fingerprint:  67342a29300b (331 uncommitted paths)
```

**Why superseded.** That checkout is **987 lines behind production across 6 files** on the
Now What? surface, and is missing `lib/nowWhat/invitation.ts` (the invitation gate),
`lib/nowWhat/rooms.ts`, `app/now-what/arrive/page.tsx`, and 431 lines of tests entirely.
`app/now-what/arrive/` is present only as an **untracked local draft**. Observations taken
against it would have described a Now What? no member has ever encountered.

## Study instrument (declared separately from the observed build)

- **Instrument fingerprint:** `84dde085c5d8` — ✅ **verified byte-identical** at
  `b5faaf622`, at `ea39fe3b0`, and in the working tree
- **Review provenance (what was reviewed):** `b5faaf622`
- **Merged artifact (what carries authority on trunk):** `ea39fe3b0`
- **Instrument files:**
  - `scripts/field-study-scaffold.sh`
  - `.claude/skills/field-study/SKILL.md`
  - `docs/specs/developmental-environment/FIELD_STUDY_METHOD_CANDIDATE_2026-07-29.md`

### Scaffold auto-stamped instrument value — PRESERVED, SUPERSEDED

```text
Instrument last commit:  5a1e7e396
```

**Why superseded.** `5a1e7e396` was rebased away and is recorded as **superseded
provenance, never a declaration target**. The scaffold read it from local branch history.
⭐ The fingerprint was simultaneously **correct** — a live demonstration that *the
fingerprint establishes instrument equivalence; the SHA establishes review provenance.*
The fingerprint held while the SHA misreported.

## Fingerprint algorithm and exclusions

- **Observed build:** `git rev-parse --short HEAD` + branch + `git status --porcelain`
  filtered through the exclusion rule, hashed with `shasum`, truncated to 12 chars.
- **Study instrument:** `shasum` of each instrument file, hashed again, truncated to 12.
  Content-based, not git-based.
- **Exclusion rule (the ONLY one):** `docs/field-studies/`
- **Why excluded:** generated by the observation process itself.

**Narrowness is load-bearing.** No other path may be excluded. A curated exclusion list
turns the fingerprint from a detector of drift into an opinion about drift.

## Evidence boundary

Established from production's access configuration (`middleware.ts:290`,
`config/accessMatrix.ts:53–70`), not assumed.

**Walkable unauthenticated (pre-authorization surface):**
- `/now-what` — public; edge-redirects to `/now-what/room`
- `/now-what/welcome` — public (40 lines, contains a redirect)
- `/now-what/arrive` — public; the environment's own door (339 lines)
- `/now-what/pitch` — public static slideshow (out of the practice entry path)
- **The redirect seam itself** — unauthenticated `/now-what/room` → `/now-what/arrive`
  (Kelly ruling 2026-07-16: the environment's own door, not generic `/signin`)

**Read only (production-equivalent checkout, not directly encountered):**
- `field` (237) · `map` (16) · `next` (196) · `position` (223) · `questions` (201, gated)
  · `reflections` (105) · `room` (63) · `themes` (108)

**Unavailable — appropriately preserved authorization boundary:**
- All post-door states. `/now-what/room` is `minTier: free`; the participant states behind
  it require a member record and an invitation **only Larry can mint**.
- ⛔ This observer will **not** mint production auth or fabricate a participant record.

> ⭐ **Unavailable due to an appropriately preserved authorization boundary is not a product
> defect.** It is recorded in the **governance ledger**. It becomes a *product* observation
> only if the environment fails to explain the boundary honestly — which is itself walkable
> at the redirect seam, and is tested in this sitting.

**Unknown (neither observation nor code evidence establishes it):**
- Whether any participant has completed a return cycle; all temporal-gravity claims about
  actual return behavior.

## Known divergence from production

**None for the observed surface.** The Read basis is a clean checkout at the deployed SHA.
Production is 11 commits behind trunk; **none of the 11 touch the Now What? surface**
(verified by path-scoped diff).

⚠️ **Standing governance condition, recorded not resolved by this study:** the invitation-gate
change (#802, `5e1c76092`) **is live in this instance** and its provenance-stamped
physical-device acceptance walk is **still outstanding**. This sitting observes a surface
carrying an unaccepted change. ⛔ **This study does not and cannot discharge that acceptance
gate** — a desktop environmental study is a different instrument answering a different
question.

## Study ethics declaration

- Member content read: **none**
- Sanctuary content: **categorically excluded**
- Evidence deliberately foregone (conclusions left provisional as a result):
  - Real participant return behavior — would be the best evidence for temporal gravity.
    Logged **Needs consent**; not reached for.
  - Post-invitation participant states — logged **Unavailable**; no auth minted.
