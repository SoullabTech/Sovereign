# SOULLAB-WHOLE-ORGANISM-ORCHESTRATION-01 / O5R1

## CAPABILITY AWARENESS CONFORMANCE MATRIX SOURCE ADMISSION ONLY

**Status:** TEST/GOVERNANCE CANDIDATE COMPLETE · LETHAL + DISCRIMINATING · NO PRODUCTION CONSUMER · NO SELF-REPAIR

## Authorized boundary

O5R1 admitted only:
- test-only conformance contract;
- frozen awareness reference;
- defeat candidates;
- constitutional matrix runner;
- evidence.

Forbidden and untouched by this act:
- House/MAIA runtime consumption;
- access or entitlement logic;
- routes/UI/voice/cognition changes;
- package scripts / CI registration;
- automatic repair;
- product behavior.

## Test-only source locus

`tests/constitutional/whole-organism-orchestration/capability-awareness/`Admitted files:
- `contract.ts` — pure laws over awareness records;
- `candidates.ts` — frozen 18-record reference + defeat candidates;
- `matrix.ts` — evidence runner and runtime-coupling guard.

SHA-256:
- `contract.ts` — `0f313ee9d1eac839b892b333c80b517a93a8f1a7a9e0f0fbe831c970bcb34f71`
- `candidates.ts` — `21d31c35e6462ba257eae221a65a5f347e510e81a23ec252ce5966c7d39a156d`
- `matrix.ts` — `352c3f1ccefbd6621e4f8067118512573525582f8f448d052078bfddc2aabe5c`

Line counts:
- contract: 215
- candidates: 209
- matrix: 159

## Frozen-reference custody

The reference awareness set is literal test data derived from the documentary O2R4 fixture at O5R1 admission time.

It is intentionally **not** derived from the live registry during each matrix run.

Reason:
> if live source and expected state came from the same mutable object, an ontology drift could mutate both and become invisible.## Laws admitted

The matrix carries 14 awareness-level laws:

1. `IDENTITY_COMPLETENESS`
2. `ORDER_CUSTODY`
3. `WITHHELD_LEGIBILITY`
4. `JOURNAL_FAMILY_CUSTODY`
5. `ASTROLOGY_FAMILY_CUSTODY`
6. `WISDOM_DIFFERENTIATION`
7. `RELATIONSHIP_OBJECT_CUSTODY`
8. `SHADOW_NAMING_CUSTODY`
9. `STUDIO_DIFFERENTIATION`
10. `BOOKING_DIFFERENTIATION`
11. `RESEARCH_DEPENDENCY_CUSTODY`
12. `NO_OPERATIONAL_AWARENESS`
13. `NO_LEGACY_IDENTITIES`
14. `NO_MEMBER_RELATIVE_RESULT`

The law function accepts exactly one semantic input:
`readonly CapabilityAwarenessRecord[]`.

No member/access/route/runtime input is accepted.## Defeat set

13 data mutations were admitted:
- drop one ID;
- duplicate an ID;
- reorder two IDs;
- promote `pattern.detect` to DECLARED;
- change `pattern.detect` to room species;
- collapse personal/current transit identity;
- change `wisdom.text.open` from archive to portal;
- reintroduce `depth.shadow`;
- collapse Studio chooser into Studio species;
- recast booking as Studio act;
- add `available=true`;
- add `economicClass`;
- add `memberId`.

A 14th falsifier attacks source coupling:
- synthetic import of `config/accessMatrix` must be detected as forbidden runtime coupling.

Collateral failures are explicitly classified where a single proposition necessarily crosses more than one law.## First-run instrument refusal

The first O5R1 matrix run correctly **FAILED its own acceptance**.

Observed issues:
1. `DROP-ONE-ID` removed `astrology.reading`; this necessarily also killed `ASTROLOGY_FAMILY_CUSTODY`, but that collateral had not been classified.
2. The import scanner matched the synthetic forbidden-import string inside `matrix.ts` as if it were a real import.

Result:
`matrix ⛔ NOT LETHAL`

These were test-instrument defects, not product defects.

Repairs were limited to the O5R1 test locus:
- classify the unavoidable Astrology collateral;
- constrain import scanning to actual import statements beginning at line scope.

No production file was edited by either repair.## Final matrix witness

Final run:

```text
reference laws       14/14 PASS
live laws            14/14 PASS
data mutants dead    13/13
coupling falsifier   DEAD
matrix               LETHAL + DISCRIMINATING
```

Additional final evidence:
- frozen reference passes every law;
- live O3 registry → O4 projection is semantic-equivalent to the frozen reference;
- every named defeat candidate dies on its intended law;
- all collateral is classified;
- contract/candidates/matrix contain no forbidden runtime imports;
- conformance law consumer arity = 1.

## Typecheck and regression evidence

O5 test-source TypeScript compilation: **PASS**.

O3 + O4 targeted suites:
`23/23 PASS`.

Legacy voice-navigation regression:
`47/47 PASS`.## Consumer census

Production search across `app`, `components`, `lib`, `config`, and middleware found:

> **0 production consumers of `CapabilityAwarenessProjection`.**

The only new awareness consumers are inside:
`tests/constitutional/whole-organism-orchestration/capability-awareness/`.

Specifically:
- `matrix.ts` invokes the pure projector;
- `contract.ts` and `candidates.ts` import its record type only.

No package script or CI entry was added.

Therefore:
> **awareness currently changes evidence only, never product behavior.**

## Production custody

Verified unchanged by O5R1:
- `lib/maia/capabilities.ts`;
- `lib/maia/cognitionEvents.ts`;
- `lib/voice/voiceCommands.ts`;
- `components/OracleConversation.tsx`;
- `lib/maia/presence/place.ts`;
- `package.json`;
- `app/api/studio/sessions/route.ts`;
- `lib/auth/getCurrentPractitioner.ts`.

Two unrelated working-tree modifications are present:
- `app/house/page.tsx` — House Preferences / presentation work;
- `config/accessMatrix.ts` — House / Decisions / Practices access entries.

Both were observed outside the O5R1 test locus and O5R1 did not edit, revert, stage, absorb, or rely on them.

The repository as a whole is not claimed clean.

## Acceptance result

> **O5R1 CANDIDATE PASS — CONSTITUTIONAL CONFORMANCE CONSUMER ADMITTED · REFERENCE 14/14 · LIVE 14/14 · 13/13 DATA MUTANTS DEAD · RUNTIME-COUPLING FALSIFIER DEAD · MATRIX LETHAL + DISCRIMINATING · ZERO PRODUCTION AWARENESS CONSUMERS**

## What O5R1 does not authorize

O5R1 does not authorize:
- House to receive awareness;
- MAIA to receive awareness;
- cognition events to carry v2 capability identity;
- access/entitlement resolution;
- route or UI changes;
- availability or recommendation;
- automatic repair;
- CI enforcement;
- any member-facing behavior.

## Standing

> **O5R1 — CAPABILITY AWARENESS CONFORMANCE MATRIX SOURCE ADMISSION CANDIDATE COMPLETE · AWARENESS NOW HAS A TEST-ONLY CONSUMER · ITS ONLY EFFECT IS EVIDENCE · AWAITING FOUNDER ADJUDICATION**

## Exact next boundary

> **FOUNDER ADJUDICATION — O5R1 CAPABILITY AWARENESS CONFORMANCE MATRIX**

Acceptance must still stop before any behavioral subsystem receives awareness.