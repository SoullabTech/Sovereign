# FOUNDER ADJUDICATION — SOULLAB-WHOLE-ORGANISM-ORCHESTRATION-01 / O8R1

## EXPLICIT CAPABILITY INQUIRY RESOLVER + UTTERANCE CONFORMANCE MATRIX

## I. DISPOSITION

Accept O8R1 as:

> **PASS · DESCRIPTION-ONLY RESOLVER/COMPOSER CONFORMANCE ACCEPTED · FIXED THREE-ID PILOT · 7/7 ACCEPTED · 20/20 REFUSALS · 15/15 FALSIFIERS DEAD · PLATFORM MAP WITNESS MATCH · ZERO PRODUCTION CONSUMERS**

Accepted laws:
- exact-name resolution only;
- WHAT_IS / WHAT_DOES_MEAN envelopes only;
- action / availability / navigation / ambient / fuzzy / non-pilot inputs abstain;
- ABSTAIN composes to nothing;
- deterministic exact name + purpose output;
- no contextual inference;
- no offer;
- no routing;
- no availability;
- no member-relative payload;
- no runtime coupling.

## II. RUNTIME AUTHORITY NOT GRANTED

O8R1 acceptance does not authorize:
- MAIA to call the resolver;
- House to call the resolver;
- runtime description emission;
- any new response route;
- any availability or entitlement semantics;
- any suggestion or CTA;
- capability invocation;
- persistence or telemetry beyond existing runtime laws;
- any member-visible behavior.

> **Conformance is accepted. Runtime adoption remains closed.**

## III. ACTIVE SERVING-ROUTE FINDING

The currently authoritative live MAIA serving route is:
`app/api/sovereign/app/maia/list/route.ts`

Two adjacent alternatives are not valid targets:
- `app/api/oracle/conversation/route.ts` is hard-disabled with HTTP 410;
- `app/api/sovereign/app/maia/route.ts` declares itself dormant and superseded by `/list`.

Therefore any future pilot consumer design must target `/api/sovereign/app/maia/list` only.

## IV. LOAD-BEARING DURABILITY FINDING

The active `/list` route contains an explicit F1 TURN ACCEPTANCE BOUNDARY.

At that boundary:
- request body has been parsed;
- member identity has been resolved from verified session credentials;
- message validity has been established;
- Sanctuary posture is known;
- a canonical `exchangeId` has been fixed;
- recognized non-Sanctuary member text is durably written before deeper processing.

Later, after MAIA text exists, the assistant half is written with:
- the same user;
- the same session;
- role `assistant`;
- the same `exchangeId`.

This paired identity is load-bearing.

## V. FIRST RUNTIME-SEAM RULING

If runtime adoption is later authorized, the description-only resolver must be considered only at a **pre-cognition intercept after F1 durable member-turn acceptance**.

It must not be placed:
- before message validation;
- before identity resolution;
- before F1 member-turn durability;
- inside general intent routing;
- inside capability-offer/capability-available cognition;
- inside model prompt composition.

If the resolver returns ABSTAIN:
> **normal MAIA processing proceeds unchanged.**

If it returns DESCRIBE:
> **the deterministic description becomes the assistant text for the same accepted exchange, subject to the same assistant-turn durability discipline, then the request may return without entering cognitive/model processing.**

## VI. STANDING

> **O8R1 FOUNDER ADJUDICATION — PASS · CONFORMANCE ACCEPTED · FIRST FUTURE RUNTIME SEAM IDENTIFIED AS POST-F1 / PRE-COGNITION DESCRIPTION INTERCEPT · NO RUNTIME AUTHORITY GRANTED**

## Exact next boundary

> **SOULLAB-WHOLE-ORGANISM-ORCHESTRATION-01 / O8R2 — POST-F1 PRE-COGNITION DESCRIPTION INTERCEPT CONTRACT DESIGN ONLY**

O8R2 may design:
- exact insertion locus in the active `/list` route;
- resolver call ordering;
- ABSTAIN fall-through semantics;
- deterministic response envelope;
- same-exchange assistant durability behavior;
- Sanctuary/guest behavior;
- failure behavior if assistant durability fails;
- proof that cognition/model/memory/offer paths are not entered on DESCRIBE.

O8R2 may not:
- edit the route;
- import O8 resolver into production;
- change TurnsStore behavior;
- change response UI;
- change platformKnowledge;
- authorize telemetry;
- change member-visible behavior.