# FOUNDER ADJUDICATION — SOULLAB-WHOLE-ORGANISM-ORCHESTRATION-01 / O3

## INERT CAPABILITY AUTHORITY REGISTRY v2 SOURCE ADMISSION

## I. DISPOSITION

Accept O3 as:

> **PASS · INERT v2 REGISTRY SOURCE ADMISSION ACCEPTED · EXACT DOCUMENTARY IDENTITY PRESERVED · ZERO RUNTIME CONSUMERS · NO MEMBER-FACING AUTHORITY CREATED**

The source admission is accepted because it satisfies the bounded act authorized after O2R4R1:
- exact 18-descriptor documentary match;
- 14/14 targeted registry tests pass;
- 47/47 existing voice-navigation tests pass;
- protected runtime surfaces remain unchanged;
- no runtime consumer imports the v2 registry;
- all 18 descriptors remain explicitly runtime-ineligible;
- `pattern.detect` remains WITHHELD;
- legacy vocabulary is recorded without silent aliases.

Acceptance of O3 does not ratify any capability as available, suggestible, invokable, entitled, or production-ready.## II. ACCEPTED LAWS

1. The v2 registry is canonical descriptive infrastructure for the current capability identity set.
2. `lib/maia/capabilities.ts` remains legacy runtime-adjacent vocabulary until a separately authorized migration.
3. Registry description does not grant runtime authority.
4. COMPLETE means authority references are complete for the descriptor; it does not mean WIRED.
5. PARTIAL, UNRESOLVED, and WITHHELD standings remain first-class and fail closed.
6. No legacy capability may silently alias to a v2 identity.
7. Static capability research dependency remains distinct from dynamic output epistemic standing.
8. Current access policy remains distinct from proposed future economic/entitlement architecture.

## III. AUTHORITY NOT GRANTED

O3 acceptance does not authorize:
- capability availability resolution;
- member-specific evaluation;
- suggestion or offer generation;
- `capability_available` emission;
- `capability-offer` rendering;
- invocation;
- voice-command migration;
- access/entitlement migration;
- member-data reads;
- runtime repair of known nonconformances;
- deployment or merge by implication.

## IV. NEXT DESIGN QUESTION

> **What is the first lawful consumer that may read this map without gaining power to act?**## V. FOUNDER RULING

The first consumer should not be a router, event emitter, entitlement evaluator, consent resolver, voice adapter, or member-facing cognition surface.

The first consumer should be a **pure read-only projection** that converts the full authority registry into a smaller non-operational awareness record.

Working name:

> **Capability Awareness Projection**

Its purpose is:
> **to let another part of SOULLAB know that a capability exists, what human movement it serves, where it belongs, and what its documentary standing is — without exposing or granting the means to use it.**

This creates a membrane between:

`full authority registry`
→ `descriptive awareness`
→ future separately-authorized consumers

rather than allowing every future consumer to import the raw registry directly.

## Standing

> **O3 FOUNDER ADJUDICATION — PASS · FIRST CONSUMER CLASS CHOSEN: READ-ONLY CAPABILITY AWARENESS PROJECTION · NO CONSUMER IMPLEMENTATION YET**