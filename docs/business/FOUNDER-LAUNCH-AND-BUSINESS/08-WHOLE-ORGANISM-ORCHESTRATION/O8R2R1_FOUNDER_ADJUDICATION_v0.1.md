# FOUNDER ADJUDICATION — SOULLAB-WHOLE-ORGANISM-ORCHESTRATION-01 / O8R2R1

## POST-F1 PRE-COGNITION INTERCEPT SIMULATION + ROUTE-SEAM CONFORMANCE MATRIX

## I. DISPOSITION

Accept O8R2R1 as:

> **PASS · ROUTE-SEAM SIMULATION CONFORMANCE ACCEPTED · ROUTE PLAN 2/2 · REFERENCE STATES 10/10 · 16/16 FALSIFIERS DEAD · ACTIVE ROUTE + CANON WITNESSES MATCH · LIVE ROUTE UNCHANGED · ZERO PRODUCTION CONSUMERS**

Accepted consequences:
- post-F1 / pre-F2 is the lawful future insertion seam;
- ABSTAIN fall-through semantics are accepted;
- DESCRIBE early-return semantics are accepted;
- same-exchange durability is accepted;
- Sanctuary and guest ephemerality are accepted;
- text-only pilot posture is accepted;
- direct Canon provenance is accepted;
- the route-seam proof is lethal and discriminating.

## II. RUNTIME AUTHORITY NOT GRANTED

O8R2R1 acceptance does not authorize:
- editing `/api/sovereign/app/maia/list`;
- importing the test resolver into production;
- runtime description emission;
- changing `TurnsStore`;
- changing client/UI behavior;
- adding persistence authority;
- adding telemetry;
- bypassing a route-level gate;
- any member-visible behavior.

> **Simulation authority is accepted. Runtime authority remains closed.**

## III. NEXT SOURCE-AUTHORITY RULING

The proven resolver/composer currently exists only under:
`tests/constitutional/whole-organism-orchestration/explicit-capability-inquiry/`

A live route must not import from `tests/`, and future implementation must not manually rewrite the logic at the route boundary.

Therefore the next lawful act is:

> **inert production-source admission of the already-proven pure resolver/composer, with zero runtime consumers.**

This source promotion may create reusable production code.
It may not create runtime adoption.

## IV. PRODUCTION SOURCE PRINCIPLE

The production module must preserve:
- the exact three-capability allowlist;
- the exact founder-authored names and purposes;
- the exact accepted definitional envelopes;
- the exact refusal ordering;
- harmless normalization only;
- deterministic composition;
- ABSTAIN → null composition;
- terminal render semantics;
- no member/session/context input;
- no route/access/offer/availability semantics.

The source act must prove behavioral equivalence against the O8R1 test reference rather than relying on visual similarity.

## Standing

> **O8R2R1 FOUNDER ADJUDICATION — PASS · ROUTE-SEAM PROOF ACCEPTED · INERT PRODUCTION SOURCE ADMISSION MAY OPEN NEXT · LIVE ROUTE AND RUNTIME DESCRIPTION AUTHORITY REMAIN CLOSED**

## Exact next boundary

> **SOULLAB-WHOLE-ORGANISM-ORCHESTRATION-01 / O8R3 — INERT EXPLICIT-CAPABILITY-INQUIRY PRODUCTION SOURCE ADMISSION ONLY**

O8R3 production source locus is fixed as:
`lib/maia/explicitCapabilityInquiry.ts`

The locus is currently free and has zero existing references.

O8R3 may add:
- that one pure production resolver/composer module;
- exact three-capability presentation data needed by that module;
- source-level conformance tests proving equivalence with O8R1;
- zero-consumer census;
- evidence.

O8R3 may not:
- import the module into `/api/sovereign/app/maia/list`;
- edit any route;
- alter `platformKnowledge.ts`;
- alter `TurnsStore`;
- add response envelopes or Canon headers to production;
- add telemetry;
- change MAIA, House, voice, UI, access, or member-visible behavior.