# Claude Code — Writer's Studio visual actualization

Treat the images under `references/` as visual implementation specifications, not inspiration.

First, map the existing repository to the five target fields:
- Work Home
- Writing Field
- Structure & Versions
- Developmental Review
- Materials Studio

For every visible capability in the references classify the current implementation as:
- REUSE AS-IS
- REUSE + RECOMPOSE
- EXTEND
- NEW

Then recommend the first coherent actualization unit. Prefer the shared Writer Studio shell + Writing Field unless repository evidence says otherwise.

For each actualization unit:
- use real routes, data, and components;
- preserve existing intelligence, memory, provenance, auth, import, and revision behavior;
- do not hard-code fake screenshot content;
- do not reduce the designs to generic card grids;
- render at the same viewport as the reference;
- capture the running result;
- compare reference vs implementation;
- repair the largest visual divergences;
- stop only when the field is recognizably the same product.

Do not iterate indefinitely on the existing poor field. Preserve working capability, but recompose it into the reference architecture.
