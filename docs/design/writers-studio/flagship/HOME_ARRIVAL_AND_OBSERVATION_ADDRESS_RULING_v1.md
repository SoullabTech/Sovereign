# SOULLAB WRITER'S STUDIO
# Home / Arrival + Observation Address Ruling v1

Status: FOUNDER PRODUCT / ARCHITECTURE RULING  
Programme context: `JARVIS-WRITERS-STUDIO-COMPLETE-01`

This ruling is additive to the First Arrival + Onboarding Canon and existing constitutional/programme law.

It does not authorize merge, deployment, migration, package installation, or production mutation.

---

# 1. First Arrival Canon

The authoritative artifact is:

`SOULLAB_WRITERS_STUDIO_FIRST_ARRIVAL_AND_ONBOARDING_CANON_v1.md`

Do not implement First Arrival from a summary.

Read and audit the full canon before opening its implementation act.

---

# 2. `Home` — Current Ruling

CC's finding is accepted:

> `Home` is currently a capability-false live destination.

There is:
- no Home room;
- no Home machine phase;
- no Home rendered state;
- no admitted product behavior behind the button.

Therefore:

> **Remove `Home` from live Writer's Studio navigation until an actual Work-root / arrival destination is separately specified and implemented.**

Do **not** build an improvised Home room merely to satisfy the existing nav label.

A false destination is worse than a temporarily smaller navigation set.

---

# 3. First Arrival Is a State Before It Is a Room

The First Arrival canon does **not** require a permanent generic `Home` dashboard.

Its primary law is:

> **Writer's Studio does not onboard the member into software. It welcomes the member back into relationship with their Work.**

Therefore the default Work-root experience may be:

```text
open Writer's Studio / open Work
        ↓
recognize Work
        ↓
recognize current / last place where lawful
        ↓
manuscript
        ↓
plain-language intent / Keep writing
```

This is an **arrival state over the Work**, not necessarily a fourth room.

No fourth Writer's Studio room is authorized by this ruling.

---

# 4. Current Live Navigation

Until a genuine Work-root arrival destination exists:

```text
Write · Develop · Review
```

may be the complete primary Studio mode navigation.

Work identity / Soullab shell may provide broader product navigation only where the destination actually exists.

Do not use:
- Home
- Library
- Search
- Explore
- Export

as active Studio destinations merely because visual references contain them.

Capability honesty governs.

---

# 5. Future Work-Root / Home Decision

After the First Arrival canon is audited and its substrate exists, bring a narrow proposal answering:

1. Is the arrival state:
   - the Work root,
   - the Write root,
   - or a distinct existing product destination outside the three Studio modes?

2. What exact route/state owns it?

3. What does `Home` mean to the member?

4. Does the member ever need to navigate *to* arrival after initial entry?

5. Could clicking the Work title / breadcrumb satisfy the return need more truthfully than a `Home` nav item?

Do not assume the word `Home` survives this review.

---

# 6. `OBSERVATION-ADDRESS-01` — Priority Ruling

CC's convergence finding is accepted.

`OBSERVATION-ADDRESS-01` is no longer treated merely as a local R5 dependency.

It is now recognized as a **shared flagship substrate dependency** because durable address/identity is required by at least:

### R5 — Member-authored observation
To support:
- find it later;
- retrieve it after session;
- preserve exact locus;
- edit/delete correctly.

### First Arrival / Returning Work
To support:
- lawful return to last authored place;
- recognition of the member's previous locus;
- quiet re-entry without fabricated continuity.

### R9 — Seedable human beta
To support:
- reproducible readings;
- persistent observations;
- known stale/current versions;
- deterministic reset/retrieval.

Potentially also:
- History;
- related passages;
- declared maps;
- reading freshness;
- cross-session Review trails.

Therefore:

> **`OBSERVATION-ADDRESS-01` is the highest-leverage unopened substrate lane presently identified for the flagship.**

---

# 7. What This Priority Does NOT Mean

This ruling does **not** authorize CC to open, implement, migrate, or merge `OBSERVATION-ADDRESS-01` automatically.

It changes programme priority and dependency interpretation.

Before implementation, present the smallest bounded act specifying:

- exact canonical;
- exact existing address/identity substrate;
- exact durable objects in scope;
- persistence store(s);
- version/read identity;
- owner/Work binding;
- exact-locus semantics;
- retrieval;
- edit/delete;
- migration requirement, if any;
- privacy/data-retention implications;
- falsifiers;
- collision analysis with current flagship branch.

No ambient schema expansion.

---

# 8. Recommended Sequence

Current visual work remains:

```text
R1 revised Review
        ↓
R2 V10 founder visual gate
```

In parallel, read-only specification of `OBSERVATION-ADDRESS-01` may proceed if it does not mutate the current flagship candidate.

After R2, the next implementation priority should be adjudicated between:

```text
OBSERVATION-ADDRESS-01 substrate
        and
R3 intent-first entry
```

based on exact dependency overlap.

Do not let arrival composition quietly invent persistence before the address lane exists.

---

# 9. Returning-Writer Law Until Persistence Exists

Until durable last-place state exists:

Allowed:
> `Welcome back to The River Between.`

Allowed when current request/session knows it:
> `You're in Chapter 6 — The Current Changes.`

Not allowed:
> `You last worked here three days ago.`

unless the system has durable evidence supporting that statement.

First Arrival copy must degrade honestly when continuity data is absent.

---

# 10. Beta Evidence Law

Do not run beta tasks that require cross-session retrieval until `OBSERVATION-ADDRESS-01` or equivalent durable substrate is admitted and witnessed.

A missing persistence capability must not be recorded as participant usability failure.

---

# 11. Immediate Instructions to CC

1. Read the attached First Arrival canon in full.
2. Audit it against the actual candidate.
3. Remove capability-false `Home` from live Studio nav in the next authorized composition act; do not invent a room.
4. Record the Work-root/Home question as unresolved future product semantics.
5. Treat `OBSERVATION-ADDRESS-01` as a shared flagship dependency and prepare a **read-only dependency/specification proposal**, not implementation, unless separately authorized.
6. Continue the already-open R1/R2 Review path without mixing persistence work into it.
7. Do not mark returning-state features implemented unless durable evidence actually exists.

---

# 12. Spirit of the Ruling

> **Do not create a room because a navigation label accidentally promised one.**
>
> **Do not claim continuity the system cannot remember.**
>
> **And when one substrate unlocks three flagship stages, treat it as architecture rather than a side dependency.**

**A deeper you. A more human world.**
