# Co-lab Decision Loop — Deploy Runbook → CONSOLIDATED (pointer)

**This runbook has been merged into a single canonical source.** Deployment is operational knowledge, so it now lives in `docs/ops/`:

➡️ **[`docs/ops/COLAB_DECISION_LOOP_DEPLOY_2026-06-06.md`](../ops/COLAB_DECISION_LOOP_DEPLOY_2026-06-06.md)** — single source of truth.

The canonical version preserves this draft's strengths — the real migration mechanism (`scripts/apply-migrations.sh` / `deploy-production.sh` `migrate` profile / `schema_migrations` tracking / boot gate; *`up -d --build` alone does not migrate*), the prod-DB backup step, and the clean-main cherry-pick status (`ad23b1a96`) — and adds a **phantom-migration remediation step** that neither earlier draft caught:

> `20260321000004_team_message_kinds` is **recorded in `schema_migrations` on prod but its `message_kind` columns are absent**, so the filename-tracked runner will **skip** it. It must be applied manually *before* deploy, or Co-lab decision-tagging will 500 on prod.

*Do not edit this file — update the canonical runbook in `docs/ops/` instead.*
