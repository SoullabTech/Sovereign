# Current-production re-witness @ `b8eb2c626` + forensic exception declaration

**Date:** 2026-08-14. **Runtime referent: `b8eb2c626`.**

```
ORIGINAL AUTHORIZED REFERENT .... b14d96ed8
BOUND AT EXECUTION .............. superseded before the witness began
CURRENT AUTHORIZED REFERENT ..... b8eb2c626   (re-authorized by founder act)
```

⭐ The mandate named `b14d96ed8`; binding before measuring — as the mandate itself
required — showed production had already moved. `b8eb2c626` is a descendant of
`b14d96ed8` (+3 commits), so CC-A remains in its ancestry, and the containment
base `22200f967` remains an ancestor. The witness was **not** dynamically
re-pointed; it halted and was re-authorized.

---

## Result

```
LIVE TRAFFIC WITNESS ..... NOT ESTABLISHED — NO ELIGIBLE TRIGGER
MECHANISM CONTROL ........ PASSED @ b8eb2c626
BYPASS CHECK ............. one reader found — DECLARED FORENSIC EXCEPTION
MEMBER CONTAINMENT ....... INTACT on present evidence
```

## Runtime binding (measured before anything else)

| | |
|---|---|
| `GIT_COMMIT` | `b8eb2c626` |
| `/api/health` | `"version":"b8eb2c626"` — agrees |
| `DEPLOY_LANE` | `deploy-lane` |
| container | `f46e1159503b`, started **14:19:21Z**, image `6bad1e8ea000` |
| built artifact | `BUILD_ID = tPVwMWEFskKATf-De4ENa` present |
| database *reached by the running process* | `maia_consciousness` @ `172.18.0.4`, 442 signals visible — identity, not config |

## A/B/C — live traffic: NOT ESTABLISHED

Observation window: **5 minutes** (container start 14:19:21Z → measured 14:24:43Z).

| measure | value |
|---|---|
| turns in window | **0** |
| member turns in window | **0** |
| signals written in window | **0** |
| qualifying trigger turns in window | **0** |
| rupture writes in window | **0** |
| rupture total | **97 — unchanged** |

⛔ **No positive control and no trigger, therefore no witness.** Context: last real
traffic was **11:41Z**, ~3h before the restart; 86 turns today, 112 in 24h. The
writer is not broken — **the window is simply empty.**

⛔ **No production turn was manufactured to make this pass.** A future natural
trigger can supply the missing live witness. This is *unwitnessed production
behaviour*, ⛔ not a failure of the mechanism.

## E — mechanism control: PASSED at the bound referent

Executed at a worktree on **`b8eb2c626`**, against a **local scratch database** —
production was never written to. Dependency graph proven equivalent first:
`package-lock.json` blob is **identical** at `22200f967` and `b8eb2c626`
(`4edbb06344c7`), and the containment + detector sources are **byte-identical**
between those refs.

```
1 DETECTOR (upstream, unchanged): detected=true ruptureState="ruptured" conf=0.5
2 WRITE executed (positive control): ROW WRITTEN
3 STORED rupture_state (negative control) = null   [source=maia_conversation]
4 SERVED via rowToSignal (read control)   = null
5 labtool_manual stored rupture_state     = null   ← provenance is not authorship
6 scratch rows removed from local db
```

⭐ Step 1 matters as much as step 3: the detector **still asserts `ruptured`**. The
upstream incapacity is unchanged and was never meant to be fixed — the boundary
withholds the assertion. ⭐ Step 5 confirms the founder correction holds in
execution, not merely in source: **"manual" is a tool label, not authorship.**

## F — bypass check, and the ⚖️ DECLARED EXCEPTION

**Write chokepoint: clean.** No `INSERT`/`UPDATE` against
`member_relational_signals` exists outside `relationshipSignalService`.

**Read: one path bypasses `rowToSignal`** —
`app/api/founder/relational-signals/route.ts` selects `s.rupture_state` in raw SQL
(:188) and serves `ruptureState: row.rupture_state` (:218).

### ⚖️ Founder ruling, 2026-08-14

> The founder review surface is an **intended forensic exception, not a
> containment gap.** ⛔ Do **not** route it through `rowToSignal` — that would make
> the forensic surface incapable of showing what was actually stored, which
> defeats its purpose.

**The containment guarantee, narrowed and made precise:**

> **Raw `rupture_state` must not reach member-facing or general application
> consumers. A single founder-only forensic review path may read the stored value
> directly, provided it remains fail-closed, non-mobile, non-member-facing, and
> explicitly classified as an exception.**

Evidence supporting the classification, measured at `b8eb2c626`: gated by
`requireFounder()`, which **fails closed when `FOUNDER_MEMBER_IDS` is unset**;
**0** founder routes in the mobile allowlist; consumed only by
`app/founder/relational-signals/page.tsx`.

### ⛔ Constraints binding the exception

- `requireFounder()` **must continue to fail closed**
- the route **must remain absent** from mobile/member allowlists
- raw state **must not flow** into MAIA, member APIs, analytics, memory recall, or general UI
- the exception is for **inspection of stored state**, ⛔ not for creating a parallel application model
- ⛔⛔ **no new raw SQL reader is grandfathered merely because this one is allowed**

### Open — future mechanical guard (⛔ not built here)

A guard should establish that direct raw reads of `rupture_state` are permitted
**only** in the specifically declared founder-forensic route, so a *second*
exception cannot appear accidentally. ⛔ Recording the requirement; the present
witness was not broadened into implementation.

---

## Permitted success statement

> **This boundary's mechanism was exercised at `b8eb2c626` under the recorded
> conditions and behaved according to the containment invariant. Live production
> traffic did not witness it — no eligible trigger occurred in the window.**

⛔ Nothing broader. ⛔ Not "the Relational Field is safe."

**Nothing mutated. Nothing deployed. Production `b8eb2c626`, unchanged.
Rupture corpus 97, untouched. BUILD AUTHORITY CLOSED.**
