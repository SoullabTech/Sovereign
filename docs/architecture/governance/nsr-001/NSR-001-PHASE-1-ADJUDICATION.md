# NSR-001 — PHASE 1 ADJUDICATION RECORD

**Status: `PHASE_1_ADJUDICATED_AND_SEALED — PRE-NORMATIVE`** · 2026-08-12

**Subject of adjudication:** `NSR-001-PHASE-1-CURRENT-STANDING-MAP.md`, bound below by SHA-256.
**The map is not amended.** This record binds to it, as the map itself bound to the sealed ledger without amending it.

## The adjudication standard applied

Founder-set, before Phase 1 returned:

> Not *What needs fixing?* but: **Did the census faithfully establish current jurisdiction — including the capacities whose present standing deserves protection?**

**Verdict: YES.** The map is admitted as the sealed statement of current standing.

---

## PART A — ADJUDICATOR ERROR RECORD

Preserved as findings, not errata — the same disposition ruled for the wrong-host retractions. **Both errors were committed while checking the map for exactly this class of error.**

### ERROR A1 — occurrence mistaken for importation

I challenged Row 10's `NOT_PRESENT`. I ran `grep -l realityCheckAgent`, found four files, and reported that the executor had overstated absence — naming `app/api/reality-score/route.ts`, `components/reality/RealityHygieneDisplay.tsx`, and `lib/reality/realityHygiene.ts` as importers.

**They are not importers.** All three mention the name only in comments:

| Site | Actual content |
|---|---|
| `app/api/reality-score/route.ts:29` | `questions: z.any().optional(), // RealityCheckAgent output` |
| `components/reality/RealityHygieneDisplay.tsx:6` | `* Displays consciousness-expanding questions generated by RealityCheckAgent.` |
| `lib/reality/realityHygiene.ts:5` | `* This is the layer between raw scores and the RealityCheckAgent.` |

Import statements repo-wide, re-derived at `52a3b924` restricted to `^import` / `from '…'`:

```
test-reality-hygiene.ts:17:import { generateRealityCheck } from "./lib/reality/realityCheckAgent";
```

**One. A test file.** The executor's `NOT_PRESENT` stands unmodified. **Challenge withdrawn.**

**The error is the map's own anti-pattern, committed against the map.** Row 10 records documentation asserting a deployment that import lineage contradicts. I then asserted a deployment on the strength of *comments* — the weakest surface in the authoritative-surface ordering — while adjudicating the row that exists because that surface misled five documents.

### ERROR A2 — basename/path assumption mistaken for absence

I attempted to verify the gate at `lib/consciousness/panconsciousFieldRouter.ts`, found nothing, and treated the threshold as unverified.

**The file is `lib/field/panconsciousFieldRouter.ts`** (blob `6b7cd8a84b02c2112902b7608d79da56e41b65e9`). The executor cited it by basename and line; I supplied the wrong directory from the surrounding conceptual family and read my own miss as the census's gap.

**This is hazard-class #2 of the day** — the basename hazard formally recorded after two files named `awareness-levels.ts` were found (327 colliding basenames, 24% of the tree). I recorded that hazard and then committed it.

### What the two errors establish

Both are the **substitution class**: a weaker surface (a comment; a guessed path) accepted in place of the authoritative one (import lineage; the resolved path).

> **The adjudicator was still vulnerable to the census's own hazard class while checking the census for it.**

Recorded because it bears on Phase 2. Normative reasoning has no `git grep` to correct it. If verification-under-discipline produced two substitutions in one sitting, **standing proposals will produce more, and with nothing to catch them.**

---

## PART B — INDEPENDENT VERIFICATIONS PERFORMED

Re-derived by the adjudicator at `52a3b924b7cf52013c1c8b0d635359c2cad672fc`, not accepted from the executor.

| Claim | Site | Result |
|---|---|---|
| Gate threshold | `lib/field/panconsciousFieldRouter.ts:71` — `if (avg < 2.5) { realm = 'MIDDLEWORLD'; fieldWorkSafe = false; …}` where `const avg = profile.rollingAverage` | ✅ **VERIFIED** |
| Terminal consequence | `lib/sovereign/maiaService.ts:2500` — `if (!fieldSafety.allowed)` → logs `avg=…, fieldWorkSafe=false` → returns `"Let's take the safest next step together."` → persists via `addConversationExchange` | ✅ **VERIFIED** |
| `cognitiveAltitude` is a rename | `cognitiveAltitude: cognitiveProfile.rollingAverage` at `list/route.ts:478`, `maia/route.ts:193`, `oracle/conversation/route.ts:766`, `MainOracleAgent.ts:458` — no site computes an altitude | ✅ **VERIFIED** |
| Row 2 abstention floor | `lib/sovereign/maiaService.ts:406` — `if (totalBeads < 20) return null;` | ✅ **VERIFIED** |
| Row 10 import lineage | one importer, `test-reality-hygiene.ts:17` | ✅ **VERIFIED** (after ERROR A1 withdrawn) |

**The central finding is independently verified at both ends of the chain** — the threshold that sets `fieldWorkSafe = false`, and the early return that ends the turn before generation.

---

## PART C — WHAT IS SEALED

> **what exists → what it denotes → ontological class → evidence status → current standing → warrant**

Eleven rows, admitted as sealed. Three findings carried forward at full weight:

**1. Row 1 — the completed drift chain.** `computed → available → passed → consulted → acted upon → depended upon → gated`, with **no located decision at any transition**. `WARRANT: NONE FOUND` in the constitutional sense: *no evidence establishes why this representation is entitled to the jurisdiction it exercises.* True even though every engineering step was individually reasonable.

**2. Row 2 — the live counterexample, and the protected set's first genuinely important member.** A representation that also interprets the member, and holds influence without sovereignty: abstains below 20 beads, carries its sample size to the point of use, shapes register, **cannot block, re-route, or terminate**. Its chain stops before `depended upon`.

> The architecture already contains a working instance of the principle: **capability can participate without becoming sovereign.**

Therefore Row 1's gate **cannot** be defended as intrinsic to awareness-informed behaviour — and **removing it requires no change to Row 2.** Same file, same vocabulary; no shared table, data, or code path.

**3. Row 10 — representation of capacity mistaken for capacity.** Five documents, one titled `REALITY_HYGIENE_DEPLOYED.md`, describe as deployed what has one test-file importer.

**Independently derived.** The census was not told to look for the constitutional problem, and was not supplied HYP-001. Row 4's `NO_EVIDENCE_FOR_PROPOSED_DISTINCTION` is the control that shows the vocabulary did not manufacture the result: given every opportunity to find a latent DEEP-dimension architecture, it found a **name collision** and said so.

**Founder intention and runtime finding stay separated:** DEEP-dimension remains a phenomenological reality MAIA is intended to perceive; **no coherent implementation referent for it was established.**

---

## SEALED — AND STOPPED

Phase 1 is closed. **No `REQUIRED YIELD`. No `PROPOSED STANDING`. No topology. No rehabilitation design.**

CANON-001, CANON-002 and NS-001 have **not** been applied to any row and may not be until Phase 2 is authorized.

The **HARD PRE-IMPLEMENTATION GATE** of the sealed ledger remains in force: production `3d1e2734…` carries production-proven SECREM-001, canonical `52a3b924…` does not, and `3d1e2734…` is not an ancestor of canonical. **No rehabilitation implementation may parent from canonical until that lineage is reconciled.**
