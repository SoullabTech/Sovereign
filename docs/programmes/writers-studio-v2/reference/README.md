# Moved — and the pack is in custody

The visual reference pack lives at **`docs/design/writer-studio/`** —
`references/` for the specification, `implementations/` for screenshots of the
running product, plus the manifest, the build loop and the starter prompt.

One location, so the specification cannot fork. Nothing lives here.

**Corrected 2026-08-28.** This file previously said `DESIGN-CONTRACT.md` §0
stayed DERIVED and WS2-00 stayed blocked until the images arrived. They arrived:

```text
reference pack        IN CUSTODY
custody commit        1493c28c0  (2026-08-27)
DESIGN-CONTRACT       FROZEN
WS2-00                CLOSED
canonical WS-WRITE    04-writing-field-wide.png
03 / 06 / 07          one duplicated reference state (md5-verified)
```

The pack is **immutable evidence**. Do not regenerate, rename, reinterpret or
replace its eight images. Captures of the running product go to
`implementations/`, never here.
