# Phase 2 — Foundation Evidence Package

> ## ⛔ THIS ARTIFACT CREATES NOTHING AND RULES NOTHING
> It is **evidence and navigation**, not authority. It does **not** declare Phase 2 complete —
> that declaration is a founder act and has **not** occurred. Placement in `docs/governance/` grants
> this file no status (**C1**, `78d9fb388`: location cannot create authority). Every claim below is
> either bound to a merge SHA or explicitly marked unestablished.
> **Compiled by Claude Code, 2026-07-29. Verifier of referents, not author of authority.**
>
> **Step 6 review — Kelly Nezat, 2026-07-29: PASS on all five sections.** ⚠️ This records that the
> evidence was *reviewed*, not that Phase 2 is *closed*. **The closure declaration has NOT been made**
> and cannot be made inside this file — a declaration here would turn evidence into the authority it
> disclaims. If declared, it requires its own artifact with its own ruling block and merge SHA, which
> may *cite* this package (A2 lineage citation, permitted under C3 `598d764b0`) but not contain it.
>
> Reviewer's note carried: **heuristics generate candidates; referents establish truth.**

---

## 1 · Canonical ruling referent audit

Method: `git merge-base --is-ancestor <sha> origin/clean-main-no-secrets`, and
`git cat-file -e origin/clean-main-no-secrets:<path>`. Not read from memory.

| Ruling | Merge SHA | Ancestor of canonical | PR |
|---|---|---|---|
| L1 — lifecycle gap AFFIRMED | `448cb7eda` | ✅ | #815 |
| L2 gate — candidate typing (type-only) | `500b37f6f` | ✅ | #816 |
| C1 — location ↛ authority | `78d9fb388` | ✅ | #817 |
| C2·D0 — distinguishability necessary | `f1becda51` | ✅ | #818 |
| C2·D1–D3 — lineage-bearing state model | `8c2c0614a` | ✅ | #819 |
| C3 — canonical history is the verification referent | `598d764b0` | ✅ | #820 |
| C4 — validation criterion | `fa003f7cb` | ✅ | #821 |
| C4 scope — Option 2, broad/recognized-not-enforced | `dab5d53df` | ✅ | #822 |

All 7 lifecycle artifacts retrieve from canonical: `LIFECYCLE_GAP_LAYER1_FINDING` ·
`LIFECYCLE_LAYER2_PROPERTIES` · `..._C1` · `..._C2` · `..._C3` · `..._C4` · `LIFECYCLE_C4_SCOPE`
(all `docs/governance/*_2026-07-29.md`).

## 2 · Memory graph audit

| | |
|---|---|
| artifacts | 1244 |
| reachable from `MEMORY.md` | 1037 |
| unreachable | **207** — by decision, not neglect |
| broken links | **0** |
| files deleted | **0** |
| `MEMORY.md` | 22,188 bytes (peak 24,151) |

Repair: 381 → 207 unreachable via 2 themed sub-indexes (32 cluster entry points + 61 live singletons).
⚠️ **191 singletons remain unreviewed.** Unreachability is four states, not two — *reachable* ·
*unreachable + intentional* (fine) · *unreachable + live constraint* (**defect**) ·
*unreachable + unique knowledge* (**preservation required**).

## 3 · Compression candidate report

7 lines ≥380 bytes (3,594 bytes total); 96 lines <380 bytes (already navigation hooks).
Reaching the tooling target of 17.1KB would require removing ~5KB of **entries**, not detail — which
fails **C4** and discards material merely because representing it is inconvenient. **Not done.**

⚠️ **Word-overlap scoring failed in both directions, 4 times today**: one false-compress (a unique docket
fact with no surviving referent — compression would have destroyed it) and three false-preserves
(`residue`/`residual`, `#813`, `importance ⊥ surface-area` — referents existed, wording differed).
The sound test is: **if this representation disappears, does the knowledge survive somewhere trustworthy?**

## 4 · Governance language drift scan

Method: authority-claiming vocabulary (`ratified` · `canonical` · `approved` · `official` ·
`constitutional` · `in force` · `established`) on lines carrying no SHA, across 16 governance files on
canonical.

**Result: no confirmed authority claim without a referent.** All hits are the vocabulary appearing as
*subject matter* inside the ruling artifacts, explicit negations, or unrelated senses
(`established users` in a metrics doc). ⚠️ High false-positive rate — consistent with §3: word-matching
is the wrong instrument for this class of question.

---

## Established (each bound to a merge SHA above)

- A ratified state must reach **recorded** and **verifiable**; the transition is a **required lifecycle state**, not an available action. `448cb7eda`
- **Location cannot create authority** — prohibition only; prescribes no directory structure. `78d9fb388`
- Lifecycle states must remain **distinguishable**. `f1becda51`
- Five states with **provenance separate from nature**; `living` admitted **by founder ruling with no prior canonical instance**. `8c2c0614a`
- **Canonical history is the verification referent**; citation ≠ verification; artifacts need not carry proof of their own existence. `598d764b0`
- **A mechanism cannot be admitted without being tested against the failure class it was created to address** — a validation criterion, not a displayed property. `fa003f7cb`
- That criterion is **broad but RECOGNIZED, not ENFORCED**; ownership beyond the lifecycle domain is a carried open question. `dab5d53df`

## NOT established

- **Layer 3 / instrument selection** — closed; never opened.
- **Governance ledger** — not selected. Note: a ledger *fails the C4 test as-is* (an entry can be added on a branch and never merged).
- **`derived`** as a state-admission path — tracked outside the model, unruled.
- **General invariant `representation ↛ authority`** — candidate only. Corrected basis: **3 ruled** (location `78d9fb388` · citation `598d764b0` · mechanism-claim `fa003f7cb`), **1 derived gloss** (naming — not C2's literal ruling), **1 observed finding** (indexing — never ruled). ⛔ A future surface may **not** claim "five rulings establish" this.
- **Who owns the C4 test** outside the lifecycle domain, and by what process.
- Status of the **191 unreviewed** memory singletons.

## Layer 3 entry conditions

| | Condition |
|---|---|
| ✅ | C1 established `78d9fb388` |
| ✅ | C2 established `f1becda51` + `8c2c0614a` |
| ✅ | C3 established `598d764b0` |
| ✅ | C4 established `fa003f7cb`, scope `dab5d53df` |
| ⬜ | **Instrument question opened** — requires a founder act |
| ⬜ | **Phase 2 closure declared** — requires a founder act; **not** performed by this file |

---

**Governing constraint carried forward:** *A representation should help us reach reality, but it must
never quietly become a substitute for reality.* And: *a pattern can guide inquiry before it can govern
behavior.*
