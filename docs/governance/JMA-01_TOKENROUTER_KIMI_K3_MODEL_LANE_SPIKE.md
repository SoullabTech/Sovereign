# JMA-01 — TokenRouter / Kimi K3 · ModelAdapter candidate spike

**Status:** ⭐ **ISSUED, EXECUTION BLOCKED ON CREDENTIAL** — founder directive 2026-08-24.
**Governs under** `JARVIS_ARCHITECTURE_CONVERGENCE_DIRECTIVE_2026-08-24.md`.
**Confers** the bounded authority in §3, and nothing beyond it.

---

## 0 · Placement — this is the absorption rule's first live test

The Convergence Directive §8 says that when a new system appears, the question is *which adapter
does this compete for* — not *should we add a layer*.

```text
TokenRouter / Kimi K3  →  competes for: ModelAdapter
                          competes for: NOTHING ELSE
```

⛔ It is not an execution substrate, a decision graph, or a memory system. It supplies **temporary
cognition**. Per §3.4 it therefore **does not own durable JARVIS knowledge** under any outcome of
this spike.

### 0.1 Ladder standing — this does NOT open the ladder

Gate Zero is **HELD** (Directive §9). The numbered phases JARVIS-01…05 remain closed.

ModelAdapter has **no numbered evaluation phase** in the ladder — 02 is execution, 03 is graph, 04
is memory. This spike is therefore an **out-of-ladder bounded probe, explicitly authorized by the
founder**, not a phase. ⛔ Completing it does not advance Gate Zero and does not open JARVIS-01.

---

## 1 · Prior state — what is and is not established

⚠️ **Recorded 2026-08-24 from public sources only.** `tokenrouter.com` is unreachable from the
execution container (`EGRESS_BLOCKED`), so **no primary-source verification was possible.**
Per the Marketing Claim Discipline: social-media reports are claims about claims.

| Question | Standing | Basis |
|---|---|---|
| Does model ID `moonshotai/kimi-k3-free` exist? | **PROBABLE** | multiple independent third-party reports list it at $0/$0, OpenAI-compatible at `https://api.tokenrouter.com/v1` |
| Is there a 50,000,000-token free allocation? | ⚠️ **UNVERIFIED** | reported widely; traced to one promotional thread, not a vendor page |
| Paid `moonshotai/kimi-k3` price | ⚠️ **CONFLICTING** | one source $1.50/$7.50 per M in/out; another $3/$15 (cache read $0.30). Not reconciled. |
| Is the promotion still open? | ⛔ **CONTESTED — CHECK FIRST** | one report says the free tier ran **through 2026-08-05**, which is **19 days before this issuance**. Against that, TokenRouter's own account posted that the free tier "got bigger." Unresolved. |

⛔ **Do not treat any row above as fact.** The **account dashboard is the only settling evidence**,
and it is founder-side. This table exists to be replaced by §4.

---

## 2 · Founder-side preconditions (not JARVIS work)

```text
1. Create the TokenRouter account.
2. Create an API key under "API Keys".
3. Read the dashboard and capture:
     a. whether a free allocation is actually granted, and its size
     b. whether model ID `moonshotai/kimi-k3-free` is actually offered
     c. any stated expiry on the allocation
```

⚠️ **Check (c) before spending effort on (a)/(b)** — if the promotion closed on 2026-08-05 the
premise of this spike is gone and the unit ends before execution, which is a legitimate completion
state, not a failure.

⛔ JARVIS cannot perform §2. No account creation, no dashboard read, no screenshot.

---

## 3 · Bounded mandate

| | |
|---|---|
| **Scope** | add TokenRouter/Kimi K3 as an **experimental model lane only** · prove §4 · report · STOP |
| **Authority** | this mandate |
| **Stopping condition** | §4 fully recorded, **or** first blocking finding |
| **Evidence required** | every row of §4, each with the raw observation behind it |

### Not authorized

```text
⛔ replace or modify existing Claude / OpenAI routing
⛔ make Kimi the JARVIS default for any lane
⛔ route MAIA production traffic through it
⛔ send MAIA personal/human memory (Directive §5 — absolute)
⛔ send Soullab operational memory, claims, evidence, or task packets
⛔ paste the API key into any third-party install script or setup one-liner
⛔ commit the key, or any file containing it, to the repository
⛔ modify global Claude Code configuration
⛔ proceed automatically to any adoption step
```

### Key handling

```text
environment variable only · never a tracked file · never a script argument
never echoed into logs, evidence records, or this document
```

---

## 4 · Required proof — measure, do not cite

Report each row with the observation that produced it.

| # | Proof | Acceptance |
|---|---|---|
| 1 | **Available model ID** | the exact string the account can call, read from the account — not from a post |
| 2 | **Free quota** | granted amount + **stated expiry**, read from the dashboard |
| 3 | **Successful API call** | one round-trip against `https://api.tokenrouter.com/v1`, non-sensitive prompt, response captured |
| 4 | **Latency** | wall-clock, several samples, spread reported — not a single best case |
| 5 | **Token accounting** | tokens the response reports vs. what the dashboard decrements · do they agree? |

### Test material

⛔ Non-sensitive development material only. Synthetic or public text. **No MAIA content. No member
content. No operational memory.**

### Also record (unscored, for a future ModelAdapter ruling)

```text
retention / logging policy as actually stated by the vendor
what breaks when quota is exhausted — hard stop, or silent fallback to a paid model?
```

⚠️ The second is a **cost-exposure question**, not a curiosity: a gateway that silently falls back
from a `-free` model ID to a paid one converts an exhausted quota into a bill.

---

## 5 · Completion

```text
Report §4. STOP.
```

⛔ No adoption, no default change, no routing change follows from a passing spike. Adoption is a
ModelAdapter ruling, and that belongs to JARVIS-05.

> Models provide temporary cognition. They do not own durable JARVIS knowledge.
