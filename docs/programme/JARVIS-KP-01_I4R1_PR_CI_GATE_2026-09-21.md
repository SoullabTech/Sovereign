# JARVIS-KP-01 · I4R1 — PR/CI GATE FRESHNESS RECONCILIATION

**Date:** 2026-09-21
**PR-gate canonical base:** `bd91e5bb09bf4747216f09941d33890f4f890163`
**Prior I4R1 adjudication head:** `de80383949211a37238bc5a8945b388955bc5670`
**Replayed implementation/governance head before this record:** `c8ec39db5`
**Status:** PR/CI GATE READY · NO MERGE · NO DEPLOYMENT · DEFAULT OFF

> **NO SEMANTIC JOIN WITHOUT A WARRANT.**

## Why a second freshness reconciliation was required

Immediately before opening the I4R1 pull request, GitHub reported that
`clean-main-no-secrets` had advanced from
`0319940f9dc94a637e6f1b0e9f843c971f6fb0c2` to
`bd91e5bb09bf4747216f09941d33890f4f890163`.

The newer canonical was five commits ahead and changed only JARVIS/OpenCode
execution-fabric files:

- `jarvis-desktop/src/work-unit-control.js`;
- JARVIS Desktop/provider proof fixtures;
- `scripts/builder/jarvis-local-worker.mjs`;
- JARVIS builder/provider/transport proof and runtime files;
- one D2 canonical-reconciliation programme record.

There was zero file overlap with the I4R1 candidate and no change under:

- `lib/ain/epistemic-join/**`;
- `lib/maia/relational-field-shadow/**`;
- the I4 telemetry migration;
- the I4 constitutional matrix or witness.

A stale PR was therefore not opened.

## Exact replay

The complete three-commit I4R1 chain was replayed onto
`bd91e5bb09bf4747216f09941d33890f4f890163` with:

- zero conflicts;
- zero manual implementation edits;
- unchanged patch identities.

Stable patch identity:

| Prior replay | PR-gate replay | Stable patch-id |
|---|---|---|
| `997dbaeee` | `8b70b1f02` | `a26613b425af8cab5638bbdf6fa6a94be244d5d0` |
| `890a1a042` | `d93619ec0` | `57171b81534be1bb665c9d0c6100454380a36022` |
| `de8038394` | `c8ec39db5` | `1e6a4b19f66373d7a35568f346a1ccbfd6a3a97c` |

The seam adjudication is unchanged:

> **FIRST I4 INGRESS = MAIA RELATIONAL-FIELD SHADOW.**

The separate Now What cell-candidate branch remains parked and is not present
in this PR candidate.

## Exact-head local gates

On `bd91e5bb… + I4R1`:

- strict epistemic-join typecheck → **PASS**;
- epistemic-join + relational-field Jest surface → **118/118 PASS**;
- I4 constitutional matrix → **22/22 PASS**;
- disposable I4 PostgreSQL witness → **7/7 PASS**;
- repository typehealth → **229 diagnostics vs 239 baseline · 0 regressions · EXIT 0**;
- blank PostgreSQL reconstruction → **PASS · 5/5 stages**;
- `git diff --check` → **PASS**.

The existing repository `maia-desktop` Jest haste-module collision warning was
emitted; all requested suites executed and passed.

## PR/CI authority boundary

This gate authorizes opening an exact-head pull request for hosted CI and review.

It does not authorize:

- canonical merge;
- feature activation;
- production member observation;
- I3 semantic persistence from the runtime shadow;
- standing promotion;
- representation/projection;
- memory, routing, provider, or model authority;
- Now What I4 adoption;
- I5;
- deployment.

## Standing

> **I4R1 PR/CI CANDIDATE · FRESH ON bd91e5bb · PATCH-IDENTICAL · LOCAL GATES GREEN**

The next decision after hosted CI is a separate canonical-merge adjudication.
