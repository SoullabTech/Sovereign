# `CONTINUITY-REMAINDER-01` · CLAUDE.md PRIORITY-THREAD BULLET — **DRAFT, ⛔ NOT INSTALLED**

⛔ **This file is not the record and not the anchor.** It holds a drafted bullet for
`CLAUDE.md`'s *Current priority thread*, **withheld from installation by founder instruction
(2026-09-15)**. ⛔ Do not paste it into `CLAUDE.md` without an explicit founder act.

**Standard applied (founder):** *preserve standing, rather than turn into a mini-history.*

---

## 1 · THE BULLET, AS THE FOUNDER WROTE IT

> - **CONTINUITY-REMAINDER-01** — ACT 1/2 + P1 re-adjudication complete on
>   `claude/dazzling-goldberg-wg8s4s`. Production `e57ca1baa` untouched. Measured defect: HOP 1
>   correctly detects retrospective class membership but illegitimately treats that membership
>   as antecedent identity; P1's prior green was recovery by lexical coincidence and no longer
>   stands as evidence of lawful antecedent resolution. P1 acceptance therefore cannot
>   discriminate a lawful resolver from the current unlawful mechanism; N1/N2 remain
>   undisturbed. Repair is not authorized. Next question: what evidence is sufficient for
>   antecedent identity once retrospective intent is established? Current corpus only contains
>   referential evidence when the member restates the object; `FIRST-ASK-OPAQUE-MEMORY-01`
>   remains unopened and may be required to answer the first-ask case.

⭐ **Checked against the five records: accurate in every clause.** ⛔ No correction is owed.

---

## 2 · ⚠️ THREE OPTIONAL ADDITIONS — ⛔ founder's call, ⛔ none applied

Each is **standing a future session cannot reconstruct from the bullet as written**, ⛔ not
history. Listed separately so the compactness standard stays the founder's to enforce.

**(a) Where the objects are.** ⛔ The bullet names no record and no candidate SHA, so a future
session inherits the conclusions without the evidence.
> *Records `docs/programme/CONTINUITY-REMAINDER-01_*`. Bridge candidate `0f58a7f93` on
> `feature/l1-production-base` — unmerged, undeployed.*

**(b) The W1 log is gone, and its absence is not evidence.** ⭐ Without this a future session may
re-hunt a log that no longer exists, or worse, read its absence as a finding.
> *The `🌉 [L1/bridge]` line did not survive the rollback container; `[5,15,27] via 38` is
> unverifiable and non-load-bearing.*

**(c) The adjacent open lane.** ⚠️ `DEPLOY-ROLLBACK-INTEGRITY-01` is out of scope here but still
open, and the rollback that destroyed the log is the same defect.
> *Rollback primitive witnessed defective — `DEPLOY-ROLLBACK-INTEGRITY-01`, separate lane.*

⭐ **My read:** **(a)** earns its place — a bullet whose evidence cannot be found decays into
assertion. **(b)** and **(c)** are genuinely optional.

---

## 3 · STANDING

```text
bullet ........................... ✅ DRAFTED · ⛔ NOT INSTALLED
CLAUDE.md ........................ ⛔ UNTOUCHED
optional additions ............... ⚠️ OFFERED · ⛔ NOT APPLIED
install ........................... ⛔ REQUIRES AN EXPLICIT FOUNDER ACT
```
