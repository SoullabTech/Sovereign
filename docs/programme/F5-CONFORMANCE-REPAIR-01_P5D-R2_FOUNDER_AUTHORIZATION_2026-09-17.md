# F5-CONFORMANCE-REPAIR-01 · P5-D-R2 — FOUNDER AUTHORIZATION

**Date:** 2026-09-17
**Founder act:** continuation after P5-E runtime seed STOP at `32e5eb11b3bfce900fd5c2c95aff1ccbb4060462`

```text
R2 SUBJECT                    87f4ef0514360fb66cd16cb5e3d197ae6fc781c2
RETURNED GATE                 P5-E synthetic Circle seed
R2 MODE                       CIRCLE FENCE RECORD-SHAPE REPAIR
PRODUCTION / STAGING          CLOSED
EXECUTOR / RESTORE REHEARSAL  CLOSED
CANONICAL MERGE / DEPLOY      CLOSED
```

## Authority spent

R2 may repair only the dedicated P5-D Circle anti-resurrection trigger so that each attached relation is evaluated against its actual row shape. It may add focused falsifiers and disposable PostgreSQL witnesses proving the returned defect is closed.

R2 may not change erasure dispositions, activation registry classifications, executor sequencing, route/client behavior, lineage rules, Circle constitutional outcomes, or governed-restore semantics.

## Acceptance

R2 passes only if:

1. ordinary pre-erasure inserts succeed for `shared_artifacts`, `circle_inquiry_responses`, and `circle_memberships`;
2. no Circle trigger function references a column absent from the relation it serves;
3. an erased member cannot create an active share, live response, or active membership outside the governed restore lane;
4. governed restore projects `revoked`, `withdrawn + payload NULL`, and `left`;
5. a fresh PostgreSQL 17.7 canonical bootstrap + full migration runner remains green;
6. P5 regression / sovereignty / type-health gates remain green.

R2 PASS may discharge the P5-E runtime-seed blocker. It does not itself reopen executor or restore rehearsal.
