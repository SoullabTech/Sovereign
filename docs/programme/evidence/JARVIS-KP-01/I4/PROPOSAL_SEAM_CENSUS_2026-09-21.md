# JARVIS-KP-01 / I4 — Proposal-Seam Census

**Date:** 2026-09-21  
**Founder-authorized base:** `20c8764b516e042f38e33800f71b2bb040025665`  
**Actual reconciled opening canonical:** `0319940f9dc94a637e6f1b0e9f843c971f6fb0c2`  
**Purpose:** read-only census before selecting the first epistemic-join integration shadow.

> **NO SEMANTIC JOIN WITHOUT A WARRANT.**

## Freshness reconciliation

Canonical advanced 22 commits after the Founder authorization. The interval was
Writer's Studio/editorial work. It had zero path overlap with canonical I2/I3
and zero overlap with the selected Now What interview seam.

I4 therefore opened from actual current canonical `0319940f...` without
changing the authorized law or authority boundary.

## Candidate proposal/inference seams observed

### 1. Now What interview cell inference — SELECTED

Path:

`app/api/now-what/interview/route.ts`

Existing object:

`CellCandidate { element, phase, confidence, source: 'system_inferred' }`

Existing contract explicitly says:

- inference is read-only and secondary;
- inference must never break the interview;
- no genuine keyword signal returns null;
- the route itself is ephemeral/read+reply and performs no persistence.

Why selected:

- one existing structured system inference;
- member scope is already available from authenticated request custody;
- authorship is clearly system inference rather than member confirmation;
- current output is already non-authoritative and secondary;
- no new synthesis engine is required;
- the active reply path can discard the I4 verdict completely;
- this seam is outside the Writer's Studio freshness interval.

### 2. Writer's Studio structured inference/editorial runtime — NOT SELECTED

Observed surfaces include structured inference and editorial proposal paths.

Not selected because the canonical freshness interval actively modified these
files. Using them for first I4 integration would entangle epistemic-shadow
evidence with an independently moving member-facing authoring lane.

### 3. Practitioner Studio intervention hypotheses — NOT SELECTED

Observed practitioner experiment/hypothesis structures are semantically rich but
sit inside practitioner authority and persistent practice workflows.

Not selected for first shadow because component jurisdiction and practitioner
authorship would make the integration materially broader than necessary.

### 4. Interpretive-ledger accumulating hypotheses — NOT SELECTED

The interpretive-ledger types include accumulating hypotheses and fields that may
influence routing.

Not selected because I4 explicitly grants no routing authority. A first witness
should not start at a seam where hypothesis state and routing are adjacent.

### 5. Research hypothesis pipelines — NOT SELECTED

Research pipeline and research-protocol hypotheses exist, but they are research
subsystems rather than the smallest real member-facing application proposal seam.

## Selected integration boundary

The bounded I4 locus is:

`detectCellCandidate(member turn)`
→ `runNowWhatCellShadow(...)`
→ canonical `evaluateJoin(...)`
→ structural-only telemetry
→ discard.

The adapter does not receive the active response object, provider selector,
router, memory store, graph writer, projection surface, or I3 store.

## Parallel-candidate custody finding

During local falsification, a separate local worktree was discovered at:

`/Users/soullab/jarvis-kp-i4-shadow-20260921`

with local head:

`19ee4fad0a40a723f8c76900a0113db96640de23`.

That candidate is intentionally not adopted into this lane because its file
population includes a new database migration and telemetry store. The current
Founder authorization explicitly prohibits I4 schema/migration changes beyond
canonical I3.

The alternate worktree was left untouched. No competing I4 PR was present on
GitHub at candidate time.

## Census disposition

> **Now What cell inference is the smallest stable real proposal seam capable of
> proving canonical I2 shadow integration without activating I3 persistence,
> representation authority, routing, or member-visible behavior.**
