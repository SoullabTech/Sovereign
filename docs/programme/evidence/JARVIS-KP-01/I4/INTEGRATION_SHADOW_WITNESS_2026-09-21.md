# JARVIS-KP-01 / I4 — Integration Shadow Witness

**Date:** 2026-09-21
**Founder-authorized base:** `20c8764b516e042f38e33800f71b2bb040025665`
**Reconciled opening canonical:** `0319940f9dc94a637e6f1b0e9f843c971f6fb0c2`
**Exact implementation head witnessed:** `193e2af37845aa215bac8ffe8300dd7f069f415c`

> **NO SEMANTIC JOIN WITHOUT A WARRANT.**

## 1. Scope witnessed

Executable I4 changes at the witnessed implementation head:

1. `app/api/now-what/interview/route.ts`
2. `lib/ain/epistemic-join/shadow/feature.ts`
3. `lib/ain/epistemic-join/shadow/nowWhatCell.ts`
4. `tests/constitutional/epistemic-join/i4-shadow-matrix.ts`

The selected live-path seam is the existing Now What `CellCandidate`.

The route's active response shape remains unchanged. The shadow result is not
assigned and is not included in the returned JSON.

## 2. I4 falsification matrix

Executed:

`npx tsx tests/constitutional/epistemic-join/i4-shadow-matrix.ts`

Final result:

`20/20 PASS`

The matrix proves:

1. flag absent → OFF;
2. false-like values → OFF;
3. literal-1 flag reaches canonical I2;
4. active response shape does not consume shadow verdict;
5. no routing handle;
6. no provider/model-selection handle;
7. no Member Memory writer;
8. no Wisdom Graph writer;
9. no Living Constellation writer;
10. no Relational Practice Ledger writer;
11. no I3 store import/invocation;
12. malformed translation is behaviorally inert;
13. missing warrant blocks elevation while lower-standing representation remains possible;
14. missing jurisdiction fails closed before evaluation;
15. member confirmation is not fabricated;
16. reference/reliance remain explicit and disjoint;
17. telemetry-sink exceptions cannot fail shadow execution;
18. telemetry contains no member scope, proposition text, or inferred content label;
19. downstream representation authority remains literally false;
20. I4 shadow flag cannot activate I3 persistence.

Witness footer:

```text
RESULT: 20/20 PASS
SHADOW AUTHORITY: OBSERVATION ONLY
I3 PERSISTENCE: NOT ACTIVATED
DOWNSTREAM REPRESENTATION: CLOSED
```

## 3. Canonical I2/I3 regression

Executed:

`npm run test:epistemic-join -- --runInBand`

Result:

- 4 suites PASS;
- **104 / 104 tests PASS**.

Executed:

`npm run matrix:epistemic-join-i3`

Result:

- **18 / 18 PASS**.

This preserves canonical I2 evaluation and canonical I3 persistence law.

## 4. Dedicated epistemic typecheck

Executed:

`npm run typecheck:epistemic-join`

Result:

**PASS — exit 0**

## 5. Repository full-typecheck control

Candidate run reported:

- program diagnostics: 229;
- baseline: 239;
- three identities reported by the baseline gate.

The same command was then run against exact opening canonical
`0319940f9dc94a637e6f1b0e9f843c971f6fb0c2`.

Canonical control reproduced the **same three identities exactly**:

- `app/wisdom-keepers/sacred-texts/page.tsx:207 TS2322`;
- `components/focus/InboxTriage.tsx:131 TS2322`;
- `components/focus/NextStepBuilder.tsx:167 TS2322`.

Therefore:

> **I4 introduces zero new full-typecheck diagnostic identities.**

The baseline was not rewritten.

## 6. Diff / import integrity

`git diff --check 0319940f...HEAD`

Result:

**PASS**

Static forbidden shadow import scan covered:

- I3 persistence store;
- Member Memory;
- Living Constellation;
- Wisdom Graph;
- Relational Practice Ledger;
- LLM/provider selection;
- routing imports.

Result:

**CLEAN**

The local witness worktree was clean after execution.

## 7. Default-OFF / persistence separation

I4 flag:

`AIN_EPISTEMIC_JOIN_INTEGRATION_SHADOW_ENABLED`

is enabled only by literal:

`1`.

Canonical I3 persistence flag:

`AIN_EPISTEMIC_JOIN_PERSISTENCE_ENABLED`

remains separately default-OFF.

The I4 matrix proves that an environment enabling I4 alone does not enable I3.

## 8. Structural telemetry proof

Telemetry is restricted to structural categories:

- fixed surface;
- shadow status;
- requested jurisdiction;
- admitted standing;
- refusal codes;
- reliance validity;
- composite-required state;
- non-authority markers.

It excludes member scope, message/proposition text, inferred cell label, source
text, clinical content, Sanctuary content, and private practitioner content.

## 9. Behavior / authority witness

The I4 adapter has no reference to:

- active response object;
- router;
- provider selector;
- memory writer;
- graph writer;
- projection writer;
- I3 store.

Canonical I2 remains the evaluator.

The route discards the result.

No standing act is written.

No adoption act is written.

No persistence occurs.

No downstream representation authority opens.

## 10. Parallel-candidate custody

A separate local I4 candidate was discovered at:

`19ee4fad0a40a723f8c76900a0113db96640de23`.

It adds a new I4 database migration and telemetry store. Because the Founder
authorization explicitly prohibits I4 schema/migration changes beyond canonical
I3, that candidate was not adopted or modified.

At witness time, no competing I4 PR was present.

## 11. What this witness establishes

The evidence supports:

> **Real application proposals can be evaluated through canonical I2 in shadow
> mode while remaining behaviorally inert and without activating canonical I3
> persistence or downstream representation authority.**

This witness does **not** establish or authorize:

- production shadow;
- I3 feature activation;
- response influence;
- memory or graph mutation;
- projection;
- standing promotion;
- routing or provider changes;
- I5;
- deployment.

The final candidate SHA is the branch head after the evidence-only records and
is returned for separate Founder adjudication.
