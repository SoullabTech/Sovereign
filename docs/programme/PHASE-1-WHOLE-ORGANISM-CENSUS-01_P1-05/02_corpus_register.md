# PHASE-1-WHOLE-ORGANISM-CENSUS-01 · P1-05 · 02 · REMAINING CONTRADICTION CORPUS — ADJUDICATION REGISTER

```text
STEP     P1-05 · CONTRADICTION / GAP PASS — worker 02 (remaining corpus)
INPUT    P1-04 MAP4_contradiction.md (92 item blocks, 12 PARTs) · P1-05 INSTRUMENT · P1-04 AMENDMENT-4
         · P1-02 INSTRUMENT (D-P1-06 · D-P1-07 · D-P1-08) · P1-03 INSTRUMENT (INF-1…INF-5) · P1-04 INSTRUMENT (SYN-1…SYN-4 · INF-7)
SCOPE    every MAP 4 item EXCEPT the six owned by `01_named_objects`
TYPE     RECORD ONLY — ⛔ no repair · no design · ⛔ no organism re-tracing · ⛔ no runtime inspection
```

> ⭐⭐ **The purpose of adjudication is not to make the organism consistent. It is to determine
> precisely where the evidence permits consistency, contradiction, or uncertainty to be claimed.**

> **Adjudication may determine WHAT KIND of contradiction the existing evidence contains. ⛔ It may
> not manufacture new evidence to make the contradiction disappear.**

---

## 0 · Declared before use

### 0a · ⛔ Owned elsewhere — referenced by id, never adjudicated here

```text
X-DEF-1 · X-DEF-2 · X-DEF-3 · CONCLUDE COUNT · DEV-54/51 · the MAP 3 slice-05 tally discrepancy
                                                        → owned by 01_named_objects
```

Where an item below depends on one of those six, it says so and is listed again in §6.

### 0b · ⭐⭐ What a classification does NOT mean

```text
⛔ "RESOLVED BY SCOPE" / "RESOLVED BY ALTITUDE" ≠ reconciled · compliant · benign · repaired
✅ it names THE KIND OF DIVERGENCE THE EVIDENCE SUPPORTS, and nothing else
```

⛔ No item here is called legitimate or illegitimate. ⛔ `ABSENT` is not written. ⛔ `AUTH-EXPOSURE-01`
is neither cited, awaited nor answered. ⛔ No item is ranked by severity, likelihood or repair order.

### 0c · The two operating principles this worker applied, stated once

```text
P-A   DESCRIPTIVE ↔ DESCRIPTIVE       an in-source claim ABOUT WHAT THE CODE DOES, quoted by the
                                      corpus against the code that does otherwise, is C5 — a comment,
                                      a registry field or a header sentence is not a different
                                      altitude from the behaviour it describes
P-B   NORMATIVE ↔ DESCRIPTIVE         a rule · policy · ADR · canon clause · declared scope, against
                                      an implementation, is C2 — both hold (INF-4: IMPLEMENTED ↛
                                      GOVERNED, and its converse), and the divergence stands recorded
```

⭐ `C2` is used only where BOTH ALTITUDES ARE NAMED AND BOTH TEXTS QUOTED. ⭐ `C1` is used only where
the source records themselves distinguish the objects/paths/conditions — ⛔ never by assertion.
⛔ Where either requires a likelihood judgement, the outcome is `C6`.

### 0d · ⛔ Forbidden promotions, binding on every line below

```text
⛔ "GOVERNED ACT = 0"  →  "no acts are governed"        ✅ "no row in this census established GOVERNED ACT"
⛔ the 62 rows         →  "62 unauthorized capabilities" ✅ named findings, each keeping its row id
⛔ NONE LOCATED        →  ABSENT                         ⛔ UNKNOWN → NONE LOCATED
⭐⭐ Numerically equal states are not equivalent when their epistemic provenance differs.
```

### 0e · `C4` carries two sub-kinds and they are marked, ⛔ never pooled

```text
C4-CENSUS     a defect in the CENSUS's own record or bookkeeping   → artifact 2 material
C4-ORGREC     a defect in the ORGANISM's own records / identifiers, ⛔ not organism behaviour
```

---

## 1 · PART III-A · register 01 · domains A · B · C (20 items)

**ITEM `A-1 · Registry vs orchestrator`**  ·  **OUTCOME `C5 GENUINE CONTRADICTION`**
BASIS · 01_normalized_A_B_C: Side A `maiaRuntimeContext.ts:81` — "between/chat `callsMaiaResponse: false`, reason 'uses maiaOrchestrator, not
getMaiaResponse'"; Side B `maiaOrchestrator.ts:506` — "it does". The registry field's own stated reason names the very function the other record shows
it calls (P-A).
RESIDUE · The carried line — "a live lane reaches cognition outside the wrapper its own header (`maiaRuntimeContext.ts:3-5`) declares mandatory" — is
a further finding on the wrapper, ⛔ not adjudicated here.

**ITEM `A-2 · DEEP addenda, in one function`**  ·  **OUTCOME `C5 GENUINE CONTRADICTION`**
BASIS · 01_normalized_A_B_C: Side A `maiaService.ts:2519-2521` — "buildComprehensiveVoicePrompt … currently does NOT iterate MaiaContext addenda";
Side B `maiaService.ts:2526-2535` + `maiaVoice.ts:972` — "it does. Four lines apart." Same function, same file, same tense; the comment is
descriptive, not normative (P-A).
RESIDUE · NONE. ⛔ No dated evidence of two states exists in the corpus, so no `C3` reading is available.

**ITEM `A-3 · Guardrail parity`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · 01_normalized_A_B_C states the scoping itself: "The witness is true of `floor.ts:23-26`; it is not true of `fastPathResponse`." Side A is
`CMT-01_M0-M2_WITNESS_2026-09-03.md:82-84` ("FAST prompt opens with the runtime prompt and closes with the three guardrails"); Side B is
"`lib/sovereign/maiaService.ts` contains none of the three identifiers". Two named objects, one true of each.
RESIDUE · Which object the witness's phrase "FAST prompt" denotes is not settled by the records; if it denotes the served FAST path rather than
`floor.ts`, the item reopens as `C5`. ⛔ Not decided here.

**ITEM `A-4 · Oracle lane`**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · ALTITUDE 1 — session-anchor operational evidence: `CLAUDE.md` — "~zero live traffic" (D-P1-06: "CLAUDE.md = operational / session instruction
evidence"). ALTITUDE 2 — implementation: `oracle/conversation/route.ts:452` — "hard 410 as the first executable statement of POST". Both hold; a route
refusing at its first statement and a record of ~zero served traffic are not mutually exclusive. The record's own carried line: "Per D-P1-06 the
anchor is evidence, not law; the code stands."
RESIDUE · Whether the route serves member traffic at all is UNKNOWN (X-3 · D-1 · C-F5), and register 01 declares "whether the 2026-05-23 traffic audit
still describes the subject (A-01)" unanswered (UC-ABC-Q). ⛔ The altitude classification settles the KIND only; it establishes no traffic fact in
either direction.

**ITEM `A-5 · Reported tier vs executed tier`**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · ALTITUDE 1 — client-facing report field: `maiaService.ts:3245` reports "`processingProfile: 'DEEP'` **for client compatibility**" — the source
itself declares the field a compatibility value. ALTITUDE 2 — execution: "the turn is an RCN turn that 'ran no tier'". Both hold.
RESIDUE · The emitted value misdescribes the executed path; whether any consumer reads `processingProfile` as a claim about execution is not
established in the corpus. ⛔ Not traced.

**ITEM `A-6 · Sole-gateway claim`**  ·  **OUTCOME `C5 GENUINE CONTRADICTION`**
BASIS · Side A `modelService.ts:72` — "Main gateway for ALL text generation in MAIA"; Side B — "at least four independent model reaches bypass it —
getClaudeService (A-14), six `new Anthropic()` routes (A-15), A-13's MultiLLMProvider, DEEP-primary's consciousnessWrapper". The corpus states the
verdict in its own words at the row: "A-16 … 'THE GATEWAY CLAIM IS FALSE AT THE SUBJECT'" (04_ladder INF-6 inventory, E-24). A universal descriptive
claim against four named counter-reaches (P-A).
RESIDUE · Shares its sentence with C-3 (G) (E-23), whose Side B is a different artifact (the import allowlist). ⛔ The two items are not merged; each
keeps its own basis.

**ITEM `A-7 · Egress mislabel`**  ·  **OUTCOME `C5 GENUINE CONTRADICTION`**
BASIS · Side A `list/route.ts:1390` emits "`endpoint: '/api/sovereign/app/maia'`"; Side B — "it is emitted 'from the /list handler'", and the source
itself concedes it: "acknowledged in-source as a pre-existing mislabel, left untouched". An emitted identifier and its emitter cannot both be as
stated.
RESIDUE · What downstream telemetry keyed on that field attributes to which endpoint is not evaluated in the corpus. ⛔ Not traced, ⛔ not repaired.

**ITEM `B-1 · "unmapped" vs wired to a prompt`**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · ALTITUDE 1 — registry classification: `substrateMap.ts:204-251` classifies eight services "`underutilized-consciousness`", notes "unmapped",
and for Morphic "declared service, no producer, no runtime slot" — predicates about producer-registry membership. ALTITUDE 2 — import graph:
`MemoryPalaceOrchestrator.ts:8-15` "imports all eight", ":21-88 retrieves from seven, :175-233 writes to four", and
`oracle/conversation/route.ts:902,:1499,:2787` "consumes and prompt-injects". Both hold.
RESIDUE · Whether `substrateMap`'s classification was intended as a reachability claim is not established; if it was, the item reopens as `C5`. Route
liveness is UNKNOWN (carried: "Whether that route serves traffic is `UNKNOWN`"). ⛔ The enumeration limb "`consumers: []`" is a different quote and is
adjudicated at CONTRA-2.

**ITEM `B-2 · memoryHealth.pattern`**  ·  **OUTCOME `C5 GENUINE CONTRADICTION`**
BASIS · Side A `memoryHealth.ts:103` — "`pattern?: { count } // theme_signals feeds this`", under "Wired by Cut 1"; Side B `route :1178-1209`
"supplies no `pattern` input, so `layerStatus(undefined) = 'empty'` on every turn — while route :947 loads theme signals and :991 routes them into the
FAST prompt". Both are descriptive claims about the same field's feed (P-A).
RESIDUE · The theme-signal load at :947 is real and reaches the FAST prompt by another path; ⛔ that does not rescue the `pattern` field's stated feed,
and the two paths are not merged (see X-1).

**ITEM `B-3 · memoryHealth.semantic`**  ·  **OUTCOME `C5 GENUINE CONTRADICTION`**
BASIS · Side A `memoryHealth.ts:63` declares `semantic`, documented at :49 as "enduring facts, roles, relationships, preferences"; Side B
`memoryHealth.ts:97-100` + `route :1182-1186` — what feeds it "is an atoms row count" and "no semantic retrieval exists on this path"; and
`MAIA_MEMORY_INTEGRITY_GAP_MAP_2026-08-04.md:38` — "a self-report that misstates the memory state MAIA is in… occurring in the observability layer
itself". Same field, declared denotation against its actual feed.
RESIDUE · The gap map is a third source agreeing with Side B; ⛔ its governing standing is not established and is not needed for the classification.

**ITEM `B-4 · Two definitions of decay`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · Two distinct named objects: `confidenceDecay.ts` exporting `calculateDecayedConfidence` (TS, "imported at `MemoryBundle.ts:16`") and the SQL
`calculate_decayed_confidence` ("`MemoryBundle.ts:266` applies the SQL … the TS function is never called in that file").
RESIDUE · "there is no single authoritative definition of decay today" — ⛔ NONE LOCATED, not ABSENT. The semantic divergence between the two (what
member confirmation does) is C-2 (§8.2); the anchor's "0.0225" claim has no located referent (CONTRA-6 · UC-ABC-Q · E-31).

**ITEM `B-5 · The influence plan's tier scope`**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · ALTITUDE 1 — computation and telemetry: "route :991 computes `memoryInfluenceAddendum` on every eligible turn at every tier; route :1229
reports it in `buildMaiaRuntimeContext.addenda`". ALTITUDE 2 — prompt assembly: "`maiaService.ts:3291-3308` establishes it cannot reach a CORE or DEEP
prompt, against a recorded prevalence of 'CORE 72.8% / FAST 27.2%'". The corpus states the reconciliation itself: "The telemetry was repaired; the
behaviour was deliberately left, named a 'deliberate product decision, deferred by founder ruling 2026-08-13.'"
RESIDUE · A reported addendum that cannot reach the prompt on the majority-tier turns; the gap is deferred by a dated ruling, ⛔ not closed. ⛔ The
ruling is recorded as evidence, not adjudicated.

**ITEM `B-6 · Episodic consent`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · Different named objects: read path — "`memoryLoaders.ts:332` reads `episodic_recall_enabled`; `producerRegistry.ts:133` names it
`consentBasis`"; member-facing preference route — "`recall-preferences/route.ts:43-45` admits one column and names episodic only in a comment (:20) as
future work". Both true of their own object.
RESIDUE · ⭐ The column the registry names as the CONSENT BASIS has no located member-facing write surface; the column's default and any other writer
are ⛔ NOT LOCATED in the permitted evidence — owed read, §5.

**ITEM `B-7 · `lattice` recalls and discards`**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · ALTITUDE 1 — implementation: "`maiaService.ts:3057` performs a real resonance recall, mode-gated :3049-3053, truncated per mode :3069-3085".
ALTITUDE 2 — consumption at runtime: "nothing consumes it (:3102 `memoryField = null`)". Both hold; neither refutes the other.
RESIDUE · The item's contradiction character rests on the corpus's intent reading — "the shape of code written to be used" — and ⛔ no source states
that the recall is expected to reach the prompt. ⛔ Not resolved into a defect claim.

**ITEM `B-8 · Six enumerations of the memory arenas`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · Six differently-named objects of different kinds: "`MemoryHealth` (12) · `MemoryHealthInputs` (11) · `MemoryHealth` as populated (8) ·
`MemorySource` (9) · `ADDENDA_SPECS` (27) · `substrateMap` (30) — none a subset of another", with "⛔ No code enumeration nominated as authoritative."
The records name each object separately and state no common inclusion rule.
RESIDUE · ⭐ Any sentence of the form "MAIA has N memory arenas" is unevaluable until the enumeration is named — the same discipline the VC-* items
carry. Inside one object, declared 12 against populated 8 is a declaration/population gap, ⛔ not a second enumeration. ⛔ No winner chosen (also
VC-memory-corpus).

**ITEM `C-1 (§8.1) · `member_patterns` ownership     ⚠️ source-label collision: register 01's C-1, ⛔ not C-1 (G)`**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · ALTITUDE 1 — ownership/governance document: `docs/DATA_BOUNDARIES_AND_OWNERSHIP.md:28` places the table in MAIA Core, "Inner Patterns | MAIA's
memory of themes, growth edges", "in a domain whose declared Owner is 'The member' (doc:9)". ALTITUDE 2 — implementation: "the schema is
`practitioner_id UUID NOT NULL REFERENCES practitioners(id)`; rows are created by a practitioner lane; `getMemberPatterns.ts:55` withholds `emerging`
rows from the member as 'practitioner-internal'". Both hold (INF-4).
RESIDUE · Whether that document is ratified — i.e. whether altitude 1 is governance at all — is ⛔ not established in the permitted evidence; if it is
not, the item reopens as `C6`. P1-GOV-ACCESS-01 stands adjacent. ⚠️ E-29 leaves undetermined whether Side B's `practitioner_id` co-refers with C-I-7's
token.

**ITEM `C-2 (§8.2) · Confidence decay, two live impls    ⚠️ register 01's C-2, ⛔ not C-2 (G)`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · Two named implementations: Side A "TS `halfLifeDays * 1.5` on member confirmation"; Side B "SQL reference-date shift
(`COALESCE(last_confirmed, formed_at)`) plus a `GREATEST(0.3, …)` floor".
RESIDUE · ⭐ The corpus's own sentence is the residue and is not dissolved by the classification: "The two are not two spellings of one rule; they
express different semantics for what member confirmation does." Which is reached on which path is stated only for retrieval (CONTRA-6 Side A: "the SQL
function is what runs in retrieval"); no artifact nominates either authoritative. ⛔ NONE LOCATED, not ABSENT.

**ITEM `C-3 (§8.3) · R16 guard scope vs wiring     (source class: TENSION)`**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · ALTITUDE 1 — declared rule: "the guard declares a class-level rule binding 'any response-shaping subsystem' and is written to fire for future
fields". ALTITUDE 2 — wiring: "it is invoked at exactly one site; two other loaders of the same table exist and do not pass through it". Both hold.
RESIDUE · The corpus expressly declines the defect characterization — "⛔ Not characterised as a defect" — and that refusal is carried, ⛔ not
overridden. Whether the two unguarded loaders are reached is not established.

**ITEM `C-4 (§8.4) · route liveness vs guard placement`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · The corpus withholds the fact the adjudication needs: "⚠️ No runtime witness for either route was located in-repo, so neither side is
confirmed under the LIVE calibration." Side A `CLAUDE.md` "~zero live traffic" + "names the list route as live"; Side B "the R16 guard's sole call
site is in `oracle/conversation/route.ts`". Under INF-1 (WIRED ↛ LIVE) neither side is evaluable.
RESIDUE · OWED READ (§5): a dated in-repo runtime witness for either route under the LIVE calibration. ⛔ Not performed. Depends on X-DEF-2 (owned by
`01_named_objects`).

**ITEM `C-5 (§8.5) · Practitioner access`**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · ALTITUDE 1 — written scope model that does not run: "`practitionerProjection.ts` and `relationship/scope.ts` state strict, non-merging,
server-derived scope rules — **and are dormant**". ALTITUDE 2 — the live read: "`studio/clients/[id]/patterns/route.ts:33-44` gates on an
authenticated practitioner plus a `practitioner_clients` linkage row 'carrying no consent or scope column'". Both hold.
RESIDUE · ⭐ The written model does not govern the live path (INF-1 · INF-4); P1-GOV-ACCESS-01 — "PRACTITIONER / MEMBER VISIBILITY GOVERNANCE ABSENT" —
is the standing finding, ⛔ and no verdict of legitimacy is drawn here. Twinned with C-I-3 (E-28).

---

## 2 · PART III-B · register 02 · domains D · E · F (20 items)

**ITEM `CONTRA-1 · default-OFF flag one caller does not consult`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · The records name the two reaches separately: gated — "`isPFIMindEnabled()` at :247, consulted at `maiaService.ts:2916`", with
"`MAIA_PFI_MIND=true`: Enable PFI mind **entrypoint** (default: OFF)"; ungated — "`fieldOrchestrator.ts:161-165` sets `pfi: true` unconditionally …
reached from `maiaService.ts:1521` and :1946 with no flag check". The flag's own text scopes itself to an entrypoint.
RESIDUE · ⭐ The flag's declared default is true of the entrypoint it names and ⛔ not of the capability; INF-3 — a flag selecting behaviour authorizes
nothing, in either direction. Whether the ungated reaches run on member turns is ⛔ not re-traced here.

**ITEM `CONTRA-2 · "no live consumer" vs the import graph`**  ·  **OUTCOME `C5 GENUINE CONTRADICTION`**
BASIS · Side A `substrateMap.ts:383-391` — "Resonant field memory … **consumers: []** … Service preserved; no live consumer wired"; Side B —
"`CoherenceFieldService` is imported at `MemoryPalaceOrchestrator.ts:14`, called at :75,:188,:249". An empty consumer enumeration against three
located call sites on the same named class (P-A).
RESIDUE · ⚠️ The second limb — "no **live** consumer wired" — is ⛔ NOT adjudicated as contradicted: under INF-1 an import is not liveness, and the
only traced onward route's liveness is UNKNOWN. ⭐ The `C5` attaches to `consumers: []`, ⛔ not to the word "live". Carried: "(For QuantumFieldMemory,
side A holds.)"

**ITEM `CONTRA-3 · "DORMANT" on an object on the canonical path`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · Side A names one token — `fieldMonitorTelemetry.ts:165` "Field Intelligence (activates DORMANT **talkModeFieldIntelligence** …)"; Side B names
another — "**analyzeFieldIntelligence** is dynamically imported and used at `maiaService.ts:1082-1112`". ⛔ No sentence in the permitted evidence
states that the two tokens name one object, and the hard vocabulary rule forbids merging them on similarity. Reading "DORMANT" as contradicted
therefore requires a likelihood judgement.
RESIDUE · OWED READ (§5): the referent of `talkModeFieldIntelligence` at `fieldMonitorTelemetry.ts:165`. ⛔ Not performed. Side B's onward claim
("producing a prompt block on the canonical member path") is the disputed side of X-DEF-1 (owned) — §6.

**ITEM `CONTRA-4 · "The ONLY file that knows how the field engines connect"`**  ·  **OUTCOME `C5 GENUINE CONTRADICTION`**
BASIS · Side A `fieldOrchestrator.ts:3` "asserts sole-seam status"; Side B — "at least four other seams connect field engines into cognition —
`maiaService.ts:1082`, `maiaService.ts:2838`, `oracle/conversation/route.ts:853`, `:902`, `voice/stream-conversation/route.ts:82`". A universal
descriptive claim against named counter-seams (P-A).
RESIDUE · Two of the named counter-seams sit on a route whose liveness is UNKNOWN, and one (`:1082`) is the block disputed at X-DEF-1 (owned); ⛔ the
sole-seam claim is not rescued by either, since `maiaService.ts:2838` (`enforceFieldSafety`) is undisputed in the corpus.

**ITEM `CONTRA-5 · `persistentFieldStates` is not persistent`**  ·  **OUTCOME `C5 GENUINE CONTRADICTION`**
BASIS · Side A `QuantumFieldMemory.ts:2-6` — "maintaining persistent field states"; Side B — "`:91` is `new Map()`; the file's only import is
`crypto`; no `query`/`INSERT`/`SELECT` in 810 lines". The `C5` attaches to the header SENTENCE, a descriptive claim (P-A).
RESIDUE · ⛔ Per the vocabulary rule no finding attaches to the identifier `persistentFieldStates` itself; that limb is carried at
VC-names-asserting-what-they-do-not-carry (E-34).

**ITEM `CONTRA-6 · two SQL decay definitions and one TS helper`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · Named objects, and the record states which runs where: Side A — "SQL, both copies identical: baseline:711-737 and
`20251231_memory_architecture_enhancements.sql:178-214` — no confirmed-memory bonus of any kind; **the SQL function is what runs in retrieval**"; Side
B — "`lib/memory/confidenceDecay.ts:78-79` — `effectiveHalfLife = confirmedByUser ? halfLifeDays * 1.5 : halfLifeDays`".
RESIDUE · ⭐ Side C is a separate, unresolved limb: the anchor's "the SQL confirmation term caps at 0.0225" is "NOT confirmed — no confirmation term of
any form exists in either SQL copy", while register 01 records "the referent of 'the SQL confirmation term caps at 0.0225' (C §10, **NOT FOUND at the
subject**)". With the referent NOT LOCATED, whether the anchor's claim conflicts with the quoted SQL is ⛔ undetermined — owed read, §5.

**ITEM `C-E1 · Phase is 1–3, and phase is 1–12`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · Two named objects, and the record states the conversion between them: "`spiralogic-core.ts:17` — `export type Phase = 1 | 2 | 3`, a 12-cell
grid of 4 elements × 3 phases"; "`20260213200001_member_spiral_state.sql:19` — `phase INTEGER CHECK BETWEEN 1 AND 12`; `conductor.ts:24-28` normalizes
to 1..12. **The conductor writes the former into the latter.**"
RESIDUE · ⛔ The corpus does not establish that a 1..12 grid coordinate and a 1..12 scalar are the same quantity, nor that the normalization is
invertible. VC-phase carries the wider collision.

**ITEM `C-E2 · And phase is `Intelligence | Intention | Goal``**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · A third named vocabulary at a named site: "`maiaService.ts:1134-1137` — a third vocabulary on the canonical lane, computed and deliberately
not appended. Both stand alongside C-E1."
RESIDUE · ⭐ "MAIA assigns a phase" remains unevaluable until the referent is named (VC-phase). The "deliberately not appended" limb is a separate fact
about surfacing, ⛔ not adjudicated here.

**ITEM `C-E3 · the route both has no phase and injects one`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · "`maiaService.ts:1727/:1737/:2275` state in code that the route has no phase and default to 1" against "`maiaService.ts:1098` computes `Phase
detected: …` for the same route's prompt", carried as "made by the same file about the same turn". ⛔ But the corpus also declares, at VC-phase, that
it "cannot evaluate 'MAIA assigns a phase' until the referent is named", and it names ⛔ no referent for either site. Whether the two statements
concern one phase vocabulary or two therefore requires a likelihood judgement.
RESIDUE · OWED READ (§5): the phase vocabulary in force at `:1727/:1737/:2275` and at `:1098`. ⛔ Not performed. Depends on VC-phase.

**ITEM `C-E4 · "8 voices" vs the code`**  ·  **OUTCOME `C5 GENUINE CONTRADICTION`**
BASIS · Side A `CLAUDE.md` — "MythicAtlas + MaiaVoice + ShadowAgent + Fire/Water/Earth/Air/Aether"; Side B code — "MythicAtlas + MaiaVoice +
**WisdomRouter** + up to six elemental rows in which `shadow` is an ELEMENT VALUE, not a separate ShadowAgent". Both are descriptive enumerations of
one composition (P-A).
RESIDUE · ⭐ D-P1-06 constrains the reading, ⛔ not the class: the anchor "is not permitted to override a ratified or otherwise stronger governing
source merely because every session reads it first", and the conflict is PRESERVED + REPORTED. ⛔ No repair. ⛔ No dated evidence of an earlier
composition exists, so no `C3` reading is available.

**ITEM `C-E5 · "imported only by soulPortrait/schema" vs zero importers`**  ·  **OUTCOME `C5 GENUINE CONTRADICTION`**
BASIS · Side A "the cited import"; Side B "at the subject `lib/soulPortrait/schema.ts:19` is a **comment, not an import**". Carried: "The subject
reading governs E's record; both preserved."
RESIDUE · The artifact that carries the citation is ⛔ not identified in the permitted evidence, so no provenance or dating of the claim is available
and no `C3` reading can be tested.

**ITEM `C-E6 · continuity substrate with no writer`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · Side A "migration :1-5 'prevents treating returning members like new people'; `CLAUDE.md` documents a live wire into
`oracle/conversation/route.ts`"; Side B "that route refuses at `:446-453`". Side B is exactly the characterization X-DEF-2 holds open — the same route
is recorded there as a live cognition path, as unconditionally refused, and as ~zero traffic, with the corpus refusing to let any settle it.
RESIDUE · Depends on X-DEF-2 (owned by `01_named_objects`) — §6. OWED READ (§5): a dated runtime witness for the route. ⛔ Not performed. ⛔ "no
reachable writer" is NOT LOCATED, not ABSENT.

**ITEM `C-E7 · R16 strips; the member API returns unstripped`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · The corpus names the scoping itself: "**Two different boundaries, one field class.**" Side A `developmentalStateAdmission.ts:31-39,:61`
(prompt-shaping admission); Side B `app/api/members/spiral-state/route.ts:28-29` (member-facing read). Both true of their own boundary.
RESIDUE · Whether one field class ought to cross both boundaries is a governance question with ⛔ no located source; P1-GOV-ACCESS-01 stands adjacent.
⛔ Not adjudicated (the corpus's own words).

**ITEM `C-E8 · S-16 shape against a nonexistent Prisma model`**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · ALTITUDE 1 — implementation: "code present and route-exposed". ALTITUDE 2 — data-model declaration: "`prisma.spiralProcess` has no declared
model among 53". Both hold; a route may exist while the model it names is undeclared.
RESIDUE · What happens if the path is reached is ⛔ not established — no runtime witness, and reachability is not traced. ⛔ No inference drawn from the
absence of a model to the behaviour of the route.

**ITEM `C-F1 · I Ching prompt vs the symbolic lens boundary`**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · ALTITUDE 1 — ratified canon: `MAIA_SOVEREIGNTY_INVARIANTS.md:245`, "Tier 1 names I Ching: only 'this tradition associates…'". ALTITUDE 2 —
deployed implementation: `app/api/changes/[id]/interpret/route.ts:21,40` (+ identical studio route) — "Do not explain the I Ching tradition — speak
from within it." / "Speak as if the hexagram itself is addressing the person." The corpus states the altitude difference itself: "⛔ Side B is
canon-bound; Side A is deployed on a member-authenticated, persisting route."
RESIDUE · ⭐ A same-altitude limb remains: two deployed prompts differ from each other — `maia/list/route.ts:283` "frame it explicitly as a traditional
association" against `interpret/route.ts:21`. Two routes are two objects (`C1`), yet one canon clause is quoted over both. ⛔ Preserved, ⛔ not
repaired, ⛔ no verdict of legitimacy.

**ITEM `C-F2 · `safe_for_retrieval``**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · ALTITUDE 1 — protocol document: `CORPUS_DISCIPLINE_PROTOCOL_v1.0.md:104` — "indexed but never retrieved". ALTITUDE 2 — implementation: "zero
occurrences in code or SQL". The corpus itself refuses the truth-contest framing: "not resolved by deciding which is 'really' true."
RESIDUE · ⭐ The protocol's mechanism is NONE LOCATED in code, ⛔ not ABSENT; whether the discipline is achieved by some other located mechanism is ⛔
not established.

**ITEM `C-F3 · P1-01 slice 05 versus the code on tarot`**  ·  **OUTCOME `C4 RECORD DEFECT  (C4-CENSUS)`**
BASIS · Side A `P1-01` — "tarot named in three canon docs, **zero files**"; Side B the subject re-read — "1,901 ln across 5 files plus a live route".
Carried: "The re-read governs F's record; both recorded." The disagreement is between two census passes, ⛔ not between two organism behaviours.
RESIDUE · ⛔ The P1-01 record is NOT corrected here and is NOT described as needing correction; both readings stand side by side, as the close record
requires of census self-catches. Artifact 2 material.

**ITEM `C-F4 · SYMBOLIC_LENS_BOUNDARY scope vs wiring     (source class: TENSION)`**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · ALTITUDE 1 — declared scope: "declared over six families at `:282`". ALTITUDE 2 — wiring: "applied at two call sites (`:646`, `:732`).
Archetypes, cycles, and the I Ching / Tarot that Invariant 13 adds are not reached." Both hold.
RESIDUE · Carried and ⛔ not converted into a defect claim: the wrapper is "a model-compliance instruction, not a structural refusal. Nothing measures
obedience; no test, falsifier or post-generation check found" (E-18), and Invariant 13's Tier-2 refusal "has ⛔ no implementing code anywhere in Domain
F" (E-19).

**ITEM `C-F5 · the oracle/conversation route's traffic`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · Side A `CLAUDE.md` — "receives ~zero live traffic"; Side B "`oracle/conversation/route.ts:30` imports `evaluateEncounter`;
`OracleConversation.tsx:9723` renders `SacredPassageBlock`". The corpus forecloses the only available resolution: "⛔ the anchor may not settle this by
assertion", and an import plus a render are not traffic (INF-1).
RESIDUE · OWED READ (§5): a dated in-repo runtime witness for the route under the LIVE calibration. ⛔ Not performed. This item is X-DEF-2's SIDE C —
depends on X-DEF-2 (owned), §6.

**ITEM `C-F6 · Two objects named `PersonalOracleAgent``**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · The records distinguish the objects by path: "`lib/agents/PersonalOracleAgent.ts` — consumes `lib/knowledge`, unreached from `app/`" against
"`app/api/_backend/src/agents/PersonalOracleAgent` — reached from two routes; declared sovereignty-violating and retired at
`app/api/maia/chat/route.ts:7,26`". Carried: "⛔ Not merged."
RESIDUE · ⭐ A `C6` sub-question rides inside and is ⛔ not dissolved by the classification: "⛔ which of the two objects those citations mean is NOT
DETERMINED BY SOURCE RECORD" for P3-D-14 · P3-E-19 (E-36, itself classed SYN-4) — owed read, §5.

---

## 3 · PART III-C · register 03 / slice 06 · domains G · H · I (19 items)

⚠️ The declared ID collision is kept: `C-1 (G)` … `C-6 (G)` are register 03's labels, ⛔ not register 01's
`C-1 (§8.1)` … `C-5 (§8.5)`. ⛔ Neither was renamed.

**ITEM `C-1 (G) · provider failure`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · The corpus names the seams and forecloses the altitude reading itself: "**This is not a document-versus-code conflict. Both behaviours are
implemented, on different seams**, at the same subject." DEGRADE — `lib/ai/sovereignRouter.ts:15-17,50-61` returns `DEGRADED_TEXT` "as an ordinary
TextResult on four failure branches"; REFUSE — `lib/ai/structured/router.ts:116-131` returns "`{ ok:false, refusal:'provider_unavailable' }`" with
"THE FAILURE STOPS HERE"; SIDE C — "with `MAIA_INFERENCE_MODE` unset, `modelService.ts:180-193` falls back to a local Ollama text model with no drift
event". PART V records the two coverage families: CONVERSATIONAL TEXT vs STRUCTURED-READING.
RESIDUE · ⭐ "Which governs production: UNKNOWN" — owed read (§5), and the value lies outside the permitted evidence entirely. ⛔ INF-3: an environment
variable selecting the disposition authorizes nothing.

**ITEM `C-2 (G) · CLAUDE.md vs the tree on cloud providers    (also PART IV, one of the two anchor-level divergences)`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · Side A `CLAUDE.md` — "Never use OpenAI or other cloud AI providers." Side B `provider-policy.json` "tiers OpenAI as `lab` /
`removal_in_progress` with a 32-file allowlist — **a governed migration debt, not a prohibition**", with "30 live-tree files import the SDK or call
api.openai.com", `Dockerfile:57` `OPENAI_API_KEY=dummy-build-key`, and "`docs/adr/012` is Open / Deferred". ⛔ The item cannot be classed by altitude
because the corpus declares one altitude undetermined: "**Authority standing UNKNOWN.**" Per the instrument, an altitude that cannot be established is
`C6`.
RESIDUE · ⭐ The norm-vs-tree limb ALONE would be `C2` — a prohibition quoted at governance altitude against 30 named importing files at implementation
altitude (INF-4) — and the SECOND LIMB likewise: "'or other cloud AI providers' is contradicted by Moonshot, in the tree, reachable by a per-request
`meta` flag, in no tier and no guard." ⛔ Neither limb is promoted to the item's class. D-P1-06 binds: the anchor may not override a stronger governing
source by assertion, the divergence is PRESERVED + REPORTED, ⛔ repair unauthorized. OWED READ (§5): whether `provider-policy.json` and ADR-012 are
ratified governing sources.

**ITEM `C-3 (G) · "Main gateway for ALL text generation"`**  ·  **OUTCOME `C5 GENUINE CONTRADICTION`**
BASIS · Side A `lib/ai/modelService.ts:71-73`; Side B `scripts/anthropic-import-allowlist.json` "(2 + 1 + 57)". Carried: "**Both are the project's own
text.**" A universal descriptive claim against the project's own enumeration of direct importers (P-A).
RESIDUE · ⚠️ The allowlist is a CI instrument — INF-2, CI-GATED ↛ RUNTIME GOVERNED — so it evidences importers, ⛔ not governance. Shares its Side A
sentence with A-6 (E-23); ⛔ the two items are not merged.

**ITEM `C-4 (G) · ADR-001 (Accepted) vs `selectClaudeModel``**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · ALTITUDE 1 — governance: ADR-001 "prescribes 7-level awareness → Opus/Sonnet routing 'in `lib/ai/claudeClient.ts`'" and "says all routing
decisions **MUST** follow it". ALTITUDE 2 — implementation: "`claudeClient.ts:50-86` routes on `reasoningMode` and force flags and defaults to Sonnet,
awareness level surviving only in a log string". Both hold (INF-4).
RESIDUE · ⭐ The code asserts a "NEW PHILOSOPHY (Jan 2026)" while "**No superseding ADR or ruling was located**" — NONE LOCATED, ⛔ not ABSENT, and ⛔ an
in-code assertion is not a governing act. OWED READ (§5): a governing-source search for a superseding ADR or ruling. ⛔ Not performed.

**ITEM `C-5 (G) · the degraded string's claim vs the seam`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · Side A `sovereignRouter.ts:16` — "I've saved your message."; Side B — "The router performs no persistence." The corpus withholds the fact that
would settle the end-to-end claim: "**Whether a caller persists the turn is caller-dependent and is not resolved.**"
RESIDUE · ⭐ The ratified wording is carried exactly and ⛔ not strengthened: "The emitting seam makes a persistence claim it does not itself discharge;
end-to-end truth of that claim is caller-dependent and presently unresolved." ⛔ NOT "MAIA lies about saving" — "That stronger statement has not been
earned." OWED READ (§5): whether the callers of `sovereignRouter` persist the turn on the four degraded exits.

**ITEM `C-6 (G) · declared scope of Kimi vs its trigger`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · Side A `modelService.ts:126` — "Never used for live chat - only when explicitly requested"; Side B `:127` "`req.meta?.useKimi`, where `meta`
is `Record<string, unknown>` with **no validation and no provenance**". ⛔ With the flag's provenance undetermined by the corpus's own words, whether a
live-chat request can set it — and therefore whether Side A is contradicted — requires a likelihood judgement.
RESIDUE · OWED READ (§5): the provenance of `req.meta` at the live-chat entrypoints — who may set it. ⛔ Not performed. ⛔ INF-3: a request flag
selecting a provider authorizes nothing.

**ITEM `C1 (H) · MAIA'S DEFAULT VOICE PROVIDER`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · Two sources each claiming canon status over the same default: Side A `lib/tts/cloudVoicePolicy.ts:1-40` ("VOICE-SOVEREIGNTY-01, Founder canon
ruling, 2026-08-27") — "⛔ THE DEFAULT IS THE CANON. Cloud voice is forbidden unless `MAIA_ALLOW_CLOUD_VOICE=1` is set explicitly"; Side B
`app/api/voice/openai-tts/route.ts:115,128-131` — "MAIA vow: default voice is always maia_core (OpenAI Alloy)", with "unset or unknown archetype
resolve to OpenAI". The corpus declares the adjudicating facts missing: "**Both sides call themselves the vow**"; "the archetype branch returns before
the qualification, canon and consent gates are reached"; "the env var each depends on is set for one side and absent for the other in the production
compose file"; "**UNKNOWN.**"
RESIDUE · OWED READ (§5): which path serves a production voice turn (runtime witness), and the standing of the in-route "MAIA vow" sentence as a
governing source. ⛔ Not performed. ⛔ `MAIA_VOICE_OVERRIDE`'s absence from the production compose is NONE LOCATED, not ABSENT.

**ITEM `C3 (H) · non-degradation gate reads green`**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · ALTITUDE 1 — CI instrument: "the gate asserts convergence — one cognition call, reached once". ALTITUDE 2 — runtime egress: "convergence says
nothing about EGRESS. The 2026-09-07 defect … left this gate GREEN throughout." Both hold — INF-2, CI-GATED ↛ RUNTIME GOVERNED.
RESIDUE · Carried whole: "The repair exists and the gap is structurally closed — but by a **SECOND, SEPARATE gate**, and the non-degradation gate is
still green independently of it." ⛔ Whether any instrument now asserts egress as a gate condition is not restated in the permitted evidence.

**ITEM `C4 (H) · sovereignty of the ear`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · The corpus names the scoping itself: "**Both sentences are true of different transports**; neither is qualified in the other's presence." Side
A `transcribe-simple/route.ts:11-14` and `transcribe/route.ts:27` — "inbound member audio never leaves the host", "never OpenAI cloud"; Side B — "the
DEFAULT transport for Chrome and Safari members is `web-speech`, where recognition is performed by the browser vendor off-device; Desktop alone is
refused Web Speech, by shell classification".
RESIDUE · ⭐ The qualification that makes both true exists ONLY in this census — the source sentences are unqualified where they sit, on the routes,
while the default member transport is the other one. ⛔ No repair. Related to C2 (H), which is adjudicated separately and differently.

**ITEM `C5 (H) · Sanctuary and the voice path`**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · ALTITUDE 1 — declared invariant: `CLAUDE.md` Sanctuary invariant 1 ("No content retention") and invariant 6 ("Absolute boundary"). ALTITUDE 2
— implementation: "four `saveConversationMemory` call sites, none gated on `isSanctuary`; `lib/services/memoryService.ts` contains no occurrence of
'sanctuary' in any case". Both hold (INF-4).
RESIDUE · ⭐⭐ The altitude is weaker than it looks and the weakness is carried, ⛔ not hidden: "the six Sanctuary invariants live in `CLAUDE.md`, which
D-P1-06 classes as **evidence, ⛔ not a governing source**" (B-23, E-27). So this is an unlocated-governance finding as much as a divergence: ⛔ NONE
LOCATED, not ABSENT. Carried: "⚠️ MODALITY-SYMMETRIC — the typed path is identical; routed out, not repaired." ⛔ No verdict of legitimacy, ⛔ no
exposure claim.

**ITEM `C6 (H) · citation coordinates`**  ·  **OUTCOME `C4 RECORD DEFECT  (C4-ORGREC)`**
BASIS · "Canon names the convergence at `:7268`; it is at `:7397`." · "The exit map and the test header both name the crisis script at `:6712`; it is
at `:6854`." · "The stale comment the canon flags at `:7266` is still present at `:7395`". Coordinates in documents and test headers are bookkeeping
about records, ⛔ not organism behaviour.
RESIDUE · ⚠️ Two characterizations are carried and ⛔ neither is chosen: slice 06 says "⛔ Recorded as staleness, ⛔ not divergence", while the register
classes it DIVERGENCE. ⭐ This `C4` is a defect in the ORGANISM's records, ⛔ NOT in the census's — it is not artifact-2 material. The stale comment
still reads "Browser STT → /api/between/chat → Browser TTS"; ⛔ no repair.

**ITEM `C-I-1 · "Deferred, held FALSE, no path" vs a live path`**  ·  **OUTCOME `C5 GENUINE CONTRADICTION`**
BASIS · Side A `20260626000001_member_field_note_threads.sql:8,40,96` "states the capability DEFERRED and the column '**held FALSE, no path**'"; Side
B `app/api/maia/vision-studio/field-note/route.ts:107-130` "binds it to a client-supplied value" and `app/studio/fields/[memberId]/page.tsx:69` "reads
it". "No path" is a descriptive claim about the column, against a located path (P-A).
RESIDUE · Whether the route is reached is ⛔ not established (INF-1) — and ⛔ it need not be: the `C5` attaches to "no path", which a located binding
refutes. The deferral's authority is ⛔ not established (INF-4).

**ITEM `C-I-2 · "the consented facilitator view" vs a role-only gate`**  ·  **OUTCOME `C5 GENUINE CONTRADICTION`**
BASIS · Side A `page.tsx:9-11` "asserts consent"; Side B the same file at `:104-107` checks "only that the viewer is *some* active practitioner", and
`:79-85` applies "**no predicate at all** to `members.name` / `members.username`". One file's header sentence against its own located gate (P-A).
RESIDUE · No consent predicate is located anywhere on this page — NONE LOCATED, ⛔ not ABSENT. P1-GOV-ACCESS-01 stands adjacent. ⛔ No verdict of
legitimacy; ⛔ no exposure claim — that is not this census's territory and `AUTH-EXPOSURE-01` is neither cited nor awaited.

**ITEM `C-I-3 · two written access models vs the wired one`**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · ALTITUDE 1 — written scope model: "`lib/relationship/scope.ts` and `lib/coachField/*` encode member/practitioner read scopes, crossing rules
and verb limits". ALTITUDE 2 — the wired read: "`app/studio/fields/[memberId]/page.tsx` performs a practitioner read of member material and **imports
neither**". Both hold (INF-4).
RESIDUE · Twinned with C-5 (§8.5) across two registers (E-28); ⛔ the two items are not merged. P1-GOV-ACCESS-01 is the standing finding. ⛔ No verdict
of legitimacy.

**ITEM `C-I-4 · `/caseload` declared with a role`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · Two named paths the record distinguishes: `config/accessMatrix.ts:470` declares "`{prefix:'/caseload', minTier:'pro',
rolesAnyOf:['practitioner']}`"; "the API family lives at `/api/caseload/*`, **which that prefix does not match** and no other rule matches; the
unmapped default is permissive". The declaration is true of the prefix it names.
RESIDUE · ⭐ Whether anything gates the API family at request time is UNKNOWN — "⚠️ Production `ACCESS_CONTROL_MODE` is UNKNOWN" (UC-17 I-03, E-45) —
owed read, §5, outside the permitted evidence. ⛔ The scope classification establishes no enforcement fact in either direction.

**ITEM `C-I-5 · `/api/supervision` free-tier vs clinical content`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · Side A `config/accessMatrix.ts:578` — "free-tier, no role"; Side B "the routes' '**HIPAA compliant**' headers and their absent handler
authorization". ⛔ A compliance claim is not adjudicable from the permitted evidence, and evaluating it would be a verdict this census may not render;
the matrix entry's runtime force is itself undetermined (production `ACCESS_CONTROL_MODE` UNKNOWN). Carried: "⛔ Not adjudicated."
RESIDUE · OWED READ (§5): the production access-control mode, and a located authorization in the "seven of eleven supervision routes" slice 06 left
unread (UC-17 I-02). ⛔ Not performed. ⛔ "absent handler authorization" is read as NONE LOCATED, not ABSENT.

**ITEM `C-I-6 · facilitator and practitioner`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · Two named objects, distinguished by the record: "`member_memory_atoms.facilitator_id` attributing `source_type='practitioner_observation'`"
against "`CircleRole`, where `facilitator` is a distinct enforced role **unrelated to `practitioners`**".
RESIDUE · ⛔ Per the vocabulary rule no finding attaches to the shared token. Whether any `practitioner_observation` row exists is UNKNOWN — "no writer
located" (UC-17 I-10) — owed read, §5.

**ITEM `C-I-7 · `practitioner_id` refers to two tables`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · "references `practitioners(id)` in some tables" / "and `members(id)` in others — Same column name, different referent", carried with the
schema's own sentence: "**the declared shape in this repository is therefore NOT authoritative**" (`20260802000002…:45-52`, with three competing table
definitions at `:26-39`). ⛔ With the repository's declared shape disclaimed by the repository itself, the referent cannot be determined from the
permitted evidence.
RESIDUE · OWED READ (§5): the authoritative deployed schema for the tables carrying `practitioner_id` — a database read, ⛔ outside the permitted
evidence and ⛔ not performed. ⛔ Per the vocabulary rule no finding attaches to the token. E-29 (SYN-4) carries this uncertainty into C-1 (§8.1)'s Side
B.

**ITEM `C-I-8 · the matrix's own DECLARED-vs-ENFORCED divergences`**  ·  **OUTCOME `C2 RESOLVED BY ALTITUDE`**
BASIS · Both altitudes are declared inside one artifact: `config/accessMatrix.ts:288,299,392-404,531` — "four cases the file records itself, one
labelled '**Unreconciled**' in its own note" — DECLARED access policy against ENFORCED behaviour.
RESIDUE · Whether the declarations bind at request time depends on the production `ACCESS_CONTROL_MODE`, UNKNOWN (UC-17 I-03). ⭐ A file recording its
own divergences is evidence of the divergence, ⛔ not of its resolution.

---

## 4 · PARTS IV · V · VI · VIII · IX

### 4a · PART IV — session-anchor versus canon (1 item adjudicated here)

**ITEM `C2 (H) · STT/TTS LATITUDE                     (anchor vs canon)`**  ·  **OUTCOME `C5 GENUINE CONTRADICTION`**
BASIS · Side A `CLAUDE.md` — "STT/TTS are sensory infrastructure and **may change freely**; the mind may not be substituted." Side B
`docs/canon/MAIA_CONVERSATIONAL_INTELLIGENCE_NON_DEGRADATION.md:87` — "**the ear may be improved freely**; the mind may not be substituted" —
"latitude to the ear only". Two normative grants over the same act (changing the mouth), same subject; the anchor's grant extends to what the canon
clause withholds, and ⛔ both cannot govern.
RESIDUE · ⭐ D-P1-06 constrains the reading and does ⛔ not dissolve the class: "an operational session anchor cannot amend a governing source by
assertion"; the divergence "remain[s] a recorded defect, not census law"; ⛔ "Repair remains unauthorized." ⭐ A third fact is carried and matches
NEITHER side: "The code gates the mouth and does not gate the ear — the exact inverse of the latitude CLAUDE.md grants." ⛔ Not adjudicated here; C4
(H) carries the ear's transport scope.

**ITEM `C-2 (G) · PROVIDER PROHIBITION                (anchor vs tree/policy — PART IV repeat)`**  ·  **OUTCOME `C6 UNKNOWN  → adjudicated once, at §3 above. ⛔ Not restated differently, ⛔ not double-counted.`**

### 4b · PART V — provider-failure plurality (1 item)

**ITEM `PROVIDER-FAILURE PLURALITY`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · Three named seams with three named coverage families, stated by the register: DISPOSITION 1 LOCAL FALLBACK "`modelService.ts:180-193` →
`localModelClient.ts` … COVERAGE: CONVERSATIONAL TEXT FAMILY only"; DISPOSITION 2 DEGRADED FIRST-PERSON OUTPUT "`sovereignRouter.ts:15-17,50-61`; four
exits … COVERAGE: CONVERSATIONAL TEXT FAMILY only"; DISPOSITION 3 HARD REFUSAL "`structured/router.ts:116-131` … Reached from the five Writer's-Studio
reader callers, always. COVERAGE: STRUCTURED-READING FAMILY only." The register's own law: "provider failure semantics are PLURAL in the implemented
organism, and ⛔ no synthesis may simplify this to 'MAIA falls back' or 'MAIA refuses.'"
RESIDUE · ⭐ Three limbs survive the classification, ⛔ none of them resolved by it. (i) "Which behaviour governs production is UNKNOWN from repository
evidence alone" — `MAIA_INFERENCE_MODE` "appears in no tracked deployment config"; owed read, §5. (ii) Under `consciousness_engine` the branch
"returns `model: 'template-engine'` — **a fourth answer-producing path that is not a model at all**". (iii) "The billing/auth no-fallback guard is, as
read, **unreachable** from the Anthropic client" — it checks "properties no code sets" and "`claudeClient.ts:199-205` re-wraps every error so it
cannot carry them". ⛔ INF-3 throughout: the env var selecting the disposition authorizes nothing.

### 4c · PART VI — vocabulary collisions (11 items)

⛔ Standing on every item in this sub-section: "No collision is resolved. ⛔ No two objects are merged because
they share a word." ⭐ `C1` here classifies the KIND — one token, several objects the records distinguish by
path, table or type — and ⛔ settles no sentence that uses the bare word.

**ITEM `VC-field`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · Register 02 (D §5.1) enumerates "'field' — seven unrelated semantic families", "at least **19** distinct code/table objects", each with its
own path/table and row id (`FieldContext` · `FieldRoutingDecision` · `ConsciousnessField` · `field_ideas` · `living_field_affinities` · "Field Lab" ·
"MAIA Field" · `practice_field*` · `wisdom_field*` · `field_records` · `field_state_snapshots` …), with the sharpest instance distinguished by casing
alone: "`lib/field-protocol/` (P3-D-27) and `lib/fieldProtocol/` (P3-D-26) are two different directories with no shared code". Register 01 enumerates
"`field` — 9 referents" over the memory corpus. ⭐ The two enumerations are at different scopes and register 02's is self-declared non-exhaustive ("at
least 19"), so their non-identity is ⛔ not an enumeration conflict.
RESIDUE · ⭐⭐ ONE PAIR INSIDE IS `C6` AND IS ⛔ NOT DISSOLVED: D-OBJ-1's `FieldContext` (P3-D-01, ungated) and D-OBJ-9's `fieldContextAdapter` (P3-D-09,
gated by `MAIA_FIELD_CONTEXT_ENABLED`) are "both called 'field context'", and the record poses and refuses the question — Q-D2: "Is this one
capability with two implementations, or two capabilities that share a name? **⛔ A worker may not choose.**" Owed read, §5. Carried: "'Field memory is
live' and 'field memory is not live' are both unevaluable until the referent is named."

**ITEM `VC-coherence`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · "six distinct quantities, none convertible into another", each named with type and site: `FieldContext.pfi.coherence` (number) ·
`FieldContext.unified.coherenceLevel` (string) · `coherenceScore` (0–1, persisted) · `fci` · somatic/breath coherence · `rhythmCoherence`.
RESIDUE · Carried: "A sentence of the form 'MAIA's coherence is X' is unevaluable at the subject." ⚠️ A seventh adjacent reading stands unclassified:
"`setCoherenceLevel(… || 0.85)` at `OracleConversation.tsx:6147`, which D found 'resolves to the 0.85 literal on every turn.'" ⛔ Not adjudicated.

**ITEM `VC-voices`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · Objects distinguished by path and lane: "three service-level voices plus up to six elemental rows in which `shadow` is an element value";
"'Corpus Callosum' — two lanes writing differently-named agent rows: `corpusCallosumService` (P3-E-12) and `maiaOrchestrator` (P3-E-13)";
"`MaiaSystemPrompt` — 2 files of the same basename in different trees: `lib/oracle/MaiaSystemPrompt.ts` and `lib/voice/MaiaSystemPrompt.ts`."
RESIDUE · ⛔ "no claim is made that either is 'the' system prompt". The anchor-versus-code count limb is a different item and is `C5` (C-E4). ⚠️ A
collision labelled on the bare token `voice` "is not identified in the authorized inputs" — UC-MODE · UC-CONVERGENCE, §6.

**ITEM `VC-RFI/UFI`**  ·  **OUTCOME `C4 RECORD DEFECT  (C4-ORGREC)`**
BASIS · "`RFI` → 0 code objects; `UFI` → one comment that explicitly excludes it", and the record states the dissolution itself: "'RFI is built' and
'RFI is not built' were both unevaluable. **Once the referent is demanded, the question dissolves — there is no referent in code.**" The disagreement
is between sentences in records about a token with no located object, ⛔ not between organism behaviours.
RESIDUE · ⭐ "0 code objects" is a search that concluded — **NONE LOCATED**, ⛔ not ABSENT, and ⛔ not a claim that no such capability could exist
elsewhere in the organism.

**ITEM `VC-phase`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · "'phase' — three incompatible vocabularies and a fourth trap": `Phase = 1|2|3` (`spiralogic-core.ts:17`) · `phase INTEGER 1..12`
(`member_spiral_state.sql:19`) · `Intelligence | Intention | Goal` (`maiaService.ts:1134-1137`) · "⭐ and `SpiralogicPhase` in
`lib/spiralogic/PhaseDetector.ts:8`, which is an **ELEMENT NAME, not a phase at all**". Each is a distinct named object.
RESIDUE · Carried: "⛔ 'P1-04 cannot evaluate "MAIA assigns a phase" until the referent is named.'" C-E3 depends on this item and is `C6` for exactly
that reason (§6).

**ITEM `VC-resonance`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · "five objects, including one exact class-name duplication": `ResonanceFieldOrchestrator` at `lib/field/…:97` (783 ln) and
`ResonanceFieldOrchestrator` at `lib/oracle/…:40` (629 ln) — "identical class name, different files, different bodies, neither reachable" ·
`ResonanceFieldGenerator` ("the only one the canonical path actually uses") · `ResonanceEngine` · `resonance_events` (a table).
RESIDUE · The duplicated class name is distinguished ONLY by path; ⛔ no claim is made about which a citation elsewhere intends, and ⛔ neither is
nominated canonical.

**ITEM `VC-element`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · "two live detectors and three persistence substrates": `ConversationElementalTracker` (in-memory) · `ElementalOracleBridge` (per-message) ·
`member_spiral_state` · `bead_events.spiralogic_element` · Prisma `ElementalState`/`ElementalEvolution` — "three substrates for one concept."
RESIDUE · ⭐ A live divergence is carried and ⛔ not adjudicated: the two detectors "can disagree on the same turn and both reach the same prompt by
different routes, **with nothing reconciling them**". Whether they do disagree in production is ⛔ not established — no runtime witness.

**ITEM `VC-IChing`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · "two independent corpora": `lib/iching/` (1,925 ln) and `lib/divination/iching/` (2,687 ln) — "different hexagram data files, different
casting modules, no shared import".
RESIDUE · "⛔ no artifact declaring which is canonical" — NONE LOCATED, ⛔ not ABSENT. "F-07/F-08 use the former; F-10 and the recall lineage the
latter", so the choice is already made differently in two lineages.

**ITEM `VC-names-asserting-what-they-do-not-carry`**  ·  **OUTCOME `C4 RECORD DEFECT  (C4-ORGREC)`**
BASIS · The item is explicitly a name/object register — "recorded as collisions between a name and its object, ⛔ not as repairs" — over
`symbolicAuthorityContracts` · `crossDomainGovernance` · `promptIngressGovernance` (P3-F-20) · `QuantumFieldPersistence` (P3-D-17) ·
`persistentFieldStates` (CONTRA-5) · `archetypeEvolutionEngine` (P3-F-19) · `member_spiral_state` (C-E6). ⛔ Per the vocabulary rule no finding
attaches to a token, so an identifier cannot stand as one side of a contradiction.
RESIDUE · ⭐ Where a SENTENCE rather than a name makes the claim, the contradiction is real and is adjudicated at its own item: CONTRA-5 = `C5`; C-E6 =
`C6`. ⛔ This classification does not weaken either.

**ITEM `VC-memory-corpus  (five further tokens + the appended items)`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · Each token's referents are enumerated with distinct paths/tables: `semantic` — 5 referents ("two files, same basename, different trees") ·
`developmental` — 5 · `trust` — 5 · `pattern` — 6 ("⚠️ Plus `MemoryHealth.pattern` … the two are not the same object") · `relationship` — "3 sibling
directories, 3 different architectures".
RESIDUE · Three appended limbs are ⛔ NOT vocabulary matters and are carried unresolved: "`pattern_ledger` declared by two migrations (20260204100001,
20260315120000), ⛔ not reconciled" (a duplicate-declaration limb); "`memory layers` — six code enumerations, none a subset of another. ⛔ No winner
chosen" (= B-8); "`recurring_interests` — a column, not a table, with zero code references."

**ITEM `VC-updateOffering`**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · "`updateOffering` also exists in `lib/offerings/offeringService.ts:79` — **a different function**; vocabulary collision recorded."
RESIDUE · NONE. ⛔ No finding attaches to the token.

### 4d · PART VIII — unresolved coverage · unknown by scope (10 items)

**ITEM `UC-17 · the 17 named UNKNOWN sub-questions withheld by slice 06`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · Slice 06 withheld a position on each, by name — "G-02 persistence · G-03 `CONSIDERS` · G-05 `DECIDES` · G-08 `DECIDES` · G-09 whether
`minimumBloomLevel` was ever evaluated · G-11 `DECIDES` and authority · G-15 identity across provider change · H-11 `CONTRIBUTES`/`DECIDES` · H-15
reachability of the remaining `lib/voice/` surface (119 entries) · I-02 seven of eleven supervision routes · I-03 production `ACCESS_CONTROL_MODE` ·
I-04 whether any flow requires `verify-passcode` first · I-05 the referent of `requireMemberId()` at each call site · I-06 most of ≈76 Studio routes
classified rather than read · I-10 whether any `practitioner_observation` row exists (no writer located) · I-11 the `consent_status` schema default ·
I-12 whether any deployment runs `app/api/_backend/**`."
RESIDUE · OWED READS (§5): all seventeen, ⛔ each routed and ⛔ none performed. ⛔ A withheld position is UNKNOWN; it may not be read as `NONE LOCATED`,
and `NONE LOCATED` may not be read as ABSENT.

**ITEM `UC-GHI-ENV · values carried as explicitly UNKNOWN by the G/H/I records`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · "production value of `MAIA_INFERENCE_MODE` · production value of `ACCESS_CONTROL_MODE` · values of `FOUNDER_MEMBER_IDS` /
`LAB_ACCESS_MEMBER_IDS` / `CIRCLE_ACCESS_MEMBER_IDS` · the `relationship_spaces` DDL and its `consent_status` default · whether `minimumBloomLevel`
was ever evaluated · whether any deployment runs `app/api/_backend/**` …"
RESIDUE · ⭐ These lie OUTSIDE the permitted evidence entirely — they are deployment/runtime reads, not record reads. OWED READS (§5), ⛔ not performed.
Items depending on them: C-1 (G) · C-I-4 · C-I-5 · C1 (H) · PROVIDER-FAILURE PLURALITY (E-45 · E-46 · E-47).

**ITEM `UC-ABC-Q · whole-question gaps the A/B/C records declare rather than answer`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · "live tier distribution (A OQ-2) · whether the 2026-05-23 traffic audit still describes the subject (A-01) · how many of the 25 peripheral
routes are member-reachable (A OQ-4) · whether `scrubMemoryAmnesia`'s result replaces member-facing text (B OQ-4) · whether `/api/oracle/conversation`
serves member traffic (B OQ-2) · the referent of 'the SQL confirmation term caps at 0.0225' (C §10, NOT FOUND at the subject)."
RESIDUE · OWED READS (§5), six, ⛔ none performed. ⭐ Two of them are the load-bearing dependencies elsewhere: the traffic question (A-4 · X-3 · D-1 ·
C-F5 · X-2) and the 0.0225 referent (CONTRA-6 · B-4, E-31).

**ITEM `UC-CROSSCUT · cross-cutting `NONE FOUND` findings carried forward unattached`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · "member deletion or erasure of formed memory (B §10.2) · the four freeze conditions have no evaluator of any kind (B §9) · practitioner
visibility into member memory (B §10.2) · `MAIA_SAFE_MODE` — no ruling on who may set it or what it may remove (A §4) · what a practitioner may see of
a member's developmental material (C §9) · erasure of `member_spiral_state`, `member_relational_signals`, `member_theme_signals`, `trust_observations`
(C §9)." ⛔ Each is a one-sided finding: ⛔ no counter-claim is quoted anywhere in the corpus, so no contradiction is established and none is refuted.
RESIDUE · ⭐ Each is a **located absence of governance** — NONE LOCATED, ⛔ not ABSENT — and D-P1-07 governs the freeze limb exactly: "the freeze
remains binding; evaluator absence does not weaken it. ⭐ The absence means the **opposite** of 'probably stale.'" OWED READS (§5): a governing-source
search for each. ⛔ Not performed. P1-GOV-ACCESS-01 carries two of the six.

**ITEM `UC-PARTICIPATES · the `PARTICIPATES` vocabulary-evidence gap (A · B · C)`**  ·  **OUTCOME `C4 RECORD DEFECT  (C4-CENSUS)`**
BASIS · The corpus states it as a fact about its own records: "PARTICIPATES 0 — ⭐ NO RECORD IN THIS SLICE USES OR ESTABLISHES THIS RUNG" (04_ladder),
and "PARTICIPATES is EMPTY across A, B and C — **a corpus gap at the vocabulary level, ⛔ not a finding that nothing participates**" (P1-03 CLOSE §3a).
RESIDUE · ⛔ FORBIDDEN PROMOTION REFUSED: "no PARTICIPATES assignment ≠ DOES NOT PARTICIPATE" (P1-04 INSTRUMENT §3). ⛔ The zero may not be counted
alongside any observed negative.

**ITEM `UC-CONSIDERS · `CONSIDERS` is zero across G · H · I`**  ·  **OUTCOME `C4 RECORD DEFECT  (C4-CENSUS)`**
BASIS · The zero has two provenances and the corpus supplies both: coverage — "No record in this slice establishes that a fact a capability holds is
read into a computation"; and one observed negative — "At the subject, `selectClaudeModel()` never reads an awareness level" (P3-G-03) — "KNOWS
without CONSIDERS, which is precisely the promotion INF-6 forbids". AMENDMENT-4 §4b binds the handling: "47 rows the question was never posed (SYN-4)
· 1 row an observed negative (SYN-1)."
RESIDUE · ⭐⭐ ⛔ THE TWO ZEROS ARE NOT AGGREGATED — "Numerically equal states are not equivalent when their epistemic provenance differs." ⚠️ Carried
from the same slice: "`EXISTS` on 48 of 48 is the weakest possible recovery and must not be read as coverage." ⛔ `CONSIDERS = 0` is not a shortfall
claim about the organism.

**ITEM `UC-EXISTS-BASIS · the slice-06 `EXISTS` qualification`**  ·  **OUTCOME `C4 RECORD DEFECT  (C4-CENSUS)`**
BASIS · P1-03 CLOSE §1a: "`EXISTS` is recovered on 48 of 48 rows. Its basis is **a slice-level shared quote** — the three records' own §0
subject-verification statements — ⛔ not a per-row quote. Every other rung on every row carries its own separate quote." · "The worker flagged this
itself and reported it separately 'so it is not read as coverage.'"
RESIDUE · ⭐ Artifact-2 material of the strongest kind: the defect was found and declared by the pass that produced it. ⛔ The 48/48 may not be cited as
per-row evidence.

**ITEM `UC-NOTDET · the `NOT DETERMINED BY SOURCE RECORD` inventories`**  ·  **OUTCOME `C4 RECORD DEFECT  (C4-CENSUS)`**
BASIS · Register 01: "LADDER — 60 of 62 rows"; "GOVERNANCE GATE — 14 rows where the record names neither a gate nor an absence … ⛔ distinct from NONE
FOUND, which is a positive finding of absence. ⭐ Every one of them is in domain B"; "COVERAGE — 17 rows … 10 of them wholly". Register 02: "111 rows ×
11 fields. `NOT DETERMINED BY SOURCE RECORD` appears **197 times**"; "⭐ Domain E assigns no ladder position anywhere". Register 03: "LADDER 41 of 48 …
⛔ The three P1-02 records never use the ladder vocabulary."
RESIDUE · ⭐ `NOT DETERMINED BY SOURCE RECORD` ≠ `NONE FOUND` ≠ `UNKNOWN` ≠ `ABSENT`; ⛔ the four may not be merged in any count or legend (AMENDMENT-4
§2). ⚠️ Domain E's non-use of the ladder is a vocabulary-scale finding, ⛔ not a defect: "it uses a vocabulary vs executable axis and asks in Q1
whether EXISTS ≠ PARTICIPATES even holds at its scale."

**ITEM `UC-MODE · a labelled collision on `mode` is not identified`**  ·  **OUTCOME `C6 UNKNOWN (by scope)`**
BASIS · "Within those inventories no collision block is labelled on the bare tokens `mode`, `convergence`, or `voice`. ⛔ Nothing is guessed at and
nothing is mapped onto a row. Following the precedent slice 06 set for X-DEF-2: **UNKNOWN, by scope.**"
RESIDUE · OWED READ (§5): whether any record NOT authorized for this pass carries such an inventory. ⛔ Not performed. ⛔ Absence from the authorized
inputs is not absence from the organism.

**ITEM `UC-CONVERGENCE · a labelled collision on `convergence` is not identified`**  ·  **OUTCOME `C6 UNKNOWN (by scope)`**
BASIS · Same block, and the corpus names what it DOES carry instead: "for `convergence` — as C3 (H) and C6 (H) in PART III-C, which concern the
convergence gate and its citation coordinates, **⛔ not a set of competing referents for the word**."
RESIDUE · OWED READ (§5): as UC-MODE. ⛔ C3 (H) and C6 (H) are adjudicated on their own terms above and are ⛔ not treated as answering this item.

### 4e · PART IX — cross-record divergences (5 items)

**ITEM `X-1 · `member_theme_signals``**  ·  **OUTCOME `C1 RESOLVED BY SCOPE`**
BASIS · Two different paths, each quoted: SIDE B (domain B, P3-B-07) — "theme signals are loaded at route :947 and surfaced into the FAST prompt
inside the influence block — PARTIAL, SURFACED (FAST only)"; SIDE C (domain C, P3-C-09) — "WIRED-BUT-UNOBSERVED (write path only; **no member-facing
read located**)", "and explicitly SUSPENDED from the collective surface". A prompt-assembly path and a member-facing read surface are different
predicates over one table.
RESIDUE · ⭐ Whether domain C's search covered domain B's prompt path is ⛔ NOT established; if it did, the item reopens as `C5`. Slice 04 D-2 expressly
declines resolution: "⛔ Both stand. P3-C-09 is UNKNOWN. ⚠️ P3-B-07 keeps its own record's affirmative trace (KNOWS · CONSIDERS) … ⛔ not a resolution
of the divergence." ⛔ The rows are not merged and P3-C-09 stays UNKNOWN.

**ITEM `X-2 · Episodic / Coherence liveness`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · Side A `CLAUDE.md` Cat 3 — "0 **live** callers"; Side B Domain B §10.3 — "contradicted at the subject — both are called by
`MemoryPalaceOrchestrator` (:61, :75, :188)". ⛔ Two readings of Side A are available and the corpus settles neither: read as "0 callers" it is refuted
by three call sites; read as "0 LIVE callers" it is not, because INF-1 holds that WIRED ↛ LIVE and the only located onward route's liveness is
UNKNOWN. Choosing between the readings is a likelihood judgement.
RESIDUE · OWED READ (§5): a dated in-repo runtime witness for the orchestrator's caller route. ⛔ Not performed. Depends on X-DEF-2 (owned) and on X-3
— §6.

**ITEM `X-3 · `/api/oracle/conversation` traffic`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · "Domain A: hard-refused at POST (410)" against "Domain B and C both carry live-path material whose only located consumer is that route
(P3-B-24, P3-C-03 loader 1)", carried with "Domain B records the question as OQ-2; ⛔ unanswered."
RESIDUE · OWED READ (§5): whether the route serves member traffic — the single most depended-upon unknown in this corpus (A-4 · C-4 §8.4 · C-F5 · X-2
· D-1 · B-1 · C-E6 · CONTRA-4). ⛔ Not performed. Depends on X-DEF-2 (owned) — §6.

**ITEM `D-1 · `P3-B-24` MemoryPalaceOrchestrator`**  ·  **OUTCOME `C6 UNKNOWN`**
BASIS · The corpus states the disjunction and refuses to close it: "⛔ If the POST is refused at its first statement, neither the retrievals nor the
prompt seam runs; if the seam runs, the route is reached. **Both stand. No position derived — not even KNOWS.** (Register X-3; B OQ-2.)" Side B quotes
":902 `retrieveMemoryContext` → :2787 … interpolated into the prompt template; :1499 `storeConversationMemory`"; Side A quotes "The 410 is the first
executable statement of `POST`" plus refusal-19's assertion "that the body is never read".
RESIDUE · ⚠️ refusal-19 is "a CI instrument, not a governing document" (INF-2) — it evidences a gate, ⛔ not runtime governance. OWED READ (§5): as
X-3. Depends on X-DEF-2 (owned) — §6.

**ITEM `D-2 · `P3-C-09` `member_theme_signals``**  ·  **OUTCOME `C1 RESOLVED BY SCOPE  — the same object as X-1; adjudicated there, ⛔ not separately.`**
BASIS · The corpus records it as a pointer — "→ the same object as X-1; block above" — and slice 04's D-2 closes "(Register X-1.)" (E-40).
RESIDUE · As X-1. ⛔ Not double-counted in the tally below; it is carried as its own source-labelled item because the source wrote it as one.

---

## 5 · Outcome tally by class — ⛔ NAVIGATION ONLY

> ⛔ **The tally stands in for nothing. The items above are the finding.** ⛔ No class is a severity, a
> priority or a repair order. ⛔ Two items in one class are not thereby the same kind of fact.

```text
ITEMS ADJUDICATED   87        (92 corpus blocks − 6 owned by 01_named_objects + 1 PART IV repeat counted once)

C1  RESOLVED BY SCOPE          27
C2  RESOLVED BY ALTITUDE       17
C3  RESOLVED BY TIME/SUBJECT    0     ⭐ see the note below — this zero is a finding about the EVIDENCE
C4  RECORD / ARITHMETIC DEFECT  8     → C4-CENSUS 5 · C4-ORGREC 3   ⛔ not pooled
C5  GENUINE CONTRADICTION      15
C6  UNKNOWN                    20
```

⭐⭐ **ON `C3 = 0`.** ⛔ This is NOT a finding that no item has a temporal explanation. It is a finding about
the permitted evidence: **no item in this corpus carries dated evidence of two states of one subject.** The
candidates that would need it — the anchor's "~zero live traffic" (A-4), the "8 voices" enumeration (C-E4),
the "NEW PHILOSOPHY (Jan 2026)" assertion against ADR-001 (C-4 (G)) — are each blocked by a declared gap:
register 01 itself lists "whether the 2026-05-23 traffic audit still describes the subject (A-01)" as
unanswered, and "No superseding ADR or ruling was located." ⛔ A temporal resolution was available in no
case, and ⛔ none was manufactured.

⚠️ `C4-CENSUS` (C-F3 · UC-PARTICIPATES · UC-CONSIDERS · UC-EXISTS-BASIS · UC-NOTDET) and `C4-ORGREC`
(C6 (H) · VC-RFI/UFI · VC-names-asserting-what-they-do-not-carry) are different epistemic objects: the first
is the census catching itself, the second is the organism's own records. ⛔ They are never summed as one.

---

## 6 · ⭐⭐ C5 — GENUINE CONTRADICTIONS (15) · every one named, both sides quoted

> **Same object · same scope · same altitude · same subject — and both cannot hold.** ⛔ This section is
> not compressible to a count. ⛔ Nothing here is a verdict of legitimacy, a severity, or a repair order.
> ⭐ In every case below the two sides are BOTH DESCRIPTIVE — a claim about what the code does, against the
> code — which is why no altitude separates them.

```text
C5-01  A-1 · Registry vs orchestrator                                        ROWS P3-A-11
  A  "between/chat `callsMaiaResponse: false`, reason 'uses maiaOrchestrator, not getMaiaResponse'"  — maiaRuntimeContext.ts:81
  B  "maiaOrchestrator.ts:506 — it does"                                                             — 01_normalized_A_B_C

C5-02  A-2 · DEEP addenda, in one function                                   ROWS P3-A-05
  A  "buildComprehensiveVoicePrompt … currently does NOT iterate MaiaContext addenda"                — maiaService.ts:2519-2521
  B  "maiaService.ts:2526-2535 + maiaVoice.ts:972 — it does. Four lines apart."                      — 01_normalized_A_B_C

C5-03  A-6 · Sole-gateway claim                                              ROWS P3-A-16
  A  "Main gateway for ALL text generation in MAIA"                                                  — modelService.ts:72
  B  "at least four independent model reaches bypass it — getClaudeService (A-14), six `new Anthropic()`
      routes (A-15), A-13's MultiLLMProvider, DEEP-primary's consciousnessWrapper"                   — 01_normalized_A_B_C
  ⭐ the corpus's own row sentence: "THE GATEWAY CLAIM IS FALSE AT THE SUBJECT"                       — 04_ladder

C5-04  A-7 · Egress mislabel                                                 ROWS P3-A-01
  A  "list/route.ts:1390 emits `endpoint: '/api/sovereign/app/maia'`"                                 — 01_normalized_A_B_C
  B  "it is emitted 'from the /list handler'; acknowledged in-source as a pre-existing mislabel, left untouched"

C5-05  B-2 · memoryHealth.pattern                                            ROWS P3-B-20 · P3-B-07
  A  "`pattern?: { count } // theme_signals feeds this`, under 'Wired by Cut 1'"                      — memoryHealth.ts:103
  B  "route :1178-1209 supplies no `pattern` input, so `layerStatus(undefined) = 'empty'` on every turn"

C5-06  B-3 · memoryHealth.semantic                                           ROWS P3-B-20
  A  "memoryHealth.ts:63 declares `semantic`, documented :49 as 'enduring facts, roles, relationships, preferences'"
  B  "what feeds it is an atoms row count and … 'no semantic retrieval exists on this path'" — memoryHealth.ts:97-100 · route :1182-1186
  ⭐ third source, same side: "a self-report that misstates the memory state MAIA is in… occurring in the
     observability layer itself"                                     — MAIA_MEMORY_INTEGRITY_GAP_MAP_2026-08-04.md:38

C5-07  CONTRA-2 · "no live consumer" vs the import graph                     ROWS P3-D-10
  A  "Resonant field memory … consumers: [] … Service preserved; no live consumer wired"              — substrateMap.ts:383-391
  B  "CoherenceFieldService is imported at MemoryPalaceOrchestrator.ts:14, called at :75,:188,:249"
  ⚠️ the C5 attaches to "consumers: []" ONLY; ⛔ the word "live" is not adjudicated (INF-1)

C5-08  CONTRA-4 · "The ONLY file that knows how the field engines connect"   ROWS P3-D-01
  A  "fieldOrchestrator.ts:3 asserts sole-seam status"
  B  "at least four other seams connect field engines into cognition — maiaService.ts:1082, maiaService.ts:2838,
      oracle/conversation/route.ts:853, :902, voice/stream-conversation/route.ts:82"

C5-09  CONTRA-5 · `persistentFieldStates` is not persistent                  ROWS P3-D-12
  A  "maintaining persistent field states"                                                            — QuantumFieldMemory.ts:2-6
  B  ":91 is `new Map()`; the file's only import is `crypto`; no `query`/`INSERT`/`SELECT` in 810 lines"

C5-10  C-E4 · "8 voices" vs the code                                         ROWS P3-E-12
  A  "MythicAtlas + MaiaVoice + ShadowAgent + Fire/Water/Earth/Air/Aether"                             — CLAUDE.md
  B  "MythicAtlas + MaiaVoice + WisdomRouter + up to six elemental rows in which `shadow` is an ELEMENT
      VALUE, not a separate ShadowAgent"                                                              — 02_normalized_D_E_F
  ⛔ D-P1-06: the anchor may not amend by assertion; PRESERVED + REPORTED, ⛔ repair unauthorized

C5-11  C-E5 · "imported only by soulPortrait/schema" vs zero importers       ROWS P3-E-01
  A  "the cited import"
  B  "at the subject `lib/soulPortrait/schema.ts:19` is a comment, not an import"
  ⭐ carried: "The subject reading governs E's record; both preserved."

C5-12  C-3 (G) · "Main gateway for ALL text generation"                      ROWS P3-G-01 · P3-G-10
  A  "lib/ai/modelService.ts:71-73"
  B  "scripts/anthropic-import-allowlist.json (2 + 1 + 57)"
  ⭐ carried: "Both are the project's own text."   ⚠️ INF-2: the allowlist is a CI instrument, not governance

C5-13  C-I-1 · "Deferred, held FALSE, no path" vs a live path                ROWS P3-I-01
  A  "states the capability DEFERRED and the column 'held FALSE, no path'"  — 20260626000001_member_field_note_threads.sql:8,40,96
  B  "app/api/maia/vision-studio/field-note/route.ts:107-130 binds it to a client-supplied value" and
     "app/studio/fields/[memberId]/page.tsx:69 reads it"

C5-14  C-I-2 · "the consented facilitator view" vs a role-only gate          ROWS P3-I-01
  A  "page.tsx:9-11 asserts consent"
  B  ":104-107 checks only that the viewer is *some* active practitioner", and ":79-85 applies no predicate
      at all to `members.name` / `members.username`"
  ⛔ no verdict of legitimacy · ⛔ no exposure claim · ⛔ AUTH-EXPOSURE-01 neither cited nor awaited

C5-15  C2 (H) · STT/TTS LATITUDE          ⭐ the one anchor-versus-canon C5   ROWS P3-H-01 · P3-H-02 · P3-H-03 · P3-H-07
  A  "STT/TTS are sensory infrastructure and may change freely; the mind may not be substituted."      — CLAUDE.md
  B  "the ear may be improved freely; the mind may not be substituted" — "latitude to the ear only"
                                          — docs/canon/MAIA_CONVERSATIONAL_INTELLIGENCE_NON_DEGRADATION.md:87
  ⭐ third fact, matching NEITHER: "The code gates the mouth and does not gate the ear — the exact inverse
     of the latitude CLAUDE.md grants."
  ⛔ D-P1-06 binds the reading, ⛔ not the class: "an operational session anchor cannot amend a governing
     source by assertion"; the divergence is a recorded defect, ⛔ not census law; ⛔ repair unauthorized
```

⭐ **Two of these fifteen are one sentence of the project's own, contradicted in two different registers**
(A-6 and C-3 (G) both quote `modelService.ts:71-73`, against two different counter-enumerations, E-23).
⛔ They are not merged: each keeps its own Side B, its own rows, and its own record.

---

## 7 · C6 — UNKNOWN (20) · each with its OWED READ, named as an obligation and ⛔ NEVER PERFORMED

> ⭐ A preserved UNKNOWN is the product this step exists to protect. ⛔ None of these is a near-miss
> resolution, a "probably", or a candidate for tidying into `NONE LOCATED`.

```text
ID                     WHY THE EVIDENCE CANNOT ADJUDICATE            OWED READ (⛔ not performed)
C-4 (§8.4)             "No runtime witness for either route was       a dated in-repo runtime witness for
                        located in-repo … neither side is confirmed   oracle/conversation and for the list
                        under the LIVE calibration"                   route, under the LIVE calibration
CONTRA-3               Side A names `talkModeFieldIntelligence`,      the referent of `talkModeFieldIntelligence`
                        Side B `analyzeFieldIntelligence`; ⛔ no       at fieldMonitorTelemetry.ts:165
                        sentence states they are one object
C-E3                   VC-phase: "cannot evaluate 'MAIA assigns a     the phase vocabulary in force at
                        phase' until the referent is named"           maiaService.ts:1727/:1737/:2275 and :1098
C-E6                   Side B is X-DEF-2's contested characterization a dated runtime witness for the route
                                                                      (+ X-DEF-2's own adjudication)
C-F5                   "⛔ the anchor may not settle this by            a dated runtime witness for
                        assertion"                                    oracle/conversation
C-2 (G)                "Authority standing UNKNOWN" — one altitude    whether provider-policy.json and
                        is undetermined, so C2 is unavailable         docs/adr/012 are ratified governing sources
C-5 (G)                "Whether a caller persists the turn is         whether the callers of sovereignRouter
                        caller-dependent and is not resolved"         persist the turn on the four degraded exits
C-6 (G)                `meta` has "no validation and no provenance"   the provenance of `req.meta` at the
                                                                      live-chat entrypoints — who may set it
C1 (H)                 "Both sides call themselves the vow" ·         which path serves a production voice turn;
                        "UNKNOWN"                                     and the standing of the in-route "MAIA vow"
C-I-5                  a compliance claim is not adjudicable from     production ACCESS_CONTROL_MODE; a located
                        the permitted evidence, and ⛔ a verdict is     authorization in the seven of eleven
                        outside this census                           supervision routes left unread
C-I-7                  "the declared shape in this repository is      the authoritative deployed schema for the
                        therefore NOT authoritative"                  tables carrying `practitioner_id`
UC-17                  slice 06 withheld a position on each, by name  all seventeen named sub-questions
UC-GHI-ENV             the values lie outside record evidence         the production deployment values named
UC-ABC-Q               the A/B/C records declare rather than answer   all six named questions, incl. the traffic
                                                                      question and the "0.0225" referent
UC-CROSSCUT            one-sided findings; ⛔ no counter-claim quoted   a governing-source search for each of the
                        anywhere in the corpus                        six (⛔ D-P1-07 binds the freeze limb)
UC-MODE                "UNKNOWN, by scope"                            whether a record not authorized for this
UC-CONVERGENCE         "UNKNOWN, by scope"                            pass carries such a collision inventory
X-2                    "0 live callers" has two readings and INF-1    a dated runtime witness for the
                        blocks the stronger one                       orchestrator's caller route
X-3                    "Domain B records the question as OQ-2;        whether /api/oracle/conversation serves
                        ⛔ unanswered"                                 member traffic
D-1                    "Both stand. No position derived — not even    as X-3
                        KNOWS."
```

⭐⭐ **Eight of these twenty reduce to ONE unread fact** — whether `/api/oracle/conversation` serves member
traffic (C-4 (§8.4) · C-E6 · C-F5 · X-2 · X-3 · D-1, and it governs the residues of B-1 and CONTRA-4).
⛔ That is recorded as a structural property of the corpus, ⛔ not as a recommendation to go and read it.

⚠️ `NONE LOCATED` appears in many residues above and is ⛔ never rendered as `ABSENT`; `UNKNOWN` is ⛔ never
upgraded to `NONE LOCATED` to tidy this table (AMENDMENT-4 §2).

---

## 8 · Items depending on another item's outcome

### 8a · Depending on one of the six owned by `01_named_objects`

```text
DEPENDS ON X-DEF-2  (one route, three characterizations)          — owned by 01_named_objects
  C-E6   C6   Side B ("that route refuses at :446-453") is X-DEF-2's Side B
  C-F5   C6   this item IS X-DEF-2's SIDE C, carried there verbatim
  X-3    C6   the same route-traffic question, from the A/B/C side
  D-1    C6   the same route, at row P3-B-24 (E-05)
  X-2    C6   the orchestrator's only located onward consumer is that route
  C-4 (§8.4) C6   the R16 guard's sole call site is on that route (E-07)
  B-1    C2   residue only — "Whether that route serves traffic is UNKNOWN" (E-06)
  CONTRA-4 C5  residue only — two of the four counter-seams sit on that route; ⛔ the C5 does not rest on them
  A-4    C2   residue only — the traffic limb (E-03)

DEPENDS ON X-DEF-1  (reaching the prompt at CONTRIBUTES ↔ surfaced nowhere)
  CONTRA-3 C6  Side B's onward claim ("producing a prompt block on the canonical member path") is X-DEF-1's
               disputed side (E-10); ⛔ the C6 here rests on the SEPARATE referent question, not on X-DEF-1

DEPENDS ON THE CONCLUDE ENUMERATION
  C-F1   C2   shares rows P3-F-07 · P3-F-08 with the CONCLUDE inventory (E-16); ⛔ the altitude classification
              does not turn on the count
  C-F4   C2   the wrapper is named "the sole constraint" on CONCLUDE (E-18); the count itself is owned

DEPENDS ON DEV-54/51
  (none of the 87 items owned here turns on it; A-4 shares row P3-A-13 with it via E-09, ⛔ residue only)

DEPENDS ON X-DEF-3 · THE MAP 3 SLICE-05 TALLY
  (none — ⛔ no item owned here rests on either)
```

### 8b · Depending on another item in THIS register

```text
C-E3          → VC-phase           the referent question that makes C-E3 C6 is VC-phase's own refusal
CONTRA-6      → B-4 · C-2 (§8.2)   one function, three items, three different sides (E-30) — ⛔ not merged
B-4           → CONTRA-6           the "which runs in retrieval" sentence lives in CONTRA-6's Side A
C-2 (§8.2)    → B-4 · CONTRA-6     the semantic-difference sentence is this item's residue, ⛔ not its class
X-1           → D-2                D-2 is the same object; adjudicated once (E-40)
C-I-4 · C-I-5 · C-I-8 · C-1 (G) · PROVIDER-FAILURE PLURALITY · C1 (H)
              → UC-GHI-ENV         each carries a production value the corpus declares UNKNOWN (E-45 · E-46 · E-47)
C-I-6         → UC-17 (I-10)       whether any `practitioner_observation` row exists
A-6           ↔ C-3 (G)            one sentence, two registers, two counter-enumerations (E-23) — ⛔ not merged
CONTRA-5 · C-E6 → VC-names…        named inside that inventory (E-34 · E-35); ⛔ the token carries no finding
C-5 (§8.5)    ↔ C-I-3              two registers on the same dormant scope modules (E-28) — ⛔ not merged
C-1 (§8.1)    → C-I-7              E-29 (SYN-4) leaves undetermined whether the `practitioner_id` tokens co-refer
B-2           → X-1                the theme-signal path that does reach the FAST prompt is X-1's Side B
VC-voices     → C-E4 · UC-MODE/UC-CONVERGENCE   the count limb is C-E4; the bare token `voice` is by-scope UNKNOWN
VC-field      → CONTRA-2 · CONTRA-5 · X-DEF-1   rows named inside the enumeration (E-41)
```

---

## 9 · Close

```text
P1-05 · 02 · REMAINING CONTRADICTION CORPUS · COMPLETE
ITEMS ADJUDICATED     87 of 92 blocks (+ 1 PART IV repeat adjudicated once) · 6 owned by 01_named_objects
AUTHORITY EXERCISED   READ (MAP 4 · P1-05 INSTRUMENT · AMENDMENT-4 · P1-02/P1-03/P1-04 INSTRUMENTS) · CLASSIFY
⛔ NO SOURCE CODE RE-TRACED · ⛔ NO RUNTIME INSPECTED · ⛔ NO GOVERNING DOCUMENT SEARCHED
⛔ NO AUTH-EXPOSURE-01 RESULT USED, CITED OR AWAITED
⛔ NO SOURCE RECORD REPAIRED · ⛔ NO ROW VALUE ALTERED · ⛔ NO TOTAL RECONCILED
⛔ NO SEVENTH OUTCOME · ⛔ NO "PROBABLY" USED AS A RESOLUTION
⛔ NO VERDICT OF LEGITIMACY · ⛔ `ABSENT` NOT WRITTEN · ⛔ NONE LOCATED NOT PROMOTED · ⛔ UNKNOWN NOT UPGRADED
⛔ NO ITEM RANKED · ⛔ NO LANE OPENED · ⛔ NO REPAIR PROPOSED
⛔ NO FILE EDITED EXCEPT THIS ONE
⭐ 15 GENUINE CONTRADICTIONS NAMED WITH BOTH SIDES · 20 UNKNOWNS PRESERVED WITH THEIR OWED READS ROUTED
```
