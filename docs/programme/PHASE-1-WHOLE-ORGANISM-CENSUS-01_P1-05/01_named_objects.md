# PHASE-1-WHOLE-ORGANISM-CENSUS-01 · P1-05 · 01 — THE FIVE NAMED CONTRADICTION OBJECTS

```text
STEP     P1-05 · CONTRADICTION / GAP PASS — the five named objects
TYPE     RECORD ONLY — ⛔ no repair · no design · ⛔ no organism re-tracing
BOUND BY P1-05 INSTRUMENT (C1…C6, §4a, §4b) · P1-03 AMENDMENT 3 (orthogonal axes,
         EFFECT-WITHOUT-LOCATED-AUTHORIZATION) · P1-04 AMENDMENT 4 (INF-7;
         NONE LOCATED ≠ UNKNOWN; INF-6 rows stay first-class) · INF-1…INF-7 · SYN-1…SYN-4
INPUT    P1-01 · P1-02 · P1-03 · P1-04 records and the quotes they already contain
```

> ⭐⭐ **The purpose of adjudication is not to make the organism consistent. It is to determine
> precisely where the evidence permits consistency, contradiction, or uncertainty to be claimed.**

> **Adjudication determines WHAT KIND of contradiction the existing evidence contains. ⛔ It does
> not manufacture new evidence to make a contradiction disappear.**

⛔ **Declared before use, binding on every line below:** no source file was opened; no runtime was
inspected; no governing document was searched for; `AUTH-EXPOSURE-01` is neither cited, awaited nor
answered; no source record was edited; ⛔ **no row value was altered to make a total reconcile**;
no summary was "fixed"; no seventh outcome was used; the word *probably* appears nowhere as a
resolution.

⛔ **Two aggregate promotions refused in advance and never made below.** `GOVERNED ACT = 0` is
written here only in its lawful form — **no row in this census established `GOVERNED ACT`** — ⛔
never as *"no acts are governed."* The **62** `EFFECT-WITHOUT-LOCATED-AUTHORIZATION` rows remain
**named findings**, ⛔ never *"62 unauthorized capabilities."* ⛔ And numerically equal states are
not aggregated where their epistemic provenance differs.

---

# OBJECT 1 · `X-DEF-1` — THE FIELD-INTELLIGENCE BLOCK

```text
OBJECT        X-DEF-1
OUTCOME       C5 · GENUINE CONTRADICTION
```

## BASIS

**The instrument's five identity tests, answered from the records alone.**

**(a) Same named block? — YES.** Both records name the same function, the same source module, the
same host file and the same line neighbourhood.

> `D_field.md` §2 D-OBJ-7 (P1-02), verbatim:
> *"`D-OBJ-7` · `analyzeFieldIntelligence` — Talk Mode field intelligence"* ·
> *"COMPUTED WHERE `lib/maia/talkModeFieldIntelligence.ts:287 analyzeFieldIntelligence()`"* ·
> *"CANONICAL CALL `maiaService.ts:1082` — dynamic import inside Talk Mode assembly."* ·
> *"SOURCE EVIDENCE `talkModeFieldIntelligence.ts:27,287,327; maiaService.ts:1079-1114,1198`"*

> `E_spiralogic_elemental.md` §3.3 (P1-02), verbatim:
> *"DECLARED WHERE `lib/sovereign/maiaService.ts:1079  let fieldAwareness = ''`"* ·
> *"COMPUTED WHERE `:1084-1094 analyzeFieldIntelligence(input, conversationHistory)` from
> `lib/maia/talkModeFieldIntelligence`"*

⭐ The two records cite **the same module, the same function, the same host file and the overlapping
line span `1079–1114`**. The sub-question P1-03 explicitly withheld — *"⛔ P1-03 does not decide
whether these are two descriptions of one object or two different objects"*
(`02_normalized_D_E_F.md`) — is **settled here as ONE object**, on those identifiers, ⛔ without
reading code.

**(b) Same cognition family? — YES.** Both place it inside Talk-Mode assembly on the canonical
`maiaService.ts` lane: D — *"dynamic import inside Talk Mode assembly"*; E — *"GATE `mode ===
'dialogue' AND process.env.TALK_MODE_FIELD_INTELLIGENCE !== 'false'`"*.

**(c) Same stage? — YES.** Both answer the **same named field**, `SURFACED WHERE`, i.e. prompt
assembly. ⭐ This is the sharpest available form of the test: not two different fields compared, but
**one field name given two opposite values in two records.**

**(d) Same prompt object? — YES.** D: *"SURFACED WHERE ⭐ **INTO THE PROMPT** as a labelled block:
`maiaService.ts:1094-1110` '🎯 TALK MODE FIELD INTELLIGENCE (Reference Context)' … ⛔ Not shown to
the member."* E: *"SURFACED WHERE ⛔ **NOWHERE**. `grep -n fieldAwareness` → exactly 1079, 1094,
1198."* and, in E §1 Q3: *"**it is never concatenated into any prompt.** Computed, logged,
dropped."*

**(e) Same branch / mode / condition? — YES, and the two records read the SAME quoted comment in
opposite directions.** Both quote `maiaService.ts:1198`.

> D, `PATH` line: *"⚠️ `maiaService.ts:1198` — 'fieldAwareness intentionally NOT appended - too
> diagnostic for early exchanges': **the block is suppressed on early turns**."*
> E, `SURFACED WHERE`: *"`:1198  // Note: fieldAwareness intentionally NOT appended - too
> diagnostic for early exchanges`"* → *"⛔ NOWHERE."*

⭐ D reads *"too diagnostic for early exchanges"* as a **condition** (suppressed on early turns,
appended otherwise). E reads the same sentence as a **rationale** for an unconditional non-append.
⛔ No third document adjudicates between those two readings, and the comment's own text is the whole
of the quoted evidence on both sides.

**The two records additionally attach OPPOSITE STATUS VALUES to the one object:**
D — *"CURRENT STATUS `WIRED-BUT-UNOBSERVED`"*; E — *"CURRENT STATUS `COMPUTED · LOGGED · NOT
SURFACED` → recorded as DORMANT at the prompt boundary while remaining LIVE as an observation"*,
and E's own `:1112` line: *"It is emitted to the server log at `:1112` and dropped."*

**⭐ The line-number collision, visible in the records and requiring no re-trace.** D names
`:1094-1110` as the **surfacing** site. E names `:1097`, `:1098`, `:1101` — inside that same span —
as the **CONTENT** the string is built from, and its exhaustive grep (`exactly 1079, 1094, 1198`)
reports no further use of the variable. ⭐ **The same lines are cited by one record as the append
and by the other as the construction of a string never appended.**

**⛔ Why no lower-cost outcome is available.**

```text
C1 SCOPE      refused — same module · same function · same host file · same span ·
              same mode gate · same SURFACED WHERE field
C2 ALTITUDE   refused — both sides are implementation reads of the same lines;
              neither side is governance, documentation, CI or runtime
C3 TIME/SUBJ  refused — same P1-02 pass, same subject, same dated corpus
C4 RECORD     refused — no count, tally or bookkeeping value is in dispute;
              the disputed proposition is about the organism's behaviour
C6 UNKNOWN    refused — the evidence is sufficient to establish that the two claims
              CANNOT BOTH HOLD; ⛔ C6 is for when that itself cannot be established
```

## WHAT IS NOW SETTLED

1. `X-DEF-1`'s two sides describe **ONE object**, not two — the `fieldAwareness` block built by
   `analyzeFieldIntelligence` in Talk-Mode assembly at `maiaService.ts:1079-1114`. The SYN-4
   sub-question P1-03 and P1-04 both carried is **closed on identity grounds**.
2. The two records assign **opposite values to the same named field (`SURFACED WHERE`) for the same
   object under the same mode gate**. Both cannot hold: a string either is or is not concatenated
   into a prompt.
3. The disagreement's locus is now named exactly: **whether `maiaService.ts:1094-1110` constitutes
   an append into the prompt or the construction of a string that is never appended**, and
   **whether the `:1198` comment states a condition or a rationale**.
4. `P3-D-07` and `P3-E-04` therefore stand as **a contradiction of record about organism
   behaviour**, ⛔ not as a scope or vocabulary artifact. P1-03's withholding of the `CONTRIBUTES`
   assignment — *"the one place in the slice where an EXPLICIT ladder assignment is withheld"* —
   was correct and remains correct.

## WHAT REMAINS OPEN

- ⛔ **Which side is true is NOT decided here**, and P1-05 has no lawful instrument to decide it:
  deciding it requires reading `lib/sovereign/maiaService.ts`, which this pass is forbidden. `C5`
  records that the corpus contains a true contradiction, ⛔ never which limb survives.
- `P3-D-34` remains attached by its source — *"its only traced consumption
  (`maiaService.ts:1083,1091`) sits inside the same disputed block"* (`05_ladder_D_E_F.md`) — and
  its position therefore stays undetermined for exactly as long as `X-DEF-1` does.
- `CONTRA-3` (edge `E-10`) touches the same span from a third direction — `fieldMonitorTelemetry.ts:165`
  calling the module *"DORMANT"* against its use at `maiaService.ts:1082-1112`. ⛔ Not merged with
  `X-DEF-1` here; it is a separate item with its own two sides.
- ⛔ Nothing above is a repair item, a defect attribution to either P1-02 worker, or a ranking.

---

# OBJECT 2 · `X-DEF-2` — ONE ROUTE, THREE CHARACTERIZATIONS

```text
OBJECT        X-DEF-2   ·   app/api/oracle/conversation/route.ts
OUTCOME       C1 · RESOLVED BY SCOPE
```

⭐ **The partition is EARNED below from sentences the records already wrote, ⛔ not assumed.** The
three characterizations are shown to answer **three different named questions**, each with its own
quoted basis. Where the records do **not** support a partition, that is said instead of invented.

## BASIS

**QUESTION 1 — what does the file CONTAIN and COMPOSE? (structural identity)**
`D_field.md` answers this and only this, and its own status lines make no reachability claim.

> `D_field.md` §3 heading: *"CAPABILITY RECORDS — Families B and C (**other cognition paths**)"*;
> §1 table: *"Family B — objects on the second cognition path
> (`app/api/oracle/conversation/route.ts`)"*
> `D-OBJ-9`: *"GOVERNANCE GATE ⭐ PRESENT AND NAMED: `app/api/oracle/conversation/route.ts:853`
> `if (process.env.MAIA_FIELD_CONTEXT_ENABLED === 'true')`"* ·
> *"CURRENT STATUS `WIRED-BUT-UNOBSERVED` · **flag state at runtime UNKNOWN in this container**"*
> `D-OBJ-10`: *"SURFACED WHERE ⭐ INTO THE PROMPT: `MemoryPalaceOrchestrator.ts:337-346` emits an
> 'ELEMENTAL COHERENCE' block … injected at `app/api/oracle/conversation/route.ts:2787`"* ·
> *"CANONICAL CALL PATH `app/api/oracle/conversation/route.ts:46,902,1499`"*

⭐ **D nowhere asserts that the route serves traffic.** Its terminal status for both objects is
`WIRED-BUT-UNOBSERVED`, and it states the runtime flag state as `UNKNOWN`.

**QUESTION 2 — what happens at the ENTRY GATE? (reachability)**
`E_spiralogic_elemental.md` answers this, repeatedly, and **applies the answer itself to line
numbers that bracket D's**.

> E §1 Q2: *"whose **only call site** is `app/api/oracle/conversation/route.ts:1611` — **below that
> route's unconditional HTTP 410 at `:446-453`**, in a block the file itself labels
> `// Unreachable below` (`:455`)."*
> E §1 Q3: *"The one prompt block that names Spiralogic Element / Spiralogic Phase / phase name /
> central question explicitly (`app/api/oracle/conversation/route.ts:**2773-2780**`) **sits below
> the 410**."*
> E elsewhere, verbatim: *"`app/api/oracle/conversation/route.ts:**846**` (below the 410)"* ·
> *"`:881` — also below the 410"* · *"`:1711` — ⛔ BELOW THE 410"* ·
> *"The calls exist at `:648`, `:1592`, `:1611` — ⛔ all below the unconditional 410 at `:446-453`.
> **The described behaviour cannot occur.**"*

⭐⭐ **This is the earning, and it is E's own work, not mine.** E states the rule (*everything below
`:455` is in a block the file labels `// Unreachable below`*) and then **applies it itself to
`:846`, `:881`, `:1592`, `:1611`, `:1711` and `:2773-2780`** — sites that bracket D's `:853`,
`:902`, `:1499` and `:2787`. ⛔ No new placement was performed here; the placement rule and its
application are both quoted.

Register 01 states the gate's scope in its own words:

> `A-4`, `01_normalized_A_B_C.md`: *"`oracle/conversation/route.ts:452` — hard 410 as **the first
> executable statement of POST**"*

**QUESTION 3 — how much TRAFFIC did it historically serve? (documentation about runtime)**
This is a third question, at **documentation altitude**, and two records independently refuse to let
it settle either of the first two.

> `F_symbolic.md` C-F5, quoted by `02_normalized_D_E_F.md`: *"a third statement about the same route
> — the anchor's '~zero live traffic' — which F **expressly does not let settle the question**"* ·
> *"⛔ the anchor may not settle this by assertion."*
> `A-4`, carried: *"Per D-P1-06 **the anchor is evidence, not law; the code stands.**"*
> `C-4 (§8.4)`, carried, and filed `SYN-4`: *"⚠️ **No runtime witness for either route was located
> in-repo**, so neither side is confirmed under the LIVE calibration."*

## WHY THE THREE CAN ALL HOLD — stated as the records state it

```text
Q1  the file CONTAINS and COMPOSES prompt material          D: quoted, line-cited
Q2  the POST entry refuses unconditionally at :446-453      E: quoted, line-cited
Q3  historical served traffic is ~zero                      CLAUDE.md: quoted, and
                                                            expressly barred from settling Q1/Q2
```

⭐ **A file may contain a complete cognition path AND refuse at its entry AND have served ~zero
traffic.** None of the three propositions denies another. ⛔ And the compatibility is not asserted
on my authority: E itself wrote the sentence that makes it explicit for the general case — *"the
table survives restart and the rows persist; **nothing at the subject can write a new one**"* —
structure present, effect unreachable, both true at once.

⛔ **C5 is refused** because no two of the three sides assert the same proposition with opposite
values. ⛔ **C2 alone is refused** because only the third limb (anchor vs code) is an altitude
difference; Q1 vs Q2 are both implementation-level readings of **different questions about the same
file**, which is scope. ⛔ **C6 is refused for the three-way partition** because the partition is
carried by quoted sentences, not by inference — but see the residues below, which are **not**
resolved by it.

## WHAT IS NOW SETTLED

1. The three characterizations of `app/api/oracle/conversation/route.ts` are **not mutually
   exclusive**, and the partition into *structural identity · entry-gate reachability · historical
   traffic* is **supported by quoted sentences in `D_field.md`, `E_spiralogic_elemental.md`,
   `F_symbolic.md` and register `01`** — ⛔ not assumed.
2. `D_field.md` is **not** in conflict with the 410: it makes no liveness claim, and terminates both
   objects at `WIRED-BUT-UNOBSERVED` with runtime state `UNKNOWN`.
3. The anchor's *"~zero live traffic"* stands at **documentation altitude**, expressly barred by two
   records from settling the code question. The `A-4` pairing (anchor vs 410) is an altitude limb
   inside this object, ⛔ not an independent organism contradiction.

## WHAT REMAINS OPEN

- ⛔⛔ **Whether the route serves member traffic is STILL UNKNOWN and is NOT settled by this
  outcome.** Carried forward unchanged: `B OQ-2` — *"whether `/api/oracle/conversation` serves
  member traffic"*; `C-4 (§8.4)` `SYN-4` — *"No runtime witness for either route was located
  in-repo."* ⛔ `C1` resolves the **characterization conflict**, ⛔ never the traffic question.
- ⛔ **Whether D's specific sites (`:853`, `:902`, `:1499`, `:2787`) are individually below the
  410 is inferred from E's stated rule applied to E's own bracketing citations, ⛔ not stated
  site-by-site by any record.** That limb is recorded as **supported but not individually
  witnessed**, and ⛔ no record was re-read to close it.
- ⛔ **The two dependency lists are NOT identical and are NOT reconciled here.** Register 02: *"rows
  `P3-D-09, P3-D-10, P3-E-05, P3-E-10, P3-E-16, P3-F-24` all depend on it."* Slice 05: *"`P3-D-09 ·
  P3-D-10 · P3-E-05 · P3-E-10 · P3-E-11 · P3-E-27`."* Slice 05 states why two of the register's six
  nonetheless recovered a position (`P3-E-16` — *"`enabledApplied` defaults to `[]` at both call
  sites, one of which is `app/api/maia/spiralogic/route.ts:89`, **not the disputed route**"*;
  `P3-F-24` — *"the quoted sentence is about what `evaluateEncounter` does when it runs, ⛔ not
  about whether the route runs"*). It states nothing about `P3-E-11` or `P3-E-27`. ⛔ Both lists
  stand as written.
- ⭐ **Slice 06's limb is CARRIED INTACT and is untouched by this outcome**, because it concerns a
  different question — domain membership, not characterization:
  > *"⛔ No `X-DEF` label appears anywhere in `03_normalized_G_H_I.md` or in the three P1-02 records
  > read here … Carried to P1-05 intact. … **UNKNOWN, by scope.**"*
  ⛔ This adjudication does **not** claim that the route lies inside, or outside, domains G · H · I.
  That limb remains `UNKNOWN BY SCOPE` exactly as slice 06 wrote it.

---

# OBJECT 3 · `X-DEF-3` — THE CENSUS'S SELF-CONTRADICTION

```text
OBJECT        X-DEF-3   ·   D_field.md §6 ans.4 vs D_field.md §7 (P1-D-GOV-02)
OUTCOME       C4 · RECORD DEFECT  (⛔ NOT organism behaviour)
```

⛔ **The P1-02 record is NOT edited, NOT corrected, and NOT described as needing correction. The
contradiction itself is the evidence.**

## BASIS

**Both sides are sentences of ONE P1-02 record, about one object, in one pass.**

> **SIDE A** — `D_field.md` §6 answer 4: *"(a)–(d) **CONTRIBUTE** to what MAIA says. (e) is
> categorically different: it **DECIDES**, and its text **replaces** MAIA's. Against the preserved
> distinctions: (a)–(d) sit at `CONTRIBUTES`; **(e) is the only object in Domain D at HAS
> AUTHORITY**."*
> **SIDE B** — `D_field.md` §7, `P1-D-GOV-02`, the same record: *"Field safety (`D-OBJ-6`) can
> REFUSE a member's turn and substitute its own text (`maiaService.ts:2848`). **No ratified source
> authorizing realm-based refusal was located.** The in-source 'H2'/'W2' commentary is a citation,
> not a located source."*
> ⚠️ Stated a second time at its own row: *"no ratified document authorizing realm-based refusal
> was located (§7)."*

**The census caught it, named it, and refused the promotion.**

> `05_ladder_D_E_F.md`: *"`P3-D-06` is the case the prohibition exists for, **and the record itself
> failed it** … The sentence is preserved as evidence and the authority claim is ⛔ not carried."*
> The recorded `INF-6` firing line, verbatim: *"`P3-D-06`   DECIDES established · HAS AUTHORITY not
> established · INF-6 prevents promotion"*
> `P1-03_CLOSE` §2: *"⛔ The P1-02 source record is NOT corrected. The defect and the later refusal
> are preserved side by side."*

**The close record's own characterization, which the text supports on its face:**

> *"This is **not a missing source**, and **not a disagreement between documents**. It is a
> contradiction **introduced by the census process itself**."*

**⭐⭐ THE DECISIVE ESTABLISHMENT — the organism's two facts are NOT in conflict.** Under
Amendment 3 §2 the axes are orthogonal, and the pair Side A observes and Side B observes is a
**listed coherent state**:

```text
PARTICIPATION  DECIDES        AUTHORITY  NONE LOCATED        ← Amendment 3 §2, verbatim
```

⭐ So the contradiction does **not** live in the organism at all. What Side A added on top of the
two compatible observations is an **inference** — the promotion of an observed effect to
`HAS AUTHORITY` — and Amendment 3 §2 had already removed that rung: *"`HAS AUTHORITY` therefore
leaves the ladder. It was never a rung; treating it as the top one is exactly what `INF-6`
forbids."*

**⛔ Why each other outcome is refused.**

```text
C1 SCOPE      refused — §6 and §7 speak about the SAME object (D-OBJ-6 / P3-D-06) under the
              same conditions; ⛔ no scope difference is available to carry the weight
C2 ALTITUDE   refused — both sentences sit at the same altitude: one P1-02 census record
C3 TIME/SUBJ  refused — one pass, one record, one subject
C5 GENUINE    refused — ⭐ the two OBSERVATIONS are compatible (Amendment 3 §2). What conflicts
              is an unsupported INFERENCE against the record's own finding, ⛔ not two
              incompatible facts about the organism
C6 UNKNOWN    refused — the evidence is complete and internal: both sentences are quoted, the
              inference is identified, and the refusal is already recorded
```

## WHAT IS NOW SETTLED

1. `X-DEF-3` is a **record-level inference defect**, ⛔ **not an organism contradiction**, and the
   establishment rests on Amendment 3 §2's own enumeration of `DECIDES + NONE LOCATED` as a
   coherent state — ⛔ not on assumption.
2. The organism facts survive intact and separately: the effect at `maiaService.ts:2848` is
   `OBSERVED`; the authorization is `NONE LOCATED`. ⛔ `NONE LOCATED` is not rendered as absence
   here (Amendment 4 §2), and `P3-D-06` is not called unauthorized (`INF-7`).
3. The `INF-6` firing record for `P3-D-06` stands as **first-class evidence at row level**
   (Amendment 4 §3), ⛔ not reduced to a count anywhere in this file.
4. ⭐ **The counter-example in the same corpus is load-bearing and is preserved**: `F_symbolic.md`
   §3.3 — *"authentication establishes `PARTICIPATES`, not `HAS AUTHORITY`"* (`P3-F-07`,
   `P3-F-08`). ⛔ One P1-02 record made the promotion and another refused it, **in the same pass, on
   the same distinction** — which is why this belongs in the SELF-CORRECTION register and ⛔ not in
   any inventory of organism defects.

## WHAT REMAINS OPEN

- ⛔ **Nothing about `D-OBJ-6`'s actual authorization is settled by this outcome.** Its standing
  remains `NONE LOCATED` — the census looked in its permitted evidence and did not find authority;
  ⛔ that is **not** a finding that no authority exists, and ⛔ **not** a verdict of illegitimacy
  (Amendment 3 §1, Amendment 4 §1).
- ⛔ The P1-02 record stays as written. ⛔ No correction, retraction or annotation to `D_field.md` is
  proposed, requested or implied here.
- ⭐ Routed to **Artifact 2 (SELF-CORRECTION REGISTER)** as its leading entry, with the counter-example
  beside it: the method's ability to discriminate against its own output is the finding, ⛔ not noise.

---

# OBJECT 4 · THE SYMBOLIC `CONCLUDE` ENUMERATION

```text
OBJECT        CONCLUDE COUNT (domain F) — three statements of "which symbolic systems
              can conclude about a member"
OUTCOME       C1 · RESOLVED BY SCOPE  (different inclusion rules, different predicates)
```

⭐ **Treated as an ENUMERATION question first, per the instrument.** The four comparisons are made
explicitly below, ⛔ before any semantic reading.

## BASIS — the four comparisons

### 1 · MEMBERSHIP

```text
(i)   F §2 table, rows annotated "concluding"      F-07 F-08 F-09 F-11 F-16 F-29
(ii)  F §4 "Where a concluding-capable system has  F-07 F-08      F-11 F-12 F-16 F-29
      no refusal surface"  (GATE: NONE FOUND)
(iii) F §6 / §11 / closing prose                   ⛔ NOT ENUMERATED — one member named (F-06)

(i) △ (ii) = { F-09 , F-12 }
```

### 2 · COUNT

```text
(i)    6        (ii)    6        (iii)    "Six … one refuses … the other five"
```

⭐⭐ **All three say SIX, and the three sixes are sixes OF DIFFERENT SETS.** The numeral is stable
while the membership is not — which is why this could not be adjudicated as an arithmetic question.

### 3 · REFUSAL CAPABILITY — the axis each list encodes

```text
(i)    ⛔ does not encode refusal at all
(ii)   ⭐ refusal IS its inclusion predicate — every member has GATE: NONE FOUND
(iii)  partitions the set:  1 refuses (F-06)  ·  5 do not
```

### 4 · DEFINITION OF `CONCLUDE` IN USE — ⭐ three different ones, each quoted

**(i) — an ANNOTATION, and ⛔ not a member of the record's own declared vocabulary.**

> `F_symbolic.md` §2, verbatim, immediately above the table: *"Classification vocabulary per the
> task: `informational · interpretive · computational · memory-bearing · runtime-participating ·
> member-facing · dormant`. **These are not exclusive.**"*

⭐⭐ **`concluding` is NOT in that list.** F added it in bold to six rows on top of the declared
vocabulary. ⛔ A table annotation applied outside the declared classification set does **not** make
its own absence a negative finding — **silence in (i) is silence, not a determination that a system
cannot conclude.**

**(ii) — a CONJUNCTIVE, functional predicate**: concluding-capable **AND** `GOVERNANCE GATE: NONE
FOUND`. Its heading is the predicate: *"Where a concluding-capable system has no refusal surface."*

**(iii) — a BROADER predicate, and it counts a refuser in**: *"**Six systems can produce a statement
about a member's situation or trajectory.** One of them refuses to (`F-06`, by construction and by
test). The other five have no mechanism that could refuse."* ⭐ `F-06` is in (iii) and in neither
(i) nor (ii) — because (iii) asks what a system *can produce*, while (ii) asks what is *ungated*
and (i) records an annotation.

## WHY THE DIVERGENCES ARE INCLUSION-RULE DIVERGENCES

```text
F-09  in (i), absent from (ii)   ⭐ EXPLAINED BY (ii)'s SECOND CONJUNCT, and the register says so:
      "omits F-09 mentor, which table (i) marks concluding and which CARRIES ITS OWN INLINE
       RESTRAINT at mentor/route.ts:32"      → it has a refusal surface, so (ii) excludes it

F-12  in (ii), absent from (i)   ⭐ EXPLAINED BY (i)'s ANNOTATION STATUS, and F §3.4 supplies the
      substantive reading the table cell does not: "F-12 runes … SAME SHAPE, same zero auth
      matches" as F-11 — which (i) does annotate "concluding"

F-06  in (iii), in neither list  ⭐ EXPLAINED BY (iii)'s BROADER PREDICATE ("can produce a
      statement") and by F-06's own recorded property: CONCLUDE "STRUCTURALLY REFUSED"
```

⛔ **`C5` is refused** because no two of these lists are the same purported set with different
content: each carries a **different, quoted inclusion rule**, and each divergence is accounted for
by that rule difference or by the annotation's declared status. ⛔ **`C6` is refused** for the
enumeration question because the inclusion rules are stated in the record itself, not inferred.
⛔ **`C4` is refused as the OBJECT's outcome** because the enumerations are not a tally that fails
to add — they are three differently-scoped questions; see the residue below for the one part that
*is* `C4`-shaped.

## WHAT IS NOW SETTLED

1. There are **not three enumerations of one set**. There are **two enumerations under two different
   quoted inclusion rules, plus one cardinality sentence that enumerates nothing** and names exactly
   one member.
2. ⭐ The `F-09` and `F-12` divergences are **fully accounted for** — `F-09` by (ii)'s
   no-refusal-surface conjunct (with the register's own reason quoted), `F-12` by `concluding`
   being an annotation outside F's declared classification vocabulary, with F §3.4's *"same shape"*
   sentence standing as the substantive evidence the §2 cell does not carry.
3. ⛔ **No number is adopted, and no system is added to or removed from any list** — the register's
   and slice 05's standing instruction is honoured exactly: *"⛔ No system was added to or removed
   from any list."*
4. ⭐ The correlation F recorded and declined to infer from — *"the one system that structurally
   cannot conclude is the only one carrying tests, a producer registry, a Sanctuary gate and a dated
   production witness"* — is **still not inferred from here**, in either direction.

## WHAT REMAINS OPEN

- ⚠️⚠️ **A NEWLY NAMED WITHIN-RECORD ARITHMETIC RESIDUE, ⛔ NOT REPAIRED, ⛔ NOT USED TO CHANGE THE
  OUTCOME ABOVE.** On **one predicate** — `GOVERNANCE GATE: NONE FOUND` — `F_symbolic.md` **lists
  six rows** and **counts five** in three separate places:
  ```text
  F §4 table, rows listed          F-07 · F-08 · F-11 · F-12 · F-16 · F-29     → 6
  F §11 open question 1            "FIVE concluding-capable symbolic systems have
                                    GOVERNANCE GATE: NONE FOUND"               → 5
  F closing block                  "1 concluding-capable system with a structural refusal ·
                                    5 with GOVERNANCE GATE: NONE FOUND"        → 5
  F §6 prose (iii)                 "the other five have no mechanism that could refuse" → 5
  ```
  ⭐ **Rows say six; the record's own summaries say five.** ⛔ Not fixed here, ⛔ neither figure
  adopted, ⛔ no row added to or removed from the §4 list. Routed to **Artifact 2 (SELF-CORRECTION
  REGISTER)** as a within-record count/list disagreement in `F_symbolic.md`.
- ⛔ **(iii)'s membership is unenumerated**, so its six **cannot be compared member-by-member** with
  (i)'s or (ii)'s six. Whether (iii) conflicts in membership with either list is therefore
  **NOT ESTABLISHED** — a `SYN-4` limb carried, ⛔ not converted into a conflict and ⛔ not
  converted into an agreement.
- ⛔ **Whether `app/api/oracle/runes/route.ts` (`F-12`) is concluding-capable as a matter of fact is
  not settled here.** What is settled is that the two lists' disagreement about it is an
  annotation-vs-determination difference, ⛔ not two contradictory determinations.
- ⛔ `P3-F-12`'s carried note stands verbatim and unedited: *"F §2 classifies runes 'interpretive ·
  member-facing' (⛔ not concluding) while F §4's no-refusal-surface list includes it. Both
  preserved."*
- ⛔ None of the six named systems is called unauthorized, illegitimate or a repair item
  (`INF-7`). Their authority standings stay as their rows record them.

---

# OBJECT 5 · THE TWO ARITHMETIC DISAGREEMENTS

⚠️ The instrument names these as one object and directs *"for **each**, compare source total ·
row-derived total · inclusion rule · denominator · excluded states."* They are adjudicated as two
sub-objects because **the comparison yields two different outcomes**, and merging them would be the
very collapse this pass exists to refuse.

---

## 5(a) · `DEV-54/51` — slice 04's 54 against register 01's 51

```text
OBJECT        DEV-54/51   ·   domains A · B · C
OUTCOME       C1 · RESOLVED BY SCOPE  (two different named fields on two orthogonal axes)
```

### BASIS — the five comparisons

```text
SOURCE TOTAL        51    01_normalized_A_B_C.md: "GOVERNING SOURCE — 51 of 62 rows are
                          NONE LOCATED"   (restated at its own tally: "GOVERNING SOURCE
                          NONE LOCATED: 51 of 62 rows")
ROW-DERIVED TOTAL   54    04_ladder_A_B_C.md: rows, its own tally ("NONE LOCATED 54") and its
                          own summary line ("AUTHORITY GOVERNED 2 · SPLIT 1 · UNKNOWN 5 ·
                          NONE LOCATED 54") ALL AGREE — ⭐ slice 04 has no internal
                          summary-vs-row disagreement
INCLUSION RULE      ⭐ DIFFERENT, AND DECLARED. 04_ladder_A_B_C.md §0, verbatim:
                    "⚠️ Declared deviation from the register's tally, so it is auditable …
                     This pass records 54 as NONE LOCATED, because three of the eleven named
                     sources are, by the records' own sentences, NOT AUTHORIZATION TEXT:
                     P3-A-08 (a witness that records its own witness as not yet obtained),
                     P3-A-10 (Canon v1.1 provenance headers), P3-A-13 (refusal-19, which the
                     register itself calls 'a CI instrument, not a governing document').
                     ⛔ No row's governing-source cell was altered; only its reading on the
                     AUTHORITY AXIS is stated here."
DENOMINATOR         62 in both
EXCLUDED STATES     none declared on either side
```

**The arithmetic closes exactly, with the difference fully named:**

```text
51  +  { P3-A-08 , P3-A-10 , P3-A-13 }  =  54          ⛔ no unaccounted row
```

**The corpus states the reason in its own words, and Amendment 3 §2 ratifies it:**

> `P1-03_CLOSE` §3b: *"one declared, auditable deviation where slice 04 reads `NONE LOCATED` on 54
> rows against the register's 51 — ⛔ both numbers carried, not averaged, because **`GOVERNING
> SOURCE` and `AUTHORITY STANDING` are different fields on different axes.**"*
> Amendment 3 §2: *"⛔ **Not one hierarchy.** Two independent axes, recorded separately, on every
> row."*

**⛔ Why `C4` is refused here.** Nothing fails to add. Both totals are correct **about their own
field**: 51 is a true count of `GOVERNING SOURCE = NONE LOCATED` cells; 54 is a true count of
`AUTHORITY STANDING = NONE LOCATED` readings. No row value was altered; the deviation was declared
before use and its three rows were named. ⛔ There is no bookkeeping defect to find. **C4 would
manufacture an error where the corpus recorded a distinction.**

**⛔ Why `C6` is refused**, although P1-04 filed the item `SYN-4` (*"Whether the two figures conflict
at all is therefore not established by the corpus"*): P1-04 was forbidden to adjudicate and
correctly declined. P1-05 is the adjudicator, and the evidence it needs is present and quoted — the
declared rule, the three named rows, the exact arithmetic, and a ratified amendment establishing the
two fields as orthogonal axes.

### WHAT IS NOW SETTLED

1. **Both numbers are correct and they are not about the same thing.** `51` counts a
   **normalization field** (`GOVERNING SOURCE`); `54` counts a **ladder-pass reading** on the
   **AUTHORITY axis**. Amendment 3 §2 makes those axes independent by ruling, not by convenience.
2. ⭐ The difference is **exactly three rows, all named** — `P3-A-08`, `P3-A-10`, `P3-A-13` — and the
   reason for each is the records' own sentence about what kind of document the named source is.
3. **MAP 3 used the ROW-DERIVED representation (`54`) for A · B · C**, consistent with its declared
   construction from rows (see 5(b)). ⛔ It did not average, choose between, or annotate away the
   register's `51`.
4. ⛔ No governing-source cell was altered by slice 04, and none is altered here.

### WHAT REMAINS OPEN

- ⛔ **Whether authorization exists for `P3-A-08`, `P3-A-10` or `P3-A-13` is NOT settled.** Their
  standing is `NONE LOCATED` — the census looked and did not find; ⛔ that is not absence
  (Amendment 4 §2) and ⛔ not a verdict (`INF-7`).
- ⛔ **Both numbers remain carried in the corpus, side by side**, exactly as `P1-03_CLOSE` §3b
  requires. ⛔ This outcome does not authorize replacing `51` with `54` anywhere, or vice versa.
- ⚠️ Whether **other** slices' registers and ladder passes diverge in the same lawful way was
  **not examined** here; this outcome is about `DEV-54/51` alone.

---

## 5(b) · MAP 3's recorded discrepancy — slice 05's summary `46 · 58` against its rows `71 · 33`

```text
OBJECT        slice 05 (D · E · F) AUTHORITY STANDING — summary block vs per-row lines
OUTCOME       C4 · RECORD / ARITHMETIC DEFECT  (⛔ internal bookkeeping, ⛔ not organism behaviour)
```

### BASIS — the five comparisons

```text
SOURCE TOTAL        05_ladder_D_E_F.md, its own summary block, verbatim:
                      "AUTHORITY STANDING (second axis)
                         GOVERNED                               4
                         GOVERNING TEXT LOCATED, UNIMPLEMENTED  3
                         NONE LOCATED                          46
                         UNKNOWN                               58"
ROW-DERIVED TOTAL   MAP3_authority.md, verbatim: "slice 05's own summary block tallies
                    NONE LOCATED 46 · UNKNOWN 58, while its 111 PER-ROW AUTHORITY LINES read
                    71 · 33 (marked * above). This map is built FROM THE ROWS, which is what
                    the nodes quote. ⛔ The discrepancy is recorded and left to P1-05;
                    ⛔ no row's value was changed to make a column agree."
INCLUSION RULE      ⭐ THE SAME on both sides — one axis (AUTHORITY STANDING), the same four
                    categories, no stated qualification or exclusion on either side
DENOMINATOR         ⭐ 111 on both sides, and BOTH PARTITIONS ARE EXHAUSTIVE:
                       46 + 58 + 4 + 3 = 111        71 + 33 + 4 + 3 = 111
EXCLUDED STATES     none on either side; GOVERNED (4) and GOVERNING TEXT LOCATED /
                    UNIMPLEMENTED (3) are IDENTICAL in both representations
```

**⭐⭐ Every discriminator that could have produced a `C1` is absent.** Same record, same axis, same
denominator, same four categories, identical values in two of the four, **and 25 rows swapped
between the remaining two**. Nothing in slice 05 declares a second inclusion rule, a second
denominator, or an exclusion — unlike 5(a), where the deviation was declared, named and
arithmetically closed.

**Corroboration of MAP 3's row count, from the permitted record itself** (⛔ a count of lines
already written, ⛔ not a new reading of the organism, ⛔ not a correction to any file):

```text
05_ladder_D_E_F.md    111 "ROW P3-…" lines    111 "AUTHORITY" lines      (1 : 1)
                      GOVERNED                                    4
                      GOVERNING TEXT LOCATED / UNIMPLEMENTED      3
                      NONE LOCATED (plain + qualified forms)     71
                      UNKNOWN                                    33
                                                            ───────
                                                                111
```

⭐ MAP 3's `71 · 33` reproduces exactly. ⛔ **This corroborates which representation MAP 3 used; it
does NOT repair, replace or annotate slice 05's summary block, which stands as written.**

**⚠️ One observation recorded and ⛔ NO INFERENCE DRAWN FROM IT.** The numeral `46` also appears in
the same summary section of the same file as the participation-axis figure — *"`RECOVERED` 46 of 111
rows"*. ⛔ The census has no evidence about how either figure was produced, and ⛔ nothing here
asserts a relation between them. The observation is recorded because it is visible in the permitted
evidence; ⛔ it is not a cause, and ⛔ it is not a resolution.

**⛔ Why the other outcomes are refused.**

```text
C1 SCOPE     refused — same field, same axis, same denominator, same categories,
             no second inclusion rule declared anywhere in the record
C2 ALTITUDE  refused — both figures are the same slice's own bookkeeping
C3 TIME/SUBJ refused — one pass, one record, one subject
C5 GENUINE   refused — ⭐ the disputed thing is a TALLY, ⛔ not a proposition about the
             organism. No organism behaviour is asserted differently by the two figures.
C6 UNKNOWN   refused — the rows are present, countable, exhaustive and self-consistent;
             the discrepancy is established, not merely suspected
```

### WHAT IS NOW SETTLED

1. Slice 05's summary block and slice 05's own 111 per-row `AUTHORITY` lines **do not agree**, and
   the disagreement is **internal bookkeeping in the census's output**, ⛔ not a finding about the
   organism.
2. **MAP 3 used the ROW representation, and said so before use** — *"This map is built from the
   rows, which is what the nodes quote"* — which is why MAP 3's `D·E·F` column reads `71` /
   `33`. ⭐ That choice is **declared**, ⛔ not silent, and it is the same discipline MAP 3 applied
   to A·B·C in 5(a) (`54`, the rows, not `51`).
3. The rows' `71 · 33` is **arithmetically closed** against the same 111 denominator and reproduces
   on an independent count of the permitted record. ⛔ The summary block is nonetheless **not
   corrected, not marked wrong in its own file, and not replaced.**
4. ⭐⭐ **The categories in disagreement are precisely the two Amendment 4 §2 forbids merging.** A
   25-row difference between `NONE LOCATED` and `UNKNOWN` is a difference between *"the census
   looked and did not find"* and *"even that search conclusion cannot be supported."* ⛔ That makes
   this a consequential bookkeeping defect, ⛔ not a cosmetic one — and it is exactly why the two
   figures must not be averaged, merged or quietly harmonized.

### WHAT REMAINS OPEN

- ⛔ **The correction itself is NOT performed here and is NOT authorized by this outcome.** `C4`
  classifies the defect; ⛔ it does not edit `05_ladder_D_E_F.md`, and ⛔ no row value was changed.
- ⚠️ **Which of the two representations the summary block intended remains unstated by its record.**
  Nothing in slice 05 explains the `46 · 58` figures, and ⛔ no explanation is supplied here.
- ⚠️ **Downstream consumers of the summary figures, if any exist outside MAP 3, were not surveyed.**
  MAP 3's use is declared and row-based; ⛔ no claim is made about any other reader.
- ⭐ Routed to **Artifact 2 (SELF-CORRECTION REGISTER)**, beside `X-DEF-3` and the `F` five-vs-six
  residue: three defects the census introduced into its own output and then caught in its own
  output.

---

# CLOSING — WHAT THIS FILE DOES AND DOES NOT ESTABLISH

```text
OBJECT                          OUTCOME
X-DEF-1  field-intelligence block            C5  GENUINE CONTRADICTION
X-DEF-2  oracle/conversation/route.ts        C1  RESOLVED BY SCOPE
X-DEF-3  census self-contradiction           C4  RECORD DEFECT
CONCLUDE enumeration (domain F)              C1  RESOLVED BY SCOPE
5(a) DEV-54/51                               C1  RESOLVED BY SCOPE
5(b) slice 05 summary vs rows                C4  RECORD / ARITHMETIC DEFECT
```

⭐ **One genuine organism contradiction out of five named objects, and two of the remaining defects
are the census's own.** ⛔ That ratio is stated as a reading of these five objects only — ⛔ it is
not a claim about the corpus as a whole, and ⛔ no object outside the five was adjudicated here.

⛔ **Standing prohibitions, restated at the close because outcomes are the easiest place to lose
them:**

```text
⛔ "No row in this census established GOVERNED ACT" ⛔ NEVER "no acts are governed"
⛔ The 62 rows stay NAMED FINDINGS ⛔ NEVER "62 unauthorized capabilities"
⛔ NONE LOCATED is never rendered as ABSENT ⛔ UNKNOWN is never upgraded to NONE LOCATED
⛔ Participation and authority are related, ⛔ never collapsed (INF-7)
⛔ Numerically equal states are not equivalent when their epistemic provenance differs
⛔ No source record was edited ⛔ no row value altered ⛔ no summary fixed ⛔ no code re-read
```

```text
P1-05 · 01 · THE FIVE NAMED OBJECTS · COMPLETE
5 objects · 6 outcomes recorded (object 5 adjudicated as two sub-objects)
1 × C5 · 3 × C1 · 2 × C4 · 0 × C2 · 0 × C3 · 0 × C6
3 entries routed to the SELF-CORRECTION REGISTER (X-DEF-3 · F five-vs-six · slice-05 46/58)
⛔ NO ORGANISM RE-TRACED · NO RUNTIME INSPECTED · NO RECORD REPAIRED · NO VERDICT RENDERED
```
