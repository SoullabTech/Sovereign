# JARVIS-KP-01 / I4 — Epistemic Join Integration Shadow

**Date:** 2026-09-21
**Status:** EVIDENCE-READY CANDIDATE · NO PR · NO MERGE · NO I5
**Founder-authorized base:** `20c8764b516e042f38e33800f71b2bb040025665`
**Reconciled opening canonical:** `0319940f9dc94a637e6f1b0e9f843c971f6fb0c2`
**Witnessed implementation head:** `193e2af37845aa215bac8ffe8300dd7f069f415c`

> **NO SEMANTIC JOIN WITHOUT A WARRANT.**

Canonical operation grammar remains:

`REFERENCE → REPORT/MOVE → COMPARE/JUXTAPOSE → HYPOTHESIZE → WARRANTED JOIN → PROMOTE/DISCHARGE`

## I4 objective

Prove that one real application proposal can be translated into canonical I2
structure and evaluated in strict observation-only shadow without changing the
active request, activating I3 persistence, or granting downstream representation
authority.

## Selected locus

The selected real path is Now What interview cell inference:

`app/api/now-what/interview/route.ts`

The route already produces a bounded system-inferred `CellCandidate` and
explicitly defines it as read-only, secondary, and non-fatal.

I4 adds no new proposal producer.

## Shadow flag

Dedicated flag:

`AIN_EPISTEMIC_JOIN_INTEGRATION_SHADOW_ENABLED`

It is enabled **only** when the exact value is:

`1`

Absence, empty string, `0`, `false`, `true`, `yes`, and all other values
remain OFF.

The I3 flag remains independent:

`AIN_EPISTEMIC_JOIN_PERSISTENCE_ENABLED`

I4 shadow enablement does not imply I3 persistence enablement.

## Translation contract

The Now What adapter translates only structure already known at the selected
seam.

It explicitly supplies:

- authenticated member scope;
- MAIA-proposed authorship for the system inference;
- member-authored provenance for the source turn;
- exact `maia_conversational_inquiry` jurisdiction;
- explicit relied-upon endpoint refs;
- empty reference-only refs;
- relation semantic `association`;
- operation `HYPOTHESIZE`;
- no offered warrants;
- no adoption acts;
- explicit uncertainty;
- explicit operative boundaries;
- requested standing `CANDIDATE_UNESTABLISHED`.

It does not fabricate:

- member confirmation;
- warrant;
- causal standing;
- diagnosis;
- scientific standing;
- another person's motive;
- source independence;
- representation authority.

## I2 authority

The adapter calls canonical:

`evaluateJoin(...)`

from:

`lib/ain/epistemic-join/evaluate.ts`

No I2 constitutional law is duplicated in the application route.

## I3 remains closed

The shadow module does not import:

`lib/ain/epistemic-join/persistence/store.ts`

and does not call a persistence function.

The literal-1 I4 flag is mechanically independent of the I3 persistence flag.

No I3 table, read API, admission succession, or persistence path is exercised.

## Runtime integration

The real route calls:

`await runNowWhatCellShadow(...)`

after the existing read-only cell candidate is produced.

The returned shadow telemetry is not assigned to a variable and is discarded.

The active response remains:

- `reply`;
- `cellCandidate`;
- provider/model provenance;
- field provenance.

No shadow verdict is included in the response body.

## Structural telemetry only

When shadow is enabled, the optional sink may receive only:

- fixed surface id;
- shadow status;
- requested jurisdiction;
- admitted standing;
- refusal-code vocabulary;
- reliance-valid boolean;
- composite-required boolean;
- the literal non-authority markers.

Telemetry does not include:

- member id;
- proposition text;
- member message text;
- cell element/phase;
- warrant/source text;
- clinical material;
- Sanctuary content;
- practitioner-private content.

Sink failure is swallowed.

## Failure posture

Translation and evaluation are both contained.

Malformed structure, missing jurisdiction, unexpected translation exceptions,
I2 malformation, or telemetry-sink exceptions cannot fail the active Now What
request.

No fallback epistemic authority is invented.

## Authority markers

Every shadow telemetry record carries:

`shadowOnly: true`

and:

`downstreamRepresentationAuthorized: false`.

Canonical I2 independently returns:

`downstreamRepresentationAuthorized: false`.

## Explicit non-authority

I4 introduces no authority for:

- I3 persistence activation;
- production persistence;
- production shadow;
- response influence;
- prompt/context mutation;
- memory mutation;
- graph-edge creation;
- Living Constellation relation display;
- practitioner-facing relations;
- Writer's Studio relation display;
- teaching-surface relation display;
- automatic adoption;
- standing promotion;
- routing;
- provider/model selection;
- schema or migration;
- production data access;
- I5;
- deployment.

## Disposition criterion

The candidate may advance only if local evidence supports:

> **Real application proposals can be evaluated through canonical I2 in shadow
> mode while remaining behaviorally inert and without activating canonical I3
> persistence or downstream representation authority.**
