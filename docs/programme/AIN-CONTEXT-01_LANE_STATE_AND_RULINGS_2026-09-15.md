# AIN-CONTEXT-01 · LANE STATE AND STANDING RULINGS

**Date**: 2026-09-15 · **Branch**: `claude/sweet-mayer-on4m5x`
**Purpose**: capture founder rulings issued after ACT 3 closed, and fix the lane's standing so the
next act is unambiguous. ⛔ Not a census, not a design, not an implementation. No source touched.

---

## 1. STANDING

```
ACT 1   ✅ CLOSED   Longitudinal continuity failure census          (C1–C9)
ACT 2   ✅ CLOSED   Invariants · Amendment 1 · Completion 1         (LC-1…LC-21, 4 discriminators)
ACT 3   ✅ CLOSED   Spiralogic runtime & process-continuity census  (D1 · owed read discharged)

A6 → F1a   ⭐ NAMED BY FOUNDER AS THE NEXT MOVE — ⚠️ NOT IDENTIFIED IN THIS TREE (§5)
ACT 4      ⛔ UNOPENED, deliberately
```

**[F]** *"I would keep ACT 4 unopened until F1a has run and received its founder ruling. Otherwise
this lane starts accumulating parallel evidence programs while the smallest live continuity defect
already has bounded authorization."*

⛔ Nothing repaired · nothing ratified · C4 unrepaired · `writeSummary` inactive · summary slot
empty · `member_spiral_state` untouched · Bridge D untouched.

---

## 2. THE LAUNDERING BOUNDARY — CANONICAL FORMULATION **[F]**

> **MAIA senses an utterance; the carrier turns that sensing into a property of the member.**

```
utterance
  ↓  bounded elemental cues + reasons / provenance
moment-level inference ........................... ✅ reusable
  ↓  hysteresis
  ↓  member-keyed persistence
"dominant_element / Elemental Identity" .......... ⛔ ontological promotion
```

**[F]** *"We do not need to dismantle the Elemental sensory apparatus because its current storage
architecture overclaims what the sensor knew."*

---

## 3. RULINGS ISSUED

### R1 · LC-4 reframed — provenance is discarded, not absent **[F]**
> *"LC-4 here is not asking us to invent explainability after the fact. It asks us eventually to
> stop discarding provenance that already exists at the moment of perception."*

⭐ This changes LC-4's cost profile wherever `reasons[]`-class traces already exist (ACT 3 §D1.3):
the obligation is **retention**, not **construction**.

### R2 · LC-22 · The unit-travel law **[F]** — generalized beyond prompts
> **A guard bound to what it guards cannot be lost. A guard appended beside it can.**
>
> For any representation capable of influencing MAIA, these travel as **one conceptual unit**:
> ```
> representation + provenance + epistemic standing + applicable constitutional guard
> ```
> ⛔ Never: representation now, with a later assembly layer hopefully remembering provenance,
> correction, humility and standing.

**[F]** *"That applies directly to L3 as well. A corrected interpretation cannot be retrieved first
and have its correction 'added later if available.' **Standing must travel with the thing whose
standing it governs.**"*

⭐ LC-22 subsumes the ACT 3 §D1.7 finding as its first evidence: `SYMBOLIC_LENS_BOUNDARY` is bound
to its payload and survives every tier; `INTERFACE_HUMILITY_GUARDRAIL` is appended beside it and
does not reach FAST.

### R3 · LC-23 · Constitutional correctness cannot depend on prompt tier **[F]**
> **FAST may contain less cognition. It cannot contain less constitutional protection.**

⛔ **This does not absorb CMT-01's D1** — *"the finding belongs where you've kept it"* **[F]**. LC-23
binds **anything this lane later builds**; it does not reach across and adopt another lane's open
defect.

### R4 · LC-21's governing formulation **[F]**
> **Signal may persist. Interpretation may persist. Neither persistence converts either one into
> identity or authority.**

Carried as the sharpened statement of LC-21 (*accumulation must not silently become authority*),
⛔ not as a new invariant. Its three enforceable children are unchanged: **LC-6** bars the source,
**LC-12** the channel, **LC-20** the count.

### R5 · Hysteresis, bounded **[F]**
> *"`applyHysteresis` by itself is not inherently a developmental violation."* The problematic
> object is the loop — **infer → persist as member state → carry across time → seed future
> inference → require new evidence to overcome old state** — because *"persistence currently
> changes the burden of proof for novelty."*

Consistent with ACT 3 §D1.4, which located the shift at `applyHysteresis` and named Bridge D the
amplifier, ⛔ not at `scoreRoute`, where prior state is `+0.10` of `~0.75` and is outvoteable by one
sentence.

---

## 4. ACT 4's OWED ITEMS — ORDERED BY FOUNDER, NOT INHERITED FROM ACT 3

**[F]** *"without inheriting conclusions from ACT 3"*:

1. **C4 first** — *falsify the restore ordering behaviour rather than carrying the derived claim
   forward as fact.* ⚠️ C4 is `ORDER BY created_at ASC LIMIT 100` read from three files and **never
   run**. It is a derived claim and must be treated as one.
2. **Bridge D cross-session hysteresis** — establish **experimentally** whether old persisted state
   creates an asymmetric burden for present novelty.
3. **`WuXingConstitution` standing** — lens-bound or member-bound, under the D1.5 natal ruling.
4. **Only then** decide whether the orchestrators, elemental agents, `FieldCoherenceEngine` and the
   Corpus Callosum elemental voices require census expansion.

⛔ **ACT 4 does not open until F1a has run and received its founder ruling.**

---

## 5. ⚠️ ONE THING THIS RECORD CANNOT RESOLVE

**`A6 → F1a` is named as an already-authorized act with bounded authorization. It is not
identifiable from this session or from this repository, and no guess has been recorded as if it
were.**

Searched: `docs/**`, `docs/programme/**`, and the reachable commit log.

| Candidate | What it actually is | Match? |
|---|---|---|
| `§A6` in `AIN-CONTEXT-01_ACT2…` | *my own* Amendment 1 section on `member_spiral_state` — the prohibited shape | ⚠️ a section label, ⛔ not an authorized act, and it has no `F1a` |
| `F1a–F1l` in `FOCUS-WITNESS-01_PROCEDURE_2026-09-10.md` | falsifier labels in the **FOCUS** lane | ⛔ different lane |
| `A6` in `WS2-07-BUILD-07B_READER_CONTRACT` | a section label | ⛔ no |

⛔ **No act pairing an `A6` with an `F1a` was found anywhere.** Since *"the smallest live continuity
defect already has bounded authorization"*, the authorization exists somewhere this session cannot
see — most likely a prior session, another branch, or a record not yet committed here.

> **⛔ Jarvis does not run a falsifier it cannot read the procedure for, and does not reconstruct a
> bounded authorization from context.** The lane holds at this line until the pointer arrives.

---

*Signal may persist. Interpretation may persist. Neither persistence converts either one into
identity or authority.*
