# JARVIS-ROUTING-INTELLIGENCE-01 · J6-FRESHNESS-RECONCILIATION-01 — Verdict

**Date:** 2026-09-18
**Mode:** documentary / local repository reconciliation only
**Reviewed J6 candidate:** `b42dc0d4c0d47c37f60a66bc06c52b4927118b84`
**Canonical subject:** `7937fc7cf1b0aede76b4e5302e36800c85f997da`
**Merge base:** `98dda3bed79d2052fa86afb17882a387187d3d2d`
**Founder authority:** `JARVIS-ROUTING-INTELLIGENCE-01 / J5`
**Mutation authority:** none — no change to PR #1383 or canonical

## 1 · Overall verdict

> **PARTIALLY CONVERGENT, CONSTITUTIONALLY DIVERGENT — RECONCILIATION REQUIRED.**

The two lineages share the same high-level direction:

- local-first intelligence;
- Qwen for code-shaped work;
- GPT-OSS for architecture/deep reasoning;
- Inkling / Nemotron as bounded external challenge;
- route evidence is not authority;
- external acts remain explicit;
- model output cannot merge/deploy/ratify itself.

They are **not** the same routing law.

Current canonical introduces valuable and stronger implementation mechanics, especially:

- an import-free deterministic route function;
- immutable/versioned route records;
- explicit required authority with `granted_authority: []`;
- structured reason codes / blockers;
- route record persistence into a canonical Work Unit;
- `provider_strategy: []` for route-bound Work Units;
- `execution_connected:false`;
- Desktop preview before creation;
- explicit refusal of `run-provider` for route-bound Work Units;
- stale asynchronous preview protection in the renderer.

Reviewed J6 contains the stronger, founder-ratified J5 constitutional law on several decisive points that current canonical does not preserve.

Neither lineage may be adopted wholesale.

## 2 · Authority reconciliation

The reviewed lineage contains the explicit founder ruling:

`JARVIS-ROUTING-INTELLIGENCE-01 / J5`

It ratifies the J5 routing constitution, including:

- deterministic capability first;
- LOCAL_ONLY continuity remains local;
- model-family eligibility only from evidence-backed task classes;
- a retry is not an independent second opinion;
- model family before transport;
- unavailable admissible transport → HOLD, no silent family substitution;
- distinct load-bearing external repository disclosure;
- host-derived lifecycle;
- durable provider result precedence;
- bounded model/adapter response-budget profiles;
- structured route provenance.

The canonical parallel lineage records one explicit founder adjudication:

`R1-NEMOTRON-01`

That ruling governs Nemotron posture only.

Canonical's `single_mechanical` fast path appears as a proposal/contract choice and is later called “ratified” in code, but the inspected discovery/contract lineage contains no founder ruling ratifying that exception.

Therefore where the parallel R1 contract conflicts with J5, **J5 governs this programme**.

The Nemotron ruling is compatible with J5 where it says:

- governed repository Nemotron uses Tinker;
- Zen remains text-only/manual;
- NVIDIA does not enter automatic V1 routing without separate adjudication.

Those points should be retained.

## 3 · Current canonical is internally proven, but not J5-conformant

Canonical native proof results at exact `7937fc7c…`:

- pure router: **20 / 20 PASS**
- route binding: **8 / 8 PASS**

Those proofs establish that the canonical R1/R3 design implements its own contract correctly.

They do **not** establish J5 conformance.

Controlled J5/J4 probes against exact canonical produced:

| J4 invariant | Canonical verdict |
|---|---|
| M1 deterministic-first | **FAIL** |
| M2 LOCAL_ONLY continuity external refusal | **FAIL** |
| M3 Nemotron excluded from CODE_GROUNDED | **FAIL** |
| M4 same-model retry ≠ second opinion | **FAIL** |
| M5 unavailable transport → HOLD / no substitution | **FAIL** |
| M6 load-bearing repository disclosure | **FAIL** |
| M7 model consensus ≠ authority | **PASS** |
| M8 durable result outranks wrapper/process | **FAIL** |
| M9 model/adapter response-budget profile | **FAIL** |
| M10 model-authored lifecycle state has no effect | **PASS** |

This is the key reconciliation fact:

> Canonical is not “bad code.” It is a coherent implementation of a narrower parallel contract that is missing or contradicts ratified J5 law.

## 4 · Required invariant-by-invariant treatment

### Deterministic-first routing

**Canonical:** routing-intelligence has no deterministic-capability input and no connection to the deterministic capability registry. A synthetic `git.rev_parse` capability still routes to Qwen.

**Reviewed J6:** explicitly checks deterministic capability before model routing.

**Verdict:** J6 law must survive.

**Canonical strength to preserve:** keep the route core import-free. The reconciled design should pass a trusted host-derived deterministic capability fact into the pure router rather than importing repository state into the router.

### E0–E4 evidence custody

**Canonical:** represents local worktree / exact external bundle / task text, but has no E0–E4 evidence-class primitive and cannot represent E2 continuity or E4 sensitive/production material.

**Reviewed J6:** explicit E0–E4 classification with E2/E4 fail-closed behavior.

**Verdict:** J6 evidence-class law must survive; canonical evidence-policy detail is additive.

### E1 → E3 external crossing

**Canonical:** knows exact external bundle vs local worktree but does not encode the constitutional E1→E3 crossing.

**Reviewed J6:** explicitly converts repository-local evidence to E3 when an external family is requested.

**Verdict:** J6 crossing law must survive.

### Load-bearing repository disclosure

**Canonical route contract:** names `repository_external_disclosure` as required route authority.

**Canonical lower execution seam:** does not carry a distinct disclosure permission in `derivePermissionEnvelope()` or `resolveOpenCodeProvider()`.

Mechanical probe: network + spend allowed Inkling resolution with no separate disclosure field.

**Reviewed J6:** adds `repo.disclose:external-readonly`, permission-envelope projection, E3 derivation, and lower resolver refusal.

**Verdict:** preserve canonical route requirement **and** J6 lower-seam enforcement.

### Evidence-backed model eligibility

**Canonical:** provider-role law is coarse:
- mechanical → Qwen
- deep reasoning → GPT-OSS
- Inkling adversarial challenge
- Nemotron frontier challenge

It permits a Nemotron frontier challenge on a mechanical-code task.

**Reviewed J6:** model-family/task profiles are derived from J3 evidence and explicitly deny Nemotron CODE_GROUNDED standing.

**Verdict:** J6 eligibility matrix governs. Canonical `review_pressure`, `challenge_mode`, and `frontier_posture` are useful orthogonal controls and should be retained.

### Same-model retry independence

**Canonical route-specific reconciliation:** an explicit named local challenger cannot be satisfied by a repeated primary provider.

**Canonical general Work Unit reconciliation:** two Qwen attempts still reach `EVIDENCE_PRESENTED`.

**Reviewed J6:** derives model family and requires distinct family / deterministic falsifier.

**Verdict:** preserve the stronger J6 general rule. Route-specific provider ordering is additive.

### Model family before transport

**Canonical:** route records choose provider IDs directly (`qwen-local`, `inkling-tinker`, `nemotron-tinker`).

**Reviewed J6:** chooses cognitive model family first, then resolves transport.

**Verdict:** canonical provider-first route record conflicts with J5 and must be refactored.

### No silent substitution / transport HOLD

**Canonical:** does not inspect provider readiness by design and therefore cannot represent unavailable transport as HOLD. It also does not silently choose a different provider.

**Reviewed J6:** separate family/transport resolution can HOLD when the preferred admissible transport is unavailable.

**Verdict:** preserve canonical purity by keeping readiness out of cognitive selection, then add a separate pure/structured transport-resolution step after family selection. No family substitution.

### Host-owned lifecycle

**Canonical:** model prose does not directly drive lifecycle, which is good.

**Canonical durable status:** a result with `exit_code:4`, `recommended_next_action:reject`, `test_results:pass` derives `ready_to_integrate`.

**Reviewed J6:** nonzero durable exit / reject derives failure.

**Verdict:** J6 host lifecycle repair must survive.

### Durable-result precedence

**Canonical:** outer wrapper/process status remains load-bearing in Desktop and `ain-delegate.sh` does not return the durable provider exit after printing the result.

**Reviewed J6:** repairs delegate return, Desktop caller outcome, and lifecycle derivation.

**Verdict:** J6 repair must survive.

### Response-budget profiles

**Canonical:** no response-budget profile exists.

**Reviewed J6:** bounded adapter/model profiles grounded in J3-B-R1, including Tinker 4096 ceilings and no auto-expand.

**Verdict:** J6 profile/provenance survives.

### Route-decision provenance

**Canonical:** stronger structure:
- `route_version`;
- immutable record;
- typed blockers;
- reason codes;
- evidence policy;
- required authority;
- `granted_authority: []`;
- execution disposition.

**Reviewed J6:** stronger constitutional fields:
- evidence class;
- eligible model families;
- family/transport separation;
- response-budget profile;
- authority facts;
- held reason.

**Verdict:** combine them. Canonical's immutable versioned record is the base shape; J5 fields are mandatory additions.

### Non-executing planning vs explicit execution

**Canonical R3:** stronger implementation membrane:
- preview in MAIN;
- route record persisted;
- no provider strategy in a routed packet;
- `execution_connected:false`;
- `run-provider` refused;
- routed UI hides Run Strategy.

**Reviewed J6:** plan-only `route-plan`, but does not persist the plan as strongly as R3.

**Verdict:** preserve canonical R3 binding and strengthen the lower controller so execution disconnect is load-bearing below MAIN as well.

## 5 · File-level reconciliation

### Direct conflicts — manual reconciliation required

Exactly seven paths conflict in a synthetic three-way merge:

1. `jarvis-desktop/src/main.js`
2. `jarvis-desktop/src/operator-work-unit.js`
3. `jarvis-desktop/src/work-unit-control.js`
4. `jarvis-desktop/test/operator-work-unit.test.mjs`
5. `jarvis-desktop/test/work-unit-cockpit.test.mjs`
6. `scripts/builder/__tests__/routing-intelligence-proof.mjs`
7. `scripts/builder/routing-intelligence.mjs`

No wholesale “ours” or “theirs” resolution is constitutionally acceptable.

### Candidate-only J5 repairs — no canonical code overlap

These reviewed J6 seams do not conflict with the parallel routing commits and should be brought forward deliberately:

- `scripts/ain-delegate.sh`
- `scripts/builder/work-unit.mjs`
- `scripts/builder/opencode-provider.mjs`
- `scripts/builder/__tests__/work-unit-proof.mjs`
- `scripts/builder/__tests__/opencode-adapter-governance-proof.mjs`
- `scripts/builder/__tests__/desktop-preload-allowlist.mjs`
- `jarvis-desktop/test/work-unit-control.test.mjs`

They still require rerun on the reconciled head; “no textual conflict” is not an automatic admission.

### Canonical-only strengths — preserve

- `jarvis-desktop/src/renderer.js`
- `scripts/builder/__tests__/routing-intelligence-binding-proof.mjs`
- canonical R1 discovery/contract records as historical parallel evidence

The renderer's preview/stale-response protections and the route-binding proof are not present in reviewed J6 and should not be discarded.

## 6 · Evidence staleness adjudication

### Still valid / not stale

The following remain valid evidence because the fact they establish is independent of the overlapping implementation:

- J3-A local model discrimination;
- J3-B / J3-B-R1 external model evidence;
- response-budget observations;
- model-role failure signatures;
- J5 founder ruling;
- J4 routing-law falsification as a constitutional test set;
- PR #1383 CI results as evidence that exact reviewed candidate `b42dc0d4…` passed its review gates.

### Historically valid but stale for current conformance

These cannot be used as current-canonical implementation evidence:

- J6 exact-head 15/15 routing proof;
- J6 route-plan fixtures;
- J6 Desktop / Work Unit / provider conformance claims where overlapping canonical files changed;
- PR #1383 mergeability/freshness.

They remain valid evidence about `b42dc0d4…`, not about `7937fc7c…`.

### Current canonical evidence

Valid for exact `7937fc7c…`:

- canonical pure-router 20/20;
- canonical route-binding 8/8;
- the J5/J4 probe matrix recorded by this reconciliation.

Those prove both canonical strengths and current J5 gaps.

## 7 · Final reconciliation verdict

The correct operation is **not**:

- merge #1383 as-is;
- discard #1383;
- revert canonical routing;
- select canonical wholesale because it is newer;
- select reviewed J6 wholesale because it was CI-green.

The correct operation is:

> **Build one new reconciled candidate from current canonical, retaining canonical R3 purity/binding mechanics while restoring the founder-ratified J5 law and reviewed J6 lower-seam conformance repairs.**

PR #1383 must remain frozen historical evidence until that new candidate exists.

## 8 · Standing

**J6-FRESHNESS-RECONCILIATION-01: PASS as diagnosis / STOP before repair.**

No code repair is authorized by this record.

No external call, provider spend, PR mutation, canonical mutation, merge, deploy, schema change, production mutation, or MAIA member-facing change occurred.
