# WS-PROPOSAL-INTERACTION-01 — RUNTIME WITNESS, PREDECLARED

**Written BEFORE the walk runs.** Recorded so the prediction cannot be fitted to the result.

## Custody — settled

```
clock              -0400
checkout → 0bd2b6578   22:07:24   (reflog)
next-server started    22:10:29
HEAD                   0bd2b65789
tracked diff           NONE on the relevant paths
preview (witness session)  HTTP 200 · state: acceptable
```

⭐ **The reflog is the load-bearing fact, not my clock arithmetic.** I offered
commit-time-versus-start-time; the founder produced the stronger form — *the checkout
reached the SHA at 22:07:24 and the server launched at 22:10:29*, so no replaced-module
ambiguity remains. A commit's authorship time is not when a checkout arrived at it.

`state: acceptable` also settles `WRITERS_STUDIO_WRITE_ENABLED` and the session.

## What arrival does NOT prove

⛔ The locus was visible on arrival in the screenshot. **That is not evidence the reveal
worked** — §23's locus sits close enough to the section start to fall in the viewport
anyway. Arrival is uninformative here; the asked-for return is the measurement.

⭐ `ACCEPT CHANGES` is absent because `a734677d` is `inspection_only`. Expected,
confirms EW-F1a is live, and **no acceptance gesture is touched in this walk.**

## The matrix

```
                first asked return   second asked return   passive scroll
Section view    locus / section-top  locus / section-top   must stay put
Whole view      locus / section-top  locus / section-top   must stay put
```

`locus` = centred on `[, fixated]`. `section-top` = §23's opening line, change off-screen.

## Predeclared prediction

```
SECTION   expected PASS
WHOLE     expected FAIL — section reached, exact locus not revealed
```

⛔ **Recorded as behaviour, never inferred from the code path.** If Whole lands at the
section top, the missing `revealToken → renderProposalEvidence` wiring becomes the
**causal explanation to verify afterward** — it is not the evidence.

⭐ **The second press is the decisive cell**, in both views: it is what separates
repeatable writer-request authority from one-time arrival behaviour. The passive-scroll
row is the negative control — the guard against involuntary return must still hold, or a
repair has traded one defect for its opposite.

## If Whole fails as predicted

The finding is narrow: *`revealToken` carries repeatable writer-request authority in the
Section proposal renderer, and that authority is not carried into `ProposalEvidenceInWork`
in Whole view.*

⛔ That justifies a small EW-F2 follow-up **in the lane's existing design** — not
resurrection of `d519c838b7`, and no schema or authorship work.
