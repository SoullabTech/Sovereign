# F5-CONFORMANCE-REPAIR-01 · P5-D-R1 — FOUNDER AUTHORIZATION

**Date:** 2026-09-17
**Founder act:** continuation after P5-E RETURN/STOP at `d349f48ce`

```text
SUBJECT                 d349f48ce · P5-E migration stop
REPAIR                  P5-D-R1 · S5 FENCE TARGET AUTHORITY
PRODUCTION / STAGING    CLOSED
P5-E EXECUTOR/RESTORE   CLOSED UNTIL R1 PASSES
```

## Authority spent

This continuation authorizes only the bounded repair identified by P5-E: make the generic S5 member-fence target population mechanically equal to lawful durable row-trigger storage relations.

R1 may amend the P5-D S5 migration, add falsifiers, run fresh disposable PostgreSQL bootstrap/migration witnesses, and record/commit/push the resulting evidence.

R1 may not change erasure dispositions, widen the v3 registry, repair lineage, run a real member deletion, connect to staging/production, merge canonically, deploy, or resume P5-E executor/restore rehearsal before this repair passes its migration witness.

## Acceptance

R1 passes only if:

1. ordinary views and materialized views cannot enter the generic row-trigger population;
2. ordinary and partitioned durable tables carrying governed identity columns remain eligible for the generic fence;
3. Circle special-state tables remain outside the generic fence;
4. an identity-bearing relation of an unexpected kind causes a loud migration failure;
5. a fresh canonical bootstrap plus the unmodified migration runner applies both account-erasure migrations successfully;
6. no production or staging connection is made.
