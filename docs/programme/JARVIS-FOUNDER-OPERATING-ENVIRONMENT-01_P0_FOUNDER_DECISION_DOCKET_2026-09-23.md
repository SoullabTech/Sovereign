# JARVIS-FOUNDER-OPERATING-ENVIRONMENT-01 — P0 · Founder Decision Docket

Only decisions **actually required before implementation**. Carried from F2 without re-argument: **FD-1** architecture · **FD-2** one read channel vs multiplex · **FD-3** projector law · **FD-4** ops-script JSON/TSV mode · **FD-5** may B1–B4 precede the F1 walk (recommended **yes**) · **FD-6** CSP. New in P0:

| ID | Decision | Why it is required before implementation | Recommendation (⛔ recommended, not taken) |
|---|---|---|---|
| **OE-1** | Open a voice child lane (proposed `JARVIS-VOICE-DOORWAY-01`) and rule: (a) push-to-talk first vs wake word; (b) STT = local whisper.cpp on the Mac Studio, ⛔ never production `maia-whisper`; (c) TTS = Kokoro local; (d) the transcript enters the O1 intent seam — one mind, two capture paths | voice is a founder direction with no owning programme; a wake word is an always-listening microphone and needs a consent ruling even on the founder's own machine; Electron media permission is a main/preload change under the five-question law | open the lane; **push-to-talk first**, wake word deferred; (b)(c)(d) as stated; sequence after B6 so voice lands on a wired intent seam rather than on a form |
| **OE-2** | Constitute a governed **No** in WORK-UNIT-01 (D-06 carry) | P5 cannot render a decline affordance until the substrate has one; without it the founder's only refusal is silence | open the act in WORK-UNIT-01 before B6; Work renders "decline not yet constituted" until then |
| **OE-3** | RB-6 Condition B: does P5 wait for RB-6B closure (Layer B IPC hop) before any execution from the app, or is P5 explicitly **plan-only** until then? | JOP-04's embargo is ACTIVE; the Condition A repair is not visible on this branch (location UNVERIFIED); an execution path built ahead of the ruling would be built into an embargo | **plan-only P5 first** (no new authority, immediate daily value); execution wiring opens on RB-6B closure — and the founder should ask JOP-04 where the Condition A repair lives |
| **OE-4** | Admit a **read-only GitHub instrument** (PR/CI status) to the B3 registry? | JOP-04 forbids writes; reads are unaddressed; the Monitor cannot show CI otherwise | yes, read-only, token custody named, refusal on any write verb |
| **OE-5** | Name the **owner of desktop daily-use reliability** (crash/relaunch, window state, restart persistence, identity equality) | no record names one; BL-F1 is unassigned and P8 cannot open | JOP (distribution) extended by charter amendment, or a new `JOP-05_RELIABILITY` act |
| **OE-6** | Artifact **reveal** (Finder, `showItemInFolder`) vs **open** (`openPath`) capability for results | preload law today permits reveal only; R-07 needs one of them; each is a five-question preload act | reveal-only in B5/B6; open deferred |
| **OE-7** | Schedule the continuity import (currently by hand) as an ops act? | resume (R-08) is only as fresh as the last manual import; scheduling is an ops change outside WORKSPACE-01 | yes, as a LaunchAgent under the ops record, read-only import, after its "read that writes" defect is repaired by CONTINUITY-BRIDGE-01 |
| **OE-8** | Non-code families: confirm that JARVIS **routes to** Writer's Studio / mail / connectors as governed houses rather than absorbing their capabilities; and whether a research (web fetch) lane may open under Provider Governance's dev-lane hold | the coverage matrix classifies 3, 9, 10 as elsewhere-governed; building JARVIS paths into them without this ruling would breach their frozen laws | confirm routing-not-absorption; research lane **not** opened until the dev-lane governance canon exists (the 2026-09-20 hold) |

**Not on the docket, deliberately:** anything a child programme already owns (C1 repair, label repair, O4 reconciliation, Merge Authority re-pin, branch-policy Q1/Q2) — those are owed *in their programmes* and P0 only names them.

**STOP.** P0 is complete on this docket. No P1–P8 act is open.
