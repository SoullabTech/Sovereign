# JARVIS-KP-01 / I5-P0R3 — §1 PREFLIGHT INSTRUMENT (ADDITIVE CANDIDATE)

STATUS: RECORD (⛔ not an authorization · ⛔ no production reading)
**Date**: 2026-09-22

> NO SEMANTIC JOIN WITHOUT A WARRANT.

---

## 1. What the §1 amendment required that no instrument did

The amendment names **three** repository-side objects — A governing authority · B
current canonical · C chosen deployment tree — and asks for **both scopes** compared
at B and C against the adjudicated property. The accepted `seam-identity.mjs check`
compares two objects and one scope. That is the gap.

## 2. Landed, ⭐ ADDITIVELY, at its own address

- `scripts/witness/seam-preflight.mjs` — emits the revised pre-deployment evidence
  block for A/B/C
- `scripts/witness/seam-preflight-falsifiers.sh` — **6/6 PASS**

⭐ It **imports the adjudicated `seam-identity.mjs` unchanged** and edits **none** of
the six re-accepted blobs. *Additive law is lawful; editing accepted custody is not.*
The six blobs are verified by the tool itself, by name, against the adjudication.

### Design laws

| law | statement |
|---|---|
| **no default** | the adjudicated digests must be passed explicitly; a default would be **inherited evidence**, which §1 B forbids |
| **both scopes, independently** | full and image compared separately at B and at C |
| **digest, never SHA** | the deployment tree's relationship to canonical is *reported*; only digest equality decides |
| **three-valued ancestry** | a grafted history yields `UNVERIFIABLE`, ⛔ never a negative |
| ⛔ **no production computation** | the tool computes nothing about production; those rows print `OWED` and are never filled |

### ⭐⭐ The line the amendment sharpened, made structural

> A lane-side **instrument** measuring production is admissible.
> A lane-side **copy** of production source substituted for production is not.

A git tree at production's asserted SHA is a lane-side *copy* of what production
**claims** to be. The tool therefore refuses to produce any production value, and
prints five `OWED` rows instead — verified by falsifier **P5** (5 rows, **0** digests
anywhere in that section).

## 3. Falsifiers — 6/6

| # | proposition |
|---|---|
| P1 | a wrong adjudicated full-scope digest is refused |
| P2 | a wrong adjudicated image-scope digest is refused, independently |
| P3 | the adjudicated digests are **not defaulted** |
| **P4** | ⭐ a stale deployment tree fails **full** scope while **image** scope still matches — proving the scopes are independent and SHA identity substitutes for nothing |
| **P5** | ⭐ every production row is `OWED` and carries no computed value |
| P6 | custody not matching the adjudication is refused **by name** (the pre-`csv_count` blob is rejected) |

## 4. ⚠️ ONE FINDING ROUTED OUT, ⛔ NOT REPAIRED

`seam-identity.mjs check` prints a line of the form `production=<sha9> seam_id=…`.
That value is computed **from the git tree at production's asserted SHA** — i.e. the
lane-side copy the amendment just named as inadmissible-as-substitute. The number is
correct for what it is (**entailed freshness**, §I.2) and it is ⛔ **not** a
production measurement, but **the label invites exactly the misreading the amendment
forbids.**

⛔ **Not repaired here**: `seam-identity.mjs` is one of the six blobs the founder
re-accepted hours ago, and editing it would invalidate that custody to fix a
human-readable label. Recorded for a later act. ⭐ The new preflight tool uses the
correct vocabulary throughout, so the safe path is available today without touching
accepted bytes.

## 5. Reading taken at this act, ⛔ NOT to be inherited

```
canonical   fa5274fd9c8761bed4d07e8437bdd1fc36e13a9a
full  scope   b828400c7aaceafbbfcc66144018a6b0fe538dab1c806bbe3de635b2fc6ff6b4
image scope   a63cf931fe80227004ba9d8730c628bb0c0d65deae6e53e8c29b6bc3b3fd3b51
```

⛔ **These are recorded, not carried.** §1 B forbids inheriting them, including from
this record. R3 must re-run the preflight at execution time and compare what it reads
then. Canonical has already moved twice in this lane without the seam moving; the
binding survives that, and the reading must still be re-taken.

## 6. Standing

`I5-P0 NOT READY · BLOCKED ON PRODUCTION FULL-SEAM FRESHNESS`

preflight instrument **CANDIDATE, additive, 6/6** · six adjudicated blobs
**UNCHANGED** · `seam-identity.mjs` labelling **ROUTED OUT, unrepaired** ·
production **NOT READ by this act** · R3 **NOT EXECUTED** · `--apply` **NOT RUN** ·
deployment **NOT AUTHORIZED by this record** · flags **ALL OFF** · rows **NONE** ·
B1 / B2 **UNREPAIRED** · ⛔ R4 · ⛔ I5-P1 · **PRODUCTION UNTOUCHED.**
