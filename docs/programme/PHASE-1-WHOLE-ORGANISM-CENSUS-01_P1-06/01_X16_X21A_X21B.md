# PHASE-1-WHOLE-ORGANISM-CENSUS-01 · P1-06 · 01 — THE FREEZE FAMILY · `X-16` · `X-21A` · `X-21B`

```text
STEP     P1-06 · CHOSEN ↔ ACTUAL COMPARISON — worker 01 (freeze family)
TYPE     RECORD ONLY — ⛔ comparison only · ⛔ re-decides neither side
INPUT    P1-00 §3a pinned exclusions (comparison authority) · P1-01 slices 03 · 04 + CLOSE ·
         P1-02 B_memory · D_field (+ P1-02 INSTRUMENT D-P1-07) · P1-03 registers/ladders ·
         P1-04 MAP1 · MAP3 · MAP4 · P1-05 01_named_objects · 02_corpus_register · 04_obligations
BOUND BY P1-06 SCHEMA §1–§6 in full
```

> ⭐⭐ **A strategic choice is not proved correct because the organism matches it, and an organism
> is not proved wrong because it diverges. P1-06 records the RELATIONSHIP between the two.**

---

## 0 · Declared before the rows

```text
⛔ NO source code re-opened · ⛔ NO runtime evidence read · ⛔ NO exclusion repaired
⛔ NO C5 or C6 re-adjudicated · ⛔ NO limb of a C5/C6 chosen to complete a comparison
⛔ NO governance created where P1 found NONE LOCATED · ⛔ NO percentage, score or aggregate
⛔ AUTH-EXPOSURE-01 neither cited, awaited nor answered
```

⭐ **Named objects only.** `X-21A` and `X-21B` are compared against `D-OBJ-n` / `P3-D-n`, never
against the words *field*, *coherence*, *RFI* or *UFI*. P1-01 slice 04 §5 found *"field"* carrying
seven objects (plus two more in the memory corpus, slice 03 / `B_memory §6`), *"coherence"* three,
*"RFI"* two; P1-02 `D_field §1.1` found *"coherence"* six distinct numeric quantities and **RFI / UFI
zero code objects**. ⛔ A comparison made against the word would be a comparison made against nothing.

⭐ **Schema §7 read and applied.** §7 is addressed to `X-05` · `X-29` · `X-19`, none of which is in
this file, but two of its precisions bear directly on these rows and are applied rather than
assumed: **§7a** — *"Absence can support alignment — but only where the exclusion's OWN SCOPE makes
that inference lawful"*, applied at `X-21A`'s `RFI`/`UFI` limb, where non-existence is recorded as
**vacuous** and ⛔ never promoted to *"the strategic exclusion is satisfied"*; and **§7b/§7c** —
`MIXED` *"is lawful only where different **named** axes or objects genuinely land differently — ⛔
never as a container for unresolved evidence"*, which is why every `MIXED` below names its objects
and states which of them carries an `UNKNOWN` that is ⛔ not counted as alignment. ⛔ Nothing here is
read as historical causation: a present-day comparison establishes no claim about how anything
was built.

⚠️ **Two things kept apart throughout, per the lane's own distinction:** *a surface is not a claim,
and an influence is not a surface.* `X-21A` is about **surfaces**. `X-21B` is about **claims**.
`D_field §6` answer 4's five traced paths are **influences**. None of the three is read onto another.

---

# ROW 1 · `X-16` — THE FULL MEMORY FIELD

```text
EXCLUSION   X-16 · The full memory field   (register @ ee3e4c47, L215)
LEVEL/CLASS STRATEGIC · DEFERRED — accepted, not doctrine
```

## CHOSEN — verbatim from the P1-00 pinned text

> *"Arena-by-arena activation, each with its own Phase 2-equivalent spec."*
> — `P1-00_CUSTODY §3a`, row 2, quoting the register at `ee3e4c47` L215

Two of the three inherited standing rules carried into the lane attach to this entry, and are
quoted because they constrain what the comparison may be read to mean:

> 1. **X-16** — *"`reopen condition satisfied ≠ feature authorized`. Satisfying a condition means the
>    question may be asked again, nothing more."*
> 2. **X-16 REVIEW FLAG** — *"the §0.C criteria were written 2026-05-24 and have not been revisited.
>    Per correction C3 in the register, the live risk is **stale reopen criteria, not missing ones**."*
> — `P1-00_CUSTODY §3a`

## GOVERNING TEXT — located, and ⚠️ NOT SINGULAR

`P1-01_CLOSE §2` maps this exclusion to one text:

> | **X-16** memory arena-by-arena | `docs/specs/COHERENCE_FIELD_WIRE_UP_SPEC_2026-05-24.md` §0.C | verbatim, slice 03 |

**Freeze A — `COHERENCE_FIELD_WIRE_UP_SPEC` §0.C, captured verbatim at `P1-01/03_memory_governance.md`
finding 1** (:29-51 of the source):

> **This document does not authorize wiring.** The freeze is the default state. Wiring requires
> Kelly's explicit lift, recorded in the `CLAUDE.md` priority thread, before any of the §3 route
> work begins.

> ### What the observation phase has NOT YET produced (freeze remains until these hold)
> - Stable evaluation
> - Closed learning loops
> - Routing coherence
> - Settled memory topology
>
> The freeze lifts only when those four hold, and only by Kelly's explicit declaration. Engineering
> pressure does not lift the freeze.

**⚠️ Freeze B — the second, distinct freeze text the same P1-01 finding records**, `MEMORY_EXPANSION_PLAN_2026-05-24.md`
§5 (:227-235), whose **exit condition is different in kind**:

> **A second, distinct freeze text** governs the other arenas: **S-8 §5 "What This Plan Does Not
> Authorize"** … Its exit condition is different in kind — **not four conditions but a per-layer
> procedure**: *"Each subsequent activation requires its own spec doc, locked-answer table…, and
> explicit Kelly directive."*
> — `P1-01/03_memory_governance.md`, Slice finding 1

⛔ **P1-06 does not decide which of the two freezes `X-16` names.** The register's entry names one;
the corpus contains two; both are quoted above. The same P1-01 finding also records, unadjudicated,
that *"§0.C is cited more broadly than its own text scopes it"* — carried, ⛔ not resolved.

⭐ Per §1 of the schema, the governing texts **illuminate**. The frozen choice, and only the frozen
choice, is the comparison authority.

## ACTUAL — P1-01…P1-05 evidence, cited by row / node / item id

**A · Activation IS differentiated per named arena and per tier — no wholesale state is evidenced.**

`P1-02/B_memory §1` enumerates **31 distinct named memory objects** (`B-01`…`B-31`) with a
per-object stop stage on `EXISTS → COMPUTED → PERSISTED → LOADED → SURFACED → UPDATED`. The
positions are not uniform: `B-03` alone meets the `LIVE` calibration; `B-06`/`B-07`/`B-15`/`B-27`
are `PARTIAL`; `B-25`/`B-28` `DORMANT`; `B-26` `ORPHANED`; the rest `WIRED-BUT-UNOBSERVED`.

`§3` of the same record names **five injection points** (`S-1`…`S-6`) and gives the per-tier split:

> - **All three tiers:** atoms (B-03), conversational recall (B-04), episodic (B-05), divination
>   (B-14), relational context bridge (B-13), member web (B-10).
> - **FAST + CORE:** relationship memory (B-12), selflet (B-17).
> - **FAST only:** `MemoryBundle` (B-02), developmental influence plan (B-06), theme signals inside
>   it (B-07).
> - **Never (loaded and discarded):** lattice resonance recall (B-15).

**B · At least one arena is prompt-influencing while the spec named for it does not exist.**

`P1-02/B_memory §2.4` (`B-05`, episodic — member-marked moments):

> ```text
> SURFACED WHERE   route :1087 → meta :1427 → FAST maiaService.ts:1433 → :1507;
>                  CORE/DEEP-repair via maiaVoice.ts:426 ADDENDA_SPECS.
> UPDATED WHERE    POST/DELETE app/api/sovereign/episodes/mark/route.ts
> ```

`P1-01/03_memory_governance.md`, Unlocated governance:

> `docs/specs/EPISODIC_LAYER_PHASE_2_SPEC.md` — named by S-10:95 and S-12:20 as **unauthored**.
> Three sources (S-10, S-12, S-13) route inputs to it. **The single largest governance vacuum in
> this slice**: episodic is repeatedly called the threshold arena, and its spec does not exist.

The organism carries the same expectation in its own source, quoted by `B_memory §2.4`:
`app/api/members/recall-preferences/route.ts:20` — *"episodic_recall_enabled (when episodic Phase 2
lands)"* — with the record's finding beside it: 🔴 *"`episodic_recall_enabled` is READ but has no
member write path… The gate exists and is enforced on read; the member cannot move it."*

**C · ⚠️ A third limb exists and is ⛔ EXCLUDED from the outcome — it DEPENDS ON a `C6`.**

`P1-02/B_memory §9`, offered by its own worker *"as evidence and not as an argument"*:

> several of the objects the freeze names — Morphic, Somatic, Coherence Field — **are reachable
> through `MemoryPalaceOrchestrator` into a prompt seam** (§2.7, S-6). Whether that constitutes
> contact with the frozen surface is **not** a question this worker may answer.

`B-24`'s surfacing is recorded as **on `/api/oracle/conversation` only** (`§1` row B-24; `§3` row
S-6). Whether that route serves member traffic is `C6` — `P1-05/02_corpus_register §7`: `X-3`
(*"whether /api/oracle/conversation serves member traffic"*), `D-1`, `X-2`, `C-4 (§8.4)`, `C-E6`,
`C-F5` — the register's own note: ⭐⭐ *"Eight of these twenty reduce to ONE unread fact."*
`P1-05/04_unresolved_obligations` `OBL-12` states the stake in terms: *"The answer decides whether
eight consciousness memory services are participants or a dead limb, and whether frozen surfaces
are being touched."*

⛔ Not both possible actual states would diverge, so `DIVERGENT` is unavailable on this limb, and
the limb is carried rather than folded into the outcome.

**D · The freeze has no implementation-side observer — recorded, and ⛔ not used.**

`P1-02/B_memory §9`: **Finding: NONE FOUND.** Zero hits for the four condition phrases in `app/` or
`lib/`; no `observationPhase` / `freezeCondition` / `liftCondition`; *"No code or operational
mechanism was found that reads, computes, tests, records or reports on any of the four conditions."*

⭐ Per `P1-02_INSTRUMENT` D-P1-07, ruled: *"the freeze remains binding; evaluator absence does not
weaken it. The absence means the **opposite** of 'probably stale.'"* ⛔ This row draws nothing from it.

## ACTUAL-SIDE STANDING

```text
ESTABLISHED   for limbs A and B — the two the outcome rests on
              (B_memory §1 · §2.4 · §3 stop stages and injection points at the subject;
               P1-01 slice 03's unlocated-spec finding)
⚠️ DEPENDS ON C6   for limb C (X-3 · D-1 · X-2 · C-4 (§8.4) · C-E6 · C-F5 · OBL-12)
                   — ⛔ carried, ⛔ excluded from OUTCOME
```

## OUTCOME

```text
MIXED
```

⭐ **Not a euphemism, and not a contradiction.** The frozen choice is one sentence with two clauses,
and the records answer them differently, on different scopes:

- **Clause 1 — *arena-by-arena activation*** — the organism's memory state is differentiated per
  named object and per tier; no record evidences a wholesale activation. **Lands with the choice.**
- **Clause 2 — *each with its own Phase 2-equivalent spec*** — `B-05` (episodic) is surfaced into
  the prompt on all three tiers while `EPISODIC_LAYER_PHASE_2_SPEC.md` is recorded as **unauthored**.
  **Lands against the choice at that named object.**

⛔ The evidence for the two clauses does not conflict; they are different questions — the *shape* of
activation, and the *governing instrument accompanying* it. That is the schema's own definition of
`MIXED`.

## BASIS

| Side | Quote | Source |
|---|---|---|
| CHOSEN | *"Arena-by-arena activation, each with its own Phase 2-equivalent spec."* | `P1-00_CUSTODY §3a`, row 2 |
| CHOSEN (standing rule) | *"`reopen condition satisfied ≠ feature authorized`"* | `P1-00_CUSTODY §3a` |
| GOVERNING (Freeze A) | *"The freeze lifts only when those four hold, and only by Kelly's explicit declaration."* | `P1-01/03_memory_governance.md` finding 1, quoting `COHERENCE_FIELD_WIRE_UP_SPEC` §0.C:43-50 |
| GOVERNING (Freeze B) | *"Each subsequent activation requires its own spec doc, locked-answer table…, and explicit Kelly directive."* | same finding, quoting `MEMORY_EXPANSION_PLAN` §5:235 |
| ACTUAL (clause 1) | *"**All three tiers:** atoms (B-03), conversational recall (B-04), episodic (B-05)… **FAST only:** `MemoryBundle` (B-02), developmental influence plan (B-06)… **Never (loaded and discarded):** lattice resonance recall (B-15)."* | `P1-02/B_memory §3` |
| ACTUAL (clause 2) | *"`docs/specs/EPISODIC_LAYER_PHASE_2_SPEC.md` — named by S-10:95 and S-12:20 as **unauthored**… The single largest governance vacuum in this slice"* | `P1-01/03_memory_governance.md`, Unlocated governance |
| ACTUAL (clause 2, same object) | *"SURFACED WHERE route :1087 → meta :1427 → FAST maiaService.ts:1433 → :1507; CORE/DEEP-repair via maiaVoice.ts:426 ADDENDA_SPECS."* | `P1-02/B_memory §2.4` (`B-05`) |
| ACTUAL (limb C, excluded) | *"Whether that constitutes contact with the frozen surface is **not** a question this worker may answer."* | `P1-02/B_memory §9` |

## ⭐ WHAT THIS DOES NOT ESTABLISH

1. ⛔ **Nothing about whether the four §0.C conditions are met, unmet, or stale.** The absence of an
   evaluator is recorded and is used for nothing. `D-P1-07` is ruled: evaluator absence does not
   weaken the freeze, and P1 authority *"may observe, may not invent."*
2. ⛔ **Not a finding that the freeze has been breached.** Whether a surfaced arena is a frozen
   arena is limb C, and limb C depends on an unread fact (`C6` · `X-3`).
3. ⛔ **Not a finding that `B-05`'s activation was unauthorized.** The record establishes that a
   named spec does not exist; it establishes nothing about what authorized the arena, and
   `P1-04` Amendment 4's `IMPLEMENTED ↛ GOVERNED` runs in both directions.
4. ⛔ **Does not decide which of the two freezes `X-16` names**, nor whether §0.C reaches the
   arenas it is cited over. Both are preserved P1-01 findings and are left as such.
5. ⛔ **Does not rank the 31 objects, count them as a score, or treat `1 LIVE` as a measure of the
   choice.** The `LIVE` calibration is a census evidence class, not a strategic metric.
6. ⛔ **Does not establish that the arena enumeration used is the right one.** `OBL-11` is open:
   six code enumerations, no ruled vocabulary. The comparison is made per named object precisely
   so that no enumeration has to win first.

---

# ROW 2 · `X-21A` — MEMBER-FACING FIELD / COHERENCE SURFACES

```text
EXCLUSION   X-21A · Member-facing field / coherence surfaces   (register @ ee3e4c47, L232)
LEVEL/CLASS STRATEGIC · DEFERRED
```

## CHOSEN — verbatim from the P1-00 pinned text

> *"Withhold every member-facing 'field state,' 'coherence,' 'RFI' or 'UFI' surface."*
> — `P1-00_CUSTODY §3a`, row 4, quoting the register at `ee3e4c47` L232

## GOVERNING TEXT — located; ⚠️ its REACH is an open question, and that is preserved

`P1-01_CLOSE §2`: *"four sources forbidding it, incl. 'no chrome, no pill, no `/maia/field`' —
verbatim, slice 04."* `P1-01/04_field_governance.md` finding 4 opens:

> **NO. Across all sixteen sources read, none authorizes a member-facing field, coherence,
> field-state, RFI or UFI surface. The absence is the finding, and it is not merely an absence —
> four sources forbid it in terms.**

The refusing texts it tabulates:

> | S-1 §7 L276 | *"**No member-facing surface** — no chrome, no pill, no `/maia/field`, no admin panel beyond the existing `/admin/maia/substrate` row."* |
> | S-1 §5 L251 | drift canary: *"No UI surface (chrome / pill / banner) added in this cut that claims field state."* |
> | S-1 §2.5 L154 | *"no named field-state recitation, no 'your coherence today is [X]' sentence anywhere in MAIA's voice region."* |
> | S-12 L164–L166 | *"a UI element citing 'Sacred Mirror Infrastructure' as a current capability"* |
> | S-13 L396 | *"Drift-visibility as runtime surface (member-facing)… **no member-facing equivalent**"* |

⚠️ **The reach of that prohibition is not settled by any record.** `P1-02/D_field §7`:

> **P1-D-GOV-05** The one prohibition P1-01 slice 04 located … **is scoped to the Coherence/Field
> wire-up (S-1), whose subject is the layer corresponding to D-OBJ-10.** ⚠️ Whether it reaches
> D-OBJ-1, D-OBJ-7, D-OBJ-35 or D-OBJ-37 **is a scope question this worker may not answer.**

Carried forward unresolved as `P1-05/04_unresolved_obligations` **`OBL-34`** (*"WHY RULING — 'a scope
question this worker may not answer.' SET a founder ruling on the prohibition's scope"*) and
**`OBL-33`** (*"Does the 'no chrome, no pill' prohibition reach lab-gated surfaces?"*).

## ACTUAL — named objects, ⛔ never the word

**A · No dedicated member-facing field-intelligence or coherence surface exists.** `P1-02/D_field §6`
answer 3, after a declared search of `app/**/page.tsx`, `app/**/layout.tsx`, the matching API routes
and 52 `components/**` files:

> - **No dedicated member-facing field-intelligence or coherence page exists.** There is no
>   `/maia/field` route; `app/field/*` is a voice conversation shell (**D-OBJ-32**) and
>   `app/maia/field-lab` is an experiment shelf (**D-OBJ-31**). Neither displays a field value.

**B · Every enumerated surface that renders such a quantity is access-gated and disclaims member
status.** Same answer:

> - Every surface that *renders* a field/coherence quantity — **D-OBJ-35** (FCI), **D-OBJ-36**
>   (somatic) — sits behind `app/labtools/layout.tsx` → `requireLabAccess()`, which states in
>   source: *"⛔ Lab Tools is NOT a member surface."*

`P1-04/MAP3` `P3-D-35`, `P3-D-36`, `P3-D-38`: each `UNKNOWN — SILENT [SYN-4]` on participation,
`AUTH NONE LOCATED [SYN-1]`, `SCOPE OF GOVERNANCE — UNKNOWN`.

**C · `RFI` and `UFI` have no object to withhold.** `P1-02/D_field §5.4`:

> **Finding: at the census subject, neither RFI nor UFI names any code object.** UFI's single
> appearance is a comment in an architecture-layer legend that explicitly excludes it. Both terms
> are **DOCUMENTATION-ONLY**.

`P1-04/MAP3` `P3-D-41` (`RFI`) and `P3-D-42` (`UFI`): `ART ⛔ NO CODE OBJECT` ·
`UNKNOWN — NOT A PARTICIPATION QUESTION [SYN-4]`. `P1-05/02_corpus_register §0d` binds the reading:
*"`NONE LOCATED` → `ABSENT`"* is a forbidden promotion, and *"there is no object"* is a fourth,
distinct UNKNOWN class (`OBL-101`), ⛔ not a finding of compliance earned by the organism.

**D · ⚠️ ONE NAMED EXCEPTION — `D-OBJ-37`, whose placement the records expressly withheld.**
`P1-02/D_field §6` answer 3 and `§4.4`:

> ⚠️ **ONE EXCEPTION, NAMED: D-OBJ-37.** `components/OracleConversation.tsx:10734-10753` mounts a
> `Coherence: N%` readout inside the component rendered on `/maia`, conditioned on `rhythmMetrics`
> and hidden by opacity rather than by mount.

> ⛔ This record does not assert that a member sees it, and does not assert the code is
> unauthorized. It records what the code does. **Whether an opacity-0, pointer-events-auto control
> rendering a coherence percentage on a member surface falls inside or outside the "no chrome, no
> pill" prohibition in §7 is a P1-04 question, not a worker's.**

`P1-04/MAP3` `P3-D-37`, carrying its authority standing:

> `UNKNOWN — the record expressly withholds the determination [SYN-4]` · `AUTH NONE LOCATED [SYN-1]`
> · `SCOPE OF GOVERNANCE — UNKNOWN` · BASIS … *"⛔ NONE FOUND"; P1-D-GOV-05's scope question "is a
> P1-04 question, not a worker's"*

`P1-04/MAP1` D-37: *"UNKNOWN — record expressly withholds · SYN-4 · COV N/A — UI overlay."*
`P1-05` **`OBL-32`**: *"Which is the finding about the opacity-hidden, pointer-events-auto coherence
readout mounted on the member surface — that it is there, or that nothing rules whether it may be?
WHY RULING. SET a founder ruling."*

⛔ P1-04 recorded the question and did not answer it; `OBL-32` and `OBL-34` are open founder items.
**This row uses that standing and ⛔ does not extend, rate or decide it.**

**E · Objects enumerated to prevent conflation, ⛔ not counted as field-state surfaces.**
`P1-02/D_field §4.2`: **D-OBJ-30** "Living Field" is member-facing (`/maia/living-field`) and shows
*"NO field-intelligence number found"*; **D-OBJ-29** "Fields" workspace is *"out of D's substantive
scope; enumerated to prevent conflation"*; **D-OBJ-39** `/api/maia/field` is *"⚠️ **0 in-repo
callers** — ORPHANED by its own header's determination"*. Each bears the word and not the quantity.

## ACTUAL-SIDE STANDING

```text
ESTABLISHED   for the enumerated surfaces — the search scope is declared, the gates are quoted
              from source by the record, and RFI/UFI carry a grep-level negative (D_field §5.4)
UNKNOWN       for D-OBJ-37 — P1-02 expressly withheld the determination, P1-04 carried it as
              SYN-4 with AUTH NONE LOCATED, and OBL-32 · OBL-34 are open founder items
              (⛔ this is not a C5 and not a C6; it is a declared withholding)
```

## OUTCOME

```text
MIXED
```

Different **named objects** land differently, and the records establish the different scopes:

- **`RFI` · `UFI` (`P3-D-41` · `P3-D-42`)** — nothing is surfaced because **no object exists**. The
  choice is satisfied at the subject, ⛔ and satisfied vacuously: `NONE LOCATED` is not `ABSENT`,
  and *"there is no object"* is not the organism having withheld one.
- **`D-OBJ-35` · `D-OBJ-36` · `D-OBJ-38`, and the absence of any `/maia/field` page** — the only
  surfaces rendering such a quantity are access-gated, with the source itself stating *"Lab Tools is
  NOT a member surface."* **Lands with the choice** at the census's own evidence class.
- **`D-OBJ-37`** — a `Coherence: N%` readout is mounted inside the component rendered on `/maia`,
  and every record that touched it **declined to place it**. **Neither aligned nor divergent —
  UNKNOWN, and it stays UNKNOWN here.**

⛔ `MIXED` is not standing in for a contradiction: no two records disagree about any of these
objects. What differs is the object.

## BASIS

| Side | Quote | Source |
|---|---|---|
| CHOSEN | *"Withhold every member-facing 'field state,' 'coherence,' 'RFI' or 'UFI' surface."* | `P1-00_CUSTODY §3a`, row 4 |
| GOVERNING | *"**No member-facing surface** — no chrome, no pill, no `/maia/field`, no admin panel beyond the existing `/admin/maia/substrate` row."* | `P1-01/04_field_governance.md` finding 4, quoting `COHERENCE_FIELD_WIRE_UP_SPEC` §7 L276 |
| GOVERNING (reach, open) | *"⚠️ Whether it reaches D-OBJ-1, D-OBJ-7, D-OBJ-35 or D-OBJ-37 is a scope question this worker may not answer."* | `P1-02/D_field §7` `P1-D-GOV-05` |
| ACTUAL (A) | *"**No dedicated member-facing field-intelligence or coherence page exists.** There is no `/maia/field` route"* | `P1-02/D_field §6` answer 3 |
| ACTUAL (B) | *"sits behind `app/labtools/layout.tsx` → `requireLabAccess()`, which states in source: '⛔ Lab Tools is NOT a member surface.'"* | `P1-02/D_field §6` answer 3 |
| ACTUAL (C) | *"**Finding: at the census subject, neither RFI nor UFI names any code object.**"* | `P1-02/D_field §5.4`; `P1-04/MAP3` `P3-D-41` · `P3-D-42` |
| ACTUAL (D) | *"⛔ This record does not assert that a member sees it, and does not assert the code is unauthorized."* | `P1-02/D_field §4.4`, carried at `P1-04/MAP3` `P3-D-37` |
| ACTUAL (D, standing) | `UNKNOWN — the record expressly withholds the determination [SYN-4]` · `AUTH NONE LOCATED [SYN-1]` | `P1-04/MAP3` `P3-D-37` |

## ⭐ WHAT THIS DOES NOT ESTABLISH

1. ⛔ **Does not decide `D-OBJ-37`.** Not whether a member sees it, not whether the prohibition
   reaches it, not whether its presence is a breach. `OBL-32` and `OBL-34` remain open founder items
   and this row creates no pressure on either answer.
2. ⛔ **Does not establish that the withheld surfaces are withheld *by* the exclusion.** Every
   `P3-D-n` node in the field domain carries `AUTH NONE LOCATED`. An access gate is not an
   authorization and not a governance record — `P1-04` Amendment 3's `EFFECT-WITHOUT-LOCATED-
   AUTHORIZATION` and `INF-3` (*selection authorizes nothing*) both apply. **Alignment observed is
   not compliance demonstrated.**
3. ⛔ **Does not convert `RFI`/`UFI`'s zero code objects into a claim that the organism withheld
   them.** There was no object to withhold; `NONE LOCATED` is never rendered `ABSENT`.
4. ⛔ **Says nothing about influence.** `P1-02/D_field §6` answer 4 traces five paths by which field
   objects reach what MAIA says — `(a)`–`(d)` **CONTRIBUTE** (`maiaService.ts:1521→:1534` FAST ·
   `:1946→:1962` CORE · `:1082→:1094` Talk Mode · `oracle/conversation/route.ts:902→:2787`), and
   `(e)` **DECIDES** (`maiaService.ts:2838→:2848`, `enforceFieldSafety`, whose *"text **replaces**
   MAIA's"*). ⭐ **`X-21A` governs surfaces, not influence.** An organism that surfaces nothing may
   still be shaped throughout by the same objects, and this row makes no claim either way.
5. ⛔ **Does not characterize `D-OBJ-6`'s substituted copy as a member-facing field surface.** No
   record does, and doing so here would be a new characterization, not a comparison.
6. ⛔ **Does not clear `D-OBJ-35`.** `§4.3` records it rendering the hardcoded fallback *"`0.5` →
   '50%'… with nothing distinguishing the fallback from a measurement"*. That is inside `OBL-33`,
   not inside this row's alignment.
7. ⛔ **Does not treat the census's search as exhaustive of the organism.** `P1-02` states its own
   scope; a surface outside the declared search scope is not evidence of absence.

---

# ROW 3 · `X-21B` — CLAIMS OF RFI / UFI / FIELD INTELLIGENCE

```text
EXCLUSION   X-21B · Claims of RFI / UFI / field intelligence   (register @ ee3e4c47, L240)
LEVEL/CLASS STRATEGIC · UNPROVEN
```

## CHOSEN — verbatim from the P1-00 pinned text

> *"Even once a surface becomes technically possible, the ontological claim stays evidence-gated."*
> — `P1-00_CUSTODY §3a`, row 5, quoting the register at `ee3e4c47` L240

Standing rule carried with it:

> 3. **X-21B** — the CAN/CANNOT claim table. Episodic shipping does not license the ontology.
> — `P1-00_CUSTODY §3a`

## GOVERNING TEXT — located, and the strongest in the family

`P1-01/04_field_governance.md` finding 1, reproducing `COHERENCE_FIELD_WIRE_UP_SPEC` §0.D L64–L79
exactly, and naming it: **"This is the governing text for X-21B."**

> If and when the freeze lifts, **activation must claim a bounded continuity layer, not proof of a
> resonant field.** This clause exists not as future-capability framing but as a *boundary that
> holds even after activation* — a sharper version of the freeze, not a weakening of it.
>
> | What activation CAN claim | What activation CANNOT claim |
> | --- | --- |
> | "MAIA has continuity signals from prior interactions." | "The resonant field is alive." |
> | "Coherence readings inform routing." | "The field is forming." |
> | "Field state may modulate elemental tone lightly." | "We have proven RFI." |
> | "The field layer is observational." | "MAIA possesses field intelligence." |

The three properties the slice states *"in the text, not inferred"*: it is **conditional on the
freeze lifting yet holds after activation** — *"the post-activation ceiling"*; the CAN column
*"licenses **only** continuity, routing influence, light tonal modulation, and observational status.
Every entry is mechanism-level. No entry licenses an ontological noun"*; and the CANNOT column is
*"the only located text that names the exact strings 'We have proven RFI' and 'MAIA possesses field
intelligence' as forbidden claims."*

## ACTUAL — what P1 earned, and what it never looked at

**A · The census's object is code objects and traced paths. It contains no claim inventory.**
Across `P1-02` domain D's eleven sections, `P1-03`'s registers, `P1-04`'s four maps and `P1-05`'s
adjudications, **no record evaluates any outward statement of the organism against the §0.D CAN /
CANNOT table**. `P1-02/D_field §11` is titled *"WHAT THIS RECORD DOES NOT CLAIM"*; `P1-04/MAP3`
declares *"⛔ No node, edge, caption, heading or summary line in this file renders a verdict of
legitimacy on anything."* The comparison `X-21B` asks for — *is the claim evidence-gated* — has no
actual side in the P1 corpus.

**B · The term the CANNOT column names has no referent in code, and two referents in documents.**

`P1-02/D_field §5.4`:

> ⭐ This is precisely the vocabulary rule's point: *"RFI is built"* and *"RFI is not built"* were
> **both unevaluable**. Once the referent is demanded, the question dissolves — **there is no
> referent in code.**

`P1-01/04_field_governance.md` finding 5, sub-collision:

> **"RFI" names two different objects.** S-14 L28 lists *"RFI ('becoming active now')"* as a
> **not-built layer** of the 15-layer memory stack. S-16 L11 defines RFI as an **interactive
> environment / research instrument** whose iteration-1 components it claims are *"assembled and
> operational."* These are not the same object, and neither document acknowledges the other. **Any
> statement of the form "RFI is / is not built" is therefore ambiguous until the referent is named.**

`P1-01_CLOSE §3a` draws the binding consequence in terms:

> ⭐ **The RFI collision has teeth.** … **The X-21A/X-21B comparison therefore cannot be made against
> the word — only against a named object.**

**C · Nothing in the corpus bridges from an object to a claim state.** Two candidates exist and both
were expressly left unplaced:

- **`D-OBJ-35`** — `P1-02/D_field §4.3` records the page rendering the hardcoded fallback with
  *"nothing distinguishing the fallback from a measurement"*, and **"⛔ No repair proposed; no ruling
  made."** `P1-04/MAP3` `P3-D-35`: `UNKNOWN — SILENT on cognition [SYN-4]` · `AUTH NONE LOCATED`.
  Its disposition is `OBL-33`, an open founder ruling.
- **`D-OBJ-37`** — placement expressly withheld (Row 2 above); `OBL-32` open.

⛔ Neither record characterizes either object as an **ontological claim**. Characterizing them so
would be this worker deciding, which is forbidden.

**D · The descriptive in-source claims the corpus DID adjudicate are a different object.**
`P1-05/02_corpus_register §6` lists `C5-07` (`substrateMap.ts:383-391` *"consumers: [] … no live
consumer wired"*), `C5-08` (`fieldOrchestrator.ts:3` sole-seam), `C5-09` (`QuantumFieldMemory.ts:2-6`
*"maintaining persistent field states"* against *"`:91` is `new Map()`"*). The register's own
principle `P-A` classifies these as **descriptive ↔ descriptive** — *claims about what the code
does*. ⛔ They are not ontological claims about a field's existence, and ⛔ they are not re-adjudicated
or repurposed here.

## ACTUAL-SIDE STANDING

```text
UNKNOWN
```

⭐ Precisely: the object-side facts cited above (`§5.4`'s zero code objects; `P3-D-41`/`P3-D-42`;
the two documentary referents for `RFI`) are themselves **ESTABLISHED** — but they are not the
actual side this exclusion addresses. **P1 gathered no claim-state evidence at all**, so the side
that would have to be compared has no standing, established or otherwise.

⛔ This is not blocked on a `C5` or a `C6`. No adjudication is pending that would supply it.

## OUTCOME

```text
NOT COMPARABLE
```

Per the schema's definition — *"chosen statement and actual evidence operate on non-equivalent
objects, and the existing records do not lawfully bridge them"*:

- The **chosen statement's object is a claim state** — what may be asserted, and on what evidence,
  once a capability exists.
- The **census's object is code objects and traced paths**. It enumerated 40 field-domain objects
  before any status, placed each on participation and authority axes, and expressly rendered no
  verdict of legitimacy on any of them.
- The **one term the governing CANNOT column names** — `RFI` — carries **zero code objects** and
  **two unreconciled documentary referents**, so the sentence *"the ontological claim about it"* has
  no single subject at the census subject. P1-01 says so directly: the comparison *"cannot be made
  against the word — only against a named object,"* and the named object does not exist.
- The two candidate bridges (`D-OBJ-35`, `D-OBJ-37`) are both objects whose characterization the
  records **withheld** and routed to founder rulings (`OBL-32` · `OBL-33` · `OBL-34`). ⛔ Using
  either to complete the comparison would be supplying the missing characterization.

⛔ `ALIGNED` is unavailable: nothing in P1 shows the organism refusing the forbidden claims, only
that it has no code object to attach one to. ⛔ `DIVERGENT` is unavailable: no record quotes the
organism making one. ⛔ `MIXED` is unavailable: there are not two scopes landing differently, there
is one scope never examined.

## BASIS

| Side | Quote | Source |
|---|---|---|
| CHOSEN | *"Even once a surface becomes technically possible, the ontological claim stays evidence-gated."* | `P1-00_CUSTODY §3a`, row 5 |
| CHOSEN (standing rule) | *"the CAN/CANNOT claim table. Episodic shipping does not license the ontology."* | `P1-00_CUSTODY §3a` |
| GOVERNING | *"activation must claim a bounded continuity layer, not proof of a resonant field… a boundary that holds even after activation — a sharper version of the freeze, not a weakening of it."* | `P1-01/04_field_governance.md` finding 1, quoting §0.D L64 |
| GOVERNING | CANNOT: *"The resonant field is alive." · "The field is forming." · "We have proven RFI." · "MAIA possesses field intelligence."* | same, §0.D L66-71 |
| ACTUAL (B) | *"**Finding: at the census subject, neither RFI nor UFI names any code object.** … Once the referent is demanded, the question dissolves — **there is no referent in code.**"* | `P1-02/D_field §5.4` |
| ACTUAL (B) | *"'RFI' names two different objects… neither document acknowledges the other. Any statement of the form 'RFI is / is not built' is therefore ambiguous until the referent is named."* | `P1-01/04_field_governance.md` finding 5 |
| ACTUAL (bridge refused) | *"The X-21A/X-21B comparison therefore cannot be made against the word — only against a named object."* | `P1-01_CLOSE §3a` |
| ACTUAL (C) | *"**Recorded as evidence. ⛔ No repair proposed; no ruling made.**"* | `P1-02/D_field §4.3` (`D-OBJ-35`) |
| ACTUAL (A) | *"⛔ No node, edge, caption, heading or summary line in this file renders a verdict of legitimacy on anything."* | `P1-04/MAP3 §0` |

## ⭐ WHAT THIS DOES NOT ESTABLISH

1. ⛔ **`NOT COMPARABLE` is not a verdict on the exclusion.** It says the census cannot be laid
   against it, ⛔ not that the exclusion is unclear, unmet, or unnecessary. `X-21B`'s governing text
   is the **best-located and most specific** of the four located governing texts in this family.
2. ⛔ **Not a finding that the organism makes no ontological claim.** P1 never looked. Product copy,
   marketing surfaces, onboarding text, admin copy and MAIA's own generated sentences were outside
   every domain record's scope. **Silence in a census that did not ask is not a negative finding.**
3. ⛔ **Not a finding that the organism does make one.** No record quotes one.
4. ⛔ **`RFI`/`UFI` having zero code objects does not satisfy `X-21B`.** The exclusion governs the
   claim *"even once a surface becomes technically possible"* — an object's absence removes the
   subject of a claim, ⛔ it does not demonstrate the gate.
5. ⛔ **Does not resolve which of the two `RFI` referents is meant** by any source, nor amend either.
   P1-01 recorded the collision as a governance gap and did not repair it; neither does this.
6. ⛔ **Does not read `C5-07` · `C5-08` · `C5-09` as ontological claims.** They are descriptive
   claims about code, classified `P-A` by the register, ⛔ not re-adjudicated and ⛔ not repurposed.
7. ⛔ **Does not decide `D-OBJ-35` or `D-OBJ-37`.** `OBL-32` · `OBL-33` · `OBL-34` are untouched.
8. ⛔ **Does not name what evidence would gate the claim.** Writing that standard would be amending
   a frozen strategic entry — the same boundary `D-P1-07` drew for `X-16`'s conditions.

---

## CLOSING — the relationship, stated once

```text
X-16    MIXED            ESTABLISHED (two limbs) · ⚠️ one limb DEPENDS ON C6 — excluded
X-21A   MIXED            ESTABLISHED (enumerated surfaces) · UNKNOWN (D-OBJ-37, withheld)
X-21B   NOT COMPARABLE   UNKNOWN — P1 gathered no claim-state evidence
```

⭐ Read together, these three rows say something the schema's governing sentence anticipated: the
freeze family is **most comparable where it names a mechanism** (`X-16`'s arenas and specs;
`X-21A`'s surfaces), and **least comparable where it names a claim** (`X-21B`). ⛔ That is a property
of what P1 was authorized to census, ⛔ not a ranking of the three choices, ⛔ not a finding that a
claim-level exclusion is weaker, and ⛔ not a recommendation that anything further be read.

```text
⛔ NO exclusion repaired · ⛔ NO C5 or C6 re-adjudicated · ⛔ NO limb chosen
⛔ NO governance created where P1 found NONE LOCATED · ⛔ NO percentage or score
⛔ SOURCE UNCHANGED · ⛔ PRODUCTION UNTOUCHED
```
