# PHASE-1-WHOLE-ORGANISM-CENSUS-01 · P1-06 · ROW 02 · X-19

```text
STEP     P1-06 · CHOSEN ↔ ACTUAL COMPARISON
ROW      X-19 · System-inferred trust does not silently become relational authority
TYPE     RECORD ONLY — ⛔ comparison only · ⛔ re-decides neither side
SCHEMA   frozen at …_P1-06_SCHEMA_2026-09-15.md — this row is bound by it in full
```

> ⭐⭐ **A strategic choice is not proved correct because the organism matches it, and an organism
> is not proved wrong because it diverges. P1-06 records the RELATIONSHIP between the two.**

⚠️ **This row is written under the schema's X-19 constraint, quoted so that violating it later
reads as a breach:**

```text
X-19    ⛔ Do NOT collapse CONCLUDE · DECIDES · inference · persistence · member-facing
        effect into one axis. P1-05 specifically preserved those distinctions.
```

The `ACTUAL` section is therefore structured **by axis**, five axes, each with its own evidence.
The outcome is stated **once, for the exclusion as a whole**, and §6 names which axes it rests on
and which it does not.

---

## 1 · EXCLUSION

```text
X-19    Entry: "System-inferred trust does not silently become relational authority"
        Register line L312 @ ee3e4c47
        LEVEL · CLASS   STRATEGIC (retained) · DEFERRED — evidence-and-authority-gated
```

---

## 2 · CHOSEN — verbatim from the P1-00 pinned text

> *"System-inferred trust does not silently become relational authority."*
> — P1-00 CUSTODY §3a, row 3, quoting the register at `ee3e4c47` L312

**Recorded ALTERNATIVE (the excluded state), as pinned:**

> using inferred trust observations to weight **affinity**, **matching**, **access**,
> **recommendation** or **relational significance** — **WITHOUT A NEW AUTHORITY ACT**.

⭐ The alternative names **five consumers** and **one condition**. This row compares against all
six, separately. ⛔ The five are not treated as one, and the condition is not treated as satisfied
merely because no authorization was located — see §5.0.

---

## 3 · GOVERNING TEXT

```text
⚠️  NO SINGLE GOVERNING TEXT LOCATED
```

**The P1-01 finding, quoted:**

> *"⚠️ **X-19 and X-29 have no single governing text.** Slice 05 assembled X-19's inputs from
> eleven sources and quoted them without adjudicating … **The absence is itself the P1-01
> finding**, and it was predicted before the slices ran."*
> — `…_P1-01_CLOSE_2026-09-14.md` §2

**The eleven sources, as assembled at `…_P1-01/05_spiralogic_symbolic.md` §4**
(*"Which documents could produce a claim about a person, and what guardrails do they state?
(X-19 input — QUOTED, NOT ADJUDICATED)"*):

```text
S-2   ICHING_STRUCTURAL_ENGINE            guardrail: presentation only · no status line
S-16  CANONICAL_SPIRALOGIC_12_PHASE_REF   guardrail: NONE STATED · no status, no date
S-21  integration guide                   guardrail: NONE STATED
S-18  docs/framework/facets/*             guardrail: NONE STATED
S-6   MAIA_KNOWLEDGE_FIELD_v1.0           per-member domain affinity signals; ⚠ no consent gate
S-12  JOHN_OF_THE_CROSS_USE_FRAME         strongest in slice: disallowed phrasing, discernment
S-11  USE_FRAME_ACTIVATION                seven boundaries; ⚠ all five adoption decisions OPEN
S-1   symbolic guidance doctrine          optional/scoped/reversible; confidence threshold
S-17  MAIA_SPIRAL_ORIENTATION_CUT2        "No phase assertions. Evidence → questions"
S-15  SPIRAL_CONTINUITY_ENGINE            governs the whole class — formation restrictions
S-23  SOVEREIGNTY_INVARIANTS §Invariant 13 governs the whole class — claim-type floor
```

**The slice's own closing sentence, carried:**

> *"X-19 relevance is recorded, not ruled: **S-15:130** and **S-23:245** are the two located
> sentences that speak to inference silently becoming relational authority. Both are quoted above
> in full context. **No adjudication is made here.**"*

⛔ **Consequence, stated before the comparison rather than after it.** Per schema §1, P1-06 may
compare the frozen choice to the organism *"⛔ but **must not pretend that comparison is
constitutional verification.**"*

⭐ Two sentences that *speak to* a subject are not a governing text for it. Neither S-15:130 nor
S-23:245 defines the exclusion's operative term — **"relational authority"** — and neither was
written as X-19's governing source. They **illuminate**; they do not substitute.

**S-15:130** (`docs/canon/SPIRAL_CONTINUITY_ENGINE.md`, self-declared Canon, articulated 2026-05-21):

> *"These are not display restrictions. They are formation restrictions… A developmental
> assessment silently held but never spoken has already violated the canon."*

**S-23:245** (`docs/canon/MAIA_SOVEREIGNTY_INVARIANTS.md` §Invariant 13, constitutional):

> *"The governor on a symbolic statement is its claim type, not its tradition."* … Tier 1 — any
> interpretive framework *"may be offered only **as a lens**… Never as a statement about the
> member's reality ('this is what is happening to you'). **The member remains the verifier.**"*

⭐ Both bear on **CONCLUDE** (formation of, and claim-type of, an assessment about a person).
⛔ **Neither bears on DECIDES, PERSISTENCE, or MEMBER-FACING EFFECT**, and this row does not stretch
them across those axes. That stretch is the precise move Amendment 3 §3 forbids, in reverse.

---

## 4 · ACTUAL — BY AXIS

⭐ Five axes, five bodies of evidence. Per Amendment 3 §3, **a yes on one establishes nothing about
the others**, in either direction:

```text
CONCLUDING-CAPABLE = yes  establishes NONE of
    DECIDES  ·  CONTRIBUTES  ·  MEMBER-FACING  ·  PERSISTED  ·  AUTHORIZED
```

### 4.1 · AXIS 1 — does the system INFER something about a person?

**ESTABLISHED, broadly, and on named objects.**

| Node | Object | Record's own words |
|---|---|---|
| `P3-C-03` | `member_spiral_state` | *"**SYSTEM-INFERRED.** **PROVENANCE COLUMN: NONE FOUND.**"* — no `created_by`, `source` or `authored_by`; *"Provenance is asserted only in a code comment … not recorded per row."* |
| `P3-C-04` | `relationship_entries` | ⚠️⚠️ *"**MIXED PROVENANCE INSIDE ONE WRITE**"* — member `felt_signals`/`free_text` beside MAIA `maia_reflection`/`pattern_hint`. *"**PROVENANCE COLUMN: NONE FOUND.** Provenance is carried by *column name*, not by a recorded field."* |
| `P3-C-05` | `member_relational_signals` | *"**PROVENANCE COLUMN: PRESENT** — `source … CHECK (source IN ('maia_conversation','labtool_manual'))` … **the only substrate in domain C with a schema-enforced provenance discriminator.**" |
| `P3-C-08` | `living_field_affinities` | *"**system-created from member memory atoms** … `affinity_score NUMERIC(4,3)`"*; `created_by … DEFAULT 'system'` · `evidence_reason TEXT NOT NULL` |
| `P3-C-09` | `member_theme_signals` | *"**SYSTEM-INFERRED. PROVENANCE COLUMN: NONE FOUND**"* |
| `P3-C-12` | `trust_observations` | `engagement_proxy FLOAT · feedback ∈ {positive, negative}`; *"**PROVENANCE COLUMN: NONE FOUND. MEMBER AUTHORITY: NONE FOUND.**"* |

⭐ **The domain's own consolidated statement** (`C_developmental_relational.md` §4):

> *"**No shared provenance convention exists.** Five mechanisms, and four substrates with none."*

⭐ **This axis is where the exclusion's word *silently* has its sharpest purchase, and the evidence
is split even within it**: `living_field_affinities` and `member_relational_signals` carry
schema-enforced provenance; `member_spiral_state`, `member_theme_signals`, `relationship_entries`
and `trust_observations` carry none. ⛔ Not aggregated into a verdict here.

### 4.2 · AXIS 2 — does that inference PERSIST?

**ESTABLISHED for every object in §4.1** — each is a declared table with a traced write path
(`C_developmental_relational.md` §3, `DECLARED WHERE` / `PERSISTED WHERE` on C-3, C-4, C-5, C-8,
C-9, C-12). The census's own central sentence on what that persistence carries (ibid. §6):

> *"⭐⭐ **member authority is strongest exactly where the object is member-authored, and is
> `NONE FOUND` on every system-inferred developmental object except the one the member can merely
> read.**"* · *"⭐ **ERASURE of formed inferred developmental material has NO located member path
> in bands II, III(signals), V.**"*

⚠️ **Persistence carries no authority by itself, and this row does not let it:** *"⛔⛔ the
migration's own declared intent ('Feeds future symbolic affinity weighting') is **NOT promoted** to
`DECIDES` or `CONTRIBUTES`. **A declared future consumer is not a participation.**"* — MAP3
`P3-C-12`

### 4.3 · AXIS 3 — can a symbolic capability CONCLUDE about a person?

**ESTABLISHED — and, per Amendment 3, establishing nothing else.**

> *"⭐⭐ **Six systems can produce a statement about a member's situation or trajectory. One of
> them refuses to (F-06, by construction and by test). The other five have no mechanism that could
> refuse.** The one system that structurally cannot conclude is the only one carrying tests, a
> producer registry, a Sanctuary gate and a dated production witness."*
> — `…_P1-02/F_symbolic.md` §6

The five without a refusal mechanism, with their reachability as the record states it:

```text
F-14 astrology + F-28 Mayan   member turn · /api/sovereign/app/maia/list :695   CONCLUDE ⚠ prompt-gated only
F-07 I Ching interpret        member-authed non-turn route                      CONCLUDE ✅ INSTRUCTED · gate NONE FOUND
F-16 archetypal narrative     /api/astrology/narrative                          CONCLUDE ✅ · gate: auth only
F-11 tarot · F-12 runes · F-13 iching cast/search/hexagram   ⭐ NO AUTHENTICATION
```

**F-06's refusal, quoted as the contrast:**

> *"**CONCLUDE** — ⭐⭐ **structurally refused.** Header :48-49 — *'Does NOT re-interpret the
> cast…'*"* — `F_symbolic.md` F-06

⛔ **The worker's own restraint is carried forward rather than spent here** — *"⛔ I do not draw
the inference that suggests itself here. That is P1-04's to make."* ⭐⭐ **And P1-04 declined to
translate it.** Amendment 3 §3 ruled the vocabulary apart:

> *"`CONCLUDE` — what a symbolic capability can **EPISTEMICALLY PRODUCE**. `DECIDES` — its
> **POSITION IN AN IMPLEMENTED** decision / response process. **Different questions.** ⛔ `KNOW` /
> `SAY` / `CONCLUDE` must never be mechanically translated onto the participation ladder."*

⭐ **Consequence: five concluding-capable systems with no refusal mechanism is a finding on the
CONCLUDE axis and on no other.** ⛔ It establishes nothing about what any of them decides,
persists, or holds. **And the two sentences that speak to X-19 (S-15:130, S-23:245) live on exactly
this axis** — which is why they read as more governing than they are: they govern the axis where
the organism's exposure is broadest and the axis the exclusion's own alternative *does not name*.

### 4.4 · AXIS 4 — does something DECIDE an outcome?

**ESTABLISHED at exactly one named object on the inference→weighting path, and the record states
the effect and the missing located authorization in one breath.**

**`P3-C-08` · `living_field_affinities`** — `C_developmental_relational.md` §5.3, traced complete:

```text
indexAtom.ts:62            system writes affinity_score
        ↓
encounterContext.ts:110-119   ORDER BY lfa.affinity_score DESC  LIMIT 10
        ↓
formatGatheredMaterial → encounter/route.ts:107,153,227 · refine/route.ts:35
```

> *"⭐⭐ **A system-computed score selects and orders which of the member's own Keeps enter MAIA's
> prompt, with a hard `LIMIT 10`.** This is *ranking* and *what MAIA says*, from a system-inferred
> judgement."*

**Authority on that effect — `SPLIT`, and the split is the point:**

> *"⭐ SPLIT, AND THE SPLIT IS THE POINT — **GOVERNED for inspectability only**:
> `docs/canon/ECOLOGY_OF_MIRRORS.md` is cited in-route and the denominator + criterion are
> disclosed. **NONE LOCATED for the DECIDES effect**: '⛔ no gate was located governing the scoring
> or the ranking itself'; §9: '`ECOLOGY_OF_MIRRORS.md` governs *inspectability*, not *selection
> weight*.'"* — `…_P1-03/04_ladder_A_B_C.md`, row `P3-C-08`

> *"⭐⭐ **A governing source covering a DIFFERENT ASPECT of the same object is not an
> authorization for THIS one.** Under the old single hierarchy this row would have read *'governed'*
> and the gap would have vanished. ⛔ It is an X-19 input and is ⛔ **not adjudicated here.**"*
> — `…_P1-03_ADJUDICATION_04_2026-09-14.md` §3b

**It is one of only two `SPLIT` authority nodes in the whole 183-node map** (MAP3: *"SPLIT (two
halves kept apart) 1 C-08 — 1 G-18 — 2"*), and one of four rows P1-04 calls load-bearing
(`A-02 · B-09 · C-06 · C-08`).

⭐ **The scope the record attaches to it, carried verbatim and never dropped:**

> *"⚠️ **Scope stated precisely: this is the member's OWN material, self-scoped by `member_id` on
> both queries. It is NOT member-to-member.**"*

**A second DECIDES node exists and is deliberately NOT counted into this axis.**
`P3-C-06` `member_patterns` — *"⭐⭐ what a member may not see about themselves"*; `emerging` rows
withheld from the member under *"only as a code comment"*. ⛔ **Excluded from the X-19 comparison
because the judgement is practitioner-authored, not system-inferred** (`practitioner_id UUID NOT
NULL`). It bears on **access**, which the alternative names — but not on *system-inferred* trust.
Recorded as adjacent; **routed, per P1-05 OBL-22, to `P1-GOV-ACCESS-01`.**

### 4.5 · AXIS 5 — does any of it reach a MEMBER-FACING effect?

**ESTABLISHED on two paths, REFUSED on one, and UNRESOLVED on two.**

**Reaches a member-facing surface:**

```text
affinity_score DESC   → gathering/route.ts:55   ⭐ orders the MEMBER-FACING gathering
                      → encounter/refine routes → MAIA's prompt → what MAIA says
pattern_hint          → salientThemes[] (relationshipContextService.ts:146-148)
                      → list/route.ts:916 formatRelationalContextForPrompt → MAIA's prompt
relational_phase
autonomy_streak       → app/api/members/spiral-state/route.ts:28-29, returned UNSTRIPPED
                        to the member's own client
```

⭐ **The mitigations on the `pattern_hint` path are recorded with it, not against it**
(`C_developmental_relational.md` §5.2): explicit member handoff required (`list/route.ts:909-910`);
sanctuary-gated (`:908`); ambient fallback deliberately off with the in-code reason *"ambient
detection is membrane leakage if it arrives before observation"*.

**Refused from a relational/collective surface — the exclusion's own logic, enacted in code:**

> *"⭐⭐ **GOVERNANCE GATE — PRESENT, AND IT IS A REFUSAL.** `lib/circles/fieldPulseService.ts:5-10`:
> *'System-inferred member themes (`member_theme_signals`) are SUSPENDED from the … Do not
> reintroduce `member_theme_signals` here without a ratified collective …'* **A system-inferred
> developmental signal is explicitly withheld from a collective surface.** Recorded as the clearest
> in-code instance of the X-19 concern being *anticipated*; ⛔ **not adjudicated here.**"*

⚠️ **And the authority reading that must travel with it**, from the ladder row `P3-C-09`:

> *"Recorded as **NONE LOCATED**, because the register states 'the refusal is in-code, naming a
> ratification that has not occurred.'"*

**Traced negative — an inferred relational signal that does NOT reach the member path:**

> *"⭐ **NEGATIVE FINDING, TRACED:** `relationshipContextService.ts` — the module that feeds MAIA's
> prompt — **does not read `member_relational_signals`** … the system-detected signal store is
> written on the live route but **its only surfacing path found is the founder review lane**."*
> MAP3 `P3-C-05`: *"CONSIDERS REFUSED BY A TRACED NEGATIVE."*

### 4.6 · ⭐⭐ THE NEGATIVES, WHICH MATTER AS MUCH

`C_developmental_relational.md` §5.5, quoted in full on the two points the alternative names:

> *"⛔ **NO member-to-member matching, recommendation or similarity path exists** anywhere in
> `lib/` or `app/` at the subject. Searches for `matchMember` / `memberMatch` /
> `findSimilarMembers` / `recommendMember` return **zero** results. **No system-inferred judgement
> was found ranking one member against another.**"*

> *"⛔ **NO practitioner path reaches** `member_memory_atoms`, `episodic_memories`,
> `member_spiral_state`, `developmental_readings`, or `member_relational_signals`. The only
> practitioner-facing developmental surface located is `member_patterns` (C-6)."*

⭐ **And the object the alternative names by its own word — `trust` — is orphaned:**

> *"⭐ The migration header states its own intent: '*Fire-and-forget writes from oracle route, no
> UI, no analytics yet — **Feeds future symbolic affinity weighting***'. **The declared consumer
> does not exist**, and the declared future use is exactly an inference→weighting path.
> **X-19-relevant as a DECLARED INTENT, not as a live path.** … **CURRENT STATUS — ⭐ `ORPHANED`**"*

⚠️ **Vocabulary collision declared by the record and carried here:** `trust_observations` is
*"**unrelated** to `lib/trust/service.ts` (privacy envelopes, live) and to `skills_registry.trust_level`
(rollout control)."* ⛔ *"System-inferred trust"* in the exclusion is compared **only** against the
named object, never against the word.

---

## 5 · ACTUAL-SIDE STANDING

```text
HEADLINE   ESTABLISHED — on the axes the outcome rests on
CARRIED    two named objects are NOT established, and the outcome does NOT rest on either
```

| Axis / object | Standing |
|---|---|
| AXIS 1 · INFER · AXIS 2 · PERSIST | **ESTABLISHED** — six named objects, schemas and write paths quoted (§4.1–4.2) |
| AXIS 3 · CONCLUDE | **ESTABLISHED** — `F_symbolic.md` §6: six systems, one structural refusal |
| AXIS 4 · DECIDES | **ESTABLISHED at `P3-C-08`** — §5.3 traced complete; authority `SPLIT` |
| AXIS 5 · MEMBER-FACING | **ESTABLISHED on two paths · REFUSED on one** (§4.5) |
| `member_spiral_state` / R16 wiring | ⛔ **DEPENDS ON C6** (§5.1) |
| `member_theme_signals` participation | ⛔ **UNKNOWN** (§5.2) |

### 5.0 · ⭐⭐ THE STRUCTURAL FINDING — `DIVERGENT` IS NOT REACHABLE ON THIS ROW

The alternative's condition is **"WITHOUT A NEW AUTHORITY ACT."** Establishing `DIVERGENT` would
therefore require establishing the **ABSENCE of an authority act**. P1-03 Amendment 3 §1 ruled
exactly that inference out, and renamed the inventory to prevent it:

```text
⛔ NOT      EFFECT-WITHOUT-AUTHORIZATION
✅ IS       EFFECT-WITHOUT-LOCATED-AUTHORIZATION

DECIDES + governance gate NONE FOUND + no quoted authorization located
        ≠  PROVED UNAUTHORIZED
```

> *"⭐ This is the same discipline the lane applied to the two-lane receipts law and to
> `interrupted`: **a system may not store a confident negative it cannot know.** *'Unauthorized'* is
> a verdict; *'no authorization located'* is a reading."*

⭐⭐ **So on X-19 specifically, the `DIVERGENT` limb of the outcome vocabulary is structurally
unavailable to P1-06 — not because the organism is clean, but because the comparison's excluded
state contains a negative this census was ruled incapable of proving.** Amendment 3 names where it
could become provable: *"⛔ `AUTH-EXPOSURE-01` is where some of these may become PROVED
unauthorized, through actor / resource / relationship witnesses."* ⛔ That lane is not cited, awaited
or assigned work here.

⚠️ **This is recorded as a limit on the instrument, not as a finding in the organism's favour.**

### 5.1 · ⛔ DEPENDS ON C6 — the R16 guard, and the limb not chosen

> **`C-4 (§8.4) · route liveness vs guard placement` · OUTCOME `C6 UNKNOWN`**
> *"The corpus withholds the fact the adjudication needs: '⚠️ No runtime witness for either route
> was located in-repo, so neither side is confirmed under the LIVE calibration.' … Under INF-1
> (WIRED ↛ LIVE) neither side is evaluable."* — `…_P1-05/02_corpus_register.md`

The blocked fact is `X-3`, *"the single most depended-upon unknown in this corpus"* — whether
`/api/oracle/conversation` serves member traffic — on which **eight adjudications rest**
(P1-05 CLOSE §4). ⛔ **P1-06 does not get to write the missing sentence.**

**What the records establish irrespective of the limb, and therefore what this row may carry:**

- `admitPersistedStateForShaping()` (`developmentalStateAdmission.ts:61-71`) strips
  `relational_phase` / `autonomy_streak` *"unless authorization is `member-marked` or
  `in-encounter`"*, asserted by refusal test **R16**. Coverage: `LOADER 1 oracle/conversation
  :1711 → GUARDED` · `LOADER 2 encounterContext.ts:126 → UNGUARDED` · `LOADER 3
  MemberLiveContext.ts:390 → UNGUARDED`.
- Loader 2: *"the inferred-developmental class is carried past the guard but was not observed
  reaching the prompt at that renderer … available to any future consumer of `ctx.spiralState`
  without passing the admission boundary."* Of four shaping modules declaring those fields,
  *"exactly one is fed, and that one is the one behind the guard."*
- P1-05: the scope-vs-wiring tension is **`C2 RESOLVED BY ALTITUDE`**, with *"'⛔ Not characterised
  as a defect' … carried, ⛔ not overridden"*; the member-facing bypass is **`C1 RESOLVED BY
  SCOPE`** — *"**Two different boundaries, one field class**"* — residue: *"Whether one field class
  ought to cross both boundaries is a governance question with ⛔ no located source."*

⛔ **Neither limb is chosen and no comparison is completed on this object.** Its open questions are
already owed as founder rulings — **OBL-19** (*"Is a class-level rule with one call site a rule or a
site?"*), **OBL-20** (*"Is declared-but-unfed safe or latent?"*), **OBL-42** (*"Is R16's admission
class under-drawn, or is disclosure correctly outside a shaping law?"* — *"⛔ Not answerable at
census authority"*), **OBL-41** (stale persisted developmental state).

### 5.2 · ⛔ UNKNOWN — `member_theme_signals`

`P3-C-09` participation is **`UNKNOWN — RECORDS DISAGREE`**, and P1-05 adjudicated the divergence
as **`X-1 · C1 RESOLVED BY SCOPE`** — *"A prompt-assembly path and a member-facing read surface are
different predicates over one table"* — with the residue kept live:

> *"⭐ Whether domain C's search covered domain B's prompt path is ⛔ NOT established; **if it did,
> the item reopens as `C5`.** … ⛔ The rows are not merged and **P3-C-09 stays UNKNOWN**."*

⛔ **P1-06 may not choose a limb to complete a comparison** (schema §2). The located refusal in
`fieldPulseService.ts` is therefore recorded on AXIS 5 as **an in-code refusal whose authority
standing is `NONE LOCATED`**, and ⛔ **not** as evidence that the exclusion is honoured elsewhere:

> *"⭐ A located refusal withholding a capability from one surface does not establish where that
> capability sits on another."* — MAP3, node `P3-C-09`

---

## 6 · OUTCOME

```text
X-19    OUTCOME   MIXED

        EARNED BY      AXIS 1 INFER · AXIS 2 PERSIST · AXIS 4 DECIDES ·
                       AXIS 5 MEMBER-FACING · and the §4.6 traced negatives
        NOT EARNED BY  AXIS 3 CONCLUDE — ⛔ a neighbouring axis the exclusion does not govern
        NOT ABSORBING  ⛔ member_spiral_state / R16 (DEPENDS ON C6) and member_theme_signals
                       (UNKNOWN) are UNKNOWN in the ACTUAL-SIDE STANDING field, ⛔ NOT inside MIXED
```

⭐ Per schema §7c the outcome must name its axes, so they are named in the outcome block itself and
not only in the discussion. §6.3 expands each. §6.4 answers §7c's first consequence directly.

**`MIXED` is used in the schema's strict sense and no other:**

> *"MIXED — different NAMED objects or paths legitimately land differently."*
> *"⛔⛔ **`MIXED` must never be a euphemism for unresolved contradiction.**"*

⭐ **The records here do not conflict; they establish different named objects with explicitly stated
different scopes** — self-scoped versus member-to-member, prompt-assembly versus member-facing read,
live path versus orphaned, member lane versus founder lane. Each scope statement is the record's
own sentence, quoted in §4. That is what licenses `MIXED` rather than `UNKNOWN`.

### 6.1 · Where each named object lands against the alternative's five consumers

```text
MATCHING              ⭐ ALIGNED — traced zero. "No system-inferred judgement was found
                      ranking one member against another." (positive search evidence)
RECOMMENDATION        ⭐ ALIGNED — same traced zero
AFFINITY, from        ⭐ ALIGNED BY ORPHANHOOD — trust_observations has NO CALLERS; its
  trust observations     declared consumer "does not exist". ⚠️ ALIGNED because the path was
                         never built, ⛔ NOT because an authority act refused it.
AFFINITY, from        ⛔ NOT RESOLVED — P3-C-08 establishes the alternative's MECHANISM
  member atoms           (system-computed weighting that DECIDES what reaches cognition,
                         gate NONE LOCATED for the selection) while failing all three of
                         the exclusion's qualifiers: not trust-derived, not member-to-member,
                         and not silent (created_by · evidence_reason NOT NULL ·
                         denominator + criterion disclosed). See §6.2.
RELATIONAL            ⭐ ALIGNED ON THE PROMPT PATH — member_relational_signals is written
  SIGNIFICANCE           on the live route and the prompt-feeding module does not read it;
                         CONSIDERS refused by a traced negative.
                      ⚠️ Carried, not weighed: the same object has "⛔ no consent gate, no
                         member notice and no member visibility surface".
ACCESS                ⛔ ROUTED OUT — the one DECIDES node on access (P3-C-06,
                         member_patterns.emerging) is PRACTITIONER-authored, not
                         system-inferred; it is P1-GOV-ACCESS-01 / OBL-22 territory.
                      ⛔ UNRESOLVED on member_theme_signals (§5.2) and on the R16 wiring (§5.1).
```

### 6.2 · ⭐⭐ The one object where the comparison itself stops

`P3-C-08` is the row this exclusion was written to catch and the row the census cannot finish.

- The **mechanism** the alternative names is present and traced end to end.
- The **qualifiers** the exclusion states are each absent: the score is not derived from trust
  observations; the ranking is self-scoped and never ranks one member against another; and the
  object is the least silent in the entire domain.
- The **condition** — *without a new authority act* — is `NONE LOCATED`, which §5.0 establishes is
  not the same as absent.
- The **operative term** — *relational authority* — has **no governing text**, so whether
  *"selecting and ordering the member's own Keeps into MAIA's prompt"* falls inside it is an
  interpretation of the exclusion, ⛔ not a reading of the organism.

⭐ And it is already an open founder question in its own words — **OBL-21**:

> *"May a system-computed score select and order what reaches MAIA's prompt? `affinity_score DESC
> LIMIT 10` over the member's own material. **WHY** RULING — an X-19 input; the located canon
> governs inspectability, ⛔ not selection weight. **SET** a founder ruling, or a source covering
> selection weight specifically."*

⛔ **P1-06 does not answer it, and `MIXED` does not encode an answer to it.**

### 6.3 · ⭐ Which axes the outcome RESTS ON, and which it does NOT

```text
RESTS ON
  AXIS 1 INFER          six named system-inferred objects, four with no provenance column
  AXIS 2 PERSIST        all six persist; member authority NONE FOUND on the inferred set
  AXIS 4 DECIDES        exactly one node (P3-C-08), authority SPLIT
  AXIS 5 MEMBER-FACING  two paths reach a member; one collective surface refuses
  §4.6 NEGATIVES        matching and recommendation traced to zero; trust object orphaned

⛔ DOES NOT REST ON
  AXIS 3 CONCLUDE       a real and serious finding on its own axis — and Amendment 3 §3
                        forbids translating it into DECIDES · CONTRIBUTES · MEMBER-FACING ·
                        PERSISTED · AUTHORIZED. ⛔ Evidence neither for nor against X-19's
                        alternative, which names no consumer on this axis. (§6.4)
  §5.1 R16 / spiral     DEPENDS ON C6 — limb not chosen
  §5.2 theme signals    UNKNOWN — records disagree; limb not chosen
  P3-C-06 access        practitioner-authored; routed to P1-GOV-ACCESS-01
```

⭐⭐ **If AXIS 3 had been folded in, this row would have read `DIVERGENT` on the strength of five
ungoverned symbolic concluders — wrong twice over:** concluding is not deciding, and `DIVERGENT`
requires proving an absence the census cannot prove (§5.0).

### 6.4 · ⭐⭐ §7c CONSEQUENCE A — IS THIS THE AXIS THE EXCLUSION GOVERNS?

> *"⛔ A row may not become `DIVERGENT` merely because ONE axis diverges, **if the frozen exclusion
> actually governs ANOTHER axis.**"* — schema §7c

**Asked, and answered either way, as instructed.**

```text
THE AXIS X-19 GOVERNS
  the exclusion's subject is AUTHORITY — inferred trust becoming RELATIONAL AUTHORITY.
  its recorded alternative names five CONSUMERS of that authority — affinity · matching ·
  access · recommendation · relational significance — and one CONDITION — no new authority act.
  ⭐ Those consumers are read on AXIS 4 (DECIDES — a position in an implemented process)
  and on AXIS 5 (MEMBER-FACING), conditioned by AXIS 1 (is it inferred) and AXIS 2 (does it
  persist). ⭐ That is why the outcome is earned there.

NEIGHBOURING AXES, NAMED SO THEY ARE NOT SILENTLY BORROWED
  AXIS 3 CONCLUDE — an inference REACHING A PROMPT, or a symbolic system being able to
  produce a statement about a person, is an EPISTEMIC PRODUCTION. ⛔ It is not, by itself,
  any of the five consumers. Amendment 3 §3 ruled the two apart in terms.
  ⛔ So the five ungoverned concluders (§4.3) do NOT make this row DIVERGENT, however
  serious they are on their own axis — and ⛔ this row does not soften them either.
  They are already owed as founder questions in Domain F's own record.
```

⚠️ **The hardest case is `pattern_hint`, decided on this test rather than on instinct.** A
MAIA-generated inference is promoted into `salientThemes` and formatted into MAIA's prompt
(`relationshipContextService.ts:146-148` → `list/route.ts:916`), in a row with **no provenance
column** — *"a consumer reading `pattern_hint` cannot tell from the row that it is inferred."*
That is an inference reaching cognition **silently** in the precise sense.

⭐ **But reaching cognition is AXIS 3/5, not one of the exclusion's five consumers.** It weights no
affinity, matches no one, grants no access, recommends nothing, and ranks no person's relational
significance against another's. ⛔ **So it is recorded on its axes and is NOT counted toward
`DIVERGENT`** — and the fact that it would have been, under a collapsed reading, is exactly what
the schema's X-19 constraint exists to prevent.

⚠️ **Stated in the other direction, as §7c requires:** ⛔ **this ruling does not clear the path.** The
missing provenance column, the mixed-provenance write, and the absence of any located gate on the
promotion are all real, all quoted, and all live as **OBL-24**. ⭐ *A finding placed on its correct
axis has not been dismissed; it has been stopped from doing work it cannot honestly do.*

---

## 7 · BASIS — both sides, sources named

**CHOSEN side**

| Quote | Source |
|---|---|
| *"System-inferred trust does not silently become relational authority."* | `…_P1-00_CUSTODY_2026-09-14.md` §3a row 3, quoting register `ee3e4c47` L312 |
| ALTERNATIVE: inferred trust observations weighting affinity / matching / access / recommendation / relational significance **without a new authority act** | P1-00 pinned exclusion text (comparison authority, schema §1) |
| *"⚠️ X-19 and X-29 have no single governing text … eleven sources … quoted them without adjudicating."* | `…_P1-01_CLOSE_2026-09-14.md` §2 |
| *"X-19 relevance is recorded, not ruled … No adjudication is made here."* | `…_P1-01/05_spiralogic_symbolic.md` §4 |
| *"These are not display restrictions. They are formation restrictions… A developmental assessment silently held but never spoken has already violated the canon."* | S-15 `SPIRAL_CONTINUITY_ENGINE.md`:130, via slice 05 |
| *"…never as a statement about the member's reality … The member remains the verifier."* | S-23 `MAIA_SOVEREIGNTY_INVARIANTS.md` §Invariant 13:245, via slice 05 |

**ACTUAL side**

| Quote | Source |
|---|---|
| *"SYSTEM-INFERRED. PROVENANCE COLUMN: NONE FOUND."* (`member_spiral_state`) | `…_P1-02/C_developmental_relational.md` §3 · C-3 |
| *"MIXED PROVENANCE INSIDE ONE WRITE … A consumer reading `pattern_hint` cannot tell from the row that it is inferred."* | ibid. §3 · C-4 |
| *"No shared provenance convention exists. Five mechanisms, and four substrates with none."* | ibid. §4 |
| *"A system-computed score selects and orders which of the member's own Keeps enter MAIA's prompt, with a hard `LIMIT 10`."* · *"⛔ no gate was located governing the scoring or the ranking itself"* · *"governs inspectability, not selection weight"* · *"self-scoped by `member_id` … It is NOT member-to-member."* | ibid. §5.3, §3 · C-8, §9 |
| *"NO member-to-member matching, recommendation or similarity path exists … return zero results."* | ibid. §5.5 |
| *"The declared consumer does not exist … X-19-relevant as a DECLARED INTENT, not as a live path." · ORPHANED* | ibid. §3 · C-12, §5.4 |
| *"GOVERNANCE GATE — PRESENT, AND IT IS A REFUSAL … SUSPENDED … without a ratified collective"* | ibid. §3 · C-9 |
| *"member authority is … NONE FOUND on every system-inferred developmental object except the one the member can merely read."* | ibid. §6 |
| *"its sole caller … below the 410 … and the same two fields are returned verbatim to the member's own client … with no such strip. Reported as X-19 input; ⛔ not adjudicated here."* | `…_P1-02/E_spiralogic_elemental.md` Q4 |
| *"Six systems can produce a statement about a member's situation or trajectory. One … refuses … The other five have no mechanism that could refuse."* | `…_P1-02/F_symbolic.md` §6 |
| *"SPLIT, AND THE SPLIT IS THE POINT"* · `DECIDES established · HAS AUTHORITY not established · INF-6 prevents promotion` | `…_P1-03/04_ladder_A_B_C.md` row `P3-C-08` |
| *"A governing source covering a DIFFERENT ASPECT of the same object is not an authorization for THIS one."* | `…_P1-03_ADJUDICATION_04_2026-09-14.md` §3b |
| *"CONCLUDE … DECIDES … Different questions."* · `CONCLUDING-CAPABLE = yes` establishes none of five | `…_P1-03_AMENDMENT-3_2026-09-14.md` §3 |
| `EFFECT-WITHOUT-LOCATED-AUTHORIZATION` ≠ `PROVED UNAUTHORIZED` | ibid. §1 |
| `SPLIT (two halves kept apart) 1 C-08 … 2` · *"governance of a thing is not necessarily authorization of what that thing does"* | `…_P1-04/MAP3_authority.md` §tally, closing block |
| `C-4 (§8.4) · C6 UNKNOWN` · `X-1 · C1 RESOLVED BY SCOPE` · `C-E7 · C1 RESOLVED BY SCOPE` · `C-3 (§8.3) · C2 RESOLVED BY ALTITUDE` | `…_P1-05/02_corpus_register.md` |
| `OBL-19 · OBL-20 · OBL-21 · OBL-22 · OBL-26 · OBL-27 · OBL-41 · OBL-42` | `…_P1-05/04_unresolved_obligations.md` |

---

## 8 · ⭐ WHAT THIS DOES NOT ESTABLISH

⛔ **Mandatory field. This row is a comparison, not a verdict on either side.**

1. ⛔ **It does not establish that X-19 was verified.** X-19 has **no single governing text**. The
   comparison in §6 is between a frozen strategic sentence and a census of an organism. Per schema
   §1 that is **not constitutional verification**, and nothing here may later be cited as if it were.

2. ⛔ **It does not establish that any object is unauthorized.** Every `NONE LOCATED` in §4 and §6
   is *a reading, not a verdict* (Amendment 3 §1). `DIVERGENT` was structurally unreachable on this
   row (§5.0) — **that is a limit of the instrument, ⛔ never evidence of compliance.**

3. ⛔ **It does not establish that the organism is clean because matching and recommendation are
   absent.** Those are traced zeros in `lib/` and `app/` at one SHA — the absence of a **located
   path**, not a constraint that would refuse one. **No mechanism was found that would prevent a
   member-to-member consumer of `affinity_score` from being added.**

4. ⛔ **It does not establish that `trust_observations` is safe.** It is `ORPHANED`, and its
   migration header declares the excluded path as its intent. Alignment by *"the consumer was never
   built"* is not alignment by an authority act, and the row says so in §6.1.

5. ⛔ **It does not establish anything about the R16 guard's adequacy.** That object `DEPENDS ON C6`
   and both limbs are preserved unchosen (§5.1). ⛔ The absence of the evaluator does not authorize
   P1-06 to decide whether the guard's conditions are met — the constraint the schema wrote for
   X-16, applied here on its own terms.

6. ⛔ **It does not establish where `member_theme_signals` sits.** `P3-C-09` is `UNKNOWN — RECORDS
   DISAGREE` and stays so (§5.2). ⛔ The located in-code refusal is not read as a governing gate, and
   ⛔ not read across to any other object.

7. ⛔ **It does not translate `CONCLUDE` into authority.** Five concluding-capable symbolic systems
   with no refusal mechanism is a finding on **one axis**. ⛔ Nothing here says they decide, persist,
   reach a member, or hold relational authority — and ⛔ nothing here says they do not.

8. ⛔ **It does not decide `P3-C-08`.** `OBL-21` remains a founder ruling. `MIXED` encodes **no**
   answer to *"may a system-computed score select and order what reaches MAIA's prompt?"*

9. ⛔ **It does not adjudicate practitioner access.** `P3-C-06`'s withheld `emerging` judgement is
   named and routed to `P1-GOV-ACCESS-01` (`OBL-22`, `OBL-26`), ⛔ not folded into X-19 because the
   authorship is practitioner, not system.

10. ⛔ **It does not import, await or anticipate `AUTH-EXPOSURE-01`.** That lane is where some of
    these may become *proved* unauthorized. ⭐ **Keeping the name honest is what stops the two lanes
    contaminating one another.**

11. ⛔ **It establishes nothing about the strategic choice's correctness.** X-19 may be exactly
    right and the organism may be exactly right; the organism's divergences may be the choice's
    vindication or its refutation. **This row records the relationship and stops.**

---

```text
X-19   COMPARISON RECORDED

CHOSEN                 pinned, verbatim
GOVERNING TEXT         ⚠️ NO SINGLE GOVERNING TEXT LOCATED — eleven sources, quoted, unadjudicated
ACTUAL                 five axes, ⛔ never collapsed
ACTUAL-SIDE STANDING   ESTABLISHED on the axes the outcome rests on ·
                       ⛔ DEPENDS ON C6 (R16 / member_spiral_state) · ⛔ UNKNOWN (member_theme_signals)
OUTCOME                MIXED — different named objects, different stated scopes
  EARNED BY            AXIS 1 INFER · AXIS 2 PERSIST · AXIS 4 DECIDES · AXIS 5 MEMBER-FACING
  ⛔ NOT EARNED BY      AXIS 3 CONCLUDE — the exclusion governs a different axis (§6.4)
  ⛔ NOT ABSORBING      the C6 and UNKNOWN objects — they are UNKNOWN in ACTUAL-SIDE STANDING
                       ⛔ NO OBJECT ESTABLISHED AS DIVERGENT · ⛔ DIVERGENT STRUCTURALLY UNREACHABLE

⛔ NO SOURCE CODE READ · ⛔ NO RUNTIME EVIDENCE · ⛔ NO C5 OR C6 RE-ADJUDICATED
⛔ NO LIMB CHOSEN · ⛔ NO EXCLUSION REPAIRED · ⛔ NO GOVERNANCE CREATED WHERE P1 FOUND NONE
⛔ NO PERCENTAGE · ⛔ NO SCORE · ⛔ NO AGGREGATE
REPOSITORY SOURCE UNCHANGED · PRODUCTION UNTOUCHED
```
