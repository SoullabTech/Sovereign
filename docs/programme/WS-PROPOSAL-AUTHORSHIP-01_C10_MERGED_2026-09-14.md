# C10 · MERGED — PR #1296 closed, lane outcome

```text
MERGED AT   2026-09-14T23:01:52Z        MERGED BY  Soullab (repository owner)
MERGE       7c136532c  "Merge pull request #1296 …"  · 2 parents
BASE TIP    claude/proposal-authorization-integration → 7c136532c
```

⚠️ **The merge was performed by the repository owner, not by this session.** The lane's standing
said `MERGE ⛔ NOT AUTHORIZED` — ⭐ that was a constraint on **Jarvis**, never a prohibition on the
founder, who owns both the repository and the ruling. ⛔ Recorded so the record is not later read as
a breach.

## Verified on the base after the merge

```text
181fbaa2c ancestor of the base tip    ✅ YES   integration candidate landed
4df4e91d  ancestor of the base tip    ✅ YES   ⭐ the sealed repair is IN THE HISTORY
sealed implementation digest          ✅ 2e8e3e426d08566f…   UNCHANGED
```

```text
⭐⭐ BOTH SEALED OBJECTS SURVIVED INTEGRATION INTO CANONICAL HISTORY.
   4df4e91d is still citable, still byte-identical, still the object its evidence describes.
```

⭐ That was the whole point of refusing the rebase: a year from now, every claim in the evidence
chain can still be checked against the exact bytes it was made about.

## The lane, closed

```text
ORIGINATING COMPLAINT   "Show Change doesn't open"
FOUND                   the control was NOT broken — it was MISLABELED (F1), and behind it the
                        capability it implied DID NOT EXIST: Whole asked for the section shell
                        and had no exact-locus return at all
SHIPPED                 the capability, with a member-visible refusal when it cannot be honoured
HELD                    the rename — and its premise has since moved anyway
```

```text
4df4e91d    🔒 sealed repair       RED absence · MUTANT discrimination · CLOSING confirmation
181fbaa2c   🔒 sealed integration  static gates · bounded 2/2 runtime witness
7c136532c      merged to canonical
```

## ⚠️ Carried forward — unchanged by the merge

```text
CI-GATE DEFECT   ⛔ OPEN · its own lane
  canonical-pr-quality (the only un-swallowed `npm run typecheck`) is branch-filtered to
  clean-main-no-secrets, so it did NOT run on #1296. Six checks went green without it —
  ⭐ the PR #1150 shape, reproduced. The typecheck evidence for this merge is LOCAL, and
  only local.

F1 CONTROL LABEL ⛔ HELD on a MOVED PREMISE
  the base already renamed the control to "Show me where"; F1's subject is now a choice
  between two location labels, not the repair of a disclosure lie.

BASE DEBT        ⚠️ 4 inherited failures (proposalWorkMode · consentSurface), verified red on
                 the base alone, deliberately not repaired here.
```

## ⭐⭐ LANE CLOSED (founder, 2026-09-14)

```text
WS-PROPOSAL-AUTHORSHIP-01 / C10

STATUS        ✅ CLOSED
REPAIR        4df4e91d   🔒 preserved in history
INTEGRATION   181fbaa2c  🔒 preserved in history
MERGED AS     7c136532c
WATCH         ✅ ended
EVIDENCE      ✅ intact and still reproducible
```

⭐ **The closing condition was met, not declared:** #1296 merged, both sealed objects remain
reachable, and the evidence chain survived integration intact.

### ⛔ Nothing remaining belongs inside C10 — carried forward as SUBJECTS, not loose ends

```text
NEXT LANE     CI-GATE-BASE-COVERAGE-01
SUBJECT       six green PR checks can coexist with NO canonical typecheck
EVIDENCE      #1296 demonstrated the defect IN PRACTICE
BOUNDARY      workflow / gate architecture ONLY — ⛔ no C10 reopening

SEPARATE      F1 · the control-label premise has MOVED ("Show me where")
SEPARATE DEBT four inherited base-red tests
```

⭐ `CI-GATE-BASE-COVERAGE-01` is **no longer a census hypothesis.** #1296 supplied the
discriminating example the gate's own historical warning describes — **green UI, absent
typecheck.**

---

## ⭐⭐ THE CLOSURE LESSON

```text
A SEALED OBJECT SURVIVES INTEGRATION WHEN HISTORY IS ADDED AROUND IT
RATHER THAN REWRITTEN THROUGH IT.
```

⭐ Every custody refusal in this lane — no rebase, no amend, no force-push, merge commit over
rewrite, fast-forward over reconstruction — was in service of that one sentence. ⛔ And it is
checkable, not rhetorical: `4df4e91d` is reachable from canonical and its digest still reads
`2e8e3e42…`.

---

## Standing at close

```text
C10                                ✅ CLOSED
Gate B (authorship / succession)   ⛔ CLOSED / NOT OPENED — requirement precedes object (FR-W3a)
Gate C (schema authorization)      ⛔ BLOCKED BY F6′ — ledger reconciliation still owed
CI-GATE-BASE-COVERAGE-01           ⭐ NEXT LANE — not opened here
F1 control label                   ⛔ separate, premise moved
base debt (4 tests)                ⛔ separate, named not absorbed
```

> ⭐⭐ **A voluntary return is not complete because Whole reached the right section. It completes
> only when that request's exact locus is actually addressed — or the issuer tells the member
> plainly that it could not be.**
>
> ⭐ That is now true of canonical, with the evidence to show it — and with both sealed objects
> still readable in the history that carries it.
