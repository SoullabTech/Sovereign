# JARVIS-GKF-J8-DEEP-SUBSTRATE-01 — DEEP Runtime Restoration + Canonical Re-Witness

**Date opened:** 2026-09-17
**Parent:** `JARVIS-GOVERNED-KNOWLEDGE-FLOW-01`
**Trigger evidence:** `JARVIS-GOVERNED-KNOWLEDGE-FLOW-01_J8_PRODUCTION_WITNESS_2026-09-17.md`
**Production repair:** `145d2c13cc787187af101a5c19081592616ee4d3`
**Witness record:** `b821c2936fc328caedd7ad0e467c130e98949830`
**Class:** production runtime / model-substrate conformance
**Status:** OPEN — J0 charter complete; J1 runtime-truth census next

---

## 1. Why this run exists

The governed-knowledge repair is live in production and succeeds through the actual authenticated
`/api/sovereign/app/maia/list` route on FAST and CORE.

DEEP also retrieves and canonically admits the governed Elemental Alchemy source, but its downstream
consciousness stage fails to produce a substantive answer.

The witnessed DEEP turn returned only:

> “I'm here with you.”

This run exists to repair that DEEP runtime failure without reopening the governed corpus or the
FAST / CORE retrieval work that already passed.

## 2. Frozen production facts

The following facts are inputs to this run, not hypotheses to be re-litigated:

- production container reports `GIT_COMMIT=145d2c13c`;
- FAST canonical member witness: PASS;
- CORE canonical member witness: PASS;
- DEEP retrieval: 3 governed Elemental Alchemy hits, max similarity `0.822`;
- `retrieved.governed_knowledge`: ADMITTED, 6,000 chars;
- canonical shadow: `zeroDiff=true`;
- production corpus after witness: 1,238 rows, one source, indexes 0–1237, 768 dimensions;
- DEEP cognition: STOP.

The DEEP runtime logged unavailable local model references including:

- `deepseek-r1:latest`;
- `llama3.1:8b-instruct-q8_0`;
- `mistral:7b-instruct-q8_0`;
- `nous-hermes2-mixtral:8x7b`;
- `llama3.1:70b-instruct-q4_k_m`.

It then logged `consciousness-stage-timeout` and fell through to a generic fallback.

These error names do **not** establish that all five models should be installed.

## 3. Governing question

> What is the smallest truthful repair that restores substantive DEEP cognition while preserving
> the already-witnessed governed retrieval and production authority boundaries?

The run must distinguish:

1. canonical model dependency;
2. optional orchestration candidate;
3. stale historical model reference;
4. legitimate fallback;
5. unavailable / unsupported configuration.

A missing model name alone is not authority to download, install, or make it canonical.

---

## 4. Hard containment

This run does **not** authorize:

- mutation of the 1,238 governed corpus rows or embeddings;
- changes to corpus admission, source identity, source revision, or authority records;
- changes to `RetrievalService`, `GovernedRetrievalService`, or similarity threshold `0.55`;
- changes to FAST or CORE cognition;
- changes to `retrieved.governed_knowledge` producer authority;
- a new knowledge crossing;
- training or fine-tuning a model;
- installing every model named by a failed orchestration attempt;
- deployment before the repair candidate earns its repository and host-side gates;
- J9 or J10 progression while this J8 DEEP sub-gate remains unresolved.

## 5. Success contract

The lane closes only when one production DEEP member turn proves all of the following together:

1. HTTP 200 from canonical `/api/sovereign/app/maia/list`;
2. authenticated member identity;
3. actual selected processing profile = `DEEP`;
4. governed retrieval returns only authorized Elemental Alchemy material;
5. `retrieved.governed_knowledge` is ADMITTED in the canonical manifest;
6. canonical shadow remains `zeroDiff=true`;
7. the DEEP cognition engine actually consumes the governed knowledge seam;
8. the response is substantive rather than a generic availability fallback;
9. corpus state remains exactly 1,238 rows / one source / 768 dimensions;
10. no unauthorized model or provider path is introduced.

A response that is merely fluent is not enough. The cognition path itself must be evidenced.

---

## 6. J0–J10 execution map

### J0 — Charter + boundary freeze — COMPLETE

Freeze the problem, parent evidence, production SHA, non-goals, and success contract.

**Exit:** this charter exists and no repair has been taken inside J0.

### J1 — Production runtime truth census

Read-only census on minisforum and the deployed image.

Establish:

- exact Ollama endpoint used by DEEP;
- `ollama list` / available local model inventory;
- all source-code model names DEEP may request;
- environment/config values controlling those choices;
- ordering and fallback semantics;
- which calls produced each witnessed 404;
- host RAM, disk, CPU/GPU availability relevant to candidate models;
- whether any required model is already present under a different tag.

**STOP if:** production truth cannot be tied to exact code/config or a second uncontrolled model server exists.

### J2 — Model authority adjudication

Classify every DEEP model reference as:

- REQUIRED;
- OPTIONAL;
- RETIRED / STALE;
- FALLBACK;
- UNKNOWN.

No model download occurs in J2.

For each REQUIRED candidate, record exact model/tag, role, runtime, resource fit, license/source,
and the code/config authority that requires it.

**STOP if:** the repair would be “install what the error printed” rather than an adjudicated dependency.

### J3 — Repair contract

Choose the smallest repair shape based on J1/J2 evidence.

Allowed shapes include:

- correct a stale model tag;
- retire an obsolete engine from the orchestration set;
- bind an already-approved installed model to the existing role;
- install one or more adjudicated REQUIRED models;
- repair timeout / selection behavior if availability is already correct.

The contract must state exact files, configuration, host actions, hashes/tags, and rollback.

**No implementation before J3 closes.**

### J4 — Resource + safety preflight

Before any model pull or runtime change prove:

- disk floor after download estimate;
- memory/VRAM fit;
- model source and digest where available;
- Ollama reachability;
- production app remains healthy;
- rollback path exists;
- no concurrent deploy/model operation is active.

**STOP before resource expansion if any floor is unmet.**

### J5 — Contained substrate repair

Execute only the J3-authorized substrate change.

Requirements:

- no corpus write;
- no database migration unless separately adjudicated;
- no FAST/CORE change;
- exact host/model actions recorded;
- any model pull is limited to adjudicated REQUIRED artifacts;
- resulting Ollama inventory is captured.

If code/config changes are necessary, use a dedicated repair branch and bind exact executable hashes.

### J6 — Host-side DEEP engine witness

Before production route deployment/re-witness, prove the repaired DEEP engine locally/host-side:

- exact governed knowledge addendum can enter the DEEP native context;
- required engine calls resolve rather than 404;
- no `consciousness-stage-timeout`;
- substantive synthesis is produced;
- no fallback-only response;
- no corpus/database write caused by the witness.

**STOP if the engine is merely available but still does not consume the governed knowledge field.**

### J7 — Repository / configuration conformance

If J5 changed source or configuration:

- focused tests;
- TypeScript no-regression comparison;
- provider-governance gates;
- exact DEEP wiring tests;
- provenance / diff check;
- independent review where required.

If J5 was host-model-only, J7 instead proves the deployed executable remains byte-identical to
`145d2c13c` and only the adjudicated runtime substrate changed.

### J8 — Canonical DEEP production re-witness

Run exactly the previously failed class of turn through authenticated production
`/api/sovereign/app/maia/list`.

Required evidence:

- profile = DEEP;
- governed hits > 0 and only authorized source;
- manifest admits `retrieved.governed_knowledge`;
- shadow `zeroDiff=true`;
- native DEEP stage reports successful processing;
- substantive response materially reflects the governed source;
- no engine 404 / stage timeout / generic fallback.

### J9 — Independent post-witness truth

Independently verify after the DEEP turn:

- production SHA / runtime configuration;
- model inventory and exact selected model path;
- corpus remains 1,238 / one source / 768d;
- no unauthorized `ain_knowledge_chunks` writer ran;
- no retrieval-authority drift;
- no FAST/CORE regression signal introduced by the repair.

J9 is evidence collection, not repair.

### J10 — Parent J8 closure adjudication

Only after J8 + J9 pass may the parent programme change from:

`J8: FAST PASS / CORE PASS / DEEP STOP`

to:

`J8: PASS`

J10 of the parent governed-knowledge programme remains separately blocked by its Law 7 crossing debts.
Closing this DEEP run does **not** close the parent programme's J10.

---

## 7. Evidence law

Every claim must name whether it is:

- repository truth;
- production runtime truth;
- model-server truth;
- observed member-turn behavior;
- inference.

Model availability is not model authority.
Model authority is not successful execution.
Successful execution is not proof of governed-knowledge consumption.

## 8. Falsifiers

This run fails if any of the following occurs:

- DEEP still falls back to “I'm here with you” or equivalent generic presence;
- governed retrieval disappears while the response becomes fluent;
- the repair requires widening corpus authority;
- an unadjudicated model is installed or made authoritative;
- model selection silently shifts to an external provider;
- FAST/CORE behavior changes to make DEEP pass;
- canonical manifest no longer admits `retrieved.governed_knowledge`;
- shadow parity becomes non-zero without a separately adjudicated reason;
- corpus count/source/dimensions drift;
- the runtime cannot say which model actually produced the substantive DEEP answer.

---

## 9. Current standing

```text
J0  ✅ COMPLETE — charter + boundary freeze
J1  ▶ NEXT     — production runtime truth census
J2  ·
J3  ·
J4  ·
J5  ·
J6  ·
J7  ·
J8  ·
J9  ·
J10 ·
```

### Next exact act

> Run J1 read-only on minisforum: enumerate the actual DEEP model-selection graph, current Ollama
> inventory, controlling environment/config, and hardware/resource truth. Do not download, install,
> retag, remove, or restart any model or service during J1.
