# CMT-01 — M0–M2 WITNESS

**Lane**: CMT-01 Canonical MAIA Turn · **Authorization**: §14-A of `MAIA_CANONICAL_TURN_ARCHITECTURE_SPEC_v0.1.md` (M0–M2 only)
**Branch**: `claude/canonical-maia-turn-j92opb` · **Base**: `a4305f4` · **Date**: 2026-09-03
**Environment**: remote container, fresh `npm ci` (2445 packages), TypeScript 5.9.3, jest 29 / ts-jest. **No production access. No deployment.** Everything below is executed against the working tree.

Per the constitutional method: hostile-positive mutations applied and observed to fail; innocent-negative controls observed to pass; tests actually executed; outputs recorded verbatim (ANSI stripped).

---

## What landed

| Step | Artifact | State |
|---|---|---|
| M0 | `tests/constitutional/refusal-registry/refusal-25..30-canonical-*.ts` + `cmt-01-gates.ts` runner + `fixtures/cmt-01-pp-1-admission.json` | written; **R25/R26 witnessed RED**, R27–R30 GREEN |
| M1 | `lib/maia/canonical-turn/{types,producerRegistry,policy,adjudicate,identity,floor,manifest,render,construct,shadow,index}.ts` | 38 producers on the amended three-field axis; pure MIPA; content-free manifest; one renderer; one identity resolver; **zero live callers of the renderer** |
| M1 | `lib/sovereign/maiaVoice.ts` | behavior-identical refactor: `PLATFORM_KNOWLEDGE_BOUNDARY`, `INTERFACE_HUMILITY_GUARDRAIL` exported; the inline speech-act string named `MEMORY_SPEECH_ACT_BOUNDARY` (same text) so G1 is checkable structurally |
| M2 | `app/api/sovereign/app/maia/list/route.ts` | shadow block after `buildMaiaRuntimeContext`: canonical identity → 13 route-supplied addenda lifted into typed candidates → MIPA → floor → `[MAIA/manifest]` → `[MAIA/shadow]` diff. Wrapped in try/catch; **legacy assembly remains response-producing**; `meta`, prompt, response, writes untouched |
| Docs | spec §5.2-A + §14-A; `REFUSAL_REGISTRY.md` CMT-01 rows; `CLAUDE.md` LATEST | — |

Decision 2 applied before M1 as ruled: `authoredBy` + `participationClass` + `authority` on every registry entry; R27 fails on any slash-compound value; three mixed producers marked `partitionPending` (owed at M3).

---

## M0 — expected-red falsifiers (run: `npx tsx tests/constitutional/refusal-registry/cmt-01-gates.ts`)

```text
── [R25] No cognition tier renders a MAIA prompt without the standing sovereignty floor
   ✅ PASS  MEMORY_SPEECH_ACT_BOUNDARY is a named export in maiaVoice.ts
   ✅ PASS  PLATFORM_KNOWLEDGE_BOUNDARY is a named export in maiaVoice.ts
   ✅ PASS  INTERFACE_HUMILITY_GUARDRAIL is a named export in maiaVoice.ts
   ✅ PASS  appendAllContextAddenda appends all three guardrails
   ❌ FAIL  FAST prompt path does NOT carry the floor  → fastPathResponse neither calls the renderer/addenda channel nor references the guardrails (census D1)
   ✅ PASS  CORE prompt path carries the floor  (appendAllContextAddenda channel)
   ✅ PASS  DEEP-repair prompt path carries the floor  (appendAllContextAddenda channel)
── [R26] Cognition cannot be handed an open, untyped context channel
   ✅ PASS  CanonicalTurn type has no open channel
   ❌ FAIL  (meta as any) reads in maiaService: 204  → open channel still read by cognition (baseline 200 at M0)
   ❌ FAIL  MaiaRequest.meta is Record<string, unknown>  → cognition accepts an open bag
── [R27] … producer-set closure            6/6 PASS  (38 producers; three-field axis; no slash classes; no `as any`)
── [R28] … identity provenance              5/5 PASS
── [R29] … manifest completeness            4/4 PASS
── [R30] … no expansion beyond the seed     4/4 PASS  (pp-1; no overrides; 38 producers ⊆ seed)

Summary
  R25  RED    expected RED until M3
  R26  RED    expected RED until M3
  R27  GREEN  expected GREEN
  R28  GREEN  expected GREEN
  R29  GREEN  expected GREEN
  R30  GREEN  expected GREEN
  26 passed · 3 failed · 0 warned
runner exit: 0
```

**D1 is now witnessed by a test, not a comment.** `(meta as any)` baseline: 204 occurrences on 200 lines. The runner exits 0 because red is the *expected* state and is asserted as such; an unexpected red (or an unexpected green on R25/R26 before M3) exits 1.

Innocent-negative encountered and fixed during M0: R26/R28 initially matched their forbidden patterns inside **doc comments** (`Record<string, unknown>`, `x-member-id` in headers). Both checks now strip comments before matching — a structural check must not fail on prose.

---

## M1 + M2 — runtime falsifiers (run: `npx jest lib/maia/canonical-turn`)

```text
  G4 — identity provenance
    ✓ refuses an identity that was not minted by resolveCanonicalIdentity
    ✓ mints verified / anonymous identities from the one resolver only
  G2 — producer-set closure
    ✓ refuses the whole turn on an unregistered producer
    ✓ refuses an unknown input key (the open channel trying to return)
  MIPA — eligibility and restraint
    ✓ excludes member-about producers for an anonymous identity
    ✓ holds member-about producers under sanctuary even when a block was supplied
    ✓ records eligible-but-absent producers as held:no_material (not silently absent)
    ✓ pp-1: between does not admit atoms; sovereign_chat does (no levelling-up)
    ✓ a member gesture does not rewrite authorship (Decision 2)
  G6 — manifest completeness, content-free
    ✓ has one row per admitted participant, digests only, fieldDigest reproducible
  G7 — surface parity
    ✓ web / ios / desktop produce identical participation and fieldDigest
  G1 (renderer) — floor invariance across tiers
    ✓ FAST prompt opens with the runtime prompt and closes with the three guardrails
    ✓ CORE prompt opens with the runtime prompt and closes with the three guardrails
    ✓ DEEP prompt opens with the runtime prompt and closes with the three guardrails
    ✓ the turn is deeply frozen — a tier cannot extend it
  M2 acceptance — paired legacy/canonical structural zero-diff (G8 shadow)
    ✓ the canonical object faithfully shadows the legacy addenda: zero diff            (9 legacy = 9 canonical)
    ✓ HOSTILE: legacy loses a provider → comparison is non-zero (missingInLegacy = [member.atoms])
    ✓ HOSTILE: canonical loses a provider → comparison is non-zero (missingInCanonical = [retrieved.member_web])
    ✓ HOSTILE: a provider changes text → digestMismatch = [house.place]
    ✓ INNOCENT: a floor/tier producer is outside the shadow domain and does not perturb the diff

Test Suites: 1 passed, 1 total
Tests:       20 passed, 20 total
```

**M2 acceptance, fixture level — met**: paired legacy/canonical structural zero-diff, and the hostile mutation in *both* directions (legacy loses a provider; canonical loses a provider) turns the comparison non-zero, plus a text-mutation digest mismatch and an innocent control.

**M2 acceptance, live level — NOT yet witnessed.** The `[MAIA/shadow]` line is emitted per `/list` turn in production only after a deploy, which is not authorized. The live witness is: a bounded window of production `[MAIA/shadow]` lines showing `zeroDiff:true` under real member traffic (ops diagnostic below). That evidence is what M3 adjudication should ask for.

---

## Surrounding gates

| Gate | Result |
|---|---|
| Scoped typecheck — `lib/maia/canonical-turn/**` + tests + R25–R30 + harness (TS 5.9.3, extends repo tsconfig) | **exit 0** |
| `lib/sovereign/__tests__/platformKnowledge*` (the `maiaVoice.ts` seam I exported from) | 2 suites, **70/70 pass** |
| `scripts/ci/maia-route-guard.test.ts` | **6/6 pass** (`/list` still calls `getMaiaResponse(`; registry unchanged) |
| `__tests__/voice-non-degradation.test.ts` (deep-intelligence gate; not touched, run as regression) | **15/15 pass** |
| `npm run check:no-supabase` | ✅ No Supabase detected |
| Main refusal suite `index.ts` | `101 passed · 5 failed` — **identical on the untouched base `a4305f4`** (verified in a detached worktree): R21-family sanctuary-store checks (`refusal@NaN` / `guard@NaN` — the check's line-anchoring, not this diff). Pre-existing; not this lane's. |
| `npm run typecheck` (no-regression gate, `tsconfig.ship.json`) | see appendix — three runs recorded |

**Coverage**: `tsconfig.ship.json` excludes `lib/maia/**` in general; the parallel `pdc-1` commit (`9ba2d93`, merged below) adds `lib/maia/canonical-turn/**/*.ts` to the ship include list, so from this merge the module is inside `npm run typecheck`, not only the scoped `tsc`.

---

## Ops diagnostic (for the live M2 witness, after a separately-authorized deploy)

```bash
# Per-turn structural diff — the M2 acceptance signal. Expect zeroDiff:true on every line.
ssh soullab@minisforum 'docker logs maia-sovereign --since 24h 2>&1 | grep "\[MAIA/shadow\]"'

# Per-turn participation manifest — content-free; admitted/held/excluded with reasons.
ssh soullab@minisforum 'docker logs maia-sovereign --since 24h 2>&1 | grep "\[MAIA/manifest\]"'

# Any construction failure (must be zero; legacy turn unaffected either way).
ssh soullab@minisforum 'docker logs maia-sovereign --since 24h 2>&1 | grep "\[MAIA/shadow\] canonical construction failed"'
```

`MAIA_CANONICAL_SHADOW=0` disables the instrument only. It gates no floor and no gate.

---

## What this witness authorizes and does not authorize

**Authorizes**: presenting M0–M2 as landed on this branch with the falsifiers executed; asking for the live shadow window; scheduling M3 adjudication.

**Does not authorize**: M3. Does not claim any member-facing behavior changed — none did: the response path is byte-identical to the legacy path (the only runtime additions are one extra `auth_sessions` select per `/list` turn and two log lines). Does not claim D1 is closed — R25 is RED by design until the renderer becomes the tier seam. Does not claim the seed classification is correct beyond what §14-A adjudicated. Does not claim P3-global.

---

## Appendix — full typecheck gate

First run — **gate FAILED, environmentally**: 46 NEW diagnostics, all `TS2305: Module '"@prisma/client"' has no exported member …` (`lib/types/database.ts`, `lib/db/prisma.ts`, `lib/services/consciousnessSessionTracker.ts`, `app/api/consciousness-computing/feedback/route.ts`). Cause: this container's `npm ci --ignore-scripts` skipped `postinstall: prisma generate`, so `node_modules/.prisma/client` does not exist. **None of the 46 is in a file this lane touched** (`grep canonical-turn|maiaVoice|sovereign/app/maia/list|refusal-` → none). The same run reports *"8 error(s) fixed since the baseline"* and "167 new files entered the program" (also environmental — generated/optional inputs). Not absorbed into the baseline. A second run after `prisma generate` is recorded below when available.

Second run — after `npx prisma generate` (exit 0), same pre-merge tree: **`typecheck exit: 0`** — *"8 error(s) fixed since the baseline (6 identities gone, 0 reduced)"*, 167 new files entered the program, **no NEW diagnostics**. The 46 were the missing generated client and nothing else.

Third run — merged tree, `lib/maia/canonical-turn/**` inside the ship include list (module + both test suites + `participationDisposition.ts` now gate-checked): **`typecheck exit: 0`**, no NEW diagnostics, zero diagnostics under `canonical-turn`, *"8 error(s) fixed since the baseline"*, 168 files entered the program. Gate green on the tree as committed.

---

## Merge with the parallel `pdc-1` commit (`9ba2d93`, session `01GVBKyuhH3PAkYaSEaKPbEg`)

While M0–M2 were being built here, a founder-authorized parallel session landed the
**Participation Disposition Contract** (`lib/maia/canonical-turn/participationDisposition.ts`,
`CMT-01_PARTICIPATION_DISPOSITION_CONTRACT.md`, spec §5.2/§6.1/§6.3/§7.2/§14.2 amended in place,
`tsconfig.ship.json` include) on a base that predates this module. Merged with `--no-ff`
(no rebase of the other session's history). One textual conflict (the spec status line) —
resolved by keeping **APPROVED (§14-A)** plus the remote's *Amended* paragraph; my §5.2-A was
trimmed to a binding note because the remote's §5.2 rewrite is now the ratified text.

**Integration, not coexistence** — the contract's own rule: *M1's constructor imports it rather
than redeclaring it.* Changes made so there is one vocabulary:

| Before (this lane, M1) | After (bound to `pdc-1`) |
|---|---|
| `AuthoredBy` / `ParticipationClass` / `Authority` redeclared in `types.ts` | imported from `participationDisposition.ts`, re-exported (identical values — the two sessions had converged on the same three axes independently) |
| `Participation { admitted, held, excluded }` | `{ admitted, offered, held, excluded }` — `OFFERED` representable from M1, **empty under pp-1** (no doorway producer exists; the first `OFFERED` row is W1's) |
| `Participant` carried no basis | `disposition: 'ADMITTED' \| 'OFFERED'` + `reason` (`member_placed` for placed material, `eligible` otherwise; `member_invoked` reserved for W1) |
| held/excluded rows: `{ producerId, reason }` | full contract entries: axes + disposition + reason |
| manifest `admitted` rows: axes + chars + digest | `AdmittedRow` (pdc-1 `AdmittedEntry` narrowed to `ProducerId`); **floor rows included** as `ADMITTED / mandatory_floor` so the floor is provable per turn; `fieldDigest` stays over the non-floor field (G7), `floorDigest` separate (G1) |
| no row validation | `assertTurnDispositioned([...held, ...offered, ...admitted, ...excluded])` in `buildManifest` — nothing `AVAILABLE` survives a completed turn; unknown keys refused |
| `HeldReason` had `no_material` | `no_material` **added to the `pdc-1` HELD seed** (the contract names its v1 families as a seed MIPA implementation grows; this is that growth, at M1) |

Re-run after integration: jest **30/30** (20 mine + 10 pdc-1 pins); cmt-01 gates unchanged
(R25/R26 RED expected, R27–R30 GREEN; R29 now also asserts the `assertTurnDispositioned` call).

---

## M0–M2 ACCEPTED — M2 shadow deployment authorized (2026-09-03)

Founder ruling: deploy exactly `2fafaa4` to collect the live M2 witness; M3 stays unauthorized; branch frozen to a single writer until the witness completes.

**Pre-deploy condition (the only check)** — *the shadow path is observational* — verified and pinned as **R31**, 6/6 PASS on this tree (see runbook §0). No repair was needed; none was made.

**Deploy execution**: not possible from this session — the remote container has no DNS route to `minisforum` and no `ssh` binary. The runbook (`CMT-01_M2_SHADOW_DEPLOY_RUNBOOK_2026-09-03.md`) carries the exact SHA-pinned commands for the Mac Studio, the verify block, the stop rule, and the collector (`scripts/witness/cmt-01-shadow-witness.ts`, self-tested: synthetic zero-diff set → ACCEPTED/exit 0; a single hostile `zeroDiff:false` line → STOP/exit 1).

**Duplicate M0/M1 ref**: none exists in this environment — `7ae4d4b` is a merge parent in the pushed history, not a separate ref. Nothing to discard here.

---

## INCIDENT — first M2 shadow deploy of `2fafaa4`: launch provenance lost (2026-09-03)

**STOP invoked at step 2 (verify).** The deploy gate ran from the Mac Studio, built the immutable
snapshot of `2fafaa4c4`, passed disk + Co-Lab (33/33) gates, swapped the container — and the
running container reported `GIT_COMMIT=unknown`. No shadow evidence from that container is
accepted. Preserved verbatim (classifier run by Kelly on minisforum):

```text
=== CHECKOUT ===
HEAD:   5370b42a3e94cc904d7ef0d7b967a79e6a036866      (detached; Aug 25 merge)
=== CHECKOUT COMPOSE PROVENANCE LINES ===
28:    GIT_COMMIT: ${GIT_COMMIT:-unknown}              (x-maia-build args — correct)
115:      GIT_COMMIT: ${GIT_COMMIT:-unknown}           (maia environment: — THE OVERRIDE)
=== .env.production PROVENANCE ===
none
=== RUNNING CONTAINER ===
CID=789108e95a84
GIT_COMMIT=unknown
APP_VERSION=1.0.0
DEPLOY_LANE=deploy-lane
=== RUNNING CONTAINER IMAGE ID ===
image=sha256:6c28d6de3baa9d4007cc53d4b108f29d017a82f4db4e649ea408b60eb5770572 created=2026-09-03T14:12:06.379942423Z
--- image :current ---
GIT_COMMIT=2fafaa4c4
APP_VERSION=1.0.0
DEPLOY_LANE=deploy-lane
--- image :previous ---
GIT_COMMIT=3e31bc0ff    (image metadata only — NOT proof of the prior container's env)
```

**Classification.** The build stamp survived image creation (`image Config.Env GIT_COMMIT=2fafaa4c4`)
and was overwritten during container construction (`container Config.Env GIT_COMMIT=unknown`).
Deploy chain between the checkout that ran (`5370b42a3`) and the deployed SHA is byte-identical
(`git diff --stat` empty), so the scripts read here are what executed. The mechanism of harm is
compose line 115, a runtime `environment:` override interpolated at `up`; whether the gate's own
`up` or a concurrent stack-wide `up` (other services were recreated at 13:49:49) constructed the
container is **not established** — the deploy tail after `Swapping maia container` was not
captured, and the (a)/(b) container-label classifier was not run before the repair was authorized.
Recorded as unknown, not reconstructed. **A deploy-chain defect, not a CMT defect.**

**Ruling (Kelly).** Deploy-chain repair authorized; STOP stays in force for the witness; the
`unknown` container is incident evidence, not to be altered; the witness runtime becomes a new
SHA `R` whose history contains `2fafaa4`; CMT behaviour must be unchanged between them; M3 stays
unauthorized.

**Repair — one bounded commit (`R`), no `lib/` `app/` `components/` change.** Five requirements met;
detail in `docs/ops/IMMUTABLE_SHA_DEPLOY.md` addendum. Certification, executed here:

```text
scripts/verify-deploy-provenance.sh
  M1 compose runtime override        innocent GREEN · reintroduced RED · via materialize RED · restored GREEN · other-service RED
  M2 .env.production GIT_COMMIT      innocent GREEN · =unknown RED · export RED · restored GREEN
  M3 structure from snapshot         -f inside snapshot · --project-directory = project dir · outside-snapshot RED · pre-materialize RED · static (both drivers) GREEN
  M4 pre-swap image stamp            == SHA GREEN · unknown RED · other SHA RED · empty RED · static ordering (both drivers) GREEN
  M5 post-swap container stamp       both match GREEN · THE INCIDENT (Config.Env=unknown) RED · printenv-only RED · Config.Env-only RED · both wrong RED
  restoration                        real tree sha256 unchanged
  27 passed · 0 failed
scripts/verify-deploy-context.sh     18 passed · 0 failed (fixture given a clean compose; dual-channel seams)
cmt-01 gates                         R25/R26 RED (expected), R27–R31 GREEN — unchanged
collector                            --anchor recorded; a manifest with buildSha "unknown" is refused (exit 1)
```

**Anchor discipline from here:**

```text
CMT code proven at       2fafaa4
deploy-chain repair at   R
live M2 witness at       R
```

---

## INCIDENT, continued — deploying `R` (2026-09-03, 17:5x–18:0x UTC)

Recorded verbatim from Kelly's terminal; nothing reconstructed.

**Checkout step refused.** `git checkout --detach eeb3fbb` on minisforum aborted: `Caddyfile` is
locally modified there, differs by +113 lines between `5370b42a3` and `R`, and is bind-mounted
**live** into `maia-caddy` (`./Caddyfile:/etc/caddy/Caddyfile:ro`). Not forced, not stashed.

**Deploy ran through the UNREPAIRED gate.** `pre-deploy-gate.sh deploy-maia eeb3fbb` executed the
`5370b42a3` checkout's scripts and compose (override line 115 present). Fingerprints: no
`Deployment structure = snapshot compose`, no `Built image provenance verified`, single-channel
post-swap message. Container created `17:54:10Z`, `GIT_COMMIT=eeb3fbb6c` on **both** channels —
green by circumstance (the variable happened to be present at `up`). A second data point for the
launch-provenance incident: same script, same override line, opposite outcome. **Not** a witness
runtime — not `R` through the repaired gate.

**Image bytes proven.** `docker run --rm --entrypoint sh maia-sovereign:eeb3fbb6c -c 'grep -c
verify:deploy-provenance package.json; printenv GIT_COMMIT'` → `1` / `eeb3fbb6c`. All build layers
reported `CACHED` including the content-hashed `COPY package.json`, which differs from every prior
commit — so an identical build of `R`'s tree existed on the host before 17:54. Actor unknown.
Recorded as unexplained; harmless to provenance because the image content was verified directly.

**Repaired chain brought onto the host without touching HEAD or Caddyfile.** Sparse checkout of the
eight deploy-chain files from `eeb3fbb`; `git diff eeb3fbb -- scripts docker-compose.production.yml`
shows only unrelated `scripts/*` the Aug-25 checkout lacks (the collector among them);
`verify-deploy-provenance.sh` on minisforum: **27 passed · 0 failed**.

**Second deployer on the production lane.** The redeploy of `R` through the repaired gate was
refused by the lane lock:

```text
pid=246130  started=2026-09-03T17:59:47Z  user=soullab@soullab
entry=deploy-production.sh deploy   git_commit=5370b42a3
```

Identification (read-only classifier): the holder had exited by the time it ran; crontab carries
only `backup-postgres` (02:00) and the 15-min `sweep-stale-sessions` — **not a scheduler**;
`.bash_history` on minisforum ends with `scripts/deploy-production.sh deploy "$TARGET"`; recent
logins are `pts/*` from `192.168.0.101` (a LAN host, i.e. an interactive ssh session). **A person
or session, not automation.**

**Result — `R` was replaced.** `IS R STILL LIVE`:

```text
2026-09-03T18:05:45.230604676Z sha256:98662677f51d…   GIT_COMMIT=a4305f4d6
```

Production now runs **`a4305f4`** — the merge-base of this lane (PR #1177, the `clean-main-no-secrets`
tip at census time). It contains **no** `lib/maia/canonical-turn`, no shadow block, no manifest.
The M2 witness is impossible on this container. `R`'s image survives as `maia-sovereign:previous`
/ `:eeb3fbb6c`.

**Classification.** The deploy-lane flock did its job — it serialized. What it cannot do is protect
a witness from a second writer with a different intent: the single-writer freeze on the *branch*
was honoured; the freeze must also cover the *production deploy lane* for the witness window.
Until the other deployer is identified and stood down, any witness runtime can be overwritten
between START and END.
