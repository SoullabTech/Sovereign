# P2 · NEW WHOLE/FIRST CELL — PREDECLARATION

```text
Authority   FR-W5 · P2 elected dc5eb5e8 (P1 formally exhausted, E2 = UNSURE)
Status      NEW EVIDENTIAL OBJECT
            ⛔ Does NOT reconstruct or replace the inherited Whole/first FAIL.
            ⛔ Historical seal remains UNTOUCHED.
RUNTIME     ⛔ untouched until this predeclaration is pinned
EDIT        ⛔ none
```

> ⭐ **PURPOSE — create ONE fully-provenanced Whole / first asked-return observation whose
> away-condition is specified BEFORE execution and CHECKABLE AFTERWARD.**
>
> ⭐ That is exactly what the inherited cell lacked: the condition, the act and the result will
> each carry their own provenance instead of being reconstructed afterward.

---

## Subject

```text
view             Whole manuscript
proposal         existing current proposal in Section 23
act              FIRST SHOW CHANGE press in this fresh runtime
code             unchanged        schema  unchanged        instrumentation  none
```

## PHASE A · ARRIVAL

```text
1. Fresh-load the proposal in Whole.
2. Touch nothing while automatic proposal arrival runs.
3. Wait until the viewport is visibly still.
```

## AWAY-CONDITION — PREDECLARED, STRUCTURAL ENDPOINT

```text
4. MANUALLY SCROLL upward through the manuscript.
     ⛔ No outline click.      ⛔ No rail / navigation control.
     ⛔ No browser Find.       ⛔ No SHOW CHANGE.
     ⛔ No programmatic jump.

5. Stop ONLY when the viewport is at the manuscript beginning:
     Section 1 / position 0 is the visible manuscript location.

6. Wait until the viewport is visibly still.
```

⭐ **Why Section 1 rather than "well away".** Whole mounts a viewport-derived window with
`OVERSCAN = 3`; the proposal is at index 22. A settled viewport at the manuscript beginning creates
a **structural** separation, instead of relying on a judgement that some arbitrary distance was
"far enough". ⭐ A structural endpoint is checkable; a distance is an opinion.

## PROVENANCE CAPTURE — REQUIRED BEFORE THE PRESS

```text
Capture ONE screenshot showing, IN THE SAME FRAME:
  • Whole manuscript mode
  • the manuscript visibly at Section 1 / position 0
  • the proposal panel still present for Section 23
  • SHOW CHANGE still available

That screenshot IS the act-condition record.
It proves the DECLARED END-STATE was reached before the asked return.

⛔ The screenshot does NOT prove every intermediate scroll gesture.
   The founder separately states that movement to this endpoint was performed by manual
   scrolling — recorded as FOUNDER STATEMENT, contemporaneous with this cell.
```

## CELL ACT

```text
7. Press SHOW CHANGE exactly ONCE.
8. Touch nothing afterward.
9. Observe where the first asked return terminates.
```

## CLASSIFY — CELL ONLY

```text
LOCUS-ADDRESSED     exact affected wording exposed
SECTION-ADDRESSED   Section 23 reached but exact locus remains off-screen
INVALID             away screenshot missing · endpoint not clearly Section 1 / position 0 ·
                    non-manual navigation used · prior SHOW CHANGE occurred ·
                    any competing navigation act before classification
```

```text
STOP — no second press · no repair · no explanation
```

---

## ⚠️ Evidence class — stated precisely, not inflated

⛔ This cell must never be written up as *"the screenshot proves Section 23 was unmounted."*
It does not. Without instrumentation, mount state remains inferred.

```text
ESTABLISHED              viewport endpoint = Section 1 / position 0
ESTABLISHED FROM SOURCE  Whole mounts a viewport-derived window with OVERSCAN = 3
DERIVED BY CONSTRUCTION  Section 23 is outside the intended mounted window at that endpoint
⛔ NOT DIRECTLY OBSERVED  React mount state itself
```

---

## Sequencing — ⭐ founder correction accepted

```text
P2 creates and seals the NEW CELL FIRST.
⛔ O1 IS NOT THE SAME PRESS.
```

⭐ Otherwise the lane would again collapse **"establish the defect under known provenance"** with
**"observe its post-landing dynamics"** — two different questions sharing one act.

### ⚠️⚠️ OPEN — O1's attachment point needs one more founder ruling

The proposed follow-on (*if the new cell is `SECTION-ADDRESSED`, do not reload; return to Section 1
again, capture the endpoint again, and spend O1*) ⛔ **conflicts with O1 as frozen:**

```text
O1 PRECONDITION (frozen b3c02ef0)   asked returns: ZERO PRIOR SHOW CHANGE PRESSES in this runtime
PROPOSED FOLLOW-ON                  O1's press would be the SECOND asked return in that runtime
```

⭐ And the conflict is not bookkeeping. O1 is titled *FIRST-ASKED-RETURN post-landing settle*, and
**first-versus-second is the very distinction C4 is unresolved about.** Spending O1 on a second
press would observe the settle behaviour of the act we are trying to hold constant.

```text
⭐ THE SAME CORRECTION, ONE STEP FURTHER:
   O1 is not the same press — AND it is not the NEXT press in the same runtime either.
```

Three lawful resolutions, ⛔ none chosen here:

```text
R-a  O1 GETS ITS OWN RUNTIME. Seal the new cell; then a SECOND fresh load, arrival, the same
     Section-1 away-condition with its own screenshot, and O1's press is that runtime's FIRST.
     ⭐ Preserves O1 exactly as frozen. Costs one more construction.

R-b  RE-SCOPE the follow-on as what it actually is — a NEW `Whole / second` cell under the
     Section-1 away-condition. ⭐ Valuable in its own right: it would reproduce the
     FAIL-then-PASS pattern under KNOWN provenance, which the inherited pair cannot.
     ⛔ But it is not O1 and must not be labelled O1.

R-c  AMEND O1's preconditions to admit a second press.
     ⛔ Jarvis advises against it: it would dissolve the distinction the observation exists to
        measure.
```

---

## Standing

```text
P1        ⛔ FORMALLY EXHAUSTED
P2        ✅ ELECTED · this predeclaration READY FOR RATIFICATION
OLD FAIL  ⭐ SEALED · UNTOUCHED

NEW CELL  first act after the predeclared Section-1 manual away-condition
O1        ✅ frozen · attaches ONLY after the new cell is sealed
          ⚠️ attachment point OPEN — R-a / R-b / R-c owed

RUNTIME   ⛔ untouched until this predeclaration is pinned
EDIT      ⛔ none          C4  ⛔ unresolved
```
