# JARVIS-WRITERS-STUDIO-FULL-REDESIGN-01 / PC0 FINDING
## What CC Was Missing

**Status:** BLOCKING FINDING · NO IMPLEMENTATION AUTHORITY

## 1. The full 27-image design corpus is not in canonical repository custody

At canonical `e886888416062c7fcbcf899040e3827bc8013835`, none of the exact 27 design-image filenames in the founder's design corpus are present in the Git tree.

The repository contains only a reduced / derived reference subset, including:

- `docs/design/writers-studio/flagship/00-visual-canon-board.png`
- `01-write-resting.png`
- `02-write-contextual-maia.png`
- `03-write-alternatives-read-in-context.png`
- `04-write-applied-history-undo.png`
- `05-develop-overview.png`
- `06-review-and-mobile-write.png`
- `ref-a-write-develop-mockup.png`
- `ref-c-develop-themes-revised.png`
- `ref-e-story-spiral-map.png`
- `ref-f-review-reference-a.png`
- `ref-g-review-finding-arrival.png`
- `ref-h-observation-and-staleness.png`

Those objects are useful historical references. They are not a substitute for the founder's full 27-image corpus.

### Consequence

An implementation agent operating only from the repository could not inspect the full set the founder was judging.

Therefore "built to the approved references" could mean:

> built to the reduced repository subset

rather than:

> materially conforms to the founder's full design corpus.

This is a custody defect.

---

## 2. The visual gate compared the implementation mostly against its own derived render lineage

The flagship programme accumulated:

- component renders;
- regenerated screenshots;
- mechanical visual laws;
- old-UI falsifiers;
- token-normalization evidence.

Those proved properties of the candidate.

They did not establish full visual fidelity to all 27 founder references.

A mechanically green render can still look like generic productivity software.

The founder has now demonstrated exactly that failure.

---

## 3. Earlier canon narrowed parts of the image design before the full corpus was durably bound

Several written rulings intentionally narrowed or altered visible features from the design boards.

Examples include:

- reducing navigation to only capabilities deemed live;
- restricting MAIA geometry;
- disallowing certain persistent regions;
- treating some visual board items as future capability;
- changing Develop vocabulary / lens model;
- constraining map rendering to declared structure;
- moving Review toward a governed observation architecture.

Many of those semantic protections are valuable.

But because the full image corpus was not simultaneously present as durable visual authority, semantic narrowing and visual redesign became entangled.

The result could be constitutionally careful and still visually wrong.

PC1 must separate:

`semantic/trust law to preserve`

from:

`visual rulings that must now be succeeded`.

---

## 4. No exact screen-to-screen implementation matrix bound every image to code

CC needs, for each of the 27 references:

- exact state name;
- viewport;
- shell;
- region geometry;
- typography;
- spacing rhythm;
- colors by role;
- border / radius / shadow;
- imagery / crop;
- visible controls;
- interaction states;
- live vs contextual regions;
- underlying data object;
- provenance class;
- evidence requirement;
- exact component owner;
- runtime substrate owner;
- mobile behavior;
- negative visual falsifiers.

Without that matrix, a developer is forced to infer.

Inference produced the Office-like result.

---

## 5. No mandatory original-reference visual diff existed before acceptance

Required going forward:

Every candidate frame must be rendered at the same viewport as its original reference and reviewed:

```text
ORIGINAL 27-CORPUS IMAGE
        vs
CURRENT CANDIDATE RENDER
```

Mechanical image-diff may assist.

It cannot replace founder judgment.

No implementation may cite its own previous render as the visual reference unless the founder explicitly promoted that render to authority.

---

## 6. Generic-productivity visual drift must be a release-stopping failure

Founder negative evidence now includes:

> "Microsoft office diy"

This class includes:

- generic document tree;
- generic copilot sidebar;
- generic white card dashboard;
- default SaaS spacing;
- tokenized UI that loses the literary / relational atmosphere;
- standard productivity toolbar look;
- flat utility panels replacing designed environmental composition;
- an interface that feels assembled from components rather than authored as a coherent place.

This is not cosmetic feedback.

It is a release blocker.

---

# Exact correction

Before CC writes another presentation component:

1. place the exact 27 founder images into repository custody unchanged;
2. verify SHA-256 against `VISUAL_CUSTODY_LEDGER_2026-09-23.md`;
3. ratify PC1 visual succession against conflicting older rulings;
4. complete PC2 screen/function reconciliation;
5. only then open PC3 implementation;
6. require original-reference side-by-side renders for every screen;
7. stop after each screen family for founder review.

## Standing

> **BLOCKED BEFORE IMPLEMENTATION · FULL VISUAL CORPUS NOT YET IN REPOSITORY CUSTODY**

No further visual implementation is authorized by this finding.
