# MAIA-WISDOM-01 · R9 — AIN Collective Intelligence Field

**Status: R9 RATIFIED (founder act, 2026-09-15) · NAMING RECONCILIATION OWED.**
**Read-only verification against repository @ `f179c32f`.** ⛔ No code, schema, or canon
changed. ⛔ ACT 2 not opened.

---

## 1. The ruling, and what it establishes

R9 is recorded in full in the founder's act. Its load-bearing claims:

- **AIN is the collective intelligence field of the Soullab organism**, distinct from an
  individual MAIA.
- **Each MAIA is a situated relational intelligence** in relationship with one member.
- ⛔⛔ **MEMBER MEMORY ≠ AIN KNOWLEDGE.** No personal relational material enters AIN merely
  because a member shared it with MAIA.
- **Standing is preserved on contribution** — lived experience · lived success · practice
  observation · counterexample · emerging pattern · collective pattern · founder teaching ·
  inherited wisdom · ratified knowledge.
- ⭐ **A reported elemental success is valid as lived evidence without becoming a universal
  prescription.**
- **Fire · Water · Earth · Air · Weather are collective learning fields**, not only
  individual interpretive environments.
- **Unified Field ≠ AIN** — the Unified Field *discloses the living relational condition*;
  AIN *develops durable collective learning*.
- **AIN returns distilled wisdom to MAIA** — reciprocal, ⛔ never extractive.
- ⭐⭐ **The intelligence belongs to the governed AIN substrate, not to any foundation
  model. Models may change; the collective field must remain.**

⭐ **This changes the programme's object.** MAIA-WISDOM-01 was opened as *"MAIA's second
brain."* Under R9 that framing is wrong at the top: **AIN is not MAIA's second brain — it
is the field every MAIA participates in.** The prior records are not withdrawn; they are
re-situated, and their findings hold.

---

## 2. ⛔⛔ "AIN" ALREADY DENOTES FOUR DIFFERENT THINGS

Same discipline as the aether/weather finding, and the Circles lane's *"Commons denotes
three things."* Verified, not inferred:

| Sense | Where | What it means there |
|---|---|---|
| **1. AIN-as-ENGINE** | `packages/ain-engine/README.md`, `mcp-servers/ain` | *"**AIN (Agentic Intelligence Networks)** is the deliberative computation engine that powers MAIA."* |
| **2. AIN-as-INHERITED-CORPUS** | `ain_knowledge_chunks`, `data/ain/source` | *"AIN Knowledge Base: Embedded source texts… mode-aware wisdom injection from **108+ source texts**"* |
| **3. AIN-as-ONTOLOGICAL-FRAMEWORK** | `CLAUDE.md` | *"the broader ontological and architectural framework: a view of intelligence as participatory, distributed, and meaning-bearing"* |
| **4. AIN-as-COLLECTIVE-FIELD** | **R9**, and `20260115000002_ain_collective_warmup.sql` | The collective intelligence field that learns across the organism |

### 2.1 ⛔⛔ Sense 1 states the OPPOSITE dependency direction from R9

`packages/ain-engine/README.md` declares an **Architecture Invariant**:

```
║  MAIA = Platform/Product (temple, interface, relationship)     ║
║  AIN  = Engine (deliberative core, computation primitives)     ║
║  RULE: AIN is a DEPENDENCY called by MAIA, never the reverse.  ║
║  RULE: AIN cannot import from MAIA or any app code.            ║
║  RULE: AIN has no public API routes.                           ║
```

**R9 requires AIN to learn *from* every MAIA** — contributed member wisdom, MAIA field
learning, Unified Field observations. **The engine invariant forbids AIN from knowing MAIA
exists.**

⚠️ **These are not necessarily in architectural conflict — they are in *naming* conflict.**
A computation dependency that must not import MAIA, and a collective field that must
receive from every MAIA, are coherently **two different objects**. ⛔ They cannot keep one
name. Under one name, the invariant reads as a refusal of R9, and R9 reads as a violation
of the invariant — and whichever a future session meets first will govern.

### 2.2 ⛔ Sense 2 is the thing R9 says AIN is *not*

`ain_knowledge_chunks` holds **books** — *"108+ source texts,"* and `data/ain/source`
carries third-party clinical works beside Soullab material (ACT 1 §1). Under R9's own
taxonomy that is **inherited wisdom**, one of the streams *entering* the collective field —
⛔ not the collective field itself.

> ⭐⭐ **The most AIN-named data object in the system holds precisely the category R9
> distinguishes AIN from.**

⛔ **A founder ruling is owed before any ACT 2 schema names an AIN object.** ⛔ Do not
reconcile silently, and ⛔ do not rename anything on this finding alone: `ain_knowledge_chunks`
has a live reader, and `packages/ain-engine` states a checked invariant.

---

## 3. ⭐⭐ The toroidal return stroke already exists in SQL

`database/migrations/20260115000002_ain_collective_warmup.sql`:

```
-- AIN COLLECTIVE WARMUP MODE
-- Improves efferent wisdom retrieval for sparse fields
-- 1. get_active_field_movements: warmup mode (min_count=1 when total < 20)
-- 2. get_relevant_breakthrough_patterns: fallback to recent patterns when no exact match
```

⭐ **`efferent` is the codebase's own word for the return stroke** — outward from the centre
into relationship. R9's circulation is not a new metaphor being imposed; **it is already
the vocabulary of the collective substrate**, and the afferent side is
`collective_breakthrough_system`'s *"Individual breakthroughs → Anonymized patterns →
Collective wisdom"* (ACT 1B §E.2).

⭐ **And the warmup mode is quietly the most R9-aligned thing in the repository.** It exists
because *a young field has few patterns*, and rather than returning nothing or fabricating
confidence it lowers the threshold and **declares that it has done so**. That is a system
designed to be honest about a thin field — which is exactly what R9 requires of standing.

⚠️ **Population UNKNOWN** (ACT 1B §B). ⛔ These functions may never have returned a row.

## 4. Unified Field — ⚠️ the name is occupied, the distinction is not drawn

**46 code files** carry unified/collective-field language: `lib/field/fieldOrchestrator.ts`,
`lib/consciousness/collective/CollectiveFieldArchitecture.ts`,
`CollectiveFieldOrchestrator.ts`, `lib/consciousness/field/UnifiedElementalFieldCalculator.ts`,
`ElementalFieldIntegration.ts`, `lib/anamnesis/UNITY_DECLARATION.md`.

⛔ **Whether any of it draws R9's distinction — *Unified Field senses the whole, AIN learns
from the whole* — is UNVERIFIED.** These carry a `CollectiveField` name while
`collective_breakthroughs` carries the durable-learning shape, which is the R9 boundary
running straight through two similarly-named things.

⚠️ **Recorded as an open question, ⛔ not censused here.** A Unified-Field census is its own
read-only act and was not authorized.

---

## 5. What R9 adds to the evidence owed

**F.12 — An AIN naming ruling** (§2), before any ACT 2 object is named. Three candidates,
⛔ none chosen here: qualify the engine (`ain-engine` → deliberative core, keep sense 1
scoped to `packages/`); qualify the corpus (`ain_knowledge_chunks` is *inherited wisdom*,
rename deferred); or reserve bare `AIN` for sense 4 and require every other use to qualify.

**F.13 — A Unified Field ⇄ AIN boundary census** (§4) — read-only, its own act.

**F.14 — Contribution standing model.** ⭐ R9 supplies the vocabulary ACT 1B §E.4 found
missing — *lived experience · lived success · practice observation · counterexample ·
emerging pattern · collective pattern · founder teaching · inherited wisdom · ratified
knowledge*. ⛔ Still no substrate holds a counterexample (ACT 1B §E.4), so this remains the
one requirement built from nothing.

**F.15 — The reciprocity surface.** R9 makes *"Offer this to the field"* a first-class
member act. ⛔ No such gesture exists. ⚠️ The nearest substrate is the `visibility`
gradient (`private | shared | commons | anonymized`, `DEFAULT 'private'`) — **already
correct in default and boundary** (ACT 1B §E.1), with ⛔ no verified surface exposing it.
⛔ **Do not build the gesture before the standing model** (F.14): a contribution act that
cannot record *lived success, single observation* would flatten exactly the distinction R9
exists to protect.

---

## 6. Standing

**R9 RATIFIED AND RECORDED · ⛔ AIN NAMING COLLISION OPEN (4 senses, one stating the
opposite dependency direction) · ⛔ UNIFIED FIELD ⇄ AIN BOUNDARY UNVERIFIED · ⭐ EFFERENT
RETURN STROKE FOUND IN SUBSTRATE, POPULATION UNKNOWN · ⛔ NO COUNTEREXAMPLE SUBSTRATE ·
⛔ NO CONTRIBUTION GESTURE · ⛔ NOTHING RENAMED · ⛔ ACT 2 NOT OPENED · PRODUCTION
UNTOUCHED.**

> *The circulation R9 names is already half-built and half-named: the system has a word for
> the return stroke and no word it can keep for the field itself.*
