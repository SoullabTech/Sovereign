# RELATIONAL-GEOMETRY-INTEGRATION-01 — R11 / H3f Result

**Status:** EXECUTED · SINGLE-PREDICATE MODEL FALSIFIED  
**Date:** 2026-09-16  
**Authority:** offline research only

## Result

12 adversarial operator/directionality runs produced 8/12 exact matches.

Correct 2/2 each:
- directed `INHIBITS`
- symmetric `TENSION`
- directed `EVOKES` into affect
- directed `EVOKES` into body

The remaining cases exposed representational problems rather than simple model errors.
## Falsifier

The phrase "When I imagine leaving, my body settles" was classified as `EVOKES`, while the frozen key expected `ALIGNMENT`. Both can be simultaneously true: one names directional elicitation, the other names configuration.

Likewise, an unbound AIR uncertainty + WATER fear pair was labeled `ALIGNMENT`; the earlier H3c law still rejects such a configuration because co-occurrence alone is not binding evidence.

## Architectural correction

A relation should not be forced into one exclusive predicate. The emerging geometry needs independent coordinates such as:

- **binding** — bound / unbound;
- **directionality** — directed / symmetric;
- **dynamic operator** — evokes / inhibits / none;
- **configuration** — alignment / tension / unresolved / none.

Constitutional sentence:

> **A relation may have multiple geometric properties. Binding, direction, dynamic operator, and configuration must not be collapsed into one label.**