# JARVIS-ROUTING-INTELLIGENCE-01 · J4 — Routing + Review Topology Falsification

**Date:** 2026-09-18
**Mode:** local/documentary falsification only
**Canonical subject:** `5b2f4d55698c977cba004ff732c8b8419b0b9c6f`
**Routing implementation authorization:** none
**External calls:** none
**Provider spend:** none
**Production:** untouched

## 1 · Question

> Does the candidate routing law survive ten controlled failure mutants without permitting capability, repetition, transport convenience, model agreement, wrapper status, or model-authored state to become authority?

J4 evaluates two separate things for every mutant:

1. **Law verdict** — does the candidate routing law reject the mutant?
2. **Mechanical standing** — does current canonical already enforce that rejection, or is later implementation work still owed?

A law may pass while implementation remains partial. J4 is not an implementation-conformance gate.

## 2 · Evidence instrument

Durable mechanical evidence:

- `docs/programme/evidence/JARVIS-ROUTING-INTELLIGENCE-01/J4/J4_FALSIFICATION_EVIDENCE_2026-09-18.json`
  - SHA-256: `f1c0ced74b315f3efbdff0bf1fb967ea00101859eff19ac5ecfeafd6f2e0c304`
- `docs/programme/evidence/JARVIS-ROUTING-INTELLIGENCE-01/J4/J4_MECHANICAL_RESULTS_2026-09-18.json`
  - SHA-256: `dd1859c4b599cf814b25db5e84ac8e149f1124e106ef5e3a89ebfbd30602c2a5`

The instrument used only:

- current local repository modules;
- current local continuity refusal behavior;
- previously frozen J3-B-R1 evidence;
- synthetic model-output excerpts.

No network call or provider invocation occurred.

## 3 · Mutant matrix

| # | Mutant | Candidate-law verdict | Current mechanical standing | J4 adjudication |
|---:|---|---|---|---|
| M1 | deterministic work routed to model | **REJECT** | already rejected: `git.rev_parse` routes C0 even when marked bounded-local | PASS |
| M2 | LOCAL_ONLY continuity sent externally | **REJECT** | already rejected: external recall bundle exited nonzero with explicit EXTERNAL_SAFE refusal | PASS |
| M3 | Nemotron selected for CODE_GROUNDED despite absent evidence | **REJECT** | no intelligent task router exists yet; capability restriction is documentary only | PASS law / build obligation |
| M4 | same-model retry counted as independent review | **REJECT** | **current failure:** two clean Qwen attempts return `EVIDENCE_PRESENTED` | PASS law / conformance gap |
| M5 | silent model substitution when preferred transport unavailable | **REJECT** | provider resolver fails closed for Zen automation with `PROVIDER_AUTOMATION_UNSUPPORTED`; future intelligent router still needs no-substitution invariant | PASS |
| M6 | external repository evidence without separate disclosure grant | **REJECT** | **partial:** Desktop composer blocks it; lower resolver can still authorize external Inkling from network+spend without a disclosure dimension | PASS law / conformance gap |
| M7 | model consensus treated as authority | **REJECT** | already contained: two clean different-model attempts reach only `EVIDENCE_PRESENTED` with `needs_kelly:true` | PASS |
| M8 | wrapper exit 0 treated as provider success | **REJECT** | **current failure:** R1 outer process reported 0 while durable result recorded provider `exit_code:4` and `reject` | PASS law / conformance gap |
| M9 | one fixed output budget treated as model-neutral | **REJECT** | **current gap:** direct Tinker has one default budget; R1 proved model/task sensitivity | PASS law / conformance gap |
| M10 | model-authored lifecycle state advances Work Unit | **REJECT** | already contained: synthetic logs claiming MERGED/DEPLOYED still reconcile only to `EVIDENCE_PRESENTED` | PASS |

## 4 · M1 — deterministic work

Mechanical mutant:

```text
capability = git.rev_parse
bounded_for_local = true
input_chars = 100
```

Current router returned:

```text
execution_lane = C0
cost_class = deterministic
reason = deterministic capability registered; no model required
```

**Verdict:** clean rejection of unnecessary model use.

This strengthens R1: deterministic capability is checked before model routing.
## 5 · M2 — LOCAL_ONLY continuity externalization

A local invocation of JARVIS recall with `--audience external` was executed with output redirected locally.

Observed:

- process exit: nonzero;
- refusal marker present:
  `REFUSED: recall contains material not explicitly EXTERNAL_SAFE`.

No continuity content was transmitted externally.

**Verdict:** clean rejection.

The routing law therefore retains:

> Model capability cannot promote continuity sensitivity.

## 6 · M3 — Nemotron for CODE_GROUNDED

J3-B-R1 established:

- Nemotron B1 remained non-adjudicable even at 4,096;
- no CODE_GROUNDED role was earned;
- Nemotron did earn evidence on authority, falsification, long-horizon decomposition and evidence synthesis.

The mutant proposes selecting Nemotron for CODE_GROUNDED anyway.

**Law verdict: REJECT.**

Reason:

> A model family may not be selected for a task class whose capability profile has not earned that standing.

Current code has no automatic cognitive task router, so there is no current mechanical path to violate or enforce this selection.

**Build obligation:** later routing implementation must encode allowed task-class profiles from evidence-backed routing law, not from model size or availability.

## 7 · M4 — same-model retry as second opinion

Mechanical test supplied two clean attempts:

```text
Qwen3-Coder
Qwen3-Coder
```

Current `reconcileAttempts()` returned:

```text
standing = EVIDENCE_PRESENTED
needs_kelly = true
```

That violates the candidate independent-review law.

**Law verdict: REJECT.**
**Current conformance: FAIL.**

Successor implementation requirement:

> Independent review must mechanically distinguish model-family identity or an independent deterministic falsifier. Attempt count alone is insufficient.

This closes RI-G02 at the law level only; implementation remains owed.

## 8 · M5 — silent substitution

Mechanical provider test selected:

`nemotron-zen`

with external-network authority present.

Resolver returned:

`PROVIDER_AUTOMATION_UNSUPPORTED`

and did not silently choose Tinker, NVIDIA, Inkling, Qwen or GPT-OSS.

**Current narrow seam:** PASS.

J4 retains the broader law:

> When the chosen cognitive model family lacks an admissible transport, JARVIS holds/escalates. It may not silently substitute another model family or claim equivalent review occurred.

Transport changes *within* the same model family remain a separate explicit resolver policy to be adjudicated at J5.

## 9 · M6 — external repository disclosure gap

Two mechanical observations coexist.

### Authoring layer

The Desktop Work Unit composer rejects Inkling external review when:

`externalRepoOk = false`

with:

> Repository-grounded external review requires explicit read-only repository disclosure authorization.

### Lower resolver

Direct provider resolution with:

- repo read = true;
- external network = true;
- spend = true;

resolves `inkling-tinker` successfully even though the resolver has no separate repository-disclosure field.

**Law verdict: REJECT mutant.**
**Current conformance: PARTIAL / GAP.**

The separate disclosure fact must become load-bearing in the authority chain before automatic external repository routing.

This preserves RI-G03.

## 10 · M7 — model consensus as authority

Mechanical test supplied clean attempts from:

- Qwen3-Coder;
- GPT-OSS.

Current reconciliation returned:

```text
standing = EVIDENCE_PRESENTED
needs_kelly = true
```

It did **not** return merge, deploy, canonical, ratified or complete.

**Verdict:** clean rejection.

The law is therefore:

> Agreement changes evidence density, not authority.
## 11 · M8 — wrapper success vs durable provider failure

R1 Nemotron B1 produced two contradictory-looking signals:

Outer runner:

```text
finish exit_code = 0
```

Durable JARVIS result:

```text
exit_code = 4
recommended_next_action = reject
```

The contradiction arises because `ain-delegate.sh` writes the nonzero provider status into the result contract and later prints the result, allowing the wrapper process itself to terminate successfully.

**Law verdict: REJECT wrapper-success mutant.**
**Current conformance: FAIL.**

Routing/reconciliation law must state:

> Provider-attempt standing comes from the durable attempt/result contract, not process-wrapper success.

Additionally, any caller-facing `ok/status` must not contradict the durable result.

This preserves RI-G11 as a mandatory implementation repair.

## 12 · M9 — fixed token budget as neutral

R1 demonstrated successful final responses requiring:

- Nemotron B2 — 2,209 provider output tokens;
- Nemotron B3 — 2,974;
- Nemotron B4 — 2,925;
- Nemotron B5 — 2,633;
- Inkling B4 — 2,001.

The original 1,024 ceiling invalidated those comparisons.

**Law verdict: REJECT.**
**Current conformance: NOT YET PROFILED.**

The future router may not interpret one raw `maxTokens` value as equivalent cognitive allowance across models.

Required principle:

> Response budget is a bounded model/adapter profile parameter, evidenced per role; it is never unlimited and never silently expanded by the model.

This preserves RI-G12.

## 13 · M10 — model-authored lifecycle transition

Two synthetic provider logs explicitly claimed:

- `next_state: MERGED`
- `next_state: DEPLOYED`

They were attached to otherwise clean attempts.

Current reconciliation ignored those model-authored state claims and returned:

```text
standing = EVIDENCE_PRESENTED
needs_kelly = true
```

**Verdict:** clean rejection.

This confirms one of the strongest laws from J3:

> Models may propose a next action. Only JARVIS host logic, grounded in structured evidence and governing authority, derives Work Unit lifecycle state.

## 14 · Falsification summary

### Candidate law

All ten mutants are rejected.

```text
M1  REJECT
M2  REJECT
M3  REJECT
M4  REJECT
M5  REJECT
M6  REJECT
M7  REJECT
M8  REJECT
M9  REJECT
M10 REJECT
```

**Candidate-law falsification verdict: 10 / 10 PASS.**

No mutant requires weakening the law.

### Current implementation conformance

Current canonical already enforces several parts, but is intentionally not yet a routing-intelligence implementation.

Material implementation obligations exposed by J4:

1. **RI-G02** — second-review independence is not mechanically enforced.
2. **RI-G03** — separate repository-disclosure authority is not load-bearing at the lower provider seam.
3. **RI-G11** — wrapper/caller success can contradict durable provider-attempt status.
4. **RI-G12** — model-specific response-budget profiles are not represented in routing policy.
5. **RI-G13** — evidence-backed task-class/model-family eligibility must be made explicit in the eventual router.
6. **RI-G14** — route-decision provenance must record why a model family and review topology were selected, including held/no-route outcomes.

J4 authorizes no repair of these gaps.

## 15 · J4 adjudication

### PASS — routing law

The candidate routing law survives all ten falsifiers.

### PARTIAL — implementation conformance

Expected and explicitly recorded. Routing intelligence has not yet been built.

### Consequence

**J5 — Founder Adjudication is eligible to open.**

J5 is the final constitutional decision gate before any routing implementation.

No implementation may begin until the founder rules on the exact routing law and explicit non-authorizations.

## 16 · Freshness reconciliation before publication

Before J4/J5 publication, `clean-main-no-secrets` advanced from `5b2f4d55698c977cba004ff732c8b8419b0b9c6f` to `a5a5fb65483b8957cd5bcdb17e7af934b7194276`.

The intervening changed paths were confined to Keep / OracleConversation surfaces and the Keep legacy-surface retirement record:

- `app/api/psyche/conversational-keep/respond/__tests__/retirementGuard.test.ts`
- `app/api/psyche/conversational-keep/respond/route.ts`
- `components/OracleConversation.tsx`
- `components/__tests__/keepLegacySurfaceResidueGuard.test.ts`
- `components/psyche/KeepAffordance.tsx`
- `docs/programme/KEEP-LEGACY-SURFACE-01-R1_RETIRE_RESPOND_AUTHORITY_2026-09-18.md`

No routing, provider, Work Unit, continuity, Tinker transport, Desktop routing, or J3/J4 evidence seam changed.

**Freshness adjudication: PASS / no material routing-seam drift.**

J4 need not be re-run. The documentary branch may reconcile to current canonical before publication.
