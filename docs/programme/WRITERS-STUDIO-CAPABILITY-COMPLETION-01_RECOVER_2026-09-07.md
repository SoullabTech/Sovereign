# WRITER'S STUDIO — CAPABILITY COMPLETION · 01
## ACT 1 — RECOVER · the prior R&D, retrieved

**Act** RECOVER (corrected lane flow: RECOVER → RECONCILE → PRESENT → FOUNDER ACT → BUILD)
**Custody** branch `claude/writers-studio-capability-clxw8d`
**Mode** READ-ONLY retrieval. No product judgement is made in this document.

---

## 0 · THE RETRIEVAL FAILURE, NAMED

**The Writer's Studio R&D record is not on this branch.**

```text
docs/programmes/writers-studio-v2/     18 files   ABSENT HERE
docs/design/writer-studio/references/   8 images  ABSENT HERE
```

Both live on `origin/claude/writers-studio-organization-wxpb7q`. The census read
only the working tree, found labels with no substrate, and inferred that the
capabilities were roadmap noise. **They are ratified R&D whose record was one
branch away.**

This is the retrieval problem the founder named. It is the whole cause of the
withdrawn rulings.

> `studioMap.ts` itself pointed at the record — *"the governing record lives on
> the one Writer's Studio lane, `claude/writers-studio-organization-wxpb7q`"* —
> and the census read that sentence without following it.

Recovered sources: `FIELD-MAP.md` · `CAPABILITY-MAP.md` · `DECISIONS.md`
(D-001…D-023) · `CAPABILITY-COVENANT.md` · `FUNCTION-PLACEMENT.md` ·
`DESIGN-CONTRACT.md` · reference pack `01`–`08`, canonical `04-writing-field-wide.png`.

---

## 1 · WHAT THE PRIOR R&D ACTUALLY SAYS, PER DESTINATION

| Destination | Prior R&D — ratified intent | Class in R&D | Census said | Divergence |
|---|---|---|---|---|
| **Goals** | `FIELD-MAP §1` "**Goals strip**" in WS-WRITE; `§5` "**active goals**" on Work Home; `§7` goal progress against a **writer-declared** target is explicitly computable and showable | **NEW** | "no substrate" | Census factually right, **product inference wrong** — intent is ratified twice and its permission is already written |
| **Timeline** | `FIELD-MAP §3` WS-STRUCTURE — "**Timeline · Threads · Flow · Table views**" | **NEW** | "defer / rename; Studio has order, not time" | ⛔ Census proposed retiring a ratified capability. Timeline is a **view of structure**, not a general time model — the census's technical point is a *design constraint*, not grounds for removal |
| **Threads** | `FIELD-MAP §3` twice — in the views row **and** "**Themes & Threads ribbon**"; `§4` "Themes → `LENSES.threads` — but **Threads is a Structure concept in the target**" | **NEW / EXTEND** | "orphan declaration — remove" | ⛔ **The most serious census error.** Threads is ratified, appears in the reference pack, and has lens substrate. `STRUCTURE_SURFACES` is not an accident — it is the one place the ratified concept landed |
| **Statistics** | `§7` computable and showable: word · section · material · version · finding · passage counts · reading time · goal progress | permitted | "live in band, unavailable in rail" | No divergence. **X-2 stands as a technical finding** |
| **Find / Replace** | `§1` "find in chapter / find in manuscript" — **REUSE AS-IS** on `findInDraft`, `lib/studio/manuscriptTools.ts` | **REUSE AS-IS** | "no substrate" | ⚠️ **Both are true and the R&D is stale**: neither `findInDraft` nor `manuscriptTools.ts` exists on **either** branch. R&D believed a substrate that is gone |
| **Insights** | `§5` "**recent MAIA insights**" on Work Home, substrate `studio/companion` — **EXTEND**; governed by **D-019** (MAIA is a relational mode, not a content owner) | **EXTEND** | "presumptively obsolete; retire" | ⛔ Census proposed retiring a ratified capability with named substrate |
| **Discover** | not individually specified in the recovered maps; `§6` "one MAIA, contextual per field" | unresolved | "retire" | ⛔ Retirement proposed against no recovered ruling either way |
| **Suggestions** | not individually specified; `MaiaReading` evidence noun `'suggestions'` is a **citation count**, per `maiaOffering.ts` | unresolved | "retire" | ⛔ Same |
| **Notes** | `CAPABILITY-MAP` known gaps: "no **notes** / research / templates / **word web** / comments surfaces" — listed as **unbuilt**, not as un-intended; materials carry a `note` **kind** | gap | "HOLD pending Notes-vs-Keeps" | Question survives; it is **genuinely unresolved**, not a reason to doubt intent |
| **Word Web** | named only in that same known-gaps list. **No semantics defined anywhere, on any branch** | gap | "semantics unknown" | ✅ Census correct. Truly unspecified |
| **Materials · Structure · Versions** | `§1–§3` REUSE AS-IS / REUSE + RECOMPOSE throughout | live | "live as panels, misdeclared" | ✅ No divergence |
| **Conversations** | `§6` "one MAIA, contextual per field"; D-019 | EXTEND | live-as-panel, conditional | ✅ No divergence (see Finding X-6) |

---

## 2 · THE LOAD-BEARING CONSTRAINT THE CENSUS NEVER SAW

`FIELD-MAP §6`, on what MAIA receives per turn:

```text
RoomFacts = workTitle · workPurpose · workForm · workStage
            materials[] · manuscriptTitle · draftChars
            draftExcerpt        // DRAFT_EXCERPT_CHARS = 6000 — the opening only
```

> No structure. No revision history. No cursor position. No prior finding.
> **MAIA is structurally *beside* the writing movement, not inside it.**

Every "MAIA observes X" panel in the reference pack requires `RoomFacts` to
carry something it does not carry — *"the single largest EXTEND in this map,
and the one that must be done with exclusion designed in, never by handing MAIA
the whole manuscript."*

This is the real reason the MAIA band is empty. **Not that the capabilities were
abandoned — that their precondition was never built.**

---

## 3 · D-003 / §7 — THE REFUSAL THAT ALREADY GOVERNS

```text
86% Movement Health · 76% Overall Cohesion · Coherence: Strong
82%/88%/87% per movement · High Priority · 87% Complete
```

**None of these ship.** They are judgments wearing the costume of measurement.
What ships is what MAIA noticed, with the passage she noticed it in, and the
writer assigns the importance. Already executable in `maiaOffering.ts`.

---

## 4 · STALE CLAIMS INSIDE THE RECOVERED R&D

Reported so the R&D can be trusted precisely, not blindly:

- `CAPABILITY-MAP` lists `lib/studio/manuscriptTools.ts` as live. **It exists on
  neither branch.**
- `CAPABILITY-MAP` labels `manuscripts/[id]/candidates` as "find/replace
  candidates". On **both** branches that route is the **Recognition Room quote
  extractor** (member-pulled, evidence-never-meaning, proposes-never-keeps).
- `CAPABILITY-MAP` names room components (`Companion.tsx`, `StructureRail.tsx`,
  `VersionsPanel.tsx`, `DevelopmentalReview.tsx`) that this branch does not carry
  under those names.

**These are implementation-reality corrections. They do not touch product intent.**

---

## 5 · WHAT REMAINS GENUINELY UNRESOLVED

Not answerable from any recovered record — these are the only questions that
legitimately need the founder:

1. **Word Web** — never specified. Lexical or semantic is undecided, not lost.
2. **Notes vs Keeps vs material-kind `note`** — three overlapping things, no
   ruling distinguishing them.
3. **Discover** and **Suggestions** — individually unspecified in the recovered
   maps; only the general "one MAIA, contextual per field" covers them.
4. **Whether the `writers-studio-v2` record and reference pack should be brought
   onto the working branch** — D-020 ("one lane") bears on this; a governing
   record that is one branch away is how this whole failure happened.

---

## 6 · STANDING

```text
RECOVER      COMPLETE
RECONCILE    partially performed in §1 (the divergence column)
PRESENT      this document
FOUNDER ACT  NOT PERFORMED
BUILD        NOT AUTHORIZED
DEPLOY       NOT AUTHORIZED

No capability retired. No capability redefined. No sequencing proposed.
```
