# JARVIS-FOUNDER-WORKSPACE-01 — B4R1 · Programme Identity + Succession Reconciliation (evidence)

**Programme:** `JARVIS-FOUNDER-WORKSPACE-01`
**Authorized by:** founder act 2026-09-23 — *"B4R1 — PROGRAMME IDENTITY + SUCCESSION RECONCILIATION ONLY: build the governed `programme-projection-map.v1`; resolve legacy aliases only where evidence supports them; use explicit succession first, Git ancestry second; include governed `docs/ops` lanes without swallowing the whole ops directory; reduce conflicts/unclassified subjects without forcing `complete:true`; stop again for adjudication."* **Against:** lane tip `624241b2` (B3/B4/V1 commit); canonical `840194ba` unchanged. **Standing: B4R1 DELIVERED · MAP GOVERNED (evidence per entry) · 1 unclassified · 8 same-commit conflicts · `complete=false` HONESTLY · ⛔ STOP FOR ADJUDICATION.**

Parallel founder lane (not this record's to report): the F1 rendered walk on the Mac Studio. B5 opens only on both.

## 1 · What landed

- `scripts/builder/founder-workspace/programme-projection-map.v1.json` — the governed map. Four groups, **every entry carries `evidence[]`** and the loader refuses one that does not (RM-1): `families` ×2 (`S3` → `S3-DESIGN-01`, member pattern `^S3(?:[-_]|$)`, thread token `S3`; `RGR` → `RELATIONAL-GEOMETRY-REASONING`, `^RGR-\d+$`) · `ops_lanes` ×1 (`docs/ops/WORKSTATION_STORAGE_RELIEF_2026-09-22.md` → `WORKSTATION-STORAGE-RELIEF`, resolving PD-3 by name) · `aliases` ×6, all role `supporting` (`MAIA_WHOLE_ORGANISM_MAP/` → `JARVIS-RD-HUMAN-EXPERIENCE-ARCHITECTURE-01` · `MAIA_CANONICAL_TURN_` → `CMT-01` · `CIRCLES_INVOCATION_SUBSTRATE_CENSUS.md` + `CIRCLE_` → `JARVIS-CIRCLES-01` · `S3_` → `S3-DESIGN-01` · `evidence/REVIEW-CUSTODY-01-STEP3R?/` → `REVIEW-CUSTODY-01`) · `classifications` ×10 (examined, never programmes: `parked-defect` · `standalone-finding` · `act-series-record (J-series)` · `unregistered-lane:LIVING-CONSTELLATION` · `unregistered-lane:WRITERS-STUDIO` · `unregistered-programme:MEMORY-ORGANISM` · `operating-manual` · `unassociated-record` · `legacy-thread-note` · `thread-note`). The evidence strings name the record line, thread bullet or filename convention that supports each entry; ⛔ no entry was written from memory of what a file "should" belong to.
- `programme-state-projector.mjs` → `founder-workspace/programme-state-projector@2-b4r1`. New named decisions, each replaceable and each killed in the matrix: **R-A0** a record's own `**Programme:**`/`**Lane:**` declaration governs association (51 files now declare) · **R-O1** `docs/ops` enters the population **only** through `opsLanes(map)` — a named list, never a directory read · **R-A4** map alias (role from the map) · **R-F1** family canonical · **R-A5** a still-unassociated file cited by the records/bullets of exactly one programme → supporting; cited by several → unclassified with the ambiguity named · **R-A6** exactly one distinct backticked id in the file's own text → supporting · **R-A3b/R-A3c** family thread token / single plain id in a bullet's bold title · **RM-3** map classification · **R-S1** explicit `**Supersedes:**`/`**Predecessor:**` line · **R-S2** git first-add order from one `git log --diff-filter=A --name-only --format=%ct -- docs/programme docs/ops` (null when git is absent) · **R-S3** remaining ties stay `UNVERIFIED / CONFLICT`. ⭐ **Supporting-role records never supply standing** — an aliased file with a tier-1 filename and a Disposition line is attached to its programme and does not govern it (R-A4s).
- `tests/constitutional/founder-workspace/{b4-matrix,b4-candidates}.mjs` — B4R1 laws over a second synthetic repository (declarations · family · ops lane + a sibling ops file that must never enter · alias · classification · three same-day sets resolved by R-S1, by R-S2 with an injected git log, and left tied by R-S3 · one leftover unclassified file) and **ten new defeat candidates DC-P7…DC-P16**.

## 2 · Matrix (this container)

`npm run matrix:founder-workspace` → B1 · B2 · B3 · B4 all exit 0; `npm run typecheck:founder-workspace` strict checkJs **exit 0** (⚠️ `@types/node` resolved from a scratchpad — the founder's run is the evidence of record); `check:no-supabase` clean; `matrix:voice-doorway` unchanged, exit 0.

B4R1 laws: R-A0 · R-F1 · R-O1 (synthetic **and** real: docs/ops files in the population = map `ops_lanes` exactly, 1) · R-A4s · RM-1 (evidence-less entry refused; the committed map loads) · RM-3 · R-S1 · R-S2 · R-S3 · R-C1 · PJ-0 determinism on the B4R1 projection. **PJ-9 revised, not weakened**: the B4 `KNOWN_OUTSIDE_POPULATION` exception is retired — `WORKSTATION-STORAGE-RELIEF` must now be present **and** associated by R-O1 from `docs/ops/`. The B4 baseline laws (PJ-1…PJ-7) now run with `map: null, git: null` so they test the bare FD-3 projector, not the map.

**DC-P7…DC-P16 all DEAD**: DC-P7 filename-over-declaration → emits `ZETA-06`, loses `EPSILON-05` · DC-P8 Supersedes-is-prose → ETA-07 CONFLICT · DC-P9 git-is-incidental → THETA-08 CONFLICT · DC-P10 same-commit-alphabetical → IOTA-09 resolved · DC-P11 lenient loader → accepts an evidence-less entry · DC-P12 whole-directory ops → the sibling file enters · DC-P13 category-as-programme → `PARKED-DEFECT` emitted · DC-P14 alias-as-record → ALPHA-01 governed by the aliased Disposition · DC-P15 one-leftover-tolerated → `complete=true` with 1 unclassified. DC-P1…DC-P6 still DEAD.

⭐ **The matrix found a defect in the instrument before it found one in a candidate** (third time in this programme's lineage; see B1 and REVIEW-CUSTODY-01 Step 2): the first B4R1 `select()` called `DECISIONS.succeedBySupersedes` / `DECISIONS.succeedByFirstAdd` on the **frozen module object**, not on the injected decision set — so DC-P8, DC-P9 and DC-P10 **survived unexercised** while every law reported PASS. `select` now receives the effective decisions as a third argument and succession runs through it. *A seam that is not actually reached by the override is a seam in prose only.*

## 3 · The real projection, before and after (observed against `624241b2`)

| reading | subjects | programme ids | unclassified | classified | conflicts | complete |
|---|---|---|---|---|---|---|
| B4 record (projector @1) | 630 (570 · 60) | 197 | 94 | 0 | 17 | false |
| B4R1 rules only, `map:null`, `git:null` (same checkout) | 634 (573 · 61) | 197 | 65 | 0 | 12 | false |
| **B4R1 rules + map + git** (this record and its CLAUDE.md bullet included in the population) | **637 (575 · 62)** | **182** | **1** | **52** | **8** | **false** |

Evidence states after: OBSERVED 55 · UNVERIFIED 119 · UNVERIFIED / CONFLICT 8. Association rules used: R-A1 435 · R-A0 52 · R-A4 13 · R-A1+R-F1(S3) 12 · R-A2 8 · R-A0+R-F1(S3) 7 · R-A6 6 (+1 with R-F1) · R-A5 3 · R-O1 1 · R-A1+R-F1(RGR) 1. Reproduce: `npm run project:programme-state`.

⭐ **A second instrument finding, caught by writing this record.** The first draft of this record named the one unclassified file by path in its docket — and the projection flipped to `complete:true`: R-A5 counted the mention as a *citation*, attached `F5_ERASURE_TRACE_ADJUDICATION` to `JARVIS-FOUNDER-WORKSPACE-01` as a supporting record, and the arithmetic closed. A record saying *this file belongs to nobody* had made it belong to someone. R-A5 is now narrowed to citations from **standing-bearing (tier-1/2) file records** only — a docket note, a no-standing record, a supporting alias or a thread bullet is not a claim of custody. Law R-A5n + candidate DC-P16 (credits every mention → the loose file attaches and `complete` flips → DIES). Collateral, all in the honest direction: the `DEV_LANE_PROVIDER_EXPOSURE_FINDING` is no longer attached to `DC-C7` through a bullet mention and is classified `standalone-finding`; R-A5 associations fell from 6 to 3.

- **Succession resolved by R-S2 (4 sets)**: `DEPLOYMENT-SAFETY-03` → `…_R1_CANONICAL_RECONCILIATION_2026-09-21` · `JARVIS-FOUNDER-WORKSPACE-01` → `…_F1R1_FOUNDER_ADJUDICATION_2026-09-23` (the true current act) · `MAIA-TEACHING-INTELLIGENCE-01` → `…_FOUNDER_PRODUCTION_EXPERIENCE_WITNESS_2026-09-19` · `WS-EDITORIAL-SCOPE-01` → `…_LIVE_WITNESS_PROCEDURE_2026-09-20`. **R-S1 resolved none in the real repository** — no record today carries an explicit `**Supersedes:**` line naming a same-day sibling; the rule is live and waiting for authors to use it.
- **Families**: 19 `S3-*` records (`S3-O1`, `S3-F8-WITNESS-01`, `S3-CLASS-B`, `S3-M1-M3`, …) + 7 declared `S3` files + the `S3_` censuses now read as one programme `S3-DESIGN-01` (24 sources). ⚠️ Its standing is `UNVERIFIED / CONFLICT`: the family collapses several same-day tier-1/tier-2 records (2026-09-13 and 2026-09-14) that previously conflicted only within `S3-O1`. The conflict is now visible at the programme, which is where it belongs. `RGR-*` → `RELATIONAL-GEOMETRY-REASONING` (12 sources, OBSERVED).
- **`WORKSTATION-STORAGE-RELIEF`** is emitted via R-O1 with evidence_state `UNVERIFIED`: its ops record has no `**State|Status|Standing:**` line, so the projector attaches the record and declares nothing (PS-4). The docs/ops directory holds ~60 other files; none entered.
- **The one remaining unclassified file**: `docs/programme/F5_ERASURE_TRACE_ADJUDICATION_2026-09-17.md` — carries a `**Standing:**` line and no programme id anywhere in its text (0 backticked ids; the SPM-FC-01 lineage that CLAUDE.md attributes it to is not stated in the file). ⛔ Not aliased: the founder's scope was *only where evidence supports*, and the only evidence is in the thread, which orients and does not associate.
- **The eight remaining conflicts are all same-commit sets** (R-S3): `JARVIS-WORK-UNIT-01` (W0…W5, 2026-09-18) · `JOP-04` · `MAIA-CONVERGENCE-01` · `MAIA-RELATIONAL-FIELD-SHADOW-01` · `MAIA-WISDOM-01` · `MAIA-WISDOM-CONSENT-01` · `S3-DESIGN-01` · `TEMPORAL-MEMORY-CUT1-TRACEABILITY-01`. Git cannot order files added in one commit; the projector says so rather than guessing. Two lawful resolutions exist and both are founder rules: an author adds a `**Supersedes:**` line (R-S1, already live) or the founder rules an act-sequence convention inside a commit (PD-1, still open).

## 4 · Docket (for the adjudication that follows the F1 walk)

1. **PD-1 (carried, narrowed).** 8 same-commit sets remain. Recommended: **R-S1 by authorship** — the newest record in each set gains a `**Supersedes:**` line when its owner next touches it; ⛔ the projector does not infer it. No new projector rule needed.
2. **PD-4 · act-shaped filename prefixes are still separate programme ids** and carry **no in-file evidence of their parent**: `F5-A/B/C/R1` (0 ids in text), `ER-R1…R5`, `GATE-0`, `W4-SCHEMA-*`, `W5-LANDING-02`, plus `BUILD-07A` and `DC-C7` (both thread-only: R-A3 picked the first backticked id in a long bullet, which shows that *first backticked id* is too weak for long bullets — a `**Programme:**`-style declaration in the bullet title would be the lawful fix). Lawful fix: the owning programme declares them (`**Programme:**` line, R-A0) or the founder rules a family with evidence. ⛔ Not mapped here.
3. **PD-5 · unregistered lanes.** `LIVING_CONSTELLATION_*` (LC00/LC01/LC01A), `WRITERS_STUDIO_*` (brief · board · roadmap · capability census), `MAIA_JARVIS_MEMORY_ORGANISM_*`/`JARVIS_MEMORY_FIELD_FLOW_v2`, `HOME_REDESIGN_v0.2_*`/`WS_HOME_v0.2_*` and the `J9/J10/J11_*` act series are classified, counted and **not** programmes. Each needs a founder-named id (and for J-series, a decision whether it is one programme or several) before it can carry standing on the board.
4. **PD-6 · a record without a standing line carries no standing** (`WORKSTATION-STORAGE-RELIEF`, `CMT-01`, and 117 other UNVERIFIED ids). This is the projector working as ruled (PS-4), and it is also a sign that most lanes state their standing in prose the projector will never read. Whether to require `**Standing:**` in the mandatory lane preamble is a governance question, ⛔ not a projector one.
5. **PD-7 · `S3-DESIGN-01` as the S3 canonical.** The family collapses S3 substrate/route/witness records into the design lane. If the founder prefers a different canonical (e.g. `S3`), it is one map edit with evidence; nothing else moves.

## 5 · Not done, not authorized

No projection written (`--write` not invoked); no `$AIN` touched; no Desktop mutation; no IPC; no Today surface reads this; no map entry without evidence; no alias for F5 / ER / GATE / W4 / W5 / DC / BUILD ids; ⛔ B5 not open (owed to the F1 walk + this adjudication); ⛔ `complete:true` not forced and not reachable without PD-4/PD-5 acts; production untouched.
