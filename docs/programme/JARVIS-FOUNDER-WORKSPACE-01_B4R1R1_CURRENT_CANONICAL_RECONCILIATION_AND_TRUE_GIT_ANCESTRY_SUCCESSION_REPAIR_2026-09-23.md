# JARVIS-FOUNDER-WORKSPACE-01 — B4R1R1 · Current-Canonical Reconciliation + True Git-Ancestry Succession Repair

**Programme:** `JARVIS-FOUNDER-WORKSPACE-01`
**Act:** `B4R1R1 — CURRENT-CANONICAL RECONCILIATION + TRUE GIT-ANCESTRY SUCCESSION REPAIR ONLY`
**Predecessor candidate:** `a1bcbf110831646d0956ae550c46b240c2b61d40`
**Current canonical bound by this evidence:** `bb1142863a3e9d5ba3848eeb6301c78d3cc5bde9`
**Evidence disposition:** DELIVERED FOR FOUNDER ADJUDICATION · B5 CLOSED · NO PROJECTION WRITE · PRODUCTION UNTOUCHED.

## 1 · Freshness reconciliation

B4R1 was originally observed against an older canonical population. During adjudication, canonical advanced through `6d745a4d…`, `2703cc309…`, and `bb1142863…`. Each advance was inspected before rebinding because `docs/programme/**` is itself projector input.

The final advance `2703cc309… → bb1142863…` adds exactly one governed programme record and changes neither `CLAUDE.md` nor the Founder Workspace projector/test surfaces.

Current canonical does not contain the Founder Workspace lineage. Replaying only the terminal B4R1 commit would strand the act without B1–B4 dependencies, so reconciliation preserves the exact full predecessor lineage by a clean no-conflict merge of `a1bcbf110…` onto `bb1142863…`.

## 2 · R-S2 repaired from time to relation

The predecessor labelled R-S2 as Git ancestry but implemented first-add commit **timestamps**. B4R1R1 removes timestamp authority.

Each governed file now carries its first-add **commit SHA**. R-S2 asks Git `merge-base --is-ancestor` and selects a same-day successor only when that record's first-add commit is a **strict descendant of every competing first-add commit**.

Therefore same-commit, distinct-but-incomparable, and unknown/instrument-error ancestry stay `UNVERIFIED / CONFLICT`; wall-clock order never establishes succession.

The projector identity advances to `founder-workspace/programme-state-projector@3-b4r1r1`. The governed identity map changes only its R-S2 description; no family, alias, ops lane or classification entry changes.

## 3 · Falsification

`DC-P17` asserts the exact rejected belief: *first-add wall-clock order may stand in for Git ancestry*. Its synthetic timestamps deliberately point opposite the DAG. It selects `THETA A`; the lawful ancestry rule selects `THETA B`; DC-P17 dies.

The matrix also pins explicit supersession (`ETA-07`), true ancestor→descendant first-add commits (`THETA-08`), same-commit conflict (`IOTA-09`), and distinct incomparable first-add commits (`KAPPA-10`).

Total B4 defeat candidates are now **17/17 dead** (`DC-P1…DC-P17`). The predecessor B4R1 evidence prose is corrected from the stale P7…P15 count to P7…P16; B4R1R1 adds P17.

## 4 · Real current-canonical projection

Final self-including counts: **676 subjects (613 files · 63 thread bullets) · 190 programme ids · 7 unclassified · 52 classified · 4 conflicts · 0 unreadable · `complete=false` · evidence states 62 OBSERVED / 124 UNVERIFIED / 4 UNVERIFIED-CONFLICT.** Conflict ids: `JARVIS-FOUNDER-WORKSPACE-01` · `JOP-04` · `MAIA-RELATIONAL-FIELD-SHADOW-01` · `S3-DESIGN-01`. Projection content hash before this non-governing prose fill: `79b827ffcf0ad147b9247bd64c3a0e6f4cdb6258f4f226bd92a30f5a9f9db878`.

No projection was written. These numbers are a read-only rebuild from repository authority.

## 5 · Witness gates

- `b4-matrix.mjs`: **PASS** · R-S1/R-S2/R-S3 + incomparable-commit witness green · `DC-P1…DC-P17` all dead.
- B3 matrix on a disposable **single-worktree exact-tree copy**: **PASS · exit 0**; `WORKTREES=1`; both census runs left the repository snapshot byte-identical; composed Monitor validated live. This avoids making 287 unrelated registered worktrees part of the timing cost while leaving the gate code unchanged.
- B1/B2 matrices: **PASS** in the composite run before B3; B4 independently PASS on the identical candidate code.
- `typecheck:founder-workspace`: **PASS · exit 0**, using the Mac Studio's already-installed `tsc` + `@types/node` via explicit typeRoots; no dependency installation.
- `matrix:voice-doorway`: **PASS · exit 0**.
- `check:no-supabase`: **PASS**.
- remaining pre-commit checks run individually after the hook stopped: `check:no-direct-anthropic` **PASS** · `check:phi-gate` **PASS** (one pre-existing migration warning, gate green) · staged `check:design-canon` **PASS**.
- **PRE-COMMIT PROVIDER GOVERNANCE: REFUSAL, inherited from the predecessor lineage, not repaired here.** `check:no-openai` rejects `tests/constitutional/voice-doorway/matrix.mjs:37` because its V-F2 defeat fixture contains the literal OpenAI STT endpoint. Exact file blob is `c9a3f9fc5685f88af3e5eb66fd213b55e5d2534c` both in predecessor `a1bcbf110…` and in this reconciliation; current canonical `bb1142863…` does not contain the file. B4R1R1 did not alter it. No voice-policy or provider-policy change is authorized in this act.

Therefore the B4R1R1 semantic/reconciliation witness is green, while **canonical-admission readiness remains separately blocked by inherited provider-governance debt**. No bypass is presented as a green gate.

## 6 · Scope held

No new aliases. PD-1 unresolved. PD-4/PD-5/PD-6/PD-7 unspent. No B5. No Desktop mutation. No IPC. No `$AIN` projection write. No production mutation.

The repair is deliberately narrow: **identity architecture preserved; empirical population refreshed; succession changed from a clock comparison to a Git relation.**
