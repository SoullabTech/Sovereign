# PHASE-1-WHOLE-ORGANISM-CENSUS-01 · P1-05 · ADJUDICATION CONTRACT

```text
STEP     P1-05 · CONTRADICTION / GAP PASS
STATUS   🟢 OPEN
TYPE     RECORD ONLY — ⛔ no repair · no design · no organism re-tracing
```

> ⭐⭐ **The purpose of adjudication is not to make the organism consistent. It is to determine
> precisely where the evidence permits consistency, contradiction, or uncertainty to be claimed.**

### The central rule

> **Adjudication may determine WHAT KIND of contradiction the existing evidence contains. ⛔ It may
> not manufacture new evidence to make the contradiction disappear.**

⛔ P1-02 implementation tracing is **not** reopened because a contradiction is inconvenient.

---

## 1 · The six outcomes — ⛔ and there is no seventh

```text
C1   RESOLVED BY SCOPE
     both claims true, about different named objects / paths / conditions

C2   RESOLVED BY ALTITUDE
     both true, but one is implementation · governance · runtime · documentation · CI …

C3   RESOLVED BY TIME / SUBJECT
     claims concern different evidenced subjects or dated states

C4   RECORD / ARITHMETIC DEFECT
     internal bookkeeping, ⛔ not organism behaviour

C5   GENUINE CONTRADICTION
     same object · same scope · same altitude · same subject — and both cannot hold

C6   UNKNOWN
     the existing evidence cannot adjudicate which interpretation is correct
```

⛔ **No category for *"probably."*** A resolution that requires a likelihood judgement is `C6`.

---

## 2 · Evidence boundary

```text
MAY READ    P1-01 governing-source records · P1-02 domain records
            P1-03 normalized + ladder records · P1-04 maps
            the quotes and citations already contained in those records

⛔ MAY NOT  re-trace source code · inspect new runtime behaviour
            search for new governing documents · use AUTH-EXPOSURE-01 results
            repair source records · ⛔ ALTER ROW VALUES TO MAKE TOTALS RECONCILE
```

If adjudication requires evidence outside that set:

```text
→ C6 UNKNOWN
→ route the owed read forward
```

⭐ *That is what protects the distinction between ADJUDICATING the census and RE-RUNNING it.*

---

## 3 · The five named objects

**`X-DEF-1`** — ask whether it is the *same named block · same cognition family · same stage · same
prompt object · same branch/mode/condition*. Differ → `C1`/`C2`. Genuinely the same object under
the same condition → **`C5`**. ⛔ Do not reread code to settle it.

**`X-DEF-2`** — three characterizations *may not actually conflict*: structural identity, blocked
reachability, and near-zero historical use can coexist. ⛔ **But the partition must be EARNED from
the existing records, ⛔ never assumed.**

**`X-DEF-3`** — expected to classify as a **record-level inference contradiction**, ⛔ not an
organism contradiction. ⛔ **Do not edit the P1-02 record. The contradiction itself is evidence.**

**Symbolic `CONCLUDE`** — treat as an **enumeration** contradiction first, ⛔ not a semantic one.
Compare *membership · count · refusal capability · definition of CONCLUDE*. Different inclusion
rules → `C1`. Same purported set, different content → `C5` or `C6`.

**`54/51` + the MAP 3 arithmetic discrepancy** — for each, compare *source total · row-derived
total · inclusion rule · denominator · excluded states*. Rows summing one way against a summary
saying another → **`C4`**. ⛔ **Do not "fix" the summary here.** Record which representation the
maps used, and why.

---

## 4 · ⭐⭐ Two aggregate promotions that are FORBIDDEN

### 4a · `GOVERNED ACT = 0`

```text
⛔ FORBIDDEN   "No acts are governed."
✅ LAWFUL      "No row in this census established GOVERNED ACT."
```

⭐ The forbidden form is the **`NONE LOCATED → ABSENT` promotion in aggregate form** — the same
error the gate has refused at row level all along, wearing a total as a disguise.

Likewise the **62** rows remain **named findings**, ⛔ never *"62 unauthorized capabilities."*

### 4b · The two zeros

```text
47 rows   the question was never posed          (SYN-4)
 1 row    an observed negative                   (SYN-1)
```

⛔ Do not aggregate them into a single semantic zero. Stated generally, and binding on the whole
pass:

> ⭐⭐ **Numerically equal states are not equivalent when their epistemic provenance differs.**

---

## 5 · Output — three artifacts

```text
1  CONTRADICTION REGISTER      every contradiction → exactly one of C1…C6
2  SELF-CORRECTION REGISTER    defects the census introduced into its OWN output,
                               incl. X-DEF-3, the arithmetic discrepancies, and the
                               checker corrections
3  UNRESOLVED OBLIGATIONS      questions P1 cannot lawfully answer from its evidence
```

⭐ **Artifact 2 matters.** This census has repeatedly caught its own overclaims and its own checker
defects. ⛔ That is not noise to bury — *it is the evidence that the method can discriminate
against its own outputs.*
