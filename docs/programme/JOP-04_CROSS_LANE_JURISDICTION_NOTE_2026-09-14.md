# JOP-04 · Cross-Lane Jurisdiction Note

**Date:** 2026-09-14 · **Scope:** a Writer's Studio thread that appeared inside this session

---

## What happened

A Writer's Studio Canvas runtime capture (SHOW CHANGE / proposal-locus) was raised in the JOP-04
session. **This session had no jurisdiction over it:** no `revision-proposal` route on this branch, no
programme record for the defect, no branch custody, no artifacts. The only `SHOW CHANGE` matches on
this branch are in `components/maia/changes/` — a different surface.

**The session said so rather than proceeding:** *"I don't have this lane."*

## Ruling (founder, 2026-09-14)

```text
STRAY THREAD    CLOSED — SUPERSEDED / NO JURISDICTION
```

The canonical `WS-PROPOSAL-INTERACTION-01` work has since overtaken every caution the stray thread
raised — 6/6 behavioral cells complete, locus-vs-shell criterion predeclared, repeatability and
passive control measured, smooth-scroll artefact resolved, causal verification confirmed. ⛔ **The
stray thread has nothing further to contribute.**

⛔ **No record of `WS-PROPOSAL-INTERACTION-01` is written here.** Its disposition belongs to the
session holding its artifacts. Writing a closure record for a lane whose evidence this session has
never seen would be the provenance defect this programme spent the day refusing — *a programme
deriving authority from a document it cannot retrieve.*

## ⭐ The methodological point that survives, and generalizes

The founder's preference — **close a diagnosis-scoped witness lane; carry the repair separately** —
rests on the same separation this programme is built on:

> **A witness lane that absorbs its own repair becomes self-accepting.** The apparatus that diagnosed
> the defect would also be the thing certifying the fix.

That is the subject/instrument separation JOP-04 has enforced throughout: `SUBJECT SHA` ≠
`INSTRUMENT SHA` ≠ `REPAIR SHA`, and *"the repair must not modify its judge in the same
undifferentiated commit."* ⭐ It is also the shape of the 2026-09-07 finding that *merging to
canonical is latent deploy authorization* — **a lane quietly accumulating authority it was never
granted.**

A successful witness lane is the most likely thing to become an implementation catch-all, precisely
because it has earned trust.

## Standing

```text
JOP-04 / RB-6B CONTRACT LANE     ⭐ OPEN — D4 ratified, five rulings owed (D1 D5 D6 D2 D3)
                                 IMPLEMENTATION BLOCKED · HANDLERS TOUCHED 0
STRAY WRITER'S STUDIO THREAD     CLOSED — superseded / no jurisdiction
WS-PROPOSAL-INTERACTION-01       ⛔ NOT THIS SESSION'S TO DISPOSE
```
