# DRIVER-01 — automated on-device witness driver — PLAN ONLY — 2026-09-12

**Authority:** founder act 2026-09-12 — *automated driver AUTHORIZED, PLAN ONLY; XCUITest permitted as an external witness instrument; VoiceKernel source FROZEN; harness source PREFER UNCHANGED; the app under test MUST remain the exact artifact if technically possible; manual loop RETIRED for rate estimation; W4 manual CLOSED; W4 automated MAY BE PLANNED; B2 HOLD; E1–E4 HELD; mechanism claim NONE.* Principle: **automate the witness, not the organism.**

**Status: DRAFT FOR ACCEPTANCE. Nothing built. Nothing installed.**

## 0. The answer to the founder's flagged question first

**XCUITest does not require rebuilding the harness.** A UI-test bundle drives any installed app by bundle identifier — `XCUIApplication(bundleIdentifier: "life.soullab.voicekernel.k00")` — and needs no test hooks, no launch arguments, no linkage to the app, and no change to the app's source or signature. The UI-test bundle must be hosted by *some* app target in its own Xcode project; that host is a **separate, empty placeholder app** (the driver's host), not the harness. The device therefore carries three signed bundles: the untouched harness (`24a6fcfa1`, dylib `CC0D3604-7902-373E-A2BB-2C093D9BF804`), the driver host, and the XCTest runner. The organism measured is the exact organism already measured.

Two things must be said plainly, because they are the driver's own divergences from the manual protocol, and they are recorded as strata, not hidden:

- **Launch authority.** A manual sample is launched by SpringBoard from an icon tap. An XCUITest sample is launched either (Mode I) by the driver tapping the harness icon on SpringBoard through `XCUIApplication(bundleIdentifier: "com.apple.springboard")` — the closest reproduction of the manual act — or (Mode L) by `XCUIApplication(bundleIdentifier:).launch()`, where testmanagerd launches the process. Mode L is a different launch path from the icon; it is not a debugger and injects nothing, but it is not SpringBoard either. **Mode I is the primary design; Mode L is the fallback if Mode I proves unreliable in calibration.** Whichever runs is named in every ledger row (`AUTOMATED-COLD-ICON` / `AUTOMATED-COLD-LAUNCH`).
- **Termination.** `XCUIApplication.terminate()` sends the app a kill through testmanagerd; a manual force-quit is a swipe in the app switcher. Both end the process. The cold precondition is not inferred from either; it is **verified** (§3).

## 1. Architecture (the founder's shape, made concrete)

```
EXACT APP UNDER TEST     ios/VoiceKernelHarness product already built from 24a6fcfa1 · installed · UNTOUCHED
                         bundle life.soullab.voicekernel.k00 · debug dylib CC0D3604-7902-373E-A2BB-2C093D9BF804

SEPARATE TEST AUTHORITY  ios/VoiceKernelDriver/            (new; XcodeGen project.yml; team ZVK2X646Z2)
                           DriverHost/                     empty SwiftUI app, bundle life.soullab.voicekernel.driverhost — exists only to host the UI-test bundle
                           DriverUITests/                  XCUITest bundle, bundle life.soullab.voicekernel.driver.xctest
                                                           imports XCTest only · does NOT import VoiceKernel · does NOT reference any harness or kernel source
                         scripts/witness/k00-driver-batch.sh   Mac orchestration + evidence custody (one command per batch)
                         scripts/witness/k00-ledger.py         mechanical classifier (the existing four classes) — reads journals, never steers
```

The driver touches the harness only through its **visible controls**, by their exact on-screen labels, which already exist and are unambiguous: `Enter conversation` / `Leave` (one button, label follows floor), `Export journal`, `Voice processing: ON (default)` / `Voice processing: OFF (control run)`. No accessibility identifiers are added — adding one would be a harness change and therefore a new subject; if label matching ever proves insufficient, that is **called out before implementation**, not patched.

## 2. Constraint compliance (founder's eight)

1. **External control only.** The test bundle's whole vocabulary: launch/activate, terminate, read `app.state`, read button labels, tap `Enter conversation` / `Leave` / `Export journal` / the VP button, dismiss the export share sheet (`Cancel`/`Close` on the system sheet). No VoiceKernel API, no internal state, no `UserDefaults`, no fault injection, no app-side hook.
2. **No debugger, no hidden launch conditions.** No `launchArguments`, no `launchEnvironment` on the `XCUIApplication` (asserted in the source gate). Xcode's test runner attaches to the **runner**, never to the harness. Mode L's testmanagerd launch is listed here as the one launch-condition difference; the founder accepts or rejects it by choosing Mode I/L.
3. **Cold process demonstrated, not assumed.** Before every sample the orchestrator runs on the Mac `xcrun devicectl device info processes --device A0736AC8-…` and requires **no** `VoiceKernelHarness` process; the test additionally asserts `XCUIApplication(bundleIdentifier:).state == .notRunning` before launching. If either check fails the sample is recorded as `PRECONDITION-FAILED` with the offending process list and **is not repaired and is not counted**; the batch continues to the declared N so no selection is introduced. Generation 1 is verified from the journal (`graph_started generation 1`, no prior generation).
4. **Mac owns orchestration and custody.** `k00-driver-batch.sh <stratum-label> <N> [--vp on|off] [--w4 <ms>]`: for i in 1…N → precondition check → `xcodebuild test-without-building -only-testing:DriverUITests/OneSample` (one sample per invocation; the strongest precondition evidence, ≈ 30–45 s overhead each) → list `tmp/` in the app container → copy the one new `kernel00-*.jsonl` → SHA-256 → `k00-ledger.py` row → append to `…/ledger/<stratum>.md` and keep the file beside it. The classifier is invoked after the batch decision was already made; it never steers. **The declared N is finished.** (Alternative, faster: one test method looping N samples with only the in-test `.notRunning` check — offered as an option; not recommended, because the Mac-side process check between samples is what the founder asked for.)
5. **Strata never merged.** `MANUAL-COLD` (run 6 + Stage 1/2: 7 of 9 gen-1 starts took) is closed and stays its own row. Every automated sample carries `AUTOMATED-COLD-ICON` or `AUTOMATED-COLD-LAUNCH`. The record shows the strata side by side; convergence or divergence between them is itself a finding.
6. **Explicit reinstall boundaries.** Stage A: N cold launches on the current installation, no reinstall. Stage B: reinstall the exact same product (`devicectl device install app`), capture install output + `dwarfdump --uuid` of the product (must read `CC0D3604-…`) + post-install `installed / not running`, then N cold launches. The orchestrator refuses to run Stage B without those three artefacts written into the ledger directory. No reinstall inside a stage.
7. **W4 from the journal.** The W4 test method taps `Enter conversation`, sleeps a target (default 500 ms), taps `Leave`, waits 3 s, taps `Export journal`. **PASS-for-exercising the condition = journal `leaveConversation` command time − `enterConversation` command time ≤ 2000 ms**, computed by the ledger script from the JSONL, never from the driver's sleep. The W4 verdict itself (nothing after `session_released`; orphan-free replay) is read from the same journal. If the automation cannot get under 2000 ms, that is recorded as a limitation of the UI/test path.
8. **No VoiceKernel decision-logic change.** Nothing in `ios/VoiceKernel/` or `ios/VoiceKernelHarness/` moves. The source gate pins a content hash of both trees at `24a6fcfa1` and fails on any change; it also asserts the driver imports only XCTest, never `VoiceKernel`, and sets no launch arguments/environment.

## 3. Batch shape (fixed before execution)

```
CALIBRATION   3 automated cold VP-ON samples (Mode I) → each journal must show: 13-step gen-1 trace, no input_format_before_vp,
              session id fresh, cold precondition met, export file produced. No mechanism inference. If Mode I cannot launch
              reliably, calibration is rerun in Mode L and the stratum label changes accordingly; the founder is told before Stage A.
STAGE A       30 cold VP-ON launches · current P5-B0 installation · no reinstall between samples
STAGE B       reinstall exact P5-B0 product · identity verified (install output · dylib UUID · installed/not running) · 30 cold VP-ON launches
STAGE C       only after A and B are read: founder decides whether to install the exact Phase-A product (4596b9bdb, dylib 11A057AA-…) and run a matched 30
W4-AUTO       one automated run meeting the journaled ≤ 2000 ms condition suffices to exercise the missing condition (target 500 ms; up to 3 attempts, each recorded)
VP-OFF        an optional matched control batch (N declared before running) is available with --vp off; not part of A/B unless the founder adds it
```

**Why 30.** The manual stratum shows 2 gen-1 failures in 9 (≈22%). With N = 30 per stage, an underlying 20% rate yields ≈ 6 failures; a stage with 0 failures against another with ≈ 6 is a gross difference the eye can see without a test, while differences of a few percentage points are **not** resolvable at this N and will not be claimed. N is fixed here; it is not chosen after seeing outcomes, and no early stopping exists.

## 4. Classifier (mechanical, closed vocabulary)

`k00-ledger.py` per journal: SHA-256 · session id · record count · subject check (gen-1 trace = 13 steps, `input_format_before_vp` absent) · cold check (`app_lifecycle didBecomeActive` first; `graph_started generation 1`) · class:
- `gen-1 listen` — first `floor_transition → listening` in generation 1 with no prior `recovery_requested`;
- `failure then recovery` — no gen-1 listen; `listening` reached in a later generation;
- `failure then degradation` — no listening; floor reaches `degraded`;
- `other observed shape` — anything else, with the reason printed (truncated export, interruption, precondition failure, …).
Plus evidence columns: `is_running_immediate`, `graph_started.engineRunning`, first-callback ms, listening ms, generations, hold seconds, and for W4 the Enter→Leave delta. It prints Markdown rows; it changes nothing.

## 5. What the driver cannot do, stated now

- It cannot make an icon tap identical to a human's; Mode I is the nearest reproduction and is labelled as its own stratum.
- It cannot read anything the harness does not show; if a needed control is not visible, that is reported, not worked around.
- It cannot prove the harness process was cold on the audio-server side; "cold" means process-absent, as in the manual protocol.
- It adds two signed bundles to the device; their presence is recorded as an environmental difference from the manual stratum.

## 6. Implementation plan (on acceptance; each a bounded act)

1. `ios/VoiceKernelDriver/` (XcodeGen `project.yml`, host app, UI-test bundle with `OneSample`, `W4Sample`, and calibration methods; Mode I primary, Mode L behind a test-plan flag) — no changes anywhere else in `ios/`.
2. `scripts/witness/k00-ledger.py` and `scripts/witness/k00-driver-batch.sh` (+ `k00-reinstall.sh` writing the three Stage-B artefacts).
3. Source gate additions: kernel + harness tree hashes pinned; driver imports XCTest only; no launch args/env; no `VoiceKernel` symbol in the driver.
4. **MAC-COMPILE-08 for the driver only** (`xcodegen generate`, signed build of host + runner; the harness is not rebuilt; the installed harness's identity re-read after the runner is installed).
5. Calibration ×3 → founder reads → Stage A → Stage B → founder decides Stage C. W4-AUTO whenever the founder places it.

Not authorized by this plan: any change to `ios/VoiceKernel/` or `ios/VoiceKernelHarness/`; E1–E4; B2; thresholds; recovery law; STT/TTS; architecture; a rebuilt harness.

## 7. Founder decisions requested before implementation

- **D1 Launch mode:** Mode I (SpringBoard icon tap, primary) with Mode L as calibration fallback — accept, or restrict to one.
- **D2 Sample invocation:** one `xcodebuild` invocation per sample with a Mac-side process check between samples (recommended), or one in-test loop with the in-test `.notRunning` check only.
- **D3 N = 30** per stage, fixed — accept or set another fixed N now.
- **D4 W4-AUTO target 500 ms, ≤ 3 attempts**, judged from the journal — accept or amend.
- **D5** Acceptance of §0's stated divergences (launch authority; two extra signed bundles on the device) as recorded strata rather than blockers.

## 8. ACCEPTED (founder, 2026-09-12) — decisions and the one added law

- **D1** Mode I primary, Mode L fallback — **never mixed within a calibration or stage**: if Mode I is unreliable, that calibration is declared invalid *as a driver calibration*, calibration reruns from zero in Mode L, and every subsequent sample in the programme is `AUTOMATED-COLD-LAUNCH`. No silent per-sample fallback.
- **D2** One `xcodebuild` invocation per sample with the Mac cold-process check before every sample. The in-test N-loop is not used in this programme.
- **D3** N = 30 per stage, fixed; no early stopping; characterization, not high-powered discrimination.
- **D4** W4-AUTO: target 500 ms, up to 3 recorded attempts, stop after the first journal-qualified `leaveConversation − enterConversation ≤ 2000 ms`; none qualifying → `W4 automated condition UNEXERCISED`. A qualifying timing does not itself pass W4; the post-release/orphan result is read separately from the same journal.
- **D5** The launch-authority difference and the two extra signed bundles are accepted as recorded strata, never merged with `MANUAL-COLD`.
- **Added law (gate):** *a failed driver operation is not an audio sample.* Icon not found, runner timeout, precondition unverifiable, journal not retrievable → `DRIVER/INFRASTRUCTURE FAILURE`, never `other observed shape`. `other observed shape` requires that the harness launched, entered the conversation, and produced a valid subject journal whose physiology simply did not fit the first three classes.
- The six-manual-journal placement question is **closed as moot**.

## 9. IMPLEMENTED (this session; nothing compiled here)

| Piece | Path | Notes |
|---|---|---|
| Driver project | `ios/VoiceKernelDriver/project.yml` | XcodeGen; targets `DriverHost` (empty placeholder app, `life.soullab.voicekernel.driverhost`) and `DriverUITests` (`bundle.ui-testing`, `life.soullab.voicekernel.driver.xctest`); scheme `DriverUITests`; **no package dependency on VoiceKernel** |
| Placeholder host | `ios/VoiceKernelDriver/DriverHost/DriverHostApp.swift` | shows one sentence; links nothing |
| Driver | `ios/VoiceKernelDriver/DriverUITests/K00DriverTests.swift` | `testOneSample` (precondition → launch → VP → Enter → hold → Export → terminate) · `testW4Sample` (Enter → target ms → Leave → 3 s → Export) · `testTerminateOnly`. Mode I = `XCUIDevice.press(.home)` → SpringBoard `icons["VoiceKernel K00"].tap()`; Mode L = `launch()` with no arguments/environment. Precondition `state != .notRunning` → `PRECONDITION-FAILED`. Every other failure → `DRIVER/INFRASTRUCTURE FAILURE: <why>`. Config reaches the **runner** via `TEST_RUNNER_K00_*` variables (never the harness). The share sheet is not used: Export writes the file first; the sample ends by terminating the harness. |
| Classifier | `scripts/witness/k00-ledger.py` | four classes + `DRIVER/INFRASTRUCTURE FAILURE` (no `enterConversation`) + `SUBJECT-MISMATCH` (gen-1 trace ≠ 13 steps for `--subject p5b0`, ≠ 14 for `--subject phase-a`); evidence columns; `--w4` adds `enterToLeaveMs` / `w4Qualified` and `afterRelease`. Self-check against the record: 6a → gen-1 listen · 6a-2 → failure then recovery · S1-3 → other observed shape · 6b → gen-1 listen (interruption noted) · run-5a → SUBJECT-MISMATCH under p5b0, gen-1 listen under phase-a · W4 → enterToLeaveMs=3565. Reads journals only; runs no process. |
| Orchestration | `scripts/witness/k00-driver-batch.sh <stratum> <N> [--vp] [--mode] [--hold] [--w4 MS] [--subject] [--ledger]` | `build-for-testing` once (driver only) → per sample: `devicectl` process check (lingering harness → one `testTerminateOnly` → still present → batch ABORTED as infrastructure) → one `test-without-building` invocation → diff of container `tmp/` → copy → ledger row. Writes `docs/programme/VOICE-2026/driver-ledger/<STRATUM>-<UTC>/{ledger.md, batch.log, journals/, sample-N-xcodebuild.log}` — committed from the Mac, so evidence custody never passes through chat. Finishes the declared N. Never reinstalls. W4 stops on first qualified journal (D4). |
| Reinstall boundary | `scripts/witness/k00-reinstall.sh <ledger-dir>` | writes `reinstall-<UTC>.txt` with the local product's `dwarfdump --uuid`, `codesign` identity, the `devicectl` install output and the post-install process check; the only place a reinstall happens |
| Gates | `__tests__/voice-kernel-00-source-gates.test.ts` — **35/35** | kernel tree (`Sources`, `Tests`, `Package.swift`) and harness tree (`Harness`, `project.yml`) **byte-pinned by content hash** at `24a6fcfa1`; driver: `import XCTest` only, no VoiceKernel symbol, no `launchArguments`/`launchEnvironment`/`UserDefaults`/dynamic loading, drives `life.soullab.voicekernel.k00` by bundle id, uses only the five visible labels, asserts the cold precondition, names driver failures; classifier closed and process-free; batch script checks the process every sample, finishes N, uses one invocation per sample, never installs |

Identifiers the Mac side needs: devicectl device `A0736AC8-793B-516F-AC72-C076DB6CEE38` (env `K00_DEVICE`), xcodebuild destination `00008140-00163D9922E0801C` (env `K00_XCODE_DEST`), team `ZVK2X646Z2` (env `K00_TEAM`). Mode I requires the `VoiceKernel K00` icon to be on the **first Home Screen page** (the driver does not swipe pages; an absent icon is a driver failure, not a sample).

**Owed next:** MAC-COMPILE-08 (driver only: `xcodegen generate` + `build-for-testing`) → re-read installed harness identity → `k00-driver-batch.sh CALIBRATION 3` → founder reads the calibration ledger → Stage A.

## 10. MAC-COMPILE-08 (driver only) — attempt 1, 2026-09-12 17:27 local — BUILD FAILED (C-D1: test bundle had no Info.plist)

- Checkout `8183bbe6a` · source gate **35/35** on the Mac · `xcodegen generate` PASS (`Created project at …/VoiceKernelDriver.xcodeproj`).
- `xcodebuild build-for-testing … -scheme DriverUITests` → `The following build commands failed: Building project VoiceKernelDriver for testing with scheme DriverUITests (1 failure)`. Only the summary was captured; the error lines are owed from `driver-ledger/CALIBRATION-*/build-for-testing.log`.
- The batch script, run afterwards, recorded `DRIVER/INFRASTRUCTURE FAILURE: build-for-testing failed` and produced **no sample** — the added law held on its first exercise.
- The ledger commit failed pre-commit (`check-branch-allowed.sh … usage`) because HEAD was detached at `8183bbe6a`; remedy: `git checkout claude/voice-2026-census-01` (same commit) before committing ledgers.
- **Installed-harness identity re-read (PASS):** local product dylib `CC0D3604-7902-373E-A2BB-2C093D9BF804`; device lists `VoiceKernel K00 · life.soullab.voicekernel.k00 · 0.0.1 · 1`. The app under test is unchanged.
- A manual session on this build arrived alongside (`K00-4d509ee9`, origin unstated): filed as `MANUAL-COLD-EXTRA`, outside every stage, not counted for rate estimation:
| MANUAL-COLD-EXTRA |  |  | `K00-4d509ee9` (`KERNEL-00_WITNESS_2026-09-12_manual-extra_K00-4d509ee9_vpON.jsonl`) | 68 | `6950167bf8829a79614495caabcced69e461ea047d59eb917f0e1bd44f66a711` | **gen-1 listen** | cold=True · isRunningImmediate=true · graphStartedRunning=true · firstCallbackMs=98 · generations=1 · holdS=31.8 · listeningMs=437 |

**C-D1 (driver-only defect, attempt 1):** `error: Cannot code sign because the target does not have an Info.plist file and one is not being generated automatically … (in target 'DriverUITests')`. Fix: `GENERATE_INFOPLIST_FILE: YES` on the `DriverUITests` target in `ios/VoiceKernelDriver/project.yml`. Nothing else changed; the kernel/harness pins are unaffected (gate re-run here).
**Checkout note:** `git checkout claude/voice-2026-census-01` in `~/MAIA-SOVEREIGN` is refused because another session holds a worktree of that branch at `/private/tmp/voice-driver-01`. Mac-side work and ledger commits go through that worktree (`git -C /private/tmp/voice-driver-01 pull --ff-only`), or through a local branch created from the SHA in the main checkout and pushed as `HEAD:claude/voice-2026-census-01`.
