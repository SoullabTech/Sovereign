# PRE-WITNESS-05 · P5-B0 — pre-VP format-read removal control — 2026-09-12 — IMPLEMENTED, NOT COMPILED

**Authority:** founder ruling of 2026-09-12 (run-5 record §10, ruling 2), conditional on ruling 1; condition satisfied by run-5 §11 (repro #1 and #2 both listened). **Selected ahead of E1–E4**, which stay held.

## 1. Question

Is the Phase-A read `input.outputFormat(forBus: 0)` placed **before** `setVoiceProcessingEnabled` the cause of the VP-ON engine becoming running on this device/runtime (P5-F1)?

## 2. Subject

Identical to the Phase-A subject `4596b9bdb` except that **one read is removed**. Source delta in `ios/VoiceKernel/Sources/VoiceKernel/AudioGraph.swift`:

- removed: `let before = input.outputFormat(forBus: 0)` and its `trace(.inputFormatBeforeVP, …)` line, both inside `start(voiceProcessing:clock:trace:)` between `engine_created` and `vp_enable_begin`;
- removed: the enum case `StartTraceStep.inputFormatBeforeVP` (`"input_format_before_vp"`) — the seam no longer exists on this subject, so the closed set is **13** steps; a journal from this subject is recognisable by the absence of that step between `engine_created` and `vp_enable_begin`;
- comments only otherwise.

**Kept, verbatim:** every other Phase-A trace step and read (`vp_enable_return` elapsed + `isVoiceProcessingEnabled` read-back, `output_connected`, `input_format_after_vp`, tap/observer/prepare/start steps with elapsed ms, `is_running_immediate`), the kernel's `graph_start_trace` / `engine_running_observed` / `first_input_callback` journaling, the §3 format guard, the PW-03 exit guard and bounded route recovery, the PW-04 one-shot classifier and deferral, thresholds, RecoveryPolicy, the mutating call order (`setVoiceProcessingEnabled → attach → connect → requireValid → input installTap → player installTap → addObserver → prepare → start`, each once). The first touch of the input format on this subject is `input_format_after_vp`, exactly the `35b0f61d0` position. No harness change.

## 3. Gates and tests (this session; nothing compiled here)

- Source gate `__tests__/voice-kernel-00-source-gates.test.ts`: **32/32** — the Phase-A block now pins the enum at 13 and no longer expects the pre-VP read; new **P5-B0 block ×4**: no format read of any kind precedes `setVoiceProcessingEnabled` in `start()` · exactly one `input.outputFormat(forBus: 0)` remains, after VP enable and before `requireValid` · the seam `input_format_before_vp` is absent from every non-test kernel/harness source (comments stripped; the XCTest negative assertion is excluded by scope — the C21 lesson) · all other Phase-A reads kept.
- `swift test`: `StartTraceTests` asserts the 13-step set and the absence of `input_format_before_vp` (expected **30/30** on the Mac; count unchanged, one test rewritten).

## 4. Predeclared decision table (founder, verbatim substance)

| Device outcome on this subject | Reading |
|---|---|
| VP ON **fails** to become running / listen again (the run-2/3/4 shape) | the pre-VP read is **strongly causal**; the initialization dependency is promoted from inference to finding |
| VP ON **still listens** | the read is **falsified as a sufficient cause**; the mechanism stays open |
| **mixed** across sessions | nondeterminism; no mechanism claim |

VP OFF control runs alongside; expected unchanged (listening ≈ 330 ms).

## 5. What happens next (in order; each a founder act)

1. **MAC-COMPILE-07** on exactly this SHA: `swift build` · `swift test` (expect 30/30) · gate (expect 32/32) · `xcodegen generate` · unsigned + signed device build · debug dylib UUID recorded → `KERNEL-00_MAC-COMPILE-07_2026-09-12.md`.
2. Founder acceptance → install (acceptance by conduct) → **run 6**: (6a) force-quit → icon → VP ON → Enter once → ~15 s → Export; (6b) force-quit → icon → VP OFF → Enter once → ~15 s → Export; if 6a listens, a second VP-ON session (6a-2) so "mixed" is distinguishable from "still listens". Optionally the still-owed W4 in-flight (Leave **within 2 s** of Enter) as its own session.
3. Record `KERNEL-00_WITNESS_2026-09-12_run6.md` per the table in §4; founder attestation.

Not authorized: E1–E4 · B2 · thresholds · STT/TTS · any second change to `start()`.
