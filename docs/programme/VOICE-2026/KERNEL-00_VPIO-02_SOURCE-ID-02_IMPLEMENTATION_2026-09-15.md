# KERNEL-00 · VPIO-02 · `SOURCE-ID-02` — IMPLEMENTATION RECORD (code/offline only · 2026-09-15)

**Authority:** founder ruling on `d02ce1094` (design record §9–§11; discriminator plan §18.34): *SOURCE-ID-02 implementation is now AUTHORIZED, code/offline only* — diff budget = the ruled Q4 surface; return before any Mac compile with the named offline evidence. **Nothing else is open:** Mac compile CLOSED · install CLOSED · SID entry witness REQUIRED · CLOSED · S-b N=10 CLOSED · S-a NOT OPEN · S3 CLOSED · device execution authority NONE.

**Implementation SHA: `6f0e2e0b25ff74636dab21b6c4239865ba51c691`** (one commit, instrument + organism + tests + gate together, gate read green before it). ⛔ **NOT COMPILED** — no Swift toolchain in this container; the Swift below earns itself on the Mac under its own MAC-COMPILE authority, which this record does not open. **Subject by custody:** bundle `life.soullab.voicekernel.vpio02sid` · display `VoiceKernel VPIO-02-SID` · UUID / dylib SHA / executable SHA / manifest **UNRECORDED** (they exist only after a compile; the reinstall refuses the subject with `pins-unrecorded` until then).

---

## 1. Diff inventory (exactly the ruled surface)

| File | Lines | What |
|---|---|---|
| `ios/VoiceKernel/Sources/VoiceKernel/AudioGraph.swift` | +147 | `SourceObservation` (7 magnitudes in `SIMD8<Double>` lanes, frame bookkeeping) · `SourceEstimator` (fixed-bin Goertzel over a Hann frame of 4 callbacks; coefficients `2cos(ω)` from the OBSERVED rate; Hann by cos/sin recurrence; state = three `SIMD8` + scalars; no allocation, no lock, no unit call; callback-size change → frame discarded + `reset` flag) · `SourceSignature.modulationIndex` (`m2 = 2·|X(2 Hz)|/X(0)` over frame time) · one hook in `pullInput` after `measure()` on the same consumed buffer; `onSource` fired outside the buffer closure · estimator constructed after `try hw.requireValid()` and before `callbacks_armed` (no trace step; the fourteen seams unchanged, gate-proven) |
| `ios/VoiceKernel/Sources/VoiceKernel/VoiceKernel.swift` | +50 −1 | `ag.onSource` hop (25/s) · `SourceAggregate` (per-bin sum/max/min, **ordered** `e997`/`e440` envelopes, cap 64) · `handleSource` (generation gate → `sourceAgg.add`, nothing else) · `input_source_sample` journalled once, component `SourceEvidence`, cause `sample`, from the existing `sampleIfDue` tick beside an unchanged `input_health_sample`; **never assigned to `lastObservationSeq`** (never a causal parent) · resets with the health aggregate at generation begin and at each tick |
| `ios/VoiceKernel/Tests/VoiceKernelTests/PureLogicTests.swift` | +113 | `SourceEstimatorTests` ×6 (§3) |
| `ios/VoiceKernelHarness/project.yml` | 2 lines | bundle `life.soullab.voicekernel.vpio02sid` · display `VoiceKernel VPIO-02-SID` |
| `scripts/witness/fixtures/k00-sid-nearend-997hz-gated-2hz-180s.wav` + `.sha256` | new | 997 Hz · 180 s · mono · 48 kHz · PCM16 · peak 0.20 FS · 250 ms on / 250 ms off (2 Hz, 50 % duty) · 2 ms raised-cosine edges · OFF halves digital zero · **SHA `30d51cf4b7527d28131bd9c2535c6bd4dcd2403c8dc0343fc045f6f6959875eb`** (generator in §4, reproducible) |
| `scripts/witness/k00-source-ledger.py` | new | evidence-only source reader, §10.B law as ruled §11 (§5) |
| `scripts/witness/k00-driver-batch.sh` | +26 −2 | SID subject row · `CLASSIFIER_SUBJECT` mapping (`vpio-02-sid → vpio-02`) on the entry-classifier line · second header line `classifierSubject=` when it differs · token `sid-nearend-gated` (lawful only with `--subject vpio-02-sid`); **`s2-nearend` now refused with `vpio-02-sid`** (the stationary S-a arm is NOT OPEN on the SID subject, as ruled) · `SOURCE_LEDGER` only under the gated token · source reader invoked inside the output guard after the two frozen readers |
| `scripts/witness/k00-reinstall.sh` | +13 −1 | `VPIO02SID_*` constants (bundle set; five pins EMPTY) · SID case row · `pins-unrecorded` refusal before any device read |
| `ios/VoiceKernelDriver/DriverUITests/K00DriverTests.swift` | +4 −1 | SID subject row + closed-set string; the app under test still receives nothing (⚠️ named by me as a custody declaration *necessary to recognize the subject* — the batch launches by the driver's table; founder may reject) |
| `__tests__/voice-kernel-00-source-gates.test.ts` | +237 | gate by history (§6) |

**Byte-identical to `ac12dedf4` (gate-proven, per file):** `AudioSessionAuthority.swift` · `HealthSupervisor.swift` · `Journal.swift` · `KernelState.swift` · `RecoveryPolicy.swift` · `Replay.swift` · `RouteComparison.swift` · `StateProjection.swift` · `Package.swift` · every `Harness/*.swift`. **Byte-identical to `b198e2e37`:** `k00-ledger.py` · `k00-output-ledger.py` (frozen readers). `InputObservation`, `classify`, thresholds, health meaning, recovery, projection, route/session semantics, harness behaviour, installed `.vpio02`: untouched.

## 2. Two explicit substitutions on historical instrument lines (pinned, not hidden)

The batch and reinstall preservation law (*every historical executable line verbatim, in order*) is kept with exactly two named substitutions, each normalized explicitly in the gate before the in-order scan: **(a)** the entry-classifier call receives `--subject "$CLASSIFIER_SUBJECT"` (== `$SUBJECT` for every historical subject; `vpio-02` for `vpio-02-sid`, because the frozen classifier knows no SID subject and the SID trace is the same fourteen seams — *declared custody + trace compatibility = subject identity*, founder ruling 2026-09-14); **(b)** the unknown-subject refusal strings name the five-subject closed set. Every other historical line is verbatim; every added line matches a closed allow-list (`SID_ALLOWED`) or sits inside the SID blocks. The driver's and batch's historical comment/header lines were restored verbatim after a first draft edited them (the gate caught it; the SID information now travels on added lines).

## 3. Offline evidence returned (as the ruling required)

| Obligation | Result |
|---|---|
| Source estimator unit vectors | `SourceEstimatorTests` (Swift, **not yet executed** — no toolchain): 997 Hz at rms 1e-3 → own bin 7.07e-4 ± 2 %, controls ≤ −60 dB; silence → 0; bins exactly the seven; frame closes every 4th callback; frame-reset path |
| Leakage / tail arithmetic | test pins the four amplitude coefficients and asserts each own harmonic at magnitude 0.1 leaks into 997 within +10 % of its coefficient and ≥ 50 % of it (a bound of the right order, not a fiction); the tail `2.82e-6 × e880Max` is pinned in the reader |
| Deterministic 2 Hz signature incl. the `own_modulated` veto | Swift: ideal gate (14 phases, K = 25 and 26) ≥ 1.15 and ≤ 1.40; steady = 0; zero energy → nil; 7 Hz ripple + drift < 0.9; a gated 997 + continuous 440 through the whole estimator → `m2_997 ≥ 0.9`, `m2_440 < 0.2`. Reader: case 16 (`own_modulated`) and case 15 (`signature_absent`) |
| Every source-reader path | `--selftest` **20/20**: no_source_evidence · geometry · V1 (twice) · healthy-only baseline · V2 · no full window · SURVIVES · SUPPRESSED · capture-not-intact → floor · mixed → CHARACTERIZE-SRC · between · floor by noise · floor by LEAKAGE · signature_absent · own_modulated · frameReset · residual descriptive · residual rendered · never PASS/FAIL |
| Gated-fixture checks | gate: header · 8 640 000 frames · first two seconds sample-exact (±1 LSB) against the generator with edges · OFF halves exactly zero · full-file peak ±6553 · SHA = sidecar = batch pin |
| Frozen-reader row-identical replay | both frozen readers are byte-identical to `b198e2e37`, so replay is identity; the gate's corpus-partition test still reproduces every ledgered directory row-for-row (16 populations, 559 journals); the source reader over all **61 tracked VPIO-02 journals → 61 × `no_source_evidence`**, never PASS/FAIL |
| Byte-identity of the ruled invariant files | gate (§1 list), per file against `ac12dedf4` |
| VOICE source gate | **82/82** (was 76): 12 frozen-subject pins moved to history + 6 new SOURCE-ID-02 obligations (§6); read green before the implementation commit |

## 4. Fixture generator (reproducible; the SHA is the custody)

```python
fs=48000; sec=180; f=997.0; peak=round(0.20*32767)          # 6553
period=fs//2; on=fs//4; edge=int(fs*0.002)                   # 24000 · 12000 · 96
for i in range(fs*sec):
    ph=i%period
    if ph<on:
        g=1.0
        if ph<edge: g=0.5-0.5*cos(pi*ph/edge)
        elif ph>=on-edge: g=0.5-0.5*cos(pi*(on-1-ph)/edge)
    else: g=0.0
    sample=int(round(peak*g*sin(2*pi*f*i/fs)))               # PCM16 LE, mono, 48 kHz; RIFF/WAVE 44-byte header
```

## 5. The reader's law as implemented (design §10.B + ruling §11, verbatim in the module docstring)

Constants: `A440 2.16e-5 · A880 2.77e-3 · A1320 4.00e-5 · A1760 1.12e-5 · TAIL 2.82e-6 (× e880Max)` · `VIS 10 · SURV 0.1 · SUPP 0.01 · NOISE 10 · M2 0.9 · CB_MIN 90 · MIN_FRAMES 20` · geometry pinned `48 000 Hz / 1 920 frames` (any other geometry → UNMEASURED-SRC: geometry, never approximated). `NEAR-END-SURVIVES iff e997Mean ≥ 0.1·B997 ∧ e997Mean ≥ 10·F_w ∧ m2_997 ≥ 0.9 ∧ m2_440 < 0.9 ∧ frameReset = 0`; `NEAR-END-SUPPRESSED iff e997Mean < 0.01·B997 ∧ callbacks ≥ 90 ∧ ioRunning`; `INDETERMINATE-SRC: frameReset | between | floor | signature_absent | own_modulated`; own-playback residual descriptive on every window; row = SURVIVES / SUPPRESSED / CHARACTERIZE-SRC / UNMEASURED-SRC. Pairing: each `input_source_sample` with the `input_health_sample` of the same generation and `windowMs` at most three records earlier (the tick writes them adjacently).

## 6. Gate by history (what moved, and why each is lawful)

The twelve pins that asserted *the working tree is the frozen VPIO-02 subject* now assert *the frozen VPIO-02 subject is `ac12dedf4` in history* and *the working tree differs from it in exactly the four ruled files* (organism), or *the instrument differs from `b198e2e37`/`8b111709b`/`08483cfe4`/`83a382a14` only by SID-declaring lines* (`sidOnlyDelta`, closed allow-list). New block (6 obligations): seven bins exact · construction after the guard, before arming · trace enum unchanged · counts of every `AudioUnit*/AudioOutputUnit*/AudioComponent*` call, `import`, `lock.lock()` and `NSLock(` equal to history; no `DispatchQueue/Timer/Thread/Semaphore` · hook after `measure()`, emission outside the closure · kernel: one `input_source_sample`, component `SourceEvidence`, never `lastObservationSeq`, health record byte-identical, `handleSource` touches only `sourceAgg`, no `source` symbol in supervisor/policy/projection/state/replay · tests named + coefficients pinned · fixture checks · reader constants + vocabulary + self-test 20/20 + the 61-journal `no_source_evidence` regression · batch/reinstall/driver custody declarations + frozen readers byte-identical.

## 7. Deviations and flags for the founder

1. **Component name `SourceEvidence`** on the record (design §2.2 had written `HealthSupervisor`): the evidence is not the supervisor's and must not read as if it were; a one-word change if rejected.
2. **Driver row** added (§1) — argued as the minimum custody declaration for the batch to launch the SID bundle; not in the ruling's literal list.
3. **`s2-nearend` refused on the SID subject** — an instrument refusal implementing "S-a NOT OPEN"; removable by ruling.
4. **Compile-era risk, named, not resolved here:** `SIMD8<Double>` arithmetic, `.squareRoot()` on SIMD, `pointwiseMin/Max`, and the optional-chained mutating `sourceEstimator?.consume(base, n)` inside the buffer closure are all standard Swift 5.9 but unexercised until MAC-COMPILE; an importer/syntax rejection would be a bounded compile-era defect of the predeclared class (C-V1 precedent), never an API.
5. The SID pins are empty by design; **any reinstall with `--subject vpio-02-sid` refuses (`pins-unrecorded`) until a MAC-COMPILE records them** — fail-closed, gate-pinned.

## 8. Standing after this record (nothing opened)

```text
SOURCE-ID-02 implementation      LANDED · 6f0e2e0b2 · NOT COMPILED · returned
offline evidence                 §3 (Swift tests written, not executed; reader 20/20; corpus 61/61; gate 82/82)
SID subject                      life.soullab.voicekernel.vpio02sid · VoiceKernel VPIO-02-SID · identity UNRECORDED
SID MAC-COMPILE                  CLOSED (own founder act, on exactly the accepted SHA)
SID FIRST-INSTALL                CLOSED
SID ENTRY WITNESS                REQUIRED · CLOSED
S-b N=10 population              CLOSED
S-a stationary arm               NOT OPEN (instrument refuses it on the SID subject)
S3                               CLOSED
K00-06 built-in                  CHARACTERIZE ONLY · INCOMPLETE
KERNEL-00                        NOT ACCEPTED
device execution authority       NONE
```

---

## 9. Founder ruling on `6f0e2e0b2` (2026-09-15) → `SOURCE-ID-02A` offline repair LANDED at `f0c6ae13b` · implementation still NOT ACCEPTED for MAC-COMPILE

**Ruling (substance verbatim):** the implementation surface is substantially right and confined to the ruled files; the five flags are disposed — `SourceEvidence` ACCEPTED · driver row ACCEPTED (custody/selection, not app behaviour) · S-a refusal ACCEPTED IN PRINCIPLE · compile-era Swift risks DEFERRED TO MAC-COMPILE · empty SID pins ACCEPTED (`pins-unrecorded` is the correct fail-closed condition); the estimator is faithful (consumed buffer, four callbacks, observed-rate coefficients, ordered 997/440 envelopes). **Two real offline defects and one control-shape ambiguity** must be fixed before Swift enters the toolchain: **Defect 1** — the batch's historical two-stage parser refused `sid-nearend-gated` as *unknown stimulus* before the SID branch could admit it, so the record's claim that the gated token was lawful on the SID subject was **false in executable behaviour**; **Defect 2** — the reader evaluated SURVIVES and SUPPRESSED before `frameReset`, so a suppressed-amplitude window with healthy capture and a reset inside it read `NEAR-END-SUPPRESSED`; **Ambiguity** — `m2own is None or m2own < M2` let an absent/malformed own-tone control qualify like a measured quiet one. Authority: exactly one narrow offline carrier, `SOURCE-ID-02A` (batch dispatch · reader precedence · undefined-control handling · self-test coverage · gate pins · records); no Swift change, no new bins/thresholds, no compile/install/device/witness/playback/population.

**Repair, at `f0c6ae13b88db29cbd1537bec585d98376c8bc4f` (Swift untouched — `git diff` names only the batch, the reader and the gate):**

1. **Batch — one closed dispatch** (replacing the two-stage parser):
   ```text
   ""                  no stimulus
   s2-nearend          only --subject vpio-02      (the stationary S-a arm is NOT OPEN on the SID subject)
   sid-nearend-gated   only --subject vpio-02-sid
   anything else       refused; the refusal names the two lawful pairings
   ```
   Gate: exactly one `case "$STIMULUS" in`; both guarded branches precede the catch-all; no unguarded `s2-nearend) ;;`; the old "the only token is s2-nearend" text absent; the dispatch precedes the device lock. (The historical single-line `case` from `b198e2e37` is superseded by this dispatch; it was never a `8b111709b` line, so the preservation law over the K00-05/06 base is unaffected, and every added line is inside the dispatch or SID-allowed.)
2. **Reader — `frameReset` first:** `frameReset > 0 → INDETERMINATE-SRC: frameReset` is evaluated before SURVIVES and SUPPRESSED. New self-test 21 (suppressed amplitude + callbacks ≥ 90 + `ioRunning` + `frameReset=1` → `frameReset`, never SUPPRESSED).
3. **Reader — own-tone control law:** `control = clear` iff `m2_440` numeric and `< 0.9`, or `m2_440` undefined with a complete record (all seven bins' Mean/Max/Min parseable) and `e440Mean < max(C_base, C_w)` (no own-band energy to modulate); numeric and `≥ 0.9` → `own_modulated`; absent/malformed while own-band energy is material → **`INDETERMINATE-SRC: own_control_unmeasured`**. `control == clear` is part of SURVIVES; missing control evidence never earns it. Self-tests 22 (undefined + quiet → SURVIVES), 23 (undefined + material → `own_control_unmeasured`), 24 (own-band keys missing → `own_control_unmeasured`). Vocabulary gains exactly that one reason code.
4. **Self-test 24/24; gate 82/82** (pins for the dispatch, the law order `frameReset → SURVIVES → SUPPRESSED → …`, the control expression, the new reason code and the four new case names); 61 tracked VPIO-02 journals still `no_source_evidence`.

**Standing after §9:** implementation `6f0e2e0b2` + repair `f0c6ae13b` RETURNED · NOT YET ACCEPTED for MAC-COMPILE (the founder adjudicates the whole after this carrier) · core Swift/source design ACCEPTED IN PRINCIPLE · MAC-COMPILE · FIRST-INSTALL · SID ENTRY WITNESS (required) · S-b N=10 CLOSED · S-a NOT OPEN · S3 CLOSED · K00-06 built-in CHARACTERIZE ONLY · INCOMPLETE · KERNEL-00 NOT ACCEPTED · device execution authority NONE.

---

## 10. Founder adjudication (2026-09-15): `SOURCE-ID-02` ACCEPTED · `SOURCE-ID-02A` ACCEPTED · accepted code state `f0c6ae13b88db29cbd1537bec585d98376c8bc4f` · **SID MAC-COMPILE OPEN** (the only newly authorized act) — pinned §10.2, NOT YET EXECUTED

### 10.1 Ruling as captured (substance verbatim)

Accepted as repaired: one closed stimulus dispatch with `sid-nearend-gated` reachable only under `vpio-02-sid` · `frameReset` before both attribution verdicts · missing/undefined own-tone control never silently earns SURVIVES, `own_control_unmeasured` carries the unresolved case · new cases gate-pinned, repair limited to batch + reader + gate, Swift untouched · the record preserves that the implementation was not accepted before this ruling. The five flags stay disposed as previously ruled.

```text
SOURCE-ID-02          ACCEPTED
SOURCE-ID-02A         ACCEPTED
accepted code state   f0c6ae13b88db29cbd1537bec585d98376c8bc4f
offline gate          82/82
source reader         24/24
```

**SID MAC-COMPILE — OPEN.** Purpose, narrow: establish that the accepted SID subject compiles and tests on the Mac toolchain and record its product identity. It installs and launches nothing. **Subject = exactly `f0c6ae13b88db29cbd1537bec585d98376c8bc4f`** (the later `94f4cb4af` carrier is records-only and does not alter the compiled subject). Steps: (1) HEAD exact + tree clean · (2) VOICE gate 82/82 · (3) `swift test` for `ios/VoiceKernel` · (4) `xcodegen` from `project.yml` · (5) device build of the SID harness, no install, no launch · (6) identity: bundle `life.soullab.voicekernel.vpio02sid` · display `VoiceKernel VPIO-02-SID` · `VoiceKernelHarness.debug.dylib` UUID · dylib SHA-256 · executable SHA-256 · complete product manifest + manifest SHA-256 + file count · (7) the built source/test surface is still the accepted one after the build · (8) return every compile/test/custody line in a record. The Swift unit tests matter particularly: written, never executed (seven bins · leakage coefficients · reset · 2 Hz discrimination · the gated-997 / continuous-440 control case).

PASS = HEAD exact ∧ tree clean before build ∧ gate 82/82 ∧ `swift test` zero failures ∧ project generation PASS ∧ device build PASS ∧ bundle/display exact ∧ dylib UUID, dylib SHA, executable SHA readable ∧ manifest complete + sealed ∧ device install 0 · launch 0 · sample 0. **STOP** on any Swift compile/test failure, project-generation failure, signing/build failure, unreadable identity field, unexpected bundle identity or source drift. A compiler error in the predeclared risk area (`SIMD8`, pointwise operations, the optional-mutating estimator call, …) is returned as a **bounded compile defect — never repaired inside the compile act**; a repair needs its own ruling. **Do not fill the reinstall pins because the build produced them**: the compile records the identity; pinning the install instrument follows only after the compile evidence is accepted.

Still closed: FIRST-INSTALL · reinstall pin mutation (pending compile acceptance) · SID ENTRY WITNESS (REQUIRED) · S-b N=10 · S-a (NOT OPEN) · S3 · K00-06 built-in CHARACTERIZE ONLY · INCOMPLETE · KERNEL-00 NOT ACCEPTED · device execution NONE.

### 10.2 Execution pin — `SID MAC-COMPILE-01` (Mac Studio terminal directly; paste-able; zsh-safe — no `#` on command lines; every line reused from the accepted VPIO-02 MAC-COMPILE-01 / repro-build recipe, with the SHA, the paths and the SID identity substituted)

Toolchain expected (recorded, not re-pinned here): Xcode 26.3 (17C529) · Swift 6.2.4 · XcodeGen 2.46.0 · SDK iphoneos26.2; destination = the Xcode id `00008140-00163D9922E0801C` (never the devicectl id); team `ZVK2X646Z2`. The signed build compiles for the paired device and produces a signed product; it does not install, launch or sample. Evidence directory: `$OUT` (outside the worktree); return on a `feature/*` branch → cherry-pick here.

```bash
set -e
SHA=f0c6ae13b88db29cbd1537bec585d98376c8bc4f
STAMP=$(date -u +%Y%m%dT%H%M%SZ)
WT=/private/tmp/sid-mac-compile-01-$SHA
DD=$WT-derived
OUT=/private/tmp/sid-mac-compile-01-out-$STAMP
mkdir -p "$OUT"
test ! -e "$WT"
cd /Users/soullab/MAIA-SOVEREIGN
git fetch origin claude/voice-2026-census-01
git worktree add --detach "$WT" "$SHA"
cd "$WT"
git rev-parse HEAD | tee "$OUT/head.txt"
test "$(git rev-parse HEAD)" = "$SHA"
git status --porcelain | tee "$OUT/status-before.txt"
test -z "$(git status --porcelain)"
ln -s /Users/soullab/MAIA-SOVEREIGN/node_modules node_modules
xcodebuild -version | tee "$OUT/toolchain.txt"
xcodegen --version | tee -a "$OUT/toolchain.txt"
xcrun swift --version 2>&1 | head -1 | tee -a "$OUT/toolchain.txt"
npx jest --config jest.config.js __tests__/voice-kernel-00-source-gates.test.ts 2>&1 | tee "$OUT/gate.log" | tail -5
grep -E 'Tests:\s+82 passed, 82 total' "$OUT/gate.log"
( cd ios/VoiceKernel && xcrun swift build 2>&1 ) | tee "$OUT/swift-build.log" | tail -3
( cd ios/VoiceKernel && xcrun swift test 2>&1 ) | tee "$OUT/swift-test.log" | grep -E 'Executed|error|failed' | tail -4
grep -E 'Executed [0-9]+ tests, with 0 failures' "$OUT/swift-test.log"
grep -E 'SourceEstimatorTests' "$OUT/swift-test.log" | grep -E "passed" | wc -l | tee "$OUT/source-tests-passed-count.txt"
( cd ios/VoiceKernelHarness && xcodegen generate 2>&1 ) | tee "$OUT/xcodegen.log" | tail -2
( cd ios/VoiceKernelHarness && xcodebuild -project VoiceKernelHarness.xcodeproj -scheme VoiceKernelHarness -destination 'generic/platform=iOS' -derivedDataPath "$DD" CODE_SIGNING_ALLOWED=NO build ) > "$OUT/xcodebuild-unsigned.log" 2>&1 || true
grep -E '\*\* BUILD (SUCCEEDED|FAILED) \*\*' "$OUT/xcodebuild-unsigned.log" | tee -a "$OUT/build-results.txt"
grep -q 'BUILD SUCCEEDED' "$OUT/xcodebuild-unsigned.log"
( cd ios/VoiceKernelHarness && xcodebuild -project VoiceKernelHarness.xcodeproj -scheme VoiceKernelHarness -destination id=00008140-00163D9922E0801C -derivedDataPath "$DD" DEVELOPMENT_TEAM=ZVK2X646Z2 CODE_SIGN_STYLE=Automatic -allowProvisioningUpdates build ) > "$OUT/xcodebuild-signed.log" 2>&1 || true
grep -E '\*\* BUILD (SUCCEEDED|FAILED) \*\*' "$OUT/xcodebuild-signed.log" | tee -a "$OUT/build-results.txt"
grep -q 'BUILD SUCCEEDED' "$OUT/xcodebuild-signed.log"
P="$DD/Build/Products/Debug-iphoneos/VoiceKernelHarness.app"
/usr/libexec/PlistBuddy -c 'Print CFBundleIdentifier' -c 'Print CFBundleDisplayName' "$P/Info.plist" | tee "$OUT/identity.txt"
test "$(/usr/libexec/PlistBuddy -c 'Print CFBundleIdentifier' "$P/Info.plist")" = life.soullab.voicekernel.vpio02sid
test "$(/usr/libexec/PlistBuddy -c 'Print CFBundleDisplayName' "$P/Info.plist")" = 'VoiceKernel VPIO-02-SID'
dwarfdump --uuid "$P/VoiceKernelHarness.debug.dylib" | tee -a "$OUT/identity.txt"
shasum -a 256 "$P/VoiceKernelHarness.debug.dylib" "$P/VoiceKernelHarness" | tee -a "$OUT/identity.txt"
( cd "$P" && find . -type f | LC_ALL=C sort | xargs shasum -a 256 ) > "$OUT/SID-MAC-COMPILE-01.manifest.sha256"
shasum -a 256 "$OUT/SID-MAC-COMPILE-01.manifest.sha256" | tee -a "$OUT/identity.txt"
wc -l "$OUT/SID-MAC-COMPILE-01.manifest.sha256" | tee -a "$OUT/identity.txt"
codesign -dv --verbose=2 "$P" 2>&1 | grep -E 'Identifier|TeamIdentifier|Authority' | tee -a "$OUT/identity.txt"
git rev-parse HEAD | tee "$OUT/head-after.txt"
test "$(git rev-parse HEAD)" = "$SHA"
git status --porcelain | tee "$OUT/status-after.txt"
git diff --stat | tee "$OUT/diff-after.txt"
git diff --quiet -- ios/VoiceKernel ios/VoiceKernelHarness/project.yml scripts __tests__
( cd "$OUT" && find . -type f | LC_ALL=C sort | xargs shasum -a 256 ) > "$OUT/SHA256SUMS.compile"
echo "SID-MAC-COMPILE-01 $STAMP subject $SHA out $OUT"
```

Reading law for the return: every `test` line passing = the corresponding PASS criterion; `status-after.txt` may show only the known `xcodegen` footprint (`ios/VoiceKernelHarness/Harness/Info.plist` regenerated) and untracked build/evidence artefacts — the `git diff --quiet` line proves the accepted source, project.yml, instruments and gate did not move; anything else = STOP (source drift). The first failing line stops the paste (`set -e`): return `$OUT` as it stands with the failing line named; no second attempt under this authority. **Nothing in the block installs, launches, samples or writes a reinstall pin.**

**Standing after §10:** SOURCE-ID-02 + 02A ACCEPTED · accepted code state `f0c6ae13b` · SID MAC-COMPILE OPEN · pinned · NOT YET EXECUTED (Mac act) · FIRST-INSTALL · reinstall pin mutation · SID ENTRY WITNESS (required) · S-b N=10 CLOSED · S-a NOT OPEN · S3 CLOSED · K00-06 built-in CHARACTERIZE ONLY · INCOMPLETE · KERNEL-00 NOT ACCEPTED · device execution NONE.

### 10.3 Execution note — first Mac attempt was a PASTE SLIP, nothing ran (founder transcript, 2026-09-15): NOT EXECUTED · authority intact

The founder completed the pinned fetch-and-show read (the printed §10.2 block matched the committed text byte-for-byte), then the paste of the block delivered only `set -e` and `SHA=f0c6ae13b…`, echoed a stray fragment (`8bc4f`), and ran the final `echo`, which printed with `$STAMP` and `$OUT` empty. `mkdir`, `test ! -e "$WT"`, the worktree add, the gate, `swift test`, `xcodegen`, both builds, every custody read: none ran; no file, worktree, DerivedData or evidence directory was created; no device verb exists in the block anyway. **Classification (precedent §18.28.3–§18.28.4): a transport/paste non-execution, not a governed STOP; `SID MAC-COMPILE-01` is NOT SPENT and the §10.2 pin stands unchanged.** One consequence for conduct only: `set -e` is now armed in that interactive shell, so the block must be pasted in a **fresh terminal window** as one paste, from `set -e` through the final `echo`, with no fences and no prose — otherwise the first non-zero command would close the shell mid-run and leave a partial `$OUT` that looks like a STOP. Nothing else changes. (Extraction is pinned to `6d0a95e6c`, the last commit in which the file holds exactly one ```bash block; `git -C` makes the working directory irrelevant — a bare `git show` from `~` fails with `not a git repository`, as one attempt did.)

### 10.4 Second paste slip in a fresh window (founder transcript, 2026-09-15): three lines took (`set -e` · `SHA=` · `STAMP=`), nothing after — NOT EXECUTED · authority intact · transport changed to the §16.7 file shape

A fresh Terminal window received the paste and executed only the first three lines; the prompt returned with no worktree, no `$OUT`, no build, no device verb. Same classification as §10.3: paste non-execution, not a STOP; `SID MAC-COMPILE-01` unspent; the §10.2 block unchanged. Two slips in a row establish that a multi-line paste is not a reliable transport on this Mac for a block this long. **Transport for the next attempt = the §16.7 shape every Block A–D act already used:** extract the block byte-for-byte from git into a file, run it with `bash`, capture the output with `tee`. The block itself is untouched (extracted, not retyped); `set -e` then governs the script, not the interactive shell. Pinned lines (paste as ONE line each, or type):

```bash
git -C /Users/soullab/MAIA-SOVEREIGN show 6d0a95e6c:docs/programme/VOICE-2026/KERNEL-00_VPIO-02_SOURCE-ID-02_IMPLEMENTATION_2026-09-15.md | sed -n '/^```bash$/,/^```$/p' | sed '1d;$d' > /private/tmp/sid-mac-compile-01.sh
shasum -a 256 /private/tmp/sid-mac-compile-01.sh; wc -l /private/tmp/sid-mac-compile-01.sh
bash /private/tmp/sid-mac-compile-01.sh 2>&1 | tee /private/tmp/sid-mac-compile-01-transcript-$(date -u +%Y%m%dT%H%M%SZ).log; echo "rc=${PIPESTATUS[0]}"
```

The first line must produce a 50-line file (the block from `set -e` through the final `echo`); the `shasum` is custody for the transcript. `rc=` may print empty under zsh (C-D26, known); the script's own final `echo "SID-MAC-COMPILE-01 …"` line, or the last command shown before it stopped, is the status of record. Nothing else changes.

### 10.5 `SID MAC-COMPILE-01` EXECUTED (founder, Mac Studio terminal directly, 2026-09-15) → **STOP at Swift test compilation** · bounded compile defect in the predeclared risk area · authority SPENT · no repair · no rerun · no pin mutation

**What ran (founder report, recorded as stated; the `$OUT` files are not yet in this branch's custody).** The §10.2 block was transported by the §10.4 file shape (`git -C … show 6d0a95e6c:… | sed … > /private/tmp/sid-mac-compile-01.sh`; `shasum`/`wc -l` matched the pinned `a3a803f419f80e75fa973f42f69d45a04ea3fcb75c2affa77228c64faf467043` / 50 lines) and executed with `bash`. `$OUT = /private/tmp/sid-mac-compile-01-out-20260915T204348Z`, containing exactly six files: `gate.log` · `head.txt` · `status-before.txt` · `swift-build.log` · `swift-test.log` · `toolchain.txt`. Sequence as witnessed:

| step | result |
|---|---|
| worktree at `f0c6ae13b88db29cbd1537bec585d98376c8bc4f`, HEAD exact, tree clean (`head.txt`, `status-before.txt`) | PASS |
| VOICE gate `__tests__/voice-kernel-00-source-gates.test.ts` (`gate.log`) | **82/82 PASS** |
| `xcrun swift build` for `ios/VoiceKernel` (`swift-build.log`) | **PASS** — the kernel target (invariant files + `AudioGraph.swift` estimator interior + `VoiceKernel.swift` aggregate) compiles |
| `xcrun swift test` (`swift-test.log`) | **FAIL at test-target compilation**: `PureLogicTests.swift:485:13: error: the compiler is unable to type-check this expression in reasonable time; try breaking up the expression into distinct sub-expressions` → `error: fatalError` |
| `grep -E 'Executed [0-9]+ tests, with 0 failures'` (the pinned PASS test for step 3) | no match → `set -e` **STOP** |
| `xcodegen` · unsigned build · signed device build · identity · manifest · codesign · post-build surface proof · `SEAL` | **NEVER ENTERED** — no `xcodegen.log`, no `build-*.log`, no `build-results.txt`, no seal |

**Classification.** Governed execution — the act was entered and spent; this is a STOP under the §10.1 rule *"STOP on any Swift compile/test failure"*, not a paste slip (§10.3/§10.4) and not a transport refusal. The failure is a **bounded compile-era defect in the predeclared risk class** (a Swift type-checker time-out on a test expression; the pin named `SIMD8`, pointwise operations and the optional-mutating estimator call as the risk area with an open "…" — a type-checker time-out in the same new test class is inside that class of risk, and the founder's rule for it applies unchanged): **returned as the exact failure, not repaired inside the compile act**. Per §10.1 a repair requires its own ruling. Two facts must not be inflated: (i) **zero Swift tests ran** — the test target did not link, so the six `SourceEstimatorTests` vectors are neither passed nor failed; the accepted-state claim *"reader 24/24 · gate 82/82"* stands, the *"never-executed source unit vectors"* remain never-executed; (ii) **the kernel compiled** — the defect is in `ios/VoiceKernel/Tests/VoiceKernelTests/PureLogicTests.swift`, not in the SID subject's runtime surface, and `swift build` PASS is the first Mac-toolchain evidence that the Q4 interior compiles at all. Neither fact is a PASS: the pinned PASS conjunction fails at its fourth term and every later term is unmeasured.

**Where line 485 is (read here from `f0c6ae13b`'s tree, byte-identical on this branch).** Inside `SourceEstimatorTests.testTheOrderedEnvelopeModulationIndexSeparatesAGatedStimulusFromASteadyOrFluctuatingOne`, column 13 = the `ripple` binding:

```swift
let ripple = (0..<25).map { k in 0.5 + 0.3 * sin(2.0 * Double.pi * 7.0 * Double(k) * dt) + 0.01 * Double(k) }
```

An untyped single-expression closure over a `Range<Int>` whose body mixes five literals, `Double.pi`, `sin`, `Double(k)` and a captured `Double` — the textbook shape for Swift's expression-solver blow-up (many overloads of `*`/`+`/`sin` × literal-type inference); it is a **test-file expression, exercising `SourceSignature.modulationIndex`, and touches no kernel line**. The remote session has no Swift toolchain, so the time-out is not reproducible here, and whether any *later* expression in the same file would also time out once this one is split (candidates by shape: the `g` generator's `Float(0.002 * gate * sin(…) + 0.02 * sin(…))` at the whole-estimator test, the `seq26` map, the inner `on`-counting loop) is **unknown** — the compiler stops at the first such diagnostic per file. Any repair ruling should therefore expect that a second compile may stop at a sibling expression, and that is still a bounded defect of the same class, not evidence against the design.

**Candidate repair shape — NAMED, NOT WRITTEN, NOT AUTHORIZED.** The bounded correction is the one the compiler names: break the expression into typed sub-expressions with no change to the computed values or to any assertion — e.g. an explicitly typed closure `{ (k: Int) -> Double in let kk = Double(k); let arg = 2.0 * Double.pi * 7.0 * kk * dt; return 0.5 + 0.3 * sin(arg) + 0.01 * kk }` bound to `let ripple: [Double]`, and the same treatment for the sibling generator expressions if the founder chooses to pre-empt them. Diff budget of such a repair = `PureLogicTests.swift` only; kernel files, harness, readers, batch, reinstall, fixtures and the gate untouched; the gate's `SID_ALLOWED` set already admits the test file. Because the accepted code state is SHA-pinned, **any repair produces a new SHA and therefore a fresh MAC-COMPILE authority** (a new pin block with the new SHA; the reinstall pins stay empty).

**What was NOT witnessed and must not be recorded as fact.** The founder's scrollback shows a second, queued invocation of the same `bash /private/tmp/sid-mac-compile-01.sh … | tee …` line typed while the first ran. Only ONE `$OUT` directory and one governed-run transcript exist; no second `tee` artifact has been produced. The duplicate is recorded here as **visible scrollback, execution NOT ESTABLISHED** — in particular it is NOT recorded as the pin's worktree-exists precondition refusal having fired (that refusal would be the expected outcome of a second run, but expectation is not a witness). Separately, a **0-byte transcript stamped 20:43:11Z** on the Mac belongs to the §10.4 empty-script no-op (the redirection that truncated the file before `git show` failed without `-C`), not to this act; the governed run's stamp is 20:43:48Z.

**Custody on the Mac (owed as the evidence carrier).** `$OUT` (six files) + a recomputed seal, plus the two Desktop copies (`/Users/soullab/Desktop/SID-MAC-COMPILE-01-terminal-recovered.txt`, 41 lines / 3,966 bytes; `/Users/soullab/Desktop/SID-MAC-COMPILE-01-governed-run.log`, 4,351 bytes), travel on a `feature/*` branch and are cherry-picked here with `-x` before any reading beyond this founder-reported table is written. Nothing on the Mac is to be deleted, re-run or amended: the worktree may stay; the `.sh` may stay; `$OUT` is frozen evidence.

**Standing after §10.5.** `SID MAC-COMPILE-01` = EXECUTED · STOPPED at step 3 (`swift test`) · authority SPENT · evidence carrier OWED. Accepted code state `f0c6ae13b` UNCHANGED (acceptance was of the offline evidence; it is not withdrawn by a Mac test-compile defect — the founder rules what, if anything, it means). Open authority: NONE — a repair ruling (test-file only, new SHA) and a fresh compile pin are the next possible founder acts. Still closed: FIRST-INSTALL · reinstall pin mutation · SID ENTRY WITNESS (REQUIRED) · S-b N=10 · S-a (NOT OPEN) · S3 · K00-06 built-in CHARACTERIZE ONLY · INCOMPLETE · KERNEL-00 NOT ACCEPTED · device execution NONE · C-D26 HOLD.

### 10.6 FOUNDER RULING on §10.5 (2026-09-15): `SID REPAIR-01` OPEN for the line-485 expression ONLY · sibling expressions HOLD · `SID MAC-COMPILE-02` NOT OPEN · evidence carrier lands FIRST

Recorded verbatim in substance. The founder made the next ruling **narrower** than the pre-emptive repair §10.5 named: *the Mac produced one admissible compiler finding; the sibling expressions are plausible candidates by shape, but they are not yet findings — repairing them now would turn prediction into authority and blur the distinction between what failed and what might fail next.*

```text
REPAIR AUTHORITY
  file      ios/VoiceKernel/Tests/VoiceKernelTests/PureLogicTests.swift
  locus     modulation-index test · line 485 expression only
  purpose   make the existing expression type-checkable
  semantic change     NONE
  values              unchanged
  assertions          unchanged
  production/kernel   untouched
  sibling expressions HOLD
```

The §10.5 repair shape is accepted as appropriate — *explicit `[Double]`, typed closure argument, `kk`, `arg`, same arithmetic and return value; compiler-disambiguation, not test redesign.* Sequence, kept as three separate acts so that *the first failure does not pull the next two across its boundary*:

```text
MAC-COMPILE-01   EXECUTED · STOP · Swift test compile · authority SPENT
REPAIR-01        one proven compiler obstruction · one test file · new SHA
review repair diff
MAC-COMPILE-02   separately authorized against that exact new SHA
```

If `MAC-COMPILE-02` then stops on `g`, `seq26` or the inner `on` loop, *that* creates the evidence for the next repair; if it passes them, they were merely similar-looking expressions and no change was manufactured. **The evidence carrier lands before the repair**: the six frozen `$OUT` files, a recomputed seal and the two recovered Desktop copies become durable first, preserving the failed act independently of whatever follows.

**Order of execution as it follows from the ruling (this session).**
1. **Carrier (Mac, founder, first).** Script `docs/programme/VOICE-2026/SID_MAC-COMPILE-01_CARRIER_2026-09-15.sh` (30 lines; file transport per §10.4, no paste of the body): fetches the lane branch, opens a detached worktree at its tip, creates `feature/sid-mac-compile-01-evidence-20260915T204348Z`, copies the six `$OUT` files · the two Desktop copies · `/private/tmp/sid-mac-compile-01.sh` · every `/private/tmp/sid-mac-compile-01-transcript-*.log` (this deliberately includes the 0-byte 20:43:11Z transcript of the §10.4 empty-script no-op, so that attribution is carried as a file, not as a sentence) · a `ls -la` listing, into `docs/programme/VOICE-2026/driver-ledger/sid-mac-compile-01-20260915T204348Z/`, writes `SHA256SUMS.run` + `RETURN.txt`, commits and pushes the `feature/*` branch. It reads `$OUT`; it never writes into it. It touches no kernel, test, harness or reader file. It refuses (`test`) if `$OUT`, either Desktop copy, or a pre-existing carrier worktree is missing/present. Run shape:
   ```bash
   git -C /Users/soullab/MAIA-SOVEREIGN fetch origin claude/voice-2026-census-01
   git -C /Users/soullab/MAIA-SOVEREIGN show origin/claude/voice-2026-census-01:docs/programme/VOICE-2026/SID_MAC-COMPILE-01_CARRIER_2026-09-15.sh > /private/tmp/sid-carrier.sh
   shasum -a 256 /private/tmp/sid-carrier.sh; wc -l /private/tmp/sid-carrier.sh
   bash /private/tmp/sid-carrier.sh 2>&1 | tee /private/tmp/sid-carrier-transcript-$(date -u +%Y%m%dT%H%M%SZ).log; echo "rc=${PIPESTATUS[0]}"
   ```
   Expected last line `SID-MAC-COMPILE-01 CARRIER PUSHED`. The 30-line count and the SHA printed by the remote session at commit time are the custody check.
2. **Here, on the carrier's arrival:** cherry-pick with `-x`, recompute `SHA256SUMS.run`, read `swift-test.log` in full (the only file whose contents §10.5 quotes from the founder's report rather than from custody), confirm `swift-build.log` PASS and the `head.txt` SHA, then write the carrier record (§10.7).
3. **`SID REPAIR-01` (here, after 2).** Exactly one hunk in `PureLogicTests.swift`, the line-485 binding replaced by the pinned text below; nothing else in the file; gate 82/82 (the file is inside `SID_ALLOWED`; the frozen-subject pins are unaffected because the kernel tree is byte-identical); commit → **new SHA** → founder diff review. Pinned replacement (value-identical: the multiplication order `2.0 * Double.pi * 7.0 * kk * dt` is the original's left-to-right order, so every double is bitwise the same):
   ```swift
   let ripple: [Double] = (0..<25).map { (k: Int) -> Double in
       let kk = Double(k)
       let arg = 2.0 * Double.pi * 7.0 * kk * dt
       return 0.5 + 0.3 * sin(arg) + 0.01 * kk
   }
   ```
   No other line of the test file is touched — not `g`, not `seq26`, not the `on` loop, not the assertions, not the comment.
4. **`SID MAC-COMPILE-02`** — NOT OPEN. It opens only by a separate founder act against the reviewed REPAIR-01 SHA, with its own §10.2-shaped pin (same steps, that SHA, a new `$OUT` stamp; reinstall pins still empty).

**Standing after §10.6.** `SID MAC-COMPILE-01` EXECUTED · STOP · SPENT · carrier OWED (script issued) · `SID REPAIR-01` OPEN (line 485 only; not yet written — sequenced after the carrier) · sibling expressions HOLD · `SID MAC-COMPILE-02` NOT OPEN · accepted code state `f0c6ae13b` unchanged until REPAIR-01 supersedes it for compile purposes · FIRST-INSTALL CLOSED · reinstall pins EMPTY · SID ENTRY WITNESS REQUIRED, not opened · device execution NONE · C-D26 HOLD.

### 10.7 Carrier LANDED and READ (2026-09-15): `feature/sid-mac-compile-01-evidence-20260915T204348Z` @ `1c772e14f96253ef6db422ab545e205b8a4e4f1c` → cherry-picked `-x` here · seal reproduced · §10.5 confirmed from custody · one new fact (a third, 0-byte transcript at 21:32:08Z)

**Transport.** Founder ran the §10.6 four-line shape at the Mac Studio terminal: extracted script = 30 lines · `8e8ca597b68f551d912b64b6d3bf1d1fcbcb1cd1e2e2e69d759f195020161028` (matches the remote session's hash at commit); pre-commit hooks passed on the `feature/*` branch; push succeeded; final line `SID-MAC-COMPILE-01 CARRIER PUSHED`; `rc=` empty (C-D26). Carrier directory `docs/programme/VOICE-2026/driver-ledger/sid-mac-compile-01-20260915T204348Z/`, 15 files, 320 lines.

**Custody reproduced here.** `shasum -a 256 -c SHA256SUMS.run` → 0 mismatches; `SHA256SUMS.run` itself = `ba8c0528da5b9dfd4d6a7570c26be151398b22e20aab334dd4485a406accdc79` (the value the Mac printed). `head.txt` = `f0c6ae13b88db29cbd1537bec585d98376c8bc4f` (subject exact). `status-before.txt` = 0 bytes (tree clean before build). `toolchain.txt` = Xcode 26.3 (17C529) · xcodegen 2.46.0 · Apple Swift 6.2.4 (swiftlang-6.2.4.1.4). `sid-mac-compile-01.sh` = `a3a803f419f80e75fa973f42f69d45a04ea3fcb75c2affa77228c64faf467043` (the §10.2 pin, unchanged). `SID-MAC-COMPILE-01-governed-run.log` is **byte-identical** to `sid-mac-compile-01-transcript-20260915T204348Z.log` (`cmp` clean, 4,351 bytes) — the Desktop copy adds nothing and loses nothing. `SID-MAC-COMPILE-01-terminal-recovered.txt` (3,966 bytes) is the same run seen through the terminal: it additionally carries the §10.4 history (the `git show` without `-C` → `fatal: not a git repository`, the empty-file hash `e3b0c442…` / 0 lines, the no-op `bash` with `rc=`), then the correct 50-line extraction and the governed invocation; the `Updating files: 30% …` progress spool is collapsed to its final line, and a queued keystroke echo is interleaved into the `Test Suites:` line — display artefacts, not evidence differences.

**§10.5 confirmed from custody, not from report.** `gate.log`: `Tests: 82 passed, 82 total`. `swift-build.log`: all 13 kernel compilation units including `AudioGraph.swift` and `VoiceKernel.swift`, `Build complete! (4.06s)`. `swift-test.log` (1,445 bytes, read in full): `[6/8] Compiling VoiceKernelTests PureLogicTests.swift` → `PureLogicTests.swift:485:13: error: the compiler is unable to type-check this expression in reasonable time; try breaking up the expression into distinct sub-expressions`, with the compiler's own excerpt of lines 483–487 showing the caret under `ripple`, then `error: fatalError`. **It is the only diagnostic in the log** — no second expression is named, no test ran (no `Test Suite` / `Executed` line). The line-485 locus §10.5 read from this branch's tree is therefore the compiler's locus too. No xcodegen or xcodebuild artefact exists in the carrier, consistent with `set -e` stopping at the `Executed … 0 failures` grep.

**New fact — a third transcript.** `OUT-listing.txt` and the carrier glob show three `sid-mac-compile-01-transcript-*.log` files in `/private/tmp`: `20260915T204311Z` (0 bytes — the §10.4 empty-script no-op, as attributed) · `20260915T204348Z` (4,351 bytes — the governed run) · **`20260915T213208Z` (0 bytes, mtime 17:32 local)** — created ~48 minutes after the governed run, at about the time the carrier was being run. §10.5 recorded the queued duplicate as *visible scrollback, execution NOT ESTABLISHED*, and `RETURN.txt` carries `DUPLICATE_INVOCATION not-established` because that line was authored by the carrier script from §10.5's state, before the carrier's own listing existed. **The listing now bears on it, and the record must not over-read it either way.** What a 0-byte `tee` file establishes: the `bash … | tee …` line was executed at 21:32:08Z and the script wrote nothing to stdout/stderr before exiting. What it does not establish: which line exited. The §10.2 script's preamble is `set -e` · vars · `mkdir -p "$OUT"` · `test ! -e "$WT"` · `cd` · `git fetch …`. A `git fetch` failure prints to stderr and would not be 0 bytes; a `test ! -e "$WT"` failure prints nothing and is exactly 0 bytes — and the worktree `/private/tmp/sid-mac-compile-01-f0c6ae13b…` did exist from the governed run. So the most plausible reading is: **the queued duplicate line was still pending in the terminal's input, was consumed at 17:32:08 local (the four-line carrier paste into the same window would do this), executed, and refused at the worktree precondition before any device or build verb** — but that is a reading, not a witness. Two things make it checkable read-only: `mkdir -p "$OUT"` precedes the precondition, so a refused invocation leaves an **empty** directory `/private/tmp/sid-mac-compile-01-out-20260915T213208Z`; and a refused invocation touches nothing in the governed `$OUT` (its six files keep mtimes 16:43/16:44 local in the listing — **the governed evidence is unmodified whatever the third invocation did**). One read-only line settles it: `ls -ld /private/tmp/sid-mac-compile-01-out-* /private/tmp/sid-mac-compile-01-f0c6ae13b*` — an empty `…-out-20260915T213208Z` directory = the duplicate ran and refused at `test ! -e "$WT"`; no such directory = it exited even earlier (only `set -e`/assignments precede the `mkdir`, so that would itself need explaining). Until read: **duplicate invocation EXECUTED at 21:32:08Z (established by the tee file), outcome REFUSED-BEFORE-ANY-ACT (most plausible, NOT witnessed), governed `$OUT` UNAFFECTED (witnessed by mtimes).** Design note for future pins, not a repair: place `mkdir -p "$OUT"` after the preconditions so a refusal leaves no directory.

**Effect on §10.6 sequencing.** None. The carrier has landed and been read; `SID REPAIR-01` may now be written under §10.6's authority. The third-transcript question is a record matter (one `ls`), not a precondition of the repair.

### 10.8 `SID REPAIR-01` WRITTEN (2026-09-15): commit `faf918b5c5b2cd85f8e8a6c9cbda8bc76df11ce8` · one hunk · one file · returned for founder diff review · `SID MAC-COMPILE-02` NOT OPEN

**Diff (the whole of it).** `ios/VoiceKernel/Tests/VoiceKernelTests/PureLogicTests.swift`, 1 hunk, `+5 −1`, inside `SourceEstimatorTests.testTheOrderedEnvelopeModulationIndexSeparatesAGatedStimulusFromASteadyOrFluctuatingOne`:

```diff
-        let ripple = (0..<25).map { k in 0.5 + 0.3 * sin(2.0 * Double.pi * 7.0 * Double(k) * dt) + 0.01 * Double(k) }
+        let ripple: [Double] = (0..<25).map { (k: Int) -> Double in
+            let kk = Double(k)
+            let arg = 2.0 * Double.pi * 7.0 * kk * dt
+            return 0.5 + 0.3 * sin(arg) + 0.01 * kk
+        }
```

Exactly the text pinned in §10.6 step 3. **Semantic change NONE**: `kk` is the original `Double(k)`; `arg` is the original sine argument with the original left-to-right multiplication order (`2.0 * Double.pi * 7.0 * kk * dt`), so each intermediate double is the value the original expression computed; the return is the original sum in the original order. The following assertion `XCTAssertLessThan(SourceSignature.modulationIndex(ripple, frameSeconds: dt)!, 0.9)` is untouched, as is every other line of the file: the comment above, `seq26`, the `g` generator, the inner `on` loop, all six test names and all assertions — **sibling expressions HOLD, as ruled**. The change is compiler disambiguation (explicit element type, typed closure parameter and return, two named intermediates), not test redesign.

**Surface proof.** `git diff --stat f0c6ae13b faf918b5c -- ios scripts __tests__` → **1 file changed, 5 insertions(+), 1 deletion(-)** — the accepted code state and the repair differ, across the entire kernel/harness/driver/reader/gate surface, by this hunk and nothing else. Gate `__tests__/voice-kernel-00-source-gates.test.ts` alone at `faf918b5c`: **82/82** (the test file is inside `SID_ALLOWED`; the twelve frozen-subject history pins and the invariant-file identity to `ac12dedf4` are unaffected because no kernel file moved). `k00-source-ledger.py` and both frozen readers untouched. Not verifiable here: that Swift 6.2.4 now type-checks the expression — the remote session has no toolchain; that is precisely what `SID MAC-COMPILE-02` exists to witness, and a further time-out on a sibling expression would be that act's finding, not this one's.

**Standing after §10.8.** `SID REPAIR-01` WRITTEN at `faf918b5c` · awaiting **founder diff review** (this section is the diff) · `SID MAC-COMPILE-02` **NOT OPEN** — opens only by a separate founder act naming `faf918b5c5b2cd85f8e8a6c9cbda8bc76df11ce8` exactly, with its own §10.2-shaped pin (same steps; new `$OUT` stamp; `mkdir -p "$OUT"` moved after the `test ! -e "$WT"` precondition per the §10.7 design note; reinstall pins still empty). Accepted code state for **source/offline** purposes remains `f0c6ae13b`; for **compile** purposes the candidate is `faf918b5c`. Sibling expressions HOLD · FIRST-INSTALL CLOSED · SID ENTRY WITNESS REQUIRED, not opened · device execution NONE · C-D26 HOLD · the §10.7 third-transcript `ls` still owed as a record matter.

### 10.9 FOUNDER RULING on the third transcript (2026-09-15): queued duplicate → **EXECUTION ESTABLISHED · precondition refusal at the existing `$WT` · non-substantive residue only** — supersedes the §10.5 / §10.7 / `RETURN.txt` "not established" wording

Recorded verbatim in substance. The founder checked the file directly on the Mac: `sid-mac-compile-01-transcript-20260915T213208Z.log` is 0 bytes, created **21:32:08Z**, and the carrier's own transcript `sid-carrier-transcript-20260915T213212Z.log` begins **21:32:12Z** — four seconds later. With the executed script's preamble (`set -e` · … · `mkdir -p "$OUT"` · `test ! -e "$WT"`) and the original worktree already present from the governed run, a second invocation creates its `$OUT` and its tee transcript, reaches `test ! -e "$WT"`, and terminates before producing any stdout. Ruled standing:

```text
first invocation
  EXECUTED · STOP at Swift test compilation · authority SPENT

queued duplicate
  EXECUTION NOW ESTABLISHED
  precondition refusal at existing $WT
  0-byte tee transcript
  no gate · no Swift · no Xcode · no device act
  non-substantive residue only
```

This **supersedes** three earlier wordings, none of which is edited: §10.5 ("visible scrollback, execution NOT ESTABLISHED"), §10.7 ("outcome most plausibly a precondition refusal, NOT witnessed"), and the carrier's `RETURN.txt` line `DUPLICATE_INVOCATION not-established` (authored by the carrier script from §10.5's state; the carrier directory is frozen evidence and is not rewritten). The §10.7 `ls -ld …` line is no longer owed; it remains a free confirmation (an empty `…-out-20260915T213208Z` directory would be the mechanism's footprint) and, if run, would be recorded as such. The §10.7 design note stands for the next pin: `mkdir -p "$OUT"` after the worktree precondition, so a refusal leaves no residue at all.

**Ordering, as the founder stated it and as it ran.** §10.7 (carrier read, third-transcript finding) was written and committed (`e7a50435f`) **before** `SID REPAIR-01` (`faf918b5c`); the ruling here changes the finding's classification, not its place in the sequence. `REPAIR-01` remains open exactly as ruled (§10.6) and is written (§10.8), awaiting diff review. Siblings HOLD. `SID MAC-COMPILE-02` NOT OPEN.

### 10.10 Free confirmation of §10.9 (founder, Mac terminal, read-only, 2026-09-15)

```text
drwxr-xr-x  413 soullab  wheel  13216 Sep 15 16:43 /private/tmp/sid-mac-compile-01-f0c6ae13b88db29cbd1537bec585d98376c8bc4f
drwxr-xr-x    8 soullab  wheel    256 Sep 15 16:44 /private/tmp/sid-mac-compile-01-out-20260915T204348Z
drwxr-xr-x    2 soullab  wheel     64 Sep 15 17:32 /private/tmp/sid-mac-compile-01-out-20260915T213209Z
```

The mechanism's footprint is present: an **empty** `$OUT` directory (`2` links, 64 bytes = `.` and `..` only) stamped `20260915T213209Z`, created 17:32 local — one second after the shell's tee stamp `213208Z`, because the script computes its own `STAMP` after `bash` starts. The duplicate ran `mkdir -p "$OUT"`, reached `test ! -e "$WT"` with the governed worktree (16:43) present, and exited: no `head.txt`, no log, nothing written into the governed `$OUT` (unchanged at 16:44, 8 entries). §10.9's ruling is confirmed by direct evidence; nothing in the ruling changes. Residue on the Mac: the empty `…-out-20260915T213209Z` directory — non-substantive; left in place (nothing on the Mac is deleted in this lane without a ruling). Next pin: `mkdir -p "$OUT"` after the precondition.

### 10.11 FOUNDER RULINGS (2026-09-15): `SID REPAIR-01` **ACCEPTED** at `faf918b5c5b2cd85f8e8a6c9cbda8bc76df11ce8` · `SID MAC-COMPILE-02` **OPEN** against that SHA exactly → execution pin issued as a standalone file

**Ruling 1 — REPAIR-01 ACCEPTED (verbatim in substance).** The founder reviewed `faf918b5c` directly: file changed `PureLogicTests.swift` only · hunks 1 · 5 insertions / 1 deletion · kernel untouched · harness/driver untouched · siblings untouched · assertions unchanged · test values unchanged; the transformation is semantically faithful — *it gives the type solver intermediate typed expressions while preserving the calculation and assertion surface*; scope within ruling, one proven obstruction only, semantic drift NONE FOUND, no amendment required.

**Ruling 2 — MAC-COMPILE-02 OPEN (verbatim in substance).** A fresh compile act against exactly `faf918b5c5b2cd85f8e8a6c9cbda8bc76df11ce8`; purpose = *requalification after the accepted REPAIR-01, not continuation of MAC-COMPILE-01*; the pin retains the §10.2 compile path with only two changes — (1) subject SHA, (2) `mkdir -p "$OUT"` moved after `test ! -e "$WT"` so a refused invocation cannot manufacture an empty evidence directory (*evidence hygiene, not a change to the qualification being performed*). Boundary: sibling repair ⛔ HOLD · additional code change ⛔ NONE · reinstall pins EMPTY · install 0 · launch 0 · sample 0 · device mutation 0. **MAC-COMPILE-02 gets its own fate**: if the compiler now exposes `g`, `seq26`, the inner `on` loop, or anything else, that is a new finding and a STOP, not inherited permission to repair it.

**Pin issued.** `docs/programme/VOICE-2026/SID_MAC-COMPILE-02_PIN_2026-09-15.sh` — 50 lines, `554e663eca340b9cad70cf1139718930926a0b931070b4012e6417d53fc9d390`. Derived mechanically from the accepted §10.2 block (extracted from `7ec59882d`, hash `a3a803f4…` reproduced here first) by `sed`/`awk`; the complete `diff` between the two is:

| lines | change | authority |
|---|---|---|
| 2 | `SHA=f0c6ae13b…` → `SHA=faf918b5c…` | ruled change 1 |
| 7→9 | `mkdir -p "$OUT"` moved from before to after `test ! -e "$WT"` | ruled change 2 |
| 4 · 6 · 40–42 · 50 | act label `sid-mac-compile-01` / `SID-MAC-COMPILE-01` → `…-02` in the worktree path, the `$OUT` path, the manifest filename and the final echo | **labelling only — disclosed as a third class the ruling did not enumerate**: the evidence of act 02 must not be written under act 01's name, and a `-02-` worktree path also cannot collide with the still-present `-01-` worktree; every command, test, path component and identity read is otherwise byte-identical |

If the founder prefers the strict two-change reading, the label substitution is reverted and the pin re-issued before any run; **nothing runs on this pin until the founder runs it.** Steps, PASS conjunction and STOP rules are exactly §10.1/§10.2's: HEAD exact + tree clean → gate 82/82 → `swift build` → `swift test` (`Executed N tests, with 0 failures`; `SourceEstimatorTests … passed` count) → `xcodegen` → unsigned generic-iOS build → signed device build (team `ZVK2X646Z2`, destination id `00008140-00163D9922E0801C`, no install) → identity (bundle `life.soullab.voicekernel.vpio02sid` · display `VoiceKernel VPIO-02-SID` · dylib UUID · dylib SHA · executable SHA · manifest + count) → post-build `git diff --quiet -- ios/VoiceKernel ios/VoiceKernelHarness/project.yml scripts __tests__` → `SHA256SUMS.compile` → `SID-MAC-COMPILE-02 <stamp> subject faf918b5c… out <OUT>`. Any Swift diagnostic = STOP, returned as the exact failure, not repaired.

**Run shape (Mac Studio terminal directly; file transport; no paste of the body):**

```bash
git -C /Users/soullab/MAIA-SOVEREIGN fetch origin claude/voice-2026-census-01
git -C /Users/soullab/MAIA-SOVEREIGN show origin/claude/voice-2026-census-01:docs/programme/VOICE-2026/SID_MAC-COMPILE-02_PIN_2026-09-15.sh > /private/tmp/sid-mac-compile-02.sh
shasum -a 256 /private/tmp/sid-mac-compile-02.sh; wc -l /private/tmp/sid-mac-compile-02.sh
bash /private/tmp/sid-mac-compile-02.sh 2>&1 | tee /private/tmp/sid-mac-compile-02-transcript-$(date -u +%Y%m%dT%H%M%SZ).log; echo "rc=${PIPESTATUS[0]}"
```

Custody check before line 4: `554e663e…` and `50`. Do not type anything into the window while it runs. `rc=` may be empty (C-D26). The evidence carrier for `$OUT` follows the §10.6 carrier shape on a `feature/*` branch — issued after the run, whatever its fate.

**Standing after §10.11.** REPAIR-01 ACCEPTED · compile candidate = `faf918b5c` · `SID MAC-COMPILE-02` OPEN, pinned, NOT YET EXECUTED · siblings HOLD · reinstall pins EMPTY · FIRST-INSTALL CLOSED · SID ENTRY WITNESS REQUIRED, not opened · device execution NONE · C-D26 HOLD.

### 10.12 `SID MAC-COMPILE-02` EXECUTED (founder, Mac Studio terminal directly, 2026-09-15) → **final echo reached = pinned PASS conjunction satisfied, as founder-reported** · carrier issued · acceptance waits on custody

**Transport.** Label substitution accepted by execution. Extracted pin = 50 lines · `554e663e…` (matches). `$OUT = /private/tmp/sid-mac-compile-02-out-20260915T214212Z`; worktree `/private/tmp/sid-mac-compile-02-faf918b5c…` (new path, no collision with the `-01-` worktree). `rc=` empty (C-D26).

**Transcript, step by step (founder report; custody pending the carrier).**

| step | witnessed line(s) | pinned criterion |
|---|---|---|
| HEAD | `faf918b5c5b2cd85f8e8a6c9cbda8bc76df11ce8` | exact — PASS |
| tree before | `test -z "$(git status --porcelain)"` passed (script continued) | clean — PASS |
| toolchain | Xcode 26.3 (17C529) · xcodegen 2.46.0 · Apple Swift 6.2.4 — identical to MAC-COMPILE-01 | same toolchain that produced the 485:13 time-out |
| gate | `Tests: 82 passed, 82 total` | 82/82 — PASS |
| `swift build` | `[13/13] Compiling VoiceKernel AudioGraph.swift` · `Build complete! (4.00s)` | PASS |
| `swift test` | **`Executed 33 tests, with 0 failures (0 unexpected)`** (package total; per-suite lines include `Executed 6 tests, with 0 failures … 0.033 s` = `SourceEstimatorTests`) | zero failures — PASS · **the six source unit vectors have now executed for the first time on any toolchain, all passing** |
| source count | `7` (`SourceEstimatorTests` lines carrying `passed` = suite summary + six tests) | readable — PASS |
| xcodegen | `Created project at …/VoiceKernelHarness.xcodeproj` | PASS |
| unsigned generic-iOS build | `** BUILD SUCCEEDED **` | PASS |
| signed device build (team `ZVK2X646Z2`, destination id) | `** BUILD SUCCEEDED **` | PASS · no install, no launch |
| identity | `life.soullab.voicekernel.vpio02sid` · `VoiceKernel VPIO-02-SID` · dylib UUID **`4A6AD464-0A19-320F-980E-7446F6AA1440`** (arm64) · dylib SHA-256 **`a15b399d9a9a3c1071d12ba3c4fb24a56b3f6e51c708f8d3c5dd9cb811bdfc44`** · executable SHA-256 **`db036694dcaa415bb50bb6319af249d643e6db76847177541db836f2f1ec5d17`** · manifest `SID-MAC-COMPILE-02.manifest.sha256` = **`699ac758b12bd8062145655ad12fab6ed5ac2c96e1b4003cc210a5b39f72c7a4`**, **7 files** · codesign `Identifier=life.soullab.voicekernel.vpio02sid` · `TeamIdentifier=ZVK2X646Z2` · Apple Development authority chain | bundle/display exact; every identity field readable — PASS |
| post-build surface | `head-after` = `faf918b5c…` · `status-after` = ` M ios/VoiceKernelHarness/Harness/Info.plist` (19+/1−) only · `git diff --quiet -- ios/VoiceKernel ios/VoiceKernelHarness/project.yml scripts __tests__` passed | the known `xcodegen` footprint and nothing else (§10.2 reading law) — PASS |
| seal + echo | `SHA256SUMS.compile` written · `SID-MAC-COMPILE-02 20260915T214212Z subject faf918b5c… out …` | final echo reached — PASS |

**What this establishes, and what it does not.** (i) The line-485 obstruction is **resolved on the same toolchain that produced it** — no sibling expression (`g`, `seq26`, the inner `on` loop) produced a diagnostic; they were, as the founder anticipated they might be, merely similar-looking expressions, and **no change was manufactured for them**; sibling HOLD is now moot rather than lifted. (ii) The SID subject compiles, its unit vectors pass (6/6 within 33/33), and it builds signed for the registered device with the ruled identity. (iii) This is a **compile qualification only**: install 0 · launch 0 · sample 0 · device mutation 0; the reinstall pins remain **EMPTY** — the identity above is recorded here, not pinned into `k00-reinstall.sh`; that mutation is its own act after the compile evidence is accepted (§10.1). (iv) **Acceptance waits on custody**: this section is the founder's transcript; the carrier below brings `$OUT` (head/status/toolchain/gate/swift-build/swift-test/source-tests-passed-count/xcodegen/xcodebuild-unsigned/xcodebuild-signed/build-results/identity/manifest/head-after/status-after/diff-after/SHA256SUMS.compile) onto a `feature/*` branch, is cherry-picked `-x`, the compile seal is re-checked file-by-file and the manifest re-hashed here before `SID MAC-COMPILE-02` is recorded ACCEPTED.

**Carrier issued** — `docs/programme/VOICE-2026/SID_MAC-COMPILE-02_CARRIER_2026-09-15.sh` (§10.6 shape): reads `$OUT` whole (`cp -R`), the executed pin, every `sid-mac-compile-02-transcript-*.log`, a listing; runs `shasum -c SHA256SUMS.compile` inside the copy and stores the result as `SHA256SUMS.compile.check`; writes `SHA256SUMS.run` + `RETURN.txt`; commits to `feature/sid-mac-compile-02-evidence-20260915T214212Z` and pushes. Never writes into `$OUT`; touches no code. Run shape as §10.6 (`git show <sha>:… > /private/tmp/sid-carrier-02.sh` · `shasum`/`wc -l` · `bash … | tee …`).

**Standing after §10.12.** `SID MAC-COMPILE-02` EXECUTED · PASS (founder-reported) · **NOT YET ACCEPTED** (carrier owed) · REPAIR-01 ACCEPTED at `faf918b5c` · siblings: no finding · reinstall pins EMPTY · FIRST-INSTALL CLOSED · SID ENTRY WITNESS REQUIRED, not opened · device execution NONE · C-D26 HOLD. Next possible founder acts, none open: accept MAC-COMPILE-02 on custody → rule on pinning the SID identity into the reinstall instrument → rule on opening the SID ENTRY WITNESS.

### 10.13 FOUNDER RULINGS on §10.11–§10.12 (2026-09-15): label substitution ACCEPTED as constitutive act identity · `SID MAC-COMPILE-02` **PASS · authority SPENT** on the founder's independent read of `$OUT` · carrier NOT withheld

**Ruling 1 — labels (verbatim in substance).** The founder compared the issued script against the accepted §10.2 block directly; the complete differences are exactly: subject SHA `f0c6ae13b…` → `faf918b5c…` · worktree identity `sid-mac-compile-01` → `-02` · OUT identity `-01` → `-02` · `mkdir` moved after the `$WT` precondition · manifest filename `COMPILE-01` → `COMPILE-02` · final identity echo `COMPILE-01` → `COMPILE-02`; *no undisclosed build, test, identity, signing, source, or acceptance-command changes.* The `01→02` substitutions are accepted **as constitutive act identity, not an expansion of authority** — and the worktree identity change was *necessary*: retaining the preserved `-01-` worktree path would have made `MAC-COMPILE-02` refuse immediately and so could not have instantiated the separately authorized act. No re-issue required.

**Ruling 2 — PASS (verbatim in substance).** The founder independently read `/private/tmp/sid-mac-compile-02-out-20260915T214212Z`: subject `faf918b5c…` before and after · repository clean before · JS/source gate 82/82 · Swift build PASS · Swift test suite 33 tests, 0 failures, 0 unexpected · `SourceEstimatorTests` 6 tests, 0 failures · **the repaired modulation-index test `testTheOrderedEnvelopeModulationIndexSeparatesAGatedStimulusFromASteadyOrFluctuatingOne` executed and passed** — *REPAIR-01 has done more than merely compile: its affected test executed successfully* · unsigned Xcode build SUCCEEDED · signed device-target build SUCCEEDED · identity `life.soullab.voicekernel.vpio02sid` / `VoiceKernel VPIO-02-SID` / `TeamIdentifier=ZVK2X646Z2` / `Apple Development: Kelly Nezat (N9DTF6434L)` · signed artifacts hashed · product manifest 7 entries · `status-before.txt` 0 bytes · after: `M ios/VoiceKernelHarness/Harness/Info.plist` only, 20 lines — the known `xcodegen` footprint the pin permits; the protected source/project/script/test diff check passed (else the final echo was unreachable) · **compile evidence seal present: `SHA256SUMS.compile`, 17 entries, sha256 `d835bebc36a86be7d8640fdbdd68b36eca9b26cce1faaaec70b3a12c3c93e2d3`** · normal termination at the `SID-MAC-COMPILE-02 20260915T214212Z subject faf918b5c… out …` echo · empty `rc=` = the known zsh `PIPESTATUS` display issue, not failure. Standing ruled:

```text
REPAIR-01                 ACCEPTED
SID MAC-COMPILE-02        EXECUTED · PASS · authority SPENT
sibling-expression HOLD   remains HOLD — no sibling compiler finding occurred
reinstall pins            EMPTY
install 0 · launch 0 · sample 0 · device act 0
```

*Do not withhold the carrier*: the `MAC-COMPILE-02` evidence carrier is issued under the established pattern, preserving `$OUT` and the transcript before anything downstream opens.

**Corrections to this record's own wording.** §10.12 said the sibling HOLD was "moot rather than lifted"; the founder's standing is **remains HOLD** — the absence of a finding leaves the hold in place, it does not dissolve it. §10.12 also made acceptance wait on custody; the founder has ruled PASS on a direct read of `$OUT`, which is the founder's witness, not this session's. What custody still does here, when the carrier lands: reproduce the 17-entry `SHA256SUMS.compile` (expected sha256 `d835bebc…`), re-hash the 7-entry manifest (expected `699ac758…`), read `swift-test.log` in full, and record the reproduction as §10.14 — a confirmation of a ruling already made, not a condition of it.

**Carrier status.** `SID_MAC-COMPILE-02_CARRIER_2026-09-15.sh` was committed at `3bad244ad` in the same minute this ruling arrived (28 lines, `5aaa5120fdf9b2c0c8c541857b274272b9343553635561d72d45fcf179e01a65`). Run shape (Mac Studio terminal directly; no paste of the body):

```bash
git -C /Users/soullab/MAIA-SOVEREIGN fetch origin claude/voice-2026-census-01
git -C /Users/soullab/MAIA-SOVEREIGN show 3bad244ad:docs/programme/VOICE-2026/SID_MAC-COMPILE-02_CARRIER_2026-09-15.sh > /private/tmp/sid-carrier-02.sh
shasum -a 256 /private/tmp/sid-carrier-02.sh; wc -l /private/tmp/sid-carrier-02.sh
bash /private/tmp/sid-carrier-02.sh 2>&1 | tee /private/tmp/sid-carrier-02-transcript-$(date -u +%Y%m%dT%H%M%SZ).log; echo "rc=${PIPESTATUS[0]}"
```

Expected final line `SID-MAC-COMPILE-02 CARRIER PUSHED`. It reads `$OUT`; it never writes it.

**Standing after §10.13.** `SID MAC-COMPILE-02` PASS · SPENT · carrier issued, not yet landed · REPAIR-01 ACCEPTED · siblings HOLD · reinstall pins EMPTY · FIRST-INSTALL CLOSED · SID ENTRY WITNESS REQUIRED, not opened · device execution NONE · C-D26 HOLD. Downstream acts, none open until the founder opens them: reinstall-pin mutation for the SID identity (UUID `4A6AD464-…`, container unknown until an install exists) · SID ENTRY WITNESS.

### 10.14 FOUNDER RULINGS (2026-09-15) after the first `MAC-COMPILE-02` carrier attempt: carrier STOP = checker defect → **carrier-B issued** · `SID REINSTALL-PIN-SID` OPEN code-only → **written at `3f3cb15b070d9237f17f66625ede6591b1ae5940`** · `SID ENTRY-WITNESS-DESIGN` OPEN (records/design only)

**Carrier attempt 1 — STOP (founder, verbatim in substance).** The carrier reached its feature worktree and copied the evidence, then stopped at `shasum -c SHA256SUMS.compile`: the seal contains a self-entry `e3b0c442… ./SHA256SUMS.compile` — the hash of the seal while it was still empty during its own creation (the §10.2 recipe's `( … | xargs shasum ) > "$OUT/SHA256SUMS.compile"` opens the output file before `find` enumerates it, so the seal lists itself at zero length). Populated, the seal's real hash is `d835bebc36a86be7d8640fdbdd68b36eca9b26cce1faaaec70b3a12c3c93e2d3`; the copy has that hash and the other 16 sealed files verify OK. No remote evidence branch, no carrier commit. Standing: `SID MAC-COMPILE-02` PASS unchanged · carrier attempt STOP (checker defect) · compile `$OUT` frozen · failed carrier worktree preserved as residue. *Do not rewrite or "fix" the original compile seal.* Note for future compile pins (not repaired here): write the seal to a temporary path outside `$OUT`, or exclude the seal's own name, so it never lists itself.

**Carrier-B (narrow repair/re-issue, as ruled).** `docs/programme/VOICE-2026/SID_MAC-COMPILE-02_CARRIER-B_2026-09-15.sh` (36 lines) — fresh worktree `/private/tmp/sid-carrier-02b-20260915T214212Z` and branch `feature/sid-mac-compile-02-evidence-b-20260915T214212Z`; only the verification semantics changed: asserts the copied `SHA256SUMS.compile` hashes to `d835bebc…` · asserts exactly one self-entry for `./SHA256SUMS.compile` · asserts 17 lines · preserves the self-entry unchanged · verifies the other 16 entries with the self-entry excluded from the check input (`grep -v … | shasum -c -`) and asserts 16 `OK` · writes the check result plus the preserved self-entry line to `SHA256SUMS.compile.check` · then the carrier's independent `SHA256SUMS.run` and `RETURN.txt` (which names carrier attempt 1's STOP) · commit · push. The semantics were self-tested here on a synthetic seal built by the same recipe (17 lines, one `e3b0c442` self-entry, 16 OK). It never rewrites the compile seal and never writes into `$OUT`. Run shape as §10.13, substituting `CARRIER-B` in the path and `sid-carrier-02b` for the transcript name.

**`SID REINSTALL-PIN-SID` — ruling (verbatim in substance) and the mutation.** OPEN · CODE-ONLY; may record bundle identifier `life.soullab.voicekernel.vpio02sid`, dylib UUID `4A6AD464-0A19-320F-980E-7446F6AA1440`, dylib SHA-256 `a15b399d9a9a3c1071d12ba3c4fb24a56b3f6e51c708f8d3c5dd9cb811bdfc44`; container identity ⛔ NOT KNOWN — *must remain structurally unset / witness-required, must not be guessed or manufactured*, and kept **semantically unknown rather than an ordinary empty value that downstream code might mistake for a legitimate pin**; install 0 · launch 0 · sample 0 · device act 0; no install or runtime witness inherited. Written at **`3f3cb15b070d9237f17f66625ede6591b1ae5940`** (two files; gate 82/82):

- `scripts/witness/k00-reinstall.sh`: `VPIO02SID_UUID` and `VPIO02SID_DYLIB_SHA` filled with exactly the two ruled values; `VPIO02SID_EXEC_SHA` / `_MANIFEST_SHA` / `_MANIFEST_FILES` **left empty** because they are outside the ruled list — consequence, stated plainly: **`pins-unrecorded` still refuses the SID subject before any device verb**, which is the fail-closed reading of a partial ruling, not a defect. The compile evidence does hold those three values (executable SHA `db036694dcaa415bb50bb6319af249d643e6db76847177541db836f2f1ec5d17` · manifest SHA `699ac758b12bd8062145655ad12fab6ed5ac2c96e1b4003cc210a5b39f72c7a4` · 7 files); if the founder extends the ruled list to them, that is a second three-line mutation under this same shape. New constant `VPIO02SID_CONTAINER="WITNESS-REQUIRED"` — declared once, **consumed nowhere** (the container is not an install input; it exists only after an install and is captured by the ENTRY witness); the comment above it says why neither an empty string nor a UUID would be admissible there.
- `__tests__/voice-kernel-00-source-gates.test.ts`: the SOURCE-ID-02 obligation that asserted all five SID pins empty now asserts the two ruled values exactly, the three unruled pins still empty, the sentinel present exactly once, and that the sentinel is never `""` and never UUID-shaped. The historical `sidOnlyDelta` pins are unaffected (every changed/added line carries a `VPIO02SID_` / `SOURCE-ID-02` / `pins-unrecorded` token).

Complete diff returned to the founder in-session for review; nothing downstream opens on it.

**`SID ENTRY-WITNESS-DESIGN` — OPEN (verbatim in substance).** Records/design only, analogous to SOURCE-ID-01: define the witness §18.34 requires before any source population — identity inputs · preconditions · read-only observations · acceptance/refusal criteria · timing/order · evidence carriers · failure semantics · container-ID capture after install · the exact boundary between ENTRY and source population. May not install, launch, sample, populate a source, modify a device, or spend an ENTRY witness. *Designing the witness now separates what counts as admissible ENTRY evidence from whatever happens during the eventual install; it prevents the install result from retroactively defining its own test.* Design returned as `KERNEL-00_VPIO-02_SID_ENTRY_WITNESS_DESIGN_2026-09-15.md` (§10.15 on return).

**Standing after §10.14.** MAC-COMPILE-01 STOP · spent · durable · REPAIR-01 ACCEPTED · MAC-COMPILE-02 PASS · spent · carrier attempt 1 STOP (checker) · carrier-B issued, not yet run · reinstall-pin written at `3f3cb15b070d9237f17f66625ede6591b1ae5940`, diff review next · ENTRY witness design OPEN, in progress · siblings HOLD · install/launch/sample 0 · failed carrier worktree, compile `$OUT`, compile seal and all 01 residue untouched.

### 10.15 Carrier-B LANDED and READ · a terminal-hygiene incident (non-event for the repo) · FOUNDER RULINGS: act 1 ACCEPTED at `3f3cb15b0` · act 2 OPEN → written at `3035c0235` · ENTRY design CONTINUED with the three-moment framing (2026-09-15)

**Carrier-B.** Founder ran it (36 lines · `ed69fa6a…`; fresh worktree `sid-carrier-02b-…`; branch `feature/sid-mac-compile-02-evidence-b-20260915T214212Z`); pre-commit hooks passed; commit `ca735fe15f4dc83618a0c3caac0269a7ddafc091`, 23 files, 1,277 lines; final line `SID-MAC-COMPILE-02 CARRIER-B PUSHED`. Carrier attempt 1's transcript (branch `…-evidence-20260915T214212Z`, no commit) shows the STOP exactly where the founder placed it: after "Switched to a new branch", nothing — the `shasum -c` output went to the `.check` file and `set -e` exited. **Cherry-picked here with `-x`** (`docs/programme/VOICE-2026/driver-ledger/sid-mac-compile-02-20260915T214212Z/`, 23 files). Reproduced from custody: `SHA256SUMS.run` = `9db0dffa3cd7c290a68dcb3232efcef82f018fab2a3e240016e6aee74cb1b727`, **0 mismatches** · `SHA256SUMS.compile` = `d835bebc36a86be7d8640fdbdd68b36eca9b26cce1faaaec70b3a12c3c93e2d3`, 17 lines, one self-entry `e3b0c442…` preserved, **16/16 OK** excluding it (and `SHA256SUMS.compile.check` says the same) · manifest `699ac758…`, 7 entries · `head.txt` = `head-after.txt` = `faf918b5c…` · `status-before.txt` 0 bytes · `build-results.txt` two `** BUILD SUCCEEDED **` · `source-tests-passed-count.txt` 7 · `swift-test.log`: `Executed 33 tests, with 0 failures`, `SourceEstimatorTests` 6/6, and **`testTheOrderedEnvelopeModulationIndexSeparatesAGatedStimulusFromASteadyOrFluctuatingOne … started` then `passed`**, zero `error:` lines. §10.13's ruling is confirmed from custody in every field it named.

**Terminal incident (recorded because it happened in the witness window, classified as a non-event for the repository and the device).** After carrier-B, the §10.14 reinstall diff text was pasted into the same zsh window. zsh executed it line by line: every `+`/`-`/`---` line → `command not found` or `no matches found` (nothing ran); the diff's unchanged context lines (leading space, e.g. ` VPIO02SID_BID="…"`, ` VPIO02SID_EXEC_SHA=""`) **did** execute as interactive shell-variable assignments — session-local, no file touched, no redirection anywhere in the pasted text; the second block's quotes left the window in a `dquote>` continuation, buffering, not executing. Founder ruling: press `Ctrl-C` once to cancel the unfinished quoted command, then **retire that Terminal window from governed witness work** — the shell state is no longer clean enough for custody; the next governed act uses a fresh window. No repository edit, no device act, no authority spent. (Lesson already in the record from §10.3/§10.4, now in a third form: diff text is for reading, never for a terminal; the run shapes in this record are the only things meant to be pasted, and even those travel by `git show`.)

**Ruling — `SID REINSTALL-PIN-SID` act 1 ACCEPTED at `3f3cb15b0`** (verbatim in substance): the actual commit reviewed, not only the pasted diff; within the code-only authority — 2 files · reinstall script identity constants/comments only · gate test pins the new law · install verb unchanged · launch/sample unchanged · no device read/write added; the two values are exactly the witnessed ones (`4A6AD464-0A19-320F-980E-7446F6AA1440`, `a15b399d…`), visible in the compile transcript; `VPIO02SID_CONTAINER="WITNESS-REQUIRED"` accepted **as a sentinel, not identity data** — asserted exactly once, not consumed by the reinstall path — preserving that *the container does not exist as admissible identity until an ENTRY witness observes it after install*. No amendment.

**Ruling — act 2 OPEN, code-only, exactly the remaining three compile-established pins** (`VPIO02SID_EXEC_SHA` = `db036694dcaa415bb50bb6319af249d643e6db76847177541db836f2f1ec5d17` · `VPIO02SID_MANIFEST_SHA` = `699ac758b12bd8062145655ad12fab6ed5ac2c96e1b4003cc210a5b39f72c7a4` · `VPIO02SID_MANIFEST_FILES` = `7`; may change those and the corresponding gate expectations/comments only; may not change BID · UUID · DYLIB_SHA · container sentinel · reinstall transaction · device selection · first-install law · `K00_EXEC_AUTHORITY` law · install verb · launch/sample behaviour). Founder's stated consequence, carried into the script comment verbatim in substance: *once these three are populated the `pins-unrecorded` structural refusal disappears for the SID subject; that does not authorize installation — artifact matching, manifest verification, just-in-time absence and explicit execution authority still stand; "installable under a future authorized transaction" is not "installation opened".* **Written at `3035c0235`** — 2 files, `+13 −8` (three value lines, one `VPIO02SID_MANIFEST_FILES=7` unquoted to match the VPIO-01/02 form the gate already expects, the comment block extended, the gate's three empty-string expectations replaced by three exact-value expectations); `git diff 3f3cb15b0 3035c0235 -- scripts ios __tests__` = those 2 files only; gate 82/82. Returned for diff review.

**Ruling — ENTRY design CONTINUED**, with the founder's three-moment framing (PRE-INSTALL · INSTALL TRANSACTION · POST-INSTALL ENTRY · ONLY AFTER ENTRY PASS) now §2a of the design, including the four-link binding chain (compiled identity ↔ artifact at install time ↔ container from the install result ↔ live installed bundle) that makes a population sample admissible ENTRY evidence only for a bound subject. Design returned: §10.16.

### 10.16 `SID ENTRY-WITNESS-DESIGN` RETURNED (read-only; authorizes nothing) — `KERNEL-00_VPIO-02_SID_ENTRY_WITNESS_DESIGN_2026-09-15.md`

Shape: §1 the question (Q-ENTRY: does the in-process observer perturb physical entry; H0/H1; what it is not; why the observer must be demonstrably live) · §2 identity inputs (all five pins + UUID, product path, device, container UNKNOWN, `.vpio02` coexistence) · §2a the three moments and the binding chain · §3 the chain [A] pin completion → [B] FIRST-INSTALL-SID → [C] container capture (records-only) → [D] preflight → [E] ENTRY batch (`--vp on --mode L --hold 15 --subject vpio-02-sid`, no `--act output`, no `--stimulus`) → [F] carrier → [G] verification here → [H] adjudication, each its own authority · §4 preflight block (HEAD + instrument identity · both containers = 1 · harness processes = 0 · product UUID) · §5 observations (frozen classifier rows via `classifierSubject=vpio-02`; latency vs the 1 500 ms ceiling; **observer liveness** `L ≥ 10` `input_source_sample` records with `frames ≥ 1` per journal, bins/`m2` never read; `input_health_sample` cadence unchanged) · §6 predeclared reading law (validity: infrastructure ≤ 2, liveness veto, `SUBJECT-MISMATCH` = STOP-CLASS; classes ENTRY-UNPERTURBED `t/v ≥ 24/30` ∧ one-sided Fisher vs F-W1 (29,1) n.s. ∧ 0 ceiling breach ∧ all live · ENTRY-PERTURBED · INDETERMINATE-ENTRY with reason · CHARACTERIZE ONLY; latency shift descriptive; no class licenses anything) · §7 carriers (seal written outside the dir first) · §8 container capture (produced by the install result line; recorded in two places; never in an instrument; sentinel permanent) · §9 the ENTRY/population boundary table with two prohibitions (ENTRY journals never fed to the source reader; population never re-reads ENTRY rows) · §10 failure semantics, complete · §11 open questions **Q2–Q6** (N=30 recommended; comparison law as written; liveness bar; whether preflight read 4 is a gate or a read; `.vpio02` coexistence confirmed) — Q1 RULED · §12 standing.

**Standing after §10.16.** MAC-COMPILE-01 STOP · spent · durable · REPAIR-01 ACCEPTED · MAC-COMPILE-02 PASS · spent · durable (carrier-B in custody, all seals reproduced) · reinstall-pin act 1 ACCEPTED `3f3cb15b0` · act 2 written `3035c0235`, review pending · ENTRY design RETURNED, Q2–Q6 owed · [A]–[H] NOT OPEN · siblings HOLD · container WITNESS-REQUIRED · install 0 · launch 0 · sample 0 · source population 0 · the `dquote>` window retired.

### 10.17 FOUNDER RULINGS (2026-09-15): act 2 ACCEPTED at `3035c0235` · ENTRY design ACCEPTED in architecture with Q2–Q6 ruled (Q3 AMENDED) · `FIRST-INSTALL-SID` pin DRAFT OPEN (draft only) → draft returned

**Act 2 — ACCEPTED at `3035c0235`** (verbatim in substance): the actual commit reviewed; exactly the three compile-established pins (`VPIO02SID_EXEC_SHA` `db036694…` · `VPIO02SID_MANIFEST_SHA` `699ac758…` · `VPIO02SID_MANIFEST_FILES` `7`), their gate expectations and explanatory comments; BID, UUID, dylib SHA, container sentinel, install transaction, device selection, first-install law, authority gate and install verb untouched. *`pins-unrecorded` may now cease refusing the SID subject — identity completion, not install authority.* The carrier-B terminus and the terminal-paste non-event were confirmed by the founder in the same ruling (fresh Terminal window for the next governed execution).

**ENTRY design — architecture ACCEPTED; rulings folded into the design file as marked (§4, §6.3, §11, §12):**
- **Q2 · N = 30** — organism comparison against F-W1's 30; matching N keeps the baseline legible.
- **Q3 · AMENDED — UNPERTURBED floor = 27/30.** *Failure to establish significant deterioration is not the same claim as evidence of non-perturbation.* One-sided Fisher vs 29/1: 27/30 p=.306 · 26/30 p=.177 · 25/30 p=.097 · 24/30 p=.051 · 23/30 p=.026 — 24 is the statistical boundary and would give five extra failures the strongest reassuring class. Ruled classes: UNPERTURBED = `t ≥ 27/30` ∧ Fisher n.s. ∧ 0 ceiling breaches ∧ all rows OBSERVER-LIVE · PERTURBED = Fisher significant ∨ ≥ 2 breaches · INDETERMINATE-ENTRY = everything between, including 24–26 takes or exactly 1 breach, reason named.
- **Q4 · OBSERVER-LIVE = ≥ 10 `input_source_sample` records during the 15 s hold**; a valid row below that is INDETERMINATE-ENTRY (observer-dormant), never silently excluded.
- **Q5 · product existence + identity = GATE at [B]; product-path existence = READ ONLY at [D]**; disappearance between [B] and [D] does not invalidate an already-bound installed identity.
- **Q6 · coexistence YES** — `.vpio02` exactly one container · `.vpio02sid` exactly one · uninstall NEVER · overwrite of `.vpio02` NEVER; the historical comparator is preserved in the act of introducing the experimental subject.

**`FIRST-INSTALL-SID` pin — DRAFT OPEN, draft only** (verbatim in substance): may define the exact subject · bind the five compile pins · artifact path · absence precondition · coexistence requirement · one-install transaction · `K00_EXEC_AUTHORITY` requirement · install evidence capture · STOP semantics · container extraction/custody; may NOT install · launch · sample · mutate the device · capture a real container · open the ENTRY batch. *Return the complete script and its record section; do not issue a run shape; the actual act receives a separate explicit opening after the pin is reviewed.*

**Draft returned: `docs/programme/VOICE-2026/SID_FIRST-INSTALL-01_PIN_DRAFT_2026-09-15.sh`** — 57 lines, `6201d9bb03d22f334784b8abff96d5ba1003633b682f7739b040a02c7b8dfd70`, `bash -n` only (⛔ never executed here — §13.16.1 rule; the remote session has no device and the script refuses on line 3 without an authority in any case). Shape, line by line in substance:

| block | what it pins | STOP if |
|---|---|---|
| authority (line 3) | `K00_EXEC_AUTHORITY` must be non-empty in the environment — supplied by the founder at invocation (exported from a file in the fresh window, as §13.17 did), **never read from the repo; the draft contains no authority text** — its content is the founder's to author at opening | empty → refuses before any read |
| identity | instrument/lane SHA `3035c02353b3cc0dab0b0ce1823116d6712b8eb1` (act-2 bytes of `k00-reinstall.sh`; carrier-B manifest present in that tree) · subject `faf918b5c…` · bundle `…vpio02sid` · historical bundle `…vpio02` · device `A0736AC8-…` (the registered arm; `K00_DEVICE` override honoured) · UUID / dylib / exec / manifest pins = the five act-1/act-2 values | — |
| PRE-INSTALL, local | worktree and `$OUT` paths absent · **product exists at the exact MAC-COMPILE-02 path** and its dylib SHA, executable SHA and dylib UUID equal the pins (Q5 GATE) · fresh detached worktree at the SHA · HEAD exact · `k00-reinstall.sh` unmodified · manifest file self-hash `699ac758…` and 7 lines · **`mkdir -p "$OUT"` only after every precondition** (§10.7 hygiene) | any mismatch → `set -e` STOP, no device read yet |
| PRE-INSTALL, device (read-only) | `device info apps --bundle-id` twice: `.vpio02` = exactly 1 · `.vpio02sid` = exactly 0 (Q6 coexistence + SID ABSENT, the pin's own read) · `info processes` → 0 harness processes | otherwise STOP before the instrument runs |
| INSTALL TRANSACTION | **exactly one** `k00-reinstall.sh "$OUT" "$APP"` with `K00_SUBJECT=vpio-02-sid` and the four `K00_EXPECT_*` equal to the pins (a disagreeing expectation refuses inside the instrument); the instrument's own custody gate, just-in-time ABSENT read and authority gate are the admission; its `.REFUSED.txt` / `.HELD.txt` exit propagates (`pipefail`) and STOPs the pin; the pin itself contains **zero** `devicectl … install` verbs (grep-checked: 0) | instrument refusal → STOP, authority spent per instrument law |
| POST-INSTALL, capture | `.last-reinstall` → `reinstall-<stamp>.txt` → the `installationURL … /Bundle/Application/<CONTAINER>/VoiceKernelHarness.app` line → `container.txt` (**produced by the install result, extracted by pattern, never typed**) | no result line / no container → STOP |
| POST-INSTALL, bind (read-only) | `.vpio02` still exactly 1 · `.vpio02sid` exactly 1 · the extracted container appears in the SID listing and **not** in the `.vpio02` listing · 0 harness processes (nothing launched) | otherwise STOP — the install happened but the bind failed, returned as such |
| seal + echo | `SHA256SUMS.install` written to `/private/tmp` first, then moved in (never lists itself at zero length) · `SID-FIRST-INSTALL-01 <stamp> subject … instrument … container <CONTAINER> out <OUT>` | — |

Evidence carrier after a run: the §10.6 shape over `$OUT` (apps/processes before+after · reinstall artefact · invocation log · `container.txt` · seal). The container literal then travels, records-only, into the FIRST-INSTALL-SID record and the ENTRY preflight block (design §8); `VPIO02SID_CONTAINER` stays `WITNESS-REQUIRED`.

**What the draft deliberately does not do.** No authority text (founder-authored at opening; expected content: act name `SID FIRST-INSTALL-01`, subject `faf918b5c…`, instrument `3035c0235…`, bundle, "exactly one custody-gated first-install transaction", no launch/sample) · no run shape · no `.vpio02` verb of any kind · no launch · no journal pull · no ENTRY step. Draft review is the next founder act; a separate explicit opening follows review.

**Standing after §10.17.** MAC-COMPILE-01 STOP · spent · durable · REPAIR-01 ACCEPTED · MAC-COMPILE-02 PASS · spent · durable · carrier-B PASS · durable · reinstall-pin act 1 ACCEPTED `3f3cb15b0` · act 2 ACCEPTED `3035c0235` · ENTRY design ACCEPTED (Q1–Q6 ruled) · FIRST-INSTALL-SID draft RETURNED · FIRST-INSTALL-SID run NOT OPEN · ENTRY [D]–[H] NOT OPEN · install 0 · launch 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD.

### 10.18 FOUNDER RULINGS (2026-09-15): FIRST-INSTALL draft AMEND (one local custody gap) then accept · FIRST-INSTALL execution NOT OPEN, authority NOT ISSUED · ENTRY [D] and [E] drafted NOW as two separate artifacts → all three returned for review

**Ruling on the FIRST-INSTALL draft (verbatim in substance).** Structurally sound; both flagged choices accepted — the pin's pre-install device reads establish the broader pre-state (`.vpio02 = 1`, `.vpio02sid = 0`, harness process = 0) while the instrument's later absence read is the just-in-time transactional admission (*not redundant in evidentiary function*); the reinstall ledger at `$OUT` keeps the act self-contained and never mutates a tracked `driver-ledger/.last-reinstall`. **One amendment required:** the local phase proved dylib SHA · executable SHA · dylib UUID · manifest self-SHA · 7 lines, but not the seven-file product against the manifest, so a drift in another product file could survive into the first device read. Law: *before the pin performs any device read, the exact seven-file product must already be proven identical to the compile manifest*; the instrument then repeats that proof as its own independent custody gate immediately before the transaction. **Amended (59 lines, `a052f081bc44b59d51a5d11ccec99f35951f9c37bb0cbee77ae77e560dbbb617`):** two lines inserted after the manifest line-count test and before `mkdir -p "$OUT"` (which precedes the first `devicectl` read) —
```bash
( cd "$APP" && shasum -a 256 -c "$MAN" >/dev/null )
test "$(cd "$APP" && find . -type f | LC_ALL=C sort)" = "$(awk '{print $2}' "$MAN" | LC_ALL=C sort)"
```
— per-file hash equality and exact file-set equality, the same two proofs `k00-reinstall.sh` performs. Nothing else in the draft changed. **FIRST-INSTALL-SID execution NOT OPEN; `K00_EXEC_AUTHORITY` NOT ISSUED** — *the install should not occur until the downstream ENTRY instruments have been drafted and reviewed; otherwise the real container/result could unconsciously shape the test that is supposed to judge the consequences of entry.* The connected phone changes nothing by itself.

**Ruling on [D]/[E] (verbatim in substance).** Draft both before installation, as **two separate artifacts** — [D] a read-only state witness establishing installed identity / container / pre-state; [E] the physiological observation (N = 30 · VP on · Mode L · hold 15) — *not combined into one executable merely for convenience.* [D] carries a conspicuous non-value container placeholder that must refuse while present; only the FIRST-INSTALL-SID record may replace it with the witnessed literal. [D] embodies the rulings: `.vpio02` exactly 1 · `.vpio02sid` exactly 1 · SID container exactly 1 and belonging to `.vpio02sid` · historical container exactly 1 and belonging to `.vpio02` · harness process 0 · compiled product path = documentary read only · no uninstall · no launch · no sample. [E] pins the ruled law (N 30 · VP on · mode L · hold 15 s · stimulus none · output act none · observer liveness ≥ 10 source samples per valid journal · subject mismatch STOP-class) and **keeps the adjudication law outside the device instrument**. Neither draft may acquire a real container from the connected phone — no admissible SID container exists before the install.

**[D] returned — `docs/programme/VOICE-2026/SID_ENTRY-PREFLIGHT-01_PIN_DRAFT_2026-09-15.sh`** (41 lines, `bc83ca0e5e2ee2bdea0a2d90afe609647ec21fa1cd4d45bf1af3ebea6fe4c74f`; `bash -n` only). `SID_CONTAINER=__SID_CONTAINER_FROM_FIRST_INSTALL_RECORD__` → a `case` on the `__` prefix refuses with exit 2 before any other line; the replacement must also match the UUID shape and differ from the historical container `E3B88028-…`. Then: fresh detached worktree `/private/tmp/sid-entry-01-<SHA>` at instrument SHA `3035c0235…`, HEAD exact, batch/entry-reader/reinstall/driver unmodified; preflight dir `driver-ledger/VPIO-02-SID-ENTRY-preflight-<stamp>/` inside the worktree (F-W1 shape, carried later); **reads only**: `device info apps --bundle-id` for `.vpio02sid` (bundle line = 1 · SID container ≥ 1 · historical container = 0) and for `.vpio02` (bundle line = 1 · historical container ≥ 1 · SID container = 0); `info processes` → harness = 0; product path → `product-present <uuid-match-count>` or `product-absent`, **tee'd, never a gate** (Q5); writes `PREFLIGHT-CLEAN` with the stamp, subject, instrument and container. Contains no `install`, `uninstall` or launch verb (grep 0).

**[E] returned — `docs/programme/VOICE-2026/SID_ENTRY-BATCH-01_PIN_DRAFT_2026-09-15.sh`** (28 lines, `2a5b62066893fd1ed5f5a750046dd892d9dff7bb5eb1d77d591b9e0a5f9e5db5`; `bash -n` only). `K00_EXEC_AUTHORITY` non-empty first (never in the repo) · requires the **same worktree [D] created** (HEAD exact, instrument unmodified — so [E] cannot run without [D] having run, and both run on identical bytes) · requires a `PREFLIGHT-CLEAN` whose stamp is **≤ 600 s old** (the F-W1 "immediately before" rule made structural) · refuses if any `VPIO-02-SID-ENTRY-<stamp>` ledger already exists (one batch per authority) · **exactly one** `scripts/witness/k00-driver-batch.sh VPIO-02-SID-ENTRY 30 --vp on --mode L --hold 15 --subject vpio-02-sid` — no `--act`, no `--stimulus`, no `--w4` (grep 0), so the batch is byte-for-byte the historical entry instrument and the output/source readers are never invoked; the batch builds the driver itself (`build-for-testing` into its own derived-data path) · afterwards it **records** the journal count and ledger line count (not asserted: an infrastructure row has no journal and the reading law admits ≤ 2), seals `SHA256SUMS.entry` written outside the ledger dir first, and echoes `SID-ENTRY-BATCH-01 <stamp> subject … instrument … preflight <pfstamp> ledger <dir>`. The adjudication law (UNPERTURBED ≥ 27/30 ∧ Fisher p ≥ .05 ∧ 0 ceiling breaches ∧ every valid row observer-live · PERTURBED p < .05 ∨ ≥ 2 breaches · INDETERMINATE for every intermediate state incl. 24–26 takes, exactly 1 breach, observer-dormant) and the observer-liveness read live in the design (§5.3, §6.3) and are applied **here at [G]** on cherry-picked evidence — nothing in [E] classifies.

**Sequence as ruled.** 1 amend FIRST-INSTALL draft ✓ · 2 draft ENTRY-PREFLIGHT-01 ✓ · 3 draft ENTRY-BATCH-01 ✓ · **4 review all three artifacts (founder, next)** · 5 only then consider opening FIRST-INSTALL-SID and authoring `K00_EXEC_AUTHORITY`. *The witness is designed before the phenomenon it will judge occurs.*

**Standing after §10.18.** FIRST-INSTALL-SID draft AMENDED, review pending · execution NOT OPEN · authority NOT ISSUED · [D] drafted (placeholder container, refuses) · [E] drafted (frozen N = 30 law; adjudication outside) · ENTRY [D]–[H] NOT OPEN · install 0 · launch 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD · phone connected = no act.

### 10.19 FOUNDER RULINGS on the three drafts (2026-09-15): FIRST-INSTALL ACCEPTED · [D] AMEND (seal) · [E] AMEND (verify seal · 0…300 s) · FIRST-INSTALL carrier DRAFT act OPEN · liveness verifier tooling act OPEN → all four returned

**Founder review of `42f01816d` (verbatim in substance).** The batch invocation checks out against the current instrument: omitting `--act` leaves `ACT=entry`, omitting `--stimulus` leaves `STIMULUS=""`, the historical F-W1 entry path. **(1) FIRST-INSTALL draft ACCEPTED** — the two-line amendment closes the custody gap exactly where it belonged; the pre-device chain now proves product directory · dylib SHA · executable SHA · dylib UUID · manifest SHA · manifest line count · all 7 product hashes · product file set, and only then reads the phone; both earlier choices confirmed (both pre-install app reads — broad pre-state + coexistence vs the instrument's just-in-time absence test, different evidentiary functions; `$OUT` as the reinstall ledger); post-install checks appropriately narrow. Execution ⛔ NOT OPEN · authority ⛔ NOT ISSUED. **(2) [D] AMEND** — the preflight directory inside the ENTRY worktree's `driver-ledger` accepted (shared custody; one carrier commits both from one exact worktree); one custody addition: *seal [D] before [E] may consume it* — write `PREFLIGHT-CLEAN`, generate `SHA256SUMS.preflight` outside `$PF` over every preflight file including the marker, move it in (no self-entry defect). **(3) [E] AMEND** — batch correctly frozen (N 30 · VP on · mode L · hold 15 · subject vpio-02-sid · act entry by default · stimulus none · output act none · top-up none); recording the journal count rather than demanding 30 is correct (*infrastructure failures are part of the predeclared reading law; the pin should not rewrite that law after the batch has occurred*); A. verify [D]'s seal (`test -f SHA256SUMS.preflight` · `shasum -c` · `test -f PREFLIGHT-CLEAN`) before using the marker; B. freshness 600 → **0…300 s**, rejecting future stamps (*ten minutes is too broad for a state whose meaningful observations include "no harness process running"*). **(4) FIRST-INSTALL carrier — DRAFT act OPEN**, execution CLOSED: explicit refusing placeholders for everything the install creates (never a broad glob that could choose the wrong residue); carries the complete `$OUT`, reinstall record, `.last-reinstall`, `container.txt`, before/after apps and processes, `SHA256SUMS.install`, invocation transcript, pin script used, `RETURN.txt`, independent carrier seal; may not invent or infer a container. **(5) Liveness verifier — tooling-only act OPEN**: read surface deliberately tiny (`input_source_sample` records only: count records with `frames ≥ 1`, read `frameReset`; never magnitudes, bins, attribution, `m2`, stimulus fields, source-reader output); per-journal output; LIVE ≥ 10 / DORMANT < 10; never decides UNPERTURBED / PERTURBED / Fisher; self-test pins 10 → LIVE · 9 → DORMANT · `frames=0` not counted · missing/non-numeric not counted · mixed `frameReset` reported accurately · unrelated records ignored · bin/magnitude/`m2` content cannot affect output; the verifier source itself gated against forbidden keys.

**[D] amended → `SID_ENTRY-PREFLIGHT-01_PIN_DRAFT_2026-09-15.sh` 44 lines · `1e77bb052ac0e96c5d6408092bc4fe33548e963ca135ea8fccee3ebecf58b669`.** Diff = three lines appended after the `PREFLIGHT-CLEAN` tee, in the founder's shape (`SEAL=/private/tmp/sid-entry-preflight-01-$STAMP.SHA256SUMS.preflight` · `find | sort | xargs shasum` from `$PF` into `$SEAL` · `mv` into `$PF/SHA256SUMS.preflight`). Nothing else changed.

**[E] amended → `SID_ENTRY-BATCH-01_PIN_DRAFT_2026-09-15.sh` 32 lines · `990e34ffc509a5f67a166695f3253a83a5657170ecaa74a6370189e0165dcd7d`.** Diff: after locating `$PF` — `test -f "$PF/SHA256SUMS.preflight"` · `( cd "$PF" && shasum -a 256 -c SHA256SUMS.preflight >/dev/null )` · then `test -f "$PF/PREFLIGHT-CLEAN"`; freshness now `AGE=$((NOW - PFSEC))` · `test "$AGE" -ge 0` · `test "$AGE" -le 300`. Nothing else changed.

**FIRST-INSTALL carrier draft → `SID_FIRST-INSTALL-01_CARRIER_DRAFT_2026-09-15.sh` 42 lines · `bf4283b752dfe825bb631d6a42ade723e409e731ad150211769e08b8dd2baa35`** (`bash -n` only). `STAMP=__FIRST_INSTALL_STAMP__` and `SID_CONTAINER=__SID_CONTAINER_FROM_FIRST_INSTALL__` — both refuse on the `__` prefix (exit 2) before anything else; then a stamp-shape test and a UUID-shape test. Preconditions on the Mac: `$OUT` for that exact stamp exists · `SHA256SUMS.install` present · `container.txt` present and **equal to the replaced literal** (the record's container must agree with the install's own capture, or the carrier refuses) · `.last-reinstall` and its `reinstall-<stamp>.txt` present · `SHA256SUMS.install` verifies (self-entry excluded defensively) · the executed pin `/private/tmp/sid-first-install-01.sh` present. Fresh worktree at the lane tip, branch `feature/sid-first-install-01-evidence-<stamp>`, `DEST = driver-ledger/VPIO-02-SID-FIRST-INSTALL-01-<stamp>/`: `cp -R "$OUT"/.` (which carries the reinstall record, `.last-reinstall`, `container.txt`, before/after apps + processes JSON, the invocation log, `SHA256SUMS.install`) plus `.last-reinstall` re-copied as a visible `last-reinstall.txt`, the pin script, every `sid-first-install-01-transcript-*.log`, an `OUT-listing.txt`, an `SHA256SUMS.install.check`, the independent `SHA256SUMS.run`, and `RETURN.txt` (act · stamp · subject · instrument · bundle · container "from container.txt = the install result line" · launch 0 · sample 0); commit, push, echo. No glob chooses the stamp; no device verb; nothing inferred.

**Liveness verifier → `scripts/witness/k00-source-liveness.py` at `6f9c4e6ad`** (tooling-only; gate 82 → **85**). Reads each journal line as JSON, keeps records with `event == "input_source_sample"` (the real key is `event`, not `kind`; confirmed from an F-W1 journal), and touches **exactly two evidence keys**: `frames` (a non-negative integer ≥ 1 counts as present; `0`, missing, non-numeric, negative and boolean values never count) and `frameReset` — which in the real record is a **per-record reset counter** (`String(sa.resets)`), not a boolean; the founder's "flags" are therefore reported as `frameReset_nonzero_records` / `frameReset_zero_records` plus `resets_total`. Output TSV per journal: `journal · source_sample_records · frames_present_records · frameReset_nonzero_records · frameReset_zero_records · resets_total · liveness`, `LIVE` iff `frames_present_records ≥ 10`. It imports only `json`, `os`, `sys`; it names no bin, magnitude, `m2`, attribution, stimulus or class token anywhere in its source (docstring and comments stripped); it never imports or invokes either ledger reader. **Self-test 9/9** over eight synthetic journals in `scripts/witness/fixtures/k00-source-liveness-selftest/` (the fixtures carry every distractor key a real record carries — `e440Mean`, `e997Max`, `m2_997`, `magnitudes`, `stimulus`, `binsHz` — so the *source* can stay clean): `live10` → 10/LIVE · `dormant9` → 9/DORMANT · `frames0` → 12 records, 0 present, DORMANT · `badframes` → 13 records (10 good + empty + `abc` + null) = 10/LIVE · `mixedreset` → resets 0×8, 1, 2, 3 → nonzero 3, zero 8, total 6 · `unrelated` → health samples, `floor_transition`, `graph_start_trace` and an `input_source_samples` look-alike ignored, 10/LIVE · `distractors` → identical to `live10` on every column (invariance asserted) · `boundary9plus` → 9 good + `0` + `-3`(reset 4) + `x` = 9 present, DORMANT, resets 4. One expectation in the first draft of the self-test table was wrong (it said `resets_total = 1` for that last journal; the verifier's 4 is right) — the table was corrected, the code was not touched. **Negative control on real evidence:** every VPIO-02 F-W1 journal (a subject with no observer) reads `0 · 0 · 0 · 0 · 0 · DORMANT` — exactly what a no-observer subject must read, and what an ENTRY journal must *not* read. **Gate obligations added (3):** the stripped source contains the three pinned constants, exactly two `ev.get(` reads, none of the forbidden tokens, no subprocess/scipy/numpy; `--selftest` ends `selftest 9/9` with no `FAIL`; a real F-W1 journal reads `0…DORMANT` under the seven pinned column headers. The tracked-set obligation for `scripts/witness` was extended by the verifier and its eight fixtures (the same mechanism by which the source reader was admitted) — disclosed as a gate change; every other obligation unchanged.

**Standing after §10.19.** FIRST-INSTALL pin draft ACCEPTED · execution ⛔ CLOSED · authority ⛔ NOT ISSUED · [D] amended (seal) · [E] amended (seal verify · 0…300 s) — both returned for acceptance · FIRST-INSTALL carrier DRAFTED (placeholders refuse) · liveness verifier COMMITTED at `6f9c4e6ad`, self-test 9/9, gate 85/85 · ENTRY [D]–[H] NOT OPEN · install 0 · launch 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD · phone connected = factual state only.

### 10.20 FOUNDER RULINGS (2026-09-15, from the actual files at `6769627f5`): [D] ACCEPTED · [E] ACCEPTED · FIRST-INSTALL carrier ACCEPTED · liveness verifier ACCEPTED (gate 85/85 accepted) · **`SID FIRST-INSTALL-01` OPEN — one transaction** · authority issued in-session, never committed

**Acceptances (verbatim in substance).** **[D]** `1e77bb05…`: `PREFLIGHT-CLEAN` first, seal built outside `$PF` across the complete evidence set, moved in afterward — no self-entry; preflight inside the ENTRY worktree's `driver-ledger` accepted, so [D] and [E] share the same detached instrument SHA, worktree, evidence namespace and a carrier-ready custody structure. **[E]** `990e34ff…`: before the batch it establishes seal exists · seal verifies · marker exists · timestamp not from the future · age ≤ 300 s · no prior SID ENTRY population; the act is exactly one historical entry-style invocation (`VPIO-02-SID-ENTRY 30 --vp on --mode L --hold 15 --subject vpio-02-sid`), no output act, no stimulus, no classification; recording rather than asserting the journal count is correct because infrastructure failures belong to the adjudication population law. **Carrier** `bf4283b7…`: the `container.txt` equality is *exactly the binding wanted* — two independent representations exist after the install (install evidence → `$OUT/container.txt`; durable record → the literal supplied to the carrier) and the carrier refuses if they disagree; not circular, because `container.txt` originates in the transaction's captured `installationURL` and the record merely claims what that evidence said — *equality tests the claim against its source*; explicit placeholders over glob discovery accepted; the carrier refuses before creating custody while either placeholder is unresolved; execution CLOSED until a real FIRST-INSTALL result exists. **Verifier** `6f9c4e6ad`: checked against the kernel producer — `"frames": String(sa.frames)`, `"frameReset": String(sa.resets)`, `sa.resets` starting at zero and incrementing per reported frame reset, so `frameReset` is a per-window reset counter and the nonzero/zero/total representation is *better than the earlier true/false wording*; liveness depends only on `event == input_source_sample` ∧ `frames ≥ 1`, ≥ 10 LIVE / < 10 DORMANT, `frameReset` reporting-only; gate 82 → 85 accepted as three substantive obligations (jurisdiction/forbidden-token boundary · synthetic 9/9 self-test · historical no-observer negative control), and extending the closed `scripts/witness` tracked set is the appropriate mechanism.

**`SID FIRST-INSTALL-01` — OPEN (verbatim in substance).** Prerequisites designed before the phenomenon are fixed: FIRST-INSTALL pin · FIRST-INSTALL carrier · ENTRY preflight · ENTRY batch · liveness verifier · ENTRY reading law. *Opens FIRST-INSTALL-SID only* — not [D], not [E], not launch, sample or source population. **Authority text issued by the founder in-session** as a seven-line payload naming the act, the subject `faf918b5c…`, the instrument `3035c0235…`, the bundle, the bound *exactly one custody-gated first-install transaction*, launch 0 and sample 0. **By ruling it exists only as the invocation input from a fresh Terminal window and is not committed to the repository** — it is therefore not reproduced in this record; the transcript will carry its byte count, never its text (§13.17 form). The authorized act is bounded to the accepted pin exactly: `docs/programme/VOICE-2026/SID_FIRST-INSTALL-01_PIN_DRAFT_2026-09-15.sh`, 59 lines, `a052f081bc44b59d51a5d11ccec99f35951f9c37bb0cbee77ae77e560dbbb617` (the filename keeps its `_DRAFT_` token because renaming would change the accepted hash; the SHA governs).

```text
FIRST-INSTALL-SID  OPEN
permits   pre-install custody checks · read .vpio02 present exactly once · read .vpio02sid absent ·
          read harness process absent · exactly ONE k00-reinstall.sh transaction · capture installation
          result · derive container from that result · post-install identity/coexistence reads ·
          prove harness remains unlaunched · seal $OUT
does not  second install attempt · uninstall · overwrite · launch · driver test · ENTRY preflight ·
permit    ENTRY batch · sample · source population
```
*Any refusal or failure spends this authority. No repair or retry inherits it.*

**Run shape (Mac Studio, a FRESH Terminal window — the `dquote>` window is retired; file transport; the payload comes from the founder's own ruling text, never from the repository).** Step 0 is the founder's alone: write the seven-line payload to `/private/tmp/sid-fi01-authority.txt`. Then, in the same window:

```bash
export K00_EXEC_AUTHORITY="$(cat /private/tmp/sid-fi01-authority.txt)"; wc -c /private/tmp/sid-fi01-authority.txt
git -C /Users/soullab/MAIA-SOVEREIGN fetch origin claude/voice-2026-census-01
git -C /Users/soullab/MAIA-SOVEREIGN show 6769627f5:docs/programme/VOICE-2026/SID_FIRST-INSTALL-01_PIN_DRAFT_2026-09-15.sh > /private/tmp/sid-first-install-01.sh
shasum -a 256 /private/tmp/sid-first-install-01.sh; wc -l /private/tmp/sid-first-install-01.sh
bash /private/tmp/sid-first-install-01.sh 2>&1 | tee /private/tmp/sid-first-install-01-transcript-$(date -u +%Y%m%dT%H%M%SZ).log; echo "rc=${PIPESTATUS[0]}"
```

Custody check before the last line: `a052f081…` and `59`. The extracted script path and transcript name are the ones the accepted carrier expects. Do not type into the window while it runs. Expected final line on success: `SID-FIRST-INSTALL-01 <stamp> subject faf918b5c… instrument 3035c0235… container <CONTAINER> out /private/tmp/sid-first-install-01-out-<stamp>`; the `.REFUSED.txt` / `.HELD.txt` names in `$OUT` are the STOP forms. `rc=` may be empty (C-D26). After the run, whatever its fate: return the transcript's last lines and `ls -A "$OUT"`; the carrier (accepted, placeholders to be filled **from the transcript and `container.txt` only**) is the next act and needs its own explicit opening.

**Standing after §10.20.** FIRST-INSTALL pin ACCEPTED · carrier ACCEPTED (execution CLOSED) · [D] ACCEPTED · [E] ACCEPTED (execution CLOSED) · verifier ACCEPTED · **FIRST-INSTALL-SID OPEN, one transaction, NOT YET EXECUTED** · authority issued, uncommitted · launch 0 · sample 0 · source population 0 · container WITNESS-REQUIRED until the install produces it · siblings HOLD.

### 10.21 First attempt at `SID FIRST-INSTALL-01` (founder transcript, 2026-09-15): **the pin refused at its own authority gate — no payload file existed; nothing entered** · window hygiene finding · classification returned for ruling

**What the transcript shows.** The five-line run shape was pasted without step 0 (writing the payload to `/private/tmp/sid-fi01-authority.txt`): `cat: … No such file or directory` · `wc: … No such file or directory` → `K00_EXEC_AUTHORITY` was exported as the **empty string**. The fetch succeeded (`6769627f5..7b209b52d`), the extraction matched custody exactly (`a052f081…`, 59 lines), and `bash /private/tmp/sid-first-install-01.sh` printed only `shell-init: error retrieving current directory: getcwd: cannot access parent directories: No such file or directory` and returned (`rc=` empty, C-D26). The pin's third line is `test -n "$K00_EXEC_AUTHORITY"`; under `set -e` an empty authority exits there — **before any variable that names a path is even assigned, before the product checks, before the worktree, before `mkdir`, before any `devicectl` read, before the instrument.** Nothing was created on the Mac except the tee'd transcript (a few bytes: the `shell-init` line), and nothing on the device was read or written. The authority-first gate did exactly what it was designed to do.

**Window hygiene finding.** This was not the fresh window the ruling required: the prompt reads `ws-production-preflight-20260915-191643 %`, the shell reports its working directory no longer exists (`getcwd` failure — a deleted directory from another lane), and the scrollback carries an unrelated command from a different lane (`ssh soullab@minisforum 'bash -s' < scripts/witness/ws-stage2-activation.sh`, itself refused as no such file). None of that reached the pin (the extraction and hash were correct and the pin uses absolute paths), but a window whose cwd has been deleted is not witness custody. The next attempt, if one is authorized, starts with a genuinely fresh window and `cd ~` before step 0.

**Classification — returned for ruling, with this session's reading stated.** The §10.20 boundary says *any refusal or failure spends this authority; no repair or retry inherits it.* This session's reading is that the rule addresses refusals **of the governed act** — the instrument's custody gate, the just-in-time absence read, a device-side failure — and that what happened here is the act **not being entered**: the governing input (the authority payload) was never supplied, so the pin's first line declined to begin, exactly as §10.3/§10.4 paste slips were classified NOT EXECUTED with authority intact, and as the lane law distinguishes *a transport refusal ≠ a governed STOP*. On that reading `SID FIRST-INSTALL-01` is **OPEN · NOT ENTERED · authority intact**, and the correction is procedural (step 0, fresh window), not a repair of anything. The founder decides; nothing is re-run on this session's reading. If the founder rules the authority SPENT, a new opening with a new payload is required before any attempt.

**Residue.** `/private/tmp/sid-first-install-01.sh` (correct, matches custody — reusable if the founder rules the authority intact) · one transcript `sid-first-install-01-transcript-<stamp>.log` carrying only the `shell-init` line (left in place; a later carrier's glob will carry it as the record of this attempt) · no `$OUT` · no worktree · device untouched.

**Standing after §10.21.** `SID FIRST-INSTALL-01` OPEN · NOT ENTERED (attempt 1: authority payload absent, refused at pin line 3) · **spend/intact ruling owed** · [D]/[E] execution CLOSED · launch 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD. (Record note: the `Claude-Session` trailer on `7b209b52d` carries a one-character typo in the session id — a commit-message trailer only, no file, hash or record affected; not rewritten; corrected from this commit on.)

### 10.22 FOUNDER RULING on §10.21 (2026-09-16): `SID FIRST-INSTALL-01` **EXECUTED · STOP at the authority precondition · authority SPENT** · carrier NOT OPEN · supersedes this session's "not entered / intact" reading

**Ruling (verbatim in substance).** The accepted pin was invoked but never passed its first substantive precondition: authority file `/private/tmp/sid-fi01-authority.txt` MISSING → `export K00_EXEC_AUTHORITY="$(cat …)"` made the variable empty → the accepted 59-line pin was invoked → line 3 `test -n "$K00_EXEC_AUTHORITY"` FAIL. Founder's independent Mac check: authority file MISSING · transcript `/private/tmp/sid-first-install-01-transcript-20260915T235951Z.log`, **116 bytes**, containing only the `shell-init: error retrieving current directory: getcwd: …` lines · FIRST-INSTALL worktree none · `$OUT` none · device commands none evidenced · install transaction none · container none. *The `getcwd` problem is secondary: bash still entered the script; the silent `test -n` then stopped it.* Under the governing rule already established — **any refusal or failure spends the authority** — standing is `SID FIRST-INSTALL-01 · EXECUTED · STOP at authority precondition · authority SPENT`; **no retry inherits this authority.** Device effect: none — the script stopped before `STAMP`, worktree creation, `$OUT` creation, any `devicectl`, any reinstall invocation, any install, any container creation/capture. **FIRST-INSTALL carrier: NOT OPEN** — its required evidence (`$OUT` · `SHA256SUMS.install` · `container.txt` · `.last-reinstall` · reinstall record · install stamp · container literal) does not exist, so the two placeholders cannot lawfully be filled; running it would only manufacture another predictable refusal.

**Durable record, as the founder framed it:**

```text
SID FIRST-INSTALL-01
STOP — authority input absent at invocation
accepted pin        a052f081bc44b59d51a5d11ccec99f35951f9c37bb0cbee77ae77e560dbbb617
transcript          sid-first-install-01-transcript-20260915T235951Z.log · 116 bytes
device verbs reached 0
install             0
container           none
authority           SPENT
cause               the required authority file did not exist; K00_EXEC_AUTHORITY was therefore empty;
                    the line-3 authority gate refused the invocation
```

Terminal context, recorded separately and **not** as the cause of the governed STOP: the window's working directory (`ws-production-preflight-20260915-191643`) had been deleted (`getcwd` failure) and its scrollback carried another lane's command.

**Correction of this record's own reading.** §10.21 offered the classification "NOT ENTERED · authority intact" by analogy to the §10.3/§10.4 paste slips and the transport-refusal law. The founder's ruling draws the line differently and the ruling governs: the accepted pin **was invoked**, and its first gate's refusal is a refusal *of the act*, not a failure to reach it — the authority-first gate is part of the act it guards. The §10.21 reading is withdrawn; its facts stand. Lane law as now settled: **once the accepted pin is invoked, every refusal it or its instrument produces spends the authority, including the authority gate itself.** A future install needs **a new, separately opened FIRST-INSTALL act with fresh authority** — not a rerun of FIRST-INSTALL-01. Whether that act carries a new act identity (`-02` paths, a re-issued pin differing only in labels, as MAC-COMPILE-01 → 02 did) is the founder's to name when and if it is opened; this session drafts nothing toward it unasked.

**Standing after §10.22.** `SID FIRST-INSTALL-01` ⛔ STOP · spent · durable (record only; no `$OUT` to carry — the 116-byte transcript is the sole residue, left in place) · FIRST-INSTALL carrier ⛔ unopened · [D] execution ⛔ CLOSED · [E] execution ⛔ CLOSED · install 0 · launch 0 · sample 0 · source population 0 · container WITNESS-REQUIRED / not yet produced · siblings HOLD · next step record-only.

### 10.23 FOUNDER RULING (2026-09-16): `SID FIRST-INSTALL-02` OPEN under a new act identity · fresh authority ISSUED (uncommitted) · predecessor transcript to be carried under `prior-attempts/`, never via the `02` glob → **`02` pin re-issued, returned for review; NOT RUN**

**Ruling (verbatim in substance).** Proceed with a new act identity; do not reuse the spent `01` namespace. Re-issue the accepted 59-line pin with act-identity substitutions only — `sid-first-install-01-$SHA` → `-02-`, `…-01-out-$STAMP` → `-02-`, `…-01-$STAMP.SHA256SUMS.install` → `-02-`, final echo `SID-FIRST-INSTALL-01` → `-02`; future extraction path and transcript `/private/tmp/sid-first-install-02.sh` / `…-02-transcript-<stamp>.log`. *No substantive custody, device, install, post-install or sealing command may change*; subject `faf918b5c…` and instrument `3035c0235…` unchanged. *Same principle as MAC-COMPILE-02: act identity is constitutive, not a scope expansion.* Return the complete `01→02` diff and the new SHA / line count before execution. **Fresh authority for `02` issued in-session** — a seven-line payload of the same shape naming `SID FIRST-INSTALL-02`, the subject, the instrument, the bundle, the one-transaction bound, launch 0, sample 0 — to live at `/private/tmp/sid-fi02-authority.txt`, **never in git**. Preparation law for the eventual run shape: 1 fresh Terminal window · 2 `cd ~` · 3 create the payload file with exactly the seven lines · 4 `test -s` it · 5 `wc -l` and `wc -c` it · 6 `export K00_EXEC_AUTHORITY` from it · 7 only then extract / verify / invoke the accepted `02` pin. *Invocation of the accepted `02` pin spends this authority on any refusal or failure, including the authority gate itself.* **Predecessor transcript:** the successful `02` carrier carries the spent `01` attempt as lineage, but **not** by broadening the `02` transcript glob — explicitly, under `prior-attempts/SID-FIRST-INSTALL-01-STOP-transcript-20260915T235951Z.log`, with its disposition recorded (`FIRST-INSTALL-01 · STOP · authority precondition · spent · device verbs 0 · install 0 · container none`), so no future reader mistakes the `01` refusal transcript for `02` evidence.

**`02` pin re-issued — `docs/programme/VOICE-2026/SID_FIRST-INSTALL-02_PIN_2026-09-16.sh` · 59 lines · `c4b37e696e7b6936b8b4c592407a6c0bcecefa2a5b7dc65324940ea2d1858fdb`** (`bash -n` only; ⛔ NOT RUN). Derived mechanically from the accepted `01` pin (`a052f081…`) by the two label substitutions; the **complete** `diff` is five lines in two hunks and nothing else:

```diff
15,16c15,16
< WT=/private/tmp/sid-first-install-01-$SHA
< OUT=/private/tmp/sid-first-install-01-out-$STAMP
---
> WT=/private/tmp/sid-first-install-02-$SHA
> OUT=/private/tmp/sid-first-install-02-out-$STAMP
57,59c57,59
< ( cd "$OUT" && find . -type f | LC_ALL=C sort | xargs shasum -a 256 ) > "/private/tmp/sid-first-install-01-$STAMP.SHA256SUMS.install"
< mv "/private/tmp/sid-first-install-01-$STAMP.SHA256SUMS.install" "$OUT/SHA256SUMS.install"
< echo "SID-FIRST-INSTALL-01 $STAMP subject $SUBJECT_SHA instrument $SHA container $CONTAINER out $OUT"
---
> ( cd "$OUT" && find . -type f | LC_ALL=C sort | xargs shasum -a 256 ) > "/private/tmp/sid-first-install-02-$STAMP.SHA256SUMS.install"
> mv "/private/tmp/sid-first-install-02-$STAMP.SHA256SUMS.install" "$OUT/SHA256SUMS.install"
> echo "SID-FIRST-INSTALL-02 $STAMP subject $SUBJECT_SHA instrument $SHA container $CONTAINER out $OUT"
```

Lines 1–14 (authority gate, subject/instrument/bundle/device/pin constants, product path), 17–56 (every precondition, the seven-file manifest proof, the coexistence/absence/process reads, the single `k00-reinstall.sh` invocation, container capture, the bind reads) are byte-identical to the accepted pin. The only other `01` in the file is the lane branch name `claude/voice-2026-census-01` on the fetch line, which is not an act label. Subject and instrument SHAs unchanged. The `01` pin file is left in place as the accepted, spent act's artifact.

**Owed after the `02` pin is reviewed (not drafted unasked):** the run shape in the ruled seven-step form; the `02` carrier as a re-issue of the accepted `01` carrier with `01→02` labels **plus** the explicit `prior-attempts/` copy of the `01` STOP transcript and its disposition line in `RETURN.txt` — the founder's "witness before phenomenon" sequence suggests the carrier re-issue precedes the run; a ruling either way.

**Standing after §10.23.** FIRST-INSTALL-01 ⛔ STOP · spent · FIRST-INSTALL-02 ✅ OPEN · FI02 authority ISSUED (in-session, uncommitted; payload file not yet written on the Mac) · FI02 pin re-issued `c4b37e69…`, **review before run** · FIRST-INSTALL carrier execution CLOSED (`02` re-issue owed) · [D]/[E] execution CLOSED · install 0 · launch 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD.

### 10.24 Terminal non-event (2026-09-16): the §10.23 `01→02` diff text was pasted into a zsh window → `zsh: parse error near ')'` — nothing executed

The pasted buffer (`15,16c15,16` · `<`/`>`-prefixed lines · `---` · a line beginning `( cd "$OUT" …`) failed zsh's parser as a whole, and zsh executes nothing from a buffer it cannot parse — no redirection, no subshell, no variable assignment ran; the prompt returned unchanged. The window was again another lane's (`ws-stage2-20260915-200311`), not a fresh one. No repository, Mac-filesystem or device effect; no authority touched (`02` is open and unspent; its pin has not been invoked). Same lesson as §10.15, third form: diff text in this record is for reading; the only things meant for a terminal are the numbered run shapes, and those travel by `git show`. Nothing to do on the Mac now — the `02` pin awaits review, no run shape has been issued.

### 10.25 FOUNDER RULINGS (2026-09-16): `02` pin ACCEPTED (`c4b37e69…`; the zsh paste did not spend the authority — the pin was never invoked) · `02` carrier re-issued BEFORE the run with the `01` STOP transcript under `prior-attempts/` · run shape HELD until the carrier is reviewed → carrier returned

**Rulings (verbatim in substance).** (1) The `02` pin at the fetched lane tip verified — 59 lines, `c4b37e696e7b6936b8b4c592407a6c0bcecefa2a5b7dc65324940ea2d1858fdb`; the complete diff against the accepted `01` pin contains only the four act-identity substitutions; no custody test, product pin, device read, install invocation, post-install bind, coexistence check, process check or sealing semantics changed → **ACCEPTED**; authority issued · unspent; execution not yet invoked; *the failed zsh paste does not spend this authority because the accepted `02` pin was never invoked.* (2) Re-issue the `02` carrier before the install run — *the carrier reviewed before the phenomenon it will preserve exists*: the accepted FIRST-INSTALL carrier shape re-keyed to `02`, with one deliberate lineage addition (`02` evidence = `sid-first-install-02-transcript-*.log`; `prior-attempts/SID-FIRST-INSTALL-01-STOP-transcript-20260915T235951Z.log`), `RETURN.txt` explicitly distinguishing the predecessor (`PRIOR FIRST-INSTALL-01 · STOP · authority-precondition refusal · SPENT · device-verbs 0 · install 0 · container none`), the `01` transcript never mixed into the `02` glob or represented as `02` evidence; all accepted laws retained (explicit stamp · explicit container · `container.txt == supplied literal` · whole `$OUT` · install-seal verification · exact executed `02` pin · `02` transcripts · independent run seal · carrier branch/commit). (3) **HOLD the run shape** until the carrier is reviewed — *this removes the last post-hoc design variable before installation*; then fresh Terminal · `cd ~` · authority-file existence check first.

**`02` carrier returned — `docs/programme/VOICE-2026/SID_FIRST-INSTALL-02_CARRIER_2026-09-16.sh` · 48 lines · `5ef5523361660929cfd827c856054a12c57c19d6926c53396a3e47e14a29fca9`** (`bash -n` only; ⛔ execution CLOSED). Derived from the accepted `01` carrier (`bf4283b7…`) by the label substitutions, plus exactly these additions, each visible in the diff:

| addition | line(s) | why |
|---|---|---|
| `PRIOR01=/private/tmp/sid-first-install-01-transcript-20260915T235951Z.log` · `test -f "$PRIOR01"` | 18–19, before any custody is created | the lineage file is a precondition — a `02` carrier that cannot carry the `01` STOP refuses rather than silently omitting it |
| `mkdir -p "$DEST/prior-attempts"` · `cp "$PRIOR01" "$DEST/prior-attempts/SID-FIRST-INSTALL-01-STOP-transcript-20260915T235951Z.log"` | 34–35, after the `02` transcripts are copied by their own glob | explicit path, explicit name, never the `02` glob |
| `ls -la "$PRIOR01" >> OUT-listing.txt` | 38 | the predecessor's size/mtime are listed beside the `02` files, labelled by path |
| run seal built with `find . -type f ! -name SHA256SUMS.run ! -name RETURN.txt`, written to `/private/tmp/sid-first-install-02-carrier-$STAMP.SHA256SUMS.run`, then moved in | 40–41 | **disclosed adaptation, not a law change**: the accepted `01` carrier sealed with `ls -A` at the top level, which cannot hash a directory — `prior-attempts/` would have made `shasum` fail or the predecessor go unsealed. `find` descends, keeps the dot-file `.last-reinstall`, excludes the seal and `RETURN.txt` exactly as before, and writing outside `$DEST` first is the §10.14 hygiene. Self-tested here on a synthetic tree (top-level file · dot-file · subdirectory file): 3 entries, 3 OK on `shasum -c`, seal and `RETURN.txt` excluded |
| `RETURN.txt` gains the four-line `PRIOR FIRST-INSTALL-01` block, ending `(not 02 evidence)` | 42 | the founder's disposition text, verbatim in substance |

Everything else — the two refusing placeholders (`__FIRST_INSTALL_STAMP__`, `__SID_CONTAINER_FROM_FIRST_INSTALL__`), the stamp and UUID shape tests, `container.txt` equality with the supplied literal, the install-seal verification, the whole-`$OUT` copy, the visible `last-reinstall.txt`, the exact executed `02` pin from `/private/tmp/sid-first-install-02.sh`, branch `feature/sid-first-install-02-evidence-<stamp>`, `DEST = driver-ledger/VPIO-02-SID-FIRST-INSTALL-02-<stamp>/`, commit, push, echo — is the accepted `01` law re-keyed to `02`. Complete `diff` against the `01` carrier: 8 hunks, all shown in this session's return; no glob touches `01` files.

**Standing after §10.25.** FIRST-INSTALL-01 ⛔ STOP · spent · FIRST-INSTALL-02 ✅ OPEN · pin ACCEPTED `c4b37e69…` · authority issued, unspent, uncommitted · **not executed** · `02` carrier re-issued `5ef55233…`, **review next** · run shape HELD · [D]/[E] execution CLOSED · install 0 · launch 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD.

### 10.26 FOUNDER RULINGS (2026-09-16): `02` carrier ACCEPTED (`5ef55233…`; hard predecessor precondition and the `find` seal both accepted; execution not open until `02` produces evidence) · **seven-step run shape for `SID FIRST-INSTALL-02` ISSUED in-session**

**Carrier ruling (verbatim in substance).** Reviewed at `864cb37ef` against the accepted `01` carrier: 48 lines, `5ef5523361660929cfd827c856054a12c57c19d6926c53396a3e47e14a29fca9`. Hard predecessor precondition: yes — the lineage is ruled part of the durable evidence for why `02` exists; silently omitting it would weaken custody; it stays segregated under `prior-attempts/` and labelled "not 02 evidence". `ls -A` → recursive `find` seal: yes — the necessary adaptation once `prior-attempts/` is a directory; the predecessor transcript is now *sealed*, not merely copied. All other accepted laws confirmed retained. Execution ⛔ not open until FIRST-INSTALL-02 produces evidence.

**Run shape ruling (verbatim in substance).** Issue it next, in exactly the ruled order: 1 genuinely fresh Terminal window · 2 `cd ~` · 3 create `/private/tmp/sid-fi02-authority.txt` with exactly the seven issued authority lines · 4 `test -s` the file, then `wc -l` and `wc -c` · 5 `export K00_EXEC_AUTHORITY` from that file · 6 fetch/extract the accepted `02` pin and verify `c4b37e696e7b6936b8b4c592407a6c0bcecefa2a5b7dc65324940ea2d1858fdb` / 59 lines · 7 invoke exactly once with its `02` transcript path. *The `test -s` belongs before export and before pin invocation, so a missing payload cannot spend FIRST-INSTALL-02 through the same failure mode as `01`.*

**Issued (in-session, this record carries the shape and the expected custody values, never the payload text — ruling: the authority is not put in git).** Step 3 is a `cat > … <<'EOF'` heredoc of the founder's seven lines; expected step-4 readings for the correctly written file: **`wc -l` = 7 · `wc -c` = 244** (computed here from the issued lines and discarded; any other value = do not export, do not proceed). Steps 4–7 are individually `&&`-free lines so a failure is visible where it happens; step 7 is the one `bash /private/tmp/sid-first-install-02.sh 2>&1 | tee /private/tmp/sid-first-install-02-transcript-$(date -u +%Y%m%dT%H%M%SZ).log; echo "rc=${PIPESTATUS[0]}"`. Extraction is `git show 864cb37ef:docs/programme/VOICE-2026/SID_FIRST-INSTALL-02_PIN_2026-09-16.sh` (the file is byte-identical at every commit since `ec454f16b`). Expected final line on success: `SID-FIRST-INSTALL-02 <stamp> subject faf918b5c… instrument 3035c0235… container <CONTAINER> out /private/tmp/sid-first-install-02-out-<stamp>`; a `.REFUSED.txt` / `.HELD.txt` in `$OUT`, or an exit before `$OUT` exists, is a STOP that spends the authority. Returned after the run, whatever its fate: the transcript's last lines and `ls -A "$OUT"` (or the statement that `$OUT` does not exist). The accepted `02` carrier then needs its own explicit opening, with the two placeholders filled from the final echo and `container.txt` only.

**Standing after §10.26.** FIRST-INSTALL-01 STOP · spent · FIRST-INSTALL-02 OPEN · pin ACCEPTED · carrier ACCEPTED (execution closed) · authority issued · unspent · run shape ISSUED · **pin not yet invoked** · [D]/[E] execution CLOSED · install 0 · launch 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD.

### 10.27 `SID FIRST-INSTALL-02` INVOKED (founder transcript, 2026-09-16 ~00:12Z) → **STOP at the pre-install process precondition: the historical VPIO-02 harness (`E3B88028-…`, PID 3347) was RUNNING on the device** · no instrument invocation · no install · no container · classification and disposition returned for ruling

**Preparation, as ruled.** Fresh window (`Last login … ttys025`, prompt at `~`); steps 3–6 read exactly as expected — `authority-file-present` · `7` · `244` · `authority-exported` · pin `c4b37e696e7b6936b8b4c592407a6c0bcecefa2a5b7dc65324940ea2d1858fdb` · `59`. (Terminal context, recorded separately, not cause: before step 3 the founder ran unrelated acts in the same window — two pastes of the §10.23 diff text, each `zsh: parse error`, nothing executed; a `git switch`/`pull` of another lane's branch in the shared checkout that failed as *invalid reference* / *divergent branches*, HEAD unchanged at `0bd2b6578`; a read-only temporal-memory audit against production and two `docker logs` greps. None touched this lane's files, worktrees or device state, but the window was no longer "genuinely fresh" at step 7. The pin uses absolute paths and `git -C`, so none of it reached the act.)

**Step 7 — the accepted `02` pin, invoked once.** Fetch → worktree `/private/tmp/sid-first-install-02-3035c0235…` at HEAD exact → `head.txt` printed `3035c02353b3cc0dab0b0ce1823116d6712b8eb1`. **Every local custody gate passed** (the script reached `mkdir -p "$OUT"`, which follows them all): product directory present · dylib SHA `a15b399d…` · executable SHA `db036694…` · dylib UUID `4A6AD464-…` · manifest self-SHA `699ac758…` · 7 lines · **all seven product hashes and the exact file set proven against the manifest** — the first time the compiled SID product has been proven against its manifest on the Mac since MAC-COMPILE-02 produced it. Then the three read-only device reads: (1) `.vpio02` → `VoiceKernel VPIO-02 · life.soullab.voicekernel.vpio02 · 0.0.1` — exactly one ✓ · (2) `.vpio02sid` → `Apps installed:` empty — absent ✓ · (3) `info processes` → the full process table, containing **PID 3347 `/private/var/containers/Bundle/Application/E3B88028-A10F-46B1-AB27-CF0A1F83FB78/VoiceKernelHarness.app/VoiceKernelHarness`** → `test "$(grep -ci VoiceKernelHarness …)" = 0` FAILED (count 1) → `set -e` **STOP** on pin line 41, before the instrument line 42. `rc=` empty (C-D26). **Not reached:** `k00-reinstall.sh` (so no just-in-time absence read, no custody gate of its own, no `K00_EXEC_AUTHORITY` consumption by the instrument, **no install verb**), no `.last-reinstall`, no `container.txt`, no post-install reads, no seal, no final echo.

**What exists after the act (Mac).** Worktree `/private/tmp/sid-first-install-02-3035c02353b3cc0dab0b0ce1823116d6712b8eb1` · `$OUT = /private/tmp/sid-first-install-02-out-<stamp>` holding `head.txt` · `apps-before-vpio02.json` · `apps-before-vpio02sid.json` · `processes-before.json` (no `SHA256SUMS.install` — the seal is written only at the end) · one transcript `/private/tmp/sid-first-install-02-transcript-<stamp>.log` · the payload file · the extracted pin. Device: three reads, zero writes; `.vpio02` still exactly one install; `.vpio02sid` absent; **the `.vpio02` harness process was alive at 20:12:19 local.**

**The substantive finding.** The historical VPIO-02 harness was running. Nothing in this lane launched it: the `02` pin contains no launch or terminate verb; no batch, driver test or reinstall has run since the S2 population of 2026-09-15 (which terminates the harness per sample and whose last act closed at ~18:41Z); the process's PID sits inside a cluster of user apps with adjacent PIDs (3345–3352: ScreenshotServicesService, GoveeHome, **VoiceKernelHarness**, Chess, Red Alert, Venmo …), the shape of apps being relaunched together — a device restart restoring the app switcher, or the switcher being paged through. **Cause unknown; not inferred.** What it means for the lane: (i) the pin's harness-process precondition did its job — the precondition exists precisely so that a SID first install is never performed beside a live historical harness whose audio session and container state are unobserved; (ii) the `.vpio02` subject is not "never launched" as of this reading — it was launched by something outside the lane, which is a fact about the device environment, not a mutation by any governed act; (iii) the coexistence law is unbroken (one `.vpio02`, zero `.vpio02sid`).

**Classification — returned for ruling; the settled rule applied.** Under §10.22 (*once the accepted pin is invoked, every refusal it or its instrument produces spends the authority*) and the §10.23/§10.26 restatement for `02` (*any refusal or failure, including the authority gate itself*), this is `SID FIRST-INSTALL-02 · EXECUTED · STOP at the pre-install harness-process precondition · authority SPENT`. The founder rules. Distinct from `01`: the authority was present and correct, the local custody chain passed in full, and the STOP is a **device-state** refusal — the first such in this act's lineage.

**Disposition questions (founder's, not this session's).** (a) The lingering `.vpio02` harness must be gone before any future first-install attempt can pass line 41 — by what act? Candidates: a manual quit on the phone (a device act outside the automated-driver law, leaving no evidence except a later process read); the existing driver `testTerminateOnly` (`K00DriverTests.swift:154`, the batch's own per-sample precondition tool) under a bounded, separately opened terminate-only authority with a process read before and after; or waiting for the device to end it. (b) Whether the `02` STOP evidence (`$OUT` with four files, the transcript) is carried now: the accepted `02` carrier is for a *successful* result — its placeholders cannot be lawfully filled (no container, no `SHA256SUMS.install`, no `container.txt`) and it would refuse; a STOP carrier would be a new draft. (c) A future attempt is a new act (`03`) with fresh authority and a re-issued pin — never a rerun of `02` — and, per the §10.18 principle, its preconditions should not be relaxed to fit the phenomenon: the harness-absent test stays.

**Standing after §10.27.** FIRST-INSTALL-01 STOP · spent · **FIRST-INSTALL-02 EXECUTED · STOP (device-state: historical harness running) · SPENT under the settled rule, ruling owed** · `02` carrier NOT OPEN (no successful result) · [D]/[E] execution CLOSED · install 0 · launch 0 (by this lane) · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD · `.vpio02` harness process observed ALIVE at the time of the act, cause unknown.

### 10.28 FOUNDER RULINGS (2026-09-16) on §10.27: `02` classified STOP · SPENT · terminate-only act OPEN as a draft · `02` STOP carrier draft OPEN · `FIRST-INSTALL-03` OPEN IN PRINCIPLE (pin may be drafted; authority NOT issued; execution NOT open) → three drafts returned

**Ruling 1 — classification (verbatim in substance).** `SID FIRST-INSTALL-02 · EXECUTED · STOP · pre-install harness-process precondition · authority SPENT` — local custody gates PASS · historical `.vpio02` present PASS (exactly 1) · SID `.vpio02sid` absent PASS · harness-process precondition **FAIL — VoiceKernelHarness PID 3347 present** · instrument invocation NOT REACHED · just-in-time absence read NOT REACHED · install 0 · container none · launch by this act 0 · sample 0 · device writes 0 · device reads 3. *The cause of the running historical harness remains unknown; the app-switcher explanation is recorded as no more than a possible external cause.* A governed refusal after invocation, not a preparation failure.

**Ruling 2 — end the lingering harness by a separate terminate-only act, `SID HISTORICAL-HARNESS-TERMINATE-01`** (verbatim in substance): the existing `testTerminateOnly` path; *do not manually quit it on the phone, do not wait and hope*. PRE: read process state · prove the VoiceKernelHarness process present · bind it to the historical `.vpio02` environment as far as the existing instrument can lawfully establish. ACT: exactly one existing `testTerminateOnly` invocation · no launch-for-observation · no reinstall · no uninstall · no sample · no source read · no SID bundle interaction. POST: read process state again · require VoiceKernelHarness absent · seal evidence. *If it refuses or fails, stop; no fallback to manual killing.* Draft first, review before execution.

**Ruling 3 — a separate STOP carrier for `02`** (verbatim in substance): the success carrier is correctly unusable (no `container.txt`, no `SHA256SUMS.install`, no reinstall record, no successful stamp); carry the partial `$OUT` (`head.txt` · the two apps reads · `processes-before.json`), the accepted `02` pin, the `02` transcript(s), **authority-file metadata only** (line/byte count, hash — never contents), the STOP disposition (precondition · PID 3347 as observed · instrument not reached · install 0 · container none · device reads 3 · writes 0), an independent carrier seal, `RETURN.txt`; *do not fabricate an install seal*; keep the `01` STOP lineage separate rather than merging the two acts into one evidence set.

**Ruling 4 — `FIRST-INSTALL-03` OPEN IN PRINCIPLE, not executable** (verbatim in substance): new act identity, label-only re-issue of the accepted pin; *do not relax the harness-absent precondition — that refusal just proved why it exists*. `03` may not receive a run shape until: 1 the `02` STOP carrier is durable · 2 the terminate-only act is completed · 3 the post-termination process read proves the harness absent · 4 the `03` pin diff is reviewed and accepted · 5 fresh `03` authority is issued. No `03` authority yet.

**Draft A — `docs/programme/VOICE-2026/SID_HISTORICAL-HARNESS-TERMINATE-01_PIN_DRAFT_2026-09-16.sh` · 51 lines · `4bad7211317cfc127b62ce997ba8dc417859c0ead00f2fbc76c25937b43e93a5`** (`bash -n` only). Authority gate first (`test -n "$K00_EXEC_AUTHORITY"`; payload founder-authored at opening, never in git). Instrument SHA `3035c0235…`, own worktree `/private/tmp/sid-hht-01-<SHA>`, HEAD exact, batch/reinstall/driver unmodified; `$OUT` created only after those. **PRE (read-only):** `info processes` → require ≥ 1 VoiceKernelHarness line; tee the matching lines to `harness-processes-before.txt`; **bind:** require ≥ 1 line whose path contains `E3B88028-…/VoiceKernelHarness.app` **and** require the VoiceKernelHarness count to equal that historical-container count (so no harness of any other container is present — the strongest binding the process table can lawfully give); `apps --bundle-id …vpio02sid` → 0 (no SID bundle exists, so no SID interaction is possible). **ACT:** `xcodegen generate` for the driver project only, `build-for-testing` of `DriverUITests` into the worktree's own derived-data path (the batch's own lines, verbatim in substance — the harness is untouched), then **exactly one** `xcodebuild test-without-building … -only-testing:DriverUITests/K00DriverTests/testTerminateOnly` with the batch's runner env (`TEST_RUNNER_K00_MODE=L VP=on HOLD_S=15 W4_MS=500 SUBJECT=vpio-02` — the subject selects the historical bundle in the driver's table), tee'd to `terminate-only.log`; require `** TEST SUCCEEDED **` and the `testTerminateOnly]' passed` line. **POST (read-only):** `info processes` → VoiceKernelHarness = 0 · `apps` `.vpio02` = 1 with its container present · `apps` `.vpio02sid` = 0. Seal `SHA256SUMS.terminate` written outside `$OUT`, moved in; echo `SID-HISTORICAL-HARNESS-TERMINATE-01 <stamp> instrument … historical … container … harness-after 0 out …`. Verb census: `install app` 0 · `uninstall` 0 · `launch` 0 · `testOneSample`/`testOutputSample` 0 · `testTerminateOnly` invoked once. `XCUIApplication.terminate()` acts on the historical bundle by identifier and is the same call the batch has used for every lingering-harness case since DRIVER-01.

**Draft B — `docs/programme/VOICE-2026/SID_FIRST-INSTALL-02_STOP-CARRIER_DRAFT_2026-09-16.sh` · 49 lines · `e593448710128d941ae969b586badf5fd7127361b822a9209d4a330fe302fdc4`** (`bash -n` only). One placeholder, `__FIRST_INSTALL_02_STAMP__`, refusing on its prefix; it is filled from the `02` `$OUT` directory name / transcript stamp (one read-only `ls -d /private/tmp/sid-first-install-02-out-*` on the Mac supplies it). Preconditions prove the act was a STOP of exactly this shape: the four partial files present · **`SHA256SUMS.install`, `container.txt`, `.last-reinstall` absent** (a `$OUT` that went further belongs to the success carrier, and this carrier refuses it) · `head.txt` = the instrument SHA · ≥ 1 harness line in `processes-before.json` · the executed pin present and hashing to `c4b37e69…` · the authority file present (for metadata only). Carries: whole partial `$OUT` · the pin · `sid-first-install-02-transcript-*.log` · `authority-metadata.txt` (lines / bytes / sha256 — **contents never copied**) · `harness-processes-observed.txt` (the matching process lines, PID as observed) · listing · `SHA256SUMS.run` via `find`, written outside then moved · `RETURN.txt` with the ruled disposition line by line and `INSTALL-SEAL none (never reached sealing; none fabricated)`. **One choice disclosed:** the `01` STOP transcript rides under `prior-attempts/` again, labelled *separate act, not 02 evidence*, and `RETURN.txt` carries the `01` disposition block — because the `02` success carrier that was to make `01` durable will never run, and this keeps the two acts segregated by directory and label rather than merged. If the founder prefers `01` carried by its own carrier, the `prior-attempts/` lines are removed. Branch `feature/sid-first-install-02-stop-evidence-<stamp>`, `DEST = driver-ledger/VPIO-02-SID-FIRST-INSTALL-02-STOP-<stamp>/`.

**Draft C — `docs/programme/VOICE-2026/SID_FIRST-INSTALL-03_PIN_DRAFT_2026-09-16.sh` · 59 lines · `9b3526c4967afc18ad8d46ff28b370948abcd87c163772576ae28741a793c7a5`** (`bash -n` only). Derived from the accepted `02` pin (`c4b37e69…`) by `sid-first-install-02` → `-03` and `SID-FIRST-INSTALL-02` → `-03`; diff = the same five lines in two hunks as `01→02` (worktree, `$OUT`, seal temp path, final echo); every custody, device-read, instrument, bind and seal line byte-identical; **the harness-absent precondition (line 41) unchanged.** Future extraction path `/private/tmp/sid-first-install-03.sh`, transcript `sid-first-install-03-transcript-<stamp>.log`.

**Standing after §10.28.** FIRST-INSTALL-01 STOP · spent · FIRST-INSTALL-02 STOP (harness-process precondition) · spent · **`02` STOP carrier DRAFTED** (placeholder; execution closed) · **HISTORICAL-HARNESS-TERMINATE-01 DRAFTED** (authority not issued; execution closed) · **FIRST-INSTALL-03 open in principle, pin DRAFTED**, authority NOT issued, execution NOT open (five-step dependency) · [D]/[E] execution CLOSED · install 0 · launch by this lane 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD · no device act.

### 10.29 `02` STOP carrier — placeholder FILLED from the founder's read-only listing (2026-09-16): `/private/tmp/sid-first-install-02-out-20260916T001215Z` → `SID_FIRST-INSTALL-02_STOP-CARRIER_2026-09-16.sh` · 49 lines · `5e0b755a6e7a0870ed4b6897da518a2ff279dda40f19295f592e18c54b2b7b4d` · returned for review · NOT RUN

The founder ran `ls -d /private/tmp/sid-first-install-02-out-*` and returned exactly one directory, stamp `20260916T001215Z` (00:12:15Z on 2026-09-16 = 20:12:15 local, consistent with the `20:12:18` devicectl lines in the `02` transcript). The filled carrier differs from the draft (`e5934487…`) by **one line**: `STAMP=__FIRST_INSTALL_02_STAMP__` → `STAMP=20260916T001215Z`; the `case … __*)` refusal guard remains as the structural check. The draft file is kept beside it as the reviewed shape. No run shape issued; the `02` STOP carrier's execution stays CLOSED pending the founder's review of the draft shape (§10.28 Draft B) and of this filled instance. Nothing else changes.

### 10.30 FOUNDER RULINGS (2026-09-16): terminate-only pin AMEND (just-in-time bound process read) → amended · `02` STOP carrier ACCEPTED (`5e0b755a…`; `01` stays under `prior-attempts/`) → **run shape ISSUED** · `03` pin ACCEPTED (`9b3526c4…`; authority unissued; execution closed) · order = STOP carrier first · terminate authority HELD

**Rulings (verbatim in substance).** (1) Terminate-only pin: the container binding is correct against the STOP evidence (1 harness line = 1 historical-container line, PID 3347, `E3B88028-…`); both success checks good; **amend**: add one just-in-time process read immediately before `test-without-building`, because `xcodegen`/`build-for-testing` can take long enough for the harness to disappear and `testTerminateOnly` is conditional (`if app.state != .notRunning { app.terminate() }`) — without the read the test could pass as a no-op and the act could not say it encountered the lingering harness; require VoiceKernelHarness ≥ 1 and every harness path = the historical container. Chain: PRE present → driver build only → JUST-IN-TIME still present → exactly one `testTerminateOnly` → POST absent. No other change. (2) `02` STOP carrier **ACCEPTED** as filled (`20260916T001215Z` · 49 lines · `5e0b755a…`; one-line diff vs the draft); the `01` transcript stays under `prior-attempts/` — the directory boundary and `RETURN.txt` wording already establish *01 STOP = separate prior act, not 02 evidence*; no separate `01` carrier now; **may receive a run shape**. (3) `03` pin **ACCEPTED** (`9b3526c4…`): only the five identity lines move, everything substantive byte-identical, harness-absent precondition UNCHANGED; execution closed, no authority. (4) Order: **STOP carrier first** — the `02` STOP evidence is frozen on the Mac, carrying it is read-only, and it should be durable *before we intentionally alter the very device state that caused the STOP*; no need to race the live process — if it disappears first, the just-in-time precondition will say so and the terminate act refuses rather than pretending. (5) Terminate authority **HELD** until the carrier lands and the amended pin is accepted; the founder then authors a fresh payload.

**Terminate-only pin amended → `SID_HISTORICAL-HARNESS-TERMINATE-01_PIN_DRAFT_2026-09-16.sh` · 55 lines · `576fd2b2bc0eca0cb15f1791a92bc2dcd76b6aeb659a0b638a2b4a4335ee8957`.** Diff vs `4bad7211…`: four lines inserted after the `DIAG_FLAGS` line and immediately before the `env … test-without-building … testTerminateOnly` line — `info processes` → `processes-just-before-terminate.json` · require ≥ 1 VoiceKernelHarness · require the harness count = the historical-container count · tee the matching lines to `harness-processes-just-before-terminate.txt`. Nothing else changed. Returned for review; authority not issued; execution closed.

**`02` STOP carrier run shape — ISSUED (read-only act; needs no authority; any window whose cwd exists, `cd ~` first).** Extraction from `1f4baa7b5` (the file is byte-identical at every later commit); custody check before the last line: `5e0b755a6e7a0870ed4b6897da518a2ff279dda40f19295f592e18c54b2b7b4d` and `49`. Expected final line `SID-FIRST-INSTALL-02 STOP CARRIER PUSHED` after a `feature/sid-first-install-02-stop-evidence-20260916T001215Z` push; a refusal at any `test` line is a STOP of the carrier (not of any device act) and is returned as such. Then here: cherry-pick `-x`, reproduce `SHA256SUMS.run`, confirm no install artefact travelled, read `processes-before.json` for the PID 3347 line, record as §10.31.

**Standing after §10.30.** FIRST-INSTALL-01 STOP · spent · FIRST-INSTALL-02 STOP · spent · `02` STOP carrier ACCEPTED · run shape issued · NOT YET RUN · terminate-only pin AMENDED, review pending · terminate authority unissued · execution closed · `03` pin ACCEPTED · `03` authority unissued · execution closed · [D]/[E] CLOSED · install 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD.

### 10.31 `02` STOP carrier LANDED and READ (2026-09-16): `feature/sid-first-install-02-stop-evidence-20260916T001215Z` @ `3f3dcfb402f11e6688602f3cc6c00e529d564ea8` → cherry-picked `-x` · seal reproduced · no install artefact travelled · PID 3347 bound to `E3B88028-…` from custody · `02` STOP evidence DURABLE

**Transport.** Extraction matched custody (`5e0b755a…`, 49 lines); worktree at the lane tip `8b2d2dcb4`; pre-commit hooks passed on the `feature/*` branch; commit `3f3dcfb40`, 12 files, 1,823 lines; final line `SID-FIRST-INSTALL-02 STOP CARRIER PUSHED`; `rc=` empty (C-D26). Cherry-picked here with `-x`.

**Custody reproduced.** `SHA256SUMS.run` = `15e71bd9a485016cbcb23a24a68900be9fbf47bb3cc615db3ceef196d8b060b2` (the value the Mac printed), 10 entries, **0 mismatches** — the seal descends into `prior-attempts/` as designed. Directory contents exactly as the carrier declares: `head.txt` · `apps-before-vpio02.json` · `apps-before-vpio02sid.json` · `processes-before.json` · `sid-first-install-02.sh` · `sid-first-install-02-transcript-20260916T001215Z.log` · `authority-metadata.txt` · `harness-processes-observed.txt` · `OUT-listing.txt` · `prior-attempts/SID-FIRST-INSTALL-01-STOP-transcript-20260915T235951Z.log` · `SHA256SUMS.run` · `RETURN.txt`. **No install artefact travelled:** `SHA256SUMS.install`, `container.txt`, `.last-reinstall` — none present (0 of 3), as the carrier's preconditions required. `head.txt` = `3035c02353b3cc0dab0b0ce1823116d6712b8eb1`. Executed pin hashes to `c4b37e69…` (the accepted `02` pin). `authority-metadata.txt`: lines 7 · bytes 244 · sha256 `b15364106ff2616bcbf07ab17c16cd621c9305fbb76d7ba303f7ba28fec253bb` — **contents not carried**, as ruled. Predecessor transcript present under `prior-attempts/`, 116 bytes (= the §10.22 figure), labelled in `RETURN.txt` as a separate act, not `02` evidence.

**The STOP, read from custody rather than from the terminal.** `apps-before-vpio02.json`: `"bundleIdentifier" : "life.soullab.voicekernel.vpio02"` exactly once, container `E3B88028-…` present once. `apps-before-vpio02sid.json`: `"bundleIdentifier" : "…vpio02sid"` **0** entries (the file's two other `vpio02sid` strings are the query echo and `matchingBundleIdentifier`, not installed-app entries — the pin's test keys on the `bundleIdentifier` form, so the read is exactly as the pin evaluated it). `processes-before.json`: 318 process entries; exactly one whose executable is `…/Bundle/Application/E3B88028-A10F-46B1-AB27-CF0A1F83FB78/VoiceKernelHarness.app/VoiceKernelHarness`, and the `processIdentifier` in that same entry is **3347** — the binding the founder's classification named, now witnessed in the JSON rather than in the human-readable table. The transcript's last lines are the process table's tail (PIDs 3457–3459) and nothing after — the pin's line 41 refused and no instrument line ran, consistent with §10.27.

**What is now durable.** `SID FIRST-INSTALL-02 · EXECUTED · STOP · pre-install harness-process precondition · SPENT`, with its partial `$OUT`, the executed pin, the transcript, the authority metadata and the `01` lineage sealed together on this branch. Step 1 of the `03` dependency (*`02` STOP carrier durable*) is satisfied. Steps 2–5 (terminate act completed · post-termination read = harness absent · `03` pin accepted [done at §10.30] · fresh `03` authority) remain.

**Standing after §10.31.** FIRST-INSTALL-01 STOP · spent · durable (via `prior-attempts/`) · FIRST-INSTALL-02 STOP · spent · **durable** · terminate-only pin amended (`576fd2b2…`), review pending · terminate authority unissued · execution closed · `03` pin ACCEPTED · authority unissued · execution closed · [D]/[E] CLOSED · install 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD · the historical harness's current state UNKNOWN since 00:12:19Z (no read since).

### 10.32 FOUNDER RULINGS (2026-09-16): `02` STOP carrier independently verified · amended terminate-only pin ACCEPTED (`576fd2b2…`) · **`SID HISTORICAL-HARNESS-TERMINATE-01` OPEN for exactly one `testTerminateOnly`** · authority ISSUED in-session (never in git) → seven-step run shape ISSUED

**Rulings (verbatim in substance).** STOP carrier durable and independently verified (commit `3f3dcfb40` · result PUSHED · run seal PASS · authority 7 lines / 244 bytes, contents not carried · PID path `E3B88028-…/VoiceKernelHarness.app` · install 0 · container none; `01` correctly segregated under `prior-attempts/`). Amended pin reviewed at `8b2d2dcb4`: the just-in-time block is exactly where it belongs (initial PRE present and container-bound → driver build preparation → JUST-BEFORE-TERMINATE harness ≥ 1, every harness process bound to `E3B88028…`, matching lines captured → exactly one `testTerminateOnly` → POST harness = 0, `.vpio02` still installed, `.vpio02sid` still absent); `testTerminateOnly` appropriately narrow (terminates only when the historical app is running, waits for `.notRunning`, never the launch/sample path); dual success checks retained → **pin ACCEPTED · authority may be issued · execution OPEN for one terminate-only act. Any refusal or failure after invocation spends this authority; no manual fallback inherits it.** Authority payload issued in-session: eight lines naming the act, the instrument, the historical bundle and container, *exactly one testTerminateOnly invocation*, launch 0, install 0, sample 0 — **kept outside git**, in a fresh file created in a genuinely fresh window; `test -s` before export and before invocation.

**Issued (this record carries the shape and the expected custody values, never the payload text).** Payload path `/private/tmp/sid-hht01-authority.txt`; expected step-4 readings for the correctly written file: **`wc -l` = 8 · `wc -c` = 275** (computed here from the issued lines and discarded; an earlier draft of this sentence said 293 — a figure written before the computation returned, corrected here in place with this note). Extraction `git show 8b2d2dcb4:docs/programme/VOICE-2026/SID_HISTORICAL-HARNESS-TERMINATE-01_PIN_DRAFT_2026-09-16.sh` → `/private/tmp/sid-hht-01.sh`; custody `576fd2b2bc0eca0cb15f1791a92bc2dcd76b6aeb659a0b638a2b4a4335ee8957` · 55 lines (the file is byte-identical at every later commit; the `_DRAFT_` token in the name is kept because renaming would change the accepted hash). One invocation tee'd to `/private/tmp/sid-hht-01-transcript-<stamp>.log`. Expected final line on success: `SID-HISTORICAL-HARNESS-TERMINATE-01 <stamp> instrument 3035c0235… historical life.soullab.voicekernel.vpio02 container E3B88028-… harness-after 0 out /private/tmp/sid-hht-01-out-<stamp>`. STOP forms: an exit at the PRE reads (harness absent by the time of the act — the founder's *if it disappears first, the act refuses rather than pretending* case; then no terminate ran and the device state is simply the read), at the driver build, at the just-in-time read, at the `TEST SUCCEEDED` / `passed` checks, or at the POST reads (harness still present after the one call = STOP with the device state read, no second call). Returned after the run, whatever its fate: the transcript's last lines and `ls -A "$OUT"`. Evidence carrier for `$OUT` follows on its own opening.

**Standing after §10.32.** TERMINATE-01 pin ACCEPTED · authority ISSUED (uncommitted) · execution OPEN · run shape ISSUED · **not yet invoked** · FIRST-INSTALL-01/02 STOP · spent · durable · `03` pin ACCEPTED · `03` authority unissued · execution closed (steps 2–3 pending on this act) · [D]/[E] CLOSED · install 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD.

### 10.33 `SID HISTORICAL-HARNESS-TERMINATE-01` INVOKED (founder transcript, 2026-09-16 ~00:29Z) → **STOP · UI-test runner initialization failure · SPENT** (founder classification, verbatim in substance) · the failure mode has lane precedent · terminate-01 STOP carrier DRAFTED

**Preparation and custody, as ruled.** Fresh window at `~`; payload present, `8` / `275`; `authority-exported`; pin `576fd2b2…` / 55. Invoked once.

**Founder classification (verbatim in substance), the record of this act:**
```text
SID HISTORICAL-HARNESS-TERMINATE-01
EXECUTED · STOP · UI-test runner initialization failure · authority SPENT
PRE historical harness present        PASS      (PID 3347, E3B88028-… path, 20:29:25 local)
PRE historical-container binding      PASS
SID bundle absent                     PASS
driver build-for-testing              reached   (xctestrun DriverUITests_iphoneos26.2-arm64.xctestrun in the act's worktree)
JUST-IN-TIME harness present          PASS      (same PID, same path, 20:29:32 local)
JUST-IN-TIME historical binding       PASS
xcodebuild test invocation            ATTEMPTED ONCE
UI automation initialization          FAIL · timeout
                                      ("DriverUITests-Runner (3537) … The test runner failed to initialize for UI testing.
                                        (Underlying Error: Timed out while enabling automation mode.)")
testTerminateOnly body                NOT EXECUTED
XCUIApplication.terminate()           NOT REACHED
POST process read                     NOT REACHED
post harness-absent proof             NOT ESTABLISHED
seal                                  NOT REACHED
```
*Materially different from "terminate ran but failed": the terminate call never happened.* Authority spent under the settled rule: the accepted pin was invoked and failed after entering the governed act; no retry inherits it. **Device effect:** no install, uninstall, sample, or intentional harness launch/terminate; *Xcode did attempt to establish UI automation on the connected device*, so the act is not "device reads only" — it reached the UI-test infrastructure transaction, not the governed test body. **The historical harness's state is unknown after the just-before read**: alive immediately before the failed attempt; no POST read exists. `FIRST-INSTALL-03` remains closed — dependency step 2 has not succeeded and step 3 does not exist. A future termination needs a new act identity and fresh authority with the existing preconditions unchanged; *before opening that: make this STOP durable and separately investigate why automation mode timed out, or risk spending another authority on the same infrastructure failure.*

**The failure mode has precedent in this lane, and a recorded cause.** `DRIVER-01_PLAN_2026-09-12.md` (calibration sample 1: `Failed to initialize for UI testing … Timed out while enabling automation mode`, test body never ran) · `DRIVER-01_STAGE-B_2026-09-12.md`: samples 1–4 `runner could not enable automation mode` — **"the phone was not automatable at batch start — asleep or locked — and woke at sample 5"**, lesson carried: ***"the device must be awake and unlocked before the batch starts; an infrastructure row at sample 1 costs a sample, not the batch"*** · `KERNEL-00_VPIO-02_K00-05-06_WITNESS_2026-09-14.md`: samples 1–2 the same signature "at batch start; harness never launched", then `testTerminateOnly` **passed ×7** in the same batch once the device was automatable. In every prior instance the runner attached on a later invocation of the same instrument in the same session. **A single-invocation act has no later sample**: what a batch absorbs as one infrastructure row, a one-shot act pays with its whole authority. This is the lane's recorded reading, not a new inference; whether the phone was asleep or locked at 20:29:32 local is a fact only the founder can supply, and it is not inferred here. The xcresult bundle the runner named (`…/DerivedData/VoiceKernelDriver-comvzxsbabpfcefczwfmswixclmb/Logs/Test/Test-DriverUITests-2026.09.15_20-29-32--0400.xcresult`) is on the Mac and is named, not read.

**Terminate-01 STOP carrier DRAFTED — `SID_HISTORICAL-HARNESS-TERMINATE-01_STOP-CARRIER_DRAFT_2026-09-16.sh` · 45 lines · `d27d8836734f4270ba4ef19ba040f36bf020ed2358f86608a30e1e1d2105386e`** (`bash -n` only; execution closed). One placeholder `__TERMINATE_01_STAMP__` refusing on its prefix. Preconditions prove a STOP of exactly the ruled shape: all ten produced files present (`head.txt` · `processes-before.json` · `harness-processes-before.txt` · `apps-before-vpio02sid.json` · `xcodegen.log` · `build-for-testing.log` · `xctestrun.txt` · `processes-just-before-terminate.json` · `harness-processes-just-before-terminate.txt` · `terminate-only.log`) · **`processes-after.json`, `apps-after-vpio02.json`, `SHA256SUMS.terminate` absent** (a `$OUT` that went further belongs to a success carrier and is refused) · `head.txt` = instrument · harness present in the just-before read · `terminate-only.log` contains `failed to initialize for UI testing` and **zero** `testTerminateOnly]' passed` lines · executed pin hashes `576fd2b2…` · authority file present for metadata only. Carries the whole `$OUT`, the pin, every `sid-hht-01-transcript-*.log`, `authority-metadata.txt` (lines/bytes/sha256, never contents), a listing, the **path and directory listing of the newest xcresult bundle** (path only — the bundle itself is not copied), `SHA256SUMS.run` via `find` written outside then moved, and `RETURN.txt` with the founder's classification line by line, `POST-PROCESS-READ not reached (none fabricated)`, `SEAL not reached (none fabricated)`, `HARNESS-STATE-AFTER unknown`. Branch `feature/sid-hht-01-stop-evidence-<stamp>`, `DEST = driver-ledger/VPIO-02-SID-HHT-01-STOP-<stamp>/`.

**Standing after §10.33.** TERMINATE-01 STOP · spent · carrier DRAFTED (execution closed) · historical harness state UNKNOWN since 00:29:32Z · FIRST-INSTALL-03 closed (steps 2–3 unmet) · [D]/[E] CLOSED · install 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD.

### 10.34 FOUNDER: the road from here (2026-09-16, verbatim in substance) · current position = between steps 1 and 2 · founder reports the phone "open and on" (present tense; not a device read)

```text
1. preserve      the failed terminate attempt — create and land a STOP carrier for HISTORICAL-HARNESS-TERMINATE-01
2. diagnose      why UI automation timed out — read-only investigation first; do not retry blindly
3. terminate     the historical .vpio02 harness — new act, fresh authority, one testTerminateOnly, prove afterward that it is gone
4. install       FIRST-INSTALL-03 — only after the historical harness is confirmed absent; install .vpio02sid; capture its container; carrier lands the evidence
5. ENTRY         [D] preflight · [E] N = 30 entry batch — confirm the new source observer did not perturb entry
6. source        only then: SID source-population work — whose audio survives during duplex rendering?
```
*preserve → diagnose automation → terminate old harness → install SID → ENTRY witness → source discrimination.*

**Step 1 status.** Carrier drafted (§10.33, `d27d8836…`); needs the act's stamp (one read-only `ls -d /private/tmp/sid-hht-01-out-*`) filled as a separate file for review, then a run shape.

**Step 2 — what the record already knows, offered as the read-only starting point (nothing executed here).** (a) The lane's own prior finding for this exact signature: *device asleep or locked at start* (Stage-B samples 1–4; K00-05/06 samples 1–2), resolved by the device becoming automatable on later invocations. The founder now reports the phone **open and on** — a present-tense fact recorded as reported; its state at 20:29:32 local (the moment of the failed attempt) is not established by that report. (b) Read-only sources on the Mac that can say more, each its own pinned read if the founder opens it: the xcresult bundle the runner named (`…/Logs/Test/Test-DriverUITests-2026.09.15_20-29-32--0400.xcresult` — `xcrun xcresulttool get --format json --path …` is read-only) · the act's own `terminate-only.log` in full (the transcript shows only the `grep`-filtered tail) · `xcrun devicectl device info --help` to learn whether a lock/screen-state read exists in this Xcode (unknown to this record; not assumed) · a fresh `info processes` / `info apps` read to learn the harness's **current** state (alive or gone since 00:29:32Z) — a device read, therefore its own founder-opened act, not run on this session's initiative. (c) A structural point for the step-3 pin, offered not applied: a one-shot act cannot absorb an automation-mode timeout the way a batch absorbs one infrastructure row; the step-3 pin's just-in-time block is the right place for whatever "automatable now" proof step 2 yields.

**Standing after §10.34.** Position: between steps 1 and 2. TERMINATE-01 STOP · spent · carrier drafted, stamp owed · historical harness state UNKNOWN since 00:29:32Z · FIRST-INSTALL-03 closed · [D]/[E] CLOSED · install 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD.

### 10.35 Terminate-01 STOP carrier — placeholder FILLED from the founder's read-only listing (2026-09-16): `/private/tmp/sid-hht-01-out-20260916T002922Z` → `SID_HISTORICAL-HARNESS-TERMINATE-01_STOP-CARRIER_2026-09-16.sh` · 45 lines · `7606892a148239438848198db0c34e879705bc50dd4d932f8d1de67173510f9e` · returned for review · NOT RUN

Stamp `20260916T002922Z` (00:29:22Z = 20:29:22 local; the act's first devicectl line is 20:29:25, consistent). One-line diff vs the draft (`d27d8836…`): the `STAMP=` assignment; the `case … __*)` refusal guard retained. (A `zsh: parse error` preceded the founder's `ls` in the same window — another pasted diff/text block that zsh refused as a whole; nothing executed; the window is the other-lane `ws-stage2-…` one and is not to be used for a governed run.) Execution CLOSED pending review; run shape issued on acceptance (read-only carrier, no authority).

### 10.36 FOUNDER RULINGS (2026-09-16): step-1 STOP carrier ACCEPTED (`7606892a…`) → run shape issued · step-2 diagnostics OPEN, all four, read-only → pinned as one act and run shape issued · phone state at 20:29:32 = UNKNOWN, sleep/lock = HYPOTHESIS ONLY · `TERMINATE-02` NOT drafted until step 2 lands

**Rulings (verbatim in substance).** (1) The filled 45-line carrier differs from the reviewed draft by exactly the `STAMP=` line; refusal guard remains; STOP-shape requirements appropriate (ten files · POST read absent · seal absent · `passed` count 0 · xcresult by path only) → **ACCEPTED**, read-only run shape may be issued. (2) Step-2 diagnostics **OPEN, all four, in the proposed order**: full `terminate-only.log` · `xcresulttool` on the named bundle · `xcrun devicectl device info --help` (capability discovery only) · a fresh current-state read (`info processes` · `.vpio02` · `.vpio02sid`) — item 4 limited to observation: *no launch, terminate, install, uninstall, sample or UI-automation attempt*; it tells whether the historical harness remained alive, ended on its own, or changed state after the failed automation attempt. (3) Phone awake/unlocked at 20:29:32: **UNKNOWN from admissible evidence**; *do not promote the lane's earlier sleep/lock explanation into the cause unless personally remembered* — recorded as `phone awake UNKNOWN · phone unlocked UNKNOWN · sleep/lock as cause HYPOTHESIS ONLY`; a clear memory of awake-and-unlocked would materially change step 3 toward the xcresult evidence. (4) `TERMINATE-02` **not drafted** until step 2 establishes the actual mechanism / available device-state proof — *a pin drafted with a reserved unknown proof is a pin we already expect to amend*; sequence: STOP carrier → diagnostic reads → mechanism established → draft once → review → fresh authority → execute once.

**Step-2 act pinned — `docs/programme/VOICE-2026/SID_STEP2-DIAGNOSTICS-01_READ_2026-09-16.sh` · 52 lines · `53b0efa290475cbad97440c6c82124204a6f9cc8cc66b8b5fa4c810edad9c19b`** (`bash -n` only). Deliberately **not** `set -e`: every read is guarded with `|| true` so one failing tool cannot suppress the others; the only hard exits are the missing terminate-01 `$OUT`, the fetch, the worktree/branch, the commit and the push. No authority gate — observation carries none. Verb census: `install app` 0 · `uninstall` 0 · `launch` 0 · `xcodebuild` 0 · `test-without-building` 0 · `testTerminateOnly` 0. Reads, each into `driver-ledger/VPIO-02-SID-STEP2-DIAG-<stamp>/` in a fresh worktree at the lane tip: **read 1** a copy of the full `terminate-only.log` from the terminate-01 `$OUT`, its line count and a grep of error/timeout/automation/`Test Case` lines · **read 2** the xcresult bundle at the exact path the runner named (`Test-DriverUITests-2026.09.15_20-29-32--0400.xcresult`): `xcresulttool version`, `get test-results summary`, `get log --type action`, and the legacy `get object --format json` — each guarded, stderr captured, because the sub-command surface differs across Xcode versions and this record does not assume which one Xcode 26.3 accepts · **read 3** `devicectl --version`, `device info --help`, `device --help`, and a grep of those help texts for `lock|screen|wake|sleep|display` — help text only; nothing found there is invoked · **read 4** `info processes` → harness line count, historical-container line count and the PID(s) on those entries; `info apps` for `.vpio02` and `.vpio02sid` → installed-entry counts. Then `SHA256SUMS.run` (written outside, moved in), commit, push to `feature/sid-step2-diagnostics-<stamp>`, and an echo carrying the harness-now line. Read 4 is the one device-touching part and is exactly the three observations the founder listed.

**Run shapes issued (in this order; two blocks; either window whose cwd exists after `cd ~`).** STOP carrier: extraction `git show fde700a50:…SID_HISTORICAL-HARNESS-TERMINATE-01_STOP-CARRIER_2026-09-16.sh`, custody `7606892a…` / 45, one tee'd invocation, expected `SID-HHT-01 STOP CARRIER PUSHED`. Diagnostics: extraction from the commit that lands this section, custody `53b0efa2…` / 52, one tee'd invocation, expected final line `SID-STEP2-DIAGNOSTICS-01 <stamp> PUSHED harness-now harness lines now: N · historical-container lines: M`. On both branches' arrival here: cherry-pick `-x`, reseal, read — the diagnostics' reading is §10.37 and is what `TERMINATE-02` is derived from.

**Standing after §10.36.** TERMINATE-01 STOP · spent · carrier ACCEPTED, run next · step-2 act pinned, run after · phone state at 20:29:32 UNKNOWN · `TERMINATE-02` not drafted · FIRST-INSTALL-03 accepted pin, authority unissued, closed · [D]/[E] CLOSED · install 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD.

### 10.37 Step 1 + step 2 LANDED and READ (2026-09-16): terminate-01 STOP carrier in custody · step-2 diagnostics in custody · the mechanism as far as the evidence establishes it · the historical harness still alive at 00:39:27Z (PID 3347, unperturbed) · `lockState` / `displays` discovered as read-only device-state reads · what `TERMINATE-02` must prove, derived — one proposal, returned for review, nothing drafted as a pin

**Custody.** Both feature branches fetched and cherry-picked `-x` onto the lane branch; seals reproduced here, not copied. **Step 1** — `feature/sid-historical-harness-terminate-01-stop-20260916T002922Z` @ `e3cfa7d7ef03fd8a34b96f62e6a6333fefbd4087` (17 files) → `d7bd59484` · `driver-ledger/VPIO-02-SID-HHT-01-STOP-20260916T002922Z/` · `SHA256SUMS.stop` **`8349553577cf15cc…`**, 15 entries, **0 mismatches** · no `processes-after` / `apps-after-*` / `SHA256SUMS.terminate` (the STOP shape: the act died at line 44) · `testTerminateOnly]' passed` count **0** · `authority.meta.txt` 8 lines / 275 bytes / `17000e7c…` (metadata only; the payload never entered git). **Step 2** — `feature/sid-step2-diagnostics-20260916T003924Z` @ `552d6518efd8149d4b892c32b89c7e06eec8dd0e` (25 files) → `e1d5fe39a` · `driver-ledger/VPIO-02-SID-STEP2-DIAG-20260916T003924Z/` · `SHA256SUMS.run` **`372e9bc51e272553…`**, 24 entries, **0 mismatches** · all three `read2-*.stderr` files 0 bytes (every `xcresulttool` sub-command the pin guessed was accepted by this Xcode). Gate run before the record commit: `__tests__/voice-kernel-00-source-gates.test.ts` alone (count in the commit line).

**Read 1 — the full `terminate-only.log` (30 lines).** The transcript's filtered tail was complete in substance; the full log adds the timeline (local −0400): `xcodebuild test-without-building` invoked **20:29:32.713** · `DriverUITests-Runner[3537] Running tests...` **20:29:41.443** · `Failed to initialize for UI testing: Error Domain=com.apple.dt.XCTest.XCTFuture Code=1000 "Timed out while enabling automation mode."` **20:30:41.651** · `IDETestOperationsObserverDebug: 67.872 elapsed` · `** TEST EXECUTE FAILED **`. **`Running tests...` → failure = 60.21 s.** Zero `Test Case` lines. The test body never started; the 60-second figure is a fixed bound inside XCTest's UI-testing initialization, not a slow test.

**Read 2 — the xcresult bundle (`xcresulttool` 24514, schema 0.1.0 / legacy 3.56).** `summary`: `result: Failed` · `totalTestCount 1 · failedTests 1 · passedTests 0` — the one counted "test" is the runner error itself (`testIdentifierString: "DriverUITests-Runner (3537) encountered an error"`), i.e. **no test of ours was enumerated**; device `iPhone 16 Pro Max · iOS 26.7 (23H24) · arm64e · id 00008140-00163D9922E0801C`; host `Mac Studio · macOS 15.7.8`; window 00:29:32.940Z → 00:30:41.853Z (68.91 s). `action` log, three facts the transcript could not show: **(a)** *Installing DriverUITests on Kelly Nezat's iPhone → Successfully installed* in 6.17 s (00:29:34.18Z → 00:29:40.36Z), runner app `life.soullab.voicekernel.driver.xctest.xctrunner` placed at device container **`C3C07082-B253-41E4-9A59-B2EE3887ED8A`** — a device write intrinsic to any `test-without-building` (the batch does the same every sample), inside the accepted act's scope; **not** a subject install (`.vpio02sid` remains absent, read 4). **(b)** Launch section: suspended launch → *"Requested the device's screen to stay on for the duration of the launch session"* → *Resumed target process* → *Successfully launched DriverUITests* (0.61 s). The stay-on line is a request the tooling makes, not a report of the screen's state — it establishes nothing about lock or wake at 20:29:32. **(c)** The launch environment carried `K00_MODE=L · K00_VP=on · K00_HOLD_S=15 · K00_W4_MS=500 · K00_SUBJECT=vpio-02` — the `TEST_RUNNER_` prefix was stripped as intended; the env transport into the runner is proven for this pin shape. The legacy `object` dump adds one `IssueSummary` of type `Uncategorized` carrying the same sentence and nothing else. **The bundle contains no lock, screen, wake or automation-agent state of any kind.**

**Mechanism, as far as admissible evidence establishes it.** Everything Mac-side worked end to end: worktree at the instrument SHA, `xcodegen`, `build-for-testing`, runner install onto the device, runner launch and resume, env transport. The failure is entirely **device-side, inside XCTest's UI-testing initialization**: the runner waited exactly its 60 s bound for the device to *enable automation mode* and the device did not. What that enablement waits on is Apple-internal and not in evidence. Therefore: **the mechanism is located (device-side automation-mode enablement, 60 s fixed bound); its cause is UNKNOWN.** The lane's sleep/lock precedent is *consistent* with this shape and is *not proven* by it — `phone awake UNKNOWN · phone unlocked UNKNOWN · sleep/lock as cause HYPOTHESIS ONLY` stands unchanged (§10.36 ruling 3). Two derived consequences: a longer wait is not a lever (the bound is XCTest's, not ours), and a blind re-invocation would be exactly the retry ruling 4 forbids.

**Read 3 — `devicectl` 506.7 capability discovery (help text only).** `device info` exposes two read-only sub-commands this record did not previously know: **`lockState` — "Get the current locked state of the device."** and **`displays` — "Get the device's current display information."** Both accept `--json-output <path>` (the only scripting interface devicectl supports). This is the "available device-state proof" §10.36 asked step 2 to find. ⚠️ **What is NOT in evidence:** their JSON shape, key names and value vocabulary — the per-sub-command help (`devicectl help device info lockState`) was not among the four opened reads, and neither has been invoked. A `TERMINATE-02` gate written against a guessed key would be a pin we already expect to amend.

**Read 4 — current device state at 00:39:27Z (observation only).** `harness lines now: 1 · historical-container lines: 1 · "processIdentifier" : 3347` at `E3B88028-…/VoiceKernelHarness.app` — **the historical harness is alive, single, same PID as before the failed attempt**: observed at 00:29:25Z (before), 00:29:3xZ (just-before) and 00:39:27Z (after); the failed automation attempt neither terminated, relaunched nor perturbed it. No `DriverUITests-Runner` process remains (the failed runner exited). `.vpio02` installed 1 · `.vpio02sid` installed 0. FIRST-INSTALL-03's line-41-class precondition is therefore still false; step 3 remains the blocker.

**What `TERMINATE-02` must prove — derived, not drafted.** (1) Nothing new Mac-side: the accepted 55-line pin (`576fd2b2…`) is the right body, labels 01→02, the `test ! -e "$WT"` / `test ! -e "$OUT"` preconditions unrelaxed (the 01 worktree `/private/tmp/sid-hht-01-<sha>` exists on the Mac and must, per lane law, be left alone — 02 uses its own path). (2) The one thing the evidence says the act lacked: **a device-automatable proof taken immediately before line 43**, i.e. between the just-before process read and the runner invocation — and the only such read discovered is `lockState` (+ `displays`). (3) Because their output shape is unread, **the honest next act is a read, not the terminate**: a **step-2b read-only pin** (~15 lines, no authority, same guarded shape as step 2) that captures `devicectl help device info lockState` and `… displays`, then one `lockState --json-output` and one `displays --json-output` read *now* — while the founder reports the phone open and on — plus the same harness/apps lines. It yields the JSON keys **and** calibrates the read against a state the founder can vouch for at that moment (an unlocked, display-on phone must read as such, or the gate is worthless). `TERMINATE-02` is then written once: the 01 body + a just-in-time `lockState`/`displays` gate that refuses *before* the runner is invoked, with both reads copied into `$OUT` as evidence. **Alternative if the founder declines 2b:** `TERMINATE-02` with `lockState`/`displays` as evidence-only reads (recorded, not gated) — a second timeout would then at least be attributable; weaker, and it spends an authority to learn what 2b learns for free. **Recommendation: 2b first.** Nothing here is a pin; no draft is written until ruled.

**Standing after §10.37.** Steps 1 and 2 CLOSED (landed, sealed, read) · mechanism LOCATED (device-side automation-mode enablement, 60 s) · cause UNKNOWN · sleep/lock HYPOTHESIS ONLY · historical harness ALIVE at 00:39:27Z (PID 3347) · `lockState`/`displays` DISCOVERED, shape UNREAD · step-2b PROPOSED, not open · `TERMINATE-02` NOT drafted · FIRST-INSTALL-03 accepted pin, authority unissued, closed · [D]/[E] CLOSED · install 0 (runner app install is tooling, not subject) · launch 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD.

### 10.38 FOUNDER RULING (2026-09-16): `STEP-2B DEVICE-STATE CALIBRATION` OPEN AS DRAFT-ONLY (authority none · device interaction read-only · `TERMINATE-02` remains undrafted) → draft returned: `SID_STEP2B-DEVICE-STATE-CALIBRATION-01_READ_DRAFT_2026-09-16.sh` · 46 lines · `b00876bdc889275d39be5ef7b8fc1307964bed86d82c4b203c106d48777e8d52` · `bash -n` only · NOT RUN

**Ruling (verbatim in substance).** 2b approved: *spending another terminate authority merely to discover the semantics of `lockState` and `displays` would be backwards; learn the device-state vocabulary first, then write the next terminating act once.* 2b may capture, in one bounded read-only act: (1) devicectl help for `lockState` · (2) help for `displays` · (3) one raw `lockState` JSON read · (4) one raw `displays` JSON read · (5) one current process-table read · (6) `.vpio02` app read · (7) `.vpio02sid` app read. No launch, terminate, install, uninstall, XCTest, sample, source read or state-changing command. **Refinement: 2b does not itself interpret or gate on guessed JSON keys — it is calibration; preserve the raw JSON and help output first.** Alongside the act, the founder's **contemporaneous human attestation in concrete terms** (phone physically awake · display visibly illuminated · device unlocked · normal UI visible — "open and on" is too ambiguous for a calibration record), then compare that known physical state with the actual vocabulary. **After 2b only**, `TERMINATE-02` = TERMINATE-01 accepted body + labels/paths 01→02 + PRE historical-harness binding + just-in-time `lockState` gate derived from observed 2b semantics + just-in-time `displays` gate derived from observed 2b semantics + just-in-time historical-harness binding + exactly one `testTerminateOnly` + POST harness absent; *no invented "awake/unlocked" values before 2b tells us what the tool actually reports.* Standing table: TERMINATE-01 STOP · spent · durable · step-1 carrier durable · step-2 diagnostics durable · historical harness still alive · STEP-2B draft-only open · TERMINATE-02 wait for 2b evidence · FIRST-INSTALL-03 closed · [D]/[E] closed · install 0 · sample 0 · source population 0 · SID container WITNESS-REQUIRED. *The next return = the 2b read-only pin draft only, no execution.*

**The draft.** Same skeleton as the accepted step-2 pin (`53b0efa2…`): `set -o pipefail`, no `set -e`, every device read guarded `|| true`; hard exits only for a missing attestation file, the fetch, the worktree/branch, the commit and the push; no authority gate (observation carries none). Reads, each into `driver-ledger/VPIO-02-SID-STEP2B-DEVICE-STATE-<stamp>/` in a fresh worktree at the lane tip: **attestation** — the founder writes the concrete physical-state statement into `/private/tmp/sid-2b-attestation.txt` *before* invocation (a heredoc in the window, the same preparation shape as an authority payload — but this file is **evidence, not authority**, and is copied verbatim into `attestation.txt` under a UTC + local `attested-at` header; the pin refuses if it is missing or empty; it invents no wording) · **read 0** `devicectl --version` · **read 1** `devicectl help device info lockState` · **read 2** `… displays` (text only; nothing invoked from it) · **read 3** `device info lockState --json-output` · **read 4** `device info displays --json-output` — each preceded by a `taken-at-utc` stamp so the JSON is provably contemporaneous with the attestation, followed by byte counts only; **no key is parsed, no value is compared, nothing gates** · **read 5** process table → the established harness / historical-container / PID lines (the same vocabulary step 2 used) · **read 6/7** `.vpio02` / `.vpio02sid` installed-entry counts. Then `SHA256SUMS.run` written outside and moved in (self-entry excluded by `find … ! -name`), commit, push to `feature/sid-step2b-device-state-<stamp>`, final line `SID-STEP2B-DEVICE-STATE-CALIBRATION-01 <stamp> PUSHED harness-now …`. **Verb census:** `install app` 0 · `uninstall` 0 · `process launch` 0 · `process terminate` 0 · `xcodebuild` 0 · `test-without-building` 0 · `testTerminateOnly` 0 · `k00-driver-batch` 0 · `k00-reinstall` 0 · `sample` 0; every `devicectl` invocation is `device info …` (5) or `help device …` (2). The only device-touching part is the seven ruled reads, in the ruled order.

**Two disclosed choices for review.** (a) The attestation travels as a file the founder writes in the window rather than as pin text, so the pin never authors the physical-state claim; unlike an authority payload it *is* committed, because it is the calibration's reference reading. (b) The two help reads are separate from the two JSON reads (the ruling's items 1–4 as four files) rather than folded, so a help text that documents a vocabulary the JSON does not carry is visible as such.

**Standing after §10.38.** STEP-2B draft returned, NOT RUN (review → run shape) · `TERMINATE-02` NOT drafted · historical harness ALIVE (last read 00:39:27Z, PID 3347) · FIRST-INSTALL-03 closed · [D]/[E] CLOSED · install 0 · launch 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD.

### 10.39 FOUNDER RULING (2026-09-16): `STEP-2B DEVICE-STATE CALIBRATION-01` ACCEPTED at `03289d89d` (`b00876bd…` / 46 verified by the founder on the actual pin) · read-only execution OPEN (authority none · device writes none) · conduct requirement for the attestation · run shape ISSUED

**Ruling (verbatim in substance).** Both disclosed choices accepted: the attestation travels with the evidence — *not authority; the human reference state against which `lockState` and `displays` are being calibrated* — copied verbatim and committed; help outputs stay separate from raw JSON because *documentation can describe a vocabulary or behavior that an individual response does not exhibit* — distinct evidence classes. Confirmed: `lockState` JSON raw only · `displays` JSON raw only · no parsed key · no state gate · no inferred awake/unlocked value; process and app summaries observational only. **Conduct requirement:** the attestation must be the founder's observation *at the time of the calibration* — not language supplied by the session, not a remembered earlier state. Immediately before invoking, physically establish whatever is true and write only that; if all four hold, the factual form `phone physically awake · display visibly illuminated · device unlocked · normal UI visible`; if one does not hold, write the actual state instead; *do not make the phone fit the wording after writing it*; keep the observed state unchanged through the two reads if practical — *the purpose is calibration, not passing a gate.* After it lands, the next decision is mechanical: read the actual vocabulary beside the attestation, then derive `TERMINATE-02`'s just-in-time device-state gate from observed semantics, never guessed key names. Standing: TERMINATE-01 STOP · spent · durable · STEP-2B accepted, run next · TERMINATE-02 wait for 2b result · FIRST-INSTALL-03 closed · [D]/[E] closed · no `TERMINATE-02` draft yet.

**Run shape (one fresh Terminal window; no authority; the attestation text is the founder's own and is written only after looking at the phone).**

```text
1. fresh Terminal window
2. cd ~
3. look at the phone, then write ONLY what is true right now:
   cat > /private/tmp/sid-2b-attestation.txt <<'ATT'
   <one line per observed fact — e.g. "phone physically awake" / "display visibly illuminated" / "device unlocked" / "normal UI visible"; if any is not true, write the actual state>
   ATT
4. test -s /private/tmp/sid-2b-attestation.txt && wc -l /private/tmp/sid-2b-attestation.txt && cat /private/tmp/sid-2b-attestation.txt
5. git -C ~/MAIA-SOVEREIGN fetch origin claude/voice-2026-census-01 \
   && git -C ~/MAIA-SOVEREIGN show 03289d89d:docs/programme/VOICE-2026/SID_STEP2B-DEVICE-STATE-CALIBRATION-01_READ_DRAFT_2026-09-16.sh > /private/tmp/sid-step2b-01.sh
6. shasum -a 256 /private/tmp/sid-step2b-01.sh && wc -l /private/tmp/sid-step2b-01.sh
   → must print b00876bdc889275d39be5ef7b8fc1307964bed86d82c4b203c106d48777e8d52 and 46; otherwise STOP, invoke nothing
7. bash /private/tmp/sid-step2b-01.sh 2>&1 | tee ~/sid-step2b-01-transcript-$(date -u +%Y%m%dT%H%M%SZ).log; echo "rc=${PIPESTATUS[0]}"
   expected final line: SID-STEP2B-DEVICE-STATE-CALIBRATION-01 <stamp> PUSHED harness-now harness lines now: N · historical-container lines: M · vpio02 installed entries: 1 · vpio02sid installed entries: 0
```

Do not touch the phone between step 3 and the end of step 7 if practical. On the branch's arrival here: cherry-pick `-x`, reseal, then read `attestation.txt` beside `read3-lockState.json` / `read4-displays.json` and the two help texts — that reading is §10.40 and is what `TERMINATE-02`'s device-state gate is derived from.

**Standing after §10.39.** STEP-2B accepted · run next · `TERMINATE-02` NOT drafted · historical harness ALIVE at last read (00:39:27Z, PID 3347) · FIRST-INSTALL-03 closed · [D]/[E] CLOSED · install 0 · launch 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD.

### 10.40 `STEP-2B DEVICE-STATE CALIBRATION-01` EXECUTED → LANDED · READ (2026-09-16): the device-state vocabulary is now observed, in the automatable state only · ⚠️ THREE `VoiceKernelHarness` processes alive at 00:58Z — the frozen `.k00` and `.vpio01` harnesses relaunched between 00:39Z and 00:58Z inside a 23-process cluster, cause UNKNOWN · every accepted device pin (TERMINATE-01 body · FIRST-INSTALL-03 · [D]) would now REFUSE on its harness law · `TERMINATE-02` NOT drafted — the harness question is the founder's before any gate is written

**Custody.** `feature/sid-step2b-device-state-20260916T005807Z` @ `2f8c16ddadf9bb9f389eccac27ce6c2361ee4973` (22 files) → cherry-picked `-x` here · `driver-ledger/VPIO-02-SID-STEP2B-DEVICE-STATE-20260916T005807Z/` · `SHA256SUMS.run` **`83928c80b7b41cdd…`**, 21 entries, **0 mismatches**. Extraction matched custody (`b00876bd…`/46). Transcript residue: the window was the step-2 window after `cd ~`, not a fresh one (no governed consequence — 2b carries no authority); the run shape's own `#` comment lines were echoed by zsh as `command not found: #` — **a run-shape defect of this record, not of the pin**: interactive zsh does not accept `#` comments; nothing executed from them; future run shapes carry no comment lines. `rc=` empty = C-D26 HOLD.

**Calibration reading — attestation vs vocabulary, same second.** `attestation.txt`: `attested-at-utc 2026-09-16T00:58:10Z` · `phone physically awake · display visibly illuminated · device unlocked · normal UI visible` (four lines, the founder's own). `lockState` taken **00:58:10Z**, `displays` taken **00:58:10Z**. Both `outcome: success`, `jsonVersion 3`, devicectl `506.7`. Per-sub-command help documents **no vocabulary** for either (lockState: *"does nothing for macOS devices"*); the JSON is the only source.

| attested fact | `lockState` `result` | `displays` `result` |
|---|---|---|
| device unlocked | `"passcodeRequired" : false` · `"unlockedSinceBoot" : true` | — |
| display visibly illuminated · phone physically awake | — | `"backlightState" : "activeOn"` (stdout: *backlight is on and active*) |
| normal UI visible | no correlate | no correlate (orientation `faceUp` / `portrait` / not locked; one `LCD` primary display) |

**What the calibration does and does not establish.** It establishes the exact reading an awake, illuminated, unlocked phone produces on this toolchain: the tuple **`passcodeRequired:false ∧ unlockedSinceBoot:true ∧ backlightState:"activeOn"`**, and the byte-exact JSON form devicectl emits (`"key" : value`, spaces both sides of the colon). It does **not** establish what a locked or dark phone reads as — no such reading was taken, by the founder's own conduct rule (the phone was not made to fit anything). Two consequences for any gate: (a) `unlockedSinceBoot` is a since-boot latch, not a current state — it cannot distinguish locked from unlocked after the first unlock; (b) `passcodeRequired` is the only candidate current-state field and its locked-state value is **unobserved** — its name suggests `true` when locked, and that suggestion is exactly the kind of guessed value the ruling forbids. Therefore the only gate derivable from observed semantics is an **equality gate against the calibrated tuple**: refuse on any departure from the known-automatable reading, without asserting what the departure means. Its power to detect a locked phone is unverified; its power to refuse on anything other than the calibrated state is exact.

**⚠️ The process table (read 5, 00:58:10Z) — the substantive finding.** `harness lines now: 3 · historical-container lines: 1`:

| PID | container | bundle (from the record) | status |
|---|---|---|---|
| 3347 | `E3B88028-…` | `life.soullab.voicekernel.vpio02` (historical subject) | unchanged since 00:12Z |
| **3617** | `0B07D423-97E7-4196-BC1C-C69C96F994BE` | `life.soullab.voicekernel.k00` (engine subject, FROZEN, container UNTOUCHED; `reinstall-20260914T011754Z.txt`) | **NEW since 00:39:27Z** |
| **3618** | `6A2E406B-D1B8-43A4-92F3-29D50333AF19` | `life.soullab.voicekernel.vpio01` (FROZEN evidence; VPIO-01 FIRST-INSTALL-01) | **NEW since 00:39:27Z** |

Diff of the two process tables (step 2 at 00:39:27Z → 2b at 00:58:10Z): 403 → 425 processes, **23 new PIDs, 1 gone**. The new PIDs are a contiguous launch cluster `3580–3622`: `GPUExtension`, `SleepWidgetExtension`, `fitnesscoachingd`, **`SleepLockScreen.app` (3585)**, `splice`, `AppStore`, `FaceTime`, `HeadphoneProxService`, `Claude`, `MobileCal`, `parsec-fbf`, `softwareupdated` (+ its cleanup service), `momentsd`, `MomentsUIService`, `Microsoft Authenticator`, `TenWords`, **`VoiceKernelHarness` ×2 (3617, 3618)**, `App.app`, `TestFlight`, `applekeystored`, then `dtdeviceinfod` (3622 — the reads' own developer-disk-image service, i.e. the cluster completed *before* the 2b reads began). **Nothing in the lane launched any of it**: 2b's only device verbs are `device info` reads; no launch verb exists in the pin (verb census §10.38). Cause **UNKNOWN**. Shape: the same *"cluster of relaunched user apps"* that surrounded the `.vpio02` harness at FIRST-INSTALL-02 (00:12Z) — **the second such cluster tonight**. `SleepLockScreen.app` in the cluster is recorded as an observation only (20:58 local); no inference about Sleep Focus, lock, or the earlier automation-mode timeout is drawn from it.

**Precedent.** These two exact containers were terminated once already: `S2-FOREIGN-HARNESS-DISPOSAL-03` (§18.29, 2026-09-15 18:11Z, Mac Studio terminal directly) — `testTerminateOnly` invoked once per subject key `phase-a` (→ `.k00`) and `vpio-01`, `DEVICE_ACTS termination-only`, `ESCALATION none`, PASS, harness 0 after. That authority was specific and is spent. **They are back.** The disposal established that terminating a frozen subject's *process* is not touching its *container*; it did not establish why the processes return.

**Mechanical consequences for the accepted pins (read from their text, not proposed).** (1) The TERMINATE-01 body — the ruled basis of `TERMINATE-02` — asserts at lines 28 and 41 that *every* harness line is the historical container's (`grep -ic VoiceKernelHarness` = `grep -c E3B88028…/VoiceKernelHarness.app`): **3 ≠ 1 → REFUSE before the runner**; and its POST at line 47 requires `VoiceKernelHarness` count **0** → would FAIL even after a clean `.vpio02` termination. (2) `testTerminateOnly` terminates **only the subject bundle** (`K00DriverTests.swift:154–160`; `harness` = `XCUIApplication(bundleIdentifier: subject.bundleID)`), so a `vpio-02` invocation cannot clear the other two. (3) FIRST-INSTALL-03 line 41 and ENTRY [D] line 39 each require `grep -ci VoiceKernelHarness` = **0** → both accepted pins REFUSE while any foreign harness lives. (4) The ENTRY design's validity premise — a cold device with no other VoiceKernel organism holding audio while the SID subject enters — is the *reason* those laws read 0; it is not a pin nit.

**The founder's question, stated not decided.** Two shapes exist: **(i)** dispose of the foreign harnesses first, as §18.29 did — one bounded termination-only act on subject keys `phase-a` and `vpio-01` (two `testTerminateOnly` invocations, each a UI-automation attempt, each now guarded by the calibrated tuple), then `TERMINATE-02` on `.vpio02`; every accepted pin's harness-0 law stays intact, and the ENTRY validity premise is preserved. **(ii)** re-scope `TERMINATE-02`, FIRST-INSTALL-03 and [D] to the subject's own container (bind on `E3B88028…` lines only; record foreign harness lines, never assert them) — fewer device acts, but it re-opens three accepted pins for re-acceptance and quietly weakens the ENTRY premise. Whatever is chosen, the relaunch cause is unknown and a disposal may not hold (it did not, once) — the just-in-time harness reads immediately before FIRST-INSTALL-03 and [D] remain the guard, and this record does not propose relaxing them. **Recommendation, if one is wanted: (i).**

**Derived gate text (from observed semantics; for the future `TERMINATE-02`, NOT a pin, NOT drafted).** Placed between the just-in-time harness read and the runner invocation; exact-string equality to the calibrated reading; any departure refuses before the runner is invoked; both reads copied into `$OUT` as evidence:

```text
xcrun devicectl device info lockState --device "$DEV" --json-output "$OUT/lockState-just-before.json"
test "$(grep -c '"passcodeRequired" : false' "$OUT/lockState-just-before.json")" = 1
test "$(grep -c '"unlockedSinceBoot" : true' "$OUT/lockState-just-before.json")" = 1
xcrun devicectl device info displays --device "$DEV" --json-output "$OUT/displays-just-before.json"
test "$(grep -c '"backlightState" : "activeOn"' "$OUT/displays-just-before.json")" = 1
```

**Standing after §10.40.** STEP-2B EXECUTED · landed · read · vocabulary OBSERVED (automatable state only) · **historical harness ALIVE (3347) + foreign `.k00` (3617) and `.vpio01` (3618) harnesses ALIVE, cause unknown** · `TERMINATE-02` NOT drafted (harness ruling first) · FIRST-INSTALL-03 closed and would refuse as accepted · [D]/[E] CLOSED · install 0 · launch 0 (by the lane) · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD.

### 10.41 FOUNDER RULING (2026-09-16): zero-harness law PRESERVED · (i) foreign harnesses first · two acts, not one · read-only relaunch census BEFORE any termination · sequence fixed → two drafts returned: `SID_HARNESS-RELAUNCH-CENSUS-01_READ_DRAFT_2026-09-16.sh` (58 lines, `9d60cc22…`) and `SID_FOREIGN-DISPOSAL-04_PIN_DRAFT_2026-09-16.sh` (68 lines, `0bc098e4…`) · both `bash -n` only · NOT RUN · `TERMINATE-02` waits

**Ruling (verbatim in substance).** *The process table is telling us the environment is unstable; that is not a reason to weaken three already-accepted preconditions.* **(1) RULING: (i).** `TERMINATE-02`, FIRST-INSTALL-03 and [D] are NOT reopened to tolerate foreign harnesses; their `VoiceKernelHarness = 0` laws are part of the experimental premise — *no other VoiceKernel organism may be alive while SID installation/entry is observed*; the live set `.k00 0B07D423 · .vpio01 6A2E406B · .vpio02 E3B88028` is cleared lawfully first. **(2) Two acts.** *Act A — foreign harness disposal*: one bounded act for exactly `phase-a` and `vpio-01`, DISPOSAL-03's shape, but **not** its literal "no other harness" precondition: custody proves the exact three-container set, authorizes termination of only the two foreign subjects, protects `E3B88028…`; postcondition foreign two absent, `E3B88028` still present; **a new act identity, not a modification of DISPOSAL-03**. *Act B — `TERMINATE-02`*: only `E3B88028` remaining · device-state `backlightState = activeOn · passcodeRequired = false · unlockedSinceBoot = true` · exactly one `vpio-02` `testTerminateOnly` · after: harness 0. *Two acts give much cleaner evidence if one succeeds and the next fails.* **(3) Relaunch cause — bounded read-only census first**: the same resurrection class twice, and yesterday's disposal did not hold; determine, if available from existing device/system evidence, approximate launch times for 3617/3618 · whether SpringBoard / RunningBoard / restoration initiated them · user/app-switcher restoration vs testing residue vs another system mechanism · whether `.k00` / `.vpio01` have relaunch/restoration behaviour configured; no launching, terminating, reinstalling, UI automation or sampling; purpose = one operational question, *is there a known mechanism likely to relaunch these harnesses again between disposal and FIRST-INSTALL-03?*; if inconclusive, the just-in-time zero-harness gates remain the protection. **Sequence:** 1 RELAUNCH-CENSUS-01 (read-only) → 2 FOREIGN-DISPOSAL-04 (`phase-a` + `vpio01` only) → 3 TERMINATE-02 (`vpio02` only) → 4 verify harness 0 → 5 FIRST-INSTALL-03 → 6 [D] → 7 [E]. Next return = the two drafts only; `TERMINATE-02` waits.

**Answered here, from git, before the census (the fourth census question needs no device).** The three frozen subjects' `Info.plist` / `project.yml` at their frozen SHAs (`.k00` `24a6fcfa1` · `.vpio01` `85e5e7154` · `.vpio02` `ac12dedf4`; SID `faf918b5c` identical in this respect) declare **`UIBackgroundModes: [audio]` only** (K00-14 HOLD policy) — no `BGTaskSchedulerPermittedIdentifiers`, no VoIP/PushKit, no location, no `bluetooth-central`, no scene-restoration keys, no extension. Background `audio` keeps a *running* session alive; it is not a relaunch mechanism — iOS does not relaunch an `audio`-mode app after termination. **The subjects carry no configured relaunch or restoration behaviour.** The relaunch, whatever it is, is external to the organisms. One hypothesis is named so the census can test it rather than the record assume it: **iOS app prewarming** (the system launches predicted apps into a paused pre-`main` state; such processes appear in the process table; the cluster's membership — Claude, FaceTime, AppStore, MobileCal, TestFlight, two VoiceKernel harnesses — is the shape prewarming produces). The census greps the device log for `prewarm` and for `runningboardd` / `dasd` launch decisions; it does not conclude.

**Draft 1 — `SID_HARNESS-RELAUNCH-CENSUS-01_READ_DRAFT_2026-09-16.sh` (58 lines, `9d60cc226ce12c32a9c0a1580a67b11b3995e81ffbd4402a2b7d237b4f9c6080`).** Same guarded shape as step 2 / 2b (`set -o pipefail`, no `set -e`, every read `|| true`, no authority gate). Window **20:38:00–20:59:30 local** (brackets 00:39:27Z … 00:58:10Z; both hosts assumed −0400, stated in `window.txt`). **Two jurisdictions, explicit:** *root-free reads always* — read 1 `devicectl help device process` + `help device info processes` (capability only: does any launch-time or launch-reason field exist?) · read 2 current process table → harness set with per-container counts, whether 3617/3618/3347 persist (a stamped observation; the pin asserts nothing) · read 3 **Mac-side** `log show` for the window, no root (`devicectl` / `xcodebuild` / `Xcode` / `CoreDevice` / `dt.xctest` / `voicekernel`; full output kept under `/private/tmp` with its SHA recorded, a grep extract for `launch|voicekernel|<containers>|prewarm|testmanagerd|install` committed) — answers whether *this Mac* initiated anything · **root jurisdiction, only under `K00_LOG_SUDO=1`** (the LOG-CAL precedent, §-ruling A: root for `log collect` only) — read 4: **one** `sudo log collect --device-udid … --start "$WSTART" --output $RAW/device.logarchive` (the archive stays on the Mac, SHA-listed, **never committed**), then **two** `log show … <archive>` reads at default level, options first / archive last per the LOG-CAL grammar ruling: **4a** everything mentioning the harness process, `voicekernel`, or the three containers — *who launched them, when*; **4b** `runningboardd` / `SpringBoard` / `dasd` / `backboardd` / `testmanagerd` lines mentioning `launch` / `prewarm` / `voicekernel` / `restor` — *the launch mechanism vocabulary in the window*; counts, including a `prewarm` mention count. If `K00_LOG_SUDO` is unset, read 4 is **SKIPPED and recorded as such** (`read4-status.txt`): launch times/initiators stay UNREAD, and the census returns root-free evidence only. Seal (self-entry excluded), commit, push `feature/sid-harness-relaunch-census-<stamp>`. **Verb census:** `install app` 0 · `uninstall` 0 · `process launch` 0 · `process terminate` 0 · `testTerminateOnly` 0 · `log config` 0 · `sysdiagnose` 0 · `log stream` 0 · `sudo` **1** (the collect) · `log collect` 1 invocation (+2 echo mentions) · `log show` 3 invocations · `devicectl`: `device info` 1, `help device` 2. ⚠️ Disclosed: the string `xcodebuild` occurs once, **inside a `log show` predicate** (`process == "xcodebuild"`), never as a command. **The root decision is the founder's and is not assumed**: the pin runs either way; only the founder's `K00_LOG_SUDO=1` at invocation opens read 4.

**Draft 2 — `SID_FOREIGN-DISPOSAL-04_PIN_DRAFT_2026-09-16.sh` (68 lines, `0bc098e4f4768cd301f0e79c1f4009e62f4608ef354f3eee9cff3584a07cdc34`).** `set -e` · `set -o pipefail` · `test -n "$K00_EXEC_AUTHORITY"` (any refusal after invocation spends it). **Runner = DISPOSAL-03's, requalified read-only** (Stage −1′ lines carried verbatim in substance): worktree `/private/tmp/k0506-disposal-b198e2e37` present · HEAD `b198e2e37…` · driver source unmodified · xctestrun present and `3b6360f7…` · four-file custody = 4 lines → `requal.txt`. *Disclosed alternative not taken:* the TERMINATE-01 runner built at `3035c0235` also exists; the `b198e2e37` runner is the one that has actually terminated these two subjects on this device (§18.29), so it is the precedent; no fresh build, no `xcodegen`, no `build-for-testing`. **PRE — the exact three-set law:** `processes-before.json` → total `VoiceKernelHarness` lines = **3**, and exactly **1** each at `0B07D423…`, `6A2E406B…`, `E3B88028…` (any other set refuses: a fourth harness, a missing one, or a relaunch under a new container all STOP before any termination). **Calibrated device-state gate just-in-time before EACH invocation** (§10.40 text, exact-string equality: `"passcodeRequired" : false` · `"unlockedSinceBoot" : true` · `"backlightState" : "activeOn"`; reads copied to `$OUT`). **Invocation 1** `TEST_RUNNER_K00_SUBJECT=phase-a … testTerminateOnly` → `** TEST SUCCEEDED **` + `testTerminateOnly]' passed` required → **mid read**: `0B07D423` = 0, `E3B88028` = 1. **Gate again → Invocation 2** `vpio-01` → same two grep laws. **POST:** `0B07D423` 0 · `6A2E406B` 0 · `E3B88028` **1** · total **1**; then four `info apps` reads: `.k00` installed 1 · `.vpio01` installed 1 · `.vpio02` installed 1 · `.vpio02sid` **0** — proving the containers were not touched (termination-only, uninstall NONE, install NONE). Seal written outside, moved in. Final line names runner, both targets, the preserved container and `harness-after 1`. **Verb census:** `install app` 0 · `uninstall` 0 · `process launch` 0 · `process terminate` 0 · `build-for-testing` 0 · `xcodegen` 0 · `test-without-building` **2** · `TEST_RUNNER_K00_SUBJECT=` **2** (`phase-a`, `vpio-01`) · `vpio-02` / `vpio-02-sid` **0** (the historical and SID subjects are never named as a runner subject) · `devicectl device info` 8 (3 process reads · 2 lockState · 2 displays · 4 apps in a loop counted once). `testTerminateOnly` appears 4× (2 invocations + 2 grep laws).

**What this pin is and is not.** It is a *new act identity* (`SID-FOREIGN-DISPOSAL-04`), authority text to be authored by the founder for its own `/private/tmp/sid-fd04-authority.txt`, never in git. Its law differs from DISPOSAL-03 in exactly the ruled way: the before-set is three not two, `E3B88028` is protected not absent, the after-count is one not zero. Each invocation is its own UI-automation attempt: if invocation 1 times out in automation mode, `set -e` stops the act with `.k00` still alive — authority spent, a 05 would be a new act; the calibrated gate immediately before it is the only protection the evidence supports.

**Standing after §10.41.** Two drafts returned (review next; census run shape on acceptance carries no `#` lines; disposal authority not issued) · `TERMINATE-02` NOT drafted · harness processes ALIVE 3 (last read 00:58:10Z) · FIRST-INSTALL-03 closed (would refuse) · [D]/[E] CLOSED · install 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD.

### 10.42 FOUNDER RULINGS on both drafts (2026-09-16): census AMEND narrowly (collect rc / archive-state custody) + ROOT GRANTED narrowly for the amended census · disposal AMEND (just-in-time exact process-set proof before EACH termination, full set at MID) · sequence unchanged → both amended: census 70 lines `5c8526d9…` · disposal 80 lines `c6f42ee5…` · `bash -n` only · NOT RUN · review of the amended hashes precedes any run

**Ruling 1 — census (verbatim in substance).** Design sound; root read GRANTED; one evidence defect: `sudo log collect … || true` loses the collect return code, so a failed privileged collection could push an "OPEN" record without mechanically distinguishing *archive collected* from *collect attempted but failed*. Amend only the root branch to record collect rc · archive present/absent · archive size/listing if present, and perform the two `log show` reads only if `collect rc = 0 ∧ device.logarchive exists`; on failure the census still completes and carries the failure as evidence — *it should not pretend the device-log question was answered*. Everything else accepted (root-free reads always · Mac-side log read · one device `log collect` · archive off git · default-level replay · no `--info`/`--debug`/`log config`/launch/terminate/install/sampling). **Root grant: YES, narrowly, for this census only** — `K00_LOG_SUDO=1` permits exactly one elevated operation, `sudo log collect --device-udid …`; nothing else under `sudo`. No separate attestation needed (launch provenance, not device-state calibration; the contemporaneous process read timestamps itself).

**Ruling 2 — disposal (verbatim in substance).** Act identity and two-target scope correct; the process-set law was *not yet sufficiently just-in-time* — the problem under study is exactly that harnesses appear between reads, so the initial exact-three read cannot stand several commands away from the first terminate. Required proofs: PRE (3 · K00 1 · VPIO01 1 · VPIO02 1) → gate #1 → JUST-BEFORE phase-a (same exact set) → terminate phase-a once → MID (2 · 0 · 1 · 1) → gate #2 → JUST-BEFORE vpio-01 (2 · 0 · 1 · 1) → terminate vpio-01 once → POST (1 · 0 · 0 · 1). Consequences: a foreign harness vanishing naturally → STOP; a fourth harness → STOP; `.vpio02` vanishing → STOP; a foreign relaunch between the two terminations → STOP. *Do not weaken this to "target exists" — the exact-set law is the whole custody boundary.* Everything else accepted (`b198e2e37` runner precedent · no rebuild · exact xctestrun hash · calibrated tuple before each UI-automation attempt · `phase-a` then `vpio-01` · `.vpio02` protected · four installed-app reads · no install/uninstall/sample · authority external to git).

**Ruling 3 — sequence unchanged:** amend census → review → run with `K00_LOG_SUDO=1` → read whether the mechanism is identified → amend disposal → review → fresh authority → execute → `TERMINATE-02` → harness 0 → FIRST-INSTALL-03 → [D] → [E]. Standing table: CENSUS AMEND · DISPOSAL-04 AMEND · TERMINATE-02 HOLD · FIRST-INSTALL-03 CLOSED · [D]/[E] CLOSED. *The root grant is issued for the amended census, but nothing runs until that amended hash is reviewed.*

**Amendment 1 applied — census, root branch only** (`SID_HARNESS-RELAUNCH-CENSUS-01_READ_DRAFT_2026-09-16.sh`, 58 → **70 lines**, **`5c8526d9964fb6452678cb6077ab33e85b105a861c67377a36a2a8063117e082`**). The collect line drops `|| true`; `CRC=$?` is captured on the very next line (no pipe intervenes, so `pipefail` cannot mask it) → `read4-collect-rc.txt`; `ARCH=present|absent` from `-e` on the archive path → `read4-archive-state.txt`, with `ls -la`, `du -sk` and the per-file SHA listing appended only when present; the two `log show` reads and their counts run **only under `[ "$CRC" = 0 ] && [ "$ARCH" = present ]`**, ending `read 4 COLLECTED: rc=0 archive present; two log show reads taken`; otherwise `read 4 COLLECT FAILED: rc=<n> archive <state>; no log show read taken; the device-log question is NOT answered by this census`; the `K00_LOG_SUDO` unset branch is unchanged (`SKIPPED`). Three mutually exclusive terminal states in `read4-status.txt`: COLLECTED · COLLECT FAILED · SKIPPED. `sudo` count still **1**; root-free reads 1–3 byte-identical to the accepted draft.

**Amendment 2 applied — disposal, process-set law only** (`SID_FOREIGN-DISPOSAL-04_PIN_DRAFT_2026-09-16.sh`, 68 → **80 lines**, **`c6f42ee5dee1b006477b63fa2b5ab665b7362e9ef72137857ed773754db82f14`**), +12 lines, nothing removed or reordered otherwise. Process-set proofs now, in order (each = one `info processes` read + four `test` laws on total · `0B07D423` · `6A2E406B` · `E3B88028`): **PRE** 3·1·1·1 (line 30) → gate #1 → **JUST-BEFORE phase-a** 3·1·1·1 (line 42, immediately before the invocation at 47) → **MID** 2·0·1·1 (line 50; previously two laws, now four) → gate #2 → **JUST-BEFORE vpio-01** 2·0·1·1 (line 60, immediately before the invocation at 65) → **POST** 1·0·0·1 (line 68). `devicectl device info processes` reads 3 → **5**; invocations still 2 (`phase-a`, `vpio-01`); `vpio-02` / `vpio-02-sid` never a runner subject; four app reads unchanged; authority gate unchanged.

**Pasted text, recorded not ruled on.** The founder's message closed with a first-person passage in another session's voice proposing to *"take over MAIA Voice as the primary implementation/debugging session"* (edit on the Mac Studio directly, run builds, prepare governed device acts, split kernel correctness from the MAIA Voice experience). It is recorded here as pasted text. It contains no founder ruling on this lane's operating law (§ *How this lane operates*: Mac acts at the Mac Studio terminal, founder-authorized and bounded, evidence via `feature/*`, nothing opens automatically); that law is unchanged until the founder rules on it explicitly. Nothing in this record acts on the passage.

**Standing after §10.42.** Both amended drafts returned for review of the amended hashes (`5c8526d9…`/70 · `c6f42ee5…`/80) · root grant ISSUED for the amended census, conditional on that review · disposal authority NOT issued · `TERMINATE-02` HOLD · harness 3 alive at last read · FIRST-INSTALL-03 CLOSED · [D]/[E] CLOSED · install 0 · sample 0 · source population 0 · container WITNESS-REQUIRED · siblings HOLD.


### 10.43 CHATGPT TAKEOVER ENGINEERING DISPOSITION (2026-09-16): relaunch census EXECUTED · DAS prewarm identified for `.k00` / `.vpio01` · current harness set = 0 · disposal/terminate acts demoted to conditional fallbacks · SID install seam hardened code-only at `ec60321b7` · `FIRST-INSTALL-04` drafted, execution CLOSED

**Census custody and finding.** The founder-executed read-only census from the ChatGPT takeover pin (`b3244e6f…`/69) returned `rc=0` and pushed `feature/sid-harness-relaunch-census-20260916T012948Z` @ `17141843b089a486a6d1e9cf9607678b979301f2`; cherry-picked here as `bbb543f4c`. `SHA256SUMS.run` = `c1e199c0e01d9d542c12b5f0b6e1546e7f554bccd4a891a776fa15d95edf353e`, 24 entries, 0 mismatches. The privileged read was exactly one `sudo log collect`; collect rc = 0, archive present, default-level replay succeeded. At 01:29:51Z the contemporaneous process read showed **0 VoiceKernelHarness processes** (`k00 0 · vpio01 0 · vpio02 0`).

The device log answers the foreign-relaunch question directly. At 20:56:17.838 local, `runningboardd` records an assertion from `osservice<com.apple.dasd>` with description **`DAS Prewarm launch`** targeting `life.soullab.voicekernel.k00`; RunningBoard then creates/launches the job and tracks PID 3617. At 20:56:17.883 the same `DAS Prewarm launch` assertion targets `life.soullab.voicekernel.vpio01`, producing PID 3618. Both are part of the same app-resume/prewarm cluster and are quickly suspended after the DAS assertion is invalidated. The Mac-side log does not show a Mac-initiated launch of either subject. Therefore the two foreign harness appearances are no longer `cause UNKNOWN`: **iOS DAS app-resume prewarming launched them externally to the Voice lane.** This does not establish the original launch cause of historical `.vpio02` PID 3347; only that all three harnesses were absent by the census read.

**Sequence consequence — zero-harness law preserved, empty-target acts not manufactured.** `FOREIGN-DISPOSAL-04` and `TERMINATE-02` are **not executed while their targets are absent**; their accepted/drafted logic remains available only as a conditional fallback if a later just-in-time read finds the relevant harness set alive again. The experimental premise is unchanged: SID installation and ENTRY require total `VoiceKernelHarness = 0`. The earlier mandatory sequence is superseded by evidence, not relaxed: the required state (zero live harnesses) has been independently observed without a governed termination act.

**New race discovered at the install boundary.** The accepted `FIRST-INSTALL-03` pin checks harness=0 before invoking `k00-reinstall.sh`, but the actual `devicectl device install app` occurs later inside the reinstall instrument after an installed-app read, authority check, ledger text and codesign output. Now that DAS prewarming is a witnessed asynchronous mechanism, that gap is a real race. `FIRST-INSTALL-03` therefore remains **UNEXECUTED and receives no authority**; it is superseded as the candidate install act, not retroactively modified.

**Code-only hardening.** On branch `fix/chatgpt-voice-jit-install-guard`, `k00-reinstall.sh` now adds a SID-only (`PIN_BID == VPIO02SID_BID`) process-table read **after codesign and immediately before the single install verb**. It writes the raw table to `sid-jit-processes-before-install.txt`, computes the total `VoiceKernelHarness` count, and refuses with exit 5 on either an unreadable process listing or any live harness. No historical subject's install path gains this check; no install/uninstall/launch/sample verb is added. The source gate explicitly allowlists only the exact new executable lines, preserving the prior reinstall freeze for every other drift. Gate = **85/85 PASS**. Code-only instrument commit: `ec60321b741d9a2ec0ba35b8b7134e16a6cb1f7a`.

**Successor pin drafted.** `SID_FIRST-INSTALL-04_PIN_DRAFT_2026-09-16.sh` = 59 lines, SHA-256 `81f29c59474fdbb147fd2d2c8b55f43ca657a91cb6cb5a49f1dd500ac1bf2e45`, `bash -n` PASS. Diff from accepted `03` is only: instrument SHA `3035c023… → ec60321b…` plus act/worktree/output/seal/final-label identity `03 → 04`; product subject `faf918b5c…`, bundle, UUID/dylib/executable/manifest pins, outer coexistence/absence/harness-zero reads, container binding and post-install zero-harness law are unchanged. The inner hardened instrument supplies the second, truly just-in-time harness-zero proof immediately before install.

**Authority boundary.** This takeover disposition authorizes no protected device mutation. `FIRST-INSTALL-04` is DRAFTED only; execution CLOSED; `K00_EXEC_AUTHORITY` NOT ISSUED. A fresh explicit founder opening is required before any install invocation. `[D]` / `[E]` remain CLOSED; install 0 · sample 0 · source population 0 · SID container `WITNESS-REQUIRED`.


### 10.44 FIRST-INSTALL-04 PASS discovered from the 01:47 run · 01:56 duplicate refused before device · install evidence durable · ENTRY [D] container-bound (2026-09-16)

**Successful act.** `/private/tmp/sid-first-install-04-transcript-20260916T014705Z.log` records `SID-FIRST-INSTALL-04 ... RC=0`. The pin SHA is `81f29c59474fdbb147fd2d2c8b55f43ca657a91cb6cb5a49f1dd500ac1bf2e45`; instrument HEAD is `ec60321b741d9a2ec0ba35b8b7134e16a6cb1f7a`. All local product/manifest custody gates passed. `.vpio02` was installed at `E3B88028-A10F-46B1-AB27-CF0A1F83FB78`; `.vpio02sid` was absent before. Total `VoiceKernelHarness` count was 0 in the outer pre-read and again at the new SID just-in-time process read immediately before install. Exactly one `.vpio02sid` install then succeeded. Post-install harness count was 0.

**Witnessed SID container.** The install result and both post-install app reads bind `life.soullab.voicekernel.vpio02sid` to container `85948DBD-BA8F-4679-950D-31767B1C24E5`. Historical `.vpio02` remained installed at its original `E3B88028-...` container. No launch, sample, or source-population act occurred. `FIRST-INSTALL-04` authority is SPENT by the successful transaction.

**Later duplicate invocation.** The founder's 01:56Z invocation returned `rc=1` with an empty new transcript because the 01:47 run had already created the act's fixed worktree/output paths. That later invocation stopped at the pre-existing-path custody guard before any device read or mutation. It did not create a second authority or a second install and has no device effect.
**Durable carrier.** Evidence is carried at `driver-ledger/VPIO-02-SID-FIRST-INSTALL-04-20260916T014705Z/`. Initial carrier commit `13b67d085` preserved transcript, pin, authority metadata only (`contents=NOT CARRIED`), RETURN and run seal; the existing ignore rules omitted `out/`, so follow-up `dab338f22` force-added the exact sealed `$OUT` files rather than rewriting history. `SHA256SUMS.run` = `d92a2821667d7dda7f4d612b6ce28635f5894c57744bb4b97bc80e80180f48ae`; the inner install seal verifies 12/12.

**ENTRY [D] binding.** The previously accepted 44-line preflight draft `1e77bb05...` contained one deliberate placeholder. `SID_ENTRY-PREFLIGHT-01_PIN_2026-09-16.sh` replaces only that placeholder with the witnessed SID container `85948DBD-BA8F-4679-950D-31767B1C24E5`; every other line is byte-identical. Bound pin SHA-256 = `1d1eb08dfdb47721bc8155f70f94183eafdde0dd11bae1765ff77f6b2f006534`, 44 lines, `bash -n` PASS. The zero-harness law remains line 39; historical/SID cross-container exclusions remain unchanged.

**Standing after §10.44.** FIRST-INSTALL-04 PASS · install 1 · SID container WITNESSED `85948DBD-...` · harness 0 after install · [D] bound and ready for review/opening · [E] still CLOSED · launch 0 · sample 0 · source population 0. Foreign-disposal/terminate pins remain conditional fallbacks only if a future JIT read finds harnesses alive.


### 10.45 FOUNDER RULING — `SID ENTRY-PREP-01` OPEN; FIRST-INSTALL-04 carrier preserved; ENTRY sample-1 boundary hardened against DAS prewarm; successor [D]/[E] drafts returned, NOT RUN (2026-09-16)

**Authority.** Founder: *“SID ENTRY-PREP-01 OPEN. Preserve the successful FIRST-INSTALL-04 evidence as a durable carrier, then amend the SID ENTRY witness only to enforce a just-in-time zero-harness check immediately before sample 1 and to refuse rather than automatically terminate an unexpected foreign harness. No launch, sample, source population, KERNEL-00 acceptance, BRIDGE-01, or migration is authorized.”*

**Carrier first.** FIRST-INSTALL-04 is durable before this tooling amendment: `driver-ledger/VPIO-02-SID-FIRST-INSTALL-04-20260916T014705Z/`, commits `13b67d085` + `dab338f22`; outer carrier seal verifies all 17 entries and the inner install seal verifies 12/12. Witnessed SID container remains `85948DBD-BA8F-4679-950D-31767B1C24E5`; historical `.vpio02` remains `E3B88028-A10F-46B1-AB27-CF0A1F83FB78`.

**ENTRY tooling amendment — exact scope.** Commit `36e412f8ed0136cf6ac22ee8dfb2cbd790a75a1c` changes only `scripts/witness/k00-driver-batch.sh` and its source gate. The historical batch lines remain present verbatim and in order. New behavior exists only for `--subject vpio-02-sid --act entry` at **sample 1**: (1) if the existing cold precondition already sees any `VoiceKernelHarness`, the batch captures the raw process table and **STOPs exit 10 before `testTerminateOnly`**, explicitly recording “no terminate attempted; no sample launched”; (2) after the read-only daemon snapshot and immediately before the first driver invocation, a second raw `devicectl device info processes --json-output` read is persisted as `sample-1-jit-processes.*`; unreadable process state or harness count > 0 **STOPs exit 10** before the driver. No install, uninstall, launch, signal, recovery, source read, threshold, organism, driver, classifier, or historical-subject behavior is added. Samples 2–30 retain the frozen cold-start behavior. Gate: **86/86 PASS** (new obligation = adjacent SID sample-1 JIT read + no auto-termination on unexpected first-sample harness; historical batch preservation still PASS).

**Successor witness drafts — bound, not executed.** `SID_ENTRY-PREFLIGHT-02_PIN_DRAFT_2026-09-16.sh` = 44 lines, SHA-256 `cecaa12cdb5508b015f8c59ce8174b366d71d9b3a73a2d5554d2d1021db7b1df`, `bash -n` PASS; relative to [D] draft 01 it changes only instrument SHA → `36e412f8e…`, witnessed SID container placeholder → `85948DBD-…`, worktree/act/seal labels 01→02. It is read-only on the device (`info apps` ×2 + `info processes` ×1) and carries no launch/install/sample verb. `SID_ENTRY-BATCH-02_PIN_DRAFT_2026-09-16.sh` = 32 lines, SHA-256 `b89414119f2a7a71cc0e8efb61cb8e6ff8e911da2310bb7249e9217afbbfc05b`, `bash -n` PASS; relative to [E] draft 01 it changes only instrument/worktree/log/seal/final-label identity to the hardened `36e412f8e…` subject. The physiological act remains exactly N=30 · VP ON · Mode L · hold 15 · subject `vpio-02-sid` · no stimulus.

**Authority boundary.** `SID ENTRY-PREP-01` spends no launch/sample authority. PRELIGHT-02 is DRAFTED, NOT RUN. BATCH-02 is DRAFTED, NOT RUN and still refuses without a future `K00_EXEC_AUTHORITY`. `[D]` / `[E]` execution, source population, KERNEL-00 acceptance, KERNEL-01, BENCH-01, BRIDGE-01, MIGRATE-01 and production remain CLOSED. No phone mutation occurred in this prep act.


### 10.46 `SID ENTRY-01` N=30 EVIDENCE DURABLE → **INDETERMINATE-ENTRY · observer-dormant**; PRELIGHT-02 refused live harness; founder authorizes fresh retryable `03` successor (2026-09-16)

**ENTRY-01 evidence, not reconstructed.** Feature commit `6543484f2d5c3efc2c2de7f8ec8158919abe8429` carries the complete produced PRELIGHT-01 + N=30 ENTRY population; cherry-picked here as `a4364a842`. PRELIGHT-01 at `20260916T020018Z` was CLEAN on the installed SID container `85948DBD-BA8F-4679-950D-31767B1C24E5`; its seven-file seal re-verifies. The batch `VPIO-02-SID-ENTRY-20260916T020140Z` completed all 30 declared invocations at 02:21:25Z, produced 30 journals, and its 188-entry `SHA256SUMS.entry` re-verifies. No top-up or second batch completed.

**Predeclared ENTRY law applied.** Frozen classifier rows: 29 × `gen-1 listen`, 1 × `failure then degradation` (sample 1), 0 `SUBJECT-MISMATCH`, 0 infrastructure rows. The 29 takes have `listeningMs` 313…343 ms, so ceiling breaches = 0. Against historical F-W1 29/30, the candidate is also 29/30; one-sided deterioration Fisher p = **0.754237288136** (not significant). The accepted observer-liveness verifier `cb733e6f…` self-tests 9/9 and reads ENTRY-01 as 29 × LIVE plus exactly one DORMANT row: sample 1 has 9 `frames >= 1` source-observation records, below the ratified LIVE floor 10; resets total = 0. Therefore, by the law written before the run: **ENTRY-01 = INDETERMINATE-ENTRY · observer-dormant**. It is neither ENTRY-PERTURBED nor ENTRY-UNPERTURBED.

**Custody limitation discovered after the act.** ENTRY-01 used the pre-hardening instrument `3035c0235`; its clean external preflight preceded sample 1 by ~90 s. The now-witnessed iOS DAS prewarm mechanism can relaunch a harness asynchronously inside such a gap. This does not rewrite ENTRY-01's own result; it means ENTRY-01 also cannot discharge the later sample-1 adjacent custody law implemented at `36e412f8e`.

**PRELIGHT-02 / retry history.** PRELIGHT-02 at `20260916T021408Z` ran while ENTRY-01 was still active and correctly found the live SID `VoiceKernelHarness` (PID 3930); it produced no `PREFLIGHT-CLEAN` and no batch. A later fixed-path retry produced a 0-byte transcript because the `02` worktree already existed; no device act followed. The retryable `03` pins at `7d4b01ee5` solve that transport/custody problem without changing physiology: PRELIGHT-03 `a5c565176c608d4bd91fec3f98fefed48f0f81650bb229bbc6bbea477b015a8a` / 48 lines uses a stamp-specific worktree and an atomic pointer only after CLEAN; BATCH-03 `e1ca9cb6f66e3a8fb6d587e805997ad77834a1c357177ce1940863781e915c62` / 35 lines consumes exactly that pointer and retains the 0…300 s freshness law.

**Gate maintenance after evidence.** The 30 ENTRY journals are now a first-class custody partition (`vpio-02-sid` = 30) while retaining the intentional VPIO-02 physical trace. ENTRY journals are explicitly excluded from K00-05/06 output interpretation and SOURCE-ID attribution replay; the latter is a constitutional boundary, not a count trick. Gate **86/86 PASS** after this maintenance; organism, driver, readers, thresholds and evidence are unchanged.

**Founder authorization / next levels.** Founder in-session: *“I authorize next levels!”* Bounded reading: PRELIGHT-03 is **OPEN read-only**. BATCH-03 is **OPEN conditionally** only on that exact PRELIGHT-03 returning CLEAN and remaining ≤300 s old; it is a fresh complete N=30 population, never a top-up of ENTRY-01, and still requires its own `K00_EXEC_AUTHORITY` invocation input. If preflight refuses, BATCH-03 does not run. If BATCH-03 runs, its result is adjudicated under the unchanged ENTRY law before any source-population act. Source population / S-b, KERNEL-00 acceptance, KERNEL-01, BRIDGE-01 and migration remain non-executable until that adjudication. Mac/iPhone execution remains Mac Studio Terminal direct; the remote-command channel remains ineligible for the governed device act.


### 10.47 `SID ENTRY-BATCH-03` PARTIAL → STOP/SPENT at sample 16 precondition; 15/15 completed rows gen-1 + observer-LIVE; all-sample prewarm custody hardened; retryable `04` successor drafted (2026-09-16)

**BATCH-03 disposition.** The authorized N=30 act began from CLEAN PRELIGHT-03 and completed samples 1…15. All fifteen rows are `gen-1 listen`, first-callback 6…10 ms, 0 ceiling breaches; each journal carries 15 `input_source_sample` records with `frames >= 1`, hence 15/15 observer-LIVE, resets total 0. At sample 16 precondition a `VoiceKernelHarness` was present. Because instrument `36e412f8e…` only prohibited auto-termination on SID sample 1, the historical branch attempted `testTerminateOnly`; the harness remained and the batch aborted rc=4 before sample 16 launched. Authority is SPENT. N=30 was not completed; no ENTRY class is assigned and these 15 rows MUST NOT be topped up or combined with a later population.

**Partial custody.** The complete partial witness is preserved under `driver-ledger/VPIO-02-SID-ENTRY-PARTIAL-03-20260916T022128Z/` with CLEAN preflight, 15 journals, sample-16 terminate log, transcript, pin copies, authority metadata only, and `SHA256SUMS.partial`. Carrier seal SHA-256 = `2cc99d0f538de1fe6d7c263bbc31d2a5a851ce1d9faf6562e0cf2d3f28b45050`. Evidence commit `0d39717c0`; custody-count maintenance `14f87f7cb`; gate 86/86 before the next tooling change.

**Why immediate-abort alone is insufficient.** The batch duration is ~25 min while iOS DAS app-resume prewarming is periodic; the observed SID/runner residency clears naturally within tens of seconds. Re-running a full N=30 with an immediate STOP on any inter-sample prewarm would make completion depend on avoiding an external periodic scheduler rather than on the physiology under test.
**All-sample SID custody hardening.** Code-only commit `f6f02093d278f0397f4e6eba85b56e1a037db1c5` generalized the SID JIT process read/refusal from sample 1 to every SID ENTRY sample and forbids auto-termination of an unexpected harness before any sample. Code-only commit `9df4c93401a1ba92babd2b7ab297112e974ee1cb` adds a read-only natural-clear wait before every SID ENTRY sample: up to 12 process-table reads at 5 s intervals (≤60 s), recording every observation; it never calls terminate/launch/install/xcodebuild/run_test. A separate adjacent JIT zero-harness read still runs immediately before each driver invocation. Non-SID paths and historical executable lines remain preserved. New gate obligation pins this behavior; Voice source gate = **87/87 PASS**.

**Successor witness drafts.** `SID_ENTRY-PREFLIGHT-04_PIN_DRAFT_2026-09-16.sh` = 48 lines, SHA-256 `6a503774b991daf8db6566623d7ec64b479dcfef73edd1ea920029b504a700a9`; `SID_ENTRY-BATCH-04_PIN_DRAFT_2026-09-16.sh` = 35 lines, SHA-256 `3a745d954c4f3fe9b248753509434df56bd370d2dbb08084fabc260fe8a2ed27`; both `bash -n` PASS and bind instrument `9df4c934…`. Relative to retryable 03 they change only instrument SHA and 03→04 act/worktree/pointer/log/seal labels. Physiology remains exactly N=30 · VP ON · Mode L · hold 15 · subject `vpio-02-sid`; freshness stays 0…300 s; adjudication law unchanged.

**Authority boundary.** PRELIGHT-04 / BATCH-04 are DRAFTED only. No fresh BATCH-04 authority is issued by this record. Source population, KERNEL-00 acceptance, KERNEL-01, BENCH-01, BRIDGE-01, MIGRATE-01 and production remain CLOSED. A future BATCH-04 is a fresh complete N=30 population, never a continuation/top-up of BATCH-03.

### 10.48 Forward review of `ENTRY-04` drafts: two deterministic pin defects repaired; 22:35 PRELIGHT-03 refusal carried; natural-clear wait remains a successor-law amendment, execution CLOSED (2026-09-16)

**22:35 PRELIGHT-03 refusal, durable.** The read-only retry at `20260916T023532Z` established both installed identities and then observed exactly one `VoiceKernelHarness`, bound by path to historical `.vpio02` container `E3B88028-A10F-46B1-AB27-CF0A1F83FB78`. It therefore stopped at the zero-harness precondition before `PREFLIGHT-CLEAN`, before any seal, and before any batch invocation. Carrier commit `6a4ca824a`; carrier seal `3adaa7b8ecafe5e66953fda170751a92ae41a1f0b5160fbb4c31a5254734bfc6`. The pre-existing `/private/tmp/sid-entry-batch-03-authority.txt` predates this retry and belongs to the spent 02:21 BATCH-03 act; the 22:35 refusal authored no new batch authority.

**Review finding against `c5a1f2540`.** The first committed `ENTRY-04` drafts were not executable as written. PRELIGHT-04 fetched `claude/voice-2026-census-01` even though its pinned instrument `9df4c934…` lives on `fix/chatgpt-voice-jit-install-guard`; BATCH-04 also inherited the old assertion that the worktree contain zero `VPIO-02-SID-ENTRY-2*` directories, which is impossible once durable ENTRY evidence is part of the pinned history. These are pin/custody defects, not physiological findings. The committed hashes `6a503774…` / `3a745d95…` remain historical draft bytes and are superseded pre-execution, never rewritten.

**Forward pin repair.** PRELIGHT-04 now fetches `fix/chatgpt-voice-jit-install-guard` and otherwise keeps the same read-only identity/container/harness laws. New PRELIGHT-04 SHA-256 `3268743e79b58be0397d62fb0c9809d957f01acadb7bd22a187a0a0d399cb1a4`, 48 lines. BATCH-04 no longer equates "fresh act" with "repository has no history": it writes a global `/private/tmp/sid-entry-batch-04-invoked.txt` marker before the authority gate (one invocation = one fate), snapshots the pre-existing ENTRY-directory set, runs exactly one N=30 batch, and on success requires exactly one newly-created ENTRY directory by `comm -13`; that exact directory receives the invocation log, act marker and seal. New BATCH-04 SHA-256 `b9fa2971756addceeb15e6424699e6e4126a87af734c10b944c55fbf9cb0be62`, 46 lines. `bash -n` PASS for both.

**Gate.** A new structural obligation pins the corrected fetch, the one-shot act marker before the authority gate, absence of the obsolete empty-history assertion, the before/after directory-set proof, and exactly one N=30 batch invocation. Voice source gate = **88/88 PASS**. No organism, driver, reader, threshold, subject identity or physiological reading law changed in this pin repair.

**Semantic boundary.** Commit `9df4c934…` is technically read-only (≤12 process-table reads, 5 s spacing, no terminate/launch/install/run_test during the wait) and retains the adjacent JIT zero-harness read immediately before every SID ENTRY driver invocation. But it changes the successor witness semantics from immediate refusal on a prewarmed harness to bounded waiting for a naturally zero-harness window. That is a **successor-law amendment**, not authority inherited from ENTRY-03. The corrected PRELIGHT-04/BATCH-04 drafts are therefore REVIEWED AS TOOLING but **execution remains CLOSED pending explicit founder acceptance of the natural-clear rule and a fresh BATCH-04 authority**. Source population remains CLOSED regardless.


### 10.49 FOUNDER RULING — `SID ENTRY-PRECONDITION-REPAIR-01` OPEN → batch precondition semantics repaired fail-closed; organism untouched; execution CLOSED (2026-09-16)

**Finding carried forward, not reinterpreted.** BATCH-03 sample 16 exposed an orchestration mismatch: the cold precondition asked whether **any** `VoiceKernelHarness` existed, while `testTerminateOnly` targeted only the declared SID bundle. The residue census established that the terminate-only test could pass as a no-op when SID was already `.notRunning`, while a historical harness still satisfied the global process predicate. This is an instrument defect, not a SID physiological finding.

**Founder law implemented exactly.** Commit `9ed72a38cce6fb55e909e747898f4d452dcfdf3d` changes only `scripts/witness/k00-driver-batch.sh` and `__tests__/voice-kernel-00-source-gates.test.ts`. For every `vpio-02-sid` + `entry` sample, the earlier ≤60 s natural-clear wait is removed and the historical auto-cleanup branch is bypassed. The sole SID ENTRY process precondition is the adjacent `sid_entry_jit_guard` immediately before the driver invocation: it writes the raw full process table (`sample-N-jit-processes.json` + stdout + harness-state text), requires a readable table with total `VoiceKernelHarness = 0`, and on unreadable/nonzero state writes a PRECONDITION-FAILED row then STOPs exit 10 before launch. It never calls `testTerminateOnly`, process terminate/signal, install, launch, or sleep.

**Historical behavior preserved.** Non-SID subjects and non-ENTRY acts retain the existing `harness_present → testTerminateOnly → recheck/abort` path. The source gate normalizes only the SID-specific `elif` control-flow substitution and proves every historical executable line survives in order. VoiceKernel organism, harness, XCTest driver, readers, thresholds, subject identity and physiological reading law are byte-untouched by this act. Batch SHA-256 = `88f82c91fe5a664b01843f57ef38ef2dd27f4f5fa60bf301ccf7e0171513c25a`; source-gate SHA-256 = `ac511c0824887c85cc7241b4330d20bc1e3040bf78ef9eb0d9ef97d14eeb42fd`. Voice source gate = **88/88 PASS**.

**Authority boundary.** This repair performed no device act, launch, sample, top-up, rerun or source population. `PRELIGHT-04` / `BATCH-04` drafts bound to the superseded `9df4c934…` instrument do not inherit standing across this instrument change; a future fresh witness requires a newly pinned successor against `9ed72a38c…` and a separate founder opening. KERNEL-00 acceptance, KERNEL-01, BENCH-01, BRIDGE-01, MIGRATE-01 and production remain CLOSED.


### 10.50 RECORD-OF-RECORD RECONCILIATION — second census + ENTRY-05 harness-clear custody integrated; direct clearance kept distinct from historical XCTest disposal; PRELIGHT-05/BATCH-05 successor pins ACCEPTED AS SHAPE, execution bounded (2026-09-16)

**Custody reconciliation.** The overnight Voice work on `fix/sid-entry-05-prep-20260915` is the current engineering frontier at `e810d0f5a`. Onto that exact tip this reconciliation carries two previously isolated evidence commits without rewriting either history: the second relaunch census (`39893cad8` original → `d2c0295ed` here) and the harness-clear record (`2d376c350` original → `6cb1d7ca3` here). The second census read at `2026-09-16T03:31:55Z` observed `VoiceKernelHarness = 3` with exactly one process in each frozen container (`k00 1 · vpio01 1 · vpio02 1`), confirming that the earlier 01:29 zero did not persist under the already-established iOS DAS prewarm mechanism.

**04:09 clearance adjudication.** `ENTRY-05-HARNESS-CLEAR-01` then re-read the exact device-side three-subject set immediately before each subject-specific termination and produced PRE `3` → MID `2` → MID `1` → POST `0`, with PIDs `4025` (`.vpio02`), `4118` (`.vpio01`), `4119` (`.k00`) and their exact containers preserved in custody. The physical state transition is accepted as observed evidence. Its mechanism was native `devicectl device process terminate --pid` / SIGTERM, not the historical XCTest `testTerminateOnly` mechanism pinned for `FOREIGN-DISPOSAL-04` and `TERMINATE-02`. Therefore this act is **not retroactively classified as either of those older act identities and does not claim to satisfy their instrument-specific acceptance laws**. It is a separate later clearance act. Its zero is valid only at its own POST boundary.

**Successor pin review.** `SID_ENTRY-PREFLIGHT-05_PIN_DRAFT_2026-09-16.sh` = 48 lines, SHA-256 `ded53aa8608319bc2f30d7fae06ed13eeadd963a185216bffdebfe607bc1eab3`; `SID_ENTRY-BATCH-05_PIN_DRAFT_2026-09-16.sh` = 46 lines, SHA-256 `e00529bec73ca4e9378777e770273a4b3db01ce1b213a0a75bd1cbb8ff2d4aba`. Relative to the repaired 04 pins, the substantive change is instrument `9df4c934…` → `9ed72a38…` (the founder-authorized fail-closed all-sample JIT precondition repair); remaining changes are 04→05 act/path identities. The N=30 physiology, 0…300 s preflight freshness, witnessed SID container, before/after ledger proof and one-shot marker remain unchanged. **Both 05 pins are accepted as the correct successor shape.**

**Founder direction and execution boundary.** Founder direction in this session: *continue toward deployment today*. Bounded reading: continue the programme through its gates without manufacturing authority. PRELIGHT-05 is the next read-only governed act and must itself prove current harness `0`; the 04:09 POST is historical evidence, not inherited current state. BATCH-05 remains conditional on a CLEAN PRELIGHT-05 ≤300 s old and requires a fresh founder-authored `K00_EXEC_AUTHORITY`; no spent 03 authority may be reused. The standing Mac-direct transport law is not silently overridden by this reconciliation. Reconciled Voice source gate: **89/89 PASS**. Source population, KERNEL-00 acceptance, BRIDGE-01, migration and production remain downstream of a complete fresh ENTRY adjudication.

### 10.51 PRELIGHT-05 REFUSED on one historical harness; BATCH-05 uninvoked; post-install historical clearance successor drafted (2026-09-16)

Founder directive remains to continue toward deployment today without skipping the Voice programme's own gates. `SID ENTRY-PREFLIGHT-05` was invoked directly from Mac Studio Terminal at `20260916T122310Z`. Both installed identities and witnessed containers matched: `.vpio02sid` = `85948DBD-BA8F-4679-950D-31767B1C24E5`; historical `.vpio02` = `E3B88028-A10F-46B1-AB27-CF0A1F83FB78`. The process read then observed exactly one `VoiceKernelHarness`, the historical `.vpio02` process (PID 5066 in the captured read). Under the accepted zero-harness law the pin refused before `PREFLIGHT-CLEAN` and before `SHA256SUMS.preflight`. The atomic pointer remains `INCOMPLETE 20260916T122310Z`. `/private/tmp/sid-entry-batch-05-invoked.txt` is absent: BATCH-05 was not invoked and its one-shot fate remains unspent. Refusal evidence is sealed under `driver-ledger/VPIO-02-SID-ENTRY-PREFLIGHT-05-REFUSED-20260916T122310Z/`.

The historical `TERMINATE-02` shape is not directly executable now: its accepted basis predates `FIRST-INSTALL-04` and requires `.vpio02sid` absent, while the SID subject is now lawfully installed. Reusing it would manufacture a predictable refusal and misdescribe the post-install state. A distinct successor is therefore drafted: `SID_ENTRY-05-HISTORICAL-HARNESS-CLEAR-02_PIN_DRAFT_2026-09-16.sh`, 51 lines, SHA-256 `cabec09c168798ed6848342a5f4a2749d77b131d3349086ba176b6aced16f01c`, `bash -n` PASS. It requires fresh external `K00_EXEC_AUTHORITY`; binds both historical and SID installed apps to their witnessed containers; requires the total harness set to be exactly one and historical; derives its PID from that read; repeats the full process read immediately before mutation and requires the same single historical path and same PID; performs exactly one native `devicectl device process terminate --pid`; then requires total harness 0 and proves both installed apps remain present in the same containers. Verb census: terminate 1; launch/install/uninstall/xcodebuild/k00-driver-batch 0. Execution remains CLOSED until a fresh founder authority payload is supplied in the governed Mac Studio Terminal act. After PASS, PRELIGHT-05 must run again and independently earn CLEAN before BATCH-05.

### 10.52 `ENTRY-05-HISTORICAL-HARNESS-CLEAR-02` STOP · target absent before mutation; authority spent; PRELIGHT-05 may retry read-only (2026-09-16)

The bounded post-install clearance act was invoked after PRELIGHT-05 had refused on historical `.vpio02`. Both installed apps and their witnessed containers passed the act's PRE identity reads. The subsequent process read contained **zero** `VoiceKernelHarness` entries, so the exact historical-target grep refused before PID capture, before the just-in-time read, and before the sole `devicectl device process terminate` verb. No device mutation occurred. The authority is spent because the governed pin was invoked and refused. This is the honest empty-target case: the historical harness disappeared naturally between PRELIGHT-05 and CLEAR-02; no termination is manufactured. Evidence is sealed under `driver-ledger/VPIO-02-SID-ENTRY-05-HISTORICAL-CLEAR-02-STOP-20260916T124711Z/`. BATCH-05 remains uninvoked. Next = fresh read-only PRELIGHT-05; only a new CLEAN marker can open the ≤300 s batch window.


### 10.53 `SID ENTRY-BATCH-05` COMPLETE N=30 → **ENTRY-UNPERTURBED**; evidence durable; source population becomes the next unopened gate (2026-09-16)

**Execution and custody.** A fresh PRELIGHT-05 at `20260916T125122Z` returned CLEAN on instrument `9ed72a38c…`, with both installed containers bound and `VoiceKernelHarness = 0`. BATCH-05 then ran exactly one fresh N=30 population at `20260916T125437Z` under the accepted one-shot pin `e00529be…`; every per-sample adjacent JIT read reported harness 0; all 30 samples completed; terminal rc = 0. The batch seal re-verifies after transfer. Raw preflight and population are durable at `driver-ledger/VPIO-02-SID-ENTRY-preflight-20260916T125122Z/` and `driver-ledger/VPIO-02-SID-ENTRY-20260916T125437Z/`. The adjudication wrapper is `driver-ledger/VPIO-02-SID-ENTRY-05-ADJUDICATION-20260916T125437Z/`; authority contents are NOT carried, only lines/bytes/SHA metadata.

**Predeclared [G] law applied, unchanged.** Frozen classifier = **30/30 `gen-1 listen`**, 0 SUBJECT-MISMATCH, 0 infrastructure rows; `listeningMs` range 315…400 ms, therefore 0 breaches of the 1 500 ms ceiling. Observer-liveness verifier self-test = 9/9 and reads **30/30 LIVE**; per-journal frames-present minimum 15, maximum 18, resets total 0. Against historical F-W1 29/30, the fixed-margin one-sided deterioration Fisher test for candidate 30/30 gives **p = 1.0** (not significant). The ruled class requires takes ≥27/30 ∧ Fisher n.s. ∧ 0 ceiling breaches ∧ all rows LIVE; every clause is satisfied. **Adjudication: ENTRY-UNPERTURBED.**

**Corpus maintenance only.** Admitting the 30 new journals moves tracked totals 604→634, SID custody 45→75, SID gen-1 rows 44→74, and headered custody populations 19→20. The source-gate count pins were updated only for those arithmetic facts; the constitutional exclusions are unchanged: ENTRY journals remain outside K00-05/06 output interpretation and outside SOURCE-ID attribution replay. Voice source gate = **89/89 PASS** after staging the evidence.

**Standing.** ENTRY is now satisfied for the SID subject. This class does not itself authorize source population, KERNEL-00 acceptance, BRIDGE-01, migration or production. Under the accepted design sequence, the next gate is the separately governed SID source-discrimination population; only after that evidence is adjudicated can KERNEL-00 acceptance be considered.


### 10.54 SOURCE-SB-01 successor prepared after ENTRY-UNPERTURBED: read-only preflight OPEN under the founder's continue-to-deployment direction; N=10 population pin accepted as shape, execution conditional and separately authorized (2026-09-16)

ENTRY-05 is durably adjudicated `ENTRY-UNPERTURBED` at `26116d4e4`, satisfying the prerequisite the accepted source design imposed before S-b could become eligible. The accepted source design is unchanged: **S-b only, N=10**; S-a remains NOT OPEN; gated 997 Hz fixture (250 ms on/off, 2 Hz), seven-bin estimator, source reader law from design §10.A/§10.B as accepted and SOURCE-ID-02A repaired; population flags exactly `--act output --vp on --mode L --hold 15 --cancel-at 1000 --settle 2 --subject vpio-02-sid --stimulus sid-nearend-gated`. No source/organism/driver/reader threshold changes are made here.

Two successor pins are returned. `SID_SOURCE-SB-01_PREFLIGHT_PIN_DRAFT_2026-09-16.sh` = 56 lines, SHA-256 `f675d5d76851f94b35fecb1151da12425c3595f8b2d10a926e544dd933b724e2`, read-only: requires `26116d4e4` as an ancestor of the fetched canonical branch and reads its exact adjudication as `RESULT ENTRY-UNPERTURBED`; detached worktree at instrument `9ed72a38c…`; source-reader self-test 24/24; fixture SHA `30d51cf4…`; `/usr/bin/afplay` SHA `88f3b577…`; exactly one default `Mac Studio Speakers` / built-in output; volume 69; unmuted; SID bundle/container bound; device process table harness 0; then sealed `PREFLIGHT-CLEAN` + atomic pointer. It contains no batch invocation and no install/uninstall/process-terminate verb. `SID_SOURCE-SB-01_POPULATION_PIN_DRAFT_2026-09-16.sh` = 32 lines, SHA-256 `b43e370968221d0d5a8b930edf0e1290871ac5fa87d24eb75407b9acaf1ac14e`: one-shot act marker before downstream consumption; requires the exact preflight worktree/seal/CLEAN marker aged 0…300 s; exactly one N=10 batch invocation with the accepted flags; requires exactly one new S-b ledger, 10 sample journals, both output/source ledgers; authority metadata only, never contents; independent population seal.

**Authority boundary.** The founder's standing direction to continue toward deployment is read as opening the read-only S-b PRELIGHT next, not as silently spending the N=10 population. Population execution remains conditional on that preflight returning CLEAN and on a fresh bounded `K00_EXEC_AUTHORITY`. Any population STOP or incomplete run is carried as produced; no top-up/rerun inherits the authority. Source reading follows the population under the already-accepted source-reader law. KERNEL-00 acceptance, BRIDGE-01, migration and production remain downstream. Voice source gate with the new successor obligation = **90/90 PASS**.


### 10.55 SOURCE-SB-01 forward custody repair: historical preparation retained; output-path normalization defect closed; new S-b successor pins accepted as shape (2026-09-16)

Forward review of the already-durable SOURCE-SB-01 preparation at `306654d6a` exposed one custody mismatch before any source population was spent. The batch instrument `9ed72a38c…` had been hardened fail-closed only for `vpio-02-sid + entry`; `vpio-02-sid + output + sid-nearend-gated` still entered the historical global `harness_present → testTerminateOnly → recheck` path. Because `testTerminateOnly` targets only the declared subject bundle while the predicate sees any `VoiceKernelHarness`, an asynchronously prewarmed foreign harness could either survive that normalization attempt or cause source attribution to begin from state changed by the instrument. With DAS prewarm already witnessed, that is a real source-custody defect, not a hypothetical style issue. The historical `306654d6a` pins remain preserved but are superseded as execution candidates before use.

Code-only repair `17c4ee524` (source commit `f67575797`) changes only `scripts/witness/k00-driver-batch.sh` and its source gate. ENTRY behavior is unchanged. For the exact S-b tuple (`subject=vpio-02-sid · act=output · stimulus=sid-nearend-gated`), the historical terminate-only normalization branch is bypassed. Each sample now requires two full process-set reads: **PRE-PLAY** after daemon snapshot and before `afplay`, then **JIT** after the ruled 1 s stimulus settle and immediately before the phone driver. Either unreadable/nonzero read preserves its process evidence and STOPs before phone launch; PRE-PLAY starts no stimulus, while a JIT refusal stops the already-started Mac stimulus before exit. Neither path calls `testTerminateOnly`, process terminate/signal, install, or phone launch. Fixture, player, output act, source reader, thresholds, N, cancel-at and settle semantics are untouched. Voice gate = **90/90 PASS** at this code-only state.

New successor pins landed at `422485c34` (source commit `ee67c9ba5`). `SID_SOURCE-PREFLIGHT-01_PIN_DRAFT_2026-09-16.sh` = 78 lines, SHA-256 `46f97f5db33c38eab351c4cfaa70f224a55339462d9b736568b7531c4e7fbf29`: read-only, detached at repaired instrument `f67575797…`; proves adjudicated ENTRY commit `26116d4e4` is an ancestor; source-reader self-test 24/24; both installed bundles/containers; harness 0; gated fixture SHA/format; exactly one default Mac Studio Speakers built-in output; volume 69; unmuted; `/usr/bin/afplay` SHA; sealed CLEAN + atomic pointer. `SID_SOURCE-BATCH-01_PIN_DRAFT_2026-09-16.sh` = 48 lines, SHA-256 `4430a8d093dad617e0657dcfb2a7cc4adbf2b7e64aa7540f358135af9c0391fa`: one-shot marker before authority gate; consumes only that exact preflight worktree/seal within 0…300 s; exactly one N=10 invocation with the accepted S-b flags; records journal/output/source-ledger counts rather than converting an infrastructure outcome into a pin refusal; requires exactly one new population directory and seals it. Gate with these successor obligations = **91/91 PASS**.

**Standing.** ENTRY remains `ENTRY-UNPERTURBED`. S-b remains the same accepted source experiment; S-a remains closed. Historical SOURCE-SB-01 pins from `306654d6a` are superseded pre-execution by the fail-closed custody repair; nothing physiological was run or reinterpreted. The new read-only source preflight is the next governed act under the founder's continue-to-deployment direction. The N=10 population still requires a fresh bounded authority after CLEAN. KERNEL-00 acceptance, BRIDGE-01, migration and production remain downstream of the returned S-b evidence and source-reader adjudication.

### 10.56 SOURCE-PREFLIGHT-01 REFUSED on three prewarmed historical harnesses; BATCH-01 marker spent by outer-wrapper fall-through; no source population occurred; SOURCE-02 successors + separate harness-clear prepared (2026-09-16)

`SID-SOURCE-PREFLIGHT-01` at `20260916T133822Z` verified ENTRY ancestry, the source reader and both installed subject containers, then observed exactly three live `VoiceKernelHarness` processes: historical `.vpio02` container `E3B88028-…`, `.vpio01` `6A2E406B-…`, and `.k00` `0B07D423-…`. It therefore refused before source stimulus custody, before `PREFLIGHT-CLEAN`, and before a preflight seal. Evidence is sealed under `driver-ledger/VPIO-02-SID-SOURCE-PREFLIGHT-01-REFUSED-20260916T133822Z/`.

The outer Mac-terminal wrapper supplied by this session omitted fail-fast control after the preflight return check. As a result the subsequent `SID-SOURCE-BATCH-01` pin was invoked even though the pointer still read `INCOMPLETE 20260916T133822Z`. Its one-shot marker was created, then the pin refused on pointer/worktree shape before any population directory, stimulus, driver launch, journal or source row existed. BATCH-01 authority is spent by invocation; physiology is untouched. This is an orchestration-wrapper defect, not a source result.

Fresh successors preserve the exact guarded S-b experiment and change only act identity/path custody: `SID_SOURCE-PREFLIGHT-02_PIN_DRAFT_2026-09-16.sh` = 78 lines, SHA-256 `9a335d34ce04ea9167478979a444201b890deb769fcbbe260159af13c2c1604e`; `SID_SOURCE-BATCH-02_PIN_DRAFT_2026-09-16.sh` = 48 lines, SHA-256 `f2648c839a1464213fa81d4356f1c71c128b726ac7f94e5f3cde761b8b2f1179`. Both remain bound to guarded instrument `f67575797353bf7e97ffb319a45357997c8d564d`; N=10, output act, VP on, Mode L, hold 15, gated 997 Hz, cancel-at 1000, settle 2, source-reader law and ≤300 s freshness are unchanged.

Because the device-state finding is three live historical harnesses, a separate process-state act is prepared rather than weakening source preflight. `SID_SOURCE-HARNESS-CLEAR-01_PIN_DRAFT_2026-09-16.sh` = 79 lines, SHA-256 `298deba242db25775d8424a82fca9b6b98cf6e133cc81d42856178bd27a45c84`. It binds the four installed containers; requires the exact live harness set `.k00 + .vpio01 + .vpio02` and no SID harness; derives PIDs from device JSON; re-reads the full expected set immediately before each native process termination; transitions only 3→2→1→0; any drift STOPs before the next mutation; then re-proves all four installed apps/containers unchanged. It has exactly three native `device process terminate` verbs and no install/uninstall/XCTest/driver/batch path. Fresh founder authority is required for this clearance. Only after harness 0 may PRELIGHT-02 run; only after CLEAN may BATCH-02 receive fresh population authority. Voice source gate = **93/93 PASS**. KERNEL-00 acceptance, BRIDGE-01, migration and production remain downstream.

### 10.57 SOURCE-HARNESS-CLEAR-01 PASS; SOURCE-PREFLIGHT-02 then refused three times only on Mac output-device custody; BATCH-02 remains uninvoked (2026-09-16)

`SID-SOURCE-HARNESS-CLEAR-01` executed directly from Mac Studio Terminal at `20260916T134803Z`. The precheck found exactly the ruled historical set `.k00 + .vpio01 + .vpio02`; the act bound all four installed containers, derived transient PIDs from device JSON, re-read the full remaining set before each termination, and completed exact 3→2→1→0. Final line: `PASS ... sid-preserved 85948DBD-... harness-after 0`. No SID harness was targeted and all four installed apps/containers were re-proved after the act.

Fresh `SID-SOURCE-PREFLIGHT-02` attempts at `13:48:06Z`, `13:48:13Z`, and `13:48:23Z` then each passed ENTRY ancestry, source-reader selftest 24/24, installed SID/historical bindings, total `VoiceKernelHarness = 0`, gated-fixture SHA and exact WAV geometry. Each refused at the next read-only Mac custody gate: the one default output was `B06Ultra · coreaudio_device_type_bluetooth · 44100`, not the ruled `Mac Studio Speakers · coreaudio_device_type_builtin`. Therefore no `PREFLIGHT-CLEAN` or preflight seal was written; volume and afplay identity checks were not reached. Contemporaneous read-only diagnosis after the attempts: output volume `13`, muted `false`.

All three refusal attempts are sealed together at `driver-ledger/VPIO-02-SID-SOURCE-PREFLIGHT-02-REFUSED-OUTPUT-20260916T134806Z/`. `SID-SOURCE-BATCH-02` remains completely uninvoked: `/private/tmp/sid-source-batch-02-invoked.txt` absent, no batch terminal log, no source-population directory, stimulus 0, phone sample 0, source row 0. This is a Mac output-state refusal only, not a source result. The next lawful move is external Mac setup to the already-ruled output state (Mac Studio Speakers, volume 69, unmuted), followed by a fresh PRELIGHT-02. The guarded N=10 population law is unchanged. KERNEL-00 acceptance, BRIDGE-01, migration and production remain downstream.

### 10.58 SOURCE-BATCH-02 PARTIAL STOP/SPENT at sample 6 PRE-PLAY; five valid rows preserved; fresh SOURCE-03 successor keeps full N=10 law (2026-09-16)
SOURCE-PREFLIGHT-02 finally earned CLEAN at `20260916T140110Z` on guarded instrument `f67575797353bf7e97ffb319a45357997c8d564d`: ENTRY ancestry satisfied, source reader selftest 24/24, SID/historical installed identities bound, harness 0, gated 997 Hz fixture/player custody valid, and Mac Studio Speakers built-in 48 kHz at volume 69/unmuted. SOURCE-BATCH-02 was invoked once at `20260916T140114Z`. Samples 1–5 each passed PRE-PLAY harness 0 and post-settle JIT harness 0, stimulus custody VALID, driver success, and all three readers. Their five SOURCE-ROW verdicts are all VALID + `CHARACTERIZE-SRC`; they remain descriptive only because the population is incomplete. At sample 6 PRE-PLAY the exact historical set `.k00 + .vpio01 + .vpio02` had reappeared; the fail-closed guard STOPped before afplay, before phone launch, and without terminate. Samples 7–10 were never attempted. BATCH-02 is PARTIAL/STOP/SPENT; no top-up and no rerun under its authority. Evidence is sealed under `driver-ledger/VPIO-02-SID-SOURCE-PARTIAL-02-20260916T140115Z/`.

The recurrence matches the already-witnessed iOS DAS app-resume prewarm mechanism; prior device evidence records `com.apple.appResume.prewarm` as a repeating task with interval 900 seconds. That timing is an orchestration opportunity, not a validity assumption: the next population still keeps PRE-PLAY + JIT total-harness-zero guards on every sample and must STOP on any early recurrence. Fresh successors change only act identity: `SID_SOURCE-HARNESS-CLEAR-02_PIN_DRAFT_2026-09-16.sh` = 79 lines, SHA-256 `5a7d26266d9c349f4b0d8e785a36288cf68efa81ba6b7a4a82795646317a9767`; `SID_SOURCE-PREFLIGHT-03_PIN_DRAFT_2026-09-16.sh` = 78 lines, SHA-256 `f806e92ae877e08f076b3ec88e8c1087e2ac6f77d6c78c0d60058c325f6cefa6`; `SID_SOURCE-BATCH-03_PIN_DRAFT_2026-09-16.sh` = 48 lines, SHA-256 `c2c33b557971e46efd7ea44188eaf2b6b586869e918b360861aec22e63446ba4`. SOURCE-03 remains a fresh full N=10 under the same guarded instrument, same fixture, same output act and reader law; never a five-row top-up. The post-prewarm execution wrapper is itself pinned as `SID_SOURCE-03_AFTER-PREWARM_RUN_DRAFT_2026-09-16.sh` = 82 lines, SHA-256 `0ed664eb0f06c722a614c10bfc1259425a2e5e21800ae293dbd73adbabd520c6`; it observes live `dasd` until the next `appResume.prewarm` reschedule, requires the exact historical-three set, and delegates only to external-authority CLEAR-02 → read-only PRELIGHT-03 → external-authority BATCH-03. It contains no direct terminate/install/batch invocation and no embedded founder authority. Corpus partition is now explicit: SID custody = 80 journals, split ENTRY 75 + SOURCE partial 5. Voice source gate = **96/96 PASS**. Fresh founder authority is required for CLEAR-02 and separately for BATCH-03; PRELIGHT-03 is read-only between them. KERNEL-00 acceptance, BRIDGE-01, migration and production remain downstream.

### 10.59 SOURCE-03 post-prewarm observer retired before authority spend; live device state reached harness 0 naturally; direct zero-gated SOURCE-03 wrapper pinned (2026-09-16)
The pinned post-prewarm orchestration wrapper started read-only at `20260916T143707Z` but `idevicesyslog` never attached to the iPhone, remaining at `Waiting for device with UDID ... to become available`. No clearance, preflight or batch marker was created. A later independent `devicectl` process read established current `VoiceKernelHarness = 0`; therefore the stale observer was terminated on the Mac before any governed Voice act. A new direct wrapper `SID_SOURCE-03_NOW_RUN_DRAFT_2026-09-16.sh` = 53 lines, SHA-256 `6ba680b7c9829da65d550f5ae1b76483f66462021b65d9448bf68532117dd3f3`, is fail-fast and contains no clearance or DAS observer. It requires Mac Studio Speakers/built-in + volume 69/unmuted, then independently requires total harness 0, runs exactly one read-only PRELIGHT-03, requires its CLEAN/seal, and only then delegates once to BATCH-03 using the separately supplied authority file. It contains no founder authority text, no process-terminate verb, no cleanup and no normalization. Voice source gate = **97/97 PASS**. CLEAR-02 remains unspent and unnecessary while current zero persists; BATCH-03 marker remains absent until direct Mac execution.
