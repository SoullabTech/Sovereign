# KERNEL-00 · VPIO-02 · K00-05 / K00-06 WITNESS — K00-0506 N=10 (2026-09-14) — TEN INFRASTRUCTURE ROWS · CHARACTERIZE ONLY · K00-05 INCOMPLETE · K00-06 INCOMPLETE

**Status:** EXECUTED (founder, Mac; ruling in qualification plan §10) · RECEIVED and INDEPENDENTLY READ HERE · **no K00-05 or K00-06 evidentiary row exists** · classified under the ruled `>2 infrastructure → characterize only` branch · **ADJUDICATION OWED (founder)** · nothing downstream opened.

## 1. Custody

Founder branch `feature/k00-0506-device-witness-evidence-20260914` at `36d0d683dd8d852dd92e70aa9b7032e8f830950e` → cherry-picked `-x` as `6960e13e7` (67 files). Hashes recomputed here = the founder's: `ledger.md 933d1b71…` · `batch.log a717a15d…` · `build-for-testing.log 0053c1ee…` · `sample-timing.tsv 02d452a6…`. Directories: `K00-0506-20260914T235008Z/` (ledger · batch.log · build-for-testing.log · xcodegen.log · 10 sample logs · 7 terminate-only logs · 40 daemon files · sample-timing.tsv; **no `journals/` content, no `output-ledger.md`** — nothing was pulled, so the reader never ran) · `K00-0506-preflight-20260914T234929Z/` and `…235001Z/` (both re-read here: container `E3B88028` lines 1 · harness processes 0 — two lawful read-only acts) · `K00-0506-20260914T235012Z.EMPTY-FOOTPRINT.txt` (the refused duplicate's empty directory, which Git cannot carry; recorded as a marker file, nothing inserted into the runtime directory). Raw Xcode CR/whitespace preserved.

Header custody note (founder-flagged, confirmed here): the batch header prints `last reinstall: 20260913T145025Z` — it reads the worktree's `driver-ledger/.last-reinstall`, which the VPIO installs (run from other worktrees) never updated. It is not device identity; both preflights resolved `.vpio02` to the ruled container.

## 2. What the batch did (batch.log · ledger · sample logs, read here)

Instrument identity at run: worktree `8b111709b`, `xcodegen` + signed `build-for-testing` for the device (`build-for-testing.log`), xctestrun in the worktree's `.derived`. Ten `testOutputSample` invocations, 23:50:15Z → 00:06:46Z, exit 0, `batch complete`.

| samples | row | runner reading | wall |
|---|---|---|---|
| 1–2 | DRIVER/INFRASTRUCTURE FAILURE | `Timed out while enabling automation mode` (rc 65) — the Stage-B / attempt-3 shape at batch start; harness never launched | ≈ 69 s each |
| 3–10 | DRIVER/INFRASTRUCTURE FAILURE | `'Play 3 s tone' not found after Enter` (rc 65) — see §3 | 91 · 50–58 s |

Samples 4–10 each began with `harness process present — attempting terminate-only via driver` → `testTerminateOnly` **passed** ×7 (1.09–1.15 s): the harness left running by the previous sample's aborted test was terminated by the instrument's ordinary per-sample precondition, as the ruling permits after sample 1. Sample 1's precondition was clean (the §10 pre-sample-1 concern did not occur). **After sample 10 the harness process was still alive** (`daemons/sample-10-after.json` carries one `VoiceKernelHarness` row; sample 10's failed test never reached `exportAndTerminate`) — see §6.

## 3. Samples 3–10 read line by line (sample 3 as the exemplar; 4–10 identical in shape)

`launchCold` → `Enter conversation` found (10 s wait) → VP toggle read `ON (default)` → `Enter conversation` found (5 s wait) → **`Tap "Enter conversation" Button` synthesized at t = 5.32 s** (`Wait for life.soullab.voicekernel.vpio02 to idle`) → `Waiting 5.0s for "Play 3 s tone" Button to exist` → five `existsNoRetry == 1` checks at ≈1 s intervals, each `Capturing element debug description` → not found → `Collecting debug information … Requesting snapshot of accessibility hierarchy for app with pid 66960` → `K00DriverTests.swift:238: error: … DRIVER/INFRASTRUCTURE FAILURE: 'Play 3 s tone' not found after Enter` → test failed (11.4 s). No `K00-OUTPUT:` marker in any sample log (none was reachable: the failure precedes every marker). The hierarchy snapshot the runner requested is **not in the log** (it goes to the `.xcresult`, which the batch has run with `-collect-test-diagnostics never` since C-D3), so **which buttons existed after Enter is not in evidence**.

**Organism:** Enter was tapped eight times; the organism entered conversation eight times and was terminated by the driver each time without Export, so no journal was written and **nothing physiological is claimed or claimable** — not entry, not output, not duplex. The eight sessions are not samples of anything.

## 4. Classification (ruled law, qualification plan §10)

Valid K00-05 cancel rows 0 · valid K00-05 completion rows 0 · valid K00-06 rows 0 · infrastructure rows **10** (> 2) → **CHARACTERIZE ONLY**. **K00-05 INCOMPLETE · K00-06 (built-in) INCOMPLETE.** Neither PASS nor FAIL; no replacement sample; the one K00-0506 authority is **SPENT** (ten declared invocations executed against the device). VPIO-02's F-W1 standing (K00-04 PASS) is untouched: this batch produced no journal and reads nothing about the organism.

## 5. Instrument defect candidate — C-D20 (named, NOT written, NOT authorized)

The historical driver only ever queried buttons in the first sections of the harness (`Enter conversation` · the VP toggles · `Leave` · `Export journal`) and every one existed. `testOutputSample` is the first query for a button in the **fourth** section of a SwiftUI `NavigationStack { List { stateSection · … · routeSection · outputSection · … } }` (`HarnessView.swift:9–16, 97`). **Inference, not read from a hierarchy:** on the phone the Output section sits below the fold, and a lazy `List` does not instantiate offscreen rows, so the button is absent from the accessibility hierarchy XCUITest queries — `exists` false, exactly as logged. Alternative (label mismatch) is excluded by source: the harness button is `Button("Play 3 s tone")` verbatim. Whether the row is absent or merely not hittable cannot be settled from this evidence.

Correction shape for a future ruling (driver-only; new SHA; driver-only compile; new witness ruling): before waiting for `Play 3 s tone`, scroll the harness `List` to reveal it (`swipeUp` until the button exists, bounded), and **on any not-found failure write the app's `debugDescription` hierarchy into the runner log by `NSLog`** so the next failure names what existed instead of leaving it in a discarded `.xcresult`. Both touch only `K00DriverTests.swift`; the organism, batch, readers and gate law are unaffected. The first two rows (automation-mode timeout at batch start) are the known device-side shape and are not this defect.

## 6. Device state after the batch (for the next preflight)

One `VoiceKernelHarness` process was alive after sample 10 (never terminated: the failed test skipped `exportAndTerminate`). A future §10-shaped preflight would read `harness processes: 1` and STOP by rule. Ending that process is a corrective device act and is **not authorized by this record**; it needs its own founder act (the instrument's `testTerminateOnly` is the ruled terminate-only path once authorized). The installed `.vpio02` artifact is unchanged; no reinstall occurred; `.vpio01` and K00/R1 untouched.

## 7. Returned for adjudication

1. Acceptance of the classification: ten infrastructure rows → characterize only; K00-05 INCOMPLETE; K00-06 INCOMPLETE; nothing about the organism read.
2. C-D20 disposition: whether a driver-only correction (scroll-to-reveal + hierarchy-on-failure) is authorized as a new instrument SHA, with the usual driver-only compile before any device act.
3. Disposal of the lingering harness process before any next preflight.
4. Whether a fresh N = 10 K00-0506 batch is authorized after 1–3 (a new authority string; the spent one does not carry).

**Standing:** K00-04 PASS on VPIO-02 (unchanged) · K00-05 INCOMPLETE · K00-06 built-in INCOMPLETE · K00-0506 authority SPENT · instrument `8b111709b` (defect candidate C-D20 named) · organism `ac12dedf4` FROZEN · `.vpio02` installed, untouched · `.vpio01` FROZEN · K00/R1 UNTOUCHED · route/interruption/reset/endurance NOT AUTHORIZED · KERNEL-00 NOT ACCEPTED.
