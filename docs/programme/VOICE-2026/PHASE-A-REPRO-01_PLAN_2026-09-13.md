# PHASE-A-REPRO-01 — reproduce the Phase-A source condition · PLAN ONLY

Lane: `VOICE-2026` · `KERNEL-00` · `DRIVER-01` lineage. Authorized by founder ruling 2026-09-13 (`DRIVER-01_STAGE-C_2026-09-13.md` §10), **plan only** — nothing here is built, installed, or sampled until the plan is accepted.

```text
PHASE-A-REPRO-01
source SHA        4596b9bdb
purpose           reproduce the Phase-A source condition (14-step startup trace WITH the pre-VP input.outputFormat(forBus: 0) read)
historical claim  NONE — this is not the run-5 artifact and never becomes "Stage C"
stratum           AUTOMATED-COLD-LAUNCH · Mode L · N=30 · VP ON (same driver, same procedure as Stage A/B)
```

## §1 What this experiment is and is not

It reproduces the *source condition* under which every manual VP-ON session listened (run 5 + repro, 5/5). It cannot reproduce the *artifact*: that binary is gone (historical Stage C, CLOSED · NOT EXECUTABLE). A rebuilt dylib whose Mach-O UUID equals `11A057AA-4A3C-3CAE-8C28-E29792489459` is **useful evidence that the same source and toolchain produced the same linked image; it is not custody identity** (founder). The question is therefore: *on the automated stratum, on a cleaned container, does the Phase-A source condition show a different gen-1 take rate from the P5-B0 source condition measured under the same cleaned condition?* Any answer is read as probabilistic influence at most, never mechanism.

## §2 Environment pin — MAC-COMPILE-06 as closely as it can be pinned

The MAC-COMPILE-06 record states "toolchain as MAC-COMPILE-03/04/05"; the toolchain is pinned in MAC-COMPILE-02 and the signed recipe in MAC-COMPILE-03/07. Every row is read at build time and written into the build record; a mismatch is recorded, not hidden, and any mismatch is a founder STOP/GO before install.

| pin | required value | how read |
|---|---|---|
| macOS | 15.7.8 (24G806) | `sw_vers` |
| Xcode | 26.3 (17C529) at `/Applications/Xcode.app` | `xcodebuild -version` · `xcode-select -p` |
| Swift | 6.2.4 (swiftlang-6.2.4.1.4 clang-1700.6.4.2) | `swift --version` |
| iOS SDK | iPhoneOS26.2.sdk | `xcodebuild -showsdks` |
| xcodegen | the installed version (not recorded in the compile records — read and record it now) | `xcodegen --version` |
| source | `4596b9bdb` exactly, clean tree | `git checkout 4596b9bdb` in a fresh worktree · `git status --porcelain` empty · `git rev-parse HEAD` |
| project | `ios/VoiceKernelHarness/project.yml` at that SHA (deployment target, `CODE_SIGN_STYLE: Automatic`, bundle `life.soullab.voicekernel.k00`) | generated by `xcodegen generate` from the worktree |
| configuration | Debug (the product path is `Build/Products/Debug-iphoneos`) | as the recipe below |
| team / identity | `ZVK2X646Z2` · `Apple Development: Kelly Nezat (N9DTF6434L)` | `codesign -dv` after the signed build |
| destination | `id=00008140-00163D9922E0801C` (Xcode id, never the devicectl id) | recipe |
| DerivedData | a **fresh, dedicated** DerivedData under the worktree, so the reproduction never overwrites the P5-B0 product at the shared path (the way MAC-COMPILE-07 overwrote Phase A) | `-derivedDataPath` |

Recipe (the MAC-COMPILE-02/03/07 sequence, from `ios/VoiceKernelHarness` in the worktree; `swift build && swift test` from `ios/VoiceKernel` first, as every compile record did):

```text
xcodegen generate
xcodebuild -project VoiceKernelHarness.xcodeproj -scheme VoiceKernelHarness -destination generic/platform=iOS CODE_SIGNING_ALLOWED=NO -derivedDataPath <fresh> build
xcodebuild -project VoiceKernelHarness.xcodeproj -scheme VoiceKernelHarness -destination 'id=00008140-00163D9922E0801C' DEVELOPMENT_TEAM=ZVK2X646Z2 CODE_SIGN_STYLE=Automatic -allowProvisioningUpdates -derivedDataPath <fresh> build
```

Expected test/gate counts at that SHA, for the record: `swift test` 30/30 · source gate 28/28 (MAC-COMPILE-06). The gate in the current tree pins P5-B0 and will not be run against the worktree; the worktree's own gate at `4596b9bdb` is what is run there.

## §3 Custody at build time — recorded NOW, before any install

Written to `docs/programme/VOICE-2026/driver-ledger/phase-a-repro-01/build-<stamp>.txt`:

1. every §2 pin as read;
2. `dwarfdump --uuid` of `VoiceKernelHarness.debug.dylib` (unsigned product, then signed product — MAC-COMPILE-04/07 showed signing does not move it);
3. **SHA-256 of the signed dylib**, and SHA-256 of a deterministic archive of the signed `.app` (`tar` with sorted entries and fixed mtimes, or per-file SHA-256 manifest of the bundle — the manifest form is preferred because it survives tar implementation differences);
4. `codesign -dv` identifier / team / authority;
5. the UUID gate verdict (§4).

The historical-custody problem is not repeated: the identity and the hashes exist before the first install, in the repo.

## §4 The UUID gate (founder, verbatim logic)

```text
if dylib UUID == 11A057AA-4A3C-3CAE-8C28-E29792489459
   → continue as PHASE-A-REPRO-01 → still a NEW subject
if dylib UUID differs
   → STOP before device installation → founder ruling again
```

Mechanically: `K00_EXPECT_UUID=11A057AA-4A3C-3CAE-8C28-E29792489459 scripts/witness/k00-reinstall.sh <ledger-dir> <fresh>/Build/Products/Debug-iphoneos/VoiceKernelHarness.app` — the existing refusal (exit 3, `.REFUSED.txt`, nothing installed) is the STOP. A match does not make the subject historical; the ledger stratum label is `PHASE-A-REPRO-01`, never `STAGE-C`.

## §5 Sequence — the matched post-cleanup control comes first

Housekeeping changes the app-data environment (a container of ~130 exports → 0). To avoid trading one confound for another (founder), the reproduction is compared only against a P5-B0 baseline measured under the same cleaned condition:

| step | act | label / artefact | boundary |
|---|---|---|---|
| 0 | housekeeping A–H complete (archive RECONCILED · probe · documented delete verb · delete `tmp/kernel00-*.jsonl` only · re-list → zero) | `container-archive/…` · `purge-<stamp>.txt` | environment change, recorded once |
| 1 | **P5B0-POSTCLEAN-CONTROL** — the installed `24a6fcfa1` (dylib `CC0D3604-…`, seq 5208 install, no reinstall) · N=30 · Mode L · VP ON · `--subject p5b0` | `driver-ledger/P5B0-POSTCLEAN-CONTROL-<stamp>/` | same install as Stage B; the only changed condition is the cleaned container |
| 2 | build PHASE-A-REPRO-01 per §2–§3; UUID gate §4 | `phase-a-repro-01/build-<stamp>.txt` | STOP on mismatch |
| 3 | reinstall (the one reinstall; the reinstall artefact carries the UUID read BEFORE install) | `reinstall-<stamp>.txt` | none inside the stage |
| 4 | **PHASE-A-REPRO-01** — N=30 · Mode L · VP ON · `--subject phase-a` | `driver-ledger/PHASE-A-REPRO-01-<stamp>/` | finish all 30 |
| 5 | verify here: hashes · cold · **14-step** trace with `input_format_before_vp` · VP ON · refusals = failures · resets · interruptions · max generation | record | |

Step 1 must finish and be read before step 2 is built (a build is cheap to defer and expensive to un-know). Container listing too unreliable to keep custody at any step → abort that step, never clean mid-stage.

## §6 Reading — predeclared, no thresholds manufactured

Laid side by side, never pooled: Stage A (14/29) · Stage B (15/28) · P5B0-POSTCLEAN-CONTROL (x/30) · PHASE-A-REPRO-01 (y/30) · MANUAL-COLD Phase A (5/5, different stratum).

| | reading |
|---|---|
| control ≈ A/B, repro markedly different from control | the Phase-A source condition *may influence* gen-1 take probability on this device/runtime — probabilistic, never causation, never mechanism |
| control ≈ A/B, repro ≈ control | the pre-VP read loses explanatory weight on the automated stratum; the manual 5/5 stays an unexplained stratum difference |
| control differs markedly from A/B | the cleaned container itself moved the picture; the repro is read only against the control, and the container effect is recorded as a new observation, not explained |
| mixed / infrastructure-heavy | characterize; no claim |

"Markedly" is left to the founder's reading at N=30 per stratum, as it was for Stage B's recovery tail. Nothing here authorizes E1–E4, B2, thresholds, STT/TTS, or any source change to the kernel or harness at either SHA.

## §7 What this plan does not authorize

Building anything (the plan is accepted first) · installing anything · calling any result "Stage C" or "historical" · pooling strata · a second reinstall inside either stage · repairing the organism · a rebuild whose UUID differs from `11A057AA-…` reaching the device.

## §8 ACCEPTED (founder, 2026-09-13) — with the housekeeping mechanism of the Stage-C record §11

Founder, verbatim: *"I read the complete plan at `9fcd532f3`. The experimental design is accepted: source condition `4596b9bdb` · explicitly new reproduction subject · no historical-artifact claim · MAC-COMPILE-06 environment pinned · fresh dedicated DerivedData · unsigned → signed build sequence · signed dylib SHA-256 plus per-file app manifest captured before installation · UUID must equal `11A057AA-4A3C-3CAE-8C28-E29792489459` · UUID mismatch → STOP before install · cleaned-container P5-B0 matched control comes first · Mode L, VP ON, fixed N=30 · reproduction N=30 only after the control is completed and read · strata remain separate; no causal/mechanism claim manufactured."*

Load-bearing sequencing, verbatim:

```text
PLAN                    ACCEPTED
HOUSEKEEPING            archive PASS
PURGE                   authorized as above · must verify tmp = 0
NEXT                    P5B0-POSTCLEAN-CONTROL · N=30
PHASE-A BUILD           STILL HELD until post-clean control finishes and is read
PHASE-A-REPRO-01 BUILD  only then
```

*"That preserves the plan's strongest move: we do not build the answer before we know what cleaning the container did to the control."* Nothing is built until the control is read; the §2–§4 build steps are executed only on a further founder act after that reading.

## §9 OPEN (founder, 2026-09-13) — build authorized through the existing gates; instrument `k00-phase-a-repro-build.sh`

Founder, verbatim: *"PHASE-A-REPRO-01 — OPEN. The precondition is satisfied. The post-clean control stayed on the same basic gen-1 axis as A and B (14/29 · 15/28 · 16/29) … Authorize the plan from the pinned build through its existing gates. The next act is the build. Before any install: 1. verify all environment pins 2. build unsigned 3. build signed 4. record dylib SHA-256 5. record app per-file SHA-256 manifest 6. read Mach-O UUID. If any pinned environment value materially differs, stop before installation and return the mismatch. If the UUID differs: STOP · NO INSTALL · founder ruling required. If the environment pins hold and the UUID matches, the existing plan may continue without another ruling: one reinstall → PHASE-A-REPRO-01 → N=30 → Mode L → VP ON → --subject phase-a → no reinstall inside the stage. It remains a new reproduction subject, even if the UUID matches."*

Instrument (build only; it contains no install verb — gate-pinned): `scripts/witness/k00-phase-a-repro-build.sh [<worktree>]` → `driver-ledger/phase-a-repro-01/build-<stamp>.txt` + `manifest-<stamp>.sha256` + both `xcodebuild` logs. Order enforced: pins (macOS 15.7.8/24G806 · Xcode 26.3/17C529 · Swift 6.2.4 swiftlang-6.2.4.1.4 clang-1700.6.4.2 · iphoneos26.2 · `xcode-select`; xcodegen recorded, not pinned) — any MISMATCH stops before building (exit 5) → detached worktree at exactly `4596b9bdb`, clean → `swift build` + `swift test` recorded → `xcodegen generate` → unsigned build → signed build (destination `id=00008140-…`, team `ZVK2X646Z2`), both into a fresh dedicated DerivedData under the worktree so the P5-B0 product at the shared path is never touched → custody before any install: unsigned and signed dylib UUID, signed dylib SHA-256, per-file SHA-256 manifest of the signed `.app`, codesign → UUID gate: match → prints the reinstall and batch commands; differs → STOP, exit 3, nothing installed. The reinstall then runs under `K00_EXPECT_UUID=11A057AA-…` (refuses before `install app` on any other reading), and the stage under `--subject phase-a` (14-step trace required; a 13-step journal is a SUBJECT-MISMATCH row).

## §10 First execution — two runs, the second refused by the instrument's own footprint (C-D10)

Run 1 (`build-20260913T173346Z`, from lane worktree `/private/tmp/voice-repro-96e3`): all seven pins MATCH (macOS 15.7.8/24G806 · Xcode 26.3/17C529 · `xcode-select` · Swift 6.2.4 swiftlang-6.2.4.1.4 clang-1700.6.4.2 · iphoneos26.2; xcodegen 2.46.0 recorded), worktree `/private/tmp/phase-a-repro-01` created at `4596b9bdb`, clean, `swift build` complete (5.23 s); the paste ends there — its completion is owed (see below). Run 2 (`build-20260913T173434Z`, 48 s later, from `/private/tmp/phase-a-repro-96e`): pins MATCH again, worktree found existing with 2 dirty entries (`M ios/VoiceKernelHarness/Harness/Info.plist` · `?? .derived-phase-a-repro-01/`) → `STOP — worktree not clean`, nothing built, nothing installed. Founder did not clean the worktree, correctly: its contents are run 1's evidence.

Reading: both dirty entries are run 1's own footprint. At `4596b9bdb` the harness `project.yml` declares `info: path: Harness/Info.plist`, so `xcodegen generate` rewrites that tracked file (MAC-COMPILE-06 ran the same step); and the first instrument placed DerivedData inside the worktree, unignored. So a second run on the same worktree could never pass the cleanliness check — an instrument design defect, **C-D10**, not a pin mismatch and not a custody event. The refusal was nonetheless right: two `xcodebuild`s into one DerivedData would have corrupted the artifact, and run 2 stopped before `xcodegen`, so run 1 was not interfered with.

C-D10 repair (instrument only): the worktree must be fresh (a pre-existing path is a STOP; the script never reuses or cleans a prior run); DerivedData lives outside the worktree at `<worktree>-derived`; the xcodegen footprint is recorded in the build record after `generate` instead of failing a later check; a `mkdir` lock refuses a concurrent run. Gate pins all three. Run 1's outcome decides the next act: if it completed through the UUID gate its record and product stand (run 2 touched nothing); if it did not, the repaired instrument runs once at a fresh path.

## §11 Run 1 COMPLETED — UUID GATE: STOP (`build-20260913T173346Z`, founder-run; evidence files RECEIVED — §11.1)

Run 1 ran to completion (the earlier paste had simply cut off at `swift build`). Verbatim, in order: all seven pins MATCH · xcodegen 2.46.0 recorded · worktree `/private/tmp/phase-a-repro-01` at `4596b9bdb`, 0 dirty entries · `swift build` complete (5.23 s) · `swift test` **Executed 30 tests, with 0 failures** · `xcodegen generate` OK · **unsigned `** BUILD SUCCEEDED **`**, unsigned dylib UUID `64EEC026-5F56-3706-BD2A-C5A6A20FC08B` · **signed `** BUILD SUCCEEDED **`** (destination `id=00008140-00163D9922E0801C`, team `ZVK2X646Z2`) · custody before any install: signed dylib UUID **`64EEC026-5F56-3706-BD2A-C5A6A20FC08B`** (unsigned = signed, as MAC-COMPILE-04/07 saw) · signed dylib SHA-256 `283dd24e9834875bac64537f1c414308666f0ca02b0ea3081fbd36b90f8d950f` · per-file manifest 7 files, manifest SHA-256 `09e286b671358420b39d8bd20c89aac32249bf85c474a760adb4080729d112b1` · codesign `life.soullab.voicekernel.k00` / `ZVK2X646Z2` · app path `/private/tmp/phase-a-repro-01/.derived-phase-a-repro-01/Build/Products/Debug-iphoneos/VoiceKernelHarness.app` → **`## STOP — UUID GATE: signed dylib reads '64EEC026-…' ≠ expected 11A057AA-… — NO INSTALL; founder ruling required 3`** · *nothing installed; the device is untouched.*

**What this establishes.** Same source SHA, same toolchain pins, same recipe, same signing identity: a *different* linked image identity. The ruling anticipated exactly this branch and reserved it for a founder ruling; no install, no stage, no relabelling has occurred. The custody the plan required is complete and in the record regardless of the UUID: SHA-256 of the signed dylib, the per-file bundle manifest, codesign, and the build environment as read.

**Candidate explanation — INFERENCE, not tested, not claimed.** A Mach-O UUID is derived from the linked content, and a Debug build's DWARF carries absolute source and build paths. Run 1 built from `/private/tmp/phase-a-repro-01/…` into a dedicated DerivedData; MAC-COMPILE-06 built from `/Users/soullab/MAIA-SOVEREIGN/ios/VoiceKernelHarness` into the shared DerivedData (`VoiceKernelHarness-afqjfcjktctgkjbejscivlpbxqtc`). If path embedding participates in the UUID, no build at a different path could ever have read `11A057AA-…`, and MAC-COMPILE-04's "identical across three passes" (same path, same DerivedData) is consistent with that without contradicting this result. A second unrecorded variable: MAC-COMPILE-06 never recorded its xcodegen version. Neither is asserted; both are named so the ruling is made with them visible.

**Also observed (C-D10 aftermath, no consequence to run 1):** run 2 (`173434Z`) refused before building; a later paste into the terminal executed prose as commands (harmless: `command not found`, `permission denied` on directories, one `xcodebuild` and one `xcodegen` invoked in a directory with no project — both erred out) and created a lane worktree `/private/tmp/voice-repro-a0d8` at `a0d805b78`; the repaired script did **not** run at `/private/tmp/phase-a-repro-01-run2`: the founder's later `&&` chain failed at `worktree add` (`fatal: '/private/tmp/voice-repro-a0d8' already exists`, created by the stray paste), so nothing after it executed. Founder-confirmed: `pgrep` shows no build process alive; `git -C /private/tmp/phase-a-repro-01 diff --stat` = `Harness/Info.plist | 20 +++…-` (19 insertions, 1 deletion) — the xcodegen regeneration, as read. Exactly one product exists: run 1's.

**Returned to the founder (options, not a recommendation to bypass the gate):**
1. **Rule `64EEC026-…` as PHASE-A-REPRO-01's binding.** The ruling already held that a UUID match would have been *useful evidence, not custody identity*; custody here rests on source SHA + pins + SHA-256 + manifest, all recorded. The stage would then proceed exactly as planned, with `K00_EXPECT_UUID=64EEC026-5F56-3706-BD2A-C5A6A20FC08B` on the reinstall and `--subject phase-a` on the batch; historical claim NONE, as before.
2. **Test the path hypothesis first** (one more build, no install): a worktree whose absolute path matches nothing historical cannot test it; the only path-matched build would be from `/Users/soullab/MAIA-SOVEREIGN/ios/VoiceKernelHarness` at `4596b9bdb`, which means checking that SHA out in the main working tree and a DerivedData the plan forbids reusing — a founder act with its own custody, not this instrument.
3. **Hold PHASE-A-REPRO-01.**

Standing: PINS PASS · BUILD PASS · CUSTODY RECORDED · **UUID GATE STOP** · NO INSTALL · DEVICE UNTOUCHED · founder ruling required.

### §11.1 Evidence received and verified here (`4eb4870c7` on `feature/k00-driver-ledger`, cherry-picked as `d72b5802d`)

Files under `docs/programme/VOICE-2026/driver-ledger/phase-a-repro-01/`: `build-20260913T173346Z.txt` (run 1; last line is the `## STOP — UUID GATE` refusal, exit 3) · `build-20260913T173434Z.txt` (run 2; last line `## STOP — worktree not clean`, nothing built) · `manifest-20260913T173346Z.sha256` (7 files: `Info.plist` · `PkgInfo` · `VoiceKernelHarness` · `VoiceKernelHarness.debug.dylib` · `_CodeSignature/CodeResources` · `__preview.dylib` · `embedded.mobileprovision`; SHA-256 of the manifest `09e286b6…` = the value in the run-1 record) · `xcodebuild-unsigned-20260913T173346Z.log` and `xcodebuild-signed-20260913T173346Z.log` (both contain `** BUILD SUCCEEDED **`). Read-only verification; no file edited. The custody chain the plan required (§3) is therefore in the repository, not only in the founder's paste.

Standing unchanged: **UUID GATE STOP · NO INSTALL · DEVICE UNTOUCHED · founder ruling required** on the three options above. Nothing runs on the device until that ruling; a ruling for option 1 is executed by exactly two founder commands (one reinstall with `K00_EXPECT_UUID=64EEC026-5F56-3706-BD2A-C5A6A20FC08B`, then one batch `PHASE-A-REPRO-01 30 --mode L --subject phase-a`), verified here afterwards.

## §12 FOUNDER RULING (2026-09-13) — run 1 ACCEPTED as REPRO SUBJECT R1 · install + N=30 AUTHORIZED · rebuild to chase the UUID PROHIBITED

Verbatim standing from the ruling:

```text
RUN 1 BUILD              ACCEPTED AS REPRO SUBJECT R1
RUN 2                     REFUSAL VALID · NO EFFECT ON R1

HISTORICAL UUID MATCH     FAILED
HISTORICAL EQUIVALENCE    NOT CLAIMED
REBUILD TO CHASE UUID     PROHIBITED

PHASE-A-REPRO-01-R1       AUTHORIZED TO INSTALL + RUN N=30
VOICEKERNEL REPAIR        FROZEN
B2                        HOLD
E1–E4                     HELD
MECHANISM CLAIM           NONE
```

**Reading (founder):** the Mach-O UUID is not a sufficient reproducibility identity for a rebuild of this source condition under what has actually been measured; earlier same-SHA UUID stability was evidence, not law. Repeatedly compiling `4596b9bdb` until one build happens to emit `11A057AA-…` would select an artifact after seeing the outcome — prohibited. The UUID mismatch means *not the historical binary*; it does not mean *not a valid reproduction of source SHA `4596b9bdb`*. The historical Phase-A artifact remains unavailable; the exact comparison remains CLOSED · NOT EXECUTABLE. PHASE-A-REPRO-01 continues as a **source-condition reproduction**.

**Repro subject R1 (custody):**

```text
UUID        64EEC026-5F56-3706-BD2A-C5A6A20FC08B
dylib SHA   283dd24e9834875bac64537f1c414308666f0ca02b0ea3081fbd36b90f8d950f
manifest    7 files · manifest SHA-256 09e286b671358420b39d8bd20c89aac32249bf85c474a760adb4080729d112b1
product     /private/tmp/phase-a-repro-01/.derived-phase-a-repro-01/Build/Products/Debug-iphoneos/VoiceKernelHarness.app
```

**Pre-install custody re-establishment (ruling):** UUID, dylib SHA-256 and the 7-file manifest must verify unchanged against the product immediately before install; any mismatch → STOP, no install. Implemented as instrument only in `scripts/witness/k00-reinstall.sh`: `K00_EXPECT_DYLIB_SHA` and `K00_EXPECT_MANIFEST` join `K00_EXPECT_UUID`; all three are evaluated BEFORE the install verb; the manifest check requires every listed file to hash identically **and** the product's file set to equal the manifest's (an unlisted extra file refuses); any mismatch writes `reinstall-<stamp>.REFUSED.txt` and exits 3 with nothing installed. Shim-exercised here (match → install verb reached; SHA mismatch · extra file · modified file → REFUSED, exit 3). Gate 35/35 (pins: both env names, `shasum -a 256 -c`, `FILE-SET MISMATCH` before `install app`).

**Execution (founder, two commands, no reinstall inside the 30):**

```bash
K00_EXPECT_UUID=64EEC026-5F56-3706-BD2A-C5A6A20FC08B \
K00_EXPECT_DYLIB_SHA=283dd24e9834875bac64537f1c414308666f0ca02b0ea3081fbd36b90f8d950f \
K00_EXPECT_MANIFEST="$PWD/docs/programme/VOICE-2026/driver-ledger/phase-a-repro-01/manifest-20260913T173346Z.sha256" \
scripts/witness/k00-reinstall.sh docs/programme/VOICE-2026/driver-ledger \
/private/tmp/phase-a-repro-01/.derived-phase-a-repro-01/Build/Products/Debug-iphoneos/VoiceKernelHarness.app
```

```bash
scripts/witness/k00-driver-batch.sh PHASE-A-REPRO-01 30 --mode L --subject phase-a
```

**Predeclared reading (unchanged from §6, now against the control):** P5B0-POSTCLEAN-CONTROL 16/29 gen-1 listen vs PHASE-A-REPRO-01-R1 N=30. Markedly different → the `4596b9bdb` source condition *may* affect startup physiology (never causation). About the same → the original 5/5 MANUAL-COLD Phase-A result loses substantial explanatory weight. Neither outcome makes the pre-VP format read a demonstrated mechanism. C-D9 fields (`listeningHeldAtExport` · `listeningLostLater`) travel with the stage as evidence only; classes unchanged. Strata never pooled.

## §13 Execution record (2026-09-13/14) — one pre-install batch NOT COUNTED · two R1 reinstalls · lawful batch `20260914T011819Z`

1. **Pre-install batch (sequencing error, NOT COUNTED).** `PHASE-A-REPRO-01-20260913T180117Z` was started with `--subject phase-a` before any R1 install; the device held the Stage-B P5-B0 reinstall (`reinstall-20260913T145025Z`). Classified beside the directory (`CLASSIFICATION.md`): every row SUBJECT-MISMATCH or infrastructure, no stratum, nothing deleted. Detected here from the absence of a pasted reinstall output; confirmed by the reinstall listing on the Mac (newest `145025Z`). The batch was no longer running when checked.
2. **R1 reinstall #1** `reinstall-20260914T011645Z.txt` — no harness process before; custody UUID · dylib SHA-256 · 7-file manifest all MATCH before `install app`; seq 5952; no harness process after.
3. **R1 reinstall #2** `reinstall-20260914T011754Z.txt` — 69 s later, same product, MATCH ×3 (founder paste); seq 5960. **Deviation from "one reinstall before":** two reinstalls of the identical R1 product preceded the batch, no batch between them. Founder attribution (verbatim): *"the second reinstall at `20260914T011754Z` was initiated by this ChatGPT session on the Mac Studio. The `011645Z` reinstall already existed. I then ran the second reinstall after the no-process check, not realizing the first lawful R1 reinstall had already occurred."* Both installs are R1 under the same custody; the samples are lawful R1 samples on the 011754Z install. The deviation is recorded, not absorbed.
4. **Lawful batch** `PHASE-A-REPRO-01-20260914T011819Z` (`subject=phase-a`, `last reinstall: 20260914T011754Z`) — 30/30, 0 infrastructure, received as `38e2bab9e` and VERIFIED here; result and predeclared reading in `DRIVER-01_PHASE-A-REPRO-01_2026-09-14.md`: **13/30 gen-1 listen → "about the same" as the P5-B0 band (14/29 · 15/28 · 16/29) → the 5/5 MANUAL-COLD Phase-A result loses substantial explanatory weight; the pre-VP read is not a demonstrated mechanism.**

