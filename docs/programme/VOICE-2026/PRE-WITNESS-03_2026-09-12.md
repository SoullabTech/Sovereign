# PRE-WITNESS-03 — configuration-change / route-recovery seam — 2026-09-12

**Opened by:** founder ruling on the sealed run-2 record (`KERNEL-00_WITNESS_2026-09-11_run2.md` §10), scope verbatim there. Architecture NOT reopened. Candidate B/C NOT chosen. H1 HELD out.
**Findings addressed:** K00-W3 (self-provoked, unbounded route-recovery rebuild loop — 584 generations / 6.2 min) · K00-W4 (the loop outlives Leave — engine rebuilt after `session_released`, `idle → recovering` orphan).
**Status:** IMPLEMENTED on `claude/voice-2026-census-01` · **NOT COMPILED** · MAC-COMPILE-04 owed on the exact SHA · device run 3 owed (VP-on first, then the VP-OFF control run) · the journal decides B/C.

## 1. What changed (kernel: `VoiceKernel.swift` only; harness: one control)

| Scope | Change | Where |
|---|---|---|
| **A · exit guard** | `handleConfigurationChange` now begins `guard inConversation else { journal stale_callback_dropped cause not_in_conversation (kind configurationChange, callbackGeneration, floor); return }`. No generation, no graph, floor untouched. The guard lexically precedes every other act in the handler (gate). | kernel |
| **B · bounded route recovery** | The handler no longer rebuilds. After journalling `engine_configuration_changed` (with the §3.4 format/age evidence, unchanged) it journals `recovery_requested cause configuration_change` and calls `requestRecovery(faultClass: "configuration_change")`. The existing RecoveryPolicy then governs: 3 per 60 s at 500/1000/2000 ms, then `degraded`. **`RecoveryPolicy.swift` is untouched** — no new budget; the class is a name under the ratified law. | kernel |
| **D · direct-rebuild source gate** | `rebuildGraph(cause: "route_recovery"` no longer exists anywhere; the handler body contains no `rebuildGraph(`; the gate asserts both, plus A's ordering, plus B's path. VOICE-06 call ceiling raised 4 → 5 for exactly this founder-authorized caller. | gate |
| **C · VP control** | New kernel command `setVoiceProcessing(_:)`: journals `command setVoiceProcessing {requested, inConversation}`; if in conversation → `command_refused cause setVoiceProcessing_in_conversation` and no change; else `snap.voiceProcessingEnabled = on` + `voice_processing_set {voiceProcessing}`. Default remains ON. Harness: one button in the floor card, projection of `s.voiceProcessingEnabled`, `.disabled(s.floor != .idle)`, calls `kernel.setVoiceProcessing(!snapshot.voiceProcessingEnabled)`; no local state (VOICE-07 gate). `graph_started` already journals `voiceProcessing` on every generation, so the control run is self-describing in the journal. | kernel + harness |
| evidence-only | `requestRecovery` no longer drops a request silently while one is pending: `recovery_request_coalesced {pending}` (K00-17 record coherence). Behaviour unchanged. | kernel |

Not touched: thresholds · `RecoveryPolicy` · `HealthSupervisor` · `AudioGraph` · `AudioSessionAuthority` · session route-change handling (it only records state; it never rebuilt) · interruption / media-reset recovery paths (they are not configuration-change handlers and already carry their own stamps) · H1.

## 2. Gates run here (no Swift toolchain in this container)

- `__tests__/voice-kernel-00-source-gates.test.ts` → **20 / 20** (16 prior + A, B, C, D).
- `swift test` gains `ConfigurationChangeSeamTests` ×2 (bounded budget for `configuration_change`; the W4 `idle → recovering` edge is an orphan) → expected **22** on the Mac.
- `check:no-supabase` clean.

## 3. What the next device run must show (founder's acceptance conditions, verbatim in the run-2 record §10)

```
EXIT       session_released → no later graph_started / graph_rebuilt → floor remains idle
           (journal: stale_callback_dropped cause not_in_conversation instead)
RECOVERY   configuration-change reactions cannot generate indefinitely
           → recovery_scheduled attempts 1,2,3 for class configuration_change → degraded budget_exhausted:configuration_change
VP OFF     one run with Voice processing: OFF pressed BEFORE Enter (journal shows voice_processing_set false, graph_started voiceProcessing false)
           if repeated engine_configuration_changed disappear → local evidence supports VP-induced reconfiguration
           if they continue → VP attribution is falsified
NO B/C DECISION until that control journal exists
```

Founder's caveat preserved: a VP-on run that now degrades after three configuration changes is bounded, not healthy — it is the evidence that an expected lifecycle event is being classified as a fault, which is what the B/C ruling needs.

## 4. Sequence

MAC-COMPILE-04 on the exact SHA (build · test 22 · gate 20 · xcodegen · unsigned · signed, CLI build of record, `DEVELOPMENT_TEAM=ZVK2X646Z2`) → founder accepts → install → **run 3a** VP ON: pre-enter checkpoint · Enter · export at ~15 s · Leave · export → **run 3b** VP OFF: relaunch · press the control · pre-enter checkpoint · Enter · export at ~15 s · Leave · export → `KERNEL-00_WITNESS_2026-09-12_run3.md` → founder rules on B/C.
