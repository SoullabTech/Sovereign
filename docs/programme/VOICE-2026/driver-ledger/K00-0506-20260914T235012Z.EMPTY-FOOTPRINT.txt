The runtime worktree contained docs/programme/VOICE-2026/driver-ledger/K00-0506-20260914T235012Z as an empty directory created by the refused duplicate invocation before CAL-F3 refused at the per-device lock. Git cannot represent an empty directory. No file existed inside it at return time; this note preserves that empty-footprint fact without inserting a file into the original runtime directory.
