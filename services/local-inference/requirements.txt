# maia-local-inference — Phase 1 sovereign inference adapter
# Bridges MAIA app → Ollama via the interface localInferenceClient.ts expects

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
httpx>=0.27.0
