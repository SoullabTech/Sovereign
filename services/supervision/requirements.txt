# MAIA Clinical Supervision - Python Dependencies
# All processing is LOCAL for HIPAA compliance

# Audio capture
sounddevice>=0.4.6
numpy>=1.24.0

# WebSocket communication
websockets>=12.0

# Transcription (faster-whisper is 4x faster than openai-whisper)
faster-whisper>=1.0.0

# Speaker diarization (runs locally)
pyannote.audio>=3.1.0
torch>=2.0.0
torchaudio>=2.0.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# Utilities
python-dotenv>=1.0.0
aiofiles>=23.0.0
