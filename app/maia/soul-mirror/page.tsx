'use client';

/**
 * Soul Mirror — first-touch entry mode into MAIA.
 *
 * This page renders the Soul Mirror threshold. No rail, no shell chrome,
 * no navigation — a calm, almost empty surface.
 *
 * See: docs/book-studio/SOUL_MIRROR_ROUTING.md
 */

import SoulMirror from '@/components/maia/SoulMirror';

export default function SoulMirrorPage() {
  return <SoulMirror />;
}
