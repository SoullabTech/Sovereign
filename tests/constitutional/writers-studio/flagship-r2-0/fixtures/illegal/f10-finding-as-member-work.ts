/** R2-0-D10 — MAIA's durable finding classified as member-authored Work text. Must be REFUSED. */
import type { DurableReadingOutput } from '@r2-0/contract';
export const f: DurableReadingOutput = { role: 'FINDING', inputClass: 'MEMBER_WORK_TEXT', observation: 'The far bank is where the chapter keeps returning.', doesNotEstablish: [], lens: 'recurrence' };
